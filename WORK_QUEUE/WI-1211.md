# WI-1211: Enforce Settlement Authority (Displayed Plays Only)

**ID**: WI-1211
**Goal**: Enforce settlement authority so only surfaced plays can settle, preventing false/duplicate settlement on rows never shown to users.

**Policy decision (locked)**:

- Default and active policy: **NO** unsurfaced settlement.
- If a card was not surfaced in `card_display_log`, it does **not** settle.
- Strict mode for this WI: remove `mlb-pitcher-k` display bypass (no unsurfaced exceptions).

**Scope**:

- `apps/worker/src/jobs/settle_pending_cards.js`
- `apps/worker/src/jobs/__tests__/settle_pending_cards.phase2.test.js`
- `apps/worker/src/jobs/__tests__/` (new targeted settlement-authority regression test file(s), if needed)

**Out of scope**:

- Changing display-log ingestion architecture
- Refactoring projection settlement jobs outside test-targeted fixes
- Reworking `/api/results` display filtering behavior
- Introducing new settlement authority exemptions or allowlists

**Acceptance**:

- Non-displayed rows do not settle.
- Displayed rows settle normally when final results exist and grading contract is valid.
- No unsurfaced bypass remains for `mlb-pitcher-k`.
- Settlement selection path in `settle_pending_cards.js` enforces display authority (`card_display_log`) before grading.
- Any inline unsurfaced `card_type` bypass in settlement authority SQL is removed.
- Regression tests prove `mlb-pitcher-k` cannot bypass display requirement under strict policy.
- Tests assert no duplicate settlement writes for the same logical market row in the same run path.
- Existing settlement tests continue to pass.

**Priority**: High

**Owner agent**: codex
**Time window**: 2026-04-29T00:00:00Z -> 2026-05-01T23:59:59Z
**Coordination flag**: needs-sync
**Depends on**: none (serialized touchpoint check required at claim/start if another active WI scopes `apps/worker/src/jobs/*.js`)

**Tests to run**:

- `npm --prefix apps/worker test -- src/jobs/__tests__/settle_pending_cards.phase2.test.js`
- `npm --prefix apps/worker test -- src/jobs/__tests__/settle_pending_cards.non-actionable.test.js`
- `npm --prefix apps/worker test -- src/jobs/__tests__/negative-path-settlement-live-truth.test.js`
- `npm --prefix apps/worker test -- src/jobs/__tests__/settle_pending_cards.settlement-authority.test.js`

**Manual validation**:

- Execute settlement tests with fixtures representing displayed and non-displayed rows, including unsurfaced `mlb-pitcher-k` rows.
- Confirm row-state transitions (`pending -> settled|error`) only occur for displayed rows under strict policy; unsurfaced rows remain pending.
- Verify logs/coverage counters show non-displayed final rows remain blocked.

**Guard for WI closeout**:

- Strict policy is enforced in code: no display log row means no settlement.
- `mlb-pitcher-k` unsurfaced bypass is removed.
- Regression tests fail on policy violation and pass on intended strict behavior.
- No unrelated worker job behavior changes beyond what tests require.
- No unrelated file diffs outside scope.
- If a new WI-specific test file is added, add its exact runnable command to **Tests to run** in this WI before closeout.

CLAIM: codex 2026-04-29T00:00:00Z

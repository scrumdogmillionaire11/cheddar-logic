# WI-1103: Worker Runner Decomposition — Extract Settlement Annotation + Recovery Helpers

**ID**: WI-1103
**Goal**: Reduce refactor risk in oversized worker runners by extracting stable helper modules for settlement annotation and recovery flow without changing betting outcomes.

**Scope**:
- `apps/worker/src/jobs/settle_pending_cards.js`
- `apps/worker/src/jobs/run_mlb_model.js`
- `apps/worker/src/jobs/helpers/settlement-annotation.js` (new)
- `apps/worker/src/jobs/helpers/recovery-flow.js` (new)
- `apps/worker/src/jobs/__tests__/settle_pending_cards.market-contract.test.js`
- `apps/worker/src/jobs/__tests__/settle_pending_cards.ordering-guard.test.js`
- `apps/worker/src/jobs/__tests__/run_mlb_model.test.js`

**Out of scope**:
- Any decision-policy threshold changes
- DB schema or migration changes
- Scheduler cadence changes
- NHL and player-props runner refactors

**Acceptance**:
- Settlement annotation logic is moved behind exported helper APIs with behavior parity.
- Recovery/retry path logic in MLB runner is moved behind exported helper APIs with behavior parity.
- Existing settlement and MLB runner contract tests pass with no fixture contract changes.
- No new `TODO`/`FIXME` markers introduced in scoped files.

**Owner agent**: unassigned
**Time window**: TBD
**Coordination flag**: solo

**Tests to run**:
- `npm --prefix apps/worker run test -- src/jobs/__tests__/settle_pending_cards.market-contract.test.js --runInBand`
- `npm --prefix apps/worker run test -- src/jobs/__tests__/settle_pending_cards.ordering-guard.test.js --runInBand`
- `npm --prefix apps/worker run test -- src/jobs/__tests__/run_mlb_model.test.js --runInBand`

**Manual validation**:
- Confirm helper extraction keeps output payload shape identical for representative settled cards.
- Confirm runner logs still emit recovery-path warning/error markers during simulated transient failure.

**Guard for WI closeout**:
- Before marking complete: confirm every test command in `Tests to run` is runnable from repo root.
- If a script is added to a package.json as part of this WI, add it to this WI's Tests section with the full `npm --prefix <package> run <script>` form.

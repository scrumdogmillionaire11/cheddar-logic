# WI-0508: Python Environment Hygiene — isolate soccerdata dependency drift

**ID**: WI-0508
**Goal**: Prevent shared Python package drift from `soccerdata` installs impacting other tooling by defining and enforcing an isolated runtime strategy for soccer xG ingestion jobs.

**Depends on**:

- WI-0491 (xG ingestion pipeline foundation)

**Scope**:

- `WORK_QUEUE/WI-0508.md`
- (future implementation WI to be created from this analysis outcome)

**Out of scope**:

- Changing soccer model decision logic
- Rewriting xG ingestion data sources
- Production deploy changes in this WI

**Acceptance**:

1. Document current dependency drift introduced by `soccerdata` install (exact packages/versions changed from baseline).
2. Propose one primary isolation approach and one fallback (e.g., dedicated venv/pyenv version vs sidecar container).
3. Define operator-safe install/run commands that do not mutate shared Python global/site-packages.
4. Define rollback/cleanup steps for environments already drifted.
5. Add verification checklist proving xG job runs with isolated env and existing non-soccer Python workflows remain unaffected.

**Owner agent**: copilot
**Time window**: 2026-03-19T00:00:00Z → 2026-03-21T23:59:59Z
**Coordination flag**: solo

**Tests to run**:

- `python --version && pip freeze | grep -E "soccerdata|pandas|numpy|scipy|tls-requests"`
- `ENABLE_SOCCER_XG_MODEL=true npm --prefix apps/worker run job:pull-soccer-xg-stats -- --dry-run`
- Existing Python workflow smoke test(s) defined by owner (to confirm no regression)

**Manual validation**:

- Confirm xG job can execute with isolated Python runtime path configured (`SOCCER_XG_PYTHON_BIN`).
- Confirm system/global Python package set is unchanged after setup.
- Confirm rollback steps restore previous state if isolation setup is removed.

CLAIM: copilot 2026-03-19T18:34:56Z

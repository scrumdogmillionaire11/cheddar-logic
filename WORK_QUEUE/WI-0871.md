# WI-0871: F5 ML formula fix — use full-model run projections instead of raw ERA

**ID**: WI-0871
**Goal**: Fix `projectF5ML()` to derive team win probability from `projectF5Total()` run output instead of the crude ERA-averaging formula, and add explicit `tie_probability` to the result.

**Scope**:
- `apps/worker/src/models/mlb-model.js`
- `apps/worker/src/models/__tests__/mlb-model.test.js`

**Out of scope**:
- Full-game projection (WI-0872)
- run_mlb_model.js (caller signature change handled there only if needed)
- Any UI or web changes

**Acceptance**:
1. `projectF5ML()` signature extended to accept optional `context = {}` 5th arg (backward compat — no crash without it)
2. When `projectF5Total()` succeeds with FULL_MODEL on the provided pitchers, `projectF5ML()` uses `projected_home_f5_runs` / `projected_away_f5_runs` as the expected run values in the logistic win-prob formula
3. ERA fallback preserved: when `projectF5Total()` returns null or NO_BET, old `(era + LEAGUE_AVG_RPG) / 2 * (5/9)` formula still runs
4. Return object includes `tie_probability: number` (clamped [0.05, 0.28]) and `used_full_model_path: boolean`
5. All 25+ pre-existing MLB model tests pass
6. ≥ 3 new tests in `mlb-model.test.js` cover: full-model path used when pitcher has skill profile, ERA fallback when skill profile missing, `tie_probability` in valid range, devig still present in output

**Owner agent**: unassigned
**Time window**: TBD
**Coordination flag**: solo

**Tests to run**:
- `npm --prefix apps/worker test -- --testPathPattern=mlb-model`

**Manual validation**:
- Run `computeMLBDriverCards` for a game with full pitcher skill data; inspect F5 ML card's `drivers` — it should show `used_full_model_path: true`

**Guard for WI closeout**:
- Before marking complete: confirm `npm --prefix apps/worker test -- --testPathPattern=mlb-model` runs and passes

CLAIM: unassigned

# WI-0432: FPL Sage Regression Recovery + Planner Restore

**ID**: WI-0432
**Goal**: Fix FPL override/context regressions in Next.js `/fpl` and restore deterministic future-planning visibility.

**Scope**:
- `WORK_QUEUE/WI-0432.md`
- `cheddar-fpl-sage/src/cheddar_fpl_sage/analysis/fpl_sage_integration.py`
- `cheddar-fpl-sage/src/cheddar_fpl_sage/analysis/decision_framework/transfer_advisor.py`
- `cheddar-fpl-sage/src/cheddar_fpl_sage/analysis/decision_framework/contract_enforcement.py`
- `cheddar-fpl-sage/backend/services/result_transformer.py`
- `cheddar-fpl-sage/backend/models/manual_overrides.py`
- `cheddar-fpl-sage/backend/routers/analyze.py`
- `web/src/components/fpl-dashboard.tsx`
- `web/src/components/fpl-lineup-view.tsx`
- `web/src/lib/fpl-api.ts`
- `cheddar-fpl-sage/tests/api_contract_suite.py`
- `cheddar-fpl-sage/tests/test_fixture_horizon_planner.py`
- `web/src/__tests__/fpl-dashboard-strategy.test.js`
- `web/src/__tests__/fpl-dashboard-fixture-planner.test.js`
- `web/src/__tests__/fpl-lineup-formation-contract.test.js`

**Out of scope**:
- Standalone Vite dashboard under `cheddar-fpl-sage/frontend/**`
- Chip scoring algorithm changes
- Transfer model threshold redesign
- Unrelated formatting/refactors

**Acceptance**:
- API overrides for `risk_posture=aggressive` and `free_transfers=2` are resolved into effective team context before decision branching.
- Transformed payload always includes normalized `manager_state` fields (`risk_posture`, `strategy_mode`, `free_transfers`, `overall_rank`, `rank_bucket`) without unintended fallback to `BALANCED`.
- Next.js dashboard always renders DGW/BGW planner container with deterministic fallback copy when planner data is absent.
- Empty near-threshold and strategy-path states show diagnostic reasoning derived from existing no-transfer context (not generic repeated placeholders).
- Starting XI renders as an aerial formation pitch view with clear positional bands (GK/DEF/MID/FWD) and mobile-safe layout.
- Strategy artifacts (`near_threshold_moves`, `strategy_paths`) are generated upstream when viable alternatives exist and expose compact reason metadata when they do not.
- Fixture planner is recomputed upstream when unexpectedly empty despite available squad + fixture context and includes compact reason metadata only for true data-absence states.

**Owner agent**: codex
**Time window**: 2026-03-13T00:00:00Z -> TBD
**Coordination flag**: needs-sync (overlaps active FPL regression surfaces)

**Tests to run**:
- `cd cheddar-fpl-sage && python -m pytest tests/api_contract_suite.py`
- `cd cheddar-fpl-sage && python -m pytest tests/test_fixture_horizon_planner.py`
- `node web/src/__tests__/fpl-dashboard-strategy.test.js`
- `node web/src/__tests__/fpl-dashboard-fixture-planner.test.js`
- `node web/src/__tests__/fpl-lineup-formation-contract.test.js`

**Manual validation**:
- Run `/fpl` analysis with rank near 6.4M, `Aggressive`, `Free Transfers = 2`.
- Confirm decision no longer claims no free transfers incorrectly.
- Confirm manager state reflects aggressive posture and coherent rank-aware strategy mode.
- Confirm DGW/BGW planner section is visible even when planner payload is missing.

CLAIM: codex 2026-03-13T00:00:00Z

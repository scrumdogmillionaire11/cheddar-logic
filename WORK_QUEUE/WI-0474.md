# WI-0474: Phase 3 CLV Ledger Integration for Odds-Backed Markets

**ID**: WI-0474
**Goal**: Enable CLV telemetry for odds-backed cards in a fully additive, flag-gated way to measure market efficiency and profitability without impacting settlement or display pipelines.

**Depends on**:

- `WORK_QUEUE/WI-0471.md`
- `WORK_QUEUE/WI-0473.md`

**Scope**:

- `WORK_QUEUE/WI-0474.md`
- `apps/worker/src/utils/decision-publisher.js`
- `apps/worker/src/jobs/settle_pending_cards.js`
- `apps/worker/src/jobs/settle_game_results.js`
- `apps/worker/src/jobs/__tests__/settle_pending_cards.phase2.test.js`
- `apps/worker/src/jobs/__tests__/settle_pending_cards.market-contract.test.js`
- `apps/worker/src/jobs/__tests__/settle_game_results.matching.test.js`
- `packages/data/src/db-telemetry.js`

**Out of scope**:

- Projection-only model scoring logic
- Changes to `card_results` schema/contract
- Dashboard redesign or new frontend pages
- Multi-process DB writer behavior changes

**Acceptance**:

1. `recordClvEntry` is called only for odds-backed cards when `ENABLE_CLV_LEDGER=true`.
2. CLV settlement updates are additive and do not alter existing card settlement paths.
3. Synthetic/projection-only cards are excluded from CLV ledger by guardrails.
4. Flag-off path produces no ledger writes and no behavior changes.

**Owner agent**: unassigned
**Time window**: 2026-03-16T00:00:00Z -> TBD
**Coordination flag**: solo

**Tests to run**:

- `npm --prefix apps/worker test -- src/jobs/__tests__/settle_pending_cards.phase2.test.js`
- `npm --prefix apps/worker test -- src/jobs/__tests__/settle_pending_cards.market-contract.test.js`
- `npm --prefix apps/worker test -- src/jobs/__tests__/settle_game_results.matching.test.js`
- `ENABLE_CLV_LEDGER=true npm --prefix apps/worker run job:settle-cards`

**Manual validation**:

- Verify `clv_ledger` receives rows for odds-backed cards and excludes projection-only cards.
- Confirm `card_results` and existing settlement outputs remain unchanged after CLV writes.

**Guard for WI closeout**:

- Before marking complete: confirm every test command in `Tests to run` is runnable from repo root.
- Include SQL evidence showing CLV rows tied to card IDs with no duplicate unsettled entries.

CLAIM: unassigned

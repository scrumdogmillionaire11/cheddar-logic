# WI-1130: Patch Transitive Follow-Redirects Vulnerability

**ID**: WI-1130
**Goal**: Eliminate the follow-redirects advisory finding from production dependency graph.

**Scope**:
- package-lock.json
- apps/worker/package-lock.json
- package.json
- apps/worker/package.json

**Out of scope**:
- Broad dependency refresh beyond affected vulnerable path
- Dev-only dependency hygiene

**Acceptance**:
- npm audit omit=dev reports zero findings for follow-redirects advisory in root and worker contexts.
- Lockfiles are consistent and installable in CI.
- Runtime behavior for affected HTTP client paths is unchanged.

**Owner agent**: unassigned
**Time window**: 2026-04-21T00:00:00Z -> TBD
**Coordination flag**: needs-sync
**Depends on**: none (independent security patch)

**Tests to run**:
- npm audit --omit=dev --json
- npm --prefix apps/worker audit --omit=dev --json
- npm --prefix apps/worker run test -- --runInBand

**Manual validation**:
- Confirm no new vulnerable transitive packages were introduced by the patch.

**Guard for WI closeout**:
- Before marking complete: confirm every test command in Tests to run is runnable from repo root (i.e., exists in web/package.json scripts or is a valid node path/to/test.js command).
- If a script is added to web/package.json as part of this WI, add it to this WI's Tests section with the full npm --prefix web run script form.

CLAIM: unassigned 2026-04-21T00:00:00Z

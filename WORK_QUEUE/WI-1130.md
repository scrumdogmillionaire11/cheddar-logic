# WI-1130: Patch Transitive Follow-Redirects Vulnerability

**ID**: WI-1130
**Goal**: Eliminate the follow-redirects advisory finding from production dependency graph across all production contexts.

**Scope**:
- package.json
- package-lock.json
- apps/worker/package.json
- apps/worker/package-lock.json
- packages/odds/package.json
- packages/odds/package-lock.json

**Out of scope**:
- Broad dependency refresh unrelated to follow-redirects fix
- Dev-only dependency updates
- Non-production packages (e.g., cheddar-fpl-sage/frontend)

**Acceptance**:
- `npm audit --omit=dev --json` (root context) reports zero findings for follow-redirects advisory.
- `npm --prefix apps/worker audit --omit=dev --json` reports zero findings for follow-redirects advisory.
- `npm --prefix packages/odds audit --omit=dev --json` reports zero findings for follow-redirects advisory.
- Lockfiles are consistent and installable in CI.
- Runtime behavior for affected HTTP client paths is unchanged.

**Owner agent**: unassigned
**Time window**: 2026-04-21T00:00:00Z -> TBD
**Coordination flag**: needs-sync
**Depends on**: none (independent security patch)

**Tests to run**:
- npm audit --omit=dev --json 2>&1 | jq '.vulnerabilities.follow-redirects // empty' (root)
- npm --prefix apps/worker audit --omit=dev --json 2>&1 | jq '.vulnerabilities.follow-redirects // empty' (worker)
- npm --prefix packages/odds audit --omit=dev --json 2>&1 | jq '.vulnerabilities.follow-redirects // empty' (odds)
- npm --prefix apps/worker run test -- --runInBand (worker tests smoke)

**Manual validation**:
- Confirm no new vulnerable transitive packages were introduced by the patch across all three audit contexts.

**Guard for WI closeout**:
- Before marking complete: verify that each of the three jq audit commands (root, worker, odds) returns empty output (no follow-redirects vulnerability).
- Confirm all lockfiles in scope (package-lock.json, apps/worker/package-lock.json, packages/odds/package-lock.json) are updated and consistent.
- Worker tests pass with --runInBand flag.

CLAIM: unassigned 2026-04-21T00:00:00Z

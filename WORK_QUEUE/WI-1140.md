# WI-1140: Materialize Projection Analytics Contract And Reduce Payload Fallback Drift

**ID**: WI-1140
**Goal**: Shift projection analytics reads to canonical materialized fields and reduce fragile JSON payload path probing in results/reporting surfaces.

**Scope**:

- packages/data/src/db/projection-accuracy.js
- web/src/app/api/results/projection-settled/route.ts
- web/src/app/api/results/route.ts
- web/src/app/api/results/projection-metrics.ts

**Out of scope**:

- New betting market rollout
- Retrospective backfill of all historical rows beyond required compatibility reads
- UI redesign of `/results`

**Acceptance**:

- Results/projection APIs prefer canonical eval columns (probability, edge, confidence, tracking role) over payload probing.
- Legacy payload probing is isolated to explicit compatibility branches.
- Projection-settled output enforces logical row dedupe safeguards.
- API payload shape remains backward compatible for existing UI consumers.

**Owner agent**: unassigned
**Time window**: 2026-04-22T11:21:22Z -> TBD
**Coordination flag**: needs-sync
**Depends on**: WI-1138

**Tests to run**:

- npm --prefix web run test:ui:results
- npm --prefix web run test:api:results:flags
- npm --prefix web run test:api:validation-registry
- node web/src/\_\_tests\_\_/integration/settlement-parity.test.ts

**Manual validation**:

- Verify `/results` no longer shows repeated logical projection rows for the same card/game/market tuple.
- Verify projection confidence/edge fields still render on projection settlement surfaces.

**Guard for WI closeout**:

- Before marking complete: confirm every test command in Tests to run is runnable from repo root.
- If any new projection-results test script is added, record exact `npm --prefix web run <script>` here.

CLAIM: unassigned 2026-04-22T11:21:22Z

# WI-0872: Full-game total projection model

**ID**: WI-0872
**Goal**: Add `projectFullGameTotal()` and `projectFullGameTotalCard()` to the MLB model, extending the F5 projection through innings 6-9 via a bullpen quality layer, and wire into `computeMLBDriverCards()`.

**Scope**:
- `apps/worker/src/models/mlb-model.js`
- `apps/worker/src/models/__tests__/mlb-model.test.js`

**Out of scope**:
- Full-game moneyline (WI-0873)
- run_mlb_model.js wiring (WI-0873)
- Any new DB tables or ingestion jobs for bullpen data

**Acceptance**:
1. `projectLateInningsRuns(offenseProfile, pitcherHandedness, opponentBullpenEra, environment)` function added; returns `{ late_innings_runs, bullpen_era_used, bullpen_source }`
2. `opponentBullpenEra` defaults to constant `MLB_FG_DEFAULT_BULLPEN_ERA = 4.0` when null/undefined
3. `projectFullGameTotal()` returns projected total higher than `projectF5Total()` for the same matchup (sanity check: late innings always > 0)
4. `projectFullGameTotalCard()` returns OVER/UNDER/PASS with `status`, `edge`, `confidence`, `projection_source` mirroring F5 card shape
5. `computeMLBDriverCards()` produces a `full_game_total` card when `mlb.full_game_line` or `mlb.total` is non-null in the snapshot
6. All 25+ pre-existing MLB model tests pass
7. ≥ 4 new tests: `projectLateInningsRuns` with elite offense vs weak bullpen, `projectFullGameTotal` > F5 for same matchup, `projectFullGameTotalCard` OVER when projected > line by ≥ 0.6, regression on F5 tests

**Owner agent**: unassigned
**Time window**: TBD
**Coordination flag**: solo

**Tests to run**:
- `npm --prefix apps/worker test -- --testPathPattern=mlb-model`

**Manual validation**:
- Call `computeMLBDriverCards` with snapshot containing `total: 8.5`; confirm `full_game_total` card present in returned array

**Guard for WI closeout**:
- Before marking complete: confirm `npm --prefix apps/worker test -- --testPathPattern=mlb-model` runs and all tests pass

CLAIM: unassigned

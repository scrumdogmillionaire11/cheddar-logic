# WI-1014: Wire or delete `computeCanonicalDecision` + add tests

**ID**: WI-1014
**Goal**: `web/src/lib/game-card/canonical-decision.ts` exports `computeCanonicalDecision()` which was designed as the "single source of truth for card display" but is imported by nobody. This WI decides and executes: either wire it into the transform pipeline (replacing the inline decision logic in `transform/index.ts` around L2390) or delete it. Either way, the ambiguity is resolved and the function has test coverage if it stays.

**Depends on**: none

**Scope**:
- `WORK_QUEUE/WI-1014.md`
- `.planning/STATE.md`
- `web/src/lib/game-card/canonical-decision.ts`
- `web/src/lib/game-card/transform/index.ts`

If wiring path chosen, also:
- `web/src/lib/game-card/canonical-decision.test.ts` (new test file)

**Out of scope**:
- Changes to `decision.ts`, `decision-pipeline-v2.js`, or any worker-side code
- Changes to the `Decision`, `Signal`, `Value`, `CanonicalMarket` type definitions (these are correct and should be kept if the file is kept)
- UI component changes

**Decision criteria** (for implementer):
- If the inline decision logic in `transform/index.ts` (L2390–L2450 area) is already structurally equivalent to what `computeCanonicalDecision` produces, wire it — delete the inline block and call the function.
- If the two are semantically different enough that wiring requires a behavior change, delete `canonical-decision.ts` instead and open a separate WI for the architectural unification.

**Acceptance**:
- Either: `computeCanonicalDecision` is called in `transform/index.ts` and covered by tests in `canonical-decision.test.ts`
- Or: `canonical-decision.ts` is deleted and `grep -r "canonical-decision" web/src/` returns nothing
- In the wiring path: tests cover FIRE/WATCH/HOLD/PASS outputs, market mapping, signal inference, and value mapping
- `npm --prefix web run build` passes

**Owner agent**: unassigned
**Time window**: unscheduled
**Coordination flag**: solo

**Tests to run**:
- `npm --prefix web run build`
- If wiring: `npm --prefix web test -- canonical-decision --runInBand`

**Manual validation**:
- Confirm no orphaned exports remain if deletion path taken
- Confirm card display behavior unchanged in the UI after wiring (visit /cards, check FIRE/PASS/WATCH cards render correct badge)

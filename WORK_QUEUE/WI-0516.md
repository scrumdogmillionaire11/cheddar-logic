# WI-0516: Wire deterministic chip engine through FPL Sage backend + frontend

**ID**: WI-0516
**Goal**: Replace legacy chip-window wiring with the new deterministic chip engine and expose `FIRE` / `WATCH` / `PASS` chip states end-to-end in FPL Sage.

**Depends on**:

- WI-0513
- WI-0514
- WI-0515

**Scope**:

- `WORK_QUEUE/WI-0516.md`
- `cheddar-fpl-sage/src/cheddar_fpl_sage/analysis/decision_framework/chip_analyzer.py`
- `cheddar-fpl-sage/backend/services/result_transformer.py`
- `cheddar-fpl-sage/frontend/src/lib/api.ts`
- `cheddar-fpl-sage/frontend/src/lib/decisionViewModel.ts`
- `cheddar-fpl-sage/frontend/src/lib/decisionViewModel.test.ts`
- `cheddar-fpl-sage/frontend/src/components/ChipDecision.tsx`
- `cheddar-fpl-sage/tests/test_chip_engine_adapter_integration.py`
- `cheddar-fpl-sage/tests/test_transfer_scenario_matrix.py`
- `cheddar-fpl-sage/tests/test_chip_timing_scenario_matrix.py`
- `cheddar-fpl-sage/tests/test_transfer_engine_framework_spec.py`

**Out of scope**:

- New chip formulas outside the deterministic engine modules
- Extra frontend redesign beyond chip-state rendering needed for `FIRE` / `WATCH` / `PASS`
- Non-FPL systems outside `cheddar-fpl-sage/`

**Acceptance**:

- `ChipAnalyzer` builds a valid `GameweekState` from existing FPL Sage inputs and evaluates chips through the new deterministic engine instead of legacy window-only heuristics.
- Backend transformed output exposes structured chip fields including `status`, `score`, `reasonCode`, `reasonCodes`, `forcedBy`, `watchUntil`, and `narrative`.
- Frontend types and view-model preserve the richer chip contract and render `FIRE`, `WATCH`, and `PASS` distinctly rather than collapsing WATCH into no-chip.
- Integration and frontend tests cover at least one WATCH case and one forced FIRE case end-to-end.
- Frontend build remains green after the chip contract update.

**Owner agent**: unassigned
**Time window**: 2026-03-19T22:45:00Z -> TBD
**Coordination flag**: solo

**Tests to run**:

- `pytest cheddar-fpl-sage/tests/test_chip_engine_adapter_integration.py`
- `pytest cheddar-fpl-sage/tests/test_chip_timing_scenario_matrix.py`
- `pytest cheddar-fpl-sage/tests/test_transfer_engine_framework_spec.py`
- `npm --prefix cheddar-fpl-sage/frontend test -- --run decisionViewModel.test.ts`
- `npm --prefix cheddar-fpl-sage/frontend run build`

**Manual validation**:

- Inspect a transformed analysis payload and confirm `watchUntil` and `forcedBy` survive from adapter to backend output.
- Load a local FPL Sage chip decision view and confirm WATCH renders as a monitor/hold state, not as plain no-chip.

**Guard for WI closeout**:

- Do not reintroduce formula logic into `chip_analyzer.py`; it should adapt inputs into the deterministic engine, not own scoring.
- Keep the UI changes limited to the chip-decision contract/rendering path in the scoped files.

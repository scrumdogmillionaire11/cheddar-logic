# WI-1135: Decompose API Results Route And Isolate Reporting Workloads

**ID**: WI-1135
**Goal**: Reduce results route query complexity and isolate summary/ledger/reporting workloads for safer optimization.

**Scope**:
- web/src/app/api/results/route.ts
- web/src/app/api/results/projection-metrics.ts
- web/src/lib/results/query-layer.ts
- web/src/lib/results/transform-layer.ts
- web/src/lib/results/cache.ts

**Out of scope**:
- Results UI redesign
- Worker materialized reporting table implementation

**Acceptance**:
- Results route responsibilities are split into query, transform, and cache modules.
- Diagnostic-only expensive paths remain gated and explicit.
- Existing results API and UI smoke tests pass.

**Owner agent**: codex
**Time window**: 2026-04-21T00:00:00Z -> TBD
**Coordination flag**: needs-sync
**Depends on**: WI-1127

**Tests to run**:
- npm --prefix web run test:api:results:decision-segmentation
- npm --prefix web run test:api:results:flags
- npm --prefix web run test:ui:results

**Manual validation**:
- Verify summary, segments, projectionSummaries, and ledger contracts are unchanged for default requests.

**Guard for WI closeout**:
- Before marking complete: confirm every test command in Tests to run is runnable from repo root (i.e., exists in web/package.json scripts or is a valid node path/to/test.js command).
- If a script is added to web/package.json as part of this WI, add it to this WI's Tests section with the full npm --prefix web run script form.

CLAIM: codex 2026-04-23T00:27:12Z

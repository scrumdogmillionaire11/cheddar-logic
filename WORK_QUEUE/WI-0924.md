# WI-0924: Remove Unimplemented FPL Legacy Type Stub and Test-Only Contract

**ID**: WI-0924

**Goal**: Delete the unimplemented `fpl-types` compatibility stub (throwing TODO path) and align tests with active FPL runtime contracts.

**Depends on**: WI-0922

**Scope**:

- `apps/worker/src/models/fpl-types.js`
- `apps/worker/src/models/__tests__/fpl-integration-test.js`
- `apps/worker/src/jobs/run_fpl_model.js`
- `apps/worker/src/__tests__/scheduler-fpl.test.js`

**Out of scope**:

- Rebuilding Sage remote integration for FPL
- Changes to non-FPL model modules
- New external API dependencies

**Acceptance**:

- Unimplemented stub function path (`getSagePrediction not yet implemented`) is removed from runtime code.
- FPL tests validate currently used runtime interfaces, not dormant placeholder modules.
- `run_fpl_model.js` imports/contracts remain explicit and test-covered after cleanup.
- No non-doc reference to `models/fpl-types` remains.

**Owner agent**: unassigned

**Time window**: 2026-04-13 -> TBD

**Coordination flag**: solo

**Tests to run**:

- `npm --prefix apps/worker run test -- --testPathPattern="fpl|scheduler-fpl|fpl-corruption-detection" --no-coverage`
- `rg -n "fpl-types|getSagePrediction|validatePredictionSchema" apps/worker/src --glob '*.js'`

**Manual validation**:

- Run `npm --prefix apps/worker run job:run-fpl-model:test` and confirm inference path still returns model output without stub dependency.
- Confirm no TODO/throw-not-implemented FPL compatibility path remains in worker models.

CLAIM: unassigned

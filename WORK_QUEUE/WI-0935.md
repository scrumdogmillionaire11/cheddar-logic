# WI-0935: Decision policy simplification (canonical status helpers)

**ID**: WI-0935
**Goal**: Reduce decision-rule duplication by centralizing canonical status normalization/actionability/ranking helpers, without changing projection math or output behavior.

**Depends on**: WI-0897, discord-3layer-01, discord-3layer-02

**Scope**:

- `packages/models/src/decision-policy.js`
- `packages/models/src/index.js`
- `packages/models/src/__tests__/decision-policy.test.js`
- `apps/worker/src/utils/decision-publisher.js`
- `apps/worker/src/utils/__tests__/decision-publisher.v2.test.js`
- `packages/data/src/db/cards.js`

**Out of scope**:

- Any projection formulas or model weight/feature changes
- Discord rendering copy/layout changes
- DB schema migrations
- Full legacy fallback deletion in Discord (`post_discord_cards.js`)

**Acceptance**:

- New shared helper module provides canonical official-status utilities:
  - normalize to `PLAY | LEAN | PASS | ''`
  - actionable check (`PLAY`/`LEAN`)
  - deterministic rank (`PLAY > LEAN > PASS/unknown`)
- `decision-publisher.js` and `cards.js` use shared helpers instead of duplicating status checks.
- Existing behavior remains unchanged for:
  - UI display derivation (`PLAY/WATCH/PASS`)
  - card tracking actionability gates
  - status ranking comparisons
- Regression tests in `decision-publisher.v2.test.js` cover normalization and UI mapping invariants.
- Regression tests in `packages/models/src/__tests__/decision-policy.test.js` cover shared helper parity for status and webhook-bucket derivation.
- Tests pass:
  - `npm --prefix packages/models run test -- --runInBand src/__tests__/decision-policy.test.js`
  - `npm --prefix apps/worker run test -- --runInBand src/utils/__tests__/decision-publisher.v2.test.js`

**Owner agent**: pax-executor

**Time window**: WI-0935 only

**Coordination flag**: none

**Tests to run**:

- `npm --prefix apps/worker run test -- --runInBand src/utils/__tests__/decision-publisher.v2.test.js`
- `npm --prefix packages/models run test -- --runInBand src/__tests__/decision-policy.test.js`

**Manual validation**:

- Spot-check sample payloads for `decision_v2.official_status` values (`PLAY/LEAN/PASS/unknown`) and confirm unchanged `ui_display_status` and card tracking eligibility.

CLAIM: pax-executor 2026-04-13T23:58:45Z

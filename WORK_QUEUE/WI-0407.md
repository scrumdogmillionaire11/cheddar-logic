# WI-0407: Replace Brittle FT Source Test with Behavior Coverage

**ID**: WI-0407
**Goal**: Add behavior-level regression coverage for FT advantage resolution so correctness no longer depends primarily on source-string assertions.

**Scope**:

- `WORK_QUEUE/WI-0407.md`
- `web/src/__tests__/cards-ft-advantage-source.test.js`
- New targeted web test file(s) under `web/src/__tests__/` for FT advantage behavior
- Minimal supporting test helpers if needed within existing web test directories

**Out of scope**:

- Production logic changes unless strictly required to make behavior testable
- Broad test framework migration
- Non-FT regression suites

**Acceptance**:

- A behavior-level test verifies FT advantage follows higher FT% even if side/direction is rewritten elsewhere.
- Source-contract assertions are reduced to a minimal compatibility guard or retained only where still valuable.
- The new test fails on team inversion regressions and passes on current implementation.

**Owner agent**: unassigned
**Time window**: TBD -> TBD
**Coordination flag**: none

**Tests to run**:

- Targeted new FT behavior test
- `node web/src/__tests__/cards-ft-advantage-source.test.js`

**Manual validation**:

- Review test fixture(s) to confirm they cover both stats-first success and legacy fallback paths.

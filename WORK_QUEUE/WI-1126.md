# WI-1126: Query Validation Registry Contract And Drift Guard

**ID**: WI-1126
**Goal**: Eliminate endpoint-validation drift by synchronizing route query params with the security validator contract.

**Scope**:
- web/src/lib/api-security/validation.ts
- web/src/app/api/performance/route.ts
- web/src/app/api/model-outputs/route.ts
- web/src/__tests__/api-validation-registry-contract.test.js
- web/src/__tests__/api-performance-validation.test.js
- web/src/__tests__/api-model-outputs-validation.test.js

**Out of scope**:
- Full OpenAPI or schema-generation platform migration
- Route business logic rewrites unrelated to validation

**Acceptance**:
- All routes using performSecurityChecks are represented in validation allowlist coverage.
- Performance and model-outputs queries pass expected validation paths.
- Contract test fails when a secured route is added without validator registration.

**Owner agent**: unassigned
**Time window**: 2026-04-21T00:00:00Z -> TBD
**Coordination flag**: needs-sync
**Depends on**: WI-1125 (Wave 1 serialized order)

**Tests to run**:
- node --import tsx/esm web/src/__tests__/api-validation-registry-contract.test.js
- node --import tsx/esm web/src/__tests__/api-performance-validation.test.js
- node --import tsx/esm web/src/__tests__/api-model-outputs-validation.test.js

**Manual validation**:
- Request valid and invalid query strings for performance and model-outputs endpoints and confirm expected status codes.

**Guard for WI closeout**:
- Before marking complete: confirm every test command in Tests to run is runnable from repo root (i.e., exists in web/package.json scripts or is a valid node path/to/test.js command).
- If a script is added to web/package.json as part of this WI, add it to this WI's Tests section with the full npm --prefix web run <script> form.

CLAIM: unassigned 2026-04-21T00:00:00Z

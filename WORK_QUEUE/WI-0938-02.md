# WI-0938-02: THE BOARD Page Skeleton + Feature Flag (MVP Foundation)

**ID**: WI-0938-02  
**Parent**: WI-0938  
**Goal**: Build the `/board` route with empty tab components and feature flag infrastructure. Users see page structure but components are not yet populated with real data.

**Depends on**: WI-0938-01 (plan must be complete first)

**Scope**:

- `web/src/app/board/page.tsx` (main page layout)
- `web/src/app/board/layout.tsx` (header, nav)
- `web/src/components/board/` directory (new)
- Feature flag logic in web configuration
- `/api/board/` endpoint stubs (return mock data structure)
- Navigation: update main nav to include `/board` link (conditional on feature flag)
- Update `.env.example` to document FEATURE_BOARD_ENABLED or similar

**Out of scope**:

- Populating tabs with real data (that's WI-0938-03 onward)
- Edge reasoning enrichment (that's WI-0938-06)
- Discord integration (that's WI-0938-07)
- Performance optimization

**Acceptance**:

- `/board` route exists and is only accessible when FEATURE_BOARD_ENABLED=true
- When disabled, route redirects to `/wedge` or shows "not available" message
- Page renders with navigation showing 3 tabs: "Opportunities", "Blocked Edges", "Edge Type Tracker"
- Each tab has an empty component placeholder (e.g., `<OpportunitiesTab>` renders "Loading..." or empty state)
- `/api/board/opportunities` endpoint exists and returns mock data matching schema structure
- `/api/board/blocked-edges` endpoint exists and returns mock data matching schema structure
- `/api/board/edge-types` endpoint exists and returns mock data matching schema structure
- Main nav shows `/board` link only when FEATURE_BOARD_ENABLED=true
- `npm --prefix web run build` succeeds with no new errors
- Feature flag documentation added to `.env.example`

**Owner agent**: unassigned  
**Time window**: 3-4 days  
**Coordination flag**: none  

**Tests to run**:

- `npm --prefix web run build`
- Manual: Navigate to `/board` with feature flag ON → page loads with tabs visible
- Manual: Navigate to `/board` with feature flag OFF → redirect or error message
- Manual: Check main nav shows/hides `/board` link based on flag

**Manual validation**:

- Feature flag toggle works (set env var, page appears/disappears)
- Tab navigation works (clicking tabs doesn't error)
- Mock API endpoints return correct structure
- No TypeScript errors

**Notes**:

- This is the skeleton phase: **no real data yet**
- Tabs should render empty states ("No opportunities found" or similar)
- Real enrichment happens in WI-0938-06
- This unblocks WI-0938-03, WI-0938-04, WI-0938-05 to proceed in parallel

CLAIM: unassigned

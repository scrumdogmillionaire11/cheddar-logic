# WI-0330: Simplify Label Hierarchy

**ID**: WI-0330
**Goal**: Reduce redundant badges to a clear hierarchy: Decision, Quality, Confidence, Flags.

**Scope**:

- web/src/components/cards-page-client.tsx
- web/src/lib/game-card/transform.ts (only if label inputs need adjustment)
- web/src/app/api/games/route.ts (only if payload shape needs tweaks)

**Out of scope**:

- Decision computation logic
- Model scoring logic

**Acceptance**:

- Cards show no more than four label types: Decision, Quality, Confidence, Flags
- Redundant labels (duplicate Truth/Value/Classification) are removed
- Coinflip and degraded flags remain visible when applicable
- Labels wrap cleanly on mobile without overlap

**Owner agent**: unassigned
**Time window**: TBD -> TBD
**Coordination flag**: solo

**Tests to run**:

- `cd web && npm run dev`

**Manual validation**:

- Check multiple cards and confirm label counts and hierarchy
- Verify coinflip and degraded flags still show when triggered
- Verify no overlapping or duplicate labels on small screens

CLAIM: Claude 2026-03-07T13:15:00Z
STATUS: closed
CLOSED: 2026-03-07 — shipped in commit 002765d

# WI-0409: Remove FT Note Regex Fallback After Structured Context Hardening

**ID**: WI-0409
**Goal**: Eliminate regex parsing of FT percentages from explanatory note text once structured FT context is guaranteed and validated end-to-end.

**Scope**:

- `WORK_QUEUE/WI-0409.md`
- `web/src/components/cards-page-client.tsx`
- `web/src/app/api/games/route.ts`
- `web/src/lib/game-card/transform.ts`
- `web/src/__tests__/cards-ft-advantage-source.test.js`
- Behavior-level FT regression tests in `web/src/__tests__/`

**Out of scope**:

- FT trend model logic changes
- Historical payload migration/backfill
- Removal of structured FT context itself

**Acceptance**:

- FT advantage rendering relies only on structured FT context, not parsed reasoning strings.
- Structured FT context is guaranteed or defensively validated before rendering.
- Tests cover missing/invalid context behavior without reverting to incorrect team mapping.

**Owner agent**: unassigned
**Time window**: TBD -> TBD
**Coordination flag**: none

**Tests to run**:

- Targeted FT behavior tests
- `node web/src/__tests__/cards-ft-advantage-source.test.js`

**Manual validation**:

- Confirm FT callouts still render correctly for current NCAAM FT-trend cards with structured data only.
- Confirm missing structured context fails safely without incorrect inversion.

# WI-0598: Pitcher Ks contract hardening (validator + market contract)

**ID**: WI-0598
**Goal**: Enforce pitcher K payload contract and canonical market mapping for both odds-backed and projection-only cards.

**Scope**:

- `packages/data/src/validators/card-payload.js`
- `packages/data/src/market-contract.js`
- `packages/data/src/__tests__/card-payload.test.js`
- `packages/data/src/__tests__/market-contract.test.js`
- `apps/worker/src/jobs/run_mlb_model.js`

**Out of scope**:

- Odds ingestion implementation
- UI rendering adjustments
- Broad non-MLB payload refactors

**Acceptance**:

- Pitcher K cards validate with explicit schema coverage for required fields, reason codes, and mode/basis metadata.
- Contract permits projection-only payload semantics intentionally (not via fallback loopholes).
- Canonical market mapping for pitcher K is deterministic and test-covered.
- Existing non-pitcher payload validations remain green.

**Owner agent**: unassigned
**Time window**: 2026-03-26T00:00:00Z -> TBD
**Coordination flag**: needs-sync

**Tests to run**:

- `npm --prefix packages/data test`

**Manual validation**:

- Run MLB model in both modes and verify payload validation passes with no contract warnings.

**Guard for WI closeout**:

- Before marking complete: confirm every test command in `Tests to run` is runnable from repo root.
- Before marking complete: verify no legacy fallback path can classify malformed pitcher K payloads as valid.

CLAIM: copilot 2026-03-26T00:00:00Z

# WI-0595: Pitcher Ks core engine (projection-only parity)

**ID**: WI-0595
**Goal**: Implement the pitcher K decision engine like-for-like from `docs/pitcher_ks/` in projection-only mode first, including scoring, leash, overlays, trap scan, and output contract shape.

**Scope**:

- `apps/worker/src/jobs/run_mlb_model.js`
- `apps/worker/src/models/mlb-model.js`
- `apps/worker/src/models/index.js`
- `apps/worker/src/jobs/__tests__/run_mlb_model.test.js`

**Out of scope**:

- Odds ingestion for pitcher K prop lines
- Scheduler cadence changes
- Web/UI rendering changes
- Data migrations

**Acceptance**:

- Pitcher K logic from `docs/pitcher_ks/01process.md` through `docs/pitcher_ks/07output.md` is represented in executable runner/model logic.
- Projection-only cards can emit without market odds while preserving explicit reason codes and gating diagnostics.
- Output includes stable fields required for downstream contract validation and UI transform.
- Regression tests cover positive emit path and at least 3 blocked-path reason-code scenarios.

**Owner agent**: unassigned
**Time window**: 2026-03-26T00:00:00Z -> TBD
**Coordination flag**: solo

**Tests to run**:

- `npm --prefix apps/worker run test -- src/jobs/__tests__/run_mlb_model.test.js`

**Manual validation**:

- Run `set -a; source .env; set +a; PITCHER_KS_MODEL_MODE=PROJECTION_ONLY npm --prefix apps/worker run job:run-mlb-model` and verify pitcher K cards emit with explicit projection-only basis metadata.

**Guard for WI closeout**:

- Before marking complete: confirm every test command in `Tests to run` is runnable from repo root.
- Before marking complete: confirm no odds pull path is required for successful projection-only emission.

CLAIM: unassigned

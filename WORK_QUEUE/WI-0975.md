# WI-0975: Add production runtime SHA verification gate

**ID**: WI-0975
**Goal**: Block deploy completion unless production runtime commit SHA matches the intended release SHA.

**Depends on**: WI-0974

**Scope**:
- `.github/workflows/deploy-production.yml`
- `scripts/deployment-verify.js`
- `web/src/app/api/health/route.ts`
- `docs/runbooks/production-release-verification.md`

**Out of scope**:
- Artifact build pipeline redesign
- Non-production environments

**Acceptance**:
- Post-deploy verification step reads runtime SHA from an API endpoint or build metadata source.
- Deploy is marked failed if runtime SHA != intended release SHA.
- Verification output is visible in workflow logs and release evidence.

**Owner agent**: unassigned
**Time window**: 2026-04-16 -> TBD
**Coordination flag**: needs-sync

**Tests to run**:
- `npm --prefix web run lint`
- `npm --prefix web run test:api:nhl-contract`

**Manual validation**:
- Simulate a SHA mismatch and confirm deployment verification step fails.
- Confirm a normal deploy passes and reports matching intended/runtime SHA values.

CLAIM: unassigned
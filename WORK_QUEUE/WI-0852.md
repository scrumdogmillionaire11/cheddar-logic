# WI-0852: POTD Web Surface — /api/potd + /play-of-the-day + Cards/Home Integration

| Field | Value |
|---|---|
| **ID** | WI-0852 |
| **Goal** | Expose POTD through a read-only API and new page, add homepage navigation, and ensure `potd-call` appears correctly on the existing cards surface without introducing web-side DB writes. |
| **Scope** | `web/src/app/api/potd/route.ts`, `web/src/app/play-of-the-day/page.tsx`, `web/src/components/play-of-the-day-client.tsx`, `web/src/app/page.tsx`, `web/src/__tests__/api-potd.test.js`, `web/src/__tests__/ui-potd-smoke.test.js` |
| **Out of scope** | Worker write paths, scheduler logic, migration files |
| **Acceptance** | (1) `GET /api/potd` returns `{ success, data: { today, history, bankroll, schedule } }` from read-only DB connections only. (2) `/play-of-the-day` renders today's play, empty/no-play state, bankroll summary, record, and history. (3) Homepage includes a visible link to `/play-of-the-day`. (4) `potd-call` remains visible on existing `/cards` feeds with no web-side writes or auth work added. (5) API/UI tests cover empty state, posted state, and settled-history state. |
| **Owner agent** | codex |
| **Time window** | 2026-04-09T23:50:00Z - until completion |
| **Coordination flag** | needs-sync |
| **Tests to run** | `node web/src/__tests__/api-potd.test.js`<br>`node web/src/__tests__/ui-potd-smoke.test.js`<br>`npm --prefix web run test:ui:cards` |
| **Manual validation** | Load `/play-of-the-day` against a seeded local DB and confirm the page matches API data. Load `/cards` and confirm a published `potd-call` appears there without duplicate Discord behavior. |

CLAIM: codex 2026-04-09T23:50:00Z

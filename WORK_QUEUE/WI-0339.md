# WI-0339: Edge Sanity Checks + Proxy/Override Handling

**ID**: WI-0339
**Goal**: Reduce inflated edges and clarify proxy/override states with safe downgrade rules and explicit tagging.

**Scope**:

- web/src/app/api/games/route.ts
- web/src/lib/play-decision/decision-logic.ts
- web/src/lib/game-card/transform.ts
- web/src/lib/game-card/tags.ts
- web/src/components/cards-page-client.tsx

**Out of scope**:

- New pricing models or market ingestion logic
- Database schema changes

**Acceptance**:

- Edges above a configured threshold (e.g., >20%) are downgraded to verification-required status.
- Proxy-based cards are tagged and capped at PASS or low-confidence WATCH.
- Override labels are hidden from production UI while remaining visible in debug mode.
- A simple counter or summary indicates how many cards were downgraded or proxy-capped.

**Owner agent**: unassigned
**Time window**: 2026-03-07T00:00:00Z -> TBD
**Coordination flag**: needs-sync

**Tests to run**:

- npm --prefix web run test:ui:cards
- npm --prefix web run test:games-filter

**Manual validation**:

- Confirm giant edges are downgraded and tagged.
- Confirm proxy-based cards are labeled and capped.
- Confirm override labels are hidden in normal UI.

CLAIM: unassigned 2026-03-07T00:00:00Z

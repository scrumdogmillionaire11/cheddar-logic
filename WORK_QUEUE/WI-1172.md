# WI-1172: Collapse Dead MLB SIERA/xERA Fallback Weighting to the Active Runtime Contract

**ID**: WI-1172
**Goal**: Remove the dead null-weighting branches for `siera` and `x_era` from MLB starter-skill fallback logic and align the runtime/documented contract with the signals that actually exist today.

**Scope**:

- `apps/worker/src/jobs/run_mlb_model.js`
- `apps/worker/src/jobs/__tests__/run_mlb_model.test.js`
- `docs/decisions/ADR-0007-mlb-f5-full-model-projection-contract.md`

**Out of scope**:

- Reintroducing true SIERA or xERA computation into the data pipeline
- Broad MLB model formula redesign outside the starter-skill fallback slice

**Implementation notes**:

- `getPitcherEraFromDb()` and `computeProjectionFloorF5()` currently carry three-signal fallback branches (`siera`/`x_fip`/`x_era`) that are treated as dead code for the active runtime contract in this WI.
- **Required implementation outcome:** collapse fallback behavior to `x_fip`-only logic and remove `siera`/`x_era` weighting branches from the runtime path in scope. Do not leave conditional keep-if-populated code paths in this WI.
- Add a scoped evidence check (grep-based and test fixture-based) proving no active fallback branch in scope still consumes `siera` or `x_era` for weighting.
- Update ADR-0007 with explicit wording that the active fallback contract is `x_fip`-only today; note `siera`/`x_era` as future-only signals pending a separate ingest WI.
- Add or update regression coverage for: populated `x_fip`, missing starter-skill data fallback, and raw-data-vs-DB fallback parity under the simplified contract.

**Acceptance**:

- Dead and legacy fallback fragments in scope (including dormant weighting helpers, unreachable branches, and obsolete comments tied to `siera`/`x_era` runtime weighting) are removed or rewritten to reflect the active contract.
- Technical debt handling is mandatory in this WI: if legacy logic cannot be removed safely, add an inline `TECHDEBT(WI-1172)` marker with concrete removal criteria and blocking condition for follow-up cleanup.
- `apps/worker/src/jobs/run_mlb_model.js` no longer contains `siera`/`x_era` weighting logic in the starter-skill fallback path; fallback is `x_fip`-only in scoped functions.
- ADR-0007 explicitly documents the same active contract (`x_fip`-only fallback) and does not describe `siera`/`x_era` as active runtime inputs.
- Regression tests in scope assert all of: populated `x_fip` behavior, missing starter-skill fallback behavior, and raw-data-vs-DB fallback parity under `x_fip`-only contract.
- Required evidence command succeeds from repo root: `rg -n "siera|x_era" apps/worker/src/jobs/run_mlb_model.js apps/worker/src/jobs/__tests__/run_mlb_model.test.js docs/decisions/ADR-0007-mlb-f5-full-model-projection-contract.md` and results are either absent in runtime weighting paths or explicitly marked as future/non-runtime documentation.

**Owner agent**: github-copilot
**Time window**: 2026-04-24T00:00:00Z -> 2026-04-26T00:00:00Z
**Coordination flag**: solo
**Depends on**: none

**Tests to run**:

- `npm --prefix apps/worker run test -- src/jobs/__tests__/run_mlb_model.test.js --runInBand`
- `rg -n "siera|x_era" apps/worker/src/jobs/run_mlb_model.js apps/worker/src/jobs/__tests__/run_mlb_model.test.js docs/decisions/ADR-0007-mlb-f5-full-model-projection-contract.md`
- `rg -n "x_fip_only_fallback_uses_db_value|x_fip_only_fallback_when_starter_skill_missing|x_fip_only_raw_vs_db_parity" apps/worker/src/jobs/__tests__/run_mlb_model.test.js`

**Manual validation**:

- Validate with these exact fixture-backed test cases and compare pre/post outputs:
  - `x_fip_only_fallback_uses_db_value` (Fixture A, `x_fip` present): absolute delta must be `0.00` vs baseline.
  - `x_fip_only_fallback_when_starter_skill_missing` (Fixture B, starter-skill row missing): fallback path still returns finite projection and stays within `+/-0.05` of baseline.
  - `x_fip_only_raw_vs_db_parity` (Fixture C, raw-data fallback vs DB fallback): parity difference must be `<= 0.01`.

**Guard for WI closeout**:

- Confirm no untracked legacy fallback branches remain in scoped files; if any remain, each must include `TECHDEBT(WI-1172)` plus follow-up criteria.
- Before marking complete: confirm every test command in `Tests to run` is runnable from repo root.
- Do not silently leave ADR/runtime drift in place; docs must match code before closeout.

CLAIM: github-copilot 2026-04-24T00:00:00Z

# WI-1197: Fix watchdog test expectations for stateful alert writes and check-id formatting

**ID**: WI-1197
**Goal**: Align `check_pipeline_health.watchdog.test.js` expectations with current watchdog behavior after state semantics migration.

**Scope**:

- `WORK_QUEUE/WI-1197.md`
- `apps/worker/src/jobs/__tests__/check_pipeline_health.watchdog.test.js`

**Out of scope**:

- Production watchdog job behavior changes
- Pipeline health schema changes
- Web/API changes

**Acceptance**:

- `check_pipeline_health.watchdog.test.js` passes.
- Heartbeat warning test expects both heartbeat and alert_delivery rows.
- Degraded-state reason assertion matches check-id format (`phase:check:scope`).

**Owner agent**: github-copilot
**Time window**: 2026-04-27T00:00:00Z -> TBD
**Coordination flag**: solo
**Depends on**: WI-1196

**Tests to run**:

- `npm --prefix apps/worker run test -- src/jobs/__tests__/check_pipeline_health.watchdog.test.js --runInBand`

**Manual validation**:

- Confirm updated expectations explicitly reference `watchdog/alert_delivery` and `schedule:freshness:global`.

**Guard for WI closeout**:

- Before marking complete: confirm all test commands run from repo root.
- Confirm changed files match Scope only.

CLAIM: github-copilot 2026-04-27T00:00:00Z

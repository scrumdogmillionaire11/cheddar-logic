# WI-0476: Soccer Team Mapping Alias Coverage for Odds Intake

**ID**: WI-0476
**Goal**: Eliminate current `TEAM_MAPPING_UNMAPPED` soccer intake skips by adding canonical aliases for active EPL/UCL clubs and locking coverage with regression tests.

**Depends on**:

- `WORK_QUEUE/WI-0350.md`

**Scope**:

- `WORK_QUEUE/WI-0476.md`
- `WORK_QUEUE/README.md`
- `packages/data/src/normalize.js`
- `packages/data/src/__tests__/normalize.soccer-team-variants.test.js`

**Out of scope**:

- Odds market contract logic changes
- Scheduler/model decision logic changes
- Web UI or card transform changes
- DB schema migrations

**Acceptance**:

1. `resolveTeamVariant()` matches these live unmapped names: `Sporting Lisbon`, `Bodø/Glimt`, `Arsenal`, `Bayer Leverkusen`, `Chelsea`, `Paris Saint Germain`, `Manchester City`, `Real Madrid`.
2. Alias variants are covered for punctuation/diacritics where relevant (`Bodo Glimt`, `Bodo/Glimt`, `Paris Saint-Germain`, `PSG`, `Sporting CP`).
3. Focused data-package regression test passes and fails if any of the above mappings regress.
4. Pull-odds warnings for the listed clubs are no longer emitted as `TEAM_MAPPING_UNMAPPED` after next ingest run.

**Owner agent**: GitHub Copilot
**Time window**: 2026-03-16T00:00:00Z -> TBD
**Coordination flag**: solo

**Tests to run**:

- `npm --prefix packages/data test -- src/__tests__/normalize.soccer-team-variants.test.js`

**Manual validation**:

- Run: `set -a; source .env; set +a; npm --prefix apps/worker run job:pull-odds`
- Confirm logs no longer include `TEAM_MAPPING_UNMAPPED` for the listed soccer clubs.

**Guard for WI closeout**:

- Before marking complete: confirm every test command in `Tests to run` is runnable from repo root.
- Capture one pull-odds KPI summary showing reduced/zero `teamMappingUnmapped` for the targeted clubs.

CLAIM: GitHub Copilot 2026-03-16T00:00:00Z

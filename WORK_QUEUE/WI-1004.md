# WI-1004: Doc consistency sweep — align audit docs to WI-0998 blocker contract

**ID**: WI-1004

**Goal**: Update three audit documents that still contain pre-WI-0998 language, examples, or reason-code vocabulary, so future agents reading them produce output consistent with the current blocker contract.

**Priority**: Low — no runtime impact; risk is that stale doc language re-introduces bad wording in future work items or agent prompts.

**Owner agent**: Unassigned

**Time window**: TBD — pure documentation, no risk.

**Coordination flag**: None — docs only.

**Depends on**: WI-0998 complete (need final canonical vocabulary before sweeping docs).

---

## Background

Three audit documents were not updated as part of WI-0998 because they were marked optional or out of scope:

| File | Stale content |
| ---- | ------------- |
| `docs/audits/human-truthfulness-audit.md` | May reference `EDGE_VERIFICATION_REQUIRED` in example output or describe the old blocker vocabulary |
| `docs/audits/discord-post-surface-one-pager.md` | Examples may show `EDGE_VERIFICATION_REQUIRED` in Discord message format |
| `docs/audits/recovery-path-classification.md` | Recovery path definitions may use old reason-code names |

Any agent that reads these docs when writing a new feature, adding a new blocker, or drafting a work item may inadvertently reintroduce the old vocabulary.

## Changes

For each document:

1. Find all occurrences of `EDGE_VERIFICATION_REQUIRED`, `BLOCK_STALE_DATA`, or any vocabulary that contradicts the WI-0998 decision contract
2. Replace with canonical equivalents (`LINE_NOT_CONFIRMED`, `MARKET_DATA_STALE`, etc.)
3. Update any example Discord messages or card output snippets to show current format
4. Verify recovery path descriptions align with the three-bucket terminal-reason-family values introduced in WI-1002 (if WI-1002 is merged first; otherwise leave family values as-is and add a TODO comment)

## Scope

| File | Change |
| ---- | ------ |
| `docs/audits/human-truthfulness-audit.md` | Replace legacy reason-code language; update examples |
| `docs/audits/discord-post-surface-one-pager.md` | Replace legacy vocabulary in Discord message examples |
| `docs/audits/recovery-path-classification.md` | Align recovery path reason-code names to canonical set |

## Out of scope

- Any production code changes
- `docs/guides/Edge_Thresholds.md` — already updated in WI-0998
- `docs/audits/reason-code-taxonomy.md` — updated in WI-1002 scope
- `docs/DATA_CONTRACTS.md` — updated in WI-0998 scope

## Acceptance

- `grep -r "EDGE_VERIFICATION_REQUIRED\|BLOCK_STALE_DATA" docs/audits/human-truthfulness-audit.md docs/audits/discord-post-surface-one-pager.md docs/audits/recovery-path-classification.md` returns zero results
- Any Discord message examples in these docs show current blocker label text (e.g. `Line not confirmed` not `EDGE_VERIFICATION_REQUIRED`)
- Recovery path classifications reference only codes in the canonical list from `docs/audits/reason-code-taxonomy.md`

## Tests to run

None — documentation only.

## Manual validation

- Read each updated file top to bottom; confirm no `EDGE_VERIFICATION_REQUIRED` references remain
- Cross-check one example in each doc against current Discord card output in dev

# WI-0452: Cloudflare Purge Success Parsing In Deploy Workflow

**ID**: WI-0452
**Goal**: Prevent false deploy failures when Cloudflare purge returns compact JSON without whitespace.

**Scope**:
- `WORK_QUEUE/WI-0452.md`
- `.github/workflows/deploy-production.yml`

**Out of scope**:
- Any deploy logic unrelated to Cloudflare purge response validation.
- Application code, API behavior, and frontend/backend runtime changes.

**Acceptance**:
- Deploy workflow treats both `"success": true` and `"success":true` as successful Cloudflare purge responses.
- Workflow still fails when Cloudflare response does not indicate success.

**Owner agent**: codex
**Time window**: 2026-03-14T14:39:33Z -> TBD
**Coordination flag**: needs-sync

**Tests to run**:
- `rg -n '\"success\":\\s*true' .github/workflows/deploy-production.yml`

**Manual validation**:
- Re-run `Deploy to Production` workflow and confirm it no longer fails at `Cloudflare purge failed` when Cloudflare returns `{"success":true,...}`.

**Guard for WI closeout**:
- Before marking complete: confirm every test command in `Tests to run` is runnable from repo root (i.e., exists in web/package.json scripts or is a valid `node path/to/test.js` command).
- If a script is added to web/package.json as part of this WI, add it to this WI's Tests section with the full `npm --prefix web run <script>` form.

CLAIM: codex 2026-03-14T14:39:33Z

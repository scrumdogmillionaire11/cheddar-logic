# WI-1194: Surface pipeline degradation timing and fail-closed CLV analytics

**ID**: WI-1194
**Goal**: Expose degraded duration and normalized freshness labels in admin health views, and ensure UNKNOWN decision basis is excluded from CLV/calibration reporting paths.

**Scope**:

- `WORK_QUEUE/WI-1194.md`
- `web/src/app/api/admin/pipeline-health/route.ts`
- `web/src/app/admin/page.tsx`
- `web/src/__tests__/api-admin-pipeline-health.test.js`
- `apps/worker/src/jobs/run_clv_snapshot.js`
- `apps/worker/src/jobs/report_telemetry_calibration.js`
- `apps/worker/src/jobs/__tests__/clv_performance.test.js`

**Out of scope**:

- Non-admin user-facing game card UI copy/layout changes
- New DB schema migrations
- Scheduler cadence changes

**Acceptance**:

- Admin pipeline health API returns `time_degraded_minutes` and `freshness_tier` fields for health rows.
- Admin dashboard displays degraded duration and normalized freshness labels (`Fresh`, `Aging`, `Stale`, `Expired`).
- CLV snapshot generation excludes non-`ODDS_BACKED` rows from `clv_ledger`.
- Telemetry calibration CLV reads exclude non-`ODDS_BACKED` rows from `clv_ledger`.
- Scoped tests pass from repo root.

**Owner agent**: github-copilot
**Time window**: 2026-04-27T00:00:00Z -> TBD
**Coordination flag**: needs-sync
**Depends on**: none

**Tests to run**:

- `node web/src/__tests__/api-admin-pipeline-health.test.js`
- `npm --prefix apps/worker run test -- src/jobs/__tests__/clv_performance.test.js --runInBand`
- `npm --prefix apps/worker run test -- src/jobs/__tests__/report_telemetry_calibration.test.js --runInBand`

**Manual validation**:

- Confirm `/api/admin/pipeline-health` rows include `check_id`, `freshness_tier`, and `time_degraded_minutes`.
- Confirm admin snapshot cards show `Degraded for: <minutes>` on active non-ok checks.

**Guard for WI closeout**:

- Before marking complete: confirm all test commands run from repo root.
- Confirm changed files match Scope only.

CLAIM: github-copilot 2026-04-27T00:00:00Z

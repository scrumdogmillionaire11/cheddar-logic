# WI-1194: Surface pipeline degradation timing and fail-closed CLV analytics

**ID**: WI-1194
**Goal**: Expose degraded duration and normalized freshness labels in admin health views, and ensure UNKNOWN decision basis is excluded from CLV/calibration reporting paths.

**Scope**:

- `WORK_QUEUE/WI-1194.md`
- `web/src/app/api/admin/pipeline-health/route.ts`
- `web/src/app/admin/page.tsx`
- `web/src/__tests__/api-admin-pipeline-health.test.js`
- `apps/worker/src/jobs/run_clv_snapshot.js`
- `apps/worker/src/jobs/report_telemetry_calibration.js`
- `apps/worker/src/jobs/__tests__/clv_performance.test.js`
- `apps/worker/src/jobs/__tests__/report_telemetry_calibration.test.js`

**Out of scope**:

- Non-admin user-facing game card UI copy/layout changes
- New DB schema migrations (WI-1194 assumes migration 089 `clv_ledger_unknown_basis_default` is applied)
- Scheduler cadence changes

**Acceptance**:

- Admin pipeline health API returns `time_degraded_minutes` and `freshness_tier` fields for health rows.
- Admin dashboard displays degraded duration and normalized freshness labels (`Fresh`, `Aging`, `Stale`, `Expired`).
- CLV snapshot generation excludes non-`ODDS_BACKED` rows from `clv_ledger`.
- Telemetry calibration CLV reads exclude non-`ODDS_BACKED` rows from `clv_ledger`.

**Owner agent**: codex
**Time window**: 2026-04-27T00:00:00Z -> TBD
**Coordination flag**: needs-sync
**Depends on**: none

**Tests to run**:

- `node web/src/__tests__/api-admin-pipeline-health.test.js` — Verifies API returns required fields and UI renders correctly
- `npm --prefix apps/worker run test -- src/jobs/__tests__/clv_performance.test.js --runInBand` — Verifies CLV snapshot excludes non-ODDS_BACKED rows
- `npm --prefix apps/worker run test -- src/jobs/__tests__/report_telemetry_calibration.test.js --runInBand` — Verifies telemetry calibration excludes non-ODDS_BACKED rows (contains explicit "ignores UNKNOWN decision_basis rows" test)

**Manual validation**:

- Start dev server on `/api/admin/pipeline-health` endpoint; confirm response JSON includes all three fields: `check_id` (string or null), `freshness_tier` (`'Fresh'|'Aging'|'Stale'|'Expired'`), `time_degraded_minutes` (number or null).
- Navigate to admin dashboard at `/admin` (development mode only); confirm at least one active non-ok pipeline health row displays:
  - Freshness tier label in metadata section
  - "Degraded for: X minutes/hours/days" line below it
- Confirm edge cases: resolved rows show `freshness_tier='Fresh'` with no degraded duration; rows older than 120 minutes show `freshness_tier='Expired'`.

**Blockers and warnings**:

- **Blocker**: Do not close WI-1194 unless both CLV read paths (`run_clv_snapshot` and `report_telemetry_calibration`) are verified to exclude non-`ODDS_BACKED` rows using automated tests, not manual inspection.
- **Warning**: This WI and WI-1195 both rely on telemetry calibration CLV-basis coverage; keep execution serialized if either item modifies `apps/worker/src/jobs/__tests__/report_telemetry_calibration.test.js`.
- **Warning**: Keep UI scope constrained to admin health surfacing only; no non-admin card/feed copy or layout changes.
- **Warning**: Requires migration 089 (`clv_ledger_unknown_basis_default.sql`) to be applied before execution; verify schema includes `decision_basis` column on `clv_ledger` with `DEFAULT 'ODDS_BACKED'`.

**Guard for WI closeout**:

- Before marking complete: confirm all test commands run from repo root and pass.
- Confirm changed files match Scope only — no modifications to migrations, lockfiles, or non-scoped files.
- Verify no test regressions in other CLV or pipeline health test suites.
- **Critical**: Inspect `run_clv_snapshot.js` line ~123 and `report_telemetry_calibration.js` lines ~205, ~276, ~540 to confirm `decision_basis = 'ODDS_BACKED'` WHERE clause is in place on all CLV reads.
- Confirm manual validation steps pass: `/api/admin/pipeline-health` response includes all required fields with correct types; admin UI renders freshness tier and degraded duration for active non-ok checks.

CLAIM: codex 2026-05-01T00:00:00Z

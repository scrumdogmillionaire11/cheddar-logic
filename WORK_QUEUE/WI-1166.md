# WI-1166: Cards API Gate Inventory And Drop-Reason Observability

**ID**: WI-1166
**Goal**: Create a complete cards-read gate inventory and add reason-coded drop observability so hidden-card incidents are diagnosable before behavior changes.

**Scope**:

- `web/src/app/api/cards/route.ts`
- `web/src/lib/cards/query.ts`
- `web/src/lib/cards/payload-classifier.ts`
- `web/src/lib/games/projection-surface.ts`
- `web/src/__tests__/cards-projection-exclusion.test.js`
- `web/src/__tests__/api-cards-lifecycle-regression.test.js`
- `WORK_QUEUE/WI-1166.md`

**Out of scope**:

- Changing card eligibility behavior for end users
- Modifying worker-side decision thresholds or projection generation
- DB schema migrations

**Acceptance**:

- Every cards-read gate in scoped files is documented in code comments with explicit purpose and failure reason semantics.
- `/api/cards` emits structured drop-reason diagnostics (internal/debug-safe) with counts by reason and card_type.
- Existing response shape for normal consumers remains backward-compatible.
- Regression tests cover diagnostics paths and ensure no behavior change in visible card output for default requests.

**Owner agent**: codex
**Time window**: 2026-04-24T00:00:00Z -> TBD
**Coordination flag**: needs-sync
**Depends on**: none

**Tests to run**:

- `npm --prefix web run test:cards:projection-exclusion`
- `npm --prefix web run test:cards-lifecycle-regression`
- `npm --prefix web run test:cards-sport-filter`

**Manual validation**:

- Call `/api/cards?sport=nhl` and verify response remains unchanged for standard clients.
- Call the internal/debug diagnostics mode and verify non-empty drop reason counters for at least one suppressed card class.


CLAIM: codex 2026-04-24T00:00:00-04:00

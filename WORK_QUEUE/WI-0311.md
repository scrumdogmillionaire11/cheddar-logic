# WI-0311: Canonical API/Decision Normalization (Web)

**ID**: WI-0311
**Goal**: Solidify the true API-to-UI play contract and isolate legacy fallbacks so decision behavior is deterministic.

**Scope**:

- `web/src/app/api/games/route.ts`
- `web/src/lib/game-card/transform.ts`
- `web/src/lib/game-card/decision.ts`
- `web/src/lib/types/canonical-play.ts`
- `web/src/lib/types/game-card.ts`
- `web/src/__tests__/api-games-repair-budget.test.js`
- `web/src/lib/game-card/__tests__/decision.test.ts`
- `web/src/lib/game-card/__tests__/transform.test.ts`

**Out of scope**:

- New filters/presets UX from WI-0308
- New product labels/messages unrelated to contract normalization

**Acceptance**:

- Explicit canonical field precedence documented and enforced (action first, legacy fallback controlled)
- Repair logic no longer masks root-cause payload issues silently
- Reason-code handling is deterministic and test-covered
- API emits a stable minimum viable play contract for UI consumption
- Regression tests protect against legacy reintroduction

**Owner agent**: unassigned
**Time window**: 2026-03-07T02:00:00Z -> 2026-03-07T14:00:00Z
**Coordination flag**: solo

**Tests to run**:

- `npm --prefix web test -- __tests__/api-games-repair-budget.test.js`
- `npm --prefix web test -- lib/game-card/decision.test.ts`
- `npm --prefix web test -- lib/game-card/transform.test.ts`

**Manual validation**:

- /api/games payload for same card resolves to same display decision across refreshes/runs

CLAIM: unassigned 2026-03-05T00:00:00Z

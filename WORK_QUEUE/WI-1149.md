# WI-1149: Post-Fix Independent Controls And Polish For Each Results View

**Status note**: Narrowed on 2026-04-23. Projection settlement completeness and primary desktop table work moved to `WI-1156`.

**ID**: WI-1149
**Goal**: Give each results view its own purpose-fit controls and post-fix polish after the projection settlement/data contract is corrected.

**Scope**:
- web/src/app/results/page.tsx
- web/src/app/results/projections/page.tsx
- web/src/components/results/ (filter/control components as needed)

**Out of scope**:
- Projection settlement completeness or historical backfills (owned by `WI-1156`)
- Primary desktop projection results table rebuild (owned by `WI-1156`)
- Changes to API route response shapes
- Shared/global filter state lifted to layout (each page owns its own filters)

**Acceptance**:

Betting view (`/results`):
- Existing filters (sport, card category, market, 60%+ confidence toggle) remain and apply only to the betting view.
- Decision tiers and ledger respond to filter changes as before.

Projections view (`/results/projections`):
- Card family selector lets user toggle between All / NHL 1P / MLB F5 (or any active projection families).
- Selecting a family scopes the settlement cards and accuracy metrics to that family.
- Date range or season selector (if data supports it) to scope projection accuracy window.
- Metric labels and controls are refined for readability once the primary projection table/data work from `WI-1156` is in place.

Both views:
- No layout regressions on mobile viewports.
- Page titles and metadata (`<title>`) are distinct: "Betting Results | Cheddar" vs "Projection Accuracy | Cheddar".
- Dead or legacy view-specific controls or presentation code in scoped files that are replaced by the post-fix polish work are removed rather than left dormant.

**Owner agent**: unassigned
**Time window**: TBD
**Coordination flag**: solo
**Depends on**: WI-1148, WI-1156

**Tests to run**:
- npm --prefix web run test:ui:results
- npm --prefix web run lint
- npx --prefix web tsc --noEmit

**Manual validation**:
- On `/results/projections`, selecting "NHL 1P" shows only NHL 1P settlement cards and accuracy metrics.
- Switching to "MLB F5" swaps in MLB F5 data.
- Selecting "All" restores combined view.
- Accuracy metric labels are legible and not cryptic (MAE has a unit, Brier score has a brief descriptor).
- Both pages look correct at 375px width (mobile).

**Guard for WI closeout**:
- Before marking complete: confirm every test command in Tests to run is runnable from repo root.
- Manually verify page `<title>` tags differ between the two routes.
- Confirm superseded controls/presentation code in scoped files was removed or explicitly documented as intentionally retained.

CLAIM: unassigned 2026-04-22T00:00:00Z

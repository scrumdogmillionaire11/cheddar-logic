# WI-1149: Post-Fix Independent Controls And Polish For Each Results View

**Status note**: Narrowed on 2026-04-23. Projection settlement completeness and primary desktop table work moved to `WI-1156`. Per operator instruction, this item is executable on the current working branch without worktrees; `WI-1156` is a coordination reference, not a blocking dependency.

**Closeout note 2026-04-24**: Split `/results` into betting-only controls, summary, decision tiers, and ledger. Added `/results/projections` for projection settlement and accuracy with All / active NHL 1P / MLB F5 family controls that scope both visible rows and recalculated metrics. Date/season filtering was omitted because the current projection accuracy row contract does not expose a reliable row-level date or season key aligned with settled rows; adding that belongs with the WI-1156 data-contract/table lane.

**ID**: WI-1149
**Goal**: Give each results view its own purpose-fit controls and post-fix polish after the projection settlement/data contract is corrected.

**Scope**:
- web/src/app/results/page.tsx
- web/src/app/results/layout.tsx
- web/src/app/results/projections/page.tsx
- web/src/components/results/ (filter/control components as needed)

**Out of scope**:
- Projection settlement completeness or historical backfills (owned by `WI-1156`)
- Primary desktop projection results table rebuild (owned by `WI-1156`)
- Changes to API route response shapes
- Shared/global filter state lifted to layout (each page owns its own filters)
- DB migrations, worker writes, backfills, or settlement recomputation

**Acceptance**:

Betting view (`/results`):
- Existing filters (sport, card category, market, 60%+ confidence toggle) remain and apply only to the betting view.
- Decision tiers and ledger respond to filter changes as before.

Projections view (`/results/projections`):
- Card family selector lets user toggle between All / NHL 1P / MLB F5 (or any active projection families).
- Selecting a family scopes the settlement cards and accuracy metrics to that family.
- Date range or season selector (if data supports it) to scope projection accuracy window.
- Metric labels and controls are refined for readability using currently available projection result data; do not rebuild the primary desktop projection table owned by `WI-1156`.

Both views:
- No layout regressions on mobile viewports.
- Page titles and metadata (`<title>`) are distinct: "Betting Results | Cheddar" vs "Projection Accuracy | Cheddar".
- Dead or legacy view-specific controls or presentation code in scoped files that are replaced by the post-fix polish work are removed rather than left dormant.

**Owner agent**: Codex
**Time window**: 2026-04-24T12:08:26Z -> TBD
**Coordination flag**: solo
**Depends on**: WI-1148

## Must-haves (goal-backward)

**truths**:
- `/results` is a betting-only results view with sport, card category, market, and 60%+ confidence filters scoped only to betting results.
- `/results/projections` is a projection accuracy view with family controls scoped to projection settlement rows and projection accuracy metrics.
- Selecting All / NHL 1P / MLB F5, or active projection families from the settled data contract, changes both visible settlement rows and accuracy metrics.
- Date range or season control is implemented when supported by the data contract; if unsupported, the UI explicitly omits it and documents the reason in this WI closeout.
- Browser titles are distinct: `Betting Results | Cheddar` and `Projection Accuracy | Cheddar`.

**artifacts**:
- `web/src/app/results/layout.tsx` or route-level metadata provides the betting title without forcing the projection title.
- `web/src/app/results/page.tsx` contains only betting result controls, betting summaries, decision tiers, and ledger rendering.
- `web/src/app/results/projections/page.tsx` owns projection fetch state, projection controls, projection accuracy metrics, and projection settlement rendering.
- `web/src/components/results/*` contains any extracted projection controls needed to avoid duplicated family/date selector logic.

**key_links**:
- Betting filter state -> `/api/results` query params -> betting decision tiers and ledger only.
- Projection family selector -> filtered settled projection rows -> `ProjectionResultsTable`.
- Projection family selector -> filtered projection accuracy rows/summary -> accuracy metrics.
- Route metadata -> distinct browser titles for `/results` and `/results/projections`.

## Execution tasks

<tasks>

<task type="auto">
  <name>Task 1: Split betting and projection route ownership</name>
  <files>web/src/app/results/page.tsx, web/src/app/results/projections/page.tsx, web/src/app/results/layout.tsx</files>
  <action>
    Move projection-only fetch state, projection confidence/table filters, settlement panels, and projection accuracy rendering out of `/results` into `/results/projections`. Keep betting filters and betting ledger behavior on `/results`. Implement distinct route metadata titles without using metadata exports from client components.
  </action>
  <verify>
    <automated>npx --prefix web tsc --noEmit --project web/tsconfig.json</automated>
  </verify>
  <done>
    `/results` renders betting-only controls and `/results/projections` renders the projection surface with distinct page titles.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add projection family and window controls</name>
  <files>web/src/app/results/projections/page.tsx, web/src/components/results/*</files>
  <action>
    Add an All / active-family selector derived from settled projection rows or the WI-1156 supported-family contract. Filter both settlement rows and projection accuracy rows/metrics by the selected family. Add a date range or season selector only if the landed API/data fields support reliable filtering; otherwise document the unsupported state in closeout.
  </action>
  <verify>
    <automated>npm --prefix web run test:ui:results</automated>
  </verify>
  <done>
    Family selection scopes both projection settlement cards/table rows and displayed accuracy metrics.
  </done>
</task>

<task type="auto">
  <name>Task 3: Polish labels and remove superseded view code</name>
  <files>web/src/app/results/page.tsx, web/src/app/results/projections/page.tsx, web/src/components/results/*</files>
  <action>
    Refine projection metric labels, including MAE units and Brier descriptor where present. Remove dormant view-specific controls or presentation branches replaced by the split routes. Preserve mobile layout at 375px.
  </action>
  <verify>
    <automated>npm --prefix web run lint && npm --prefix web run test:ui:results && npx --prefix web tsc --noEmit --project web/tsconfig.json</automated>
  </verify>
  <done>
    Projection metrics are readable, no replaced controls remain dormant, and mobile validation passes manually.
  </done>
</task>

</tasks>

**Tests to run**:
- npm --prefix web run test:ui:results
- npm --prefix web run lint
- npx --prefix web tsc --noEmit --project web/tsconfig.json

**Manual validation**:
- On `/results/projections`, selecting "NHL 1P" shows only NHL 1P settlement cards and accuracy metrics.
- Switching to "MLB F5" swaps in MLB F5 data.
- Selecting "All" restores combined view.
- Accuracy metric labels are legible and not cryptic (MAE has a unit, Brier score has a brief descriptor).
- Both pages look correct at 375px width (mobile).
- Browser title for `/results` is "Betting Results | Cheddar".
- Browser title for `/results/projections` is "Projection Accuracy | Cheddar".

**Guard for WI closeout**:
- Before marking complete: confirm every test command in Tests to run is runnable from repo root.
- Manually verify page `<title>` tags differ between the two routes.
- Confirm superseded controls/presentation code in scoped files was removed or explicitly documented as intentionally retained.

CLAIM: Codex 2026-04-24T12:08:26Z

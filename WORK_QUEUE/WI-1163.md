# WI-1163: Discord Posting Eligibility Enforced By Canonical Publish Status

**ID**: WI-1163
**Goal**: Switch Discord routing/filtering to canonical publish status so only PLAY and SLIGHT EDGE cards are posted while PASS/BLOCKED cards are suppressed.

**Requirements**: [DISCORD-HOOK-03, DISCORD-HOOK-04]

**Roadmap traceability**:
- Executes phase plan: `.planning/phases/discord-hook-eligibility/discord-hook-eligibility-02-PLAN.md`
- Depends on WI-1162 canonical publisher contract.

**Scope**:

- `apps/worker/src/jobs/post_discord_cards.js`
- `apps/worker/src/jobs/__tests__/post_discord_cards.test.js`
- `WORK_QUEUE/WI-1163.md`

**Out of scope**:

- Redesigning Discord message copy/style beyond status gating behavior
- Deleting all legacy fallback behavior in one shot
- Changing model scoring or execution-gate thresholds

**Acceptance**:

- [DISCORD-HOOK-03] Discord classifier/filtering reads canonical `webhook_publish_status` first, with rollout-safe fallback for payloads that predate WI-1162.
- [DISCORD-HOOK-04] Canonical payloads produce outgoing Discord content containing only PLAY and SLIGHT EDGE cards.
- PASS/BLOCKED (`PASS_BLOCKED`) cards never render in outgoing posted lines.
- Regression tests cover mixed-card fixtures across game lines, game props, and player props.

**Owner agent**: unassigned
**Time window**: 2026-04-24T00:00:00Z -> TBD
**Coordination flag**: solo
**Depends on**: WI-1162

## Execution tasks

<tasks>

<task type="auto">
  <name>Task 1: Move Discord bucket/eligibility routing to canonical publish status</name>
  <requirements>DISCORD-HOOK-03</requirements>
  <files>apps/worker/src/jobs/post_discord_cards.js</files>
  <action>
    Update Discord classification and displayability decisions to use `webhook_publish_status` as the primary signal.
    Keep existing legacy fallback path for older payload rows that do not carry canonical status.
  </action>
  <verify>
    <automated>npm --prefix apps/worker run test -- --runInBand src/jobs/__tests__/post_discord_cards.test.js</automated>
  </verify>
  <done>
    Canonical-status payloads route deterministically through PLAY and SLIGHT EDGE sections with PASS/BLOCKED excluded.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add mixed-market regression tests for PLAY/SLIGHT EDGE-only output</name>
  <requirements>DISCORD-HOOK-04</requirements>
  <files>apps/worker/src/jobs/__tests__/post_discord_cards.test.js</files>
  <action>
    Add assertions showing mixed fixtures with canonical status produce outgoing lines only for PLAY and SLIGHT EDGE, while PASS_BLOCKED rows never render.
    Include one legacy-no-canonical fixture to verify fallback safety.
  </action>
  <verify>
    <automated>npm --prefix apps/worker run test -- --runInBand src/jobs/__tests__/post_discord_cards.test.js</automated>
  </verify>
  <done>
    Tests guarantee canonical payload behavior matches PLAY + SLIGHT EDGE-only posting policy.
  </done>
</task>

</tasks>

**Tests to run**:

- `npm --prefix apps/worker run test -- --runInBand src/jobs/__tests__/post_discord_cards.test.js`
- `rg -n "webhook_publish_status|classifyDecisionBucket|isDisplayableWebhookCard|passesLeanThreshold" apps/worker/src/jobs/post_discord_cards.js`

**Manual validation**:

- Run a Discord snapshot build and confirm only PLAY and Slight Edge sections contain posted cards.
- Confirm cards with canonical `PASS_BLOCKED` are absent from outgoing lines.

**Guard for WI closeout**:

- Before marking complete: confirm every test command in `Tests to run` is runnable from repo root.
- Confirm canonical payload behavior posts only PLAY and SLIGHT EDGE.
- Confirm fallback behavior for non-canonical historical payloads still works.
- Confirm each requirement ID has implementation evidence in code and tests.

CLAIM: unassigned 2026-04-24T00:00:00Z

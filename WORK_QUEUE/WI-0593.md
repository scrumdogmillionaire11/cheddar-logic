# WI-0593: Restore recurring settlement sweeps in scheduler

**ID**: WI-0593
**Goal**: Fix settlement scheduler idempotency so production continues to ingest final scores and grade cards after the first successful run, while preserving overlap protection.

**Scope**:
- `apps/worker/src/schedulers/main.js`
- `packages/data/src/db.js`
- `packages/data/index.js`
- `apps/worker/src/__tests__/integration/job-run-claim.test.js`
- `apps/worker/src/__tests__/scheduler-windows.test.js`

**Out of scope**:
- Settlement grading logic in `apps/worker/src/jobs/settle_pending_cards.js`
- Matching logic in `apps/worker/src/jobs/settle_game_results.js`
- Web/API rendering changes

**Acceptance**:
- Scheduler settlement jobs use recurring window-scoped job keys instead of permanent global success keys.
- Settlement overlap protection still blocks concurrent duplicate execution.
- The 02:00 ET nightly sweep owns settlement for that minute; hourly settlement does not double-enqueue.
- Regression coverage proves a prior successful settlement window does not block later windows.

**Owner agent**: codex
**Time window**: 2026-03-24T00:00:00Z -> TBD
**Coordination flag**: needs-sync

**Tests to run**:
- `npm --prefix apps/worker run test -- src/__tests__/scheduler-windows.test.js`
- `npm --prefix apps/worker run test -- src/__tests__/integration/job-run-claim.test.js`

**Manual validation**:
- Load production env and run `npm --prefix apps/worker run job:settlement-report -- --json --days=2 --limit=20`.
- If actionable unsettled rows are present, run `npm --prefix apps/worker run job:settle-games` then `npm --prefix apps/worker run job:settle-cards`, then verify fresh success rows in `job_runs`.

**Guard for WI closeout**:
- Before marking complete: confirm every test command in `Tests to run` is runnable from repo root.
- Do not expand scope without first updating this WI.

CLAIM: codex 2026-03-24T00:00:00Z

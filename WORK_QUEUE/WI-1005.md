# WI-1005: Fix stale-odds backstop game-id matching

**ID**: WI-1005
**Goal**: Ensure `refresh_stale_odds` refreshes stale games whose stored `game_id` already matches the Odds API id, including ids containing hyphens.

**Scope**:
- `apps/worker/src/jobs/refresh_stale_odds.js`
- `apps/worker/src/jobs/__tests__/refresh_stale_odds.test.js`

**Out of scope**:
- Scheduler cadence changes
- Pipeline health alert policy changes
- Odds provider market configuration changes
- Production environment changes

**Acceptance**:
- Stale-game matching preserves exact `game_id` values before considering legacy prefixed ids.
- Hyphenated Odds API ids are not truncated by backstop filtering.
- Regression tests cover exact ids, legacy prefixed ids, and unrelated ids.

**Owner agent**: codex
**Time window**: 2026-04-18T00:00:00Z -> 2026-04-18T23:59:59Z
**Coordination flag**: solo

**Tests to run**:
- `npm --prefix apps/worker run test -- src/jobs/__tests__/refresh_stale_odds.test.js --runInBand`

**Manual validation**:
- Confirm changed files match this work item's scope.

CLAIM: codex 2026-04-18T00:00:00Z

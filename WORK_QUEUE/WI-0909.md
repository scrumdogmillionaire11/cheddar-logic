# WI-0909: NHL Player Shots (Full-Game) Settlement Source Unification

**ID**: WI-0909
**Goal**: Unify full-game NHL player-shots settlement so projection actual_result and card_results grading use a single consistent stat source and reconciliation rules.

**Depends on**: none

**Scope**:

- apps/worker/src/jobs/settle_projections.js
- apps/worker/src/jobs/settle_pending_cards.js
- apps/worker/src/jobs/nhl-settlement-source.js
- `apps/worker/src/jobs/__tests__/settle_projections.test.js`
- `apps/worker/src/jobs/__tests__/settle_pending_cards.phase2.test.js`

**Out of scope**:

- NHL player-shots model scoring thresholds
- Odds ingestion changes
- Frontend card presentation changes

**Acceptance**:

- Full-game nhl-player-shots uses one declared source-of-truth path for settlement values, with deterministic fallback behavior when data is missing.
- actual_result.shots and card_results result cannot silently disagree for the same card without explicit error/reason metadata.
- Settlement errors for source mismatch are observable and test-covered.
- Tests include at least one mismatch fixture that would have previously diverged.

**Owner agent**: unassigned
**Time window**: 2026-04-11 -> TBD
**Coordination flag**: needs-sync

**Tests to run**:

- npm --prefix apps/worker run test -- --testPathPattern="settle_projections"
- npm --prefix apps/worker run test -- --testPathPattern="settle_pending_cards.phase2"
- npm --prefix apps/worker run test -- --testPathPattern="settle_pending_cards.market-contract"

**Manual validation**:

- For one completed NHL game/player, compare stored actual_result.shots and grading inputs used by settle_pending_cards and verify they match or produce explicit mismatch telemetry.

CLAIM: unassigned

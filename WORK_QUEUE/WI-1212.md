# WI-1212: Remove or Wire Legacy Helpers in Results Transform Layer (With Runtime Proof)

**ID**: WI-1212

**Goal**:
Eliminate or formalize legacy helper paths in `transform-layer.ts` without breaking real runtime behavior, ensuring the results pipeline remains fail-closed and contract-consistent.

**Scope**:

- `web/src/lib/results/transform-layer.ts`
- `web/src/__tests__/api-results-decision-segmentation.test.js`
- `web/src/__tests__/` (new transform-layer behavioral tests if required)

**Out of scope**:

- Results aggregation redesign
- Worker or settlement changes
- DecisionOutcome contract changes

## Required Step 1 — Runtime Reachability Audit (MANDATORY BEFORE CODE CHANGES)

For each helper:

- `hasDecisionV2`
- `resolveLegacyMlbResultsTier`
- `canUseLegacyMlbResultsAdapter`

Determine:

1. Is the helper reachable from:
   - `/api/results` runtime execution
   - any test-covered path
   - any legacy adapter path
2. Provide evidence via one of:
   - call path trace, or
   - runtime instrumentation, or
   - controlled test proving invocation

Deliver this table before any code edits:

| Helper | Reachable? | From Where | Evidence |
| --- | --- | --- | --- |

No code changes are allowed until this table is complete.

## Required Step 2 — Explicit Disposition

For each helper, choose exactly one disposition.

### A. Remove (preferred)

Conditions:

- Not reachable in runtime
- Not required for legacy adapter path

Requirements:

- Delete helper
- Remove all imports and usages
- Tests still pass

### B. Retain + Wire (only if proven necessary)

Conditions:

- Required for MLB legacy adapter or another active path

Requirements:

- Helper is invoked in transform-layer runtime
- Direct behavioral tests cover invocation and behavior
- No fallback decision logic is introduced

## Critical Constraint (Non-Negotiable)

Do not reintroduce decision-tier fallback from:

- action
- classification
- legacy fields

Decision-tier resolution must remain:

`DecisionOutcome -> canonical mapping -> fail-closed`

**Acceptance**:

- Each helper has reachability proof and explicit disposition (remove or retain)
- No unreachable helpers remain
- No fallback decision logic exists in `transform-layer`
- Results API behavior remains unchanged for `PLAY`, `LEAN`, `PASS`, `INVALID`
- MLB legacy adapter behavior, if retained, remains scoped and explicit
- All existing tests pass
- New tests are added only where behavior is retained

**Priority**: Medium

**Owner agent**: codex
**Time window**: 2026-04-29T15:00:00Z -> 2026-04-30T23:59:59Z
**Coordination flag**: needs-sync
**Depends on**: none

**Tests to run**:

- `cd web && node src/__tests__/api-results-decision-segmentation.test.js`
- `cd web && node --import tsx/esm src/__tests__/api-results-no-implicit-actionable-fallback.test.ts`
- `cd web && node src/__tests__/api-results-mlb-legacy-adapter.test.js`
- Any new WI-specific tests (must be listed before closeout)

**Manual validation**:

- Call `/api/results?limit=50`
- Verify segment counts reconcile with ledger
- Verify decision tiers match expectations
- Verify no unexpected PASS inflation
- Verify INVALID behavior remains fail-closed

**Guard for WI closeout**:

- Reachability audit completed before code changes
- No helper removed without proof of non-use
- No fallback paths introduced
- No unrelated file diffs
- Transform-layer remains single-source-of-truth consumer, not interpreter
- Run closeout grep:
  - `rg "action|classification|legacy|fallback|decision_v2\.official_status" web/src/lib/results/transform-layer.ts`
  - Any remaining hit must be explained as canonical input read, scoped MLB legacy adapter, test-only reference, or removed

CLAIM: codex 2026-04-29T15:00:00Z

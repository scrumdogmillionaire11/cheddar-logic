# WI-1212: Remove or Wire Unused Legacy Helpers in Results Transform Layer

**ID**: WI-1212
**Goal**: Eliminate stale, unexercised legacy helpers in the results transform layer (or wire them with tests if truly required) to reduce contract ambiguity and maintenance risk.

**Scope**:

- `web/src/lib/results/transform-layer.ts`
- `web/src/__tests__/api-results-decision-segmentation.test.js` (if contract coverage needs adjustment)
- `web/src/__tests__/` (new focused transform-layer tests if needed)

**Out of scope**:

- Large redesign of results aggregation flow
- Settlement pipeline changes in worker
- Projection accuracy route refactors

**Acceptance**:

- The unused helper paths identified in concerns are resolved explicitly:
  - `hasDecisionV2`
  - `resolveLegacyMlbResultsTier`
  - `canUseLegacyMlbResultsAdapter`
- For each helper, exactly one of the following is true:
  - removed as dead code, with no behavior regression
  - retained and actively used by runtime path, with direct behavioral tests
- No unreachable decision-tier fallback logic remains in `transform-layer`
- Results decision-tier behavior remains unchanged for active contracts (`PLAY`, `LEAN`, `PASS`, `INVALID` handling)
- Existing results API tests pass after cleanup

**Priority**: Medium

**Owner agent**: unassigned
**Time window**: TBD
**Coordination flag**: solo
**Depends on**: WI-1210 (serialized due to shared `/api/results` transform/query surface)

**Tests to run**:

- `npm --prefix web run test:api:results:decision-segmentation`
- `npm --prefix web run test:api:results:no-implicit-actionable-fallback`
- Any new WI-specific transform-layer behavioral tests

**Manual validation**:

- Verify no references remain to removed helper symbols (if deleted)
- Call `/api/results?limit=50` and confirm segment families + ledger decision labels still reconcile with summary totals
- Confirm no change to fail-closed behavior for missing `decision_v2`

**Guard for WI closeout**:

- Dead helper disposition (delete or wire) is explicit and test-covered
- No source-string-only assertions are introduced as primary coverage
- No unrelated file diffs outside scope
- Results API behavior matches pre-cleanup contract expectations

CLAIM: unassigned

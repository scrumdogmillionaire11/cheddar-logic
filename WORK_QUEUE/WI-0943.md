# WI-0943: MLB Runner Obvious Dead-Code Cleanup

**ID**: WI-0943
**Goal**: Remove clearly unused imports, dead configuration, dead helper code, and noisy non-essential logging from the MLB runner without changing behavior.

**Scope**:

- `apps/worker/src/jobs/run_mlb_model.js`
- `apps/worker/src/jobs/mlb-runner-constants.js`
- `WORK_QUEUE/WI-0943.md`

**Out of scope**:

- Behavior changes to MLB market selection, gating, or write flow
- Refactors of `withoutOddsMode` or trust-class orchestration
- Cross-model cleanup in NBA or NHL runners

**Acceptance**:

- `run_mlb_model.js` no longer contains unused `getOddsSnapshots` / `getLatestOdds` imports.
- `run_mlb_model.js` no longer contains the unused `MLB_K_PROP_ODDS_MAX_AGE_MINUTES` constant.
- `run_mlb_model.js` no longer exports or defines the unused `generateMLBCard` helper or its helper-only imports.
- `run_mlb_model.js` no longer imports or instantiates the unused MLB model object.
- Oversized MLB runner log payloads are summarized while preserving their event tags and key diagnostic fields.
- Large static MLB lookup/config constants are extracted from `run_mlb_model.js` into a dedicated module without changing behavior.
- `node --check apps/worker/src/jobs/run_mlb_model.js` passes.
- Targeted MLB runner test passes.

**Owner agent**: GitHub Copilot
**Time window**: 2026-04-14T00:00:00Z -> TBD
**Coordination flag**: solo

**Tests to run**:

- `node --check apps/worker/src/jobs/run_mlb_model.js`
- `npm --prefix apps/worker run test -- --runInBand src/jobs/__tests__/run_mlb_model.test.js`

**Manual validation**:

- Review the import block, extracted constants module, and key MLB log tags in `run_mlb_model.js` and confirm the cleanup is behavior-neutral.

**Guard for WI closeout**:

- Before marking complete: confirm every test command in `Tests to run` is runnable from repo root.

CLAIM: GitHub Copilot 2026-04-14T00:00:00Z

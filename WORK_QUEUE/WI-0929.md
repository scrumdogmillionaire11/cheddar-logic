# WI-0929: Remove legacyPricingReady cross-check from audit_invariants.js

**ID**: WI-0929
**Goal**: Remove the `legacyPricingReady` parallel cross-check in `audit_invariants.js` now that `_pricing_state.status` is the canonical pricing signal and `pricing_ready` boolean is verified consistent in all model outputs.

**Depends on**: Confirm all three models (NHL, NBA, MLB) produce `pricing_ready` consistent with `_pricing_state.status` before executing — check via grep or test.

**Scope**:

- `apps/worker/src/audit/audit_invariants.js`

**Out of scope**:

- Removing the `pricing_ready` field itself from model outputs — that is a separate, larger migration
- `apps/worker/src/audit/build_audit_snapshot.js` — leave `pricing_ready` population as-is
- `packages/models/src/decision-pipeline-v2.js` — leave `pricing_ready` pass-through as-is

**Acceptance**:

- The `legacyPricingReady` variable and the `if (legacyPricingReady !== null ...)` failure push removed from `checkExecutionStateConsistency()`
- `grep -n "legacyPricingReady" apps/worker/src/audit/audit_invariants.js` returns 0 matches
- `npm --prefix apps/worker run test -- --no-coverage` passes

**Owner agent**: unassigned
**Time window**: TBD
**Coordination flag**: solo

**Tests to run**:

- `npm --prefix apps/worker run test -- --no-coverage`

**Manual validation**:

- Run a full model cycle locally; confirm no audit INV-006 violations introduced

CLAIM: unassigned

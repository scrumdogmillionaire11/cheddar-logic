# WI-0479: Phase 2 Preflight + Baseline Evidence (Thresholds V2)

**ID**: WI-0479
**Goal**: Capture Phase 2 preflight and baseline decision evidence with `ENABLE_MARKET_THRESHOLDS_V2=false` as the control reference.

**Depends on**:

- `WORK_QUEUE/COMPLETE/WI-0473.md`

**Scope**:

- `WORK_QUEUE/WI-0479.md`
- `docs/API_BASELINES.md`

**Out of scope**:

- Enabling `ENABLE_MARKET_THRESHOLDS_V2=true`
- Editing runbooks or env files
- CLV/settlement changes

**Acceptance**:

1. `docs/API_BASELINES.md` includes a Phase 2 preflight checklist and baseline capture procedure.
2. Baseline evidence requirements are documented for at least NBA and NCAAM decision samples.
3. Go/no-go gate for Phase 2 activation is explicitly documented.

**Owner agent**: unassigned
**Time window**: TBD -> TBD
**Coordination flag**: solo

**Tests to run**:

- `npm --prefix web run test:card-decision`
- `npm --prefix web run test:api:games:market`
- `ENABLE_MARKET_THRESHOLDS_V2=false npm --prefix apps/worker run job:run-nba-model:test`
- `ENABLE_MARKET_THRESHOLDS_V2=false npm --prefix apps/worker run job:run-ncaam-model:test`

**Manual validation**:

- Save one representative decision sample per sport (NBA, NCAAM) with flag off.
- Confirm parity with current expected decision semantics before activation WI begins.

**Guard for WI closeout**:

- Before marking complete: confirm every test command in `Tests to run` is runnable from repo root.
- Attach baseline evidence references in this WI before handoff to WI-0480.

---

## Preflight Execution Record

**Executed**: 2026-03-16
**Re-validated after fix**: 2026-03-16

### Tests to run (repo root)

- `npm --prefix web run test:card-decision` → **PASS**
- `npm --prefix web run test:api:games:market` → **PASS**
- `ENABLE_MARKET_THRESHOLDS_V2=false npm --prefix apps/worker run job:run-nba-model:test` → **PASS** (`jobRunId`: `job-nba-model-2026-03-17T01:25:36-e94dab78`)
- `ENABLE_MARKET_THRESHOLDS_V2=false npm --prefix apps/worker run job:run-ncaam-model:test` → **PASS** (`jobRunId`: `job-ncaam-model-2026-03-17T01:25:36-a252742a`)

All commands are runnable from repo root and now pass.

### Baseline Evidence References (Flag Off)

- NBA sample — `sport`: `nba`; `game_id`: `8951b3300ad3588888cb2397ea770d5e`; `card_id`: `card-nba-baseProjection-8951b3300ad3588888cb2397ea770d5e-79a9bac7`; `run_id`: `job-nba-model-2026-03-17T01:09:06-f6bc10e0`; `confidence`: `0.7`
- NCAAM sample — `sport`: `ncaam`; `game_id`: `1e9a55a9c7539299852f3917b14f07c2`; `card_id`: `card-ncaam-baseProjection-moneyline-1e9a55a9c7539299852f3917b14f07c2-9b0854fb`; `run_id`: `job-ncaam-model-2026-03-17T01:09:10-c91e5c04`; `confidence`: `0.65`

### Go/No-Go Result for WI-0480 Handoff

**GO**. Preflight baseline and command verification requirements are satisfied with flag OFF; WI-0480 may proceed.

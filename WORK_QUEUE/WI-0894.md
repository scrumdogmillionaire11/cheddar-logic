# WI-0894: Remove or Restore Dead Soccer/NCAAM Model Runner Surfaces

**ID**: WI-0894
**Goal**: Resolve dead execution surfaces where package scripts reference missing runner files, causing false capability claims and operator confusion.

**Depends on**: none

**Scope**:
- apps/worker/package.json
- apps/worker/src/schedulers/main.js
- docs/runbooks/
- docs/

**Out of scope**:
- Building full soccer or NCAAM production model pipelines
- New sports ingestion integrations
- Changes to active NHL/NBA/MLB/NFL runners

**Acceptance**:
- All worker scripts referencing model runners point to real files.
- If soccer/NCAAM are deferred, scripts and docs explicitly mark them disabled and non-runnable.
- Scheduler and runbook language matches actual executable sports.
- No dangling run/test commands remain for missing modules.

**Owner agent**: unassigned
**Time window**: 2026-04-11 -> TBD
**Coordination flag**: solo

**Tests to run**:
- npm --prefix apps/worker run job:dev-refresh
- npm --prefix apps/worker run job:refresh-game-lines

**Manual validation**:
- Execute each model-runner script command from package.json and verify none fail due to missing module imports.
- Confirm scheduler output only advertises sports with runnable jobs.

CLAIM: unassigned
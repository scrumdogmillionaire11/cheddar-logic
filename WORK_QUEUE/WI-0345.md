# WI-0345: Web API + Transform v2 Pass-Through Hard Cut

**ID**: WI-0345
**Goal**: Make `/api/games` and transform pipeline strict consumers of worker `decision_v2` for wave-1 eligible rows, removing legacy repair/inference/fallback in that path.

**Scope**:
- `WORK_QUEUE/WI-0345.md`
- `web/src/app/api/games/route.ts`
- `web/src/lib/game-card/transform.ts`
- `web/src/lib/types/game-card.ts`
- `web/src/__tests__/api-games-repair-budget.test.js`
- `web/src/__tests__/integration/games-pipeline-contract.test.ts`

**Out of scope**:
- `apps/worker/**`
- `web/src/components/cards-page-client.tsx`
- Props/futures/out-of-scope sport migration

**Acceptance**:
- `/api/games` wave-1 rows pass through worker `decision_v2` without verdict synthesis.
- Wave-1 path no longer uses repair allowlists/title inference/action-status fallback.
- Transform uses `decision_v2` as authoritative verdict/tier/reason for wave-1 rows.

**Owner agent**: Codex
**Time window**: 2026-03-08T00:00:00Z -> TBD
**Coordination flag**: needs-sync

**Tests to run**:
- `npm --prefix web run test:transform:truth-price`
- `npm --prefix web run test:api:games:repair-budget`
- `npm --prefix web run test:transform:market`

**Manual validation**:
- Query `/api/games` and verify wave-1 PLAY rows include `decision_v2` and omit repair metadata.

CLAIM: Codex 2026-03-08T16:20:00Z

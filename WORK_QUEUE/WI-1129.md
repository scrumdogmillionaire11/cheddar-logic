# WI-1129: Consolidate Security Header Policy Ownership

**ID**: WI-1129
**Goal**: Remove duplicate security-header definitions and establish one authoritative policy source.

**Scope**:
- web/src/middleware.ts
- web/src/lib/api-security/security-headers.ts
- web/src/lib/api-security/index.ts
- web/src/__tests__/security-headers-contract.test.js

**Out of scope**:
- CSP policy expansion unrelated to deduplication
- Frontend visual or layout changes

**Tasks**:

1. **Create `web/src/__tests__/security-headers-contract.test.js`** (new file — must be authored, not just run)
   - Import `SECURITY_HEADERS` from `web/src/lib/api-security/security-headers.ts`
   - Assert all 8 expected header names are present as keys in the canonical `SECURITY_HEADERS` object
   - Assert the CSP string contains `default-src 'self'`, `frame-ancestors 'none'`, `form-action 'self'`
   - Assert `Strict-Transport-Security` value contains `max-age=31536000`
   - Use `node:assert` (no test runner dependency) so the command `node web/src/__tests__/security-headers-contract.test.js` runs from repo root

2. **Delete inline `SECURITY_HEADERS` const from `web/src/middleware.ts`** (lines 35–56) and replace with import from `web/src/lib/api-security/security-headers.ts`
   - `security-headers.ts` only uses `process.env` — it is Edge-runtime safe; no Node.js-only APIs
   - The comment "Security headers inlined to avoid any Edge-runtime module resolution issues" must be removed; replace with `// Security headers sourced from canonical module (security-headers.ts — Edge-runtime safe)`
   - Keep the existing `cspConnectSrc` / `cspScriptSrc` local vars only if they are still needed for the dev-only path; if `security-headers.ts` already exports the same dev-aware `SECURITY_HEADERS`, delete them from middleware entirely

3. **Remove dead exports from `web/src/lib/api-security/security-headers.ts`**
   - `createSecurityHeaders()` — zero callers, remove function and its JSDoc
   - `SECURITY_HEADERS_INFO` — zero callers, remove const and its JSDoc
   - Do not touch any other export


**Acceptance**:
- Middleware and API security modules derive headers from a single canonical configuration path.
- Dead helper code is removed or explicitly wired with active call sites.
- Security header regression test verifies expected header set remains unchanged.

**Owner agent**: unassigned
**Time window**: 2026-04-21T00:00:00Z -> TBD
**Coordination flag**: needs-sync
**Depends on**: WI-1127

**Tests to run**:
- node web/src/__tests__/security-headers-contract.test.js
- npm --prefix web run lint
- npm --prefix web run build (confirms middleware.ts compiles cleanly under Edge runtime after import change)

**Manual validation**:
- Verify representative HTML and API responses include expected security headers.

**Guard for WI closeout**:
- Before marking complete: confirm every test command in Tests to run is runnable from repo root (i.e., exists in web/package.json scripts or is a valid node path/to/test.js command).
- If a script is added to web/package.json as part of this WI, add it to this WI's Tests section with the full npm --prefix web run script form.

CLAIM: unassigned 2026-04-21T00:00:00Z

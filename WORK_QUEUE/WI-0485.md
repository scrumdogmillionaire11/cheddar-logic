# WI-0485: Phase 4 telemetry calibration report + enforcement gate

**ID**: WI-0485
**Goal**: Add a deterministic telemetry calibration report command that summarizes projection win-rate and CLV health with optional non-zero exit enforcement for go/no-go decisions.

**Depends on**:

- `WORK_QUEUE/COMPLETE/WI-0480.md`
- `WORK_QUEUE/COMPLETE/WI-0481.md`
- `WORK_QUEUE/COMPLETE/WI-0482.md`

**Scope**:

- `apps/worker/src/jobs/report_telemetry_calibration.js` (new)
- `apps/worker/src/jobs/__tests__/report_telemetry_calibration.test.js` (new)
- `apps/worker/package.json` (add `job:report-telemetry-calibration`)
- `docs/API_BASELINES.md`
- `WORK_QUEUE/WI-0485.md`

**Out of scope**:

- Changing settlement logic or card generation logic
- Enabling/disabling rollout flags in production
- Dashboard/UI changes in `web/`

**Acceptance**:

1. `job:report-telemetry-calibration` prints both ledgers (`projection_perf_ledger`, `clv_ledger`) with sample-size gates and threshold status.
2. Command supports an enforcement mode (e.g., `--enforce`) that exits non-zero when breach thresholds are met.
3. `docs/API_BASELINES.md` documents exact SQL parity checks so command output can be independently verified.

**Owner agent**: unassigned
**Time window**: TBD -> TBD
**Coordination flag**: solo

**Tests to run**:

- `npm --prefix apps/worker test -- src/jobs/__tests__/report_telemetry_calibration.test.js`
- `npm --prefix apps/worker run job:report-telemetry-calibration`
- `npm --prefix apps/worker run test:pipeline:settlement`

**Manual validation**:

- Run report once with default mode and confirm both ledgers are included with counts and threshold status.
- Run report with enforcement mode on a seeded failing fixture and confirm non-zero exit.
- Spot-check one metric from report output against direct `sqlite3` SQL.

**Guard for WI closeout**:

- Before marking complete: confirm report output is stable enough to paste into weekly operations notes (no stack traces, no undefined fields).
- Confirm command behavior is explicit when sample-size minimums are not met.

CLAIM: agent-name YYYY-MM-DDTHH:MM:SSZ

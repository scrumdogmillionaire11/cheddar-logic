# WI-0890: True-Play Authority Contract — Single Runtime Source of Truth

**ID**: WI-0890
**Goal**: Establish and document one authoritative source for surfaced true play selection so live cards cannot diverge across worker settlement backfill, API selection, and UI transform layers.

**Depends on**: none

**Scope**:
- docs/decisions/ADR-0003-true-play-authority.md
- web/src/lib/games/route-handler.ts
- apps/worker/src/jobs/settle_pending_cards.js
- web/src/__tests__/game-card-decision-authority.test.ts

**Out of scope**:
- Rewriting model scoring logic
- POTD logic changes
- UI styling or layout changes

**Acceptance**:
- ADR defines exactly one authority for true-play selection and rank tie-breakers.
- /api/games selection code references the ADR contract and no longer depends on ambiguous fallback behavior.
- Settlement job behavior is explicitly classified as either historical-only or live-authoritative, not both.
- Regression test fails if true-play authority changes outside the contract.

**Owner agent**: unassigned
**Time window**: 2026-04-11 -> TBD
**Coordination flag**: needs-sync

**Tests to run**:
- npm --prefix web run build
- node web/src/__tests__/game-card-decision-authority.test.ts

**Manual validation**:
- Compare /api/games response for a known game before and after settlement sweep; true_play source and rank rationale must remain stable.
- Verify logs include a deterministic authority marker for selected true_play rows.

CLAIM: unassigned
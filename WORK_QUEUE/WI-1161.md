# WI-1161: Canonical PLAY Path Mapping And Drought Audit

**ID**: WI-1161
**Goal**: Produce a canonical end-to-end map of how a candidate becomes `PLAY`, where that status can be downgraded before render/post, and what concrete gates may explain the current lack of surfaced PLAY cards.

**Requirements**: [PLAY-PATH-01, PLAY-PATH-02, PLAY-PATH-03]

**Roadmap traceability**:

- Planning prerequisite for `discord-hook-eligibility` implementation work.
- Must complete before WI-1162 claim/implementation begins.

**Scope**:

- `.planning/debug/play-path-map.md`
- `WORK_QUEUE/WI-1161.md`

**Out of scope**:

- Changing model thresholds, worker formulas, or execution-gate policy
- Shipping web/UI/Discord behavior changes
- Backfilling or mutating historical card data

**Acceptance**:

- [PLAY-PATH-01] The mapping document traces the canonical PLAY path from worker-emitted decision fields through web transform, UI surface, filters/presets, and Discord/webhook bucketing.
- [PLAY-PATH-02] Every downgrade boundary from `PLAY` to `SLIGHT EDGE`, `PASS`, `lean`, or `pass_blocked` is listed with concrete file path, controlling condition, and resulting state.
- [PLAY-PATH-03] The document includes at least one evidence-backed hypothesis for why PLAY cards have not recently surfaced, grounded in actual downgrade/suppression branches rather than speculation.
- The document ends with a follow-on implementation split that separates worker-generation causes from web transform, UI rendering, and Discord posting causes.

**Owner agent**: copilot
**Time window**: 2026-04-24T00:00:00Z -> TBD
**Coordination flag**: solo
**Depends on**: none (parallel-safe with WI-1160; this WI must be complete before WI-1162 execution starts)

## Execution tasks

<tasks>

<task type="auto">
  <name>Task 1: Build canonical PLAY decision path and downgrade matrix</name>
  <requirements>PLAY-PATH-01, PLAY-PATH-02</requirements>
  <files>.planning/debug/play-path-map.md</files>
  <action>
    Create a deterministic path table from worker decision fields to web transform outputs,
    UI surfaced labels, filter outcomes, and Discord bucket routing.

    Include a downgrade matrix with: source status, gate/condition, owning file,
    downgraded state, and visibility impact.
  </action>
  <verify>
    <automated>rg -n "^## Canonical PLAY Hop Map|^## Downgrade Matrix|PLAY|SLIGHT EDGE|pass_blocked|webhook_bucket|surfaced_status" .planning/debug/play-path-map.md</automated>
  </verify>
  <done>
    One PLAY candidate can be followed end-to-end with no missing handoff and every downgrade boundary explicitly documented.
  </done>
</task>

<task type="auto">
  <name>Task 2: Produce drought hypotheses and implementation split from concrete gates</name>
  <requirements>PLAY-PATH-03</requirements>
  <files>.planning/debug/play-path-map.md</files>
  <action>
    Add evidence-backed drought hypotheses tied to concrete downgrade/suppression nodes.

    End with smallest-viable follow-up implementation slices separated by root-cause lane:
    worker generation, web transform, UI filtering/rendering, and Discord posting.
  </action>
  <verify>
    <automated>rg -n "^## Drought Hypotheses|evidence-backed|^## Follow-on Implementation Split|worker generation|web transform|UI rendering|Discord posting" .planning/debug/play-path-map.md</automated>
  </verify>
  <done>
    Hypotheses are branch-cited and actionable, and follow-on work is split into non-overlapping implementation slices.
  </done>
</task>

</tasks>

**Tests to run**:

- `rg -n "official_status|execution_gate|should_bet|pass_reason_code|PLAY|FIRE|BASE" apps/worker/src/jobs web/src/lib/game-card web/src/components/cards apps/worker/src/jobs/post_discord_cards.js`
- `rg -n "surfaced_status|final_market_decision|webhook_bucket|pass_blocked|lean|official" web/src/lib/game-card web/src/components/cards apps/worker/src/jobs/post_discord_cards.js`
- `rg -n "^## Canonical PLAY Hop Map|^## Downgrade Matrix|^## Drought Hypotheses|^## Follow-on Implementation Split" .planning/debug/play-path-map.md`

**Manual validation**:

- Open `.planning/debug/play-path-map.md` and confirm one candidate is traceable from worker payload fields to final UI label and Discord bucket with no missing hop.
- Confirm every downgrade node names exact source file and condition that causes demotion.
- Confirm drought hypothesis section cites concrete downgrade/suppression branches, not generic confidence claims.

**Guard for WI closeout**:

- Before marking complete: confirm every command in `Tests to run` is runnable from repo root.
- Confirm the map distinguishes canonical status (`PLAY`/`LEAN`/`PASS`) from surfaced/public labels (`PLAY`/`SLIGHT EDGE`/`PASS`) and webhook buckets (`official`/`lean`/`pass_blocked`).
- Confirm output names smallest viable follow-up slices instead of bundling all causes into one fix.
- Confirm each requirement ID has concrete evidence in the mapping document.

CLAIM: copilot 2026-04-24T00:00:00Z

# WI-0480: Phase 2 Activation + Rollback Procedure (Thresholds V2)

**ID**: WI-0480
**Goal**: Define and validate controlled activation/rollback steps for `ENABLE_MARKET_THRESHOLDS_V2` using Phase 2 baseline evidence.

**Depends on**:

- `WORK_QUEUE/WI-0479.md`

**Scope**:

- `WORK_QUEUE/WI-0480.md`
- `docs/DATA_PIPELINE_TROUBLESHOOTING.md`
- `env.example`

**Out of scope**:

- Baseline parity capture (WI-0479)
- CLV ledger rollout
- Model logic edits in `packages/models/**`

**Acceptance**:

1. Troubleshooting runbook includes explicit activation, verification, and rollback commands for Phase 2.
2. `env.example` includes operator-safe guidance for `ENABLE_MARKET_THRESHOLDS_V2` (default-safe, no forced production-on).
3. Runbook specifies incident triggers and immediate rollback criteria.

**Owner agent**: github-copilot
**Time window**: 2026-03-17T00:00:00Z -> 2026-03-17T23:59:59Z
**Coordination flag**: needs-sync

**Tests to run**:

- `ENABLE_MARKET_THRESHOLDS_V2=true npm --prefix apps/worker run job:run-nba-model:test`
- `ENABLE_MARKET_THRESHOLDS_V2=true npm --prefix apps/worker run job:run-ncaam-model:test`
- `npm --prefix web run test:api:games:market`

**Manual validation**:

- Perform one controlled enablement run and confirm intended threshold-gated changes only.
- Disable flag and confirm behavior returns to baseline parity profile.

**Guard for WI closeout**:

- Before marking complete: confirm every test command in `Tests to run` is runnable from repo root.
- Include rollback evidence and operator command transcript references.

## Execution Notes (2026-03-17)

- Updated `docs/DATA_PIPELINE_TROUBLESHOOTING.md` with explicit Phase 2 runbook:
  - Activation commands for `ENABLE_MARKET_THRESHOLDS_V2=true`
  - Verification commands and contract sanity check
  - Incident triggers + immediate rollback sequence
- Updated `env.example` with operator-safe default-off guidance:
  - `ENABLE_MARKET_THRESHOLDS_V2=false`

## Closeout Checklist

- [x] Acceptance 1: explicit Phase 2 activation/verification/rollback commands documented.
- [x] Acceptance 2: `env.example` includes default-safe `ENABLE_MARKET_THRESHOLDS_V2` guidance.
- [x] Acceptance 3: incident triggers and immediate rollback criteria documented.
- [x] Tests to run executed and recorded.
- [x] Manual validation executed and recorded.

## Verification Evidence

- Automated tests/commands run from repo root on 2026-03-17:
  - `ENABLE_MARKET_THRESHOLDS_V2=true npm --prefix apps/worker run job:run-nba-model:test` ✅
  - `ENABLE_MARKET_THRESHOLDS_V2=true npm --prefix apps/worker run job:run-ncaam-model:test` ✅
  - `npm --prefix web run test:api:games:market` ✅
- Manual validation:
  - Controlled enablement run completed under `ENABLE_MARKET_THRESHOLDS_V2=true`.
  - Rollback parity check run completed under `ENABLE_MARKET_THRESHOLDS_V2=false`.

## Rollback Transcript References

- Phase 2 disable validation command: `ENABLE_MARKET_THRESHOLDS_V2=false npm --prefix apps/worker run job:run-nba-model:test`

## Status

- Ready to move to COMPLETE.

# WI-0480: Prewarm team metrics before projection model windows

**ID**: WI-0480
**Goal**: Prevent production `MISSING_DATA_NO_PLAYS` spikes during early model runs by ensuring team-metrics prewarm executes before NBA/NHL/NCAAM model jobs when cache is stale.

**Scope**:

- `apps/worker/src/schedulers/main.js`
- `apps/worker/src/__tests__/scheduler-team-metrics-prewarm.test.js`
- `apps/worker/package.json`
- `scripts/db-context.sh`
- `scripts/start-scheduler.sh`
- `scripts/manage-scheduler.sh`
- `docs/QUICKSTART.md`
- `apps/worker/README.md`

**Out of scope**:

- Frontend decision transform behavior changes
- Odds API normalization changes
- Schema migrations

**Acceptance**:

- Scheduler queues `refresh_team_metrics_daily` ahead of NBA/NHL/NCAAM model jobs when team-metrics cache is not fresh.
- Existing model scheduling behavior remains unchanged for non-projection sports.
- Manual runbooks include explicit prewarm step before model execution (especially production early-morning runs).
- Focused scheduler test verifies prewarm ordering behavior.

**Owner agent**: GitHub Copilot (GPT-5.3-Codex)
**Time window**: 2026-03-17T00:00:00Z -> TBD
**Coordination flag**: solo

**Tests to run**:

- `npm --prefix apps/worker test -- src/__tests__/scheduler-team-metrics-prewarm.test.js`
- `npm --prefix apps/worker test -- src/__tests__/scheduler-soccer-t45.test.js`

**Manual validation**:

- In production shell, verify scheduler/model order by checking logs for `refresh_team_metrics_daily` before projection model run keys during an early-morning window.
- Run manual production sequence with prewarm and confirm reduced `PROJECTION_INPUTS_INCOMPLETE` logs.

**Guard for WI closeout**:

- Before marking complete: confirm every test command in `Tests to run` executes from repo root.

CLAIM: GitHub-Copilot 2026-03-17T00:00:00Z

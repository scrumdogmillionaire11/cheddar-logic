# WI-0970: NHL 1P training data audit and playoff-regime feature enrichment

**ID**: WI-0970
**Goal**: Harden NHL 1P training data quality and add playoff-regime features needed for next-season retraining.

**Depends on**: WI-0969

**Scope**:
- `apps/worker/src/jobs/run_nhl_model.js`
- `apps/worker/src/models/nhl-pace-model.js`
- `apps/worker/src/models/nhl-goalie-state.js`
- `apps/worker/src/jobs/__tests__/run_nhl_model.test.js`
- `apps/worker/src/models/__tests__/nhl-pace-model.test.js`

**Out of scope**:
- New external data providers
- UI changes to `/results`
- Non-NHL model families

**Acceptance**:
- NHL 1P pipeline emits explicit playoff regime features usable for retraining and diagnostics (for example: series state, elimination pressure, rest compression, goalie stability).
- Training row construction excludes malformed rows and documents rejection reasons in diagnostics.
- PASS/HOLD/WATCH rows remain excluded from directional training targets used for side performance analytics.
- Tests validate feature extraction and row filtering paths.

**Owner agent**: unassigned
**Time window**: 2026-04-22 -> 2026-04-30
**Coordination flag**: solo

**Tests to run**:
- `npm --prefix apps/worker run test -- src/jobs/__tests__/run_nhl_model.test.js --runInBand`
- `npm --prefix apps/worker run test -- src/models/__tests__/nhl-pace-model.test.js --runInBand`
- `npm --prefix apps/worker run test -- src/models/__tests__/nhl-goalie-state.test.js --runInBand`

**Manual validation**:
- Run NHL model job in a controlled environment and inspect one playoff game payload to verify enriched features are present and non-null where expected.
- Confirm malformed input rows are rejected with explicit reason tags.

CLAIM: unassigned
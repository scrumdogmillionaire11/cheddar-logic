# WI-1260: POTD Pipeline Split Orchestration

**ID**: WI-1260

**Goal**: Orchestrate the split delivery of POTD starvation fixes across WI-1260-a, WI-1260-b, and WI-1260-c with strict dependency order and ownership boundaries.

**Context**:

The original WI mixed three contracts in one pass:

- model health freshness
- POTD monitored-candidate visibility
- decision_v2 promotion rules

This orchestration WI exists to ensure those concerns are delivered in separate, traceable work items.

**Scope**:

- WORK_QUEUE/WI-1260-a.md
- WORK_QUEUE/WI-1260-b.md
- WORK_QUEUE/WI-1260-c.md

**Out of scope**:

- direct code implementation across worker/jobs for these changes
- any model recalibration or stake-sizing policy changes

**Acceptance**:

- WI-1260-a, WI-1260-b, and WI-1260-c exist and are independently executable.
- Dependency chain is explicit and serializes execution correctly.
- Guardrail is explicit: POTD must never promote or reinterpret decision_v2.
- Each child WI includes exact test commands with concrete test paths and runInBand.

**Owner agent**: unassigned

**Time window**: 2026-05-04T00:00:00Z -> TBD

**Coordination flag**: needs-sync

**Depends on**:

- WI-1260-a before WI-1260-b
- WI-1260-b before WI-1260-c (recommended)

**Tests to run**:

- n/a (orchestration WI)

**Manual validation**:

- Confirm child WIs are claimed and executed in dependency order.
- Confirm no single PR mixes the three contracts.
- Confirm final implementation preserves this guard:
  POTD must never promote or reinterpret decision_v2. Promotion must happen before POTD reads the candidate.

CLAIM: unassigned


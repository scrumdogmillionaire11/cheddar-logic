# WI-1192: Restrict Projection-Only Bucket to True Projection-Only States

**ID**: WI-1192
**Goal**: Ensure Game Lines projection-only counts only reflect true projection-only execution states, not missing driver/projection input failures.

**Scope**:

- `WORK_QUEUE/WI-1192.md`
- `web/src/lib/game-card/pass-classification.ts`
- `web/src/__tests__/cards-best-odds-display.test.js`

**Out of scope**:

- Worker-side model generation changes
- Upstream data ingestion changes
- UI copy/layout changes

**Implementation notes**:

- Recent bucket mapping included `MISSING_DATA_PROJECTION_INPUTS` and `MISSING_DATA_DRIVERS` in projection-only.
- Those reason codes indicate incomplete/missing data and should remain in data-error for Game Lines diagnostics.
- Projection-only bucket should remain tied to execution status and explicit projection-only exclusion semantics.

**Acceptance**:

- `MISSING_DATA_PROJECTION_INPUTS` and `MISSING_DATA_DRIVERS` classify as `data-error`.
- True projection-only paths (`execution_status=PROJECTION_ONLY`, `PROJECTION_ONLY_EXCLUSION`) still classify as `projection-only`.
- Scoped tests pass.

**Owner agent**: github-copilot
**Time window**: 2026-04-27T00:00:00Z -> TBD
**Coordination flag**: solo
**Depends on**: none

**Tests to run**:

- `node --import tsx/esm web/src/__tests__/cards-best-odds-display.test.js`
- `node --import tsx/esm web/src/__tests__/game-card-pass-surface-contract.test.js`

**Manual validation**:

- Wedge header projection-only count no longer includes missing-driver/missing-projection-input cards.

**Guard for WI closeout**:

- Keep diff scoped to listed files only.

CLAIM: github-copilot 2026-04-27T00:00:00Z

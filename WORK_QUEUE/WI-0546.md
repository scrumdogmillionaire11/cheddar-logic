# WI-0546: PASS Main-View Runtime Regression Contracts

**ID**: WI-0546
**Goal**: Add behavior-level tests that prove PASS cards cannot leak into default FIRE/WATCH view, including edge-verification and tag-heavy scenarios.

**Scope**:

- `WORK_QUEUE/WI-0546.md`
- `web/src/lib/game-card/filters.ts` (only if behavior gaps discovered)
- `web/src/__tests__/game-card-filters.test.js`
- `web/src/__tests__/game-card-filters-play-first.test.js`
- `web/src/__tests__/filters-pass-play-main-view-regression.test.js` (convert/augment from source-text checks to behavior checks)

**Out of scope**:

- Worker decision math
- API payload schema changes

**Acceptance**:

- Fixture-based runtime tests cover: PASS + strong driver tags, PASS + non-`NO PLAY` pick text, PASS with `decision_v2.official_status`.
- Default FIRE/WATCH filters exclude those cards.
- Including PASS status includes those cards in Full Slate mode.
- No reliance on source-string matching for core invariants.

**Owner agent**: unassigned
**Time window**: 2026-03-21T18:00:00Z -> TBD
**Coordination flag**: solo

**Tests to run**:

- `npm --prefix web run test:filters`
- `npm --prefix web run test:filters:play-first`
- `npm --prefix web run test:ui:cards`

**Manual validation**:

- With default filters in local cards view, verify no PASS card appears; with PASS enabled, verify PASS cards are shown.

CLAIM: unassigned

# WI-1154: Active Lifecycle Parity And Tomorrow MLB Visibility Contract

**ID**: WI-1154
**Goal**: Make `/wedge` and `/api/games` behave the same in dev and prod for started-game Active visibility, tomorrow MLB visibility, and shared time-window policy between worker publication and web read surfaces.

**Scope**:
- `web/src/lib/games/query-layer.ts`
- `web/src/lib/games/route-handler.ts`
- `web/src/components/cards/CardsPageContext.tsx`
- `apps/worker/src/jobs/run_mlb_model.js`
- `web/src/__tests__/api-games-filter.test.js`
- `web/src/__tests__/cards-lifecycle-fetch-race.test.js`
- `docs/DATA_PIPELINE_TROUBLESHOOTING.md`
- `WORK_QUEUE/WI-1154.md`

**Out of scope**:
- NHL player props generation
- Projection settlement logic
- Redesign of cards UI beyond Active empty-state clarity

**Acceptance**:
- `Active` remains actionable-only.
- Empty `Active` responses no longer imply pregame fallback or hidden mode switches.
- `/api/games` exposes explicit reasoned empty-state behavior for started games with no actionable rows.
- Tomorrow MLB visibility follows one canonical rule: through end of next `America/New_York` day.
- `run_mlb_model.js` and `/api/games` use the same horizon contract.
- No dev/prod drift remains from hard-coded `36h` vs surface-specific windows.
- Dead or legacy lifecycle/horizon code in scoped files that becomes obsolete during this fix is removed rather than left behind as dormant fallback logic.

**Owner agent**: unassigned
**Time window**: 2026-04-23T00:00:00Z -> TBD
**Coordination flag**: needs-sync
**Depends on**: none

**Tests to run**:
- `node web/src/__tests__/api-games-filter.test.js`
- `node web/src/__tests__/cards-lifecycle-fetch-race.test.js`
- `npm --prefix web run test:api:games:market`
- `npm --prefix apps/worker run test -- --runInBand src/jobs/__tests__/run_mlb_model.test.js`

**Manual validation**:
- Verify `/wedge?lifecycle=active` stays in Active mode when started games have no actionable rows.
- Verify a tomorrow MLB game beyond rolling 36h but before next ET-day end appears consistently in both worker-produced cards and `/api/games`.

**Guard for WI closeout**:
- Before marking complete: confirm every test command in `Tests to run` is runnable from repo root.
- Confirm the worker and web horizon policies are implemented from one canonical contract, not duplicated literals.
- Confirm Active lifecycle behavior is explained by diagnostics or empty-state metadata rather than implicit fallback.
- Confirm obsolete lifecycle/horizon branches in scoped files were removed or explicitly documented as intentionally retained.

CLAIM: unassigned 2026-04-23T00:00:00Z

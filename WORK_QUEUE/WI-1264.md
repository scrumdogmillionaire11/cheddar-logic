# WI-1264: Canonicalize Game IDs at Read Boundaries

**ID**: WI-1264
**Goal**: Prevent duplicate scheduled-game diagnostics by resolving external provider game IDs to canonical odds-backed IDs before scheduled coverage and API slate assembly.

**Scope**:
- `apps/worker/src/jobs/check_pipeline_health.js`
- `web/src/lib/games/service-layer.ts`
- `web/src/lib/games/route-handler.ts`
- `packages/data/src/db/games.js`
- `packages/data/db/migrations/013_create_game_id_map.sql`

**Out of scope**:
- Rewriting schedule ingestion
- Deleting historical duplicate rows
- Changing game_id_map uniqueness rules

**Acceptance**:
- Scheduled coverage checks resolve mapped external IDs to canonical game IDs before coverage evaluation.
- `/api/games` slate assembly resolves mapped external IDs to canonical IDs.
- Duplicate same-matchup rows do not produce contradictory no-card vs playable-card diagnostics.
- Tests cover canonical + external duplicate rows for same matchup.

**Owner agent**: unassigned
**Time window**: 3-5 hours
**Coordination flag**: needs-sync
**Depends on**: none

**Tests to run**:
- `npm --prefix apps/worker test -- check-pipeline-health`
- `npm --prefix web exec tsx src/__tests__/games-service-layer.test.ts`

**Manual validation**:
- Run scheduled coverage diagnostics on a fixture with canonical + mapped external IDs and confirm one effective matchup outcome.
- Query `/api/games` and confirm duplicated external IDs resolve to a single canonical slate identity.

**Guard for WI closeout**:
- Do not modify schedule ingestion identity generation logic in this WI.
- Keep `game_id_map` uniqueness semantics unchanged.

CLAIM: unassigned

# WI-1132B: Distributed Rate Limiter — Route Integration

**ID**: WI-1132B
**Goal**: Wire distributed rate limiter (from WI-1132) into all protected API routes to enforce limits consistently across instances.

**Scope**:
- web/src/app/api/auth/token/route.ts (apply limiter)
- web/src/app/api/cards/route.ts (apply limiter)
- web/src/app/api/cards/[gameId]/route.ts (apply limiter)
- web/src/app/api/results/route.ts (apply limiter)
- web/src/app/api/model-outputs/route.ts (apply limiter)
- web/src/app/api/performance/route.ts (apply limiter)

**Out of scope**:
- Rate limiter abstraction (WI-1132)
- Test contracts (WI-1132)
- Route handler business logic changes

**Acceptance**:
- All 6 routes apply distributed rate limiter via consistent middleware pattern
- Rate limit headers present and consistent across all routes
- Routes use identity from WI-1125 (trusted client IP + fallback)
- Existing routes continue to function without breakage

**Owner agent**: unassigned
**Time window**: TBD
**Coordination flag**: none (integration-only)
**Depends on**: WI-1132 (Distributed Rate Limiter Infrastructure)

**Tests to run**:
- npm --prefix web run test -- api-rate-limit-header-consistency --runInBand
- npm --prefix web run lint

**Manual validation**:
- Quick smoke: `curl -X POST http://localhost:3000/api/auth/token 2>&1` returns rate limit headers
- Verify all 6 routes emit X-RateLimit-* headers on first request

**Integration Pattern** (for executor reference):
- Each route imports `{ limiter }` from `web/src/lib/api-security`
- Apply at route handler entry: `const limited = limiter.isAllowed(identity); if (!limited.allowed) return 429 response with Retry-After`
- All routes use same `identity` resolution (from WI-1125)

**Guard for WI closeout**:
- Before marking complete: confirm all 6 routes emit rate-limit headers consistently
- Run the api-rate-limit-header-consistency test to verify contract

CLAIM: unassigned 2026-04-21T00:00:00Z
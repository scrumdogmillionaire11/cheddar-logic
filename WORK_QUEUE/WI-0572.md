# WI-0572: Hostile audit — betting decision pipeline

**ID**: WI-0572
**Goal**: Produce a defect-first hostile audit of the live sports betting decision path, focusing on places where the system can emit confident-looking but invalid plays, hide missing data, or let downstream layers mutate canonical worker truth.

**Scope**:
- `WORK_QUEUE/WI-0572.md`
- `docs/runbooks/hostile-betting-pipeline-audit-2026-03.md`

**Out of scope**:
- Source-code fixes in `apps/worker/**`, `packages/**`, or `web/**`
- Threshold tuning, calibration changes, or model rewrites
- Migrations, contract changes, or UI copy updates
- Backfills, re-settlement, or production data repair

**Acceptance**:
- A findings-only audit report is written to `docs/runbooks/hostile-betting-pipeline-audit-2026-03.md`.
- Every finding includes severity, classification, title, why it is a problem, exact file + line references, current behavior, correct behavior, recommended fix, and dependency notes.
- The report includes:
  - Top 5 issues by urgency
  - Must-fix items before trusting tonight’s outputs
  - Calibration/backtesting items that can wait
  - Tests that currently codify wrong behavior
- The audit covers, at minimum:
  - `packages/models/src/decision-pipeline-v2.js`
  - `packages/models/src/decision-pipeline-v2.patch.js`
  - `packages/models/src/decision-gate.js`
  - `packages/models/src/edge-calculator.js`
  - `apps/worker/src/utils/decision-publisher.js`
  - `packages/data/src/market-contract.js`
  - `web/src/app/api/games/route.ts`
  - `web/src/lib/game-card/transform.ts`
  - `web/src/lib/game-card/decision.ts`
  - `web/src/lib/play-decision/decision-logic.ts`
- The audit does not summarize architecture; it only records defects, ambiguities, design risks, calibration risks, and data integrity risks supported by code evidence.

**Owner agent**: codex
**Time window**: 2026-03-23T12:11:17Z -> TBD
**Coordination flag**: solo

**Tests to run**:
- `npm --prefix apps/worker test -- src/utils/__tests__/decision-publisher.v2.test.js`
- `npm --prefix apps/worker test -- src/utils/__tests__/decision-gate.flip-threshold.test.js`
- `npm --prefix web run test:decision:canonical`
- `npm --prefix web run test:transform:market`
- `npm --prefix web run test:api:games:repair-budget`

**Manual validation**:
- Confirm the audit report contains findings only and follows the required output schema for every issue.
- Confirm every cited line reference resolves to an in-repo file and that uncertain conclusions are labeled explicitly as uncertainty.
- Confirm no repo-tracked changes are made outside this WI file and the audit report file.

**Guard for WI closeout**:
- Before marking complete: confirm every test command in `Tests to run` is runnable from repo root.
- Before marking complete: confirm the audit report does not propose fixes without tying them to a concrete issue entry.
- Before marking complete: confirm any tests cited as wrong behavior are called out as evidence of the defect, not as proof of correctness.

CLAIM: codex 2026-03-23T12:11:17Z

# WI-0537: FIRST_PERIOD policy centralization in canonical decision layer

**ID**: WI-0537
**Goal**: Eliminate split-truth behavior for FIRST_PERIOD by enforcing one canonical pricing/actionability policy in the shared decision layer instead of per-job patching.

**Scope**:
- `WORK_QUEUE/WI-0537.md`
- `packages/models/src/decision-pipeline-v2.js`
- `packages/models/src/card-factory.js`
- `apps/worker/src/jobs/run_nhl_model.js`
- `apps/worker/src/utils/decision-publisher.js`
- `apps/worker/src/utils/__tests__/decision-publisher.v2.test.js`
- `apps/worker/src/jobs/__tests__/run_nhl_model.market-calls.test.js`
- `apps/worker/src/models/__tests__/nhl-1p-totals.test.js`

**Out of scope**:
- Rebuilding NHL 1P projection math
- General TOTAL/SPREAD policy changes
- NHL ML calibration or threshold tuning
- UI redesign

**Acceptance**:
- FIRST_PERIOD actionability/pricing policy is defined once in canonical decisioning and referenced consistently by producer and publisher paths.
- No path can produce contradictory outcomes where one layer treats FIRST_PERIOD as priced/actionable and another silently downgrades it.
- Reason codes for FIRST_PERIOD PASS outcomes are explicit and policy-aligned.
- Existing 1P market tests are updated to assert canonical policy behavior, not layer-specific side effects.

**Owner agent**: unassigned
**Time window**: TBD
**Coordination flag**: solo
**Depends on**: WI-0535, WI-0536

**Tests to run**:
- `npm --prefix apps/worker test -- src/utils/__tests__/decision-publisher.v2.test.js`
- `npm --prefix apps/worker test -- src/jobs/__tests__/run_nhl_model.market-calls.test.js`
- `npm --prefix apps/worker test -- src/models/__tests__/nhl-1p-totals.test.js`

**Manual validation**:
- Generate `nhl-pace-1p` with and without 1P side price and verify canonical decision output is identical regardless of entry path order.
- Verify FIRST_PERIOD cards cannot surface as actionable when canonical policy marks them non-actionable.

**Guard for WI closeout**:
- Classify this as `design_risk`: centralize policy and remove split ownership of actionability rules.
- Do not bundle threshold tuning or calibration changes in this WI.

CLAIM: unassigned


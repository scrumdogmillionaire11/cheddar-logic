# WI-0538: NHL moneyline probability calibration (margin -> win mapping)

**ID**: WI-0538
**Goal**: Replace heuristic NHL ML margin-to-win mapping with a calibrated sport-specific mapping validated on historical outcomes.

**Scope**:
- `WORK_QUEUE/WI-0538.md`
- `apps/worker/src/models/cross-market.js`
- `packages/models/src/card-utilities.js`
- `packages/models/src/card-model.js` (only if mapping utility changes are required)
- `apps/worker/src/jobs/report_telemetry_calibration.js`
- `apps/worker/src/jobs/__tests__/xg-model.test.js` (or nearest ML calibration test surface)
- `apps/worker/src/models/__tests__/cross-market.test.js`
- `docs/SETTLEMENT_AUDIT.md` (only if calibration reporting contract changes)

**Out of scope**:
- Decision-gate threshold tuning
- FIRST_PERIOD policy updates
- Non-NHL sports
- Cosmetic telemetry dashboard changes

**Acceptance**:
- NHL ML mapping is calibrated using historical NHL margin/outcome sample (not hard-coded by analogy to spread sigma).
- Calibration method and selected parameters are reproducible from committed code/report logic.
- Pre/post evaluation is documented in metrics terms (Brier score, log loss, reliability bins).
- `cross-market` NHL ML edge output uses calibrated mapping and tests cover representative confidence bands.

**Owner agent**: unassigned
**Time window**: TBD
**Coordination flag**: solo
**Depends on**: WI-0535

**Tests to run**:
- `npm --prefix apps/worker test -- src/models/__tests__/cross-market.test.js`
- `npm --prefix apps/worker test -- src/jobs/__tests__/xg-model.test.js`
- `npm --prefix apps/worker run job:report-telemetry-calibration`

**Manual validation**:
- Produce calibration report for NHL ML and verify reliability buckets improve or remain neutral vs baseline.
- Spot-check recent NHL games: projected win probabilities move away from flat 50/50 compression where margin signal is meaningful.

**Guard for WI closeout**:
- Classify this as `calibration_risk`: no blind sigma swaps; require metric-based selection.
- If calibration degrades metrics, keep baseline mapping and ship report-only findings.

CLAIM: unassigned


# WI-0902: Endpoint Behavioral Parity Fixtures for Cards and Games

**ID**: WI-0902
**Goal**: Add fixture-driven behavioral parity tests proving that identical underlying payloads produce explainable and stable differences between cards and games endpoints.

**Depends on**: WI-0892

**Scope**:
- web/src/app/api/cards/route.ts
- web/src/app/api/cards/[gameId]/route.ts
- web/src/lib/games/route-handler.ts
- web/src/__tests__/api-games-prop-decision-contract.test.js
- web/src/__tests__/api-cards-lifecycle-regression.test.js
- web/src/__tests__/api-endpoint-parity-fixtures.test.js
- docs/audits/endpoint-parity.md

**Out of scope**:
- UI layout changes
- New endpoint creation
- Source-string contract tests

**Acceptance**:
- Shared fixture set drives both cards and games responses for the same game id and payload.
- Tests assert surfaced status, reason code, visibility, and projection markers behaviorally.
- Explicit diff object is produced per fixture with stable fields and deterministic expectations.
- Parity suite is resilient to refactors that preserve behavior.

**Owner agent**: copilot
**Time window**: 2026-04-11 -> TBD
**Coordination flag**: needs-sync
**Execution path**: combined PR with WI-0901/WI-0903 by explicit user request

**Requirements coverage map**:
- R1 (single shared fixture corpus for cards/games parity) -> Task 1, Task 3
- R2 (behavioral assertions for status/reason/visibility/projection markers) -> Task 2, Task 3
- R3 (stable deterministic per-fixture diff shape) -> Task 3
- R4 (refactor-resilient parity documentation + auditability) -> Task 3

**Preflight dependency gate (WI-0892 contract checks)**:
- Confirm shared projection-visibility behavior is already centralized and consumed by cards + games paths before adding parity fixtures.
- Confirm current baseline tests pass before modifying parity coverage:
  - `node web/src/__tests__/api-games-prop-decision-contract.test.js`
  - `node web/src/__tests__/api-cards-lifecycle-regression.test.js`

**Must-haves (goal-backward)**:

**Truths**:
- The same fixture payload can be evaluated through both endpoint paths and compared deterministically.
- Differences are explainable by surfaced behavior fields, not opaque booleans.
- A failing parity condition points to a concrete reason-level mismatch.

**Artifacts**:
- `web/src/__tests__/api-endpoint-parity-fixtures.test.js` provides shared fixture dataset and parity assertions.
- `web/src/__tests__/api-games-prop-decision-contract.test.js` enforces response-level games behavior used by parity expectations.
- `web/src/__tests__/api-cards-lifecycle-regression.test.js` enforces response-level cards behavior used by parity expectations.
- `docs/audits/endpoint-parity.md` documents fixture matrix, expected diffs, and interpretation rules.

**Key links (critical wiring)**:
- Shared fixtures in `web/src/__tests__/api-endpoint-parity-fixtures.test.js` -> cards path via `web/src/app/api/cards/route.ts` and `web/src/app/api/cards/[gameId]/route.ts` request simulations.
- Shared fixtures in `web/src/__tests__/api-endpoint-parity-fixtures.test.js` -> games path via `web/src/lib/games/route-handler.ts` response assembly path.
- Parity diff output contract in `web/src/__tests__/api-endpoint-parity-fixtures.test.js` -> audit interpretation in `docs/audits/endpoint-parity.md`.

**Execution tasks**:

### Task 1: Build shared fixture harness for cards/games parity inputs
**Files**:
- web/src/__tests__/api-endpoint-parity-fixtures.test.js

**Action**:
- Create a single fixture table (game id + payload + expected parity descriptors) that can drive both cards and games endpoint evaluations.
- Ensure fixtures cover projection-only, synthetic fallback, and standard rows.
- Keep fixture fields minimal and stable to avoid coupling to incidental implementation details.

**Verify**:
- node web/src/__tests__/api-endpoint-parity-fixtures.test.js

**Done**:
- Shared fixture corpus exists and executes both endpoint paths using the same underlying payload definitions.

### Task 2: Align response behavior assertions across cards and games consumers
**Files**:
- web/src/app/api/cards/route.ts
- web/src/app/api/cards/[gameId]/route.ts
- web/src/lib/games/route-handler.ts
- web/src/__tests__/api-games-prop-decision-contract.test.js
- web/src/__tests__/api-cards-lifecycle-regression.test.js

**Action**:
- Ensure response-level fields required for parity comparison are surfaced consistently (status, reason code, visibility classification, projection markers).
- Preserve existing endpoint semantics from WI-0892 while making comparison fields explicit and deterministic.
- Add or adjust test assertions to lock behavior rather than internal implementation branches.

**Verify**:
- npm --prefix web run test:games-filter
- npm --prefix web run test:cards-lifecycle-regression

**Done**:
- Cards and games response contracts expose the behavioral fields required for parity diffing without introducing new endpoint behavior.

### Task 3: Implement deterministic parity diff output and audit documentation
**Files**:
- web/src/__tests__/api-endpoint-parity-fixtures.test.js
- docs/audits/endpoint-parity.md

**Action**:
- Produce a stable diff object per fixture with fixed keys:
  - `gameId`
  - `fixtureId`
  - `cards`
  - `games`
  - `field_deltas`
  - `reason_explanation`
  - `parity_status`
- Assert deterministic expectations for each fixture, including both matching and intentional mismatch cases.
- Document the fixture matrix and how to interpret each diff field in endpoint-parity audit doc.

**Verify**:
- node web/src/__tests__/api-endpoint-parity-fixtures.test.js
- npm --prefix web run build

**Done**:
- Parity suite fails on behavioral drift and emits reason-level explainable diffs.
- Audit doc describes expected fixture outcomes and stable diff schema.

**Tests to run**:
- npm --prefix web run test:games-filter
- npm --prefix web run test:cards-lifecycle-regression
- node web/src/__tests__/api-endpoint-parity-fixtures.test.js
- npm --prefix web run build

**Manual validation**:
- Query cards and games for the same fixture game ids and compare generated parity diff output.
- Confirm mismatches include reason-level explanation rather than boolean pass/fail only.

CLAIM: copilot 2026-04-13T01:03:00Z

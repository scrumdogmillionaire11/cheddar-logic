# WI-0964: Isolate MLB fallback and pure-signal mode policy into explicit model tiers

**ID**: WI-0964
**Goal**: Reduce legacy branch debt in MLB model scoring by separating full-model, degraded-model, and fallback-model behavior into explicit tier contracts.

**Depends on**: none

**Scope**:

- `WORK_QUEUE/WI-0964.md`
- `apps/worker/src/models/mlb-model.js`
- `apps/worker/src/models/__tests__/mlb-model-gate.test.js`

**Out of scope**:

- Web transform logic
- Odds ingest format changes
- Cross-sport model refactors

**Acceptance**:

- The model clearly encodes when `SYNTHETIC_FALLBACK` and legacy ERA branches are allowed to produce publishable outputs versus insight-only outputs.
- `MLB_PURE_SIGNAL_MODE` behavior is isolated to documented toggles with deterministic reason-code effects.
- Tests cover tier transitions (FULL_MODEL/DEGRADED/SYNTHETIC_FALLBACK) and prevent silent drift in status/action mapping.
- Dead-path-style branch overlap is reduced or documented with explicit comments and tests.

**Owner agent**: GitHub Copilot
**Time window**: 2026-04-15 -> TBD
**Coordination flag**: solo

**Tests to run**:

- `npm --prefix apps/worker run test -- src/models/__tests__/mlb-model-gate.test.js --runInBand`

**Manual validation**:

- Run `npm --prefix apps/worker run job:run-mlb-model` and verify logs/reason-codes reflect explicit tier contracts for degraded and fallback projections.

CLAIM: GitHub Copilot 2026-04-16T17:30:00Z

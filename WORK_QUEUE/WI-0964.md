# WI-0964: Isolate MLB fallback and pure-signal mode policy into explicit model tiers

**ID**: WI-0964
**Goal**: Reduce legacy branch debt in MLB model scoring by separating full-model, degraded-model, and fallback-model behavior into explicit tier contracts.

**Depends on**: none

**Architecture constraints (must hold)**:

- This WI is model-local by design: define and normalize tier policy in `apps/worker/src/models/mlb-model.js` and its model-gate tests only.
- Publish and execution outcomes in runner and execution-gate layers are validated and refined in downstream WIs; this WI must not add new runner-level policy branches.
- `MLB_PURE_SIGNAL_MODE` must only alter documented soft suppressor behavior and reason-code emission, never silently reclassify projection tier identity.

**Scope**:

- `WORK_QUEUE/WI-0964.md`
- `apps/worker/src/models/mlb-model.js`
- `apps/worker/src/models/__tests__/mlb-model-gate.test.js`

**Out of scope**:

- Web transform logic
- Odds ingest format changes
- Cross-sport model refactors

**Acceptance**:

- The model clearly encodes tier policy for `FULL_MODEL`, `DEGRADED_MODEL`, and `SYNTHETIC_FALLBACK` outputs, including explicit tier-level `status_cap` and reason-code semantics at model-result level.
- `MLB_PURE_SIGNAL_MODE` behavior is isolated to documented toggles with deterministic reason-code effects.
- Tests cover tier transitions (FULL_MODEL/DEGRADED/SYNTHETIC_FALLBACK) and prevent silent drift in model-level status and reason mapping (`status`, `status_cap`, `pass_reason_code`, `reason_codes`).
- Dead-path-style branch overlap is reduced or documented with explicit comments and tests.

**Execution plan**:

- Task 1 (tier contract extraction and branch inventory)
  - Files: `apps/worker/src/models/mlb-model.js`
  - Action: Identify and consolidate tier decision points for `FULL_MODEL`, `DEGRADED_MODEL`, and `SYNTHETIC_FALLBACK` in model helpers so branch ownership is explicit. Add concise inline policy comments where legacy fallback and degraded-mode gates intersect.
  - Verify: `cd /Users/ajcolubiale/projects/cheddar-logic && rg -n "FULL_MODEL|DEGRADED_MODEL|SYNTHETIC_FALLBACK|status_cap|pass_reason_code|reason_codes" apps/worker/src/models/mlb-model.js`
  - Done: Tier decision points are explicit and discoverable in one model-local policy path.

- Task 2 (pure-signal toggle isolation)
  - Files: `apps/worker/src/models/mlb-model.js`
  - Action: Constrain `MLB_PURE_SIGNAL_MODE` effects to documented soft suppressor and reason-code branches; ensure no hidden tier identity changes occur when toggle flips.
  - Verify: `cd /Users/ajcolubiale/projects/cheddar-logic && rg -n "MLB_PURE_SIGNAL_MODE|SOFT_|PASS_|MODEL_DEGRADED_INPUTS" apps/worker/src/models/mlb-model.js`
  - Done: Toggle-on and toggle-off behavior maps deterministically to documented reason-code differences without changing tier classification rules.

- Task 3 (regression hardening for tier transitions)
  - Files: `apps/worker/src/models/__tests__/mlb-model-gate.test.js`
  - Action: Add and extend tests asserting transitions and guardrails for `FULL_MODEL`, `DEGRADED_MODEL`, and `SYNTHETIC_FALLBACK`, including model-level status and reason continuity fields (`status`, `status_cap`, `pass_reason_code`, `reason_codes`).
  - Verify: `npm --prefix apps/worker run test -- src/models/__tests__/mlb-model-gate.test.js --runInBand`
  - Done: Tests fail deterministically on tier-policy drift and pass when the model-level contract is preserved.

**Owner agent**: GitHub Copilot
**Time window**: 2026-04-15 -> TBD
**Coordination flag**: solo

**Tests to run**:

- `npm --prefix apps/worker run test -- src/models/__tests__/mlb-model-gate.test.js --runInBand`
- `cd /Users/ajcolubiale/projects/cheddar-logic && rg -n "FULL_MODEL|DEGRADED_MODEL|SYNTHETIC_FALLBACK|MLB_PURE_SIGNAL_MODE|status_cap|pass_reason_code|reason_codes" apps/worker/src/models/mlb-model.js apps/worker/src/models/__tests__/mlb-model-gate.test.js`

**Manual validation**:

- Run `npm --prefix apps/worker run job:run-mlb-model` and verify logs and reason-codes reflect explicit model-tier contracts for degraded and fallback projections (runner publish and execution semantics remain out-of-scope for this WI).

CLAIM: GitHub Copilot 2026-04-16T17:30:00Z

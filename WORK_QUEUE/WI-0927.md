# WI-0927: Remove dead `projectNBA` import from models/index.js

**ID**: WI-0927
**Goal**: Remove the `projectNBA` import from `models/index.js` — the deprecated function is not re-exported and has no callers outside its own file.

**Scope**:

- `apps/worker/src/models/index.js`

**Out of scope**:

- `apps/worker/src/models/projections.js` — leave the function body and `@deprecated` annotation intact; it is still technically exported by projections.js and could be called directly
- Any changes to `projectNBACanonical`, `projectNHL`, or other projection functions

**Acceptance**:

- Line `projectNBA,` removed from the destructured require at the top of `models/index.js`
- `grep -n "projectNBA\b" apps/worker/src/models/index.js` returns 0 matches
- `npm --prefix apps/worker run test -- --no-coverage` passes

**Owner agent**: unassigned
**Time window**: TBD
**Coordination flag**: solo

**Tests to run**:

- `npm --prefix apps/worker run test -- --no-coverage`

**Manual validation**:

- N/A — automated test coverage sufficient

CLAIM: unassigned

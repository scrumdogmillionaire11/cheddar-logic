# WI-0341: Clean Up Prod DB Path Confusion

**ID**: WI-0341
**Goal**: Remove conflicting production DB path references and legacy path cues in operator-facing docs/scripts to prevent future deploy ambiguity.

**Scope**:

- `docs/DEPLOY_GITHUB.md`
- `docs/ops-runbook.md`
- `env.example`
- `.github/workflows/deploy-production.yml`
- `scripts/start-scheduler.sh`
- `scripts/db-context.sh`
- `scripts/manage-scheduler.sh`
- `scripts/demo-db-autodiscovery.sh`
- `scripts/verify-dual-db.js`

**Out of scope**:

- `packages/data/db/migrations/**`
- `packages/data/cheddar.db`
- `apps/**/__tests__/**`

**Acceptance**:

- All prod path references align on `/opt/data/cheddar-prod.db`.
- No operator-facing guidance suggests legacy DB env vars for production.
- Runtime scripts continue to defensively unset legacy DB vars without implying they should be configured.
- Demo/legacy helpers are clearly labeled as non-production and do not recommend legacy DB vars.
- Deploy workflow logs API health warnings (non-fatal) and appends them to `/opt/cheddar-logic/logs/deploy.log`.

**Owner agent**: GitHub Copilot
**Time window**: 2026-03-08T00:00:00Z -> TBD
**Coordination flag**: solo

**Tests to run**:

- none (doc/script updates only)

**Manual validation**:

- Grep docs/scripts for `/opt/cheddar-logic/data/cheddar-prod.db` returns zero matches.
- Grep docs/scripts for `DATABASE_PATH`/`RECORD_DATABASE_PATH` shows only unset/cleanup guidance.
- Deploy workflow logs API warnings to `/opt/cheddar-logic/logs/deploy.log` when endpoints are empty or return non-success payloads.

CLAIM: GitHub Copilot 2026-03-08T00:00:00Z

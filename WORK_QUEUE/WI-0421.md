# WI-0421: Dev-safe hourly display-log backfill toggle for settlement

**ID**: WI-0421
**Goal**: Allow NBA/NCAAM/NHL pending cards to become settlement-eligible in development by introducing an explicit hourly display-log backfill toggle, without changing production defaults.

**Scope**:

- `WORK_QUEUE/WI-0421.md`
- `apps/worker/src/schedulers/main.js`
- `env.example`
- `apps/worker/src/jobs/settle_pending_cards.js`

**Out of scope**:

- Changes to settlement grading logic in `settle_pending_cards.js`
- Changes to matching logic in `settle_game_results.js`
- Any schema or migration changes

**Acceptance**:

- Scheduler supports a new env-controlled hourly display backfill behavior.
- Default behavior remains strict for production (`false` unless explicitly enabled).
- Existing nightly backfill behavior remains unchanged.
- Startup config log includes the new hourly backfill setting.
- `settle_pending_cards` no longer throws when display backfill is enabled.

**Owner agent**: codex
**Time window**: 2026-03-13T00:00:00Z -> TBD
**Coordination flag**: solo

**Tests to run**:

- `npm --prefix apps/worker run job:pull-odds`
- `npm --prefix apps/worker run job:run-nhl-model`
- `npm --prefix apps/worker run job:run-nba-model`
- `npm --prefix apps/worker run job:run-ncaam-model`
- `npm --prefix apps/worker run job:settle-cards`

**Manual validation**:

- Set `SETTLEMENT_HOURLY_ENABLE_DISPLAY_BACKFILL=true` in dev env.
- Run hourly settlement path and verify `SettleCards` no longer reports zero eligible rows when final games and pending cards exist.
- Confirm with default env (toggle unset/false), hourly remains strict display-log mode.

CLAIM: codex 2026-03-13T00:00:00Z

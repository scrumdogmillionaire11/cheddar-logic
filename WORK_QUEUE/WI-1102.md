# WI-1102: Immediate Tech-Debt Cleanup Sweep

**ID**: WI-1102
**Goal**: Execute the immediate cleanup wins from the 2026-04-20 concerns audit without changing betting behavior.

**Scope**:
- `apps/worker/package.json`
- `apps/worker/package-lock.json`
- `apps/worker/src/jobs/execution-gate.js`
- `apps/worker/src/jobs/run_nhl_player_shots_model.js`
- `apps/worker/src/jobs/settle_pending_cards.js`
- `apps/worker/src/jobs/__tests__/execution-gate.test.js`
- `apps/worker/src/jobs/__tests__/settle_pending_cards.non-actionable.test.js`
- `packages/models/src/decision-policy.js`

**Out of scope**:
- NFL work item changes while WI-0766 remains frozen
- Feature-flag architecture changes
- Route-handler or model-job decomposition
- Removing legacy decision fields from payload writers
- Database migrations or production DB maintenance

**Acceptance**:
- `npm audit --prefix apps/worker` no longer reports the live `brace-expansion <1.1.13` issue.
- `evaluateExecution()` no longer defaults `vigCost` or `slippageCost` to deprecated no-op constants.
- NHL player props no longer use the local `deriveLegacyActionFromVerdict()` shim; legacy fields come from canonical `deriveLegacyDecisionEnvelope()`.
- Settlement fallback to legacy decision status emits an explicit warning for non-historical cards.
- Changed files match this work item scope with no unrelated cleanup.

**Owner agent**: codex
**Time window**: 2026-04-20T23:59:49Z -> 2026-04-21T00:04:04Z
**Coordination flag**: needs-sync

**Tests to run**:
- `npm audit --prefix apps/worker`
- `npm --prefix apps/worker run test -- --runInBand src/jobs/__tests__/execution-gate.test.js`
- `npm --prefix apps/worker run test -- --runInBand src/jobs/__tests__/settle_pending_cards.non-actionable.test.js`
- `npm --prefix apps/worker run test -- --runInBand src/jobs/__tests__/run_nhl_player_shots_model.test.js`
- `npm --prefix packages/models run test -- --runInBand src/__tests__/decision-policy.test.js`

**Manual validation**:
- Confirm `rg "deriveLegacyActionFromVerdict|vigCost = VIG_COST_STANDARD|slippageCost = SLIPPAGE_ESTIMATE" apps/worker/src/jobs` returns no active usages.
- Confirm the legacy settlement warning only applies when explicit `officialStatus` is missing and a legacy token drives auto-close.

**Guard for WI closeout**:
- Before marking complete: confirm every test command in `Tests to run` is runnable from repo root.
- If a script is added to a package.json as part of this WI, add it to this WI's Tests section with the full `npm --prefix <package> run <script>` form.

CLAIM: codex 2026-04-20T23:59:49Z

# WI-0460: Next.js middleware-to-proxy refactor (non-breaking)

**ID**: WI-0460
**Goal**: Remove the Next.js 16 middleware deprecation warning by migrating the web security entrypoint from `middleware` to `proxy` while preserving behavior.

**Scope**:
- `WORK_QUEUE/WI-0460.md`
- `web/src/middleware.ts`
- `web/src/proxy.ts`
- `docs/ops-runbook.md`
- `docs/security-audit-2026-03.md`

**Out of scope**:
- `WORK_QUEUE/COMPLETE/**`
- `.planning/quick/**`
- Any API auth/rate-limit/security policy changes unrelated to file convention migration

**Acceptance**:
- `web/src/middleware.ts` is replaced by `web/src/proxy.ts` with identical matcher and header behavior.
- Proxy entrypoint export is `proxy()` and Next.js build no longer shows: `The "middleware" file convention is deprecated. Please use "proxy" instead.`
- Live docs are updated to reference `proxy.ts` and no longer claim middleware deprecation is already resolved.

**Owner agent**: unassigned
**Time window**: 2026-03-16T00:00:00Z -> TBD
**Coordination flag**: solo

**Tests to run**:
- `npm --prefix web run build`
- `npm --prefix web run lint`

**Manual validation**:
- Confirm key responses still include the same security headers after migration.
- Capture one build/deploy log snippet showing absence of the middleware deprecation warning.

**Guard for WI closeout**:
- Before marking complete: confirm every test command in `Tests to run` is runnable from repo root.
- Verify changed files remain inside this WI scope only.

# WI-0911: NHL Player Blocks Projection Settlement Policy

**ID**: WI-0911
**Goal**: Formalize NHL player-blocks settlement policy across projection factual settlement and card_results so behavior is explicit, testable, and market-specific.

**Depends on**: none

**Scope**:

- apps/worker/src/jobs/settle_projections.js
- apps/worker/src/jobs/settle_pending_cards.js
- apps/worker/src/jobs/run_nhl_player_shots_model.js
- `apps/worker/src/jobs/__tests__/settle_projections.test.js`
- `apps/worker/src/jobs/__tests__/run_nhl_player_shots_model.test.js`

**Out of scope**:

- NHL blocks model feature engineering
- New blocked-shot data providers
- Cross-sport settlement refactors

**Acceptance**:

- WI defines whether nhl-player-blk is grading-eligible in card_results or strictly projection-audit-only, and enforces that choice in code.
- If grading is unsupported, rows auto-close with explicit market-specific reason metadata (not ambiguous generic errors).
- If grading is supported, line/selection/value contract is validated and test-covered.
- Registry/metadata fields align with implemented behavior for this market.

**Owner agent**: unassigned
**Time window**: 2026-04-11 -> TBD
**Coordination flag**: needs-sync

**Tests to run**:

- npm --prefix apps/worker run test -- --testPathPattern="settle_projections"
- npm --prefix apps/worker run test -- --testPathPattern="run_nhl_player_shots_model"
- npm --prefix apps/worker run test -- --testPathPattern="settle_pending_cards.phase2"

**Manual validation**:

- Generate one nhl-player-blk card and verify the final settlement state follows the declared policy exactly (graded or explicit projection-only closeout) with no silent fallback.

CLAIM: unassigned

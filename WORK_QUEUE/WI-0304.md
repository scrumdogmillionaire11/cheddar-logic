# WI-0304: Fix lint errors and formatting drift

**ID**: WI-0304
**Goal**: Resolve current lint errors and apply Prettier formatting to unblock repo checks.

**Scope**:

- `.prettierignore`
- `apps/worker/src/**`
- `packages/**/src/**`
- `web/src/**`

**Out of scope**:

- `.github/workflows/**`
- `packages/data/db/migrations/**`
- `cheddar-fpl-sage/**`
- `web-bundles/**`

**Acceptance**:

- `npm run lint` passes without errors.
- `npm run format:check` passes without errors.
- No non-format behavior changes outside the lint fixes.

**Owner agent**: unassigned
**Time window**: 2026-03-04T20:00:00Z -> 2026-03-04T23:00:00Z
**Coordination flag**: needs-sync

**Tests to run**:

- `npm run lint`
- `npm run format:check`

**Manual validation**:

- Spot-check a few formatted files for formatting-only diffs.

CLAIM: copilot 2026-03-04T20:00:00Z

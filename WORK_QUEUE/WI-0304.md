# WI-0304: Fix lint errors, formatting drift, and model execution blockers

**ID**: WI-0304
**Goal**: Resolve current lint errors, apply Prettier formatting, fix circular dependencies, and enable NBA/NHL/NCAAM model job execution in dev environment.

**Scope**:

- `.prettierignore`
- `apps/worker/src/**`
- `apps/worker/package.json`
- `apps/worker/package-lock.json`
- `packages/models/package.json`
- `packages/models/package-lock.json`
- `packages/models/src/card-utilities.js` (circular dependency fix)
- `packages/models/src/card-factory.js` (payload validation fixes)
- `packages/**/src/**`
- `web/src/**`

**Out of scope**:

- `.github/workflows/**`
- `packages/data/db/migrations/**`
- `cheddar-fpl-sage/**`
- `web-bundles/**`

**Acceptance**:

- `npm run lint` passes without errors.
- `npm run format:check` passes without errors.
- `npm audit` reports 0 vulnerabilities for apps/worker and packages/models.
- NBA model job generates cards without errors.
- NHL model job generates cards without errors.
- NCAAM model job generates cards without errors.
- No circular dependency warnings in Node.js runtime.
- No non-format behavior changes outside the lint fixes.

**Owner agent**: unassigned
**Time window**: 2026-03-04T20:00:00Z -> 2026-03-04T23:00:00Z
**Coordination flag**: needs-sync

**Tests to run**:

- `npm run lint`
- `npm run format:check`
- `npm --prefix apps/worker audit`
- `npm --prefix packages/models audit`
- `CHEDDAR_DB_PATH=/tmp/cheddar-logic/cheddar.db npm --prefix apps/worker run job:run-nba-model`
- `CHEDDAR_DB_PATH=/tmp/cheddar-logic/cheddar.db npm --prefix apps/worker run job:run-nhl-model`
- `CHEDDAR_DB_PATH=/tmp/cheddar-logic/cheddar.db npm --prefix apps/worker run job:run-ncaam-model`

**Manual validation**:

- Spot-check a few formatted files for formatting-only diffs.
- Verify no circular dependency warnings in model job output.
- Verify card generation success counts in job output (NBA: 4+, NHL: 30+, NCAAM: 5+).

CLAIM: copilot 2026-03-04T20:00:00Z

## Changes Summary

### Lint & Format Fixes (Phase 1)

- Fixed 6 lint errors: crypto redeclaration, duplicate variables, duplicate object keys, syntax error, undefined reference
- Removed 12 unused eslint-disable directives
- Formatted 150+ files with Prettier
- Added `web/src/app/favicon.ico` to .prettierignore

### Dependency & Security Fixes (Phase 2)

- Added `uuid@^9.0.0` dependency to packages/models
- Applied npm overrides to apps/worker to resolve 7 vulnerabilities (2 low, 5 high) from sqlite3 transitive dependencies
- Verified 0 remaining vulnerabilities in both packages

### Circular Dependency Fix (Phase 3)

- Fixed circular dependency in `packages/models/src/card-utilities.js`
- Changed from `require('./index')` to direct imports: `require('./card-model')` and `require('./edge-calculator')`
- Eliminated Node.js runtime warnings about accessing non-existent properties

### Card Factory Payload Fixes (Phase 4)

- Made `expiresAt` optional in `generateCard()` - now auto-calculates if not provided
- Added missing `generated_at` field to NBA/NHL payloads
- Added missing `odds_context` field to NBA/NHL payloads
- Added missing `prediction`, `confidence`, `recommended_bet_type` fields to NCAAM payloads
- Added `now` parameter to `buildBallSportPayload()` function

### Results

- ✅ NBA model: 4 cards generated, 0 failed
- ✅ NHL model: 31 cards generated, 0 failed
- ✅ NCAAM model: 5 cards generated, 0 failed
- ✅ 0 circular dependency warnings
- ✅ 0 npm audit vulnerabilities
- ✅ 0 lint errors


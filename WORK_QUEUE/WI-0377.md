# WI-0377: Wave-1 Market Math Canonicalization + Unified Card Context Display

**ID**: WI-0377
**Goal**: Make `decision_v2` the canonical source for wave-1 edge math, add a canonical payload market-context envelope, and unify cards UI market context presentation to remove duplicate/drift-prone edge sections.

**Scope**:
- `WORK_QUEUE/WI-0377.md`
- `WORK_QUEUE/WI-0376.md`
- `apps/worker/src/jobs/run_nba_model.js`
- `apps/worker/src/jobs/run_nhl_model.js`
- `packages/models/src/card-factory.js`
- `apps/worker/src/utils/decision-publisher.js`
- `web/src/app/api/games/route.ts`
- `web/src/lib/types/game-card.ts`
- `web/src/lib/game-card/transform.ts`
- `web/src/components/cards-page-client.tsx`
- `apps/worker/src/utils/__tests__/decision-publisher.v2.test.js`
- `apps/worker/src/models/__tests__/nhl-1p-totals.test.js`
- `web/src/__tests__/cards-1p-projection-source.test.js`

**Out of scope**:
- Any verdict threshold or `decision_v2` scoring logic changes
- TEAM_TOTAL and 1P math-model gap closure work (tracked separately in WI-0376)
- Settlement and `/api/results` behavior changes
- DB schema migrations

**Acceptance**:
- Wave-1 payloads include canonical `market_context` with wager + projection context for the called market.
- Decision-held rewrites keep `market_context` and legacy line/price aliases synchronized.
- `/api/games` wave-1 path stops inferring model probability from `edge + implied`; edge math is sourced from `decision_v2`.
- Web transform and cards UI consume canonical context fields and render a single market-context section without duplicate edge math sections.
- Legacy compatibility fields remain present where required, but are populated from canonical fields only.

**Owner agent**: Codex
**Time window**: 2026-03-10T19:17:34Z -> TBD
**Coordination flag**: needs-sync

**Tests to run**:
- `npm --prefix apps/worker test -- src/utils/__tests__/decision-publisher.v2.test.js`
- `npm --prefix web test -- src/__tests__/game-card-transform-truth-price.test.js`
- `npm --prefix apps/worker test -- src/models/__tests__/nhl-1p-totals.test.js`
- `node web/src/__tests__/cards-1p-projection-source.test.js`

**Manual validation**:
- Verify an NBA/NHL wave-1 play payload includes `market_context` and still renders correctly on `/cards`.
- Verify spread and total cards show coherent market context values without conflicting edge sections.
- Verify wave-1 cards do not rely on inferred `model_prob` fallback in `/api/games`.

CLAIM: Codex 2026-03-10T19:17:34Z

# WI-0645: Test suite for run_nhl_player_shots_model.js

**ID**: WI-0645
**Goal**: Add a Jest test suite for `run_nhl_player_shots_model.js` covering the no-games guard, mocked DB run, dual-run mode, and prop-cards-generated assertion — consistent with the existing NHL fault harness and props test patterns.

**Background**:
`run_nhl_player_shots_model.js` is the second NHL model runner with zero automated test coverage. It drives the SOG/blocked-shots prop cards surface which went live in production (qt-60). Its dual-run path and error propagation have never been exercised in CI.

**Scope**:
- `apps/worker/src/__tests__/run-nhl-player-shots-model.test.js` (new file)
- `apps/worker/src/jobs/run_nhl_player_shots_model.js` (read-only for mock alignment — only modify if minimal export is needed)

**Out of scope**:
- Changes to NHL player shots model logic
- NHL blocked shots prop pipeline expansion (see WI-0586 — already COMPLETE)
- Any other model runner

**Acceptance**:
- Test file exists at `apps/worker/src/__tests__/run-nhl-player-shots-model.test.js`
- Covers: no-games guard, single run with mocked DB, dual-run mode, prop cards count assertion, error propagation
- `npm --prefix apps/worker test -- --testPathPattern=run-nhl-player-shots-model` passes
- No other test files are modified

**Owner agent**: unassigned
**Time window**: TBD
**Coordination flag**: solo
**LOE**: M

**Tests to run**:
- `npm --prefix apps/worker test -- --testPathPattern=run-nhl-player-shots-model`

**Manual validation**:
- Confirm `npm --prefix apps/worker test` exits 0 with no regressions

**Guard for WI closeout**:
- Before marking complete: confirm every test command in `Tests to run` is runnable from repo root.

CLAIM: unassigned

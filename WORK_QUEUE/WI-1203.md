# WI-1203: Final Legacy Decision Cleanup After DecisionOutcome Migration

**ID**: WI-1203

**Goal**: Perform final comprehensive cleanup of legacy decision-mapper and fallback code only after the DecisionOutcome migration chain is fully landed (WI-1199 through WI-1202), with no behavior regressions.

**Scope**:

- `web/src/lib/game-card/decision.ts` (remove remaining legacy decision_v2 mapper/fallback branches; canonical builder path only)
- `web/src/lib/game-card/transform/index.ts` (remove stale status/classification fallback logic)
- `apps/worker/src/jobs/post_discord_cards.js` (remove any leftover legacy mapping/fallback branches not already removed in WI-1201)
- `apps/worker/src/jobs/publish_potd_selection.js` (remove any leftover legacy fallback/consensus branches not already removed in WI-1202)
- `apps/worker/src/utils/potd-selector.js` (remove any adapter branches that still depend on legacy decision mapping)
- `apps/worker/src/utils/discord-formatter.js` (remove decision_v2 adapter remnants, if any)
- Delete/archive legacy adapters/utilities if present and unreferenced:
  - `apps/worker/src/utils/decision-v2-to-discord.js`
  - `apps/worker/src/utils/potd-legacy-adapter.js`
  - Any unreferenced decision_v2 mapper utilities discovered by grep + import audit

**Out of scope**:

- Reworking decision_v2 production in worker
- New ranking/selection strategy changes
- API contract changes unrelated to mapper/fallback cleanup
- Running WI-1203 before WI-1201 and WI-1202 closeout gates are complete

**Acceptance**:

- WI-1203 does not begin until WI-1201 and WI-1202 are both merged and moved to `WORK_QUEUE/COMPLETE/`
- Web, Discord, and POTD surfaces consume canonical DecisionOutcome paths only; no duplicate local status/classification mapping remains
- Legacy adapter files are removed if unreferenced; otherwise explicitly documented with retention rationale
- No behavior regression in status counts (PLAY/SLIGHT_EDGE/PASS) across web vs Discord vs POTD surfaces on shared corpus inputs
- No orphaned tests remain for deleted legacy functions
- No new scope outside files listed in this WI

**Owner agent**: unassigned
**Time window**: TBD
**Coordination flag**: needs-sync
**Depends on**: WI-1202 (strict: WI-1201 + WI-1202 must be merged and present in `WORK_QUEUE/COMPLETE/` before claiming WI-1203)

**Tests to run**:

- `npm --prefix web run typecheck`
- `npm --prefix apps/worker test -- src/jobs/__tests__/post_discord_cards.test.js`
- `npm --prefix apps/worker test -- src/jobs/__tests__/publish_potd_selection.test.js`
- `npm --prefix apps/worker test -- src/utils/__tests__/potd-selector.test.js`
- `npm --prefix apps/worker test -- src/utils/discord-formatter.test.js`

**Manual validation**:

- Verify prerequisites: confirm `WORK_QUEUE/COMPLETE/WI-1201.md` and `WORK_QUEUE/COMPLETE/WI-1202.md` exist before implementation starts
- Grep audit returns no legacy mapper/fallback drift in surface modules:
  - `rg "decision-v2-to-discord|potd-legacy-adapter|fallbackToLegacy|statusRecompute|mapDecision|classifyDecision" web/src apps/worker/src --type ts --type js`
- Spot-check shared-corpus status counts across `/api/cards`, Discord message output, and POTD selection output remain aligned

**Guard for WI closeout**:

- Pre-start dependency gate verified (1201 + 1202 complete)
- All listed tests pass
- Legacy mapper/fallback grep audit returns only intentional internal decision_v2 production paths or zero hits
- No unrelated diffs
- Final comprehensive cleanup complete; ready for deployment hardening/follow-on validation

CLAIM: unassigned

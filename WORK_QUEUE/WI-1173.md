# WI-1173: Prune Truly Unused MLB Pitcher Game-Log Traceability Fields

**ID**: WI-1173
**Goal**: Remove the deprecated WI-0763 traceability fields that no longer drive MLB pitcher K projections and have no remaining in-repo runtime consumers.

**Scope**:

- `WORK_QUEUE/WI-1173.md`
- `apps/worker/src/models/mlb-model.js`
- `apps/worker/src/__tests__/mlb-k-bb-split.test.js`
- `docs/models__mlb_pitcher_k_inputs.md`

**Out of scope**:

- Any change to active WI-1173 command-context fields (`recent_bb_pct`, `recent_bb_pct_status`, `command_risk_flag`, `home_away_context`)
- Any retuning of MLB pitcher K projection math or confidence policy
- Any changes to worker job orchestration outside the model payload contract

**Implementation notes**:

- The deprecated fields are `bb_pct_from_logs`, `bb_pct_adjustment`, and `home_away_adj`.
- Repo-wide usage audit showed these fields are only referenced at their definition site, in the dedicated MLB K/BB split test, and in the pitcher-K input docs.
- Preserve all active WI-1173 command-context outputs; this WI only removes the inert compatibility fields.

**Acceptance**:

- `calculateProjectionK` no longer returns `bb_pct_from_logs`, `bb_pct_adjustment`, or `home_away_adj`.
- Dedicated MLB K/BB split tests are updated to assert the deprecated fields are absent while active command-context fields still behave as expected.
- `docs/models__mlb_pitcher_k_inputs.md` no longer documents the removed deprecated output fields as part of the live contract.

**Owner agent**: github-copilot
**Time window**: 2026-04-27T00:00:00Z -> TBD
**Coordination flag**: solo
**Depends on**: none

**Tests to run**:

- `npm --prefix apps/worker test -- src/__tests__/mlb-k-bb-split.test.js`

**Manual validation**:

- Confirm no in-repo runtime consumer reads the removed field names.

**Guard for WI closeout**:

- Before marking complete: confirm all test commands run from repo root.
- Keep the change limited to removal of the proven-dead fields and their documentation/tests.

CLAIM: github-copilot 2026-04-27T00:00:00Z

**STATUS**: COMPLETE — 2026-04-27T00:00:00Z

**Validation evidence**:

- `npm --prefix apps/worker test -- src/__tests__/mlb-k-bb-split.test.js`
- `rg -n "bb_pct_from_logs|bb_pct_adjustment|home_away_adj" apps/worker/src/models/mlb-model.js` (no matches)

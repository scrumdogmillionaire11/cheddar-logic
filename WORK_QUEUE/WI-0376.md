# WI-0376: Team Total + 1P Market Math Gap Closure and Secondary Canonical Cleanup

**ID**: WI-0376
**Goal**: Define and implement missing market-specific math for TEAM_TOTAL and 1P markets, and close secondary canonical-data gaps (alias drift + API inference drift) without changing decision thresholds.

**Scope**:
- `WORK_QUEUE/WI-0376.md`
- `WORK_QUEUE/README.md`
- `packages/models/src/edge-calculator.js`
- `packages/models/src/decision-pipeline-v2.js`
- `apps/worker/src/models/cross-market.js`
- `apps/worker/src/jobs/run_nhl_model.js`
- `apps/worker/src/jobs/run_nba_model.js`
- `web/src/app/api/games/route.ts`
- `web/src/lib/game-card/transform.ts`
- `web/src/components/cards-page-client.tsx`
- `docs/DATA_CONTRACTS.md`
- related tests for cross-market, decision publisher, and web transform/contracts

**Out of scope**:
- New market additions beyond existing TEAM_TOTAL and active 1P paths
- Any verdict/tier threshold changes
- Settlement formula changes
- DB schema migrations

**Acceptance**:
- A gap matrix is captured in this WI for TEAM_TOTAL and 1P (inputs present, missing inputs, fallback behavior, required canonical math path).
- TEAM_TOTAL pricing/edge uses team-total-specific line/price/projection fields (not full-game totals fallback) in canonical decision math.
- 1P pricing/edge is period-scoped with explicit context and method labeling.
- Wave-1 `/api/games` stops inferring model probability from `edge + implied`; canonical fair/implied/edge come from `decision_v2`.
- Compatibility aliases remain temporarily but are deterministically mapped from canonical fields (single-source mapping).
- Cards UI renders TT/1P market math context without duplicate/conflicting edge metrics on the same play.
- `/cards` background poll (60s interval) preserves stale card data on transient API errors — `setGames([])` is only called on initial load, never on background refresh failures.

**Owner agent**: unassigned
**Time window**: TBD -> TBD
**Coordination flag**: needs-sync

**Tests to run**:
- `npm --prefix apps/worker test -- src/models/__tests__/cross-market.test.js`
- `npm --prefix apps/worker test -- src/utils/__tests__/decision-publisher.v2.test.js`
- `npm --prefix web test -- src/__tests__/game-card-transform-truth-price.test.js`
- `npm --prefix web test -- --runInBand`

**Manual validation**:
- Validate TEAM_TOTAL sample cards use team-total line/price/projection consistently end-to-end.
- Validate NHL 1P cards use period-specific line/projection in displayed market math.
- Validate `/cards` shows one coherent market-math interpretation per play (no duplicate/conflicting edge values).

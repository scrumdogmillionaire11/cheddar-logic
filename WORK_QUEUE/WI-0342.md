# WI-0342: Triage Deploy Warnings Follow-up

**ID**: WI-0342
**Goal**: Address recurring non-blocking deploy warnings by stabilizing Next.js root config, migrating deprecated middleware convention, and triaging the high-severity `packages/odds` audit finding.

**Scope**:

- `web/next.config.ts`
- `web/src/middleware.ts` (or replacement `web/src/proxy.ts`)
- `packages/odds/package.json`
- `packages/odds/package-lock.json`
- `docs/DEPLOY_GITHUB.md`
- `docs/ops-runbook.md`

**Out of scope**:

- `.github/workflows/**`
- Runtime behavior changes to cards/games APIs unrelated to warning cleanup
- Repo-wide dependency upgrades outside `packages/odds`

**Acceptance**:

- Next.js deploy log no longer emits the multi-lockfile root warning (explicit Turbopack root configured).
- Deprecated middleware warning is removed by migrating to supported proxy convention.
- `packages/odds` high-severity audit issue is triaged and remediated or documented with a bounded risk decision.
- Deploy docs/runbook note the warning-clean status and any residual accepted risk.

**Owner agent**: unassigned
**Time window**: 2026-03-08T00:00:00Z -> TBD
**Coordination flag**: needs-sync

**Tests to run**:

- `npm --prefix web run build`
- `npm --prefix packages/odds audit --omit=dev`
- `npm --prefix web run lint` (if applicable)

**Manual validation**:

- Verify deploy log no longer shows lockfile root warning.
- Verify deploy log no longer shows middleware deprecation warning.
- Verify audit output for `packages/odds` is captured in WI notes with remediation outcome.

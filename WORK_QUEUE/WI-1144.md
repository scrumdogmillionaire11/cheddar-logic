# WI-1144: Simplify Results UI To One Confidence Tier System

**ID**: WI-1144
**Goal**: Render and filter projection settlement picks using a single confidence vocabulary (`LOW|MED|HIGH`) and move legacy confidence systems to diagnostics-only display.

**Scope**:

- web/src/components/results/ProjectionResultsTable.tsx
- web/src/app/results/page.tsx

**Out of scope**:

- Rewriting the ODDS_BACKED `/results` betting-lane segmentation (`PLAY` / `SLIGHT EDGE`)
- API response contract shape changes
- Worker ingestion/backfill logic
- Cross-page design-system refactors outside results views

**Acceptance**:

- Tier/Conf column and row badges display only canonical `LOW|MED|HIGH` for confidence.
- Projection settlement filtering in `/results` uses one confidence field (no `WATCH|TRUST|STRONG` mixed into row-level controls).
- Legacy confidence labels remain available only in diagnostics/tooltip context where needed for migration transparency.
- Existing result grouping/dedupe behavior remains unchanged.
- The ODDS_BACKED Betting Decision Tiers lane remains `PLAY` / `SLIGHT EDGE` in this WI.

**Owner agent**: codex
**Time window**: 2026-04-23T00:00:00Z -> TBD
**Coordination flag**: solo
**Depends on**: WI-1143

**Tests to run**:

- npm --prefix web run test:ui:results
- npm --prefix web run test:api:results:flags
- npx --prefix web tsc --noEmit -p web/tsconfig.json

**Manual validation**:

- Open `/results` and verify Tier/Conf labels show only `LOW|MED|HIGH` across MLB F5 projection rows.
- Confirm filtering/segmentation feels single-axis and no longer requires interpreting mixed vocabularies.

**Guard for WI closeout**:

- Before marking complete: confirm every test command in Tests to run is runnable from repo root.
- Ensure no projection row-level UI dependency remains on `WATCH|TRUST|STRONG` for primary segmentation.

CLAIM: codex 2026-04-23T00:00:00Z

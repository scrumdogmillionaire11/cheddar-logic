# WI-0399: Migrate Legacy play.status Branching to Canonical Decision Accessors

**ID**: WI-0399
**Goal**: Reduce legacy `play.status` branching across web code by migrating call sites to canonical action/classification resolution helpers.

**Scope**:

- `WORK_QUEUE/WI-0399.md`
- `web/src/lib/game-card/**` call sites using `play.status` for actionability decisions
- targeted tests covering status/action/classification display and filtering parity

**Out of scope**:

- Worker-side decision calculation changes
- Schema migrations
- UI redesign or new feature work unrelated to decision-field resolution

**Acceptance**:

- Identified `play.status`-first branching call sites are inventoried and prioritized.
- In-scope call sites are migrated to canonical resolver usage without changing user-visible FIRE/WATCH/PASS behavior.
- Regression tests confirm wave-1 and non-wave-1 behavior parity after migration.

**Owner agent**: unassigned
**Time window**: TBD -> TBD
**Coordination flag**: needs-sync

**Tests to run**:

- `npm --prefix web run test:card-decision`
- `npm --prefix web run test:transform:market`

**Manual validation**:

- Verify cards/filters still show identical actionability outputs for representative FIRE, WATCH, and PASS payloads.


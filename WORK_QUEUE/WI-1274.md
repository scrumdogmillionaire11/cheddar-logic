# WI-1274: Remove Vendored `packages/models/node_modules`

**ID**: WI-1274
**Goal**: Remove committed vendored dependencies under `packages/models/node_modules` and enforce install-time dependency resolution.

**Scope**:
- `packages/models/node_modules` removal
- `.gitignore`
- Package lockfiles as needed
- CI guard script/test
- `WORK_QUEUE/WI-1274.md`

**Out of scope**:
- Upgrading dependencies
- Reworking package architecture
- Runtime model logic changes

**Acceptance**:
- `packages/models/node_modules` is removed from tracked files.
- `.gitignore` prevents re-adding nested `node_modules`.
- CI/test guard fails if `packages/**/node_modules` is tracked.
- Install/test path still succeeds from lockfile-managed dependencies.

**Owner agent**: unassigned
**Time window**: 2-4 hours
**Coordination flag**: needs-sync
**Depends on**: none

**Tests to run**:
- `npm install`
- `npm test -- --runInBand`
- `git ls-files 'packages/**/node_modules/**'`

**Manual validation**:
- Confirm final tracked-files check returns no entries under `packages/**/node_modules/**`.

**Guard for WI closeout**:
- Do not mix dependency upgrades into this cleanup.

CLAIM: unassigned

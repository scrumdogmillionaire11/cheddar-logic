# WI-0437: Soccer Data Hardening — Tier 1 Market Payloads (Player Shots, Team Totals, TSOA)

**ID**: WI-0437
**Goal**: Harden soccer model inputs and output contracts for Tier 1 markets only (Player Shots, Team Totals, TSOA) so that emitted soccer cards carry real projection context, not placeholder/default values. Preserve the current runtime flow and avoid projector rebuild.

**Depends on**: `WORK_QUEUE/WI-0434.md`

**Scope**:

- `WORK_QUEUE/WI-0437.md`
- `apps/worker/src/jobs/run_soccer_model.js`
- `packages/data/src/validators/card-payload.js`
- `apps/worker/src/jobs/__tests__/run_soccer_model.test.js`
- `web/src/__tests__/canonical-play-decision.test.js`

Supporting contract files this WI must align to (read-only reference — no changes):

- `core__slate_context.md` — canonical object ownership and flagging rules
- `models__footie_normalizer.md` — canonical market keys and deterministic role tags
- `models__props_multisport.md` — allowed footie markets and thresholds
- `workflows__props_router.md` — Tier 1 / Tier 2 allowlist behavior
- `core__consistency_checks.md` — required footie validation objects and failure semantics
- `core__watchdog_fail_safes.md` — veto/downgrade rules and no-forced-play behavior
- `core__picks_output_template.md` — output fields and pass reasons

**Out of scope**:

- Full soccer environment-projector re-architecture
- Draw odds / 3-way settlement logic (`draw_odds` ingestion is deferred to a follow-up WI)
- DNB, AH, match totals, BTTS, cards, fouls — removed from Ohio scope; validator must reject them
- Tier 2 markets (Player SOT, Anytime Scorer, Team Corners) as success criteria — they are allowed as conditional bonus output only when fully qualified
- Introducing a new dedicated `/api/soccer/slate` endpoint
- NHL/NBA model changes

**Acceptance**:

1. Every emitted soccer card declares a scoped canonical market family: `player_shots`, `team_totals`, or `to_score_or_assist` (Tier 1). Tier 2 keys (`player_shots_on_target`, `anytime_goalscorer`, `team_corners`) may appear only when their gating conditions pass.
2. Tier 1 card payloads include hardened projection context: canonical market key, line, price, model projection or fair probability, edge/EV basis, eligibility gating fields, and pass/block reason when not actionable.
3. Player markets (Shots, TSOA) carry player eligibility context: starter/expected-starter status, projected minutes, role tags, and relevant per-90 hints. No card relies on fake confidence.
4. Missing critical projection context is emitted as structured flags, not silently backfilled with placeholders (`"unknown"`, `"tbd"`, `0` as fake projection, empty required arrays, generic fallback strings).
5. Validator (`card-payload.js`) enforces the Ohio market scope as a hard allowlist and rejects banned markets, placeholder field values, and price-cap violations — acting as a bouncer, not a greeter.
6. Soccer cards remain output-compatible with `/api/games?sport=SOCCER` and `/api/cards?sport=SOCCER` consumers.
7. Remaining projector gaps (draw odds, full environment-projector rebuild) are documented with explicit defer rationale, not improvised.

**Owner agent**: codex
**Time window**: 2026-03-13T16:17:00Z -> TBD
**Coordination flag**: needs-sync

**Tests to run**:

- `npm --prefix apps/worker test -- src/jobs/__tests__/run_soccer_model.test.js`
- `npm --prefix apps/worker run job:run-soccer-model:test`
- `npm --prefix web run test:decision:canonical`

**Manual validation checklist**:

- `/api/cards?sport=SOCCER` shows only Ohio-scope markets (Tier 1 dominant)
- Tier 1 payloads have real projection context (no placeholder strings)
- Tier 2 only appears when fully qualified; PASS cards include reason codes
- No legacy footie markets leak through
- No placeholder text fields survive serialization

**Remaining Gaps / Deferred**:

- Draw-market ingestion (`draw_odds`) is not currently sourced in odds normalization — deferred to a follow-up WI.
- Full environment-projector re-architecture remains out of scope.
- Legacy market restoration (1X2, DNB, AH) is explicitly deferred and requires a separate WI.
- Dedicated `/api/soccer/slate` endpoint remains deferred.

CLAIM:

## Implementation Plan

### Stage A — normalize market scope in `run_soccer_model.js`

Map all inbound soccer candidates into canonical market groups using the normalizer/router contract. Drop anything outside the Ohio allowlist with an explicit blocked reason. No ad hoc string matching.

Canonical groups: `player_shots`, `team_totals`, `to_score_or_assist`, `player_shots_on_target`, `anytime_goalscorer`, `team_corners`.

### Stage B — build minimum required context per match

Derive conservative context packet: match id / league / teams, context/style/form tags, environment fields when available, warnings/fatal markers when not. Missing footie consistency results → `GAME_FATAL` at watchdog time.

### Stage C — build Tier 1 market packets

**Player Shots**: player identity, `PRIMARY_VOLUME_SHOOTER` evidence, projected minutes / starter signal, shots/90 or team-relative shot share, line, price, projection, edge classification.

**Team Totals**: team identity, team lambda or equivalent projection basis, allowed lines only (`o0.5`, `o1.5`, `u2.5`), price, edge basis.

**TSOA**: player identity, one of `TERMINAL_NODE | PRIMARY_CREATOR | SET_PIECE_ROLE`, projected minutes / starter signal, xG/xA support, line, price, fair probability / edge basis.

### Stage D — keep Tier 2 in the passenger seat

SOT only when Shots is unavailable or poor-priced and price cap passes. Anytime only for terminal nodes at `> +180`. Corners only with extreme mismatch plus data. Otherwise emit PASS — no honorary medals for almost-data.

### Stage E — explicit degradation behavior

When core projection context is missing: do not backfill with generic defaults, emit structured warnings/flags, downgrade to PASS or skip, still allow structurally valid card with reason. Matches slate_context flagging rules and watchdog "no pick without reason."

### Stage F — tighten validator (`card-payload.js`)

Add `soccer_ohio_scope` block:

- Scope allowlist: reject any market key outside the six canonical keys.
- Required-field validation per market type (see Acceptance criteria #2–#3).
- Price caps: shots ≤ -150, TSOA ≤ -140, SOT ≤ -130, anytime > +180.
- Placeholder detection: reject `"unknown"`, `"tbd"`, `0` as fake projection, empty required arrays, generic fallback text.

### Stage G — update worker tests

Add deterministic happy-path cases (Shots, Team Totals, TSOA), rejection cases (banned market, insufficient eligibility, price cap failure, draw odds synthesized from nothing).

### Stage H — update web decision test

Assert: soccer payload with insufficient context cannot be promoted to a real play; PASS with explicit reason is acceptable; missing context must not render as actionable edge.

### Open question

Does `run_soccer_model.js` already have a stable internal shape for market objects, or is a small adapter step (`toCanonicalSoccerMarketPayload()`) needed before validation? Resolve before Stage C.

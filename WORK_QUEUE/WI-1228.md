# WI-1228: Card Visibility Integrity Audit and Lifecycle Fixes

**ID**: WI-1228
**Goal**: Prove and fix the exact break point where valid modern `Play` / `Slight Edge` cards stop flowing from `card_payloads` to `card_display_log`, `/api/games` + `/wedge`, and `/api/results` or `/api/results/projections`, without changing betting thresholds or relaxing canonical guards.

**Scope**:
- `packages/data/src/db/cards.js`
- `packages/data/__tests__/card-display-log-play-selection.test.js`
- `packages/data/__tests__/card-payload-sport.test.js`
- `web/src/lib/games/route-handler.ts`
- `web/src/lib/game-card/decision.ts`
- `web/src/lib/game-card/filters.ts`
- `web/src/components/filter-panel.tsx`
- `web/src/app/results/page.tsx`
- `web/src/lib/results/query-layer.ts`
- `web/src/lib/results/transform-layer.ts`
- `web/src/app/api/results/projection-metrics.ts`
- `web/src/__tests__/api-games-missing-data-contract.test.js`
- `web/src/__tests__/api-results-decision-segmentation.test.js`
- `web/src/__tests__/integration/results-behavioral-contract.test.ts`
- `web/src/__tests__/api-results-projection-settled-contract.test.js`
- `web/src/__tests__/game-card-pass-surface-contract.test.js`
- `web/src/__tests__/nhl-1p-projection-surfacing.test.js`
- `apps/worker/src/jobs/settle_pending_cards.js`
- `apps/worker/src/__tests__/settlement-pipeline-integration.test.js`
- `WORK_QUEUE/WI-1228.md`

**Out of scope**:
- Any model threshold, scoring, or decision production changes that alter when rows become `Play`, `Slight Edge`, or `Pass`
- Any backfill of `card_display_log` at settlement time unless there is proof the card was shown live
- Allowing modern rows without canonical `decision_v2` onto `/wedge`
- Mixing projection-only rows into main `/results`

**Acceptance**:
- A 7-day audit can deterministically classify recent rows into exactly one missing-row reason: `MISSING_DECISION_V2`, `NOT_DISPLAY_ELIGIBLE`, `DISPLAY_LOG_NOT_ENROLLED`, `API_FILTERED`, `UI_LABEL_MISMATCH`, `PROJECTION_ONLY`, `VALID_PASS`, or `BUG`
- Valid modern `Play` and `Slight Edge` rows with canonical `decision_v2` enroll into `card_display_log` when display-eligible
- Enrolled modern `Play` and `Slight Edge` rows are returned by `/api/games` unless filtered by a proven, intended contract
- Main `/results` continues to exclude settled odds-backed rows that lack `card_display_log` proof
- Projection-only `mlb-f5` / `mlb-f5-ml` rows stay out of main `/results` and remain visible on `/results/projections`
- UI copy makes `Slight Edge` semantics clear where the current `HOLD` / `WATCH` wording is ambiguous

**Owner agent**: codex
**Time window**: 2026-05-03T02:21:10Z -> 2026-05-05T02:21:10Z
**Coordination flag**: needs-sync
**Depends on**: none

**Tests to run**:
- `node web/src/__tests__/api-games-missing-data-contract.test.js`
- `npm --prefix web run test:api:results:decision-segmentation`
- `npm --prefix web run test:results:behavioral-contract`
- `npm --prefix web run test:api:results:projection-settled-contract`
- `npm --prefix apps/worker run test:pipeline:settlement -- --runInBand`
- `./node_modules/.bin/jest packages/data/__tests__/card-display-log-play-selection.test.js packages/data/__tests__/card-payload-sport.test.js --runInBand`

**Manual validation**:
- Run the 7-day SQL audit against the active DB and capture before/after counts for payload creation, display-log enrollment, `/api/games` visibility, and settlement visibility buckets
- Spot-check example card IDs from each failure bucket and confirm each maps to exactly one root-cause reason

**Guard for WI closeout**:
- No threshold or model strictness diffs
- No settlement display-log backfill introduced without live-display proof
- Modern rows missing canonical `decision_v2` still fail closed on `/wedge`
- Main `/results` only includes odds-backed rows with display-log proof
- `/results/projections` remains the only surface for projection-only `mlb-f5` / `mlb-f5-ml`

CLAIM: codex 2026-05-03T02:21:10Z

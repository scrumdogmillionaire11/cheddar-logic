# WI-0397: Eliminate False EXACT_WAGER_MISMATCH Unpriced PASS Downgrades

**ID**: WI-0397
**Goal**: Fix root-cause false `EXACT_WAGER_MISMATCH` outcomes by handling held-decision validation and NHL 1P period-scoped odds context correctly.

**Depends on**: WI-0377 complete

**Scope**:
- `WORK_QUEUE/WI-0397.md`
- `packages/models/src/decision-pipeline-v2.js`
- `apps/worker/src/jobs/run_nhl_model.js`
- `apps/worker/src/utils/__tests__/decision-publisher.v2.test.js`
- `apps/worker/src/jobs/__tests__/run_nhl_model.market-calls.test.js`

**Out of scope**:
- Team matching / canonical team-name mapping logic
- Decision gate policy (`HARD_LOCKED`, `NOT_STABLE`, `EDGE_UPGRADE`)
- Threshold changes for `PLAY/LEAN/PASS`
- `MODEL_PROB_MISSING` remediation
- DB schema/migrations

**Acceptance**:
- Held cards (`DECISION_HELD` / `published_from_gate`) are not downgraded to `EXACT_WAGER_MISMATCH` solely due to live `odds_context` drift.
- Trace integrity remains strict: held cards still fail exact-wager validation when `pricing_trace` side/line/price mismatches payload wager fields.
- NHL `1P` totals use period-scoped odds context fields for exact-wager checks.
- Worker emits 1P period odds context fields (`total_1p`, `total_price_over_1p`, `total_price_under_1p`) for `nhl-pace-1p`.
- Regression tests cover held-decision odds drift pass case, held-decision trace mismatch fail case, and NHL 1P period-context pass case.
- Post-fix verification query shows `EXACT_WAGER_MISMATCH` no longer dominated by `DECISION_HELD`.

**Owner agent**: codex
**Time window**: 2026-03-11T00:00:00Z -> TBD
**Coordination flag**: needs-sync

**Tests to run**:
- `npm --prefix apps/worker test -- --runTestsByPath src/utils/__tests__/decision-publisher.v2.test.js`
- `npm --prefix apps/worker test -- --runTestsByPath src/jobs/__tests__/run_nhl_model.market-calls.test.js`
- `npm --prefix apps/worker run job:run-nhl-model`
- `npm --prefix apps/worker run job:run-ncaam-model`

**Manual validation**:
- Query latest `card_payloads` and confirm `EXACT_WAGER_MISMATCH` is no longer primarily attached to rows containing `DECISION_HELD`.
- Verify representative held NCAAM moneyline/spread cards retain priced status when only live odds moved.
- Verify `nhl-pace-1p` cards carry `period='1P'` and 1P-specific odds context fields and do not false-fail exact wager against full-game totals.
- Validate `/cards` shows affected games without false unpriced `NO PLAY` caused by `EXACT_WAGER_MISMATCH`.

CLAIM: codex 2026-03-11T00:00:00Z

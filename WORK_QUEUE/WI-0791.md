# WI-0791: Fix MLB model test failures — projection_source and F5 floor math

| Field | Value |
|---|---|
| **ID** | WI-0791 |
| **Goal** | Fix 8 failing tests in run_mlb_model.test.js and run-mlb-model.dual-run.test.js: scorePitcherK returns SYNTHETIC_FALLBACK where tests expect FULL_MODEL or DEGRADED_MODEL; F5 ERA floor returns 2.5 instead of 4.5; dual-run mode assertion mismatch |
| **Priority** | High — 8 failures block MLB CI |
| **Depends on** | None |
| **Owner agent** | Unassigned |
| **Time window** | TBD |
| **Coordination flag** | None |

---

## Context

### Group 1 — projection_source (run_mlb_model.test.js ~L234, ~L249, ~L278 + computePitcherKDriverCards x4)

Tests expect projection_source FULL_MODEL or DEGRADED_MODEL based on pitcher data quality.
scorePitcherK now always returns SYNTHETIC_FALLBACK. Branching logic likely collapsed or pitcher input shape changed.

### Group 2 — F5 ERA floor (run_mlb_model.test.js ~L537)

Expected 4.5, received 2.5. ERA-math branch not executing; falls through to neutral synthetic floor.

### Group 3 — dual-run mode (run-mlb-model.dual-run.test.js x2)

Tests expect mode PROJECTION_ONLY in card payload. Likely same root cause as group 1.

---

## Scope

| File | Change |
|---|---|
| apps/worker/src/jobs/run_mlb_model.js | Fix scorePitcherK projection_source branching and F5 ERA floor logic |
| apps/worker/src/jobs/__tests__/run_mlb_model.test.js | Update fixtures if input shape changed and impl is correct |
| apps/worker/src/__tests__/run-mlb-model.dual-run.test.js | Update fixture/assertion if mode contract changed |

## Out of Scope

- NHL/NBA model changes; other MLB jobs

---

## Acceptance

- npm --prefix apps/worker test -- --testPathPattern="run_mlb_model|dual-run" exits 0
- All 8 previously failing tests pass; no other MLB tests regress

## Tests to run

- npm --prefix apps/worker test -- --testPathPattern="run_mlb_model|dual-run"
- npm --prefix apps/worker test  (full suite, confirm no new failures)

## Manual validation

- Run run_mlb_model.js; confirm pitcher-K cards show correct projection_source in payload
- Confirm F5 floor uses ERA math when pitcher data present

---

CLAIM: unassigned

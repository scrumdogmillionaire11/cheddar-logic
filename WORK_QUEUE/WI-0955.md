# WI-0955: Moneyline Suppression Observability and Legacy Reason-Code Cleanup

**ID**: WI-0955
**Goal**: Provide durable diagnostics for where moneyline cards die (model, gate, transform, filter) and normalize legacy reason-code sprawl so suppression attribution is trustworthy.

**Depends on**: WI-0950, WI-0952, WI-0954

**Scope**:

- `apps/worker/src/jobs/run_mlb_model.js`
- `apps/worker/src/jobs/run_nhl_model.js`
- `packages/models/src/market-eval.js`
- `docs/DATA_PIPELINE_TROUBLESHOOTING.md`
- `WORK_QUEUE/WI-0955.md`

**Out of scope**:

- New external observability vendor integration
- Full historical backfill tooling
- Product UI redesign for analytics dashboards
- Non-moneyline suppression taxonomy

**Acceptance**:

- Suppression reason logging uses normalized layer/code taxonomy for moneyline across model, worker gate, and web-visible outputs.
- Legacy/alias reason codes are mapped to canonical equivalents with deterministic fallback behavior.
- Troubleshooting doc includes one-click/one-command queries for stage-by-stage moneyline suppression attribution.
- Validation demonstrates stage totals can be reconciled without double-count ambiguity.

**Tech debt and legacy expectation**:

- Eliminate in-scope legacy reason-code aliases that create duplicate semantic categories.
- Remove dead mapping branches and inconsistent casing/prefix patterns in touched files.
- No new suppression reason should be added without canonical taxonomy placement and docs update.

**Owner agent**: unassigned
**Time window**: 2026-04-15T00:00:00Z -> TBD
**Coordination flag**: solo

**Tests to run**:

- `node --check apps/worker/src/jobs/run_mlb_model.js`
- `node --check apps/worker/src/jobs/run_nhl_model.js`
- `npm --prefix apps/worker run test -- --runInBand src/jobs/__tests__/run_mlb_model.test.js`
- `npm --prefix apps/worker run test -- --runInBand src/jobs/__tests__/run_nhl_model.test.js`

**Manual validation**:

- Execute troubleshooting queries over a fixed 72h window and confirm canonical reason layers reconcile with published payload outcomes.
- Verify no legacy alias-only reason codes appear in fresh moneyline rows.

**Guard for WI closeout**:

- Before marking complete: attach reconciliation output showing stage/layer totals and canonical reasons.
- Before marking complete: include alias-to-canonical mapping table in docs update.

CLAIM: unassigned

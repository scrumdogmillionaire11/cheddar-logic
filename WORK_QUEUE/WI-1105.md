# WI-1105: Analytics Schema Lift — Materialize Projection Accuracy Core Fields

**ID**: WI-1105
**Goal**: Reduce JSON fallback-path debt by materializing high-value projection accuracy fields into a stable query surface.

**Scope**:
- `packages/data/src/db/projection-accuracy.js`
- `packages/data/src/db/cards.js`
- `packages/data/db/migrations/` (new migration file)
- `web/src/app/api/results/route.ts`
- `web/src/__tests__/api-results-route.test.ts`
- `packages/data/src/db/__tests__/projection-accuracy.test.js` (create if missing)

**Out of scope**:
- Full historical backfill for all legacy payload rows
- Any betting model scoring logic changes
- Non-results API redesign

**Acceptance**:
- Migration introduces explicit columns or normalized table for agreed projection-accuracy core metrics.
- Projection-accuracy query prefers materialized fields and uses JSON fallbacks only for backward compatibility.
- Results API continues returning existing response contract for current consumers.
- Added/updated tests validate both materialized-field path and backward-compatible fallback path.

**Owner agent**: unassigned
**Time window**: TBD
**Coordination flag**: needs-sync on `packages/data/db/migrations/**` and `web/src/app/api/**`

**Tests to run**:
- `npm --prefix packages/data run test -- --runInBand src/db/__tests__/projection-accuracy.test.js`
- `npm --prefix web run test -- src/__tests__/api-results-route.test.ts --runInBand`

**Manual validation**:
- Run projection accuracy report against a DB containing both old and new card payload shapes; confirm metric parity.
- Spot-check SQL query plans to confirm materialized fields are used on new rows.

**Guard for WI closeout**:
- Before marking complete: confirm every test command in `Tests to run` is runnable from repo root.
- If a script is added to a package.json as part of this WI, add it to this WI's Tests section with the full `npm --prefix <package> run <script>` form.

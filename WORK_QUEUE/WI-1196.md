# WI-1196: Resolve CodeQL regex performance warning in pipeline health token normalization

**ID**: WI-1196
**Goal**: Replace regex-based token normalization in pipeline health check-id slugification with a linear scanner to remove ReDoS-style CodeQL risk.

**Scope**:

- `WORK_QUEUE/WI-1196.md`
- `packages/data/src/db/pipeline-health.js`

**Out of scope**:

- Any pipeline health schema changes
- Web/admin UI or API surface changes
- Worker watchdog behavior changes

**Acceptance**:

- `slugifyToken` no longer uses regex replacements on uncontrolled input.
- Existing pipeline health behavior remains intact for check-id normalization.
- Targeted pipeline health test passes.

**Owner agent**: github-copilot
**Time window**: 2026-04-27T00:00:00Z -> TBD
**Coordination flag**: solo
**Depends on**: none

**Tests to run**:

- `node web/src/__tests__/api-admin-pipeline-health.test.js`

**Manual validation**:

- Confirm CodeQL-commented regex pattern is absent in `slugifyToken`.

**Guard for WI closeout**:

- Before marking complete: confirm all test commands run from repo root.
- Confirm changed files match Scope only.

CLAIM: github-copilot 2026-04-27T00:00:00Z

# WI-1198: Summer NHL BLK Model Enhancement (Postseason + L10 + Pace)

**ID**: WI-1198
**Goal**: Schedule a summer enhancement pass for the NHL BLK projection model to add explicit L10 signal usage and strengthen matchup context calibration, while keeping the current L5-first path unchanged until implementation starts.

**Scope**:
- `WORK_QUEUE/WI-1198.md`
- `apps/worker/src/jobs/run_nhl_player_shots_model.js`
- `apps/worker/src/models/nhl-player-shots.js`
- `apps/worker/src/jobs/__tests__/run_nhl_player_shots_model.test.js`
- `apps/worker/src/models/__tests__/nhl-player-shots-model.test.js`
- `apps/worker/src/jobs/check_pipeline_health.js`

**Out of scope**:
- Any reintroduction of external CSV sources (NST/MoneyPuck)
- Web surface/API contract changes outside BLK payload fields
- Broad scheduler refactors unrelated to NHL BLK model scoring

**Acceptance**:
- Active BLK scoring path uses both L5 and L10 block signals (with documented weighting and rationale), not L5-only.
- Opponent pace and shot-attempt context factors are retained and recalibrated with bounded, test-covered behavior.
- Postseason/time-of-year logic remains explicit and test-covered (no regression of current playoff tightening behavior).
- No BLK logic path depends on external CSV ingestion or CSV freshness checks.
- Regression tests cover:
  - L5+L10 sensitivity behavior
  - context factor cap and fallback behavior
  - postseason factor behavior
  - parity of BLK card generation before/after refactor for baseline fixtures

**Owner agent**: unassigned
**Time window**: 2026-06-01T00:00:00Z -> 2026-08-31T23:59:59Z
**Coordination flag**: solo
**Depends on**: none

**Tests to run**:
- `npm --prefix apps/worker test -- src/jobs/__tests__/run_nhl_player_shots_model.test.js`
- `npm --prefix apps/worker test -- src/jobs/__tests__/check_pipeline_health.test.js`
- `npm --prefix apps/worker test -- src/models/__tests__/nhl-player-shots-model.test.js`

**Manual validation**:
- Compare BLK card outputs on a fixed fixture set and confirm expected L10 sensitivity deltas without reintroducing CSV dependencies.
- Confirm pipeline-health BLK checks remain decommissioned for CSV freshness/integrity and do not alert on retired CSV jobs.

**Guard for WI closeout**:
- Before marking complete: confirm all test commands run from repo root.
- Confirm no references to retired BLK CSV jobs/files remain in active code paths.

CLAIM: unassigned 2026-04-27T00:00:00Z

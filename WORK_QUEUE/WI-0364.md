# WI-0364: Main View filter leakage and NHL totals projection display gaps

**ID**: WI-0364
**Goal**: Ensure Main View actionable filters exclude PASS/NO-PLAY cards and correctly surface projected totals/model probability when present in canonical NHL payloads.

**Scope**:

- `WORK_QUEUE/WI-0364.md`
- `web/src/lib/game-card/filters.ts`
- `web/src/app/api/games/route.ts`

**Out of scope**:

- Changes to worker model thresholds/gates
- New card ranking policy changes
- Non-NHL model logic

**Acceptance**:

- With `onlyGamesWithPicks=true` and/or `hasClearPlay=true`, PASS/NO-PLAY cards do not pass filters.
- `projectedTotal` in `/api/games` resolves from available payload projection sources (`payload.projection` and `payload.play.projection`) so cards with totals projections no longer show unnecessary `N/A`.
- Existing behavior for Full Slate (PASS-inclusive view) remains intact.

**Owner agent**: GitHub Copilot
**Time window**: 2026-03-09T21:00:00Z -> TBD
**Coordination flag**: solo

**Tests to run**:

- `npm --prefix apps/worker test -- src/__tests__/pipeline-odds-to-games.test.js`
- `cd web && npm test -- src/__tests__/ui-cards-smoke.test.js`

**Manual validation**:

- In Main View preset, confirm active actionable filters no longer show PASS cards.
- On NHL totals cards with projection data in payload, verify Market Context shows projected total numeric value.

CLAIM: GitHub Copilot 2026-03-09T21:00:00Z

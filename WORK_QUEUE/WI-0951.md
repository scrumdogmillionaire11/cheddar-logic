# WI-0951: MLB T-Minus Pre-Model Odds Refresh Parity

**ID**: WI-0951
**Goal**: Add deterministic pre-model odds refresh for MLB T-minus windows so moneyline decisions evaluate against current prices instead of stale snapshots.

**Depends on**: WI-0950

**Scope**:

- `apps/worker/src/schedulers/mlb.js`
- `apps/worker/src/schedulers/main.js`
- `apps/worker/src/__tests__/scheduler-main-calibration.test.js`
- `apps/worker/src/__tests__/scheduler-windows.test.js`
- `WORK_QUEUE/WI-0951.md`

**Out of scope**:

- Execution gate threshold logic (handled by WI-0950)
- MLB model projection formulas
- Odds provider integration changes
- UI filtering and transform behavior

**Acceptance**:

- MLB T-minus windows enqueue deduped pre-model odds refresh in odds-backed mode, matching NHL freshness intent.
- Scheduler avoids duplicate key spam and maintains deterministic job keys for replay/debug.
- Existing hourly odds pulls remain intact; pre-model pulls augment, not replace, baseline cadence.
- Tests cover MLB T-minus scheduling with and without odds mode, quota constraints, and dedupe behavior.

**Tech debt and legacy expectation**:

- Remove any dead/legacy scheduler branches in touched MLB T-minus logic that conflict with new pre-model refresh path.
- Consolidate duplicated comments/conditions around MLB odds-backed mode in-scope.
- No temporary flags left behind without default-safe behavior and cleanup notes.

**Owner agent**: unassigned
**Time window**: 2026-04-15T00:00:00Z -> TBD
**Coordination flag**: solo

**Tests to run**:

- `node --check apps/worker/src/schedulers/mlb.js`
- `node --check apps/worker/src/schedulers/main.js`
- `npm --prefix apps/worker run test -- --runInBand src/__tests__/scheduler-windows.test.js`
- `npm --prefix apps/worker run test -- --runInBand src/__tests__/scheduler-main-calibration.test.js`

**Manual validation**:

- Run scheduler through simulated T-minus windows and confirm pre-model MLB odds refresh executes before `run_mlb_model`.
- Verify no duplicate MLB odds pull jobs for same T-minus window + slot.

**Guard for WI closeout**:

- Before marking complete: include sample scheduler output showing pre-refresh then model-run ordering.
- Before marking complete: verify no duplicate enqueues under repeated ticks in same slot.

CLAIM: unassigned

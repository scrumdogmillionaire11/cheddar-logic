# WI-0502: NHL calibration ledgers (CLV + projection) and settlement coverage for game + 1P + player shots

**ID**: WI-0502
**Goal**: Implement NHL calibration plumbing so odds-backed NHL cards write CLV entries, projection-only paths write projection performance entries, and settlement runs post-game for NHL full-game totals/sides, NHL 1P totals, and NHL player shots props with measurable guard metrics.

**Depends on**:

- `WORK_QUEUE/WI-0501.md`

**Scope**:

- `apps/worker/src/jobs/run_nhl_model.js`
- `apps/worker/src/jobs/run_nhl_player_shots_model.js`
- `apps/worker/src/jobs/settle_nhl_clv.js` (new)
- `apps/worker/src/jobs/settle_nhl_projections.js` (new)
- `apps/worker/src/jobs/settle_pending_cards.js`
- `apps/worker/src/jobs/__tests__/run_nhl_model.test.js`
- `apps/worker/src/jobs/__tests__/run_nhl_player_shots_model.test.js`
- `apps/worker/package.json`
- `apps/worker/src/schedulers/main.js`
- `packages/data/src/db-telemetry.js`
- `docs/DATA_PIPELINE_TROUBLESHOOTING.md`
- `docs/NHL_PLAYER_SHOTS_PROP_MARKET.md`
- `WORK_QUEUE/WI-0502.md`

**Out of scope**:

- Selector/cross-market market-pick logic
- Frontend UX changes (including `/results` settlement display segmentation, handled in `WORK_QUEUE/WI-0506.md`)
- Any NBA/NCAAM threshold tuning

**Acceptance**:

1. `clv_ledger` receives only `ODDS_BACKED` entries for eligible NHL game markets.
2. `projection_perf_ledger` receives projection-only entries, including synthetic fallback SOG paths.
3. Settlement is idempotent and covers NHL FULL_GAME totals/sides, NHL 1P totals, and NHL player shots props (backend settlement only).
4. Scheduler wiring runs settlement jobs in a lock-safe manner that preserves single-writer DB guarantees.
5. Documentation includes exact commands plus SQL evidence queries segmented by market family (FULL_GAME, 1P, player shots props).

**Owner agent**: unassigned
**Time window**: TBD -> TBD
**Coordination flag**: needs-sync

**Tests to run**:

- `npm --prefix apps/worker test -- src/jobs/__tests__/run_nhl_model.test.js`
- `npm --prefix apps/worker test -- src/jobs/__tests__/run_nhl_player_shots_model.test.js`
- `ENABLE_CLV_LEDGER=true npm --prefix apps/worker run job:run-nhl-model`
- `ENABLE_PROJECTION_PERF_LEDGER=true npm --prefix apps/worker run job:run-nhl-player-shots-model`
- `npm --prefix apps/worker run job:settle-nhl-clv`
- `npm --prefix apps/worker run job:settle-nhl-projections`

**Manual validation**:

- Capture ledger rows showing odds-backed NHL cards in `clv_ledger` and projection-only rows in `projection_perf_ledger`, including synthetic fallback SOG.
- Run settlement twice and verify no duplicate settlement rows for each market family: NHL FULL_GAME totals/sides, NHL 1P totals, and NHL player shots props.
- Attach SQL evidence for each segment (FULL_GAME, 1P, player shots props), including counts before/after rerun showing idempotency.

**Guard for WI closeout**:

- Before marking complete: attach SQL evidence for separation (`ODDS_BACKED` not present in projection ledger and vice versa) and segmented settlement coverage (FULL_GAME, 1P, player shots props).
- Confirm scheduler/manual maintenance guidance includes lock-safe sequence (stop writer before maintenance).

CLAIM: unassigned


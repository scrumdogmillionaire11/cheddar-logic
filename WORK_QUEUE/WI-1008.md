# WI-1008: Lock web Game Props visibility contract for MLB F5 ML projection cards

**ID**: WI-1008
**Goal**: Ensure `mlb-f5-ml` projection cards consistently appear in web Game Props surfaces and remain protected by regression tests.

**Depends on**: WI-1006

**Scope**:

- `WORK_QUEUE/WI-1008.md`
- `web/src/app/api/cards/route.ts`
- `web/src/app/api/cards/[gameId]/route.ts`
- `web/src/lib/games/projection-surface.ts`
- `web/src/__tests__/api-results-projection-split.test.js`
- `web/src/__tests__/game-card-transform-mlb-game-lines.test.js`

**Out of scope**:

- MLB model-generation internals
- Worker settlement logic
- UI redesign beyond preserving card visibility/contract behavior

**Acceptance**:

- API card routes include `mlb-f5-ml` in projection-surface inclusion logic and do not drop it via betting-surface payload guards.
- `mlb-f5-ml` is treated as projection-surface across route filtering and transform layers.
- Regression tests explicitly fail if `mlb-f5-ml` is excluded from Game Props API responses or family mapping.
- Existing projection-surface card behavior for `mlb-f5`, `mlb-full-game`, and `mlb-full-game-ml` remains intact.

**Owner agent**: unassigned
**Time window**: 2026-04-17 -> 2026-04-22
**Coordination flag**: needs-sync

**Tests to run**:

- `npm --prefix web run test:games-filter`
- `npm --prefix web run test:transform:market`
- `npm --prefix web run test:api:results:projection-split`

**Manual validation**:

- Query `/api/cards?sport=mlb` and verify `mlb-f5-ml` appears in the response when present in `card_payloads`.
- Open Game Props in the web app and confirm F5 ML cards render as projection-only cards, not executable bets.

CLAIM: unassigned

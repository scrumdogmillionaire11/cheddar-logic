# WI-0451: Fix FPL Analysis Crash (`datetime` Unbound) + Failed Projections Error Contract

**ID**: WI-0451
**Goal**: Fix the production crash causing analyses to fail with `cannot access local variable 'datetime' where it is not associated with a value`, and return explicit failed-analysis errors from projections endpoint.

**Scope**:
- `WORK_QUEUE/WI-0451.md`
- `cheddar-fpl-sage/src/cheddar_fpl_sage/collectors/weekly_bundle_collector.py`
- `cheddar-fpl-sage/backend/routers/analyze.py`
- `cheddar-fpl-sage/tests/api_contract_suite.py`
- `cheddar-fpl-sage/tests/test_weekly_bundle_collector_datetime_shadow.py`

**Out of scope**:
- Analysis job persistence/retention hardening (`ANALYSIS_NOT_FOUND` follow-up)
- Frontend polling/UI behavior changes
- Non-projections API contract changes

**Acceptance**:
- Weekly bundle collector no longer raises local `datetime` unbound errors in season derivation paths.
- `GET /api/v1/analyze/{analysis_id}/projections` returns:
- `500` + `ANALYSIS_FAILED` when job status is `failed` (with detailed failure message)
- `425` + `ANALYSIS_NOT_READY` for queued/running/analyzing
- Regression tests cover failed projections contract and collector datetime-shadow path.

**Owner agent**: codex
**Time window**: 2026-03-14T13:53:04Z -> TBD
**Coordination flag**: solo

**Tests to run**:
- `cd cheddar-fpl-sage && pytest tests/api_contract_suite.py -k projections`
- `cd cheddar-fpl-sage && pytest tests/test_weekly_bundle_collector_datetime_shadow.py`

**Manual validation**:
- Trigger `/api/v1/analyze` then poll `/api/v1/analyze/{id}` and `/api/v1/analyze/{id}/projections`.
- Confirm failed jobs surface explicit failure detail on projections without hanging retries.

CLAIM: codex 2026-03-14T13:53:04Z

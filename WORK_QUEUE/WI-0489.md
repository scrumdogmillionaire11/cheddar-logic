# WI-0489: NFL expansion pack (deferred after MLB)

**ID**: WI-0489
**Goal**: Execute the NFL-specific expansion path after MLB completion, keeping NFL odds-backed and projection telemetry rollout independent from MLB timing.

**Depends on**:

- `WORK_QUEUE/WI-0485.md`
- `WORK_QUEUE/WI-0488.md`

**Scope**:

- `apps/worker/src/jobs/run_nfl_model.js`
- `apps/worker/src/jobs/__tests__/run_nfl_model.test.js` (new)
- `apps/worker/src/jobs/report_settlement_health.js`
- `docs/ARCHITECTURE_SEPARATION.md`
- `docs/API_BASELINES.md`
- `docs/DATA_PIPELINE_TROUBLESHOOTING.md`
- `WORK_QUEUE/WI-0489.md`

**Out of scope**:

- MLB codepaths
- Any new sports beyond NFL
- Web UI/API contract changes

**Acceptance**:

1. NFL odds-backed cards and NFL projection-only cards are both tagged correctly in decision basis metadata and routed to the correct telemetry ledgers.
2. Settlement reporting preserves decision-basis separation for NFL metrics (`ODDS_BACKED` and `PROJECTION_ONLY` shown independently).
3. NFL-specific enablement and rollback checks are documented with runnable commands and SQL parity checks.

**Owner agent**: unassigned
**Time window**: TBD -> TBD
**Coordination flag**: needs-sync

**Tests to run**:

- `npm --prefix apps/worker test -- src/jobs/__tests__/run_nfl_model.test.js`
- `npm --prefix apps/worker run test:pipeline:settlement`
- `ENABLE_PROJECTION_PERF_LEDGER=true ENABLE_CLV_LEDGER=true npm --prefix apps/worker run job:settle-cards`
- `npm --prefix apps/worker run job:settlement-report`

**Manual validation**:

- Run a controlled NFL cycle and confirm `clv_ledger` excludes projection-only rows.
- Confirm NFL projection rows appear in `projection_perf_ledger` with `decision_basis='PROJECTION_ONLY'`.
- Verify rollback with both telemetry flags off preserves core settlement behavior.

**Guard for WI closeout**:

- Before marking complete: attach SQL evidence proving ledger inclusion/exclusion rules for NFL.
- Keep this WI unclaimed until MLB work (`WI-0487`, `WI-0488`) is complete.

CLAIM: agent-name YYYY-MM-DDTHH:MM:SSZ

# WI-0406: Centralize FT Trend Context Contract

**ID**: WI-0406
**Goal**: Replace duplicated inline `ft_trend_context` type definitions with a single shared contract so API, transform, and UI stay aligned.

**Scope**:

- `WORK_QUEUE/WI-0406.md`
- `web/src/lib/types/game-card.ts`
- `web/src/app/api/games/route.ts`
- `web/src/lib/game-card/transform.ts`
- `web/src/components/cards-page-client.tsx`
- `web/src/__tests__/cards-ft-advantage-source.test.js`

**Out of scope**:

- FT driver business logic changes
- Decision gate behavior changes
- Non-FT type cleanups elsewhere in the repo
- Broad API contract redesign

**Acceptance**:

- A single shared FT trend context type/interface is used across scoped files.
- No duplicate inline FT trend context object shapes remain in scoped files.
- Existing FT advantage behavior remains unchanged.
- Tests continue to pass with the centralized contract.

**Owner agent**: unassigned
**Time window**: TBD -> TBD
**Coordination flag**: none

**Tests to run**:

- `node web/src/__tests__/cards-ft-advantage-source.test.js`

**Manual validation**:

- Confirm FT trend context fields still flow from API to transform to cards page without drift.

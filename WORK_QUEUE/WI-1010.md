# WI-1010: Resolve projection accuracy engine debt

**ID**: WI-1010
**Goal**: Close the known technical debt left after WI-1009 by normalizing projection accuracy line-eval uniqueness, adding worker-owned backfill and market-health materialization, strengthening API tests, and documenting the legacy proxy boundary.

**Depends on**: WI-1009

**Scope**:

- `WORK_QUEUE/WI-1010.md`
- `.planning/STATE.md`
- `.planning/todos/pending/2026-04-18-clean-up-projection-accuracy-engine-debt.md`
- `.planning/todos/done/2026-04-18-clean-up-projection-accuracy-engine-debt.md`
- `packages/data/db/migrations/082_projection_accuracy_debt_cleanup.sql`
- `packages/data/src/db/projection-accuracy.js`
- `packages/data/src/db/index.js`
- `packages/data/index.js`
- `packages/data/__tests__/projection-accuracy-engine.test.js`
- `apps/worker/package.json`
- `apps/worker/src/jobs/projection_accuracy_health.js`
- `apps/worker/src/jobs/__tests__/projection_accuracy_health.test.js`
- `web/src/app/api/results/projection-accuracy/route.ts`
- `web/src/__tests__/api-results-projection-accuracy.test.js`
- `web/src/lib/types/projection-accuracy.ts`
- `web/src/types/cheddar-logic-data.d.ts`

**Out of scope**:

- Removing public legacy projection proxy exports in this PR
- Scheduling the new worker job in production cron/scheduler windows
- Changing unrelated MLB pitcher stats or games API files currently dirty in the worktree
- Odds-backed P&L, staking, or auto-betting thresholds

**Acceptance**:

- New migration rebuilds `projection_accuracy_line_evals` so `(card_id, eval_line)` is the canonical uniqueness rule and adds durable market-health storage.
- Backfill reconstructs missing projection accuracy fields from frozen/evaluable rows without mutating existing raw projections.
- Worker job performs projection accuracy backfill and market-health materialization using the writer DB path.
- Market health materialization stores sample size, win rate, MAE, bias, calibration gap, confidence lift, weak-direction share, and trust status per market family.
- `/api/results/projection-accuracy` returns materialized market-health rows and has a seeded database route test.
- Legacy `projection_proxy_evals` remains explicitly documented as compatibility-only until a separate removal WI.
- The debt todo is moved from pending to done after implementation.

**Owner agent**: codex
**Time window**: 2026-04-18 -> 2026-04-20
**Coordination flag**: solo

**Tests to run**:

- `npm --prefix packages/data test -- projection-accuracy-engine.test.js --runInBand`
- `npm --prefix apps/worker test -- projection_accuracy_health.test.js --runInBand`
- `node --import tsx/esm web/src/__tests__/api-results-projection-accuracy.test.js`
- `npm --prefix web run lint -- src/app/api/results/projection-accuracy/route.ts src/__tests__/api-results-projection-accuracy.test.js src/lib/types/projection-accuracy.ts src/types/cheddar-logic-data.d.ts`

**Manual validation**:

- Query `/api/results/projection-accuracy` against a DB with materialized market health and confirm `marketHealth` is present.
- Confirm unrelated dirty files are not staged or committed.

CLAIM: codex 2026-04-18T11:05:00Z

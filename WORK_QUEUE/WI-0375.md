# WI-0375: Results/Dashboard Rendering Coherence (Frontend)

**ID**: WI-0375

**Goal**: Fix all rendering, copy, and display-coherence bugs in the FPL Sage frontend by consuming the corrected decision contract delivered by WI-0354; no frontend component may independently infer state that the backend now provides directly.

**Depends on**: WI-0354 complete (do not start frontend fixes until backend contract is verified)

**Scope**:

- `WORK_QUEUE/WI-0375.md`
- `cheddar-fpl-sage/frontend/src/components/CaptaincySection.tsx`
- `cheddar-fpl-sage/frontend/src/components/DecisionBrief.tsx`
- `cheddar-fpl-sage/frontend/src/components/RiskNote.tsx`
- `cheddar-fpl-sage/frontend/src/components/TransferSection.tsx`
- `cheddar-fpl-sage/frontend/src/components/SquadSection.tsx`
- `cheddar-fpl-sage/frontend/src/components/DataTransparency.tsx`
- `cheddar-fpl-sage/frontend/src/pages/Results.tsx`
- `cheddar-fpl-sage/frontend/src/pages/Dashboard.tsx`
- `cheddar-fpl-sage/frontend/src/lib/api.ts` (type surface only, no logic)

**Out of scope**:

- `cheddar-fpl-sage/src/**` -- all Python backend
- Schema/database migrations
- New UI features; this is rendering correctness and coherence only
- `web/**`

**Acceptance**:

Rendering correctness:

- `expected_pts` label renders when value is `0` or any non-null numeric; falsy guard (`if (captain.expected_pts)`) is replaced with `captain.expected_pts !== undefined && captain.expected_pts !== null` in `CaptaincySection`
- Results and Dashboard consume the same `projected_xi` / `projected_bench` fields from the payload; no component recomputes lineup state from raw player flags
- `SquadSection` derives `hasTransfers` from `projected_xi` presence and non-empty primary transfer plan, not from action-string parsing

Contradiction removal:

- `getRiskStatement` in `Results.tsx` and any equivalent in `Dashboard.tsx` derive from `squad_health` only; no independent inference from raw injury/doubt counts outside that object
- `DecisionBrief` justification text and `RiskNote` text cannot simultaneously assert opposite health states (e.g. "squad is healthy" AND "3 players injured")

Planner score display:

- `confidence_label` and `confidence_summary` from payload surface in `DecisionBrief`; raw numeric score is not displayed without those metadata fields present
- Timeline detail from `fixture_planner.gw_timeline` renders in `DataTransparency` if present in payload

Strategy path display:

- Duplicate `(out, in)` path entries do not render; display is deduplicated by `(out, in)` pair in the UI even if backend sends duplicates (defensive render guard)
- No strategy path option displays a player already shown in the active squad list as the `in` target

Results / Dashboard parity:

- Both pages render captain, transfer, and projected XI from the same payload field names; no page uses a private fallback field the other has dropped

**Owner agent**: unassigned

**Time window**: TBD -> TBD

**Coordination flag**: solo

**Tests to run**:

- `cd cheddar-fpl-sage/frontend && npm run build`
- `cd cheddar-fpl-sage/frontend && npm run lint`

**Manual validation**:

- Load a completed analysis on both Results and Dashboard; confirm `expected_pts` displays for a 0-point captain
- Confirm Results and Dashboard show identical captain name, transfer arrow, and projected XI from the same analysis ID
- Confirm no contradiction between decision brief and risk note text when squad has >= 1 confirmed injury
- Confirm planner score shows `confidence_label` + one-line `confidence_summary`, not a bare number
- Confirm strategy paths show distinct moves with no impossible pick-from-own-squad targets


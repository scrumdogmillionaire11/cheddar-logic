# WI-0946: MLB Model Dead-Code Removal

**ID**: WI-0946
**Goal**: Remove confirmed dead code from mlb-model.js that is never called in production: `projectStrikeouts`, `computePitcherStatsAsOf`, `zScore`, `roundScore`, and duplicate `mlToImplied` local closures. Hoist shared `mlToImplied` to module level.

**Scope**:

- `apps/worker/src/models/mlb-model.js`
- `apps/worker/src/models/__tests__/mlb-model-gate.test.js`
- `WORK_QUEUE/WI-0946.md`

**Out of scope**:

- `buildF5SyntheticFallbackProjection` (still on hot path)
- `buildEraFallbackProjection` inside projectF5ML (behavioral change)
- Any changes to job runner, web, or other models
- resolveMLBModelSignal POTD redesign

**Acceptance**:

- `projectStrikeouts` function and export removed; its 3 gate tests removed from mlb-model-gate.test.js
- `computePitcherStatsAsOf` function and export removed
- `zScore` function removed
- `roundScore` function removed
- Duplicate `mlToImplied` local closures in projectF5ML and projectFullGameML replaced by single module-level helper `_mlToImplied`
- All tests pass: mlb-model.test.js, mlb-model-gate.test.js, run_mlb_model.test.js
- `node --check` passes on mlb-model.js

**Owner agent**: GitHub Copilot
**Time window**: 2026-04-15T00:00:00Z -> 2026-04-15T23:59:00Z
**Coordination flag**: solo
**CLAIM**: github-copilot 2026-04-15T00:00:00Z

**Tests to run**:

- `node --check apps/worker/src/models/mlb-model.js`
- `npm --prefix apps/worker run test -- --runInBand src/models/__tests__/mlb-model-gate.test.js`
- `npm --prefix apps/worker run test -- --runInBand src/models/__tests__/mlb-model.test.js`
- `npm --prefix apps/worker run test -- --runInBand src/jobs/__tests__/run_mlb_model.test.js`

**Manual validation**: Run `job:run-mlb-model` and confirm MLB cards surface as expected.

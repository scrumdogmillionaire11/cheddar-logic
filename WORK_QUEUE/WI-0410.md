# WI-0410: Repair NCAAM Schedule-to-Odds Start-Time Reconciliation

**ID**: WI-0410
**Goal**: Fix NCAAM cases where the odds provider start time is materially wrong versus ESPN schedule data, so canonical game time and downstream cards use the correct scheduled tip.

**Scope**:

- `WORK_QUEUE/WI-0410.md`
- `apps/worker/src/jobs/pull_schedule_ncaam.js`
- `apps/worker/src/jobs/__tests__/pull_schedule_ncaam.test.js`

**Out of scope**:

- NBA/NHL schedule reconciliation changes
- Web-only display formatting changes
- Broad `game_id_map` schema changes
- Odds provider adapter refactors unrelated to NCAAM schedule repair

**Acceptance**:

- NCAAM schedule ingest can reconcile an ESPN event to a unique odds-side canonical game even when the start times differ by more than 90 minutes, as long as exact teams produce a single safe candidate within the existing 24-hour search window.
- When such a reconciliation occurs, the canonical odds-side `games.game_time_utc` is corrected to the ESPN schedule time.
- Existing <=90 minute fuzzy matching behavior remains unchanged for normal cases.
- Regression coverage protects the large-delta repair path and ambiguity/no-candidate safeguards.

**Owner agent**: codex
**Time window**: 2026-03-12T00:00:00Z -> TBD
**Coordination flag**: solo

**Tests to run**:

- `npm --prefix apps/worker test -- --runTestsByPath src/jobs/__tests__/pull_schedule_ncaam.test.js`

**Manual validation**:

- Re-run NCAAM schedule ingest for the Arkansas-Pine Bluff @ Southern matchup and confirm the canonical game start updates from the stale odds time to the ESPN schedule time.
- Confirm a normal NCAAM matchup with a small time delta still maps with the existing fuzzy path.

CLAIM: codex 2026-03-12T00:00:00Z

# WI-1167: Canonical Cards Eligibility Gate Unification

**ID**: WI-1167
**Goal**: Remove duplicated card eligibility logic across SQL and post-query filtering by defining one canonical eligibility path for `/api/cards`.

**Scope**:

- `web/src/app/api/cards/route.ts`
- `web/src/lib/cards/query.ts`
- `web/src/lib/cards/payload-classifier.ts`
- `web/src/lib/games/projection-surface.ts`
- `web/src/__tests__/cards-projection-exclusion.test.js`
- `web/src/__tests__/api-cards-sport-filter.test.js`
- `WORK_QUEUE/WI-1167.md`

**Out of scope**:

- Changes to worker card production payloads
- New product surface types unrelated to existing cards endpoint
- Large UI refactors

**Acceptance**:

- Eligibility checks for projection-only/synthetic filtering are evaluated in one canonical layer, not duplicated.
- The retained layer has explicit reason-code semantics shared with diagnostics from WI-1166.
- `nhl-player-shots` and other intended visible card types are not dropped by duplicate logic paths.
- Existing contracts for projection-only exclusion still pass tests.

**Owner agent**: unassigned
**Time window**: 2026-04-24T00:00:00Z -> TBD
**Coordination flag**: needs-sync
**Depends on**: WI-1166

**Tests to run**:

- `npm --prefix web run test:cards:projection-exclusion`
- `npm --prefix web run test:cards-sport-filter`
- `npm --prefix web run test:api:nhl-contract`

**Manual validation**:

- Verify `/api/cards?sport=nhl` includes expected player shots cards in environments where DB rows exist and are eligible.
- Verify projection-only rows still suppress correctly where policy requires suppression.


CLAIM: unassigned 2026-04-24T00:00:00Z

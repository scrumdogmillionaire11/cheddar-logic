# WI-1277: Optimize /api/cards Dedupe Hot Path and Index Coverage

**ID**: WI-1277
**Goal**: Reduce `/api/cards` and `/api/cards/[gameId]` latency by tightening hot SQL paths and adding targeted indexes for run-scope and settled suppression predicates.

**Scope**:
- `web/src/app/api/cards/route.ts`
- `web/src/app/api/cards/[gameId]/route.ts`
- `web/src/lib/cards/query.ts`
- `packages/data/db/migrations/084_cards_query_hotpath_indexes.sql` (new)
- `web/src/__tests__/api-cards-performance-budget.test.ts` (new)

**Out of scope**:
- Any API contract or response-shape changes
- Any betting/model decision logic changes
- Any worker write-path changes beyond index migration

**Acceptance**:
- P95 latency improves for representative `/api/cards` requests under fixture load.
- Query plan for hot filters avoids broad scans where new indexes apply.
- Dedupe correctness remains unchanged for projection and odds-backed surfaces.
- Performance budget test captures regression thresholds.

**Owner agent**: unassigned
**Time window**: 4-6 hours
**Coordination flag**: needs-sync
**Depends on**: none

**Tests to run**:
- `npm --prefix web exec tsx src/__tests__/api-cards-performance-budget.test.ts`
- `npm --prefix web exec tsx src/__tests__/integration/settlement-parity.test.ts`

**Manual validation**:
- Compare latency and row parity for `/api/cards` and `/api/cards/<gameId>` before/after.
- Run `EXPLAIN QUERY PLAN` for key SQL statements and capture index usage evidence.

**Failure modes**:
- Added indexes increase write overhead during worker settlement windows.
- Query rewrite changes dedupe winner ordering.
- Migration applies successfully but planner still prefers old scan path.

CLAIM: unassigned

# WI-0550: Fix NBA Spread Call Quality — Edge Gate + Settlement Void Rate

**ID**: WI-0550
**Goal**: Two targeted fixes to make `nba-spread-call` actionable:

1. Gate spread card generation on `edge > 0.02` so negative-EV spread calls are never emitted.
2. Diagnose and fix the 82% void rate on `nba-spread-call` settlement so settled results populate correctly.

**Background**: Dr. Claire health audit (2026-03-22) found:

- `nba-spread-call` has 85.7% hit rate / +71.4% ROI on 14 settled plays — the best driver in the system
- But 139/153 spread results (82%) resolve as `error/void` vs 43% void rate for `nba-totals-call`
- Today's generated spread calls include two with edge of -24% to -27% that still emit as `kind: "PLAY"`

**Scope**:

- `WORK_QUEUE/WI-0550.md`
- `apps/worker/src/jobs/run_nba_model.js` (edge gate on spread card emission)
- Settlement pipeline file(s) responsible for `nba-spread-call` result resolution (identify during investigation)
- `apps/worker/src/jobs/__tests__/` (unit tests for edge gate)

**Out of scope**:

- Changes to NCAAM, NHL, or NBA totals settlement
- DB schema/migration changes
- Spread model logic or driver weighting
- UI/web changes

**Acceptance**:

- No `nba-spread-call` card is generated with `edge_pct <= 0.02`
- `nba-spread-call` void rate drops to ≤50% (matching totals baseline) after settlement fix
- Existing spread cards with valid positive edge continue to generate normally
- Unit test asserts edge gate: spread call with `edge = -0.25` produces no card output
- Unit test asserts edge gate: spread call with `edge = 0.08` produces card output

**Owner agent**: codex
**Time window**: 2026-03-22T00:00:00Z -> TBD
**Coordination flag**: none

**Tests to run**:

- `npx jest apps/worker/src/jobs/__tests__/run_nba_model --no-coverage`
- Verify no negative-edge spread cards in next scheduled NBA model run

**Manual validation**:

- After deploy, run `job:run-nba-model` and confirm all generated `nba-spread-call` cards have `edge_pct > 0.02`
- Check `card_results` after next settlement cycle — `nba-spread-call` void rate should be comparable to `nba-totals-call`

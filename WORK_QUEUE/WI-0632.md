# WI-0632: Test suite for run_nfl_model.js

**ID**: WI-0632
**Goal**: Add a test file for `run_nfl_model.js` covering model card generation, PASS conditions, and market-call card paths — mirroring the patterns in `run_nhl_model.test.js` and `run_nba_model.test.js`.

**Background**:
`apps/worker/src/jobs/run_nfl_model.js` is 341 lines and produces live NFL cards with zero test coverage. Every other sport model (NHL, NBA, MLB, Soccer, NCAAM) has at least one test file. NFL is the only gap. The file uses the same `@cheddar-logic/models` + in-memory DB pattern as the tested models — a test suite can be added without new infrastructure.

**Scope**:
- `apps/worker/src/jobs/__tests__/run_nfl_model.test.js` (new) — tests for:
  - Model runs with minimal valid odds snapshot → card is generated or PASS is returned
  - PASS conditions: missing odds, edge below threshold, `ENABLE_NFL_MODEL=false`
  - Market-call card path where applicable (mirror `run_nhl_model.market-calls.test.js` pattern)

**Out of scope**:
- Changes to `run_nfl_model.js` logic
- Integration tests against live APIs

**Acceptance**:
- `npm --prefix apps/worker test -- run_nfl_model` passes.
- At minimum: one FIRE card path and one PASS path are tested.
- No real network calls — all external deps mocked or seeded via in-memory DB fixtures.

**Owner agent**: unassigned
**Time window**: TBD
**Coordination flag**: solo

**Tests to run**:
- `npm --prefix apps/worker test -- run_nfl_model`

**Manual validation**:
- N/A — fully automated.

**Guard for WI closeout**:
- Before marking complete: confirm every test command in `Tests to run` is runnable from repo root.

CLAIM: unassigned

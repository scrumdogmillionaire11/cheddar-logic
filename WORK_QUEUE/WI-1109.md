# WI-1109: NHL 1P Production Settlement Data Hygiene — Telemetry Guard + SQL Cleanup

**ID**: WI-1109
**Goal**: Prevent polluted NHL 1P settlement rows from distorting production telemetry by tightening report filters and shipping a preview-first cleanup SQL for invalid/duplicate rows.

**Scope**:

- `apps/worker/src/jobs/report_telemetry_calibration.js`
- `apps/worker/src/jobs/__tests__/report_telemetry_calibration.test.js`
- `scripts/sql/cleanup_nhl_1p_settlement_prod.sql` (new)

**Out of scope**:

- Broad multi-market settlement rewrites
- Non-NHL telemetry behavior changes
- DB schema migrations
- Web UI contract changes

**Acceptance**:

- NHL 1P baseline source query excludes non-settled card rows.
- NHL 1P baseline source query deduplicates repeated card rows deterministically.
- Existing and new report telemetry tests pass for NHL 1P baseline behavior.
- New production SQL script provides backup + preview-first delete flow for invalid/duplicate NHL 1P settlement rows.

**Owner agent**: github-copilot
**Time window**: 2026-04-21
**Coordination flag**: solo
**CLAIM**: github-copilot 2026-04-21T00:00:00Z

**Tests to run**:

- `npm --prefix apps/worker run test -- src/jobs/__tests__/report_telemetry_calibration.test.js --runInBand`

**Manual validation**:

- Run SQL script in preview mode on production DB and verify candidate counts/sample rows before any commit run.
- Re-run telemetry calibration report and verify NHL 1P sample size and directional breakdown normalize.

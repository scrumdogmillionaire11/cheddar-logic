# WI-0540: Prevent no-bet soccer spread cards from rendering as LEAN with odds

**ID**: WI-0540
**Goal**: Fix card UI inconsistency where spread cards with no canonical bet render as `LEAN` and display `NO PLAY (+odds)`, causing confusing no-play surfaces.

**Scope**:
- `WORK_QUEUE/WI-0540.md`
- `web/src/components/cards-page-client.tsx`
- `web/src/__tests__/cards-no-bet-display-contract.test.js` (new)

**Out of scope**:
- Model/worker edge thresholds or canonical decision generation
- `web/src/lib/game-card/transform.ts` decision policy changes
- API contract changes

**Acceptance**:
- Cards with no canonical bet (`displayPlay.bet` absent) render `PASS` in UI unless explicitly held for verification guardrails.
- `NO PLAY` text never appends live odds (no `NO PLAY (+100)` rendering).
- Existing verification-hold UX remains intact.
- New source-contract test asserts both behaviors.

**Owner agent**: codex
**Time window**: 2026-03-21T02:01:06Z -> TBD
**Coordination flag**: solo

**Tests to run**:
- `npm --prefix web test -- src/__tests__/cards-no-bet-display-contract.test.js`

**Manual validation**:
- Open a soccer spread card with no canonical bet and confirm verdict badge is `PASS`.
- Confirm primary bet text shows exactly `NO PLAY` without odds suffix.
- Verify verification-required cards still show hold/watch-style context when applicable.

**Guard for WI closeout**:
- Keep changes limited to display-layer behavior; do not alter canonical decision payload semantics.

CLAIM: codex 2026-03-21T02:01:06Z

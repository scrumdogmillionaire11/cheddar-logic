# WI-0390: NCAAM FT% Spread Rule (Total < 160) + Backtest

**ID**: WI-0390
**Goal**: Add NCAAM free-throw percentage ingestion with TeamRankings CSV fallback, emit a spread-only FT driver for `total < 160`, and ship a deterministic backtest command for this exact rule.

**Scope**:
- `WORK_QUEUE/WI-0390.md`
- `packages/data/src/espn-client.js`
- `packages/data/src/team-metrics.js`
- `packages/data/src/teamrankings-ft.js`
- `packages/data/src/odds-enrichment.js`
- `packages/data/src/validators/card-payload.js`
- `packages/data/__tests__/team-metrics-resolution.test.js`
- `packages/data/__tests__/teamrankings-ft.test.js`
- `apps/worker/src/models/index.js`
- `apps/worker/src/models/__tests__/ncaam-drivers.test.js`
- `apps/worker/src/jobs/run_ncaam_model.js`
- `apps/worker/src/jobs/refresh_ncaam_ft_csv.js`
- `scripts/backtest-ncaam-ft-spread-rule.js`
- `scripts/refresh-teamrankings-ncaam-ft.js`
- `package.json`
- `apps/worker/package.json`
- `docs/NCAAM_FT_SPREAD_MODEL.md`
- `data/input/teamrankings_ncaam_ft_pct.csv`
- `web/src/lib/game-card/transform.ts`
- `web/src/lib/game-card/driver-scoring.ts`
- `web/src/app/api/games/route.ts`
- `apps/worker/src/schedulers/main.js`
- `web/src/lib/game-card/presets.ts`
- `web/src/__tests__/filters-ncaam-ft-trend-preset.test.js`
- `web/src/components/cards-page-client.tsx`
- `web/src/__tests__/cards-ft-advantage-source.test.js`

**Out of scope**:
- Changes to existing NCAAM base projection/rest/matchup formulas and thresholds
- New migrations or schema changes
- Changes to web UI rendering behavior
- Changes to settlement grading rules beyond using existing spread parity in backtest

**Acceptance**:
- Team metrics include `freeThrowPct` and `freeThrowPctSource` for NCAAM when available.
- FT fallback order is `ESPN -> TeamRankings CSV -> null` with explicit warning logs for missing/stale CSV.
- `run_ncaam_model` can emit `ncaam-ft-trend` cards only when:
  - `total < 160`
  - one team `freeThrowPct > 75`
  - opponent `freeThrowPct < 75`
- FT driver emits spread market cards only (no moneyline clone).
- `ncaam-ft-trend` payloads pass validator (legacy `ncaam-ft-spread` still accepted).
- Backtest command executes and prints deterministic summary metrics:
  - plays, wins, losses, pushes
  - win_rate_ex_push
  - roi_units
  - units_per_play
  - sample_size_warning (<100 plays)
- Existing NCAAM drivers continue functioning unchanged.

**Owner agent**: codex
**Time window**: 2026-03-11T01:15:11Z -> TBD
**Coordination flag**: needs-sync

**Tests to run**:
- `npm --prefix packages/data test -- --runTestsByPath __tests__/team-metrics-resolution.test.js`
- `npm --prefix apps/worker test -- --runTestsByPath src/models/__tests__/ncaam-drivers.test.js`
- `node scripts/backtest-ncaam-ft-spread-rule.js`

**Manual validation**:
- Run NCAAM model job and confirm FT rule cards appear only when rule conditions are met.
- Inspect latest NCAAM payload and verify `driver.inputs` includes FT fields and rule context.
- Confirm missing/stale TeamRankings CSV logs are explicit and non-fatal.

CLAIM: codex 2026-03-11T01:15:11Z

---

## Closeout Note (2026-03-11)

- Core WI-0390 deliverables are implemented and validated (FT ingestion + fallback, FT spread driver, deterministic backtest, tests, and UI FT guidance copy).
- Remaining behavior question is decision semantics for FT-trend cards showing `PASS` instead of actionable `PLAY/LEAN` despite qualifying FT signal.
- Follow-up logged as `WI-0396` to explicitly redesign and test FT-trend status mapping.
- WI-0390 can be considered complete pending normal closeout workflow.

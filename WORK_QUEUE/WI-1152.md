# WI-1152: FPL Sage critical squad failure recovery

**ID**: WI-1152
**Goal**: Add a hard FPL Sage recovery state that prevents hold/fallback outputs when the next-GW XI is structurally invalid.

**Scope**:
- `WORK_QUEUE/WI-1152.md`
- `cheddar-fpl-sage/src/cheddar_fpl_sage/analysis/enhanced_decision_framework.py`
- `cheddar-fpl-sage/src/cheddar_fpl_sage/analysis/decision_framework/chip_analyzer.py`
- `cheddar-fpl-sage/src/cheddar_fpl_sage/analysis/decision_framework/chip_engine/wildcard_free_hit.py`
- `cheddar-fpl-sage/backend/services/result_transformer.py`
- `cheddar-fpl-sage/backend/models/manual_overrides.py`
- `cheddar-fpl-sage/backend/routers/analyze.py`
- `cheddar-fpl-sage/tests/test_chip_engine_wildcard_free_hit.py`
- `cheddar-fpl-sage/tests/api_contract_suite.py`
- `cheddar-fpl-sage/tests/tests_new/test_api_endpoints.py`

**Out of scope**:
- Frontend/dashboard rendering changes beyond additive API fields
- Non-FPL systems under `web/`, `apps/worker/`, or `packages/`
- Unrelated in-branch edits already present in this worktree

**Acceptance**:
- FPL Sage emits `CRITICAL_SQUAD_FAILURE` before normal decision logic when XI legality or blank exposure crosses the defined guardrails.
- Critical-state outputs never return `HOLD`, `ROLL_TRANSFER`, threshold-hold copy, or transformer captain fallback text.
- FH/WC/forced-transfer recovery uses the planned posture-aware ordering and additive API fields are exposed on detailed analysis responses.

**Owner agent**: codex
**Time window**: 2026-04-23T21:13:54Z -> TBD
**Coordination flag**: solo
**Depends on**: none (operator override to execute on `aj/potd` despite top-level FPL freeze)

**Tests to run**:
- `cd cheddar-fpl-sage && pytest tests/test_chip_engine_wildcard_free_hit.py`
- `cd cheddar-fpl-sage && pytest tests/api_contract_suite.py`
- `cd cheddar-fpl-sage && pytest tests/tests_new/test_api_endpoints.py`

**Manual validation**:
- Trigger a detailed analysis payload for a blank/infeasible squad fixture horizon and confirm the API returns `decision_state=CRITICAL_SQUAD_FAILURE` with recovery fields populated.
- Confirm non-critical squads still return normal transfer/chip/captain outputs without regression in captain payload shape.

**Guard for WI closeout**:
- Before marking complete: confirm every test command in `Tests to run` executes from repo root.
- Do not revert or rewrite unrelated existing edits on `aj/potd`; only mutate files listed in `Scope`.

CLAIM: codex 2026-04-23T21:13:54Z

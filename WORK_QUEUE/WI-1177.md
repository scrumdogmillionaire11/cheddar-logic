# WI-1177: Clear Current Web Unused-Variable Warnings

**ID**: WI-1177
**Goal**: Remove the current unused-variable warnings in scoped web files without changing runtime behavior or widening cleanup beyond the reported warning set.

**Scope**:
- `web/src/components/cards/GameCardItem.tsx`
- `web/src/app/api/results/projection-settled/route.ts`
- `web/src/__tests__/reason-labels-sync.test.js`
- `web/src/__tests__/codebase-hygiene.test.js`

**Out of scope**:
- Any results API behavior changes beyond removing the unused local
- Any GameCard UI behavior or odds-book logic changes beyond deleting dead code
- Repo-wide lint cleanup outside the four reported warning sites

**Acceptance**:
- The unused `resolvePlayLiveLineBook` binding is removed from `GameCardItem.tsx` unless the implementation is intentionally restored and exercised in-scope.
- The unused `_canonicalAnalyticsPresent` local is removed or inlined in `projection-settled/route.ts` without changing route output.
- The unused `assert` imports/bindings are removed from both scoped test files while preserving their current test behavior.
- `npm --prefix web run lint -- src/components/cards/GameCardItem.tsx src/app/api/results/projection-settled/route.ts src/__tests__/reason-labels-sync.test.js src/__tests__/codebase-hygiene.test.js` completes without these warnings.

**Owner agent**: github-copilot
**Time window**: 2026-04-24T00:00:00Z -> TBD
**Coordination flag**: solo
**Depends on**: none

**Tests to run**:
- `npm --prefix web run lint -- src/components/cards/GameCardItem.tsx src/app/api/results/projection-settled/route.ts src/__tests__/reason-labels-sync.test.js src/__tests__/codebase-hygiene.test.js`
- `npm --prefix web run test:reason-labels-sync`
- `node web/src/__tests__/codebase-hygiene.test.js`

**Manual validation**:
- Confirm the warning list is reduced to zero for the four scoped files in the next lint/test pass.

**Guard for WI closeout**:
- Before marking complete: confirm every test command in `Tests to run` is runnable from repo root.
- Keep the fix minimal: remove dead bindings rather than refactoring unrelated logic.

CLAIM: github-copilot 2026-04-27T00:00:00Z

**STATUS**: COMPLETE — 2026-04-27T00:00:00Z

**Validation evidence**:

- `npm --prefix web run lint -- src/components/cards/GameCardItem.tsx src/app/api/results/projection-settled/route.ts src/__tests__/reason-labels-sync.test.js src/__tests__/codebase-hygiene.test.js`
- `npm --prefix web run test:reason-labels-sync`
- `node web/src/__tests__/codebase-hygiene.test.js`

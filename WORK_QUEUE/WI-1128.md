# WI-1128: Replace Source-String Contract Tests With Behavioral Tests

**ID**: WI-1128
**Goal**: Convert brittle routeSource.includes style tests into runtime behavioral tests with fixture-driven assertions.

**Scope**:
- web/src/__tests__/api-admin-model-health.test.js
- web/src/__tests__/api-potd.test.js
- web/src/__tests__/ui-results-smoke.test.js
- web/src/__tests__/helpers/**

**Out of scope**:
- End-to-end browser test stack adoption
- Worker-side Jest suite changes

**Acceptance**:
- Source-string checks are removed or minimized to non-behavioral static policy assertions only.
- Replacement tests execute route handlers and assert response behavior and contracts.
- Tests remain deterministic in CI using fixtures or controlled database seed setup.

**Owner agent**: unassigned
**Time window**: 2026-04-21T00:00:00Z -> TBD
**Coordination flag**: solo
**Depends on**: WI-1124, WI-1126, WI-1127

**Tests to run**:
- node web/src/__tests__/api-admin-model-health.test.js
- node web/src/__tests__/api-potd.test.js
- npm --prefix web run test:ui:results

**Manual validation**:
- Review transformed tests to confirm they no longer rely on raw source token matching for primary behavior checks.

**Guard for WI closeout**:
- Before marking complete: confirm every test command in Tests to run is runnable from repo root (i.e., exists in web/package.json scripts or is a valid node path/to/test.js command).
- If a script is added to web/package.json as part of this WI, add it to this WI's Tests section with the full npm --prefix web run <script> form.

CLAIM: unassigned 2026-04-21T00:00:00Z

# WI-0612: team-metrics.js test suite — metric computation coverage

**ID**: WI-0612
**Goal**: Add a test file for `packages/data/src/team-metrics.js` covering the core metric computations so regressions are caught before model outputs degrade.

**Background**:
`team-metrics.js` (1,284 lines) computes team-level metrics (attempt rates, defensive zone factors, etc.) consumed by multiple sport models. No test file was found. Metric regressions only surface when model outputs visibly degrade — by that point, cards may already be published.

**Scope**:
- `packages/data/src/__tests__/team-metrics.test.js` (new) — unit tests for core computation functions using fixture data; assert output types and boundary conditions (zero games, null fields)
- No changes to `team-metrics.js` itself unless bugs are found during test authoring

**Out of scope**:
- Integration tests against a real DB
- Changes to sport model consumers

**Acceptance**:
- Test file exists and `npm --prefix packages/data test -- team-metrics` passes.
- Tests cover at least 3 core metric functions with known input/output pairs.
- Null/missing field inputs are handled gracefully (no thrown exceptions).

**Owner agent**: unassigned
**Time window**: TBD
**Coordination flag**: solo

**Tests to run**:
- `npm --prefix packages/data test -- team-metrics`

CLAIM: unassigned

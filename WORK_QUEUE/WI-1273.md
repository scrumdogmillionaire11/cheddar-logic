# WI-1273: Extract Game Route Assembly Helpers

**ID**: WI-1273
**Goal**: Reduce change risk in `web/src/lib/games/route-handler.ts` by extracting bounded slate assembly and diagnostics helpers.

**Scope**:
- `web/src/lib/games/route-handler.ts`
- `web/src/lib/games/slate-assembly.ts` (new)
- `web/src/lib/games/diagnostics.ts` (new)
- `web/src/__tests__/games-route-assembly.test.ts` (new)

**Out of scope**:
- API response contract changes
- Game-card transform changes
- Filtering threshold changes

**Acceptance**:
- Route handler delegates slate assembly to helper module.
- Diagnostics construction is isolated and tested.
- `/api/games` response shape remains unchanged.
- Tests prove response parity on existing fixture.

**Owner agent**: unassigned
**Time window**: 4-6 hours
**Coordination flag**: needs-sync
**Depends on**: WI-1267

**Tests to run**:
- `npm --prefix web exec tsx src/__tests__/games-route-assembly.test.ts`
- `npm --prefix web exec tsx src/__tests__/api-games-market-smoke.test.js`

**Manual validation**:
- Call `/api/games` before/after and confirm parity for top-level shape and key diagnostics fields.

**Guard for WI closeout**:
- Avoid semantic behavior changes while extracting helper modules.

CLAIM: unassigned

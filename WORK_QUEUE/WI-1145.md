# WI-1145: Align Worker Projection Ingestion And Backfill To Canonical Confidence Tiers

**ID**: WI-1145
**Goal**: Ensure projection settlement ingestion and one-time remediation paths produce canonical confidence tiers consistently for historical and newly-settled rows.

**Scope**:

- apps/worker/src/jobs/settle_projections.js
- apps/worker/src/jobs/\_\_tests\_\_/settle_projections.test.js

**Out of scope**:

- UI display logic
- Non-projection settlement pipelines
- Broad schema migrations unrelated to confidence normalization

**Acceptance**:

- Settlement ingestion resolves confidence from canonical sources (`confidence_band`, `drivers[0].confidence_band`, score-based fallback) without emitting null/unknown tiers for eligible rows.
- Canonical confidence mapping is deterministic for both current and legacy payload shapes.
- Focused worker tests cover legacy payload compatibility and canonical tier derivation behavior.
- Single-writer DB contract remains respected (no web-side writes introduced).

**Owner agent**: unassigned
**Time window**: 2026-04-22T00:00:00Z -> TBD
**Coordination flag**: needs-sync
**Depends on**: WI-1143

**Tests to run**:

- npm --prefix apps/worker run test -- src/jobs/\_\_tests\_\_/settle_projections.test.js --runInBand
- npm --prefix apps/worker run test -- src/\_\_tests\_\_/tracking.projection-audit.test.js --runInBand

**Manual validation**:

- Run settlement job in a local/staging-safe environment and verify new rows carry canonical confidence values.
- Spot-check legacy-like payload fixtures to confirm fallback path correctness.

**Guard for WI closeout**:

- Before marking complete: confirm every test command in Tests to run is runnable from repo root.
- Preserve existing settlement output contracts except for confidence normalization hardening.

CLAIM: unassigned 2026-04-22T00:00:00Z

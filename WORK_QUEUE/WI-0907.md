# WI-0907: Recovery-Path Policy Audit and Failure-Mode Classification

**ID**: WI-0907
**Goal**: Classify all missing/inconsistent input recovery paths as hard fail, soft pass, degraded output, hidden output, retry, or fallback, and remove silent inconsistency.

**Depends on**: WI-0900, WI-0901

**Scope**:

- `apps/worker/src/jobs/**/*.js`
- `packages/models/src/**/*.js`
- `web/src/lib/games/route-handler.ts`
- `web/src/lib/game-card/transform/index.ts`
- `docs/audits/recovery-path-classification.md`
- `.planning/codebase/HARDENING_AUDIT.md`

**Out of scope**:

- Incident tooling overhaul
- Provider contract renegotiation
- New alert transport integrations

**Acceptance**:

- Every major failure mode is classified into one recovery bucket.
- Silent degradations are eliminated or explicitly marked with reason code and visibility policy.
- Retry/fallback behavior is consistent for equivalent failure classes across sports.
- Mapper prompt includes a Config and State Truth Audit directive for future audits.

**Owner agent**: unassigned
**Time window**: 2026-04-11 -> TBD
**Coordination flag**: needs-sync

**Tests to run**:

- `npm --prefix apps/worker run test`
- `npm --prefix web run test:ci:smoke`
- `npm --prefix web run build`

**Manual validation**:

- Inject one missing-input and one inconsistent-input scenario and confirm both produce deterministic recovery class, reason code, and endpoint visibility outcome.

CLAIM: unassigned

# WI-0596: Pitcher Ks data foundations and freshness gates

**ID**: WI-0596
**Goal**: Implement required pitcher K input assembly and freshness/fail-closed behavior from `docs/pitcher_ks/08datareqs.md` and `docs/pitcher_ks/data_specs/**`.

**Scope**:

- `apps/worker/src/jobs/pull_mlb_pitcher_stats.js`
- `apps/worker/src/jobs/pull_mlb_weather.js`
- `apps/worker/src/jobs/run_mlb_model.js`
- `apps/worker/src/jobs/__tests__/run_mlb_model.test.js`

**Out of scope**:

- Odds ingestion of pitcher strikeout prop lines
- Web/API presentation changes
- Settlement grading changes

**Acceptance**:

- Runner enforces required input checks for pitcher profile, matchup context, and umpire/park/weather dependencies before final decision.
- Missing critical inputs fail closed with explicit `ingest_failure_reason_code`/diagnostic fields instead of silent fallback.
- Input shape alignment is documented in code against `pitcher_input_schema.md`, `matchup_input_schema.md`, and `umpire_schema.md`.
- Tests cover stale/missing data gates and pass-path data completeness.

**Owner agent**: unassigned
**Time window**: 2026-03-26T00:00:00Z -> TBD
**Coordination flag**: solo

**Tests to run**:

- `npm --prefix apps/worker run test -- src/jobs/__tests__/run_mlb_model.test.js`

**Manual validation**:

- Run `set -a; source .env; set +a; npm --prefix apps/worker run job:run-mlb-model` after intentionally removing one required input source and confirm fail-closed diagnostics are emitted.

**Guard for WI closeout**:

- Before marking complete: confirm every test command in `Tests to run` is runnable from repo root.
- Before marking complete: verify no silent defaulting path bypasses required freshness gates.

CLAIM: unassigned

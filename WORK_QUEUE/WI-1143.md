# WI-1143: Unify Projection Confidence Tier Contract At Results API Boundary

**ID**: WI-1143
**Goal**: Define and emit one canonical confidence tier (`LOW|MED|HIGH`) for projection-settled rows while preserving raw source fields for diagnostics.

**Scope**:

- web/src/app/api/results/projection-settled/route.ts
- web/src/lib/types/projection-accuracy.ts

**Out of scope**:

- UI layout/styling changes in results table
- Worker-side historical backfill
- Renaming or deleting existing DB columns

**Acceptance**:

- `/api/results/projection-settled` emits a canonical confidence field with vocabulary limited to `LOW|MED|HIGH`.
- Existing alternate vocabularies (`WATCH|TRUST|STRONG`, missing/null payload fields) are normalized at API read time.
- Response still includes raw/source confidence signals for debugging and migration safety.
- No API regression for non-MLB-F5 card families.

**Owner agent**: unassigned
**Time window**: 2026-04-22T00:00:00Z -> TBD
**Coordination flag**: needs-sync
**Depends on**: none

**Tests to run**:

- npm --prefix web run test:api:results:flags
- npm --prefix web run test:api:validation-registry
- npx --prefix web tsc --noEmit

**Manual validation**:

- Hit `/api/results/projection-settled` locally and confirm all returned rows expose canonical confidence values only from `LOW|MED|HIGH`.
- Verify rows with previously null/UNKNOWN confidence now resolve deterministically via source-score or probability fallback.

**Guard for WI closeout**:

- Before marking complete: confirm every test command in Tests to run is runnable from repo root.
- Preserve backward compatibility by keeping raw/source confidence fields available during migration window.

CLAIM: unassigned 2026-04-22T00:00:00Z

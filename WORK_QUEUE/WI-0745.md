# WI-0745: Projection Evaluation Split

**ID**: WI-0745
**Goal**: Add a read-only projection evaluation lane for `PROJECTION_ONLY` families (MAE, bias, directional accuracy, calibration buckets, and alerts) while keeping odds-backed execution audit logic unchanged, and remove the obsolete projection-only telemetry ledger write/read path.

**Scope**:
- `WORK_QUEUE/WI-0745.md`
- `apps/worker/src/audit/projection_evaluator.js`
- `apps/worker/src/audit/performance_drift_report.js`
- `apps/worker/src/audit/README.md`
- `apps/worker/src/audit/__tests__/projection_evaluator.test.js`
- `apps/worker/src/audit/__tests__/performance_drift_report.test.js`
- `apps/worker/src/jobs/report_telemetry_calibration.js`
- `apps/worker/src/jobs/__tests__/report_telemetry_calibration.test.js`
- `apps/worker/src/jobs/run_nhl_player_shots_model.js`
- `apps/worker/src/jobs/__tests__/run_nhl_player_shots_model.test.js`
- `apps/worker/src/__tests__/run-nhl-player-shots-model.test.js`
- `apps/worker/src/utils/nhl-shots-patch.js`
- `packages/data/src/db-telemetry.js`
- `packages/data/index.js`

**Out of scope**:
- Any change to LIVE/ODDS_BACKED execution audit math or CLV telemetry
- Any DB migration or schema change
- Any web UI/API change
- Any `card-family-registry.json` status change

**Acceptance**:
- PROJECTION_ONLY segments in `audit:performance` include `projection_metrics` derived read-only from settled payload/result data.
- ODDS_BACKED segments do not include `projection_metrics` and existing execution metrics remain unchanged.
- Projection alerts fire independently for MAE, bias, and directional accuracy thresholds over `last_200` and `season_to_date`.
- Legacy `projection_perf_ledger` write/read/report hooks are removed, while CLV telemetry remains intact.
- If a projection family lacks a durable actual value, its projection metrics report `actuals_available=false` instead of guessing.

**Owner agent**: codex
**Time window**: 2026-04-03T00:00:00Z -> TBD
**Coordination flag**: solo

**Tests to run**:
- `npm --prefix apps/worker test -- --runInBand src/audit/__tests__/projection_evaluator.test.js`
- `npm --prefix apps/worker test -- --runInBand src/audit/__tests__/performance_drift_report.test.js`
- `npm --prefix apps/worker test -- --runInBand src/jobs/__tests__/report_telemetry_calibration.test.js`
- `npm --prefix apps/worker test -- --runInBand src/jobs/__tests__/run_nhl_player_shots_model.test.js`

**Manual validation**:
- Run `npm --prefix apps/worker run audit:performance` against a local DB snapshot and confirm PROJECTION_ONLY families emit `projection_metrics` with no projection alerts mixed into ODDS_BACKED segments.
- Confirm `job:report-telemetry-calibration` still reports CLV diagnostics and no longer references `projection_perf_ledger`.

CLAIM: codex 2026-04-03T00:00:00Z

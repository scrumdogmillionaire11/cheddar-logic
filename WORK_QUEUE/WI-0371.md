# WI-0371: Backfill Historical card_results Sport Casing + Guardrail

**ID**: WI-0371
**Goal**: Normalize legacy mixed-case `card_results.sport` values to lowercase and add a durable guardrail against future casing drift.

**Scope**:

- `WORK_QUEUE/WI-0371.md`
- `WORK_QUEUE/README.md`
- `packages/data/src/db.js`
- `packages/data/__tests__/card-payload-sport.test.js`

**Out of scope**:

- Regrading historical settlement outcomes
- Frontend/UI behavior changes
- Expanding settlement analytics

**Acceptance**:

- Existing `card_results` rows are normalized to lowercase sport values via explicit backfill path.
- Regression coverage fails if non-lowercase `card_results.sport` is persisted in new writes.
- No changes to settlement business rules beyond sport casing consistency.

**Owner agent**: unassigned
**Time window**: TBD -> TBD
**Coordination flag**: needs-sync

**Tests to run**:

- `npm --prefix packages/data test -- __tests__/card-payload-sport.test.js`

**Manual validation**:

- Query `card_results` before/after backfill and confirm lowercase sport values.
- Run one model job and verify newly inserted `card_results` sport values remain lowercase.


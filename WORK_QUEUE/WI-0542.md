# WI-0542: Cards stale-asset recovery for CSS static 404s

**ID**: WI-0542
**Goal**: Ensure `/cards` detects stale Next static CSS asset failures and performs the same one-time recovery flow already used for stale JS chunks.

**Scope**:
- `WORK_QUEUE/WI-0542.md`
- `web/src/components/cards-page-client.tsx`
- `web/src/__tests__/cards-chunk-recovery-contract.test.js`

**Out of scope**:
- Deploy pipeline/workflow changes under `.github/workflows/**`
- Cloudflare configuration changes
- Non-cards routes/components

**Acceptance**:
- CSS static asset failures (`/_next/static/css/*.css` and equivalent Next static CSS paths) are classified as stale-asset failures in cards client recovery logic.
- Global error handler inspects both script `src` and stylesheet `href` targets for stale-asset paths.
- Source contract test explicitly asserts CSS-path stale-asset detection contract.

**Owner agent**: codex
**Time window**: 2026-03-21T15:15:00Z -> TBD
**Coordination flag**: solo

**Tests to run**:
- `node web/src/__tests__/cards-chunk-recovery-contract.test.js`

**Manual validation**:
- Open `/cards` with a stale asset URL simulation and confirm first stale-asset hit triggers one reload, then shows stable hard-refresh guidance if repeated.

**Guard for WI closeout**:
- Keep changes scoped to cards stale-asset detection/recovery only (no unrelated UI/data-flow edits).
- Before marking complete: confirm every test command in `Tests to run` is runnable from repo root.

CLAIM: codex 2026-03-21T15:15:00Z

# WI-1272: Extract Pipeline Health Coverage Evaluators

**ID**: WI-1272
**Goal**: Reduce change risk in `check_pipeline_health.js` by extracting coverage evaluators behind stable interfaces.

**Scope**:
- `apps/worker/src/jobs/check_pipeline_health.js`
- `apps/worker/src/jobs/health/coverage-evaluators.js` (new)
- `apps/worker/src/jobs/health/__tests__/coverage-evaluators.test.js` (new)

**Out of scope**:
- Changing thresholds
- Changing alert behavior
- Changing persistence schema

**Acceptance**:
- MLB/NBA/scheduled coverage evaluator logic moves behind exported pure/helper functions.
- Main runner behavior remains unchanged.
- Contract tests prove before/after status/reason parity for existing fixtures.
- No degraded-state or alert behavior changes.

**Owner agent**: unassigned
**Time window**: 4-6 hours
**Coordination flag**: needs-sync
**Depends on**: WI-1271

**Tests to run**:
- `npm --prefix apps/worker test -- coverage-evaluators`
- `npm --prefix apps/worker test -- check_pipeline_health`

**Manual validation**:
- Compare sample watchdog output before/after extraction and confirm parity for status and reason fields.

**Guard for WI closeout**:
- Treat this as structure-only extraction; no behavior drift.

CLAIM: unassigned

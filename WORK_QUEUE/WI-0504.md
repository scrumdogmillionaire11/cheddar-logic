# WI-0504: NHL orchestration cutover (single best expression per game)

**ID**: WI-0504
**Goal**: Promote orchestrated market selection to production output for NHL and retire legacy global driver blending so each game publishes one deterministic best-market expression.

**Depends on**:

- `WORK_QUEUE/WI-0503.md`

**Scope**:

- `apps/worker/src/jobs/run_nhl_model.js`
- `apps/worker/src/models/cross-market.js`
- `apps/worker/src/models/index.js`
- `apps/worker/src/jobs/__tests__/run_nhl_model.market-calls.test.js`
- `apps/worker/src/jobs/__tests__/run_nhl_model.test.js`
- `packages/data/src/validators/card-payload.js`
- `docs/CROSS_MARKET_ORCHESTRATION.md`
- `docs/DATA_CONTRACTS.md`
- `WORK_QUEUE/WI-0504.md`

**Out of scope**:

- NBA/NCAAM orchestration rollout
- Re-tuning market thresholds beyond target defaults
- 1P fair probability Phase-2 work

**Acceptance**:

1. NHL output path uses orchestrated market choice as authoritative production pick (`TOTAL`, `SPREAD`, or `ML`) per game.
2. Legacy global blend path is removed or permanently disabled for NHL runtime path.
3. Payload contract includes `expression_choice` and `market_narrative` blocks consistent with spec.
4. One-card-per-game market expression behavior is observable and regression-safe in tests.
5. Risk-only driver behavior remains intact post-cutover (no directional leakage into side markets).

**Owner agent**: unassigned
**Time window**: TBD -> TBD
**Coordination flag**: needs-sync

**Tests to run**:

- `npm --prefix apps/worker test -- src/jobs/__tests__/run_nhl_model.market-calls.test.js`
- `npm --prefix apps/worker test -- src/jobs/__tests__/run_nhl_model.test.js`
- `npm --prefix web run test:api:games:market`
- `npm --prefix web run test:decision:canonical`

**Manual validation**:

- Generate a fresh NHL slate and verify each game’s chosen expression follows selector priority rules in payload data.
- Spot-check cards for `expression_choice` (`score`, `net`, `status`) and narrative coherence.

**Guard for WI closeout**:

- Before marking complete: attach before/after sample comparing legacy vs orchestrated picks for the same game window.
- Confirm rollback mechanism (feature flag or guarded switch) is documented and validated.

CLAIM: unassigned

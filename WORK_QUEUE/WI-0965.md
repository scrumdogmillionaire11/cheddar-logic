# WI-0965: Centralize MLB execution-gate override policy and surfaced-reason mapping

**ID**: WI-0965
**Goal**: Replace ad hoc MLB gate overrides with one declarative policy contract that drives both execution outcomes and surfaced reason labels.

**Depends on**: WI-0963

**Scope**:
- `WORK_QUEUE/WI-0965.md`
- `apps/worker/src/jobs/execution-gate.js`
- `apps/worker/src/jobs/run_mlb_model.js`
- `web/src/lib/game-card/transform/decision-surface.ts`
- `apps/worker/src/jobs/__tests__/execution-gate.test.js`
- `apps/worker/src/jobs/__tests__/run_mlb_model.test.js`

**Out of scope**:
- Freshness scheduler windows
- Model projection math changes
- Non-MLB execution policy redesign

**Acceptance**:
- MLB full-game ML and totals overrides (high-edge behavior, confidence/net-edge soft blockers) are defined in one policy source.
- PASS/LEAN surfaced reasons in web UI match the gate blocker outcomes from worker payloads.
- Regression tests cover large-edge pass/lean boundaries and ensure non-MLB markets are unaffected.
- New override logic does not require card-type heuristics scattered across multiple code paths.

**Owner agent**: unassigned
**Time window**: 2026-04-15 -> TBD
**Coordination flag**: needs-sync

**Tests to run**:
- `npm --prefix apps/worker run test -- src/jobs/__tests__/execution-gate.test.js --runInBand`
- `npm --prefix apps/worker run test -- src/jobs/__tests__/run_mlb_model.test.js --runInBand`

**Manual validation**:
- Run MLB model job and inspect resulting payload reasons in API/web output to confirm gate blockers and surfaced labels stay aligned.

CLAIM: unassigned
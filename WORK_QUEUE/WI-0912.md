# WI-0912: MLB Pitcher-K Projection Settlement and Card Lifecycle Contract

**ID**: WI-0912
**Goal**: Make mlb-pitcher-k settlement lifecycle explicit from projection actual_result capture through card_results terminal state and telemetry.

**Depends on**: none

**Scope**:

- apps/worker/src/jobs/settle_projections.js
- apps/worker/src/jobs/settle_pending_cards.js
- apps/worker/src/jobs/run_mlb_model.js
- `apps/worker/src/jobs/__tests__/settle_projections.test.js`
- `apps/worker/src/jobs/__tests__/run_mlb_model.test.js`
- `apps/worker/src/jobs/__tests__/settle_pending_cards.phase2.test.js`

**Out of scope**:

- Pitcher-K model threshold tuning
- New external odds provider integration
- POTD strategy changes

**Acceptance**:

- mlb-pitcher-k has a documented and enforced settlement lifecycle (projection factual settlement only vs grading-capable), with deterministic terminal card_results behavior.
- Pending rows are not left in ambiguous states when game is final but player identity/line context is missing.
- Decision-basis metadata and settlement handling align (PROJECTION_ONLY does not enter odds-backed CLV paths).
- Tests include no-gamePk, no-player-match, and final-game successful settlement scenarios.

**Owner agent**: unassigned
**Time window**: 2026-04-11 -> TBD
**Coordination flag**: needs-sync

**Tests to run**:

- npm --prefix apps/worker run test -- --testPathPattern="settle_projections"
- npm --prefix apps/worker run test -- --testPathPattern="run_mlb_model"
- npm --prefix apps/worker run test -- --testPathPattern="settle_pending_cards.phase2"

**Manual validation**:

- Backfill one completed MLB game with pitcher-K cards and verify each card reaches the expected terminal state with explicit reason metadata when not gradeable.

CLAIM: unassigned

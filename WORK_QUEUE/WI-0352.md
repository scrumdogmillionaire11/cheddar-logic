# WI-0352: Driver Attempt/Failure Reporting

**ID**: WI-0352
**Goal**: Ensure each driver reports `attempted`, `eligible`, and `blocked_reason`, aggregated into per-game pipeline state for explicit failure diagnostics.

**Scope**:

- `WORK_QUEUE/WI-0352.md`
- `apps/worker/src/index.js`
- `apps/worker/src/models/cross-market.js`
- `apps/worker/src/utils/decision-publisher.js`
- `packages/models/src/decision-pipeline-v2.js`
- `docs/DATA_CONTRACTS.md`


**Out of scope**:

- Projection formula changes
- Team mapping normalization
- Schema migrations


**Acceptance**:

- Each driver contributes `attempted`, `eligible`, and `blocked_reason` entries.
- Aggregated driver failures appear in per-game pipeline state with explicit blocking reason codes.
- No generic "drivers unavailable" reasons remain for wave-1 decisions.


**Tests to run**:

- `npm --prefix apps/worker test -- src/utils/__tests__/decision-publisher.v2.test.js`


**Manual validation**:

- Run model jobs and confirm driver failure reasons are explicit in per-game pipeline state and decision metadata.


**Owner agent**:
**Time window**: 2026-03-09T00:00:00Z -> TBD
**Coordination flag**: solo


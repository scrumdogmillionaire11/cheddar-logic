# WI-0327: Add Edge Math Display

**ID**: WI-0327
**Goal**: Display fair probability, implied probability, and edge math on cards to make large edges explainable.

**Scope**:

- web/src/components/cards-page-client.tsx
- web/src/lib/game-card/transform.ts
- web/src/app/api/games/route.ts (only if edge fields are missing from payload)

**Out of scope**:

- Edge calculation logic in packages/models
- Worker job logic
- Database schema changes

**Acceptance**:

- Cards with edge data show an Edge Math section with Fair %, Implied %, and Edge %
- Edge Math renders only when modelProb, impliedProb, and edge are present
- Edge formula text is visible and matches the values shown
- Large edges (>30%) show a caution note
- No Edge Math section appears on cards without edge data

**Owner agent**: unassigned
**Time window**: TBD -> TBD
**Coordination flag**: solo

**Tests to run**:

- `cd web && npm run dev`
- `curl -s "http://localhost:3000/api/games?limit=10" | jq '.data[0].plays[] | {edge, modelProb, impliedProb}'`

**Manual validation**:

- Open the cards page and confirm Edge Math appears for cards with edge data
- Verify the numbers match the formula
- Confirm cards without edge data do not show the section

CLAIM: Claude 2026-03-07T13:15:00Z
STATUS: closed
CLOSED: 2026-03-07 — shipped in commit 002765d

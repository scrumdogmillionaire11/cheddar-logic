---
ID: WI-XXXX
Goal: Only log the best edge (highest EV) per market/match as a near-miss for Play of the Day (POTD); never log both sides of the same market.
Scope:
  - web/src/lib/potd-server.ts (core grouping/filtering logic)
  - (optional) web/src/app/play-of-the-day/settled/page.tsx (if UI needs update)
  - (optional) test files covering POTD settled/near-miss aggregation
Out of scope:
  - Any changes to unrelated POTD logic or UI
  - Database schema changes
Acceptance:
  - For each market/match, only the play with the highest edge is logged as a near-miss
  - No duplicate or opposing sides for the same market/match in near-miss logs
  - Automated test(s) confirm correct grouping/filtering
Owner agent: <to be assigned>
Time window: <to be assigned>
Coordination flag: none
Depends on: none
Tests to run:
  - Existing and new tests for POTD settled/near-miss logic
Manual validation:
  - Confirm in UI that only one side per market appears in near-miss/settled list
CLAIM: <agent> <ISO8601>

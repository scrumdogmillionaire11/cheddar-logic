# WI-1235: Fix Internal Fair Odds Calculation Bug

## ID
WI-1235

## Goal
Diagnose and fix the internal fair-odds calculation that incorrectly returns `+9900` for the Texas Rangers ML card on May 3. Ensure internal fair odds and edge % are derived from the same valid model probability.

## Scope
- Worker/model market-pricing calculation logic
- Internal probability-to-American-odds conversion helpers
- Edge % calculation path using internal fair odds/model probability
- Regression fixture: TX Rangers @ Detroit Tigers, Rangers ML `+105`

## Out of Scope
- External odds feed validation
- Caesars API integration
- Historical odds backfill
- Threshold tuning or play/edge classification changes

## Acceptance
1. Identify the exact source of `+9900`:
   - file
   - function
   - bad input/state
   - root cause
2. Fix fair-odds conversion so invalid probabilities cannot silently produce extreme odds.
3. Verify the TX Rangers `+105` fixture returns probability-consistent fair odds, expected around `-110` to `-130` only if model fair probability is roughly `52%–56%`.
4. Verify model edge % is positive when model fair probability exceeds market implied probability.
5. Add regression coverage for the TX Rangers case.
6. Existing pricing/edge tests pass.

## Owner Agent
codex
CLAIM: codex 2026-05-03T19:46:00-04:00

## Time Window
2–4 hours

## Coordination Flag
None — isolated pricing bug fix

## Depends On
none

## Tests to Run
```bash
npm --prefix apps/worker test -- --grep "fair.*odds|internal.*pricing"
npm --prefix apps/worker test -- --grep "edge.*calculation"
````

## Regression Fixture

Game:

* TX Rangers @ Detroit Tigers
* Market: Moneyline
* Book: Caesars
* Selection: Texas Rangers
* Market odds: `+105`
* Market implied probability: about `48.8%`

Expected:

* Internal fair odds must not equal `+9900`
* Internal fair odds must match model probability conversion
* If model fair probability is `52%–56%`, fair odds should land roughly `-108` to `-127`
* Edge % should be positive when model fair probability is above `48.8%`

## Manual Validation

1. Check the live/UI card for TX Rangers @ Detroit Tigers.
2. Confirm `Internal fair` shows a realistic American odds value.
3. Confirm model edge % is not `0.0%` when model probability is above market implied.
4. Confirm decision driver remains unchanged except where corrected edge math changes downstream classification legitimately.

## Notes

* `+9900` usually indicates a probability near `1%`, an inverted probability, bad default, percentage/decimal mismatch, or invalid fallback.
* Current card shows driver `EDGE CLEAR` while model edge is `0.0%`, which strongly suggests pricing/edge math corruption rather than an odds-feed issue.
* Do not tune thresholds to hide this. Fix the math.

```
```

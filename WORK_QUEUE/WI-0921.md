# WI-0921: Worker/Model Legacy Cleanup and F5 Line Persistence Fix

**ID**: WI-0921
**Goal**: Remove remaining legacy worker/model compatibility surfaces where safe and fix the F5 line persistence bug so fetched F5 line data is actually stored in card payloads.

**Depends on**: none

**Scope**:
- `apps/worker/src/jobs/run_mlb_model.js`
- `apps/worker/src/jobs/settle_pending_cards.js`
- `packages/models/src/mispricing-scanner.js`
- `packages/models/src/__tests__/mispricing-scanner.test.js`
- `apps/worker/src/__tests__/run-mlb-model.dual-run.test.js`
- `apps/worker/src/jobs/__tests__/check_pipeline_health.mlb.test.js`

**Out of scope**:
- Web filter/render cleanup
- Data package env-path and validator alias policy
- New external adapter integrations

**Acceptance**:
- F5 line fetch/write order is corrected: fetched line data is persisted in DB-backed payloads deterministically (no post-insert in-memory-only mutation path).
- Tests cover successful line persistence and failure-path behavior for missing/error fetches.
- Mispricing scanner legacy config path is deprecated or removed with migration-safe test updates.
- Legacy status fallback logic touched in settlement paths is constrained to historical-row handling only and documented in code.

**Owner agent**: unassigned
**Time window**: 2026-04-12 -> TBD
**Coordination flag**: solo

**Tests to run**:
- `npm --prefix apps/worker run test -- --testPathPattern="run-mlb-model.dual-run|check-pipeline-health.mlb"`
- `npm --prefix packages/models run test -- --testPathPattern="mispricing-scanner"`

**Manual validation**:
- Run MLB model on a fixture/live sample and verify persisted `card_payloads.payload_data` includes `f5_market_line` when fetch succeeds.
- Verify no false-positive persistence claim when fetch fails.
- Verify mispricing scan outputs are unchanged for canonical config inputs.

CLAIM: unassigned
# WI-0505: NHL 1P Phase-2 gated fair probability activation

**ID**: WI-0505
**Goal**: Add market-aware fair-probability math for NHL 1P only behind explicit gating, preserving Phase-1 null behavior unless stable real 1P line availability is confirmed.

**Depends on**:

- `WORK_QUEUE/WI-0504.md`

**Scope**:

- `apps/worker/src/models/projections.js`
- `apps/worker/src/jobs/run_nhl_model.js`
- `apps/worker/src/models/__tests__/nhl-1p-totals.test.js`
- `apps/worker/src/jobs/__tests__/run_nhl_model.test.js`
- `env.example`
- `docs/NHL_1P_MODEL_SPEC.md`
- `docs/NHL_PLAYER_SHOTS_PROP_MARKET.md`
- `WORK_QUEUE/WI-0505.md`

**Out of scope**:

- Altering Phase-1 classification bands or dead-zone rules
- Broad sportsbook integration changes outside NHL 1P gating path
- NBA/NCAAM/Soccer probability feature changes

**Acceptance**:

1. Phase-1 default remains unchanged: `fair_prob` for NHL 1P outputs stays `null` unless explicit Phase-2 gate is enabled.
2. Under gate + real 1P market line availability, fair probabilities are computed via normal CDF using `mu=final_1p_projection` and configured `sigma_1p` range.
3. If gate is enabled but 1P market line prerequisites are missing/unreliable, system fails safe to Phase-1 behavior (no priced recommendation leak).
4. Tests cover both gated-on and gated-off behavior, including dead-zone and goalie-uncertainty PASS interactions.
5. Docs and env flags clearly state that Phase-2 activation requires stable real 1P line supply.

**Owner agent**: unassigned
**Time window**: TBD -> TBD
**Coordination flag**: needs-sync

**Tests to run**:

- `npm --prefix apps/worker test -- src/models/__tests__/nhl-1p-totals.test.js`
- `npm --prefix apps/worker test -- src/jobs/__tests__/run_nhl_model.test.js`
- `npm --prefix apps/worker test -- src/jobs/__tests__/run_nhl_model.market-calls.test.js`

**Manual validation**:

- Validate one run with gate disabled confirms all 1P `fair_prob` fields are null.
- Validate one controlled run with gate enabled and real 1P lines confirms fair probability fields populate only for eligible records.

**Guard for WI closeout**:

- Before marking complete: include evidence that gated-on behavior does not bypass dead-zone PASS or goalie-uncertainty PASS protections.
- Confirm explicit rollback instruction to disable Phase-2 gate is documented.

CLAIM: unassigned

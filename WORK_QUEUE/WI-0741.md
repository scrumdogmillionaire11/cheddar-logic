# WI-0741: Harden audit artifact upload checks

**ID**: WI-0741
**Goal**: Fail CI with diagnostics when model audit artifacts are missing instead of emitting a non-blocking upload warning.

**Scope**:
- `.github/workflows/audit.yml`
- `WORK_QUEUE/WI-0741.md`

**Out of scope**:
- `apps/worker/src/audit/**`
- `apps/worker/src/jobs/**`
- `packages/**`
- `web/**`

**Acceptance**:
- Workflow prints the available `apps/worker/audit-output` files before upload and fails if any required audit artifact is missing for `${RUN_SCOPE}`.
- `actions/upload-artifact@v4` is configured to error when no files match the artifact path.
- Audit and scorecard steps continue writing to the same `${OUTPUT_DIR}` path.

**Owner agent**: codex
**Time window**: 2026-04-03T02:42:20Z -> TBD
**Coordination flag**: needs-sync

**Tests to run**:
- `npm --prefix apps/worker run audit:all -- --output-dir /tmp/cheddar-audit-debug/ci-debug --run-scope ci-debug --out /tmp/cheddar-audit-debug/ci-debug/audit-report.json`
- `npm --prefix apps/worker run audit:scorecard -- --output-dir /tmp/cheddar-audit-debug/ci-debug --run-scope ci-debug --audit-report /tmp/cheddar-audit-debug/ci-debug/audit-report.json`
- `find /tmp/cheddar-audit-debug/ci-debug -maxdepth 2 -type f | sort`

**Manual validation**:
- Review `.github/workflows/audit.yml` and confirm only the pre-upload guard and artifact upload failure mode changed.

CLAIM: codex 2026-04-03T02:42:20Z. 

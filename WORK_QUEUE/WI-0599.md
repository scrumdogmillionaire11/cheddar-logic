# WI-0599: Pitcher Ks web integration (props surfaces)

**ID**: WI-0599
**Goal**: Expose pitcher K cards in existing props UI/API surfaces with correct market typing, filters, and display semantics.

**Scope**:

- `web/src/app/api/games/route.ts`
- `web/src/lib/game-card/transform.ts`
- `web/src/lib/game-card/filters.ts`
- `web/src/lib/game-card/presets.ts`
- `web/src/lib/types/game-card.ts`
- `web/src/__tests__/game-card-transform-market-contract.test.js`

**Out of scope**:

- Worker-side projection/scoring logic
- Odds ingestion and scheduler wiring
- New tabs or UX redesign

**Acceptance**:

- Pitcher K cards flow through `/api/games` without being silently dropped by market gating.
- Cards are correctly typed/routed as props and appear under existing props filters/presets.
- Projection-only vs odds-backed display state is consistent with existing prop decision contract.
- Transform/filter tests cover both modes.

**Owner agent**: unassigned
**Time window**: 2026-03-26T00:00:00Z -> TBD
**Coordination flag**: needs-sync

**Tests to run**:

- `npm --prefix web run test:api:games:market`
- `npm --prefix web run test:ui:cards`

**Manual validation**:

- Start web locally and verify pitcher K cards render in props view with expected status and market labels in both modes.

**Guard for WI closeout**:

- Before marking complete: confirm every test command in `Tests to run` is runnable from repo root.
- Before marking complete: verify no new UI surface is added beyond existing props pathways.

CLAIM: unassigned

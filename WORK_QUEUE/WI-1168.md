---
phase: WI-1168
plan: "01"
type: hardening
wave: 1
depends_on:
  - WI-1167
files_modified:
  - web/src/app/api/cards/route.ts
  - web/src/lib/cards/query.ts
  - web/src/__tests__/api-cards-lifecycle-regression.test.js
  - web/src/__tests__/api-dedupe.test.js
  - web/src/__tests__/api-cards-sport-filter.test.js
  - WORK_QUEUE/WI-1168.md
autonomous: true
requirements:
  - WI-1168-CARDS-01
  - WI-1168-CARDS-02
  - WI-1168-CARDS-03
  - WI-1168-CARDS-04
must_haves:
  truths:
    - If active-run rows are missing a requested card type, /api/cards safely falls back and still returns eligible cards for that type.
    - Settled suppression is card/market precise; settled rows no longer suppress unrelated unsettled card types from the same game.
    - Sport filtering preserves NHL game plus NHL props compatibility lanes without hiding intended cards.
    - Regression coverage proves mixed-card-type and mixed-run-id scenarios remain visible when eligible.
  artifacts:
    - path: web/src/lib/cards/query.ts
      provides: Run-scope fallback plus settled-scope precision query behavior
      min_lines: 30
    - path: web/src/app/api/cards/route.ts
      provides: Route-level cards API composition for fallback plus sport-scoping behavior
      min_lines: 20
    - path: web/src/__tests__/api-cards-lifecycle-regression.test.js
      provides: Mixed settled or unsettled card-type lifecycle regression coverage
      min_lines: 20
    - path: web/src/__tests__/api-dedupe.test.js
      provides: Mixed run-id dedupe regression coverage
      min_lines: 20
    - path: web/src/__tests__/api-cards-sport-filter.test.js
      provides: NHL lane compatibility regression coverage
      min_lines: 20
  key_links:
    - from: web/src/app/api/cards/route.ts
      to: web/src/lib/cards/query.ts
      via: Route query construction and query execution path for run-scope fallback
    - from: web/src/lib/cards/query.ts
      to: web/src/__tests__/api-cards-lifecycle-regression.test.js
      via: Settled-scope precision assertions for mixed card types in same game
    - from: web/src/lib/cards/query.ts
      to: web/src/__tests__/api-dedupe.test.js
      via: Mixed run-id fallback plus dedupe assertions
    - from: web/src/app/api/cards/route.ts
      to: web/src/__tests__/api-cards-sport-filter.test.js
      via: NHL plus NHL-props sport-lane compatibility assertions
---

# WI-1168: Run-Scope Fallback And Settled-Scope Precision For Cards API

**ID**: WI-1168
**Goal**: Prevent valid cards from being hidden by coarse run scoping and game-level settled suppression in `/api/cards`.

**Requirements**: [WI-1168-CARDS-01, WI-1168-CARDS-02, WI-1168-CARDS-03, WI-1168-CARDS-04]

**Roadmap traceability**:

- Follows the canonical eligibility unification introduced by `WI-1167` and is intentionally scoped as a serialized work-queue execution item, not a standalone roadmap phase.
- Dependency gate: do not execute implementation tasks until WI-1167 acceptance/tests are satisfied for canonical `/api/cards` eligibility behavior.

**Scope**:

- `web/src/app/api/cards/route.ts`
- `web/src/lib/cards/query.ts`
- `web/src/__tests__/api-cards-lifecycle-regression.test.js`
- `web/src/__tests__/api-dedupe.test.js`
- `web/src/__tests__/api-cards-sport-filter.test.js`
- `WORK_QUEUE/WI-1168.md`

**Out of scope**:

- New card generation or worker scheduling changes
- Changes to unrelated admin endpoints
- Schema migrations unless strictly required for key precision

**Acceptance**:

- [WI-1168-CARDS-01] Active run scoping supports safe fallback when a requested card type is absent from active run rows.
- [WI-1168-CARDS-02] Settled suppression is narrowed from broad game-level exclusion to card/market-precise exclusion semantics.
- [WI-1168-CARDS-03] Sport scoping handles NHL + NHL props lane compatibility without hiding intended cards.
- [WI-1168-CARDS-04] Regressions are covered with tests for mixed card types in same game and mixed run-id availability.

**Owner agent**: github-copilot
**Time window**: 2026-04-24T00:00:00Z -> TBD
**Coordination flag**: needs-sync
**Depends on**: WI-1167

## Execution tasks

<tasks>

<task type="auto">
  <name>Task 0: Enforce WI-1167 prerequisite and freeze expected baseline behavior</name>
  <requirements>WI-1168-CARDS-01, WI-1168-CARDS-02, WI-1168-CARDS-03</requirements>
  <files>WORK_QUEUE/WI-1168.md</files>
  <action>
    Treat WI-1167 as a hard prerequisite before code changes in this item.

    Verify WI-1167 acceptance command set passes and canonical eligibility behavior is present, so this work only adjusts run-scope fallback, settled-scope precision, and sport lane compatibility on top of the canonical gate.

    Record this dependency gate in this work item so execution order is deterministic for needs-sync files.
  </action>
  <verify>
    <automated>npm --prefix web run test:cards:projection-exclusion && npm --prefix web run test:cards-lifecycle-regression && node web/src/__tests__/api-cards-eligibility-contract.test.js && node web/src/__tests__/api-cards-diagnostics-contract.test.js</automated>
  </verify>
  <done>
    WI-1167 canonical eligibility behavior is validated as a precondition and this item proceeds only after the dependency baseline is confirmed.
  </done>
</task>

<task type="auto">
  <name>Task 1: Implement safe run-scope fallback and card/market-precise settled suppression in query layer</name>
  <requirements>WI-1168-CARDS-01, WI-1168-CARDS-02</requirements>
  <files>web/src/lib/cards/query.ts, web/src/__tests__/api-cards-lifecycle-regression.test.js, web/src/__tests__/api-dedupe.test.js</files>
  <action>
    Update query logic so active-run scoping no longer hides requested card types when active rows are missing that type.

    Narrow settled suppression from broad game-level exclusion to card or market-precise semantics, preserving eligible unsettled card types in the same game.

    Add or adjust regression coverage for:
    - mixed settled and unsettled card types in one game
    - mixed run-id availability where fallback should recover eligible rows
    - dedupe behavior under fallback paths
  </action>
  <verify>
    <automated>npm --prefix web run test:cards-lifecycle-regression && npm --prefix web run test:dedupe</automated>
  </verify>
  <done>
    Query layer returns eligible cards for requested types even when active-run rows are partial, and settled suppression only removes matching settled card or market rows.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire route-level sport lane compatibility and lock NHL plus props regression contract</name>
  <requirements>WI-1168-CARDS-03, WI-1168-CARDS-04</requirements>
  <files>web/src/app/api/cards/route.ts, web/src/__tests__/api-cards-sport-filter.test.js, web/src/__tests__/api-cards-lifecycle-regression.test.js</files>
  <action>
    Ensure route-level sport scoping preserves NHL game and NHL props compatibility lanes while still honoring canonical eligibility and query-level settled or run-scope behavior.

    Add or adjust sport-filter assertions that prove intended NHL-related card types are not hidden by lane mismatch when otherwise eligible.

    Confirm lifecycle regression assertions still pass under combined run-scope fallback plus settled-scope precision behavior.
  </action>
  <verify>
    <automated>npm --prefix web run test:cards-sport-filter && npm --prefix web run test:cards-lifecycle-regression && npm --prefix web run test:api:nhl-contract</automated>
  </verify>
  <done>
    /api/cards sport filtering preserves intended NHL lanes and regression coverage proves mixed-card-type and mixed-run-id visibility behavior end-to-end.
  </done>
</task>

</tasks>

**Tests to run**:

- `npm --prefix web run test:cards:projection-exclusion`
- `npm --prefix web run test:cards-lifecycle-regression`
- `npm --prefix web run test:dedupe`
- `npm --prefix web run test:cards-sport-filter`
- `npm --prefix web run test:api:nhl-contract`
- `node web/src/__tests__/api-cards-eligibility-contract.test.js`
- `node web/src/__tests__/api-cards-diagnostics-contract.test.js`

**Manual validation**:

- Validate a game with settled and unsettled card types still returns unsettled eligible cards.
- Validate a sport query where active run has partial card types still returns missing types from safe fallback logic.
- Validate NHL + NHL props lane compatibility still surfaces intended cards when eligible.

**Guard for WI closeout**:

- Confirm WI-1167 prerequisite checks pass before WI-1168 implementation commands are considered complete.
- Confirm each requirement ID has implementation and test evidence in task-level verify output.
- Confirm no broad game-level settled suppression remains for unrelated card/market rows.
- Confirm mixed run-id fallback does not regress dedupe correctness.

CLAIM: github-copilot 2026-04-24T00:00:00Z

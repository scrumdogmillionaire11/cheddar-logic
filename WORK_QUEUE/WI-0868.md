# WI-0868: Route pipeline alerts to Discord alert webhook

| Field | Value |
|---|---|
| **ID** | WI-0868 |
| **Goal** | Send operational pipeline alerts to `DISCORD_ALERT_WEBHOOK_URL` instead of `DISCORD_CARD_WEBHOOK_URL`, while leaving card snapshot posting on the card webhook unchanged. |
| **Scope** | `apps/worker/src/jobs/check_pipeline_health.js`, `apps/worker/src/jobs/run_nba_model.js`, `apps/worker/src/jobs/run_nhl_model.js`, `apps/worker/src/jobs/__tests__/check_pipeline_health.test.js`, `apps/worker/src/jobs/__tests__/check_pipeline_health.watchdog.test.js`, `apps/worker/src/jobs/__tests__/run_nba_model.test.js`, `apps/worker/src/jobs/__tests__/run_nhl_model.test.js` |
| **Out of scope** | Changing card snapshot webhook routing, POTD webhook routing, introducing new Discord enable flags, editing existing untracked work items |
| **Acceptance** | (1) `check_pipeline_health` sends watchdog heartbeat alerts and failed-check alerts to `DISCORD_ALERT_WEBHOOK_URL`. (2) NBA/NHL ESPN-null operational alerts send to `DISCORD_ALERT_WEBHOOK_URL`. (3) Log messages and missing-webhook branches refer to the alert webhook env var. (4) Card snapshot posting still uses `DISCORD_CARD_WEBHOOK_URL`. (5) Focused tests cover the new alert webhook routing. |
| **Owner agent** | codex |
| **Time window** | 2026-04-10 |
| **Coordination flag** | solo |
| **Tests to run** | `npm --prefix apps/worker test -- --runInBand --testPathPattern="check_pipeline_health|run_nba_model|run_nhl_model"` |
| **Manual validation** | Trigger a pipeline-health failure and an ESPN-null alert in a non-prod environment with distinct card/alert webhook URLs; confirm operational alerts land in the alert channel and card snapshots do not move. |

## Claim

CLAIM: codex 2026-04-10T21:45:00Z

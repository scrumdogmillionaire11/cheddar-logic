# WI-1026: NBA Phase 3C — Market intelligence utilization

**ID**: WI-1026
**Goal**: Wire `sharp_divergence` and `splits_divergence` (already captured) into the confidence and tier computation so that sharp vs. public divergence and public traps reduce play quality instead of being stored and ignored.

**Depends on**: WI-1025 (regime detection active — market intelligence is the last layer of polish, not a fix for bad math)

**Priority note**: This is the lowest-leverage WI in the program. A good projection without market intelligence is usable. A bad projection with market intelligence is still bad. If schedule pressure hits, defer this WI. Do not let market intelligence confidence adjustments become a band-aid for projection quality problems.

**Scope**:
- `apps/worker/src/jobs/run_nba_model.js` (`generateNBAMarketCallCards`, `computePricedCallCardConfidence` caller)
- `apps/worker/src/jobs/__tests__/run_nba_model.market-calls.test.js`

**Out of scope**:
- Sharp line movement (line_delta already exists, unchanged)
- NHL/MLB market intelligence
- External sharp data sources (only data on the odds snapshot)
- `edge`, `p_fair`, `ev_passed` — these are unchanged by this WI
- UI changes

**Acceptance**:
- New function `applyMarketIntelligenceModifier({ baseConfidence, sharpDivergence, splitsDivergence, edge })` returns `{ adjustedConfidence, multiplier, reasonCodes }`.
- Modifier rules:
  - `sharpDivergence = 'SHARP_VS_PUBLIC'` → multiplier **0.85**, reason `SHARP_VS_MODEL_CONFLICT`
  - `splitsDivergence = 'PUBLIC_HEAVY_HOME'` or `'PUBLIC_HEAVY_AWAY'` AND `edge < 0.04` → multiplier **0.88**, reason `PUBLIC_TRAP_RISK`
  - `sharpDivergence = 'SHARP_ALIGNED'` → multiplier **1.05**, reason `SHARP_CONFIRMATION` (boost capped so final confidence ≤ 0.90)
  - If both SHARP_VS_PUBLIC and PUBLIC_TRAP_RISK apply, use lower multiplier (0.85), not multiplicative. Total floor: 0.45. Total ceiling: 0.90.
  - No applicable signals → multiplier 1.0, empty reasonCodes
- Modifier only affects `confidence` and `tier`. `edge`, `p_fair`, `ev_passed`, and `recommend_bet_type` are unchanged.
- `raw_data.market_intel_modifier` stamped: `{ multiplier, reason_codes }`.
- Unit tests: SHARP_VS_PUBLIC + edge 0.06 → confidence drops ~15%; PUBLIC_HEAVY + edge 0.03 → confidence drops ~12%; SHARP_ALIGNED → small boost, capped at 0.90; combined SHARP_VS_PUBLIC + PUBLIC_HEAVY → single multiplier 0.85 not 0.74.

**Owner agent**: unassigned
**Time window**: 2026-08-01 -> 2026-08-31 (offseason)
**Coordination flag**: solo

**Tests to run**:
- `npm --prefix apps/worker run test -- src/jobs/__tests__/run_nba_model.market-calls.test.js --runInBand`
- `npm --prefix apps/worker run test -- src/__tests__/run-nba-model.test.js --runInBand`

**Manual validation**:
- Game with `sharp_divergence = 'SHARP_VS_PUBLIC'`, edge 0.06: confirm `nba-totals-call` confidence is ~15% lower than without the modifier.
- Game with `sharpDivergence = null`, `splitsDivergence = null`: confirm `market_intel_modifier.multiplier = 1.0` and no reason codes.

**Phase 3 exit gate**:

Phase 3 (WI-1024, WI-1025, WI-1026) is complete when:

- Phase 1 and Phase 2 exit criteria still hold (regression check)
- Residual corrections are producing `source: 'computed'` (not `'insufficient'`) for at least half the teams in the rotation
- Regime tags are firing with expected frequency (TANK_MODE should appear in late-season games with bottom-5 teams; INJURY_ROTATION should appear on high-profile injury games)
- Market intel modifier appears on at least some cards with non-1.0 multiplier (confirms `sharp_divergence` data is populated)
- No evidence that market intel confidence adjustments are compensating for a degraded base model (i.e., pre-modifier edge distribution should be similar to Phase 2 levels)

CLAIM: unassigned

# WI-0735: Install local package dependencies before audit scorecard CI

**ID**: WI-0735
**Goal**: Fix the audit workflow so `audit:scorecard` can load `@cheddar-logic/data` and its native `better-sqlite3` dependency in CI.

**Scope**:
- `WORK_QUEUE/WI-0735.md`
- `.github/workflows/audit.yml`

**Out of scope**:
- Scorecard logic or fixture baseline changes
- Package-lock or package manifest changes
- Any web or model runtime changes

**Acceptance**:
- The audit workflow installs `packages/data` dependencies before running `npm --prefix apps/worker run audit:scorecard`.
- CI no longer fails with `Cannot find module 'better-sqlite3'` from `packages/data/src/db/connection.js`.
- Workflow changes stay limited to the audit job install step.

**Owner agent**: codex
CLAIM: codex 2026-04-02T19:12:47Z
**Time window**: 2026-04-02 -> TBD
**Coordination flag**: needs-sync

**Tests to run**:
- `npm --prefix packages/data ci`
- `npm --prefix apps/worker ci`
- `node -e "require('./apps/worker/src/audit/scorecard.js'); console.log('scorecard module load ok')"`

**Manual validation**:
- Re-run the Model Audit workflow and confirm the `Generate scorecard` step exits 0 instead of failing during module resolution.

**Guard for WI closeout**:
- Do not broaden this WI into scorecard behavior changes; keep it to CI dependency installation only.

## Completion Note
- Verified `.github/workflows/audit.yml` already installs `packages/data` before `apps/worker` in the audit job.
- Ran `npm --prefix packages/data ci` (pass).
- Ran `npm --prefix apps/worker ci` (pass).
- Ran `node -e "require('./apps/worker/src/audit/scorecard.js'); console.log('scorecard module load ok')"` (pass).
- No code change was required in `.github/workflows/audit.yml`; closeout is documentation and queue-state only.

# WI-0674: Normalize remaining result_transformer reload tests

**ID**: WI-0674
**Goal**: Replace stale `importlib.reload(result_transformer)` usage in the remaining contract tests with safe re-import behavior that survives prior `sys.modules` eviction.

**Background**:
After fixing `WI-0673`, the full `cheddar-fpl-sage` suite exposed `tests/test_planner_contract_transformer.py` failing with the same stale-module pattern previously fixed in `api_contract_suite.py`: another test evicts `backend.services.result_transformer` from `sys.modules`, then later code calls `importlib.reload(result_transformer)` on a stale module object. The same pattern still exists in `test_result_transformer_strategy_contract.py`.

**Scope**:
- `cheddar-fpl-sage/tests/test_planner_contract_transformer.py`
- `cheddar-fpl-sage/tests/test_result_transformer_strategy_contract.py`

**Out of scope**:
- Changes to `backend/services/result_transformer.py`
- Changes to production import behavior
- Non-result-transformer tests

**Acceptance**:
- Both scoped test files survive prior module eviction without `ImportError`
- `cd cheddar-fpl-sage && python -m pytest tests/test_planner_contract_transformer.py -v` exits `0`
- `cd cheddar-fpl-sage && python -m pytest tests/test_result_transformer_strategy_contract.py -v` exits `0`
- Full suite progresses past the stale-module reload failures

**Owner agent**: codex
**Time window**: 2026-03-29T00:00:00-04:00 -> TBD
**Coordination flag**: solo
**LOE**: XS

**Tests to run**:
- `cd cheddar-fpl-sage && python -m pytest tests/test_planner_contract_transformer.py -v`
- `cd cheddar-fpl-sage && python -m pytest tests/test_result_transformer_strategy_contract.py -v`
- `cd cheddar-fpl-sage && python -m pytest tests/ -v -x`

**Manual validation**:
- Confirm both files can import or reload `backend.services.result_transformer` after earlier tests delete `backend.*` modules from `sys.modules`
- Confirm no production code changes are required for this fix

**Guard for WI closeout**:
- Before marking complete: confirm all three pytest commands above are runnable from repo root and that only the scoped test files changed.

CLAIM: codex 2026-03-29T00:00:00-04:00

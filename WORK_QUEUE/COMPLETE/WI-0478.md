# WI-0478: Phase 3 Rollout Coordinator (CLV Ledger)

**ID**: WI-0478
**Goal**: Coordinate focused Phase 3 rollout work items so preflight eligibility checks and CLV activation/rollback are executed as separate, auditable scopes.

**Depends on**:

- `WORK_QUEUE/COMPLETE/WI-0471.md`
- `WORK_QUEUE/COMPLETE/WI-0473.md`
- `WORK_QUEUE/COMPLETE/WI-0474.md`
- `WORK_QUEUE/COMPLETE/WI-0475.md`
- `WORK_QUEUE/WI-0481.md`
- `WORK_QUEUE/WI-0482.md`

**Scope**:

- `WORK_QUEUE/WI-0478.md`

**Out of scope**:

- Direct rollout execution edits in operational docs (delegated to WI-0481/WI-0482)
- Market-threshold rollout work (covered by WI-0477 family)

**Acceptance**:

1. WI-0481 and WI-0482 exist with non-overlapping scopes and concrete acceptance criteria.
2. WI-0481 and WI-0482 preserve Phase 3 safety sequence: preflight eligibility -> enablement -> settlement verification -> rollback proof.
3. Coordinator documents dependency order and closeout gate for Phase 3.

**Owner agent**: unassigned
**Time window**: TBD -> TBD
**Coordination flag**: needs-sync

**Tests to run**:

- `test -f WORK_QUEUE/WI-0481.md`
- `test -f WORK_QUEUE/WI-0482.md`

**Manual validation**:

- Confirm WI-0481 closes before WI-0482 starts.
- Confirm Phase 3 is not marked complete until both child WIs include SQL evidence and rollback proof.

**Guard for WI closeout**:

- Before marking complete: confirm every test command in `Tests to run` is runnable from repo root.
- Confirm both child WIs are closed with inclusion/exclusion SQL evidence and rollback proof.

---

## Coordinator Verification Record

**Verified**: 2026-03-16

### Scope Check — WI-0481 vs WI-0482

WI-0481 (Preflight Eligibility + SQL Verification):

- Flag state: `ENABLE_CLV_LEDGER=false` only
- Scope: `docs/ARCHITECTURE_SEPARATION.md` — inclusion/exclusion criteria, preflight SQL checks, go/no-go gate
- Out of scope: enabling CLV, env flag toggles, settlement execution changes

WI-0482 (Activation + Settlement-Safe Rollback):

- Flag state: controlled `ENABLE_CLV_LEDGER=true` activation and rollback to false
- Scope: `docs/DATA_PIPELINE_TROUBLESHOOTING.md`, `env.example` — activation, post-settlement verification, rollback procedure
- Out of scope: schema redesign, new settlement logic, market-threshold rollout

Verdict: scopes are non-overlapping. WI-0481 is preflight-only with flag off; WI-0482 is execution/rollback with controlled flag on and off. Shared files are avoided.

### Phase 3 Safety Sequence Confirmed

1. **Preflight eligibility** (WI-0481): define and verify odds-backed inclusion + projection-only exclusion via SQL checks
2. **Enablement** (WI-0482): activate `ENABLE_CLV_LEDGER` for controlled settlement run
3. **Settlement verification** (WI-0482): verify additive CLV rows and unchanged settlement outputs
4. **Rollback proof** (WI-0482): disable flag and verify no further CLV ledger writes

WI-0482 has `Depends on: WORK_QUEUE/WI-0481.md` — dependency order is formally declared.

### Dependency Order

WI-0481 must reach COMPLETE/ before WI-0482 may start.

### Phase 3 Closeout Gate

Phase 3 (CLV Ledger rollout) is NOT complete until:

1. WI-0481 is in COMPLETE/ with SQL inclusion/exclusion preflight evidence attached.
2. WI-0482 is in COMPLETE/ with activation, post-settlement verification, and rollback proof attached.
3. Both child WIs confirm every test command in their `Tests to run` sections is runnable from repo root.

**Status**: WI-0478 acceptance criteria satisfied — ready to move to COMPLETE/ once coordinator closeout commit lands.

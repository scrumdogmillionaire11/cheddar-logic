# WI-0539: Side-flip threshold unit audit + gate tuning backtest

**ID**: WI-0539
**Goal**: Audit gate threshold units and tune flip criteria using historical flip outcomes so side changes are neither over-suppressed nor noise-driven.

**Scope**:
- `WORK_QUEUE/WI-0539.md`
- `packages/models/src/decision-gate.js`
- `apps/worker/src/utils/decision-publisher.js`
- `apps/worker/src/utils/__tests__/decision-publisher.v2.test.js`
- `packages/data/src/db.js`
- `packages/data/db/migrations/012_create_decision_records.sql` (only if additional audit fields are required)
- `apps/worker/src/jobs/report_telemetry_calibration.js` (if flip-rate reporting is added)

**Out of scope**:
- Reworking canonical edge contract shape (handled in WI-0536)
- NHL ML probability calibration (handled in WI-0538)
- FIRST_PERIOD policy centralization (handled in WI-0537)
- Non-gate model scoring logic

**Acceptance**:
- Gate thresholds use explicit, unambiguous units in names/docs/reason strings.
- Backtest compares current and candidate thresholds (minimum: current, moderate, aggressive) on flip frequency + downstream quality proxy.
- Selected threshold profile is justified by measured tradeoff, not static preference.
- Tests cover edge-delta unit handling and expected allow/block behavior for side flips.

**Owner agent**: codex
**Time window**: 2026-03-21
**Coordination flag**: needs-sync
**Depends on**: WI-0535, WI-0536

**Tests to run**:
- `npm --prefix apps/worker test -- src/utils/__tests__/decision-publisher.v2.test.js`
- `npm --prefix apps/worker test -- src/jobs/__tests__/run_nba_model.test.js`
- `npm --prefix apps/worker test -- src/jobs/__tests__/run_nhl_model.test.js`

**Manual validation**:
- Replay a sample window and compare flip_count/blocked_count deltas between baseline and candidate thresholds.
- Verify decision event logs expose unit-aware threshold comparisons (`edge delta`, threshold value, rule path).

**Guard for WI closeout**:
- Classify this as `ambiguity` + `design_risk`: normalize units before changing numeric thresholds.
- Any DB schema touchpoints require serialized `needs-sync` handling.

CLAIM: codex 2026-03-21


# WI-0957: Add Missing 2025-2026 MLB Stadium Coordinates to Weather Job

**ID**: WI-0957
**Goal**: Add Rate Field, Sutter Health Park, and Daikin Park to `STADIUM_COORDS` in `pull_mlb_weather.js` so weather data is collected for White Sox, Athletics, and Astros home games.

**Depends on**: none

**Scope**:

- `apps/worker/src/jobs/pull_mlb_weather.js` — stadium coordinates + indoor/outdoor designation
- `apps/worker/src/jobs/mlb-runner-constants.js` — team name variants, abbreviations, offense/park splits
- `apps/worker/src/jobs/post_discord_cards.js` — team abbreviations and nicknames for cards/Discord
- `WORK_QUEUE/WI-0957.md` — this file

**Out of scope**:

- Changes to any other existing stadium entries or team data
- Changes to weather fetch logic or NWS API integration
- DB schema changes
- Non-MLB sports team mappings

**Acceptance**:

- `pull_mlb_weather` job no longer logs `Rate Field not in STADIUM_COORDS — skipping`, `Sutter Health Park not in STADIUM_COORDS — skipping`, or `Daikin Park not in STADIUM_COORDS — skipping`.
- Each new entry produces a valid stored weather row (`upserted` count increases when those home games are scheduled).
- Team name variants (e.g., "Oakland Athletics", "Rate Field", "Sutter Health Park") resolve to canonical abbreviations (ATH, CWS, HOU) in both MLB model and Discord output.
- Discord card headers for White Sox, Athletics, and Astros games display abbreviated matchups correctly (e.g., `CWS @ NYY`, `ATH @ LAD`, `HOU @ NYY`).
- Coordinates are verified against a public authoritative source before committing (not guessed).

**Coordinate reference** (verify before committing):

| Venue (API name returned) | Canonical/Alternate Name | Lat | Lon |
|---|---|---|---|
| `Rate Field` | Guaranteed Rate Field — Chicago White Sox. Note: `Guaranteed Rate Field` may already exist in STADIUM_COORDS; if so, add `Rate Field` as an alias pointing to the same coords `[41.8299, -87.6338]`. | 41.8299 | -87.6338 |
| `Sutter Health Park` | Sutter Health Park — Sacramento (Athletics temp venue) | 38.5802 | -121.5002 |
| `Daikin Park` | Daikin Park — Houston Astros (formerly Minute Maid Park) | 29.7573 | -95.3555 |

**Owner agent**: agent-github-copilot
**Time window**: 2026-04-15T00:00:00Z -> 2026-04-16T00:00:00Z
**Coordination flag**: solo

**Tests to run**:

- `npm --prefix apps/worker run job:pull-mlb-weather`
- `npm --prefix apps/worker run test -- --testPathPattern="run_mlb_model.test" --testNamePattern="resolveMlbTeamLookupKeys" --runInBand`
- `npm --prefix apps/worker run test -- --testPathPattern="post_discord_cards.test"`

**Manual validation**:

- Confirm no `not in STADIUM_COORDS` log lines for Rate Field, Sutter Health Park, or Daikin Park.
- Confirm `upserted` count in job output reflects the newly covered stadiums on days those teams play at home.
- Verify Discord output on test games: White Sox matchups use `CWS` abbreviation, Athletics use `ATH`, Astros use `HOU`.
- Verify MLB model team lookups resolve: Oakland Athletics → ATH, Sacramento Athletics → ATH, Chicago White Sox → CWS, Houston Astros → HOU.

**Guard for WI closeout**:

- ✅ Cross-checked coordinate pairs against Google Maps & venue Wikipedia.
- ✅ Verified existing STADIUM_COORDS entries unchanged.
- ✅ Confirmed Guaranteed Rate Field aliased correctly; Rate Field points to same coords.
- ✅ Daikin Park AND Minute Maid Park both marked as indoor/retractable.
- ✅ Team variant aliases cover Oakland/Sacramento Athletics → Athletics canonical name.
- ✅ Discord TEAM_ABBREVIATIONS & TEAM_NICKNAMES include all renamed venues + venue→team mappings.

CLAIM: agent-github-copilot 2026-04-16T00:00:00Z

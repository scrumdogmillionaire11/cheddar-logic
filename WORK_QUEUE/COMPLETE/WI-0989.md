# WI-0989: Debt Audit Consolidation and Prioritized Remediation Queue

> **STATUS: CLOSED** — Moved to COMPLETE after audit queue validation and prioritization review on 2026-04-17.

**ID**: WI-0989

**Goal**: Convert the 2026-04-17 targeted debt audit into a prioritized execution queue with clear blast-radius estimates and recommended actions.

**Scope**:

- `WORK_QUEUE/WI-0989.md`

**Out of scope**:

- Implementing any remediation in code
- Editing worker/web/package modules referenced below
- API behavior changes

**Acceptance**:

- Debt findings are grouped by category and prioritized by expected blast radius and production impact.
- Each finding includes: file path(s), problem class, estimated blast radius, and recommended action (`delete` / `consolidate` / `wrap` / `no-op`).
- Duplicate-module, dead-normalization, orphaned-jobs, stale-debug-scripts, cards-filtering duplication, and unused-exports categories are all captured.
- Follow-on WIs can be split directly from this document without re-auditing.

**Owner agent**: codex

**Time window**: 2026-04-17T00:00:00Z -> TBD

**Coordination flag**: `needs-sync` (cross-cuts worker, packages, web API, and filtering contracts)

**Tests to run**:

- Not applicable (documentation-only work item)

**Manual validation**:

- Confirm all categories from the debt audit are represented.
- Confirm each finding includes required fields (path, class, blast radius, action).
- Confirm priority ordering is highest blast radius first.

CLAIM: codex 2026-04-17T00:00:00Z

---

## Prioritized Findings Queue

### P0 - Highest blast radius

1. **Cards filtering predicate duplication across API + transform/filter layers**

- File path(s):
  - `web/src/app/api/cards/route.ts`
  - `web/src/app/api/cards/[gameId]/route.ts`
  - `web/src/lib/game-card/filters.ts`
  - `web/src/lib/game-card/transform/index.ts`
  - `web/src/lib/game-card/decision.ts`
  - `web/src/components/cards/shared.ts`
- Problem class: Divergent decision/actionability logic duplicated across route filtering, transform, and UI helper surfaces.
- Estimated blast radius: 8-14 files (API routes, game-card libs, related tests, fixtures).
- Recommended action: `consolidate`
- Notes:
  - API route predicates are duplicated between list and by-game endpoints.
  - PASS/LEAN/PLAY and actionability heuristics are not sourced from one canonical helper.

1. **Orphaned runnable job scripts not referenced by scheduler/scripts**

- File path(s):
  - `apps/worker/src/jobs/pull_espn_games_direct.js`
  - `apps/worker/src/jobs/pull_mlb_statcast.js`
  - `apps/worker/src/jobs/pull_moneypuck_blk_rates.js`
  - `apps/worker/src/jobs/pull_nhl_1p_odds.js`
  - `apps/worker/src/jobs/pull_nhl_goalie_starters.js`
  - `apps/worker/src/jobs/pull_nhl_team_stats.js`
  - `apps/worker/src/jobs/pull_nst_blk_rates.js`
  - `apps/worker/src/jobs/pull_public_splits.js`
  - `apps/worker/src/jobs/pull_vsin_splits.js`
  - `apps/worker/src/jobs/sync_nba_player_availability.js`
  - `apps/worker/src/jobs/refresh_stale_odds.js`
  - `apps/worker/src/jobs/repair_mlb_full_game_display_log.js`
  - `apps/worker/src/jobs/run_calibration_report.js`
  - `apps/worker/src/jobs/run_clv_snapshot.js`
  - `apps/worker/src/jobs/run_daily_performance_report.js`
  - `apps/worker/src/jobs/settle_mlb_f5.js`
  - `apps/worker/src/jobs/settle_projections.js`
  - `apps/worker/src/jobs/sweep_stale_settlements.js`
- Problem class: Potentially active jobs present in tree but unreferenced by `ecosystem.config.js` and package scripts.
- Estimated blast radius: 12-20 files (job wiring, scheduler references, runbooks, health checks).
- Recommended action: `wrap`
- Notes:
  - Several entries may be helper-only and should move out of `jobs/` or receive explicit invocation points.

1. **Duplicate module basename collisions (`index.js`, `normalize.js`) across src roots**

- File path(s):
  - `apps/worker/src/models/index.js`
  - `packages/models/src/index.js`
  - `packages/odds/src/index.js`
  - `packages/data/src/db/index.js`
  - `packages/adapters/src/index.js`
  - `packages/data/src/normalize.js`
  - `packages/odds/src/normalize.js`
- Problem class: Same basename exports in different domains increase wrong-import risk and contract drift.
- Estimated blast radius: 10-18 files (model jobs, adapters, odds ingestion, route handlers).
- Recommended action: `wrap`
- Completeness snapshot:
  - `apps/worker/src/models/index.js` (2068 lines, 8 imports)
  - `packages/data/src/normalize.js` (879 lines)
  - `packages/odds/src/normalize.js` (302 lines, 1 import)

### P1 - Medium blast radius

1. **Team-metrics shim duplication (`src` and package canonical)**

- File path(s):
  - `apps/worker/src/team-metrics.js`
  - `packages/data/src/team-metrics.js`
- Problem class: Thin wrapper shim coexists with canonical implementation; encourages deep import drift.
- Estimated blast radius: 3-6 files (worker jobs + package entrypoint consumers).
- Recommended action: `consolidate`

1. **Dead normalization logic outside src tree**

- File path(s):
  - `.tmp-post_discord_cards.prev.js`
  - `scripts/audit-lineage.js`
  - `scripts/calibrate_nba_totals.js`
- Problem class: Duplicate status checks outside canonical src flow.
- Estimated blast radius: 2-6 files.
- Recommended action:
  - `.tmp-post_discord_cards.prev.js` -> `delete`
  - `scripts/audit-lineage.js` -> `wrap`
  - `scripts/calibrate_nba_totals.js` -> `no-op` (intentional offline calibration classifier)
- Notes:
  - `scripts/audit-lineage.js` treats `PLAY|LEAN|PASS` as sufficient for call-action presence and does not include execution gate constraints.

1. **Stale debug/test/scratch/check scripts outside tests directories**

- File path(s):
  - `.copilot-debug-f5.js`
  - `scripts/test_enrich.js`
  - `scripts/debug/check-card-mismatch.js`
  - `scripts/debug/check-play-status.js`
  - `scripts/debug/check-ncaam-payload.js`
  - `scripts/debug/check-ncaam-market.js`
  - `scripts/debug/check-raw-data.js`
- Problem class: Unreferenced ad hoc scripts in active tree, not package-script managed.
- Estimated blast radius: 4-8 files.
- Recommended action: `delete`

### P2 - Lower blast radius

1. **Likely unused exports in src not referenced by apps/web**

- File path(s):
  - `packages/odds/src/index.js` (`axiosGetWithRetry`, `getSportConfig`)
  - `packages/models/src/index.js` (`computeWinProbHome`, `buildDriverSummary`)
- Problem class: Barrel exports with no app/web import evidence.
- Estimated blast radius: 1-3 files.
- Recommended action:
  - `axiosGetWithRetry` -> `wrap`
  - `getSportConfig` -> `no-op`
  - `computeWinProbHome` -> `no-op`
  - `buildDriverSummary` -> `no-op`
- Notes:
  - Candidate list should be revalidated against package-internal usage before deletion.

---

## Proposed Execution Split

1. `WI-0990` (P0): Consolidate cards filtering predicate source of truth and dedupe API route predicates.
1. `WI-0991` (P0): Classify orphaned worker jobs into runnable vs helper-only and wire/relocate accordingly.
1. `WI-0992` (P0/P1): Normalize duplicate module entrypoints (`normalize`, `index`, `team-metrics`) behind explicit facades.
1. `WI-0993` (P1): Remove stale debug/test temp scripts and archive intentional diagnostics.
1. `WI-0994` (P2): Revalidate and prune/retain low-risk unused exports.

## Closeout Evidence

- Manual validation complete:
  - All debt categories from the targeted audit are represented.
  - Every finding includes required fields: file path(s), problem class, blast radius estimate, and recommended action.
  - Priority ordering is grouped by blast radius and production impact (`P0` -> `P1` -> `P2`).
- Tests: Not applicable (documentation-only work item).

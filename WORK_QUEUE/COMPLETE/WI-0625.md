# WI-0625: Remove unused liveLineBook assignment in cards-page-client

**ID**: WI-0625
**Goal**: Eliminate the unused `liveLineBook` variable in `cards-page-client.tsx` so the file is lint-clean.

**Scope**:
- `web/src/components/cards-page-client.tsx`

**Out of scope**:
- Any UI behavior changes
- Any refactor beyond the unused variable removal

**Acceptance**:
- `web/src/components/cards-page-client.tsx` no longer declares an unused `liveLineBook` variable.
- `npm --prefix web run lint -- src/components/cards-page-client.tsx` exits without the reported warning.

**Owner agent**: codex
**Time window**: 2026-03-28T01:37:45Z -> TBD
**Coordination flag**: solo

**Tests to run**:
- `npm --prefix web run lint -- src/components/cards-page-client.tsx`

**Manual validation**:
- Confirm the rendered odds/book text path is unchanged aside from the removed unused local.

**Guard for WI closeout**:
- Before marking complete: confirm every test command in `Tests to run` is runnable from repo root (i.e., exists in web/package.json scripts or is a valid `node path/to/test.js` command).
- If a script is added to web/package.json as part of this WI, add it to this WI's Tests section with the full `npm --prefix web run <script>` form.

CLAIM: codex 2026-03-28T01:37:45Z

# WI-0731: First weekly scorecard run and family ranking table

**ID**: WI-0731
**Goal**: Run `audit:scorecard` after baseline lock (WI-0728) and family registry is set (WI-0730), produce the first archived scorecard, and generate a ranked family table that combines audit stability, performance status, and operational confidence into a single decision surface.

**Scope**:
- `WORK_QUEUE/WI-0731.md`
- `apps/worker/src/audit/outputs/scorecards/` — archived scorecard output (first run)
- `docs/model_audit_weekly_scorecard_YYYY-WXX.md` — first published scorecard doc (date-stamped)

**Out of scope**:
- Changes to scorecard tooling or CI config
- Changes to the family registry (WI-0730 owns that)
- Any new fixtures or baselines
- Promotion or demotion of any family based on scorecard results (those become separate WIs)

**Acceptance**:
- `npm run audit:scorecard` exits 0 and produces a result file under `apps/worker/src/audit/outputs/scorecards/`.
- A human-readable doc `docs/model_audit_weekly_scorecard_YYYY-WXX.md` exists with the ranked table:

  | Card family      | Audit stability | Performance status | Operational status |
  |------------------|----------------|--------------------|-------------------|
  | NBA_TOTAL        | High           | Healthy            | LIVE              |
  | NBA_SPREAD       | Medium         | Healthy            | LIVE              |
  | NHL_TOTAL        | Medium         | Watch              | LIVE              |
  | NHL_ML           | Medium         | Watch              | LIVE              |
  | NHL_1P_TOTAL     | High           | Not executable     | PROJECTION_ONLY   |
  | NHL_PLAYER_SHOTS | Medium         | Not executable     | PROJECTION_ONLY   |
  | MLB_PITCHER_K    | Medium         | Not executable     | PROJECTION_ONLY   |
  | MLB_F5_TOTAL     | Medium         | Not executable     | PROJECTION_ONLY   |

  Values above are defaults; replace with actuals from the run.
- Every family in the registry (WI-0730) has a row in the scorecard. No silent omissions.
- The doc includes a short "Actions" section listing any families flagged for watch, with the next review date.

**Owner agent**: copilot
**Time window**: after WI-0728 and WI-0730 close → TBD
**Coordination flag**: solo

**Dependencies**:
- WI-0728 must be complete (baselines locked) before this WI can produce a valid scorecard.
- WI-0730 must be complete (registry exists) so all family rows are present.

**Tests to run**:
- `npm run audit:scorecard` — must exit 0.
- Confirm scorecard output file is written to `apps/worker/src/audit/outputs/scorecards/`.
- Smoke test: delete one baseline file, re-run `audit:scorecard`, confirm affected family shows a failure row rather than silent omission.

**Manual validation**:
- Read the scorecard doc. Confirm every family from the registry has a row.
- Confirm "Watch" families have a next-review date set.
- Confirm no PROJECTION_ONLY family is listed as "Healthy/executable" in performance status.

**Failure modes to guard**:
- Do not treat a passing scorecard as proof of model quality — audit stability and performance quality are separate axes (see WI-0725).
- Do not allow PROJECTION_ONLY families to appear with an "executable" performance status without a documented investigation.
- Set a calendar reminder for the 2–4 week edge retention review (future WI, not in scope here).
CLAIM: copilot 2026-04-02T00:00:00Z

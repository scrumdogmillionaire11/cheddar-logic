# WI-0322: Stabilize Production DB Path Guidance

**ID**: WI-0322
**Goal**: Align production DB path guidance and checks to prevent CHEDDAR_DB_PATH drift across services.

**Scope**:

- docs/ops-runbook.md
- apps/worker/README.md
- docs/QUICKSTART.md
- env.example
- scripts/manage-scheduler.sh
- .github/workflows/deploy-production.yml
- .env.production.example
- packages/data/README.md
- scripts/consolidate-record-db.sh
- scripts/start-scheduler.sh
- scripts/dev-mode-local.sh
- scripts/dev-mode-snapshot.sh
- scripts/db-context.sh
- AGENTS.md

**Out of scope**:

- Systemd unit file changes on the Pi
- FPL Sage corruption handling (tracked in WI-0321)
- Schema or migration changes

**Acceptance**:

- Canonical prod DB path is the file that currently contains card_payloads; non-canonical prod path references are removed or deprecated
- Ops runbook includes a 3-command go/no-go checklist for DB path + schema + API
- Scheduler helper highlights drift when the open DB differs from CHEDDAR_DB_PATH
- Deploy workflow uses the canonical prod DB path and fails if it points elsewhere
- Dev defaults standardized to /tmp/cheddar-logic/cheddar.db in docs and scripts
- consolidate-record-db.sh sets only CHEDDAR_DB_PATH (no legacy DB path vars)
- AGENTS.md codifies the canonical prod DB path after validation

**Owner agent**: copilot
**Time window**: 2026-03-06T00:00:00Z -> TBD
**Coordination flag**: needs-sync

**Tests to run**:

- Trigger deploy-production workflow and verify schema gate checks the canonical DB path

**Manual validation**:

- sudo systemctl show cheddar-web -p Environment | grep CHEDDAR_DB_PATH
- sudo systemctl show cheddar-worker -p Environment | grep CHEDDAR_DB_PATH
- sqlite3 CANONICAL_PROD_DB_PATH "SELECT name FROM sqlite_master WHERE type='table' AND name='card_payloads';"
- ./scripts/manage-scheduler.sh db

CLAIM: copilot 2026-03-06T00:00:00Z

# WI-0815: Propagate `EDGE_SANITY_CLAMP_APPLIED` to watchdog CAUTION in `computeTotalEdge`

| Field | Value |
|---|---|
| **ID** | WI-0815 |
| **Goal** | When the NHL edge sanity clamp fires in `computeTotalEdge` (raw edge > 18%), force `watchdog_status='CAUTION'` so the play-cleanliness gate blocks the card from PLAY classification. Eliminates the path where model miscalibration produces a clamped-but-confident PLAY card. |
| **Priority** | Medium |
| **Depends on** | None |
| **Coordination flag** | None |

---

## Background

`computeTotalEdge` in `packages/models/src/edge-calculator.js` applies a sanity clamp for NHL-style totals (sigma ≤ 3):

```js
if (isNhlStyleTotal && Math.abs(edge) > 0.18) {
  edge = Math.sign(edge) * 0.18;
  railFlags.push('EDGE_SANITY_CLAMP_APPLIED');
}
```

A raw edge above 18% means the projection and the market line are dramatically misaligned — the sigma is wrong, the projection is broken, or the odds data is stale. The clamp caps the output at 18%, but then proceeds to pass that value through `computeConfidence` with `watchdogStatus` at its default (no degradation), producing a high-confidence PLAY signal on what is genuinely pathological input.

The `EDGE_SANITY_CLAMP_APPLIED` flag is stored in `rail_flags` within the card payload but is never forwarded to the watchdog or cleanliness gate. The play-cleanliness profile for NHL/NHL (`require_watchdog_ok: true`) would block it — but only if `watchdogStatus` were `'CAUTION'`.

This is a one-function fix inside `computeTotalEdge`.

---

## Scope

**Files to change:**

- `packages/models/src/edge-calculator.js` — `computeTotalEdge` function only
- `packages/models/src/__tests__/edge-calculator.test.js`
- `packages/models/src/__tests__/decision-pipeline-v2-sigma-fallback-gate.test.js`

**Out of scope:**

- `computeSpreadEdge`, `computeMoneylineEdge` — no sanity clamp logic there
- `decision-pipeline-v2.js` — no change; it already reads `watchdogStatus` from confidence context
- NHL prop or player-shots edge paths

---

## Detailed Implementation

### Task 1 — Forward clamp event to `confidenceContext`

**File:** `packages/models/src/edge-calculator.js`, inside `computeTotalEdge`

**Current:**

```js
let edge = p_fair - p_implied;
if (isNhlStyleTotal && Math.abs(edge) > 0.18) {
  edge = Math.sign(edge) * 0.18;
  railFlags.push('EDGE_SANITY_CLAMP_APPLIED');
}
const edgePoints = mu - L;

const result = {
  edge: Number(edge.toFixed(4)),
  ...
  confidence: computeConfidence({
    baseConfidence: 0.88,
    ...confidenceContext,
  }),
```

**After:**

```js
let edge = p_fair - p_implied;
let effectiveConfidenceContext = confidenceContext;
if (isNhlStyleTotal && Math.abs(edge) > 0.18) {
  edge = Math.sign(edge) * 0.18;
  railFlags.push('EDGE_SANITY_CLAMP_APPLIED');
  // Force watchdog CAUTION so play-cleanliness gate blocks PLAY classification.
  // A >18% raw edge signals broken sigma or stale odds — do not promote to PLAY.
  effectiveConfidenceContext = {
    ...confidenceContext,
    watchdogStatus: 'CAUTION',
  };
}
const edgePoints = mu - L;

const result = {
  edge: Number(edge.toFixed(4)),
  ...
  confidence: computeConfidence({
    baseConfidence: 0.88,
    ...effectiveConfidenceContext,
  }),
```

This is the complete change. `computeConfidence` already applies a `-0.1` penalty for `watchdogStatus === 'CAUTION'` and the `resolvePlayCleanlinessProfile` for NHL already sets `require_watchdog_ok: true`, so this single line change causes the cleanliness gate to block PLAY promotion for any edge-clamped NHL total.

---

## Acceptance Criteria

- [ ] `computeTotalEdge` with `sigmaTotal=1.8` (NHL) and projection far from line (raw edge > 0.18) returns `confidence` reduced by ≥ 0.1 compared to an unclamped call with same inputs
- [ ] `rail_flags` still contains `'EDGE_SANITY_CLAMP_APPLIED'` in the output
- [ ] A card generated from clamped NHL total input receives `official_status='LEAN'` or `'PASS'` (not `'PLAY'`) via the play-cleanliness gate
- [ ] `computeTotalEdge` with NBA sigma (sigmaTotal=14) and edge < 0.18 is unaffected — no `CAUTION` override
- [ ] All existing edge-calculator tests pass; add one new test: "clamped NHL total produces lower confidence than equivalent unclamped NBA total"

---

## Owner Agent

| Field | Value |
|---|---|
| **Owner agent** | codex |
| **Time window** | Half session — single-function change |
| **Coordination flag** | None |

CLAIM: codex 2026-04-09T03:29:53Z

---

## Tests to Run

```bash
cd /Users/ajcolubiale/projects/cheddar-logic
node packages/models/src/__tests__/edge-calculator.test.js
npm --prefix packages/models test -- --runInBand packages/models/src/__tests__/decision-pipeline-v2-sigma-fallback-gate.test.js
```

---

## Manual Validation

1. Identify an NHL totals card where `rail_flags` contains `EDGE_SANITY_CLAMP_APPLIED`:
   `sqlite3 /opt/data/cheddar-prod.db "SELECT id, json_extract(payload_data,'$.decision_v2.official_status'), json_extract(payload_data,'$.rail_flags') FROM card_payloads WHERE card_type='nhl-totals-call' AND json_extract(payload_data,'$.rail_flags') LIKE '%EDGE_SANITY_CLAMP%' LIMIT 5;"`
2. Confirm `official_status` is `LEAN` or `PASS`, not `PLAY`
3. Trigger a test run with an artificially wide projection to force a clamp, confirm the classification

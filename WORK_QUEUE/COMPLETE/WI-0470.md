# WI-0470: Soccer Tier-1 Player Identity Ingest + Mapping

**ID**: WI-0470
**Goal**: Add upstream soccer Tier-1 prop ingestion and deterministic player identity mapping so soccer player props render with resolved player identity instead of Unknown Player fallback.

**Depends on**:

- `WORK_QUEUE/WI-0437.md`

**Scope**:

- `WORK_QUEUE/WI-0470.md`
- `apps/worker/src/jobs/pull_soccer_player_props.js`
- `apps/worker/src/jobs/run_soccer_model.js`
- `apps/worker/src/schedulers/main.js`
- `apps/worker/package.json`
- `apps/worker/src/jobs/__tests__/pull_soccer_player_props.test.js`
- `apps/worker/src/jobs/__tests__/run_soccer_model.test.js`
- `apps/worker/src/__tests__/scheduler-soccer-t45.test.js`
- `packages/data/src/db.js`
- `packages/data/index.js`
- `docs/QUICKSTART.md`
- `web/src/lib/game-card/transform.ts`

**Out of scope**:

- Web UI quick-filter UX changes (`WI-0469` scope)
- FPL canonical ID mapping source integration
- Tier-2 soccer prop ingestion (`player_shots_on_target`, `anytime_goalscorer`, `team_corners`)
- DB schema migrations

**Acceptance**:

1. Worker can ingest soccer Tier-1 player prop lines (`player_shots`, `to_score_or_assist`) from Odds API event props endpoint into `player_prop_lines`.
2. Soccer model can build Tier-1 player cards from ingested lines and include resolved `player_name` plus deterministic synthetic `player_id` in payload.
3. Scheduler queues soccer prop ingest before soccer model windows (fixed + T-minus + soccer T-45 checkpoint).
4. When Tier-1 player lines are present, emitted soccer player cards are `PROP` with resolved player identity; Unknown Player fallback path is avoided for those rows.
5. Targeted worker tests pass for ingestion parsing, model identity mapping, and scheduling behavior.

**Owner agent**: codex
**Time window**: 2026-03-16T00:00:00Z -> TBD
**Coordination flag**: solo

**Status**: Ready for review (implementation complete; manual ingest validation blocked by local env key)

**Tests to run**:

- `npm --prefix apps/worker test -- src/jobs/__tests__/pull_soccer_player_props.test.js`
- `npm --prefix apps/worker test -- src/jobs/__tests__/run_soccer_model.test.js`
- `npm --prefix apps/worker test -- src/__tests__/scheduler-soccer-t45.test.js`
- `npm --prefix apps/worker run job:run-soccer-model:test`

**Manual validation**:

- Run soccer ingest + model and confirm latest soccer player cards include resolved `player_name` and non-empty `player_id`.
- Verify Brentford/Wolves-style cards no longer surface as Unknown Player when source lines are available.

CLAIM: codex 2026-03-16T00:00:00Z

## Completion evidence (2026-03-16)

**Implementation commits (primary)**:

- `db93b23` — adds Tier-1 soccer prop ingest + model identity mapping test coverage in scoped worker/data/web files.
- `6aa6088` — queues `pull_soccer_player_props` before fixed-window soccer model runs and updates quickstart notes.
- `9c317d0` — follow-up lint cleanup in `run_soccer_model` within scope.

**Acceptance verification executed**:

- ✅ `npm --prefix apps/worker test -- src/jobs/__tests__/pull_soccer_player_props.test.js` (4/4 passing)
- ✅ `npm --prefix apps/worker test -- src/jobs/__tests__/run_soccer_model.test.js` (40/40 passing)
- ✅ `npm --prefix apps/worker test -- src/__tests__/scheduler-soccer-t45.test.js` (3/3 passing)
- ✅ `npm --prefix apps/worker run job:run-soccer-model:test` (successful run; generated cards)

**Manual validation status**:

- ⚠️ `npm --prefix apps/worker run job:pull-soccer-player-props:dry` is blocked locally: `ODDS_API_KEY not set`.
- Model-run validation path was executed successfully, but ingest-path manual proof requires environment key injection.

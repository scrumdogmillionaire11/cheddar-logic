# WI-0606: Re-export `computeLineDelta` from data package to restore NBA line-delta logging

**ID**: WI-0606
**Goal**: Restore `computeLineDelta` to the public `@cheddar-logic/data` surface so NBA model jobs stop failing line-delta computation at runtime.

**Scope**:

- `packages/data/index.js`
- `packages/data/__tests__/` or `apps/worker/src/__tests__/` (regression coverage for the package export / NBA caller path)

**Out of scope**:

- Changing `computeLineDelta` math in `packages/data/src/db.js`
- NBA projection or decision thresholds
- MLB ingest or watchdog work

**Background**:
The first refresh after WI-0604 exposed an unrelated runtime regression in `run_nba_model.js`: `computeLineDelta is not a function`. The function still exists in `packages/data/src/db.js`, but it is no longer exported from `packages/data/index.js`, so worker callers receive `undefined`.

**Acceptance**:

- `@cheddar-logic/data` exports `computeLineDelta` again.
- `run_nba_model.js` no longer logs `computeLineDelta is not a function`.
- Regression coverage proves the package export exists and is callable from the current import surface.

**Owner agent**: unassigned
**Time window**: TBD -> TBD
**Coordination flag**: solo

**Tests to run**:

- `npm --prefix packages/data test`
- `npm --prefix apps/worker run job:run-nba-model:test`

**Manual validation**:

- Run `npm --prefix apps/worker run job:run-nba-model`.
- Confirm the previous `computeLineDelta is not a function` log no longer appears.

**Guard for WI closeout**:

- Before marking complete: confirm every test command in `Tests to run` is runnable from repo root.
- Verify no other package exports were unintentionally changed.

CLAIM: unassigned

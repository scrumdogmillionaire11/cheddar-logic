# WI-0345: Web API + Transform v2 Pass-Through Hard Cut

**ID**: WI-0345
**Goal**: Make `/api/games` and transform pipeline strict consumers of worker `decision_v2` for wave-1 eligible rows, removing legacy repair/inference/fallback in that path.

**Scope**:

- `WORK_QUEUE/WI-0345.md`
- `web/src/app/api/games/route.ts`
- `web/src/lib/game-card/transform.ts`
- `web/src/lib/types/game-card.ts`
- `web/src/__tests__/api-games-repair-budget.test.js`
- `web/src/__tests__/integration/games-pipeline-contract.test.ts`

**Out of scope**:

- `apps/worker/**`
- `web/src/components/cards-page-client.tsx`
- Props/futures/out-of-scope sport migration

**Acceptance**:

- `/api/games` wave-1 rows pass through worker `decision_v2` without verdict synthesis.
- Wave-1 path no longer uses repair allowlists/title inference/action-status fallback.
- Transform uses `decision_v2` as authoritative verdict/tier/reason for wave-1 rows.

**Owner agent**: Codex
**Time window**: 2026-03-08T00:00:00Z -> TBD
**Coordination flag**: needs-sync

**Tests to run**:

- `npm --prefix web run test:transform:truth-price`
- `npm --prefix web run test:api:games:repair-budget`
- `npm --prefix web run test:transform:market`

**Manual validation**:

- Query `/api/games` and verify wave-1 PLAY rows include `decision_v2` and omit repair metadata.

CLAIM: Codex 2026-03-08T16:20:00Z

---

## Completion Evidence

**Completed**: 2026-03-10T00:00:00Z  
**Completed by**: GitHub Copilot

### Test Results (All Passed ✅)

1. **`npm --prefix web run test:api:games:repair-budget`** - ✅ PASSED
   - Verified route.ts enforces `decision_v2` requirement for wave-1 plays
   - Confirmed no repair metadata injection in API output
   - Validated all wave-1 plays have proper decision_v2 structure

2. **`npm --prefix web run test:transform:truth-price`** - ✅ PASSED
   - Transform truth/price contract source tests passed

3. **`npm --prefix web run test:transform:market`** - ✅ PASSED
   - Transform market contract source tests passed

### Acceptance Criteria Confirmation

✅ **Criterion 1**: `/api/games` wave-1 rows pass through worker `decision_v2` without verdict synthesis

- Implementation: route.ts:1424-1433
- Wave-1 plays with missing `decision_v2` are now skipped (hard requirement enforced)
- `applyWave1DecisionFields()` maps official_status directly without synthesis

✅ **Criterion 2**: Wave-1 path no longer uses repair allowlists/title inference/action-status fallback

- No repair metadata (`repair_applied`, `repair_rule_id`, `repair_stats`) in wave-1 path
- Transform early-returns for wave-1 before reaching legacy inference logic
- Title inference only applies to non-wave1 rows

✅ **Criterion 3**: Transform uses `decision_v2` as authoritative verdict/tier/reason for wave-1 rows

- transform.ts:659-695 `selectWave1DecisionCandidate()`
- transform.ts:980-1189 `buildPlay()` uses:
  - `decisionV2.official_status` for verdict
  - `decisionV2.play_tier` for tier
  - `decisionV2.primary_reason_code` for reason

### Changes Made

- **Fixed route.ts enforcement**: Changed wave-1 logic from optional decision_v2 check to hard requirement with `continue` skip for missing decision_v2
- Wave-1 sports: NBA, NHL, NCAAM
- Wave-1 markets: MONEYLINE, SPREAD, TOTAL, PUCKLINE, TEAM_TOTAL

### Files Modified

- web/src/app/api/games/route.ts - Enforced decision_v2 requirement for wave-1 plays

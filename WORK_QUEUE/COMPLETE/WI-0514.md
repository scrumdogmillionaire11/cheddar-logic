# WI-0514: Deterministic Wildcard + Free Hit evaluators

**ID**: WI-0514
**Goal**: Implement deterministic `Wildcard` and `Free Hit` evaluators that follow the March 2026 chip spec for formulas, vetoes, horizon suppression, and late-season escalation.

**Depends on**:

- WI-0513

**Scope**:

- `WORK_QUEUE/WI-0514.md`
- `cheddar-fpl-sage/src/cheddar_fpl_sage/analysis/decision_framework/chip_engine/wildcard_free_hit.py`
- `cheddar-fpl-sage/tests/test_chip_engine_wildcard_free_hit.py`

**Out of scope**:

- `Bench Boost` / `Triple Captain` evaluator work
- `ChipAnalyzer` adapter wiring
- Backend/frontend API contract changes

**Acceptance**:

- `evaluate_wildcard()` uses `teamEvDelta`, `fixtureRunScore`, and `hitCostAvoided` with the specified half-season minimums and DGW-imminent vetoes.
- `evaluate_free_hit()` separately scores BGW defense and DGW attack, selects the stronger path, and hard-vetoes when `Wildcard` is still available with meaningful permanent squad EV gain.
- Tests cover normal FIRE cases, hard veto cases, WATCH cases, soft escalation, hard escalation, and GW37 emergency behavior for both chips.
- Returned decisions always populate `status`, `score`, `reasonCode`, `reasonCodes`, and `forcedBy` / `watchUntil` when applicable.

**Owner agent**: copilot
**Time window**: 2026-03-19T22:35:00Z -> TBD
**Coordination flag**: solo
**CLAIM**: copilot 2026-03-19T22:35:00Z

**Tests to run**:

- `pytest cheddar-fpl-sage/tests/test_chip_engine_wildcard_free_hit.py`

**Manual validation**:

- Review at least one `Wildcard` forced decision and confirm `forcedBy` identifies the escalation path.
- Review at least one `Free Hit` BGW-defense case and one DGW-attack case to confirm the evaluator picks the higher-value path.

**Guard for WI closeout**:

- Reuse shared helpers from WI-0513; do not duplicate horizon or force-escalation logic inside the evaluator.
- Keep all changes inside the scoped files.

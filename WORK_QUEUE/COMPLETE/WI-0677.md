# WI-0677: Stabilize observability tests against backend.main module churn

**ID**: WI-0677
**Goal**: Ensure observability tests monkeypatch the live `backend.main` module used by the app fixture, even after other tests evict and re-import backend modules.

**Background**:
After fixing `WI-0676`, the full `cheddar-fpl-sage` suite exposed `tests/tests_new/test_observability.py::test_health_probe_marks_degraded_when_upstream_unavailable`. The route implementation in `backend.main` is correct, but the test imports `backend.main` at module import time; later suite activity can replace that module object, so monkeypatching the stale reference no longer affects the app serving `/health`.

**Scope**:
- `cheddar-fpl-sage/tests/tests_new/test_observability.py`

**Out of scope**:
- Changes to `backend/main.py`
- Changes to observability middleware
- Non-observability tests

**Acceptance**:
- Observability tests patch the live `backend.main` module used at request time
- `cd cheddar-fpl-sage && python -m pytest tests/tests_new/test_observability.py -v` exits `0`
- Full suite progresses past the stale observability monkeypatch failure

**Owner agent**: codex
**Time window**: 2026-03-29T00:00:00-04:00 -> TBD
**Coordination flag**: solo
**LOE**: XS

**Tests to run**:
- `cd cheddar-fpl-sage && python -m pytest tests/tests_new/test_observability.py -v`
- `cd cheddar-fpl-sage && python -m pytest tests/ -v -x`

**Manual validation**:
- Confirm monkeypatching `check_http_health` on the live module forces `/health` to return `503`
- Confirm request-header and logging observability tests still pass

**Guard for WI closeout**:
- Before marking complete: confirm both pytest commands above are runnable from repo root and that only the scoped test file changed.

CLAIM: codex 2026-03-29T00:00:00-04:00

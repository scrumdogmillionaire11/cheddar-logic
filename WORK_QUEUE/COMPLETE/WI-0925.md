# WI-0925: Prune Unused Public Exports from Worker Models Index

**ID**: WI-0925

**Goal**: Reduce legacy API surface by removing externally-unused exports from the worker models index while preserving active `getModel` behavior.

**Depends on**: WI-0924

**Scope**:

- `apps/worker/src/models/index.js`
- `apps/worker/src/models/__tests__/ownership-enforcement.ast.test.js`

**Out of scope**:

- Rewriting inference internals (`getInference` implementation details)
- Introducing new models abstraction layers
- Web/data package changes

**Acceptance**:

- `module.exports` in models index no longer exposes externally-unused symbols identified in the legacy scan.
- Existing job runners continue consuming `getModel` and required domain helpers without behavior regressions.
- Ownership/AST tests are updated to assert the new intended export contract.
- Repo search confirms removed export names have no live external callsites.

**Owner agent**: copilot

**Time window**: 2026-04-13T12:30:01Z -> TBD

**Coordination flag**: solo

**Tests to run**:

- `npm --prefix apps/worker run test -- --testPathPattern="ownership-enforcement.ast|run_nhl_model|run_nba_model|run_mlb_model" --no-coverage`
- `node -e "const m=require('./apps/worker/src/models'); console.log(Object.keys(m).sort())"`

**Manual validation**:

- Verify key worker model jobs (`run-nhl-model:test`, `run-nba-model:test`, `run-mlb-model:test`) still execute successfully.
- Confirm removed exports are not used by any runtime module.

CLAIM: copilot 2026-04-13T12:30:01Z

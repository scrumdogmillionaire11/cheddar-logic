# WI-0436: NHL Data Enrichment (Targeted, No Rebuild)

**ID**: WI-0436
**Goal**: Close targeted NHL driver-data quality gaps identified by WI-0434 while preserving current NHL model architecture and output compatibility.

**Depends on**: `WORK_QUEUE/WI-0434.md`

**Scope**:
- `WORK_QUEUE/WI-0436.md`
- `apps/worker/src/models/index.js`
- `apps/worker/src/jobs/run_nhl_model.js`
- `apps/worker/src/jobs/__tests__/run_nhl_model.test.js`
- `apps/worker/src/jobs/__tests__/run_nhl_model.market-calls.test.js`
- `apps/worker/src/models/__tests__/nhl-goalie-state.test.js`

**Out of scope**:
- NHL model rewrite or architecture replacement
- Pace-model redesign/recalibration work
- Changes to unrelated sports (NBA/NCAAM/Soccer/FPL)
- Public API route redesign

**Acceptance**:
- Targeted NHL context fields called out in WI-0434 debt register are sourced/enriched where feasible (or explicitly gated/flagged when unavailable).
- NHL payload contract remains backward-compatible for existing consumers.
- No regression in NHL market-call semantics for moneyline/spread/total/1P paths.
- Residual NHL enrichment debt is explicitly documented with defer rationale.

**Owner agent**: codex
**Time window**: 2026-03-13T16:16:00Z -> TBD
**Coordination flag**: needs-sync

**Tests to run**:
- `npm --prefix apps/worker test -- src/jobs/__tests__/run_nhl_model.test.js`
- `npm --prefix apps/worker test -- src/jobs/__tests__/run_nhl_model.market-calls.test.js`
- `npm --prefix apps/worker test -- src/models/__tests__/nhl-goalie-state.test.js`
- `node web/src/__tests__/cards-1p-projection-source.test.js`

**Manual validation**:
- Run `npm --prefix apps/worker run job:run-nhl-model:test` and confirm job completes without contract errors.
- Spot-check generated NHL payloads for enriched fields and stable card types.

CLAIM: codex 2026-03-13T17:00:00Z

**Residual debt (deferred with rationale)**:
- `shot_environment.proxy` is now populated/flagged, but proxy deltas are metadata-only and not used to force new play-direction logic when xGF is missing. Rationale: preserve current card semantics and backward compatibility.
- `special_teams` still relies on MoneyPuck availability for full `pp_pk_delta`; when source data is absent, payloads now carry explicit `status=missing` + `missing_inputs` instead of silent neutral assumptions.

# WI-0466: Harden Decision-Basis Rollout Artifacts from /files

**ID**: WI-0466
**Goal**: Make the `/files` integration bundle repo-compatible and executable without runtime import failures, invalid commands, or ambiguous patch steps.

**Scope**:
- `files/INTEGRATION.md`
- `files/db-telemetry.js`
- `files/run_nhl_player_shots_model.PATCH.js`
- `files/run_soccer_model.PATCH.js`
- `files/decision-pipeline-v2.patch.js`
- `WORK_QUEUE/WI-0466.md`

**Out of scope**:
- Applying these patches directly to production source files under `apps/worker/src/**`, `packages/models/src/**`, `packages/data/src/**`
- UI redesign or API contract changes beyond additive optional fields

**Acceptance**:
- Import paths in artifact files are self-consistent with documented copy targets.
- Integration commands in `files/INTEGRATION.md` are runnable in this repo (root vs `--prefix apps/worker` corrected).
- Threshold patch instructions are decision-complete for current `decision-pipeline-v2.js` callsites.
- Telemetry docs and code behavior match (either enforce append-only or correct docs).
- `node --check files/*.js` passes after edits.
- Review checklist includes a copy-path validation step that catches module-resolution issues before patch apply.

**Owner agent**: codex
**Time window**: 2026-03-16T00:00:00Z -> TBD
**Coordination flag**: solo

**Tests to run**:
- `node --check files/flags.js`
- `node --check files/decision-basis.types.js`
- `node --check files/decision-pipeline-v2.patch.js`
- `node --check files/run_nhl_player_shots_model.PATCH.js`
- `node --check files/run_soccer_model.PATCH.js`
- `node --check files/db-telemetry.js`

**Manual validation**:
- Verify all `require(...)` paths in artifact files resolve after following documented copy targets.
- Verify every command in `files/INTEGRATION.md` maps to an existing script in `package.json` or `apps/worker/package.json`.
- Perform a copy-only dry run (no source patch apply) and confirm no "module not found" paths remain.

**Guard for WI closeout**:
- All test commands above are runnable from repo root.
- No edits outside Scope.
- Findings from review are closed or explicitly deferred in WI notes.

CLAIM: codex 2026-03-16T00:00:00Z

## Implementation log

- 2026-03-16: Updated rollout commands in `files/INTEGRATION.md` from `npm run worker` to `npm --prefix apps/worker run scheduler` for repo-runnable execution.
- 2026-03-16: Reconciled telemetry behavior docs in `files/db-telemetry.js` to reflect append-first with settlement-time in-place updates.
- 2026-03-16: Executed all WI syntax checks from repo root:
  - `node --check files/flags.js`
  - `node --check files/decision-basis.types.js`
  - `node --check files/decision-pipeline-v2.patch.js`
  - `node --check files/run_nhl_player_shots_model.PATCH.js`
  - `node --check files/run_soccer_model.PATCH.js`
  - `node --check files/db-telemetry.js`
- Result: all checks passed.

## Closeout status

- Remaining acceptance mismatches resolved.
- Ready to move to `WORK_QUEUE/COMPLETE/WI-0466.md`.

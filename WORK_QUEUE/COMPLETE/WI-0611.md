# WI-0611: Replace NHL fault harness stubs with real implementations

**ID**: WI-0611
**Goal**: Replace the 5 stub functions in `nhl-totals-fault-harness.test.js` with imports from the real implementation modules so the fault harness tests the actual pipeline.

**Background**:
`apps/worker/src/__tests__/nhl-totals-fault-harness.test.js` contains 5 stub functions with `// TODO: replace stub with real implementation after WI-0379/WI-0380/WI-0381/WI-0382/WI-0383` comments. All referenced WIs have closed, but the stubs remain. The fault harness is currently testing the stubs, not the real goalie/totals logic — failures in the actual pipeline will not be caught.

**Scope**:
- `apps/worker/src/__tests__/nhl-totals-fault-harness.test.js` — audit each of the 5 stubs; import the real function from the appropriate module; update test assertions where the real implementation differs from stub behavior

**Out of scope**:
- Changes to the real implementation modules
- New test scenarios beyond replacing existing stubs

**Acceptance**:
- All 5 stub references are replaced with imports from real modules.
- `npm --prefix apps/worker test -- nhl-totals-fault-harness` passes.
- No `// TODO: replace stub` comments remain in the file.

**Owner agent**: codex
**Time window**: 2026-03-28
**Coordination flag**: solo

**Tests to run**:
- `npm --prefix apps/worker test -- nhl-totals-fault-harness`

CLAIM: codex 2026-03-28T14:26:11Z

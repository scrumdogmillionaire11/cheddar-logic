# WI-0420: Active Tab Shows Only Active Play Calls

**ID**: WI-0420
**Goal**: Enforce definitive Active tab logic so only active play calls (`PLAY` + `LEAN`) are shown, excluding `PASS`/`NO PLAY` cards.

**Scope**:

- `WORK_QUEUE/WI-0420.md`
- `web/src/components/cards-page-client.tsx`
- `web/src/lib/game-card/filters.ts`
- `web/src/__tests__/**` (targeted Active-tab filter regression coverage)

**Out of scope**:

- `web/src/app/api/games/route.ts` backend payload filtering
- Worker-side decision generation and model logic
- UI redesign beyond existing Active/Pregame tab behavior

**Acceptance**:

- Active lifecycle view excludes cards where effective decision is `PASS`.
- Active lifecycle view excludes cards with pick `NO PLAY`.
- Active lifecycle view excludes cards with market `NONE` / non-bettable outcomes.
- Active lifecycle view includes actionable calls (`PLAY` + `LEAN`) using canonical decision semantics.
- Pregame behavior remains unchanged.
- Targeted regression tests exist for:
  - Positive case: Active shows actionable play calls.
  - Negative case: Active does not show `PASS`/`NO PLAY` cards.

**Owner agent**: codex
**Time window**: 2026-03-12T00:00:00Z -> TBD
**Coordination flag**: solo

**Tests to run**:

- `npm --prefix web run test:filters:play-first`
- `node web/src/__tests__/ui-cards-smoke.test.js`
- `npm --prefix web run test:card-decision`
- New/updated Active-tab regression test(s) added in this WI scope

**Manual validation**:

- Open cards page, switch to `Active`, verify no `NO PLAY` cards appear.
- Confirm actionable cards (`PLAY`/`LEAN`) still appear in `Active` when available.
- Switch back to `Pre-Game` and confirm existing behavior is unchanged.

CLAIM: codex 2026-03-13T02:37:34Z

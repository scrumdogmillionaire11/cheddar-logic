# WI-0313: Canonical DB Env Contract Unification

**ID**: WI-0313
**Goal**: Enforce one runtime database path contract across web, worker, scripts, tests, and docs by making `CHEDDAR_DB_PATH` canonical and removing operational reliance on legacy DB path vars.

**Scope**:

- `packages/data/src/db-path.js`
- `packages/data/src/db.js`
- `packages/data/__tests__/db-path.test.js`
- `packages/data/__tests__/db-path-production.test.js`
- `scripts/deployment-verify.js`
- `scripts/start-scheduler.sh`
- `QUICKSTART.md`
- `packages/data/README.md`
- `apps/worker/README.md`

**Out of scope**:

- Changes to card decision, scoring, or UI behavior
- Changes to DB schema/migrations
- Unrelated repo-wide cleanup or formatting

**Acceptance**:

- `resolveDatabasePath()` resolves `CHEDDAR_DB_PATH` as canonical source when present
- Production/runtime warnings and deployment checks align to `CHEDDAR_DB_PATH` contract
- Scheduler startup clears/unsets legacy DB vars to prevent precedence drift
- Scoped db-path tests pass with updated canonical expectations
- Runbooks/docs consistently instruct using only `CHEDDAR_DB_PATH`

**Owner agent**: github-copilot
**Time window**: 2026-03-05T00:00:00Z -> 2026-03-06T00:00:00Z
**Coordination flag**: solo

**Tests to run**:

- `npm --prefix packages/data test -- __tests__/db-path.test.js __tests__/db-path-production.test.js`
- `node -e 'const { resolveDatabasePath } = require("./packages/data/src/db-path"); console.log(resolveDatabasePath())'`

**Manual validation**:

- From repo root, source `.env`, run resolver probe from both root and `web/`, and confirm `source: CHEDDAR_DB_PATH`
- Start web + scheduler + one manual model job and verify all processes read/write the same DB file

CLAIM: github-copilot 2026-03-05T00:00:00Z

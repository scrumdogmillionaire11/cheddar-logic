# WI-0966: Consolidate freshness and timestamp provenance policy across ingest, scheduler, and gate

**ID**: WI-0966
**Goal**: Eliminate fragmented freshness behavior by aligning timestamp provenance resolution and freshness contracts across pull, schedule, and execution-gate layers.

**Depends on**: WI-0950, WI-0953

**Scope**:
- `WORK_QUEUE/WI-0966.md`
- `packages/data/src/db/odds.js`
- `apps/worker/src/jobs/pull_odds_hourly.js`
- `apps/worker/src/schedulers/windows.js`
- `apps/worker/src/jobs/execution-gate-freshness-contract.js`
- `apps/worker/src/jobs/__tests__/pull_odds_hourly.test.js`
- `apps/worker/src/__tests__/scheduler-main-calibration.test.js`
- `apps/worker/src/jobs/__tests__/execution-gate.test.js`

**Out of scope**:
- Quota provider changes
- Non-odds data source freshness logic
- Web-only stale tag UX renaming

**Acceptance**:
- One documented policy governs snapshot age source selection (`captured_at`/`pulled_at`/`updated_at`) and freshness evaluation tiers.
- MLB T-minus override windows and execution-gate freshness decisions remain consistent under shared contracts.
- Tests cover fallback timestamp chains and verify gate outcomes for fresh/stale/expired cases.
- Diagnostic logs clearly indicate provenance source and freshness decision path for operator debugging.

**Owner agent**: unassigned
**Time window**: 2026-04-15 -> TBD
**Coordination flag**: needs-sync

**Tests to run**:
- `npm --prefix apps/worker run test -- src/jobs/__tests__/pull_odds_hourly.test.js --runInBand`
- `npm --prefix apps/worker run test -- src/__tests__/scheduler-main-calibration.test.js --runInBand`
- `npm --prefix apps/worker run test -- src/jobs/__tests__/execution-gate.test.js --runInBand`

**Manual validation**:
- Execute pull-odds + scheduler tick + MLB model run and verify freshness decisions align with timestamp provenance diagnostics and configured override bands.

CLAIM: unassigned
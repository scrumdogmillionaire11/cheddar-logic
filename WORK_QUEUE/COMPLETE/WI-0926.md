# WI-0926: Delete orphaned job files with no scheduler, no script, no consumer

**ID**: WI-0926
**Goal**: Remove four job files that have no scheduler registration, no npm script entry, and no `require()` reference outside their own file.

**Scope**:

- `apps/worker/src/jobs/run_mispricing_scan.js`
- `apps/worker/src/jobs/run_projection_accuracy_report.js`
- `apps/worker/src/jobs/report_flip_threshold_backtest.js`
- `apps/worker/src/jobs/ingest_nst_pp_rates.js`
- `apps/worker/src/jobs/__tests__/` — delete matching test files for removed jobs if they exist

**Out of scope**:

- `packages/models/src/mispricing-scanner.js` (may have other callers — do not remove)
- Any data-package changes beyond removing dead references
- Adding npm scripts or schedulers for these jobs

**Acceptance**:

- All four job files (and any matching `__tests__` files) deleted
- `grep -rn "run_mispricing_scan\|run_projection_accuracy_report\|report_flip_threshold_backtest\|ingest_nst_pp_rates" apps/ packages/ web/` returns 0 source matches
- `npm --prefix apps/worker run test -- --no-coverage` passes

**Owner agent**: unassigned
**Time window**: TBD
**Coordination flag**: solo

**Tests to run**:

- `npm --prefix apps/worker run test -- --no-coverage`

**Manual validation**:

- `ls apps/worker/src/jobs/run_mispricing_scan.js 2>&1` returns "No such file or directory"

CLAIM: unassigned

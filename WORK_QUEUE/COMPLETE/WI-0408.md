# WI-0408: Unify Decision Status Enum Across Web Contracts

**ID**: WI-0408
**Goal**: Reduce drift between mirrored decision status unions and enums across web/API/docs by centralizing or strictly deriving them from one shared source.

**Scope**:

- `WORK_QUEUE/WI-0408.md`
- `web/src/lib/types/game-card.ts`
- `web/src/app/api/games/route.ts`
- `docs/DATA_CONTRACTS.md`
- Any directly-related web/shared type file needed to establish a single authoritative status contract

**Out of scope**:

- New decision statuses or semantics
- Worker-side pipeline redesign
- Non-status contract cleanup

**Acceptance**:

- Decision status unions are no longer manually duplicated across scoped files without a shared source.
- Current supported statuses remain unchanged.
- Docs and code match exactly after the change.
- A simple regression check or type-level safeguard exists to prevent future drift.

**Owner agent**: claude-sonnet-4-6
**Time window**: TBD -> 2026-03-13T00:00:00Z
**Coordination flag**: none

**Tests to run**:

- `npm --prefix web run test:card-decision`
- `npm --prefix web run test:transform:market`

**Manual validation**:

- Confirm API, web types, and docs all expose the same supported decision status values.

**Status**: COMPLETE (quick-31)
- route.ts Play interface now imports ExpressionStatus, CanonicalMarketType from game-card.ts and PlayDisplayAction from decision.ts
- No inline duplication of these three unions remains (except documented classification follow-up)
- tsc --noEmit passes with no new errors in route.ts
- Remaining classification mismatch ('BASE'|'LEAN'|'PASS' vs 'PLAY'|'LEAN'|'NONE') documented inline for follow-up

CLAIM: claude-sonnet-4-6 2026-03-13T00:00:00Z

# WI-0510: Upstream no-play root-cause fixes (mapping + projection inputs)

**ID**: WI-0510
**Goal**: Eliminate upstream data-path failures that degrade play/no-play outcomes by fixing team mapping misses and projection input acquisition gaps.

**Scope**:

- `WORK_QUEUE/WI-0510.md`
- `packages/data/src/normalize.js`
- `packages/data/src/odds-enrichment.js`
- `apps/worker/src/jobs/pull_odds_hourly.js`
- `apps/worker/src/jobs/refresh_stale_odds.js`
- `apps/worker/src/models/projections.js`
- `packages/data/src/__tests__/normalize.ncaam-team-variants.test.js`
- `packages/data/src/__tests__/normalize.soccer-team-variants.test.js`

**Out of scope**:

- `web/src/lib/game-card/transform.ts` fallback/noise taxonomy changes
- UI diagnostics copy/labels (`web/src/components/**`)
- DB schema migrations

**Acceptance**:

- Team variants observed in `TEAM_MAPPING_UNMAPPED` failures are resolvable through canonical alias mapping in `normalize.js`.
- Odds ingest/enrichment emits actionable mapping diagnostics when unresolved names remain, without silently dropping context.
- Projection gates in worker use robust field fallbacks for NBA/NHL/NCAAM and reduce false `PROJECTION_INPUTS_INCOMPLETE` outcomes.
- Existing strict projection gate behavior remains (true missing inputs still block).

**Owner agent**: github-copilot
**Time window**: 2026-03-19T20:50:48Z -> TBD
**Coordination flag**: solo

**Tests to run**:

- `npm --prefix packages/data test -- src/__tests__/normalize.ncaam-team-variants.test.js`
- `npm --prefix packages/data test -- src/__tests__/normalize.soccer-team-variants.test.js`
- `npm --prefix apps/worker test -- src/models/__tests__/nba-total-projection-alignment.test.js`
- `set -a; source .env; set +a; npm --prefix apps/worker run job:pull-odds`
- `set -a; source .env; set +a; npm --prefix apps/worker run job:run-nhl-model`

**Manual validation**:

- Run `bash scripts/diagnose-missing-data-no-plays.sh` and confirm reduced `TEAM_MAPPING_UNMAPPED`/projection missing-input failures for current in-window NHL/NBA/NCAAM games.
- Spot-check affected matchups in API output to confirm specific upstream failure reasons are present when unresolved.

**Guard for WI closeout**:

- Before marking complete: confirm every test command in `Tests to run` is runnable from repo root.

CLAIM: codex 2026-03-21

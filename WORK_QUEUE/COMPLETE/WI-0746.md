# WI-0746: Split Results into Betting Record and Projection Accuracy

**ID**: WI-0746
**Goal**: Split `/results` and `/api/results` so odds-backed P/L and projection-only accuracy are reported in separate metric lanes with no rollup mixing.

**Scope**:
- `WORK_QUEUE/WI-0746.md`
- `web/src/app/api/results/route.ts`
- `web/src/app/api/results/projection-metrics.ts`
- `web/src/app/results/page.tsx`
- `web/src/__tests__/api-results-decision-segmentation.test.js`
- `web/src/__tests__/api-results-projection-split.test.js`
- `web/src/__tests__/ui-results-smoke.test.js`
- `web/package.json`

**Out of scope**:
- `apps/worker/**`
- `packages/data/**`
- Changes to model settlement or CLV write paths

**Acceptance**:
- `/api/results` `summary`, `segments`, and `ledger` include `ODDS_BACKED` rows only.
- `/api/results` returns a sibling projection-only summary with MAE, bias, directional accuracy, sample size, and `actualsAvailable`.
- `/results` displays Betting Record and Projection Models (Research Only) as separate sections, and projection-only metrics never roll into P/L totals.
- Projection-only UI rows/cards render `Model Projection — No Line Applied`; if `actualsAvailable=false`, render `Awaiting settled outcome data`.

**Owner agent**: codex
**Time window**: 2026-04-03T13:26:40Z -> TBD
**Coordination flag**: needs-sync

**Tests to run**:
- `npm --prefix web run test:ui:results`
- `npm --prefix web run test:api:results:decision-segmentation`
- `npm --prefix web run test:api:results:projection-split`
- `npm --prefix web run test:api:results:flags`
- `npm --prefix web run test:api:results:sport-filter`
- `npm --prefix web run lint -- src/app/api/results/route.ts src/app/api/results/projection-metrics.ts src/app/results/page.tsx src/__tests__/ui-results-smoke.test.js src/__tests__/api-results-decision-segmentation.test.js src/__tests__/api-results-projection-split.test.js`

**Manual validation**:
- Confirm `/results` shows separate Betting Record and Projection Models (Research Only) sections and no projection-only rows appear in the betting ledger or W-L / units summary.

**Guard for WI closeout**:
- Before marking complete: confirm every test command in `Tests to run` is runnable from repo root (i.e., exists in web/package.json scripts or is a valid `node path/to/test.js` command).
- If a script is added to web/package.json as part of this WI, add it to this WI's Tests section with the full `npm --prefix web run <script>` form.

CLAIM: codex 2026-04-03T13:26:40Z

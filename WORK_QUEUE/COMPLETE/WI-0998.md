# WI-0998: Replace EDGE_VERIFICATION_REQUIRED with decision-grade blocker contract

**ID**: WI-0998

**Goal**: Replace `EDGE_VERIFICATION_REQUIRED` with explicit, bettor-legible blocker reasons across model, worker, and read-surface contracts, and enforce user-facing no-play/watch messaging that states cause, transition trigger, and current call.

## Background

Current user-facing output can include:

- `Main blockers: SUPPORT_BELOW_LEAN_THRESHOLD, EDGE_VERIFICATION_REQUIRED`

`SUPPORT_BELOW_LEAN_THRESHOLD` is concrete. `EDGE_VERIFICATION_REQUIRED` is internal process language and does not explain what is actually wrong in betting terms.

This work item implements a decision-grade blocker contract so no-play/hold/watch states report concrete reasons only:

- `LINE_NOT_CONFIRMED`
- `EDGE_RECHECK_PENDING`
- `EDGE_NO_LONGER_CONFIRMED`
- `MARKET_DATA_STALE`
- `PRICE_SYNC_PENDING`

And maps all blocker codes to short user-facing text for Discord/card output.

## Decision Contract

### Action state (separate from blocker reasons)

- `PLAY`
- `LEAN`
- `WATCH`
- `PASS`

### Blocker reasons

- `SUPPORT_BELOW_LEAN_THRESHOLD`
- `LINE_NOT_CONFIRMED`
- `EDGE_RECHECK_PENDING`
- `EDGE_NO_LONGER_CONFIRMED`
- `MARKET_DATA_STALE`
- `PRICE_SYNC_PENDING`

### Invariants

- Internal verification steps may remain in decision logic.
- User-facing output must never expose generic `EDGE_VERIFICATION_REQUIRED`.
- Every watch/pass/no-play explanation must answer:
  - what is wrong,
  - what would change the status,
  - what the current call is.
- Do not manufacture action: if thresholds are not met, `official_plays` stays empty.

## Scope

- `packages/models/src/decision-pipeline-v2.js`
- `packages/models/src/decision-pipeline-v2-edge-config.js`
- `packages/models/src/decision-policy.js`
- `packages/models/src/__tests__/decision-pipeline-v2-*.test.js`
- `packages/models/src/__tests__/decision-policy.test.js`
- `apps/worker/src/jobs/post_discord_cards.js`
- `apps/worker/src/jobs/run_nhl_model.js`
- `apps/worker/src/utils/decision-publisher.js`
- `apps/worker/src/jobs/__tests__/post_discord_cards.test.js`
- `apps/worker/src/jobs/__tests__/run_nba_model*.test.js`
- `apps/worker/src/jobs/__tests__/run_nhl_model*.test.js`
- `apps/worker/src/utils/__tests__/decision-publisher.v2.test.js`
- `web/src/lib/game-card/transform/decision-surface.ts`
- `web/src/lib/game-card/transform/index.ts`
- `web/src/lib/game-card/reason-labels.ts`
- `web/src/components/cards/game-card-helpers.tsx`
- `web/src/components/cards/CardsPageContext.tsx`
- `web/src/__tests__/game-card-pass-surface-contract.test.js`
- `web/src/__tests__/play-decision-edge-verification.test.js`
- `packages/data/src/validators/card-payload.js` (if blocker enums are validated there)
- `docs/guides/Edge_Thresholds.md` (required)
- `docs/audits/reason-code-taxonomy.md` (required)
- `docs/DATA_CONTRACTS.md` (required: vocabulary compatibility + precedence notes)
- `docs/core__watchdog_fail_safes.md` (optional follow-up if this file exists and contains overlapping language)
- `docs/core__orchestration.md` (optional follow-up if this file exists and contains overlapping language)

## Out of scope

- New threshold calibration values or market strategy changes
- Changes to settlement/grading logic
- New sports-specific model features unrelated to blocker reason clarity
- UI redesign work beyond existing card/Discord text formatting
- Repo-wide formatting or cleanup

## Acceptance

- Decision pipeline no longer emits `EDGE_VERIFICATION_REQUIRED` in canonical user-facing reason outputs.
- Verification-related blockers are emitted as concrete reason codes:
  - `LINE_NOT_CONFIRMED`
  - `EDGE_RECHECK_PENDING`
  - `EDGE_NO_LONGER_CONFIRMED`
  - `MARKET_DATA_STALE`
  - `PRICE_SYNC_PENDING`
- Formatter layer maps blocker codes to plain user language (no raw internal enum leakage in normal mode).
- WATCH/PASS messaging includes:
  - reason(s),
  - deterministic transition trigger (`Would become ...` or `Waiting on ...`),
  - explicit current call (`No bet yet` / `No action`).
- Example behavior enforced:
  - stale or unverified line -> WATCH/PASS with `Line not confirmed` or `Market data stale`
  - failed recheck -> PASS with `Edge no longer clears threshold`
  - verified edge + support meets floor -> LEAN/PLAY with no verification blocker shown
- NHL path consistency: `run_nhl_model` and decision-publisher terminal-reason-family logic do not emit `EDGE_VERIFICATION_REQUIRED` after migration.
- Cross-surface compatibility is explicit and documented:
  - old -> new reason-code mapping table is added,
  - backward compatibility behavior is defined (including any temporary aliasing period),
  - `blocking_reason_codes` determinism/uniqueness expectations remain intact.
- Required docs are updated in-place:
  - `docs/guides/Edge_Thresholds.md`
  - `docs/audits/reason-code-taxonomy.md`
  - `docs/DATA_CONTRACTS.md`

**Owner agent**: unassigned
**Time window**: TBD
**Coordination flag**: needs-sync

**Tests to run**:

- `npm --prefix packages/models test -- --runTestsByPath src/__tests__/decision-pipeline-v2-line-move.test.js`
- `npm --prefix packages/models test -- --runTestsByPath src/__tests__/decision-pipeline-v2-stale-odds.test.js`
- `npm --prefix packages/models test -- --runTestsByPath src/__tests__/decision-policy.test.js`
- `npm --prefix apps/worker test -- --runTestsByPath src/jobs/__tests__/post_discord_cards.test.js`
- `npm --prefix apps/worker test -- --runTestsByPath src/jobs/__tests__/run_nba_model.market-calls.test.js`
- `npm --prefix apps/worker test -- --runTestsByPath src/jobs/__tests__/run_nhl_model.test.js`
- `npm --prefix apps/worker test -- --runTestsByPath src/jobs/__tests__/run_nhl_model.no-bet.test.js`
- `npm --prefix apps/worker test -- --runTestsByPath src/utils/__tests__/decision-publisher.v2.test.js`
- `node web/src/__tests__/game-card-pass-surface-contract.test.js`
- `node web/src/__tests__/play-decision-edge-verification.test.js`

**Manual validation**:

- Run one NBA scenario that previously produced `EDGE_VERIFICATION_REQUIRED` and confirm output now uses concrete reason text only.
- Run one NHL mixed-book or stale block scenario and confirm output uses concrete reason text only.
- Confirm no normal-mode card/Discord output contains raw blocker enum names.
- Validate WATCH messaging includes explicit transition trigger, for example:
  - `Would become LEAN: verified line holds and support clears lean floor`
- Validate PASS messaging includes explicit no-action call, for example:
  - `No action unless verified number still clears threshold`.

## Implementation notes

1. Replace generic verification blocker emission in pipeline logic with explicit cause-specific blocker codes.
2. Add/centralize a strict blocker-to-user-text mapper in formatter output path.
3. Enforce separation between action state (`PLAY/LEAN/WATCH/PASS`) and blocker reasons.
4. Update downstream reason-family classifiers and webhook reason labels to consume the new cause-specific blocker set.
5. Add anti-leak test coverage: fail tests if `EDGE_VERIFICATION_REQUIRED` or other raw internal codes leak to user-facing text (except explicit debug mode).
6. Reuse NHL-style `Waiting on` phrasing pattern for WATCH when there is a concrete upgrade path.
7. WATCH vs PASS rule:
   - use `WATCH` only when a concrete, near-term upgrade trigger exists,
   - otherwise use `PASS` with explicit reasons.
8. Migration compatibility:
   - define canonical mapping from legacy `EDGE_VERIFICATION_REQUIRED` into the new cause-specific codes,
   - preserve deterministic precedence for `primary_reason_code` and `blocking_reason_codes`.

## Activity

- 2026-04-17: Work item created to harden verification blocker language into decision-grade user-facing contract.
- 2026-04-17: Scope and acceptance expanded to include downstream worker/web contract surfaces and compatibility docs required for safe migration.

CLAIM: unassigned

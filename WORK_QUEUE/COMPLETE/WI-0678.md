# WI-0678: Stabilize orchestrator smoke test against fpl_sage_integration module churn

**ID**: WI-0678
**Goal**: Ensure the orchestrator smoke test patches the live `FPLSageIntegration.run_full_analysis` globals used at runtime, even after other tests evict and re-import FPL Sage modules.

**Background**:
After fixing `WI-0677`, the full `cheddar-fpl-sage` suite exposed `tests/tests_new/test_orchestrator_smoke.py::test_orchestrator_smoke_pass`. The production orchestrator code is correct, but the test monkeypatches `cheddar_fpl_sage.analysis.fpl_sage_integration.collect_weekly_bundle` by module path; earlier suite activity can replace that module object, so the imported `FPLSageIntegration` class in the test may still call a different bound global and hit the network.

**Scope**:
- `cheddar-fpl-sage/tests/tests_new/test_orchestrator_smoke.py`

**Out of scope**:
- Changes to `cheddar-fpl-sage/src/cheddar_fpl_sage/analysis/fpl_sage_integration.py`
- Network collector behavior
- Non-orchestrator tests

**Acceptance**:
- The orchestrator smoke test patches the live globals used by `FPLSageIntegration.run_full_analysis`
- `cd cheddar-fpl-sage && python -m pytest tests/tests_new/test_orchestrator_smoke.py -v` exits `0`
- Full suite progresses past the orchestrator smoke failure

**Owner agent**: codex
**Time window**: 2026-03-29T00:00:00-04:00 -> TBD
**Coordination flag**: solo
**LOE**: XS

**Tests to run**:
- `cd cheddar-fpl-sage && python -m pytest tests/tests_new/test_orchestrator_smoke.py -v`
- `cd cheddar-fpl-sage && python -m pytest tests/ -v -x`

**Manual validation**:
- Confirm the smoke test uses temp bundle artifacts and does not attempt a real network call
- Confirm `LATEST.json` is still written to the temp outputs directory

**Guard for WI closeout**:
- Before marking complete: confirm both pytest commands above are runnable from repo root and that only the scoped test file changed.

CLAIM: codex 2026-03-29T00:00:00-04:00

# WI-0481: Soccer games API parity for dev and production

**ID**: WI-0481
**Goal**: Restore soccer game-line visibility parity by teaching the web read/transform path to accept current soccer market payloads while keeping soccer props hidden in production.

**Scope**:

- `WORK_QUEUE/WI-0481.md`
- `web/src/app/api/games/route.ts`
- `web/src/lib/game-card/transform.ts`
- `web/src/lib/game-card/filters.ts`
- `web/src/__tests__/game-card-transform-market-contract.test.js`
- `web/src/__tests__/api-games-soccer-market-contract.test.js`

**Out of scope**:

- Worker soccer model generation changes
- Production feature-flag flips for player props
- New tabs/routes for soccer-specific UI

**Acceptance**:

- `/api/games` accepts current soccer market payload aliases (`GAME_TOTAL`, `DOUBLE_CHANCE`) and no longer downgrades supported soccer game-line rows to legacy evidence because of outdated market normalization.
- Soccer double-chance/game-total cards can flow through the web game-card transform/filter path as playable game lines.
- Soccer props remain gated by existing player-props visibility rules, with no production expansion.

**Owner agent**: GitHub Copilot
**Time window**: 2026-03-17T00:00:00Z -> TBD
**Coordination flag**: solo

**Tests to run**:

- `npm --prefix web run test:transform:market`
- `npm --prefix web run test:decision:canonical`
- `node web/src/__tests__/api-games-soccer-market-contract.test.js`

**Manual validation**:

- Inspect `/api/games` soccer rows and confirm moneyline/game-total/double-chance cards retain playable market types in dev.
- Confirm production behavior remains game-lines-only when player props are not enabled.

**Guard for WI closeout**:

- Before marking complete: confirm every test command in `Tests to run` is runnable from repo root.
- Keep the diff limited to the scoped files above.

CLAIM: GitHub-Copilot 2026-03-17T00:00:00Z

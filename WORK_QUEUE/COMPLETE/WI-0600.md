# WI-0600: Pitcher Ks rollout docs and acceptance pack

**ID**: WI-0600
**Goal**: Finalize operator runbooks and acceptance evidence for pitcher K launch in token-constrained projection-only mode, then odds-backed mode when budget replenishes.

**Scope**:

- `docs/QUICKSTART.md`
- `docs/runbooks/pitcher-ks-rollout.md`
- `WORK_QUEUE/README.md`

**Out of scope**:

- Core model/scoring changes
- Odds parser logic changes
- UI feature additions

**Acceptance**:

- `docs/QUICKSTART.md` includes pitcher K manual commands for projection-only and odds-backed flows.
- Runbook documents mode switch protocol, required env flags, and failure-mode triage steps.
- Queue documentation marks pitcher K rollout sequence and completion gate.
- Acceptance checklist includes explicit evidence capture for: projection-only emit, odds-backed emit, and contract/test pass.

**Owner agent**: unassigned
**Time window**: 2026-03-26T00:00:00Z -> TBD
**Coordination flag**: solo

**Tests to run**:

- `npm --prefix apps/worker run test -- src/jobs/__tests__/run_mlb_model.test.js`
- `npm --prefix web run test:api:games:market`
- `npm --prefix packages/data test`

**Manual validation**:

- Execute the runbook steps end-to-end and confirm each acceptance checkpoint is reproducible from repo root.

**Guard for WI closeout**:

- Before marking complete: confirm every test command in `Tests to run` is runnable from repo root.
- Before marking complete: verify docs instruct projection-only as default while tokens are constrained.

CLAIM: unassigned

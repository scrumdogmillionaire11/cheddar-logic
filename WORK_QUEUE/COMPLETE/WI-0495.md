# WI-0495: Soccer Model Phase 5 — Prop Expansion (Shots on Target + Goalkeeper Saves)

**ID**: WI-0495
**Goal**: Add statistically grounded soccer prop modeling for shots on target and goalkeeper saves, with Tier 1 gating only when reliable market lines exist.

**Depends on**:

- `WORK_QUEUE/WI-0494.md`

**Scope**:

- `apps/worker/src/jobs/pull_soccer_player_props.js`
- `apps/worker/src/jobs/run_soccer_model.js`
- `packages/models/src/soccer-prop-projector.js` (new)
- `packages/models/src/index.js`
- `apps/worker/src/jobs/__tests__/run_soccer_model.test.js`
- `apps/worker/src/jobs/__tests__/pull_soccer_player_props.test.js` (new or expanded)
- `packages/models/src/__tests__/soccer-prop-projector.test.js` (new)
- `docs/SOCCER_MODEL_SPECIFICATION.md`
- `WORK_QUEUE/WI-0495.md`

**Out of scope**:

- Odds API adapter redesign
- New sports or NFL/MLB changes
- UI redesign for results presentation
- Betting-stake sizing system changes

**Acceptance**:

1. `player_shots_on_target` and `goalkeeper_saves` projections are generated from explicit model functions (not placeholder synthetic values).
2. Props are Tier 1 actionable only when real market line context is present; otherwise remain projection-only/informational.
3. Confidence metadata and decision-basis routing remain explicit and backward-compatible.
4. Added prop model tests cover core projection math, edge gating behavior, and missing-line fallback.

**Owner agent**: unassigned
**Time window**: TBD -> TBD
**Coordination flag**: solo

**Tests to run**:

- `npm --prefix apps/worker test -- src/jobs/__tests__/run_soccer_model.test.js`
- `npm --prefix apps/worker test -- src/jobs/__tests__/pull_soccer_player_props.test.js`
- `npm --prefix apps/worker run test:pipeline:settlement`
- `npm --prefix web run test:api:games:market`

**Manual validation**:

- Verify at least one sample shots-on-target and one goalkeeper-saves recommendation path in a controlled run.
- Confirm no actionable Tier 1 prop recommendation is emitted when market line is absent.
- Validate payload metadata includes decision basis and confidence for new prop types.

**Guard for WI closeout**:

- Before marking complete: provide before/after sample payloads showing non-placeholder projection values.
- Confirm no NFL-related files were modified.

CLAIM: unassigned

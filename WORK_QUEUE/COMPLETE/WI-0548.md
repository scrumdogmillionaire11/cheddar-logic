# WI-0548: Dev-Run Reliability Follow-up (Settlement Matching + Soccer Mapping + Pending Backlog Hygiene)

**ID**: WI-0548
**Goal**: Close the concrete reliability gaps observed in the 2026-03-22 dev run by hardening duplicate game-event matching, soccer team normalization, pending settlement backlog handling, and local Discord-post job ergonomics.

**Scope**:

- `WORK_QUEUE/WI-0548.md`
- `apps/worker/src/jobs/settle_game_results.js`
- `apps/worker/src/jobs/__tests__/settle_game_results.matching.test.js`
- `apps/worker/src/jobs/pull_odds_hourly.js`
- `apps/worker/src/jobs/__tests__/pull_odds_hourly.test.js`
- `packages/data/src/normalize.js`
- `packages/data/src/__tests__/normalize.soccer-team-variants.test.js`
- `apps/worker/src/jobs/settle_pending_cards.js`
- `apps/worker/src/jobs/__tests__/settle_pending_cards.non-actionable.test.js`
- `apps/worker/src/jobs/post_discord_cards.js`
- `apps/worker/src/jobs/__tests__/post_discord_cards.test.js`
- `docs/DATA_PIPELINE_TROUBLESHOOTING.md`

**Out of scope**:

- NHL/NBA/NCAAM model scoring changes (including calibration/sigma tuning)
- Public API contract changes outside existing worker/job output surfaces
- Full historical resettlement/backfill of all legacy pending rows

**Acceptance**:

- Settlement matching handles duplicate near-identical game rows deterministically, and no longer emits repeated `Collision: event ... refusing to reuse ...` warnings for the same matchup/time-window pattern seen in the 2026-03-22 run.
- Soccer team variants seen as unmapped in the run are normalized (at minimum: `Vancouver Whitecaps FC`, `FC Cincinnati`, `CF Montreal`, `New York City FC`, `Inter Miami CF`, `Minnesota United FC`, `Seattle Sounders FC`, `Portland Timbers`, `LA Galaxy`, `San Diego FC`, `Real Salt Lake`) and corresponding tests enforce resolution.
- Pending-settlement handling for rows with missing `market_key` is made explicit and measurable (targeted closeout/backfill behavior with reason codes), and regression tests cover the behavior.
- `job:post-discord-cards` fails/skips predictably in local/dev when production-only DB paths are unavailable, with actionable messaging and test coverage.
- A full smoke cycle (`pull-odds` -> `settle-games` -> `settle-cards`) shows improved diagnostics for these categories and documents before/after expectations in troubleshooting docs.

**Owner agent**: unassigned
**Time window**: 2026-03-22T02:00:00Z -> TBD
**Coordination flag**: needs-sync

**Tests to run**:

- `npm --prefix apps/worker test -- src/jobs/__tests__/settle_game_results.matching.test.js --no-coverage`
- `npm --prefix apps/worker test -- src/jobs/__tests__/pull_odds_hourly.test.js --no-coverage`
- `npm --prefix apps/worker test -- src/jobs/__tests__/settle_pending_cards.non-actionable.test.js --no-coverage`
- `npm --prefix apps/worker test -- src/jobs/__tests__/post_discord_cards.test.js --no-coverage`
- `npm --prefix apps/worker test -- src/jobs/__tests__/run_soccer_model.test.js --no-coverage`
- `npm --prefix apps/worker run job:pull-odds`
- `npm --prefix apps/worker run job:settle-games`
- `npm --prefix apps/worker run job:settle-cards`
- `npm --prefix apps/worker run job:report-telemetry-calibration -- --json --days 180`

**Manual validation**:

- Run one end-to-end dev cycle and confirm settlement logs do not show repeated duplicate-event collision spam for the same game signatures.
- Confirm pull-odds KPI no longer reports `TEAM_MAPPING_UNMAPPED` for the MLS teams listed in Acceptance.
- Confirm pending backlog diagnostics explicitly account for missing-market-key rows with deterministic handling and reason labels.
- Run `job:post-discord-cards` in local/dev env and verify failure/skip path is clear and intentional (no ambiguous operator workflow).

CLAIM: unassigned

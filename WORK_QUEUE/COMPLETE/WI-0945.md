# WI-0945: MLB F5 Moneyline PASS Selection Contract Guard

**ID**: WI-0945
**Goal**: Prevent MLB runner write failures caused by invalid moneyline selections (PASS) being emitted for F5 moneyline cards.

**Depends on**: none

**Scope**:

- `apps/worker/src/jobs/run_mlb_model.js`
- `apps/worker/src/jobs/__tests__/run_mlb_model.test.js`
- `WORK_QUEUE/WI-0945.md`

**Out of scope**:

- Changes to market formulas, edge thresholds, or confidence thresholds
- API/UI contract changes
- DB schema changes
- Scheduler cadence changes

**Acceptance**:

- `run_mlb_model.js` does not emit `mlb-f5-ml` cards when moneyline prediction is not `HOME` or `AWAY`.
- MLB runner no longer attempts to persist invalid moneyline selection payloads for F5 ML.
- Regression test covers PASS prediction in F5 ML projection path and asserts no invalid F5 ML write attempt occurs.
- Existing MLB runner tests continue to pass.

**Owner agent**: github-copilot
**Time window**: 2026-04-15T00:00:00Z -> TBD
**Coordination flag**: solo

**Tests to run**:

- `node --check apps/worker/src/jobs/run_mlb_model.js`
- `npm --prefix apps/worker run test -- --runInBand src/jobs/__tests__/run_mlb_model.test.js`

**Manual validation**:

- Run `npm --prefix apps/worker run job:run-mlb-model` and verify no `Invalid moneyline selection "PASS"` write errors are emitted.

CLAIM: github-copilot 2026-04-15T17:25:00Z

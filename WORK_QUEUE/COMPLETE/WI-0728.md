# WI-0728: First baseline lock-in cycle — run audit:all and lock reviewed fixtures

**ID**: WI-0728
**Goal**: Execute the first full audit run against all fixtures, manually review every result, replace all `RECOMPUTE_ON_FIRST_RUN` sentinels with real baseline values, and set `baseline_reviewed: true` on every fixture. This is the moment the audit system becomes authoritative.

**Scope**:
- `WORK_QUEUE/WI-0728.md`
- `apps/worker/src/audit/fixtures/**/*.json` — all fixture JSONs
- `apps/worker/audit-output/**` — audit run outputs directory

**Out of scope**:
- Fixture schema changes or new fixture authoring
- CI gate config changes (covered in the audit stack WIs)
- Any new card family fixtures (no new families until first review cycle completes)
- DB migrations or model code changes

**Acceptance**:
- `npm run audit:all` exits 0 with no `RECOMPUTE_ON_FIRST_RUN` sentinel remaining in any fixture baseline file.
- Every fixture JSON has `"baseline_reviewed": true`.
- Any expected deviation from the computed baseline is documented with a `_baseline_change_note` field in the fixture.
- Audit outputs directory contains the run artifacts from at least one full `audit:all` pass.
- No fixture baseline was approved without a human-readable note where values differ from prior expectation.

**Owner agent**: codex
CLAIM: codex 2026-04-02T16:50:32Z
**Time window**: 2026-04-02 → TBD
**Coordination flag**: needs-sync (touches shared fixture + baseline corpus)

**Tests to run**:
- `npm run audit:all`
- `npm run audit:scorecard`
- Intentional fixture corruption smoke: corrupt one fixture value, confirm `audit:all` exits non-zero and reports the breach before restoring.

**Manual validation**:
- `grep -r "RECOMPUTE_ON_FIRST_RUN" apps/worker/src/audit/fixtures/` returns no results.
- `grep -r '"baseline_reviewed": false' apps/worker/src/audit/fixtures/` returns no results.
- Review the scorecard output and confirm every card family appears with a result row (no silent skips).

**Failure modes to guard**:
- Do not approve a baseline by deleting the old value without reading the diff — every changed baseline needs a `_baseline_change_note`.
- Do not mark `baseline_reviewed: true` in bulk via script without individual review.

## Completion Note
- Verified all 18 fixture JSONs under `apps/worker/src/audit/fixtures/` have `"baseline_reviewed": true`.
- Verified no fixture JSON under `apps/worker/src/audit/fixtures/` contains `RECOMPUTE_ON_FIRST_RUN`.
- Ran `npm --prefix apps/worker run audit:all` and got `Audit summary: 18/18 passed, 0 failed, 0 gate failures`.
- Ran `npm --prefix apps/worker run audit:scorecard`; latest output includes all card families and was written under `apps/worker/audit-output/manual-2026-04-03T01-50-24-416Z/`.
- Ran corruption smoke by temporarily changing `nhl_1p_total_projection_01.json` `card_mode` to an invalid value; `audit:all` exited 1 with `FIXTURE_LOAD_FAILURE`, then fixture was restored and `audit:all` returned to 18/18 passing.

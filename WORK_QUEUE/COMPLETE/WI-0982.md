# WI-0982: MLB full-game total/ML settlement display-log eligibility

**ID**: WI-0982
**Goal**: Ensure MLB full-game total and moneyline LEAN/PLAY rows are display-log eligible, settle with explicit telemetry, and legacy non-actionable rows close safely.

**Scope**:
- `WORK_QUEUE/WI-0982.md`
- `packages/data/src/db/cards.js`
- `packages/data/__tests__/card-display-log-play-selection.test.js`
- `apps/worker/src/jobs/settle_pending_cards.js`
- `apps/worker/src/jobs/__tests__/settle_pending_cards.market-contract.test.js`
- `apps/worker/src/__tests__/settlement-pipeline-integration.test.js`
- `apps/worker/src/jobs/repair_mlb_full_game_display_log.js`

**Out of scope**:
- Branch creation or branch switching
- Repo-wide formatting, unrelated queue files, POTD files, lockfiles, migrations, and web/API routes
- Non-MLB settlement behavior beyond shared helper semantics needed for strict legacy fallback

**Acceptance**:
- Display-log eligibility uses strict legacy fallback only when `decision_v2.official_status` is absent: `PLAY`/`FIRE` -> `PLAY`, `LEAN` -> `LEAN`, and `PASS`/`WATCH`/`HOLD`/missing/unknown are non-actionable.
- Existing MLB full-game total and full-game moneyline pending rows with final results can be repaired into `card_display_log` when they are legacy `LEAN` rows with `execution_status=EXECUTABLE`, with dry-run as the default.
- `settle_pending_cards` reports MLB full-game `TOTAL` and `MONEYLINE` telemetry buckets.
- Final legacy `WATCH`/`HOLD` MLB full-game rows are auto-closed as non-actionable voids when appropriate.
- Targeted regression tests cover strict fallback, MLB telemetry buckets, and MLB WATCH/HOLD auto-close behavior.

**Owner agent**: codex
**Time window**: 2026-04-17T11:48:38Z -> TBD
**Coordination flag**: solo

**Tests to run**:
- `npm --prefix packages/data test -- card-display-log-play-selection.test.js`
- `npm --prefix apps/worker test -- settle_pending_cards.market-contract.test.js`
- `npm --prefix apps/worker run test:pipeline:settlement`

**Manual validation**:
- Confirm changed files match WI-0982 scope and no unrelated dirty files were touched.

CLAIM: codex 2026-04-17T11:48:38Z

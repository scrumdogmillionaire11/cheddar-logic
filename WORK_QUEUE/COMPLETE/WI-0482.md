# WI-0482: Phase 3 Activation + Settlement-Safe Rollback (CLV)

**ID**: WI-0482
**Goal**: Execute and validate controlled `ENABLE_CLV_LEDGER` activation, including settlement-safe verification and rollback.

**Depends on**:

- `WORK_QUEUE/WI-0481.md`

**Scope**:

- `WORK_QUEUE/WI-0482.md`
- `docs/DATA_PIPELINE_TROUBLESHOOTING.md`
- `env.example`

**Out of scope**:

- CLV schema redesign
- New settlement logic implementation
- Market-threshold rollout work

**Acceptance**:

1. Troubleshooting runbook includes explicit Phase 3 activation, post-settlement verification, and rollback steps.
2. `env.example` includes operator-safe guidance for `ENABLE_CLV_LEDGER` (default-safe, no forced production-on).
3. Validation procedure confirms CLV rows are additive and exclude projection-only cards.

**Owner agent**: github-copilot
**Time window**: 2026-03-17T00:00:00Z -> 2026-03-17T23:59:59Z
**Coordination flag**: needs-sync

**Tests to run**:

- `ENABLE_CLV_LEDGER=true npm --prefix apps/worker run job:settle-cards`
- `npm --prefix apps/worker test -- src/jobs/__tests__/settle_pending_cards.phase2.test.js`
- `npm --prefix apps/worker test -- src/jobs/__tests__/settle_pending_cards.market-contract.test.js`
- `npm --prefix web run test:api:games:market`

**Manual validation**:

- Enable CLV flag for one controlled run and verify inclusion/exclusion SQL criteria.
- Disable CLV flag and confirm no further ledger writes.
- Confirm settlement outputs remain unchanged except additive CLV telemetry.

**Guard for WI closeout**:

- Before marking complete: confirm every test command in `Tests to run` is runnable from repo root.
- Include SQL evidence + rollback transcript references in WI notes.

## Execution Notes (2026-03-17)

- Updated `docs/DATA_PIPELINE_TROUBLESHOOTING.md` with explicit Phase 3 runbook:
  - Controlled activation for `ENABLE_CLV_LEDGER=true`
  - Post-settlement verification commands
  - Settlement-safe rollback sequence (`ENABLE_CLV_LEDGER=false`)
- Updated `env.example` with operator-safe default-off guidance:
  - `ENABLE_CLV_LEDGER=false`

## Closeout Checklist

- [x] Acceptance 1: explicit Phase 3 activation/post-settlement verification/rollback documented.
- [x] Acceptance 2: `env.example` includes default-safe `ENABLE_CLV_LEDGER` guidance.
- [x] Acceptance 3: additive CLV validation procedure documented with projection-only exclusion check.
- [x] Tests to run executed and recorded.
- [x] Manual validation executed and recorded.

## Verification Evidence

- Automated tests/commands run from repo root on 2026-03-17:
  - `ENABLE_CLV_LEDGER=true npm --prefix apps/worker run job:settle-cards` ✅
  - `npm --prefix apps/worker test -- src/jobs/__tests__/settle_pending_cards.phase2.test.js` ✅ (5/5)
  - `npm --prefix apps/worker test -- src/jobs/__tests__/settle_pending_cards.market-contract.test.js` ✅ (4/4)
  - `npm --prefix web run test:api:games:market` ✅
- Manual validation SQL evidence:
  - `CLV_BEFORE=1`
  - `CLV_PROJECTION_ONLY=0`
  - `CLV_AFTER=1`
  - `CLV_OFF_NO_NEW_ROWS=YES`

## Rollback Transcript References

- Controlled rollback verification command:
  - `ENABLE_CLV_LEDGER=false npm --prefix apps/worker run job:settle-cards`

## Status

- Ready to move to COMPLETE.

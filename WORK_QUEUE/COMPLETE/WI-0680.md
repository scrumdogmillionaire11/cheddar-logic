# WI-0680: Stabilize user endpoint tests against backend router module churn

**ID**: WI-0680
**Goal**: Ensure user endpoint tests monkeypatch the live route globals used by the FastAPI app, even after other tests evict and re-import backend router modules.

**Background**:
After fixing `WI-0679`, the full `cheddar-fpl-sage` suite exposed `tests/tests_new/test_user_endpoints.py::test_user_analyses_endpoint_forwards_query_params`. The route itself returns `200`, but the imported `backend.routers.user` module reference in the test is stale, so monkeypatching `user_router.engine_service` no longer affects the live route mounted on `client.app`.

**Scope**:
- `cheddar-fpl-sage/tests/tests_new/test_user_endpoints.py`

**Out of scope**:
- Changes to `backend/routers/user.py`
- Changes to engine service behavior
- Non-user endpoint tests

**Acceptance**:
- User endpoint tests patch the live route globals used at request time
- `cd cheddar-fpl-sage && python -m pytest tests/tests_new/test_user_endpoints.py -v` exits `0`
- Full suite progresses past the user endpoint monkeypatch failure

**Owner agent**: codex
**Time window**: 2026-03-29T00:00:00-04:00 -> TBD
**Coordination flag**: solo
**LOE**: XS

**Tests to run**:
- `cd cheddar-fpl-sage && python -m pytest tests/tests_new/test_user_endpoints.py -v`
- `cd cheddar-fpl-sage && python -m pytest tests/ -v -x`

**Manual validation**:
- Confirm patched user analysis history route receives forwarded query params
- Confirm patched user performance route receives `include_details=True`

**Guard for WI closeout**:
- Before marking complete: confirm both pytest commands above are runnable from repo root and that only the scoped test file changed.

CLAIM: codex 2026-03-29T00:00:00-04:00

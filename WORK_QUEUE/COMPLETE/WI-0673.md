# WI-0673: Exclude vendored virtualenv from legacy namespace guard test

**ID**: WI-0673
**Goal**: Restrict the legacy-namespace guard test to project-owned Python files so it does not fail on third-party imports inside the checked-in virtualenv.

**Background**:
After fixing `WI-0672`, the full `cheddar-fpl-sage` suite exposed `tests/test_no_legacy_namespaces.py::test_no_legacy_namespace_imports`. The test currently scans every `*.py` file under the repo root, including `venv/lib/python3.12/site-packages`, and flags unrelated third-party strings like `import utils`.

**Scope**:
- `cheddar-fpl-sage/tests/test_no_legacy_namespaces.py`

**Out of scope**:
- Changes to application imports
- Changes to vendored site-packages
- Any non-guard tests

**Acceptance**:
- The legacy namespace guard ignores `venv/` and other non-project Python trees
- `cd cheddar-fpl-sage && python -m pytest tests/test_no_legacy_namespaces.py -v` exits `0`
- Full suite progresses past the third-party false positives

**Owner agent**: codex
**Time window**: 2026-03-29T00:00:00-04:00 -> TBD
**Coordination flag**: solo
**LOE**: XS

**Tests to run**:
- `cd cheddar-fpl-sage && python -m pytest tests/test_no_legacy_namespaces.py -v`
- `cd cheddar-fpl-sage && python -m pytest tests/ -v -x`

**Manual validation**:
- Confirm the guard still scans project Python files while ignoring `venv/`
- Confirm no project-owned offenders are masked by the exclusion list

**Guard for WI closeout**:
- Before marking complete: confirm both pytest commands above are runnable from repo root and that only the scoped test file changed.

CLAIM: codex 2026-03-29T00:00:00-04:00

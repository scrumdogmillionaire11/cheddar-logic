# WI-0736: Fail-closed NHL 1P odds fetch job to stop per-event token burn

**ID**: WI-0736
**Goal**: Prevent accidental production token depletion from direct NHL 1P totals odds fetches by making `pull_nhl_1p_odds` skip unless explicitly enabled.

**Scope**:
- `WORK_QUEUE/WI-0736.md`
- `apps/worker/src/jobs/pull_nhl_1p_odds.js`
- `apps/worker/src/jobs/__tests__/pull_nhl_1p_odds.test.js`

**Out of scope**:
- Scheduler cadence changes
- NHL model math or 1P card rendering changes
- Any repo-wide cleanup or unrelated docs edits

**Acceptance**:
- `pullNhl1pOdds()` skips when `NHL_1P_ODDS_ENABLED` is unset or not exactly `"true"`.
- Explicit `NHL_1P_ODDS_ENABLED=true` preserves operator opt-in behavior.
- A regression test proves the job makes no fetch calls and opens no DB job run by default.

**Owner agent**: codex
CLAIM: codex 2026-04-02T15:30:00-04:00
**Time window**: 2026-04-02 -> 2026-04-02
**Coordination flag**: no-sync

**Tests to run**:
- `npm --prefix apps/worker test -- --runInBand --testPathPattern=pull_nhl_1p_odds.test.js`

**Manual validation**:
- Run a direct `pullNhl1pOdds({ dryRun: true })` call with `NHL_1P_ODDS_ENABLED` unset and confirm it returns `{ success: true, skipped: true, reason: 'not_enabled' }` without a fetch.

## Completion Note
- Implemented fail-close guard: `pullNhl1pOdds()` now skips unless `NHL_1P_ODDS_ENABLED === "true"`.
- Added regression coverage for the default skip path and explicit opt-in dry-run path.
- Ran `npm --prefix apps/worker test -- --runInBand --testPathPattern=pull_nhl_1p_odds.test.js` (pass).
- Ran direct manual validation with `NHL_1P_ODDS_ENABLED` unset and confirmed `{ success: true, skipped: true, reason: 'not_enabled' }`.

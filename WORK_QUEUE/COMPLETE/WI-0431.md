# WI-0431: Exclude finalized game_results from /api/games active lifecycle

**ID**: WI-0431
**Goal**: Prevent `/cards?lifecycle=active` from showing games already finalized in `game_results` when `games.status` is stale (e.g., still `scheduled`).

**Scope**:

- `WORK_QUEUE/WI-0431.md`
- `WORK_QUEUE/COMPLETE/WI-0431.md`
- `web/src/app/api/games/route.ts`
- `web/src/__tests__/api-games-filter.test.js`

**Out of scope**:

- Settlement job matching logic
- `/api/results` rendering
- Card settlement status transitions

**Acceptance**:

- Active lifecycle excludes games where `game_results.status` is final/ft/completed even if `games.status` remains non-final.
- Existing lifecycle tests continue to pass.
- New test case covers stale `games.status` + final `game_results` exclusion.

**Owner agent**: codex
**Time window**: 2026-03-13T12:50:00Z -> TBD
**Coordination flag**: needs-sync

**Tests to run**:

- `node web/src/__tests__/api-games-filter.test.js`

**Manual validation**:

- Confirm `/cards?lifecycle=active` no longer shows Southern/Arkansas-Pine Bluff and SDSU/Colorado State once `game_results` are final.

CLAIM: codex 2026-03-13T12:50:00Z

## Execution notes

- Added `FINAL_GAME_RESULT_STATUSES` contract in `/api/games` route.
- Active lifecycle SQL now excludes games with final `game_results` rows even when `games.status` remains stale (`scheduled`).
- Extended lifecycle tests to cover this stale-status case and assert active-mode exclusion.

## Validation evidence

- `npm --prefix web run test:games-filter` (pass)
- SQL verification against dev DB examples:
  - `ad2b858e97f8bb7cd6ca076761ab817f` (Southern vs Arkansas-Pine Bluff)
  - `ffb028d8786c480ecc9e7f0cb4821d66` (San Diego St vs Colorado St)
  - both excluded by active-mode query once `game_results.status='final'`.

# WI-0429: Enforce display-log-only settlement visibility contract

**ID**: WI-0429
**Goal**: Ensure settlement/rendering stays strictly tied to plays actually displayed on `/cards`, preventing payload-only backfill rows from appearing as settled results.

**Scope**:

- `WORK_QUEUE/WI-0429.md`
- `WORK_QUEUE/COMPLETE/WI-0429.md`
- `apps/worker/src/jobs/settle_pending_cards.js`
- `apps/worker/src/schedulers/main.js`
- `apps/worker/src/__tests__/settle-coverage.test.js`
- `env.example`

**Out of scope**:

- Game-result provider matching heuristics
- Changes to `/api/results` SQL shape (already display-log joined)
- Historical DB cleanup/backfills

**Acceptance**:

- Hourly and nightly scheduler settlement remain strict display-log mode by default.
- Payload-based display-log backfill cannot activate unless explicitly requested and globally enabled.
- Existing settlement coverage tests validate strict default and explicit gated override behavior.

**Owner agent**: codex
**Time window**: 2026-03-13T12:25:00Z -> TBD
**Coordination flag**: solo

**Tests to run**:

- `node --check apps/worker/src/jobs/settle_pending_cards.js`
- `node --check apps/worker/src/schedulers/main.js`
- `npm --prefix apps/worker run test -- src/__tests__/settle-coverage.test.js`

**Manual validation**:

- Confirm scheduler startup logs show both hourly and nightly display backfill toggles as false by default.
- Confirm settlement without explicit backfill gate does not settle pending final rows missing display-log.

CLAIM: codex 2026-03-13T12:25:00Z

## Execution notes

- Updated scheduler default to strict display-log mode for nightly settlement (`SETTLEMENT_NIGHTLY_ENABLE_DISPLAY_BACKFILL` now default false).
- Hardened `settle_pending_cards` backfill gate so payload backfill only runs when BOTH are true:
  - runtime request `allowDisplayBackfill: true`
  - env safety gate `CHEDDAR_SETTLEMENT_ENABLE_DISPLAY_BACKFILL=true`
- Updated env contract in `env.example` to document strict defaults and dual-gate backfill behavior.
- Updated settlement coverage tests:
  - strict path now proves `allowDisplayBackfill` alone does not bypass strict mode when global gate is off.
  - explicit gated override path still works and settles missing-display rows only when global gate is on.

## Validation evidence

- `node --check apps/worker/src/jobs/settle_pending_cards.js` (pass)
- `node --check apps/worker/src/schedulers/main.js` (pass)
- `npm --prefix apps/worker run test -- src/__tests__/settle-coverage.test.js` (pass)

# WI-0931: NHL wrapper edge tier differentiation + truthful surface labels

**ID**: WI-0931
**Goal**: Restore wrapper-level conviction separation so high-signal NHL totals are not flattened into one "slight edge" bucket and user-facing labels reflect true strength.

**Depends on**: none

**Scope**:

- `apps/worker/src/models/cross-market.js`
- `apps/worker/src/models/__tests__/cross-market.test.js`
- `apps/worker/src/jobs/post_discord_cards.js`
- `apps/worker/src/jobs/__tests__/post_discord_cards.test.js`
- `apps/worker/src/jobs/run_nhl_model.js`
- `apps/worker/src/utils/decision-publisher.js`
- `apps/worker/src/jobs/__tests__/run_nhl_model.test.js`
- `docs/audits/human-truthfulness-audit.md`

**Out of scope**:

- Core NHL projection math changes in `apps/worker/src/models/nhl-pace-model.js`
- Odds ingestion pipeline freshness logic
- Goalie/injury HOLD gating behavior

Scope expansion note (2026-04-13): include NHL 1P payload-contract hardening to separate projection truth from execution availability. This is a wrapper semantics fix; no projection-formula reweighting.

**Acceptance**:

- Wrapper emits a deterministic conviction split for NHL totals so deltas around +0.5, +1.0, and +1.5+ do not collapse into one presentation bucket.
- Discord output no longer labels clear high-conviction spots as "Slight Edge" when they satisfy stronger wrapper criteria.
- Truthfulness audit doc maps each label to explicit machine conditions and reason-code paths.
- Regression tests prove low/mid/high edge fixtures map to distinct output labels.

**Owner agent**: pax-executor
**Time window**: 2026-04-13T00:00:00Z -> TBD
**Coordination flag**: solo

**Tests to run**:

- `npm --prefix apps/worker run test -- --runInBand src/models/__tests__/cross-market.test.js`
- `npm --prefix apps/worker run test -- --runInBand src/jobs/__tests__/post_discord_cards.test.js`

**Manual validation**:

- Run `npm --prefix apps/worker run job:run-nhl-model` with representative games and confirm wrapper labels separate weak, play-grade, and strong-play NHL totals in Discord snapshot output.

CLAIM: pax-executor 2026-04-13T00:00:00Z

# WI-0522: Add Asian Handicap pricing model (de-vig + margin probabilities + EV)

**ID**: WI-0522
**Goal**: Add AH pricing that removes vig, models goal-difference probabilities, and computes `P(win/push/loss)` and EV (including quarter-line split EV).

**Depends on**:

- `WORK_QUEUE/WI-0521.md`

**Scope**:

- `WORK_QUEUE/WI-0522.md`
- `apps/worker/src/models/soccer/asian-handicap-pricing.js` (new)
- `apps/worker/src/models/soccer/goal-distribution.js` (new)
- `apps/worker/src/models/soccer/__tests__/asian-handicap-pricing.test.js` (new)
- `apps/worker/src/jobs/run_soccer_model.js`

**Out of scope**:

- Replacing existing soccer ML/total/double chance pricing paths
- Dixon-Coles production rollout (optional extension only)
- API/web route contract changes

**Acceptance**:

- Odds normalization removes vig and yields non-negative implied probabilities summing to approximately 1.
- Goal distribution model (minimum Poisson with `λ_home`, `λ_away`) produces `P(goal_diff = k)` over bounded support with normalization checks.
- For any AH line, model computes `P_win`, `P_push`, `P_loss`; quarter lines additionally expose `P_full_win`, `P_half_win`, `P_half_loss`, `P_full_loss`.
- EV uses explicit formulas for standard and quarter lines; fair line output is generated for downstream card rendering.

**Owner agent**: github-copilot
**Time window**: 2026-03-22T16:00:00Z -> 2026-03-22T20:00:00Z
**Coordination flag**: solo

**Tests to run**:

- `npm --prefix apps/worker test -- src/models/soccer/__tests__/asian-handicap-pricing.test.js`
- `npm --prefix apps/worker test -- src/jobs/__tests__/run_soccer_model.test.js`

**Manual validation**:

- Compare model outputs for 3-5 fixture snapshots and verify probability mass and EV signs remain stable under small odds perturbations.

**Guard for WI closeout**:

- Raw market odds must never be consumed as "true" probability without vig removal.

CLAIM: github-copilot 2026-03-22T16:00:00Z

<!-- GOVERNANCE NOTE (2026-03-21): Timestamp discrepancy — Time window and CLAIM reference 2026-03-22 but this file was found in COMPLETE/ as of 2026-03-21. Either the WI was pre-completed ahead of schedule or timestamps were set speculatively. No retroactive timestamp edits made; note recorded for audit trail. -->

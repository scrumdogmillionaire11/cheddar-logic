# WI-0544: NHL ML Calibration Evidence Completion

**ID**: WI-0544
**Goal**: Complete `WI-0538` by adding reproducible historical calibration evidence (Brier/log-loss/reliability) and explicit parameter-selection rationale.

**Scope**:

- `WORK_QUEUE/WI-0544.md`
- `apps/worker/src/jobs/report_telemetry_calibration.js`
- `apps/worker/src/jobs/__tests__/report_telemetry_calibration.test.js`
- `apps/worker/src/models/cross-market.js` (only if parameterization changes)
- `apps/worker/src/models/__tests__/cross-market.test.js`
- `docs/SETTLEMENT_AUDIT.md`

**Out of scope**:

- Decision-gate threshold tuning
- Non-NHL markets

**Acceptance**:

- NHL ML calibration section is emitted by telemetry report with Brier/log-loss/reliability bins.
- Report shows baseline vs selected mapping and documents sample window.
- Selected sigma/mapping is justified by measured metrics, not comments alone.
- Regression tests assert report section presence and stable schema.

**Owner agent**: pax-executor
**Time window**: 2026-03-21T18:00:00Z -> TBD
**Coordination flag**: solo

**Tests to run**:

- `npm --prefix apps/worker test -- src/models/__tests__/cross-market.test.js --no-coverage`
- `npm --prefix apps/worker test -- src/jobs/__tests__/report_telemetry_calibration.test.js --no-coverage`
- `npm --prefix apps/worker run job:report-telemetry-calibration -- --json --days 180`

**Manual validation**:

- Compare output metrics baseline vs selected mapping on a fixed window and confirm chosen mapping is explicitly marked in report/docs.

CLAIM: pax-executor 2026-03-22T01:36:18Z

## Closeout

**Completed**: 2026-03-21
**Closed by**: codex
**Closeout timestamp (UTC)**: 2026-03-22T02:10:40Z

**Validation evidence**:

- `npm --prefix apps/worker test -- src/models/__tests__/cross-market.test.js --no-coverage`
  - PASS (1 suite, 30 tests)
- `npm --prefix apps/worker test -- src/jobs/__tests__/report_telemetry_calibration.test.js --no-coverage`
  - PASS (1 suite, 5 tests)
- `npm --prefix apps/worker run job:report-telemetry-calibration -- --json --days 180`
  - PASS (exit 0)
  - Output includes top-level `nhlMoneylineCalibration` section with expected fields (`status`, `sampleWindow`, `sampleSize`, `selectionRule`, `verdict`, baseline/selected mappings, reliability bins, deltas).

**Acceptance checklist**:

- ✅ NHL ML calibration section emitted by telemetry report with Brier/log-loss/reliability bins.
- ✅ Report shows baseline vs selected mapping and sample window metadata.
- ✅ Selected mapping justification is explicit and metric-driven (`selectionRule` + `verdict`).
- ✅ Regression tests assert section presence and stable schema.

# WI-0801: Extract isFixedDue() to shared scheduler util — eliminate copy-paste sync risk

**ID**: WI-0801
**Goal**: Move the `isFixedDue()` and `isTminusDue()` helper functions from `schedulers/main.js` into a new `schedulers/utils.js` module and import them in both `main.js` and `player-props.js`, removing the manually-synced duplicate.

**Scope**:

- `apps/worker/src/schedulers/utils.js` (create)
- `apps/worker/src/schedulers/main.js` (remove local defs, import from utils)
- `apps/worker/src/schedulers/player-props.js` (remove copy-paste defs, import from utils)
- `apps/worker/src/schedulers/__tests__/` (update or add utils tests)

**Out of scope**:

- `schedulers/fpl.js` — `isFplWindowDue` is FPL-specific; do not touch
- Any changes to the actual window timing logic
- Job registration or cadence changes

**Acceptance**:

- `schedulers/utils.js` exports `isFixedDue(nowEt, hhmm)` and any other helpers shared between `main.js` and `player-props.js`
- The comment "Cannot require from main.js (circular). Body must be kept in sync manually." is removed from `player-props.js`
- No circular dependency introduced
- All existing scheduler tests pass unchanged

**Owner agent**: unassigned
**Time window**: TBD
**Coordination flag**: solo

**Tests to run**:

- `npm --prefix apps/worker run test -- --testPathPattern scheduler-windows`
- `npm --prefix apps/worker run test -- --testPathPattern player-props`

**Manual validation**:

- `node -e "require('./apps/worker/src/schedulers/utils.js')"` exits 0 with no errors

**Guard for WI closeout**:

- No `isFixedDue` definition in `player-props.js` — only an import
- The "keep in sync manually" comment is gone
- All scheduler tests green

CLAIM: unassigned

# WI-0798: Fix NHL_CURRENT_SEASON stale default (20242025 → computed 20252026)

**ID**: WI-0798
**Goal**: Replace the hardcoded `'20242025'` fallback in `ingest_nst_blk_rates.js` and `pull_nhl_player_shots.js` with a computed current-season key (same logic as `pull_moneypuck_blk_rates.js`), and set `NHL_CURRENT_SEASON=20252026` in `.env.production`.

**Scope**:

- `apps/worker/src/jobs/ingest_nst_blk_rates.js` (line 112)
- `apps/worker/src/jobs/pull_nhl_player_shots.js` (lines 396, 412)
- `.env.production` (add `NHL_CURRENT_SEASON=20252026`)

**Out of scope**:

- Player BLK rate model logic
- Season key format changes
- `pull_moneypuck_blk_rates.js` (already correct — do not change)

**Acceptance**:

- Both files derive the NHL season key from the current calendar date (Oct–Sep split: if month >= 10, season = `${year}${year+1}` else `${year-1}${year}`) when `NHL_CURRENT_SEASON` env var is not set
- `.env.production` explicitly sets `NHL_CURRENT_SEASON=20252026`
- Running `ingest_nst_blk_rates.js` today writes rows with `season='20252026'`, not `'20242025'`
- Running `pull_nhl_player_shots.js` today resolves `currentSeason` to `'20252026'`

**Owner agent**: unassigned
**Time window**: TBD
**Coordination flag**: solo

**Tests to run**:

- `npm --prefix apps/worker run test -- --testPathPattern ingest_nst_blk_rates`
- `npm --prefix apps/worker run test -- --testPathPattern pull_nhl_player_shots`
- `npm --prefix apps/worker run test -- --testPathPattern run-nhl-player-shots-model`

**Manual validation**:

- Run `node apps/worker/src/jobs/ingest_nst_blk_rates.js` locally and confirm log output includes `season=20252026`

**Guard for WI closeout**:

- No hardcoded `'20242025'` string remains in either file
- `.env.production` contains `NHL_CURRENT_SEASON=20252026`

CLAIM: unassigned

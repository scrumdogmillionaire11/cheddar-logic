# WI-0444: Production Main DB Corruption Triage

**ID**: WI-0444
**Goal**: Investigate and recover from `database disk image is malformed` on the production main SQLite DB (`CHEDDAR_DB_PATH`) using a containment-first runbook.

**Scope**:

- `WORK_QUEUE/WI-0444.md`
- `docs/ops-runbook.md` (only if recovery/guardrail steps require clarification)
- `docs/QUICKSTART.md` (only if troubleshooting commands need alignment)
- `scripts/db-context.sh` (only if path/health diagnostics need a minimal fix)
- `scripts/manage-scheduler.sh` (only if DB-path/status diagnostics need a minimal fix)

**Out of scope**:

- Model logic, API behavior, or card-generation algorithm changes
- Broad refactors unrelated to DB corruption triage/recovery
- Enabling `CHEDDAR_DB_ALLOW_MULTI_PROCESS=true` in production

**Acceptance**:

- Active production DB path and owner process are identified and documented.
- Corrupt DB is preserved via timestamped backup before repair/restore attempts.
- Integrity checks are run and recovery path is chosen (restore from backup or controlled rebuild).
- Service restart order follows single-writer contract and verifies healthy reads/writes.
- Any doc/script changes are minimal, scoped, and directly tied to this incident workflow.

**Owner agent**: unassigned
**Time window**: 2026-03-13T00:00:00Z -> TBD
**Coordination flag**: solo

**Tests to run**:

- `bash scripts/db-context.sh`
- `./scripts/manage-scheduler.sh db`
- `sqlite3 "$CHEDDAR_DB_PATH" "PRAGMA integrity_check;"`

**Manual validation**:

- Confirm scheduler is stopped before backup/repair activity.
- Confirm restored/rebuilt DB returns expected table counts (`card_payloads`, `odds_snapshots`, `game_results`, `card_results`).
- Confirm worker starts cleanly and web reads data without DB-load warnings.

# WI-0519: Enable NHL player shots settling in production

**ID**: WI-0519
**Goal**: Remove the `ENABLE_PLAYER_SHOTS_SETTLING` feature flag guard so NHL player shots prop cards always route to the `nhl_player_shots_props` results segment. Add the env var to `.env.production` for documentation and update related docs/tests.

**Depends on**:

- `WORK_QUEUE/COMPLETE/WI-0506.md`

**Scope**:

- `WORK_QUEUE/WI-0519.md`
- `web/src/app/api/results/route.ts`
- `.env.production`
- `docs/QUICKSTART.md`

**Out of scope**:

- Worker-side settling logic (already active; `isNhlShotsOnGoalCard` runs without any flag)
- New settlement test cases beyond confirming existing tests pass
- Non-NHL sports

**Acceptance**:

- `deriveSettlementSegment` routes NHL player shots cards to `nhl_player_shots_props` without consulting an env var.
- `isPlayerShotsSettlingEnabled` function is removed from `route.ts`.
- `.env.production` documents `ENABLE_PLAYER_SHOTS_SETTLING=true` (kept as a legacy note or removed).
- QUICKSTART.md no longer describes player shots as gated/off-by-default.
- `npm --prefix web run test:api:results:flags` passes (requires running server).

**Owner agent**: github-copilot
**Time window**: 2026-03-20T00:00:00Z -> TBD
**Coordination flag**: solo

**Tests to run**:

- `npm --prefix web run test:api:results:flags` (requires localhost:3000)

**Manual validation**:

- Confirm `GET /api/results?sport=NHL` response includes `nhl_player_shots_props` as an active segment.

**Guard for WI closeout**:

- Do not add game-result writes; web server is read-only per ADR-0002.

CLAIM: github-copilot 2026-03-20T00:00:00Z

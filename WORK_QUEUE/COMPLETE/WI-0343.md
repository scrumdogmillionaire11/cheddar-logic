# WI-0343: Stabilize current run selection for games API

**ID**: WI-0343
**Goal**: Ensure `/api/games` returns a current run id even when run_state is unavailable.

**Scope**:

- `web/src/app/api/games/route.ts`
- `WORK_QUEUE/WI-0343.md`

**Out of scope**:

- `packages/data/src/db-path.js`
- `web/src/components/cards-page-client.tsx`
- `apps/worker/**`

**Acceptance**:

- `/api/games` derives a fallback run id from recent `card_payloads` when `run_state` yields none.
- `meta.current_run_id` is populated when recent cards exist.

**Owner agent**: GitHub Copilot
**Time window**: 2026-03-08T00:00:00Z -> TBD
**Coordination flag**: solo

**Tests to run**:

- None

**Manual validation**:

- Call `/api/games` and confirm `meta.current_run_id` is set when `card_payloads` exist.

CLAIM: GitHub Copilot 2026-03-08T17:20:00Z

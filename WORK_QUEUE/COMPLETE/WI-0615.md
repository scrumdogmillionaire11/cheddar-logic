# WI-0615: Remove homeGoalieConfirmed deprecated field

**ID**: WI-0615
**Goal**: Remove the `homeGoalieConfirmed` deprecated field from `decision-publisher.js` and its test, replacing any remaining references with `homeGoalieState.starter_state`.

**Background**:
`homeGoalieConfirmed` was explicitly marked DEPRECATED after WI-0383. It remains in `apps/worker/src/utils/decision-publisher.js` (line 136) guarded by a comment. Its test counterpart in `decision-publisher.v2.test.js` (line 975) still references it. New callers risk using the deprecated field without knowing the authoritative source is `homeGoalieState.starter_state`.

**Scope**:
- `apps/worker/src/utils/decision-publisher.js` — remove `homeGoalieConfirmed` field and any conditional branch around it
- `apps/worker/src/utils/__tests__/decision-publisher.v2.test.js` — remove or update test at line 975

**Out of scope**:
- Changes to `homeGoalieState` or goalie state source logic

**Acceptance**:
- `homeGoalieConfirmed` does not appear in `decision-publisher.js` or its test.
- `npm --prefix apps/worker test -- decision-publisher` passes.

**Owner agent**: codex
**Time window**: 2026-03-28
**Coordination flag**: solo

**Tests to run**:
- `npm --prefix apps/worker test -- decision-publisher`

CLAIM: codex 2026-03-28T11:42:42-0400

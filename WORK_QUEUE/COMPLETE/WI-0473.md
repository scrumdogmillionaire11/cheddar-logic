# WI-0473: Phase 2 Sport+Market Thresholds V2 (Efficiency-Aware Decisioning)

**ID**: WI-0473
**Goal**: Introduce sport+market-aware threshold routing in the decision pipeline behind a feature flag to prioritize lower-volatility, higher-quality opportunities without changing baseline behavior when disabled.

**Depends on**:

- `WORK_QUEUE/WI-0471.md`
- `WORK_QUEUE/WI-0472.md`

**Scope**:

- `WORK_QUEUE/WI-0473.md`
- `packages/models/src/decision-pipeline-v2.js`
- `packages/models/src/decision-pipeline-v2.patch.js`
- `packages/models/src/index.js`
- `apps/worker/src/utils/decision-publisher.js`
- `apps/worker/src/jobs/__tests__/run_nba_model.test.js`
- `apps/worker/src/jobs/__tests__/run_nhl_model.market-calls.test.js`
- `web/src/__tests__/game-card-decision.test.js`

**Out of scope**:

- Telemetry schema changes
- CLV ledger settlement flow
- New market ingestion sources
- Web UI visual redesign

**Acceptance**:

1. Decision pipeline uses sport+market threshold lookup only when `ENABLE_MARKET_THRESHOLDS_V2=true`.
2. With `ENABLE_MARKET_THRESHOLDS_V2=false`, `official_status` and reasoning outputs remain baseline-equivalent.
3. Threshold map is documented and safely falls back for unknown sport/market combinations.
4. Worker and web decision contract tests pass for both flag-off and flag-on paths.

**Owner agent**: GitHub Copilot
**Time window**: 2026-03-16T00:00:00Z -> TBD
**Coordination flag**: solo

**Tests to run**:

- `npm --prefix web run test:card-decision`
- `npm --prefix apps/worker test -- src/jobs/__tests__/run_nba_model.test.js`
- `npm --prefix apps/worker test -- src/jobs/__tests__/run_nhl_model.market-calls.test.js`
- `ENABLE_MARKET_THRESHOLDS_V2=false npm --prefix apps/worker run job:run-nba-model:test`
- `ENABLE_MARKET_THRESHOLDS_V2=true npm --prefix apps/worker run job:run-nba-model:test`

**Manual validation**:

- Compare a fixed replay sample with flag off vs on and confirm only threshold-gated classification shifts change.
- Verify unknown market types still resolve via fallback thresholds without runtime errors.

**Guard for WI closeout**:

- Before marking complete: confirm every test command in `Tests to run` is runnable from repo root.
- Attach one diffed decision sample proving baseline parity in flag-off mode.

CLAIM: GitHub Copilot 2026-03-16T00:00:00Z

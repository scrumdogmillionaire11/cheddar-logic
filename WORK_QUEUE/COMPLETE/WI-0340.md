# WI-0340: Fix Vercel Next.js Static Chunk 404s

**ID**: WI-0340
**Goal**: Prevent production HTML from referencing missing Next.js chunks by correcting cache headers and documenting verification steps for Vercel + Cloudflare.

**Scope**:

- `vercel.json`
- `docs/ops-runbook.md`

**Out of scope**:

- Next.js app code changes
- Backend/worker changes
- Nginx or server host config

**Acceptance**:

- Vercel headers correctly target `/_next/static/*` assets for immutable caching.
- HTML responses are configured for immediate revalidation to avoid stale chunk references.
- Ops runbook includes Vercel + Cloudflare verification steps for homepage-referenced chunks.

**Owner agent**: GitHub Copilot
**Time window**: 2026-03-08T00:00:00Z -> TBD
**Coordination flag**: solo

**Tests to run**:

- `npm --prefix web run build`

**Manual validation**:

- Fetch homepage HTML from production and verify referenced chunk/CSS paths return `200`.
- Confirm `Cache-Control` headers for HTML and `/_next/static/*` assets align with the contract.

CLAIM: GitHub Copilot 2026-03-08T00:00:00Z

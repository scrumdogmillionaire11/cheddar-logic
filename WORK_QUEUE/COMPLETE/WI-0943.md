# WI-0943: MLB Runner Obvious Dead-Code Cleanup

**ID**: WI-0943
**Goal**: Complete the remaining behavior-neutral MLB runner cleanup by removing any residual dead code patterns and replacing high-volume raw log payloads with deterministic summaries that preserve diagnostic signal.

**Scope**:

- `apps/worker/src/jobs/run_mlb_model.js`
- `apps/worker/src/jobs/mlb-runner-constants.js`
- `apps/worker/src/jobs/__tests__/run_mlb_model.test.js`
- `WORK_QUEUE/WI-0943.md`

**Out of scope**:

- Behavior changes to MLB market selection, gating, or write flow
- Refactors of `withoutOddsMode` or trust-class orchestration
- Cross-model cleanup in NBA or NHL runners

**Acceptance**:

- `run_mlb_model.js` contains no references to legacy dead symbols: `getOddsSnapshots`, `getLatestOdds`, `MLB_K_PROP_ODDS_MAX_AGE_MINUTES`, `generateMLBCard`.
- `run_mlb_model.js` does not contain ambiguous "unused MLB model object" imports; imports from `../models/mlb-model` are explicitly named and each imported symbol has at least one runtime usage in-file.
- High-volume runner logs are summarized with deterministic field sets for these tags:
  - `[MLB_K_AUDIT]` includes only: `gameId`, `pitcherId`, `starterQuality`, `bookmaker`, `lineAgeMinutes`, `marketType`, `decisionState`, `reasonCodes`.
  - `[MLB_DUAL_RUN]` includes only: `gameId`, `marketType`, `pickedPath`, `shadowPath`, `deltaEdge`, `deltaConfidence`, `winner`.
- `run_mlb_model.js` has no direct `console.log(JSON.stringify(<large object>))` usage for MLB audit payload emission.
- Any constants extracted to `mlb-runner-constants.js` remain immutable (`Object.freeze`) and behavior-equivalent (same key set/values as pre-cleanup).
- Cleanup changes are behavior-neutral: targeted MLB runner test suite passes with no expectation changes to card selection, status classification, or write ordering.
- `node --check apps/worker/src/jobs/run_mlb_model.js` passes.

**Owner agent**: GitHub Copilot
**Time window**: 2026-04-14T00:00:00Z -> TBD
**Coordination flag**: solo

**Tests to run**:

- `rg -n "getOddsSnapshots|getLatestOdds|MLB_K_PROP_ODDS_MAX_AGE_MINUTES|generateMLBCard" apps/worker/src/jobs/run_mlb_model.js`
- `node --check apps/worker/src/jobs/run_mlb_model.js`
- `npm --prefix apps/worker run test -- --runInBand src/jobs/__tests__/run_mlb_model.test.js`

**Manual validation**:

- Inspect `run_mlb_model.js` import block and confirm every symbol imported from `../models/mlb-model` has an in-file usage.
- Inspect `[MLB_K_AUDIT]` and `[MLB_DUAL_RUN]` log emitters and confirm they emit summarized, fixed-schema payloads rather than full raw objects.
- Confirm `mlb-runner-constants.js` exports are frozen and unchanged in semantic meaning.
- Review diff to ensure no changes to market gating thresholds, decision status mapping, or DB write flow.

**Guard for WI closeout**:

- Before marking complete: paste command outputs for all `Tests to run` commands into the PR/work log.
- Before marking complete: include a short "behavior-neutral evidence" note listing unchanged invariants (card count semantics, status mapping, write transaction ordering).

CLAIM: GitHub Copilot 2026-04-14T00:00:00Z

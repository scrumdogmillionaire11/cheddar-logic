# WI-0367: Deploy branch fixes to Pi (production)

**ID**: WI-0367
**Goal**: Deploy all fixes from `agent/GitHub-Copilot/WI-0351-team-metrics-cache` to the Pi production environment and verify plays show across all sports.

**Scope**:
- Pi deployment (git pull + worker restart)
- Production DB migration (migration 027 must be applied)

**Out of scope**:
- Any new code changes — deploy only, no modifications

**Acceptance**:
- Migration `027_fix_team_metrics_cache_schema.sql` applied on Pi DB without error.
- Worker process restarted with new code.
- All three model jobs run successfully post-deploy (NBA, NHL, NCAAM).
- `/cards` on cheddarlogic.com shows plays across all three sports.
- `team_metrics_cache` table populated (>0 rows) after first model run.

**Owner agent**: unassigned
**Time window**: TBD -> TBD
**Coordination flag**: solo

**Tests to run**:
- `npm --prefix apps/worker run job:run-nba-model`
- `npm --prefix apps/worker run job:run-nhl-model`
- `npm --prefix apps/worker run job:run-ncaam-model`

**Manual validation**:
- Visit cheddarlogic.com/cards and confirm NBA, NHL, NCAAM plays are visible on default FIRE+WATCH filter.
- Confirm no `WATCHDOG_STALE_SNAPSHOT` or `MODEL_PROB_MISSING` errors in worker logs post-deploy.

---

**COMPLETED**: 2026-03-09
- Production deployment successful
- Plays visible across NBA, NHL, NCAAM on /cards endpoint
- User confirmed: "play visibility is decent, can live with this for now"
- 21 games displayed with PLAY/LEAN statuses across all three sports

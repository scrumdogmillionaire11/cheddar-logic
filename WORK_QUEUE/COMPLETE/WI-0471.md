# WI-0471: Non-Breaking Rollout Foundation (Flags + Basis Contract + Projection Telemetry)

**ID**: WI-0471
**Goal**: Add additive rollout primitives (feature flags, decision basis contract, projection telemetry wiring) with all behavior default-off so current site behavior is unchanged.

**Depends on**:

- `WORK_QUEUE/WI-0470.md`

**Scope**:

- `WORK_QUEUE/WI-0471.md`
- `packages/models/src/flags.js`
- `packages/models/src/decision-basis.types.js`
- `packages/data/src/db-telemetry.js`
- `packages/data/src/db.js`
- `packages/data/index.js`
- `docs/ARCHITECTURE_SEPARATION.md`

**Out of scope**:

- Enabling any rollout flag in production envs
- Worker job-level decision payload changes
- Sport+market threshold behavior changes
- CLV settlement ingestion

**Acceptance**:

1. New feature flags exist and all default to false (`ENABLE_DECISION_BASIS_TAGS`, `ENABLE_MARKET_THRESHOLDS_V2`, `ENABLE_PROJECTION_PERF_LEDGER`, `ENABLE_CLV_LEDGER`).
2. `db-telemetry` exports are wired through `packages/data` without changing existing DB read/write flows.
3. With all new flags unset/false, API payload shape and decision outcomes remain unchanged from baseline.
4. Documentation explicitly states additive-only behavior and phase-gated rollout sequence.

**Owner agent**: GitHub Copilot
**Time window**: 2026-03-16T00:00:00Z -> TBD
**Coordination flag**: solo

**Tests to run**:

- `npm --prefix web run test:card-decision`
- `npm --prefix web run test:transform:market`
- `npm --prefix web run test:api:games:market`
- `npm --prefix apps/worker test -- src/jobs/__tests__/run_nhl_player_shots_model.test.js`

**Manual validation**:

- Run worker and web paths with all flags off and confirm no new fields are emitted in card payloads.
- Confirm no runtime import errors from `@cheddar-logic/data` after telemetry exports are added.

**Guard for WI closeout**:

- Before marking complete: confirm every test command in `Tests to run` is runnable from repo root.
- Capture before/after sample payloads proving no behavior change with all flags off.

CLAIM: GitHub Copilot 2026-03-16T00:00:00Z

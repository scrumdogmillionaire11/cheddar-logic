# WI-0419: Fix Current Lint Warnings/Errors (Web + Worker)

**ID**: WI-0419
**Goal**: Clear the current `npm run lint` failures by removing unused web symbols and fixing `no-useless-escape` in worker settlement parsing.

**Scope**:
- `WORK_QUEUE/WI-0419.md`
- `web/src/components/cards-page-client.tsx`
- `apps/worker/src/jobs/settle_game_results.js`

**Out of scope**:
- Behavioral changes to card rendering or settlement logic
- Broad lint cleanup outside the two reported files
- Any schema, migration, or API contract changes

**Acceptance**:
- `npm run lint` completes with no errors and no warnings from the two reported locations.
- `cards-page-client.tsx` no longer includes unused `DISPLAY_VERDICT`/`getVerdictBadgeWithBrand`.
- `settle_game_results.js` no longer triggers `no-useless-escape` at the reported regex line.

**Owner agent**: codex
**Time window**: 2026-03-12T00:00:00Z -> TBD
**Coordination flag**: solo

**Tests to run**:
- `npm run lint`

**Manual validation**:
- Confirm cards page status badges still render unchanged.
- Confirm Sports Reference summary parsing regex still matches game summary blocks.

CLAIM: codex 2026-03-12T00:00:00Z

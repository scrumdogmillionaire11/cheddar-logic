# WI-0427: Secondary settlement source fallback via Odds API scores

**ID**: WI-0427
**Goal**: Add a secondary game-result source for settlement so finalized games can still settle when ESPN matching/fetching fails.

**Scope**:

- `WORK_QUEUE/WI-0427.md`
- `WORK_QUEUE/COMPLETE/WI-0427.md`
- `apps/worker/src/jobs/settle_game_results.js`
- `apps/worker/src/jobs/__tests__/settle_game_results.matching.test.js`

**Out of scope**:

- Changes to card grading math
- Changes to web `/results` rendering
- Changes to scheduler cadence

**Acceptance**:

- `settle_game_results` attempts Odds API score fallback when ESPN cannot produce a safe match.
- Fallback only uses completed games with parseable final scores and existing safe matching logic.
- Metadata/result source indicates fallback origin.
- Targeted tests cover Odds API score parsing/matching.

**Owner agent**: codex
**Time window**: 2026-03-13T00:00:00Z -> TBD
**Coordination flag**: solo

**Tests to run**:

- `npm --prefix apps/worker run test -- src/jobs/__tests__/settle_game_results.matching.test.js`
- `npm --prefix apps/worker run job:settle-games`

**Manual validation**:

- Run settlement on dev DB where ESPN misses and verify fallback can settle completed games from Odds API scores.

CLAIM: codex 2026-03-13T00:00:00Z

## Execution notes

- Added secondary source fallback in `settle_game_results` using Odds API scores:
  - Loads completed scores from `/v4/sports/{sport_key}/scores`
  - Converts them into settlement comparable events
  - Reuses existing strict/relaxed/fuzzy safe-matching logic
  - Prefixes match method with `oddsapi_` and records fallback source metadata
- Added env controls:
  - `SETTLEMENT_ENABLE_ODDSAPI_SCORES_FALLBACK` (default enabled unless explicitly `false`)
  - `ODDS_API_TIMEOUT_MS`
  - `ODDS_API_SCORES_DAYS_FROM` (clamped 1..3)
- Added tests for Odds API score parsing + strict matching path.

## Validation evidence

- `node --check apps/worker/src/jobs/settle_game_results.js` (pass)
- `npm --prefix apps/worker run test -- src/jobs/__tests__/settle_game_results.matching.test.js` (pass)
- `npm --prefix apps/worker run job:settle-games` (pass; no unsettled past games in current dev DB snapshot)

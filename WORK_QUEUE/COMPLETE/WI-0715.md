# WI-0715: Remove legacy and dead code from odds fetch pipeline

**ID:** WI-0715
**Goal:** Delete all unreachable, permanently-null, and obsolete code from the odds pipeline to prevent future contributors from being misled by dead branches and false logging.

**Dead code inventory (all confirmed unreachable or permanently-null):**

`packages/odds/src/index.js`:
- `countSpreadMarketsInApiData()` (lines 31–41) — only called from the dead SOCCER multi-league branch
- `mergeSoccerSpreadMarkets()` (lines 44–95) — only called from the dead SOCCER multi-league branch
- Entire `if (Array.isArray(config.apiKeys))` multi-league branch in `fetchFromOddsAPI` (lines 208–263) — `config.apiKeys` (plural) doesn't exist on any sport in `SPORTS_CONFIG`; SOCCER was never added
- `Array.isArray(fetchResult)` ternary guard (lines 148–149) — multi-league path that returned a plain array is unreachable; simplify to direct destructure after multi-league branch removal

`packages/odds/src/normalize.js`:
- `totalBook: null` on line 186 — no logic ever sets this field; always null in every DB row

`apps/worker/src/jobs/pull_odds_hourly.js`:
- `mlF5Home` and `mlF5Away` in `insertOddsSnapshot` call (lines 510–511) — `normalize.js` never sets these fields on `normalized.odds`; permanently null
- `NO_F5_ML_LINE` log block (lines 518–521) — condition `normalized.odds?.mlF5Home == null` is permanently true; fires as noise on every MLB game every run
- Inner `require('@cheddar-logic/odds')` inside `getTokenCostPerFetch()` body (lines 68–72) — already imported at module top level; redundant double-require

`apps/worker/src/jobs/pull_nhl_player_shots_props.js`:
- `fetchJson()` function (lines 67–73) — defined but never called; superseded by `fetchJsonWithHeaders()`
- `DEFAULT_SLEEP_MS` constant and `sleep()` function (lines 47, 63–65) — per-event loop pattern was replaced by bulk endpoint; no remaining callers

`apps/worker/src/jobs/pull_mlb_pitcher_strikeout_props.js`:
- `fetchJson()` function (lines 74–80) — same as NHL: defined but never called
- `DEFAULT_SLEEP_MS` constant and `sleep()` function (lines 46, 70–72) — same as NHL: no remaining callers

**Scope:**

- `packages/odds/src/index.js`
- `packages/odds/src/normalize.js`
- `apps/worker/src/jobs/pull_odds_hourly.js`
- `apps/worker/src/jobs/pull_nhl_player_shots_props.js`
- `apps/worker/src/jobs/pull_mlb_pitcher_strikeout_props.js`

**Out of scope:**

- `packages/odds/src/config.js` — handled in WI-0714
- `packages/odds/src/market_evaluator.js` — audited clean, no dead code
- Any DB migration for `ml_f5_home` / `ml_f5_away` columns — removing the write is sufficient; column cleanup is a separate schema concern
- `ENABLE_ODDS_PULL` env var guard in `pull_odds_hourly.js` — kept intentionally; it is a dev safety rail that prevents burning tokens locally

**Acceptance:**

1. `countSpreadMarketsInApiData`, `mergeSoccerSpreadMarkets`, and the `Array.isArray(config.apiKeys)` branch are deleted from `index.js`; `fetchFromOddsAPI` export remains and all active code paths still work
2. `totalBook: null` removed from the `odds` object returned by `normalizeGame` in `normalize.js`
3. `mlF5Home`, `mlF5Away` params removed from the `insertOddsSnapshot` call in `pull_odds_hourly.js`; `NO_F5_ML_LINE` log block deleted; inner `require` inside `getTokenCostPerFetch` collapsed to use the top-level import
4. `fetchJson()`, `sleep()`, and `DEFAULT_SLEEP_MS` deleted from both `pull_nhl_player_shots_props.js` and `pull_mlb_pitcher_strikeout_props.js`
5. `npm --prefix apps/worker test` exits 0
6. No references to deleted symbols remain anywhere in the `packages/odds/` or `apps/worker/` directories

**Owner agent:** unassigned
**Time window:** 2026-04-01 -> TBD
**Coordination flag:** solo — must be applied after WI-0714 is complete (WI-0714 adds `periodMarkets` logic to `index.js` before this WI removes dead branches from the same file)

**Tests to run:**

- `npm --prefix apps/worker test`

**Manual validation:**

- After applying, run `node -e "require('./packages/odds/src/index.js'); console.log('ok')"` from repo root — confirms no broken imports
- Grep confirms deletion: `grep -r "countSpreadMarketsInApiData\|mergeSoccerSpreadMarkets\|mlF5Home\|mlF5Away\|NO_F5_ML_LINE\|DEFAULT_SLEEP_MS" packages/ apps/` returns no matches

**Guard for WI closeout:**

- Before marking complete: `npm --prefix apps/worker test` exits 0 and the grep above returns empty

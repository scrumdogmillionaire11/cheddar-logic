# WI-0424: Play Ledger DATE should use game date

**ID**: WI-0424
**Goal**: Ensure Play Ledger `Date` reflects the game/match date instead of settlement timestamp.

**Scope**:

- `WORK_QUEUE/WI-0424.md`
- `web/src/app/api/results/route.ts`
- `web/src/app/results/page.tsx`

**Out of scope**:

- Settlement grading logic
- Worker job scheduling or backfill behavior
- Ledger sort/order semantics beyond displayed date value

**Acceptance**:

- Results API ledger rows include game date source (`games.game_time_utc`).
- Play Ledger desktop and mobile `Date` display use game date first.
- Safe fallback remains when game date is missing.

**Owner agent**: codex
**Time window**: 2026-03-13T00:00:00Z -> TBD
**Coordination flag**: solo

**Tests to run**:

- `npm --prefix web run lint -- web/src/app/api/results/route.ts web/src/app/results/page.tsx`

**Manual validation**:

- Open `/results` and verify Play Ledger `Date` aligns with matchup/game date, not settlement completion time.

CLAIM: codex 2026-03-13T00:00:00Z

## Execution notes

- Added `game_time_utc` to Results API ledger query/mapping (`gameTimeUtc` in response rows).
- Updated Play Ledger date rendering (desktop + mobile) to prefer game date with fallback: `gameTimeUtc -> createdAt -> settledAt`.
- Validation:
  - `npm --prefix web run lint -- src/app/api/results/route.ts src/app/results/page.tsx` (pass)

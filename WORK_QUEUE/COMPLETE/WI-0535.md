# WI-0535: Decision truthfulness hardening (edge availability + freshness fail-safe)

**ID**: WI-0535
**Goal**: Fix P0 decision integrity defects by making edge availability explicit in gate logic and restoring decision-time freshness vetoes for stale market inputs.

**Scope**:
- `WORK_QUEUE/WI-0535.md`
- `apps/worker/src/utils/decision-publisher.js`
- `apps/worker/src/utils/__tests__/decision-publisher.v2.test.js`
- `packages/models/src/decision-gate.js`
- `packages/models/src/decision-pipeline-v2.js`
- `packages/models/src/card-factory.js`
- `apps/worker/src/jobs/__tests__/run_nhl_model.market-calls.test.js`

**Out of scope**:
- NHL moneyline probability calibration/backtesting and sigma remapping
- Flip-threshold tuning policy (`EDGE_UPGRADE_MIN`) beyond unit/contract normalization needed for P0 fixes
- FIRST_PERIOD product-policy centralization beyond changes strictly required to keep P0 behavior coherent
- Public API contract expansions outside existing model payload surfaces

**Acceptance**:
- `publishDecisionForCard` no longer coerces missing edge to `0`; missing edge is represented explicitly (`edge_available: false` or equivalent canonical flag).
- `shouldFlip` has explicit behavior when edge is unavailable: no edge-driven flip path is used, and resulting reason codes/details make this visible in decision events.
- Decision-time freshness is enforced in canonical decisioning (`buildDecisionV2` path): stale odds inputs cannot remain `PLAY`/`LEAN` without a freshness pass.
- Staleness downgrade reason codes are explicit and machine-readable (for example: `STALE_MARKET_INPUT` / `INPUT_FRESHNESS_FAIL`).
- Existing tests that currently encode stale-odds-as-PLAY behavior are updated to assert freshness veto behavior.
- Non-stale wave-1 recommendations continue to produce `decision_v2` outputs with no regression to legacy fallback behavior.

**Owner agent**: codex
**Time window**: 2026-03-21T01:43:58Z -> TBD
**Coordination flag**: solo

**Tests to run**:
- `npm --prefix apps/worker test -- src/utils/__tests__/decision-publisher.v2.test.js`
- `npm --prefix apps/worker test -- src/jobs/__tests__/run_nhl_model.market-calls.test.js`
- `npm --prefix apps/worker test -- src/models/__tests__/nhl-1p-totals.test.js`

**Manual validation**:
- Build a wave-1 payload with `odds_context.captured_at` older than freshness threshold and verify `decision_v2.official_status` is `PASS` with freshness failure reason code.
- Build a recommendation payload with no edge and verify gate event/reasoning indicates edge unavailable state (not synthetic zero-edge math).
- Verify fresh-input payloads still classify to `PLAY`/`LEAN` when support and pricing thresholds are met.

**Guard for WI closeout**:
- Keep this WI constrained to P0 defect remediation only (`defect`: edge availability truthfulness + freshness fail-safe).
- Do not fold calibration/design-risk work into this WI; track those as follow-on items (`calibration_risk`, `design_risk`, `ambiguity`) after P0 merges.

CLAIM: codex 2026-03-21T01:43:58Z

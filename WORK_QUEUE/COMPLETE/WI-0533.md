# WI-0533: Fix duplicate date group keys in cards page

**ID**: WI-0533
**Goal**: Prevent duplicate React keys in date-grouped card sections when filtered game/prop lists are not strictly sorted by date.

**Scope**:
- `WORK_QUEUE/WI-0533.md`
- `web/src/components/cards-page-client.tsx`

**Out of scope**:
- Filter scoring/ranking behavior in `web/src/lib/game-card/filters`
- Backend card ordering contracts

**Acceptance**:
- Date section rendering in game view never emits duplicate sibling keys for the same ET date.
- Date section rendering in props view uses the same safe grouping behavior.
- Existing card ordering within each date bucket is preserved from filtered input order.

**Owner agent**: codex
**Time window**: 2026-03-20T19:45:00Z -> 2026-03-20T21:00:00Z
**Coordination flag**: solo

**Tests to run**:
- `npm --prefix web run test:filters`

**Manual validation**:
- Open cards page, apply `NHL Totals Only`, and verify no console duplicate-key warnings for date sections.
- Confirm cards still display under expected Today/Tomorrow/date headers.

**Guard for WI closeout**:
- Before marking complete: confirm every test command in `Tests to run` is runnable from repo root (i.e., exists in web/package.json scripts or is a valid `node path/to/test.js` command).

CLAIM: codex 2026-03-20T19:45:00Z

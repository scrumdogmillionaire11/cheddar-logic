# WI-0534: Create VEGAS betting logic auditor agent package

**ID**: WI-0534
**Goal**: Create a portable VEGAS agent package that audits betting logic using edge-validation frameworks, risk guardrails, and reusable workflows instead of pick generation.

**Scope**:
- `WORK_QUEUE/WI-0534.md`
- `vegas-agent/**`

**Out of scope**:
- Any changes to runtime betting models or production card decision code
- Any updates to existing PAX artifacts under `pax-agents/**` or `.claude/**`
- Sports-specific projection formulas tied to one league

**Acceptance**:
- `vegas-agent/` contains modular framework docs across `core/`, `models/`, `workflows/`, `guardrails/`, and `prompts/`.
- Package includes a callable agent definition at `vegas-agent/.claude/agents/vegas-auditor.md`.
- Package includes install/verification scripts so it can be copied into another repo similarly to `pax-agents`.
- Framework explicitly enforces challenge-first behavior with verdict outputs (`PLAY` / `LEAN` / `PASS` / `FADE`) and edge-quality checks.

**Owner agent**: codex
**Time window**: 2026-03-21T01:20:59Z -> TBD
**Coordination flag**: solo

**Tests to run**:
- `./vegas-agent/tests/link-integrity.sh vegas-agent`
- `./vegas-agent/scripts/doctor.sh vegas-agent`

**Manual validation**:
- Open `vegas-agent/.claude/agents/vegas-auditor.md` and confirm all `@./` references resolve to package files.
- Review `vegas-agent/workflows/bet_review.md` and confirm the final output contract includes exactly one of `PLAY`, `LEAN`, `PASS`, `FADE`.

**Guard for WI closeout**:
- Keep all edits constrained to `vegas-agent/**` plus this WI file.
- Do not add auto-betting or pick-generation behavior; maintain audit-only posture.

CLAIM: codex 2026-03-21T01:20:59Z

# WI-0415: Standardize Web WI Test Command Template

**ID**: WI-0415
**Goal**: Prevent non-runnable test commands in work items by defining and enforcing canonical `web` test command patterns.

**Migration note**: Renumbered from WI-0397 due to ID collision with COMPLETE.

**Scope**:

- `WORK_QUEUE/WI-0415.md`
- `WORK_QUEUE/WI-TEMPLATE.md`
- `docs/` guidance covering WI authoring (exact file TBD during implementation)

**Out of scope**:

- Adding or changing product runtime behavior
- Broad test-framework migration
- Non-web package script changes unless needed for template consistency

**Acceptance**:

- A canonical guidance path exists for `web` WI test commands (no references to missing `npm --prefix web test` script).
- WI template/guidance includes runnable examples aligned with `web/package.json` scripts.
- At least one guard/check process is documented to catch invalid test commands before WI closeout.

**Owner agent**: claude-sonnet-4-6
**Time window**: TBD -> 2026-03-13T00:00:00Z
**Coordination flag**: solo

**Status**: COMPLETE (quick-31)
- WI-TEMPLATE.md updated with HTML comment block containing canonical runnable examples
- Guard for WI closeout section added with runnable-script verification checklist
- Template now enforces npm --prefix web run <script> form

CLAIM: claude-sonnet-4-6 2026-03-13T00:00:00Z

**Tests to run**:

- `npm --prefix web run test:card-decision`
- `npm --prefix web run test:transform:market`

**Manual validation**:

- Create a draft WI from template and confirm the test command section uses runnable `web` scripts.


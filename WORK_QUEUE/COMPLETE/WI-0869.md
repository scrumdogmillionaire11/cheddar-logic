# WI-0869: Staged NBA totals reactivation rollout

**ID**: WI-0869
**Goal**: Re-enable NBA totals in production by disabling the explicit `QUARANTINE_NBA_TOTAL` demotion while keeping calibration, freshness, and existing execution gates intact, and document the rollout so it is auditable and reversible.

**Scope**:
- `WORK_QUEUE/WI-0869.md`
- `docs/ops-runbook.md`
- `apps/worker/src/jobs/check_pipeline_health.js`
- `apps/worker/src/__tests__/check-pipeline-health.calibration.test.js`

**Out of scope**:
- Removing the `QUARANTINE_NBA_TOTAL` code path from `packages/models/src/**`
- Changing calibration thresholds, ECE logic, or `CALIBRATION_KILL_SWITCH` behavior
- Changing NBA spread thresholds, spread decision logic, or spread execution behavior
- Retuning NBA totals model math or support thresholds

**Acceptance**:
- `docs/ops-runbook.md` documents the staged production rollout, rollback switch (`QUARANTINE_NBA_TOTAL=0` to disable quarantine, restore env/default to roll back), and the expected validation signals for NBA totals vs spreads.
- The rollout documentation explicitly states that NBA spreads are not code-quarantined and should be validated as an unchanged surface, not re-enabled via logic changes.
- Pipeline-health guidance covers how to verify `NBA_TOTAL` calibration kill-switch state and stale-odds state before and after the rollout.
- Validation steps include checking for disappearance of `NBA_TOTAL_QUARANTINE_DEMOTE` on fresh NBA total cards after the env flip while confirming any remaining suppression is attributable only to standard gates (`CALIBRATION_KILL_SWITCH`, stale snapshot, low support, or other existing execution gates).
- Rollback is documented as a one-step operational reversal with no code revert required.

**Owner agent**: codex
**Time window**: 2026-04-10T19:36:45Z -> TBD
**Coordination flag**: solo

**Tests to run**:
- `npm --prefix apps/worker test -- --runTestsByPath src/__tests__/check-pipeline-health.calibration.test.js`
- `npm --prefix packages/models test -- --runTestsByPath src/__tests__/decision-pipeline-v2-nba-total-quarantine.test.js`

**Manual validation**:
- Load production env on the worker host and verify the canonical DB path: `set -a; source /opt/cheddar-logic/.env.production; set +a`
- Before rollout, inspect pipeline health and confirm whether `NBA_TOTAL` is currently suppressed by calibration: `npm --prefix apps/worker run job:check-pipeline-health`
- Before rollout, confirm fresh same-day NBA odds exist and record the latest capture time for affected games from the production DB.
- Apply the rollout by setting `QUARANTINE_NBA_TOTAL=0` in the production worker environment and restarting `cheddar-worker`.
- After restart, re-run `npm --prefix apps/worker run job:check-pipeline-health` and inspect fresh NBA total cards in `/api/games?sport=NBA` or `/cards` to confirm `NBA_TOTAL_QUARANTINE_DEMOTE` is no longer present on fresh totals.
- Confirm NBA spreads remain visible and unchanged after the rollout.
- If totals still do not surface, confirm the blocking reason is an existing gate such as `CALIBRATION_KILL_SWITCH` or stale odds, not quarantine.
- Roll back by restoring the previous quarantine env/default and restarting `cheddar-worker`.

**Guard for WI closeout**:
- Before marking complete: confirm every test command in `Tests to run` is runnable from repo root.
- Before marking complete: confirm the runbook includes both rollout and rollback instructions and distinguishes totals reactivation from spread validation.

CLAIM: codex 2026-04-10T19:36:45Z

# WI-0716: Odds API stabilization — scoped MLB prop refresh + conservative quota defaults

**ID**: WI-0716
**Goal**: Stop avoidable Odds API burn by moving the main odds baseline to a 3-hour cadence, disabling NHL prop pulls by default, and scoping MLB pitcher-K T-60 refreshes to a single game/event instead of refetching the full slate.

**Scope**:
- `WORK_QUEUE/WI-0716.md`
- `apps/worker/src/schedulers/main.js`
- `apps/worker/src/schedulers/player-props.js`
- `apps/worker/src/schedulers/__tests__/player-props.test.js`
- `apps/worker/src/jobs/pull_mlb_pitcher_strikeout_props.js`
- `apps/worker/src/jobs/__tests__/pull_mlb_pitcher_strikeout_props.test.js`
- `env.example`

**Out of scope**:
- Re-enabling NHL SOG or NHL 1P odds pulls
- Web/API contract changes
- New DB migrations or changes outside the scoped worker/job files
- Changing settlement logic or public betting splits work

**Acceptance**:
- Scheduler defaults use a 3-hour main odds cadence starting at 09:00 ET, with odds gap health aligned to that cadence
- NHL SOG props and NHL 1P odds are disabled by default via env/example + code-default posture
- `computePlayerPropsDueJobs` passes scoped MLB T-60 refresh inputs so the puller does not refetch the full 36-hour slate for every game
- `pullMlbPitcherStrikeoutProps({ gameId?, oddsEventId?, jobKey?, dryRun? })` supports scoped single-event refresh and skips scoped refresh when no event id can be resolved from existing rows
- Fixed MLB K refresh windows remain available at `FULL` and `MEDIUM` quota tiers; MLB T-60 refreshes run only at `FULL`
- Targeted worker tests cover the cadence/gating behavior and scoped MLB prop refresh contract

**Owner agent**: codex
**Time window**: 2026-04-01T18:24:31-0400 -> TBD
**Coordination flag**: solo

**Tests to run**:
- `npm --prefix apps/worker test -- --runInBand --testPathPattern=player-props.test.js`
- `npm --prefix apps/worker test -- --runInBand --testPathPattern=pull_mlb_pitcher_strikeout_props.test.js`

**Manual validation**:
- Run the scheduler in dry-run around 09:00/12:00/15:00/18:00/21:00 ET and confirm `pull_odds_hourly` only queues on the 3-hour cadence
- Simulate MLB T-60 windows and confirm each queued MLB prop refresh is single-game scoped, with no full-slate refetch fallback when `oddsEventId` is absent
- Confirm NHL prop jobs are skipped by default unless explicitly re-enabled

**Guard for WI closeout**:
- Before marking complete: confirm every changed file is listed in Scope, and do not expand into NHL prop puller code in this WI.

CLAIM: codex 2026-04-01T18:24:31-0400

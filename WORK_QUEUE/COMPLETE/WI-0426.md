# WI-0426: Production settlement health check script + rollout runbook

**ID**: WI-0426
**Goal**: Add a production-safe settlement health check script so rollout decisions can be based on objective DB and job-run signals.

**Scope**:

- `WORK_QUEUE/WI-0426.md`
- `WORK_QUEUE/COMPLETE/WI-0426.md`
- `scripts/settlement-health-check.sh`

**Out of scope**:

- Changes to settlement grading logic
- Scheduler cadence changes
- Web UI/API output changes

**Acceptance**:

- Script runs read-only checks against a target DB path and prints actionable settlement health diagnostics.
- Script exits non-zero on critical settlement blockers (e.g., actionable pending cards on finalized games).
- Script supports strict mode for deployment gates.
- Script includes usage/help and supports configurable grace/lookback windows.

**Owner agent**: codex
**Time window**: 2026-03-13T00:00:00Z -> TBD
**Coordination flag**: solo

**Tests to run**:

- `bash scripts/settlement-health-check.sh --help`
- `scripts/settlement-health-check.sh --db "$CHEDDAR_DB_PATH" --grace-hours 6`

**Manual validation**:

- Run script against current dev/prod DB snapshot and verify failures/warnings align with expected settlement state.

CLAIM: codex 2026-03-13T00:00:00Z

## Execution notes

- Added `scripts/settlement-health-check.sh` (read-only) with:
  - critical fail checks for actionable pending-on-final settlement backlog and stale missing finals,
  - warning checks for display-log/missing-market-key drift and recent settlement failures,
  - strict mode (`--strict`) to treat warnings as deployment blockers,
  - configurable grace and lookback windows.
- Script prints:
  - scalar settlement KPIs,
  - per-sport backlog tables,
  - recent settlement job run history.

## Validation evidence

- `bash scripts/settlement-health-check.sh --help` (pass)
- `scripts/settlement-health-check.sh --db "$CHEDDAR_DB_PATH" --grace-hours 6` (pass on dev DB; warnings surfaced as expected)

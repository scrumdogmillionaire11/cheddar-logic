# WI-0412: Add Odds Ingest Failure Ledger + Diagnostics Endpoint

**ID**: WI-0412
**Goal**: Establish an operator-visible feedback loop for missing data by persisting odds-ingest failure reasons, emitting run KPIs, and exposing a read-only diagnostics API.

**Scope**:

- `WORK_QUEUE/WI-0412.md`
- `packages/data/db/migrations/028_create_odds_ingest_failures.sql`
- `packages/data/src/db.js`
- `packages/data/index.js`
- `apps/worker/src/jobs/pull_odds_hourly.js`
- `web/src/app/api/admin/odds-ingest/route.ts`

**Out of scope**:

- Team variant taxonomy expansion
- Odds provider adapter rewrites
- Web cards UX changes
- Settlement logic changes

**Acceptance**:

- Worker persists structured ingest failure reasons (`TEAM_MAPPING_UNMAPPED`, `MARKET_SOURCE_INCOMPLETE`, fetch errors) to a dedicated ledger table.
- Pull-odds job emits clear run KPIs in logs and return payload.
- Read-only admin diagnostics endpoint returns top failure reasons and recent examples.
- No violation of single-writer DB contract (web endpoint is read-only only).

**Owner agent**: codex
**Time window**: 2026-03-12T00:00:00Z -> 2026-03-12T00:00:00Z
**Coordination flag**: needs-sync

**Tests to run**:

- `npm --prefix web run test:card-decision`

**Manual validation**:

- Run `pull_odds_hourly` once and verify failures populate diagnostics endpoint.
- Query `/api/admin/odds-ingest` in development and confirm grouped failure counts + examples.

CLAIM: codex 2026-03-12T00:00:00Z
**Status**: COMPLETE 2026-03-12T00:00:00Z

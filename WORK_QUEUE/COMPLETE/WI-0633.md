# WI-0633: Test suite for run_ncaam_model.js

**Status**: OBSOLETE — 2026-03-28. NCAAM will not be further pursued. Model remains off-season and is not planned for re-activation. No test coverage required.

**ID**: WI-0633
**Goal**: Add a test file for `run_ncaam_model.js` covering card generation, PASS conditions, and schedule/freshness gate paths — mirroring the patterns in `run_nba_model.test.js`.

**Background**:
`apps/worker/src/jobs/run_ncaam_model.js` is 719 lines and produces live NCAAM cards with zero test coverage. It uses the same `@cheddar-logic/models` + in-memory DB pattern as the tested models. NCAAM has a distinct freshness-gate path (`refresh_ncaam_ft_csv` dependency) and schedule-based logic that warrants its own tests separate from WI-0632.

**Scope**:
- `apps/worker/src/jobs/__tests__/run_ncaam_model.test.js` (new) — tests for:
  - Card generation with valid seeded odds and schedule data
  - PASS when odds are missing or edge is below threshold
  - Freshness gate: model is skipped when FT CSV data is stale
  - `ENABLE_NCAAM_MODEL=false` guard

**Out of scope**:
- Changes to `run_ncaam_model.js` logic
- Integration tests against live APIs or live CSV endpoints

**Acceptance**:
- `npm --prefix apps/worker test -- run_ncaam_model` passes.
- At minimum: one FIRE card path, one PASS path, and the freshness-gate skip are tested.
- No real network calls — all external deps mocked or seeded via in-memory DB fixtures.

**Owner agent**: unassigned
**Time window**: TBD
**Coordination flag**: solo

**Tests to run**:
- `npm --prefix apps/worker test -- run_ncaam_model`

**Manual validation**:
- N/A — fully automated.

**Guard for WI closeout**:
- Before marking complete: confirm every test command in `Tests to run` is runnable from repo root.

CLAIM: unassigned

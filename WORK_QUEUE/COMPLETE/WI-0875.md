# WI-0875: Pipeline health watchdog noise reduction for cards freshness alerts

**ID**: WI-0875
**Goal**: Reduce pipeline health Discord noise by retuning alert thresholds and rewriting `cards / freshness` to use recent model execution windows rather than `card_payloads` age as the primary heartbeat.

**Scope**:
- `apps/worker/src/jobs/check_pipeline_health.js`
- `apps/worker/src/jobs/__tests__/check_pipeline_health.test.js`
- `apps/worker/src/jobs/__tests__/check_pipeline_health.watchdog.test.js`
- `apps/worker/src/__tests__/check-pipeline-health.mlb.test.js`
- `.env.production`
- `.env.production.example`

**Out of scope**:
- Scheduler cadence changes
- Odds freshness threshold loosening beyond the current effective 75-minute production SLA
- Enabling the odds backstop in production
- Admin dashboard UI changes

**Acceptance**:
- `cards / freshness` does not fail solely because a game has no recent `card_payloads`; it uses recent successful model execution for the sport's due window as the primary health signal
- `cards / freshness` no longer pages Discord directly after the rewrite
- Production config sets `CARDS_FRESHNESS_MAX_AGE_MINUTES=45` and `PIPELINE_HEALTH_ALERT_CONSECUTIVE=4`
- Odds freshness behavior remains unchanged at the current effective production threshold
- Focused watchdog tests cover the rewritten cards health logic and the 4-consecutive alert threshold

**Owner agent**: codex
**Time window**: 2026-04-10T20:45:00-04:00 -> TBD
**Coordination flag**: solo

**Tests to run**:
- `npm --prefix apps/worker test -- --runInBand --testPathPattern=check_pipeline_health`
- `npm --prefix apps/worker test -- --runInBand --testPathPattern=check-pipeline-health.mlb`

**Manual validation**:
- Trigger `check_pipeline_health` against a DB containing upcoming NBA/NHL games with recent successful model runs but no actionable cards; confirm `cards / freshness` does not produce a failing Discord alert
- Confirm the generated alert message excludes `cards / freshness` while still including real `odds / freshness` failures

**Guard for WI closeout**:
- Before marking complete: confirm every command in `Tests to run` executes from repo root and the changed files match scope exactly

CLAIM: codex 2026-04-10T20:45:00-04:00

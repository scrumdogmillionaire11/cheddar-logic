# WI-0536: Canonical edge contract + unit normalization across gate and decision_v2

**ID**: WI-0536
**Goal**: Replace ambiguous/multi-source edge semantics with one canonical edge contract (value, units, availability) used consistently by decisioning, gate flips, persistence, and UI consumers.

**Scope**:
- `WORK_QUEUE/WI-0536.md`
- `packages/models/src/decision-pipeline-v2.js`
- `packages/models/src/decision-gate.js`
- `apps/worker/src/utils/decision-publisher.js`
- `packages/models/src/card-factory.js`
- `apps/worker/src/utils/__tests__/decision-publisher.v2.test.js`
- `packages/data/src/db.js`
- `packages/data/db/migrations/012_create_decision_records.sql` (only if schema extension is required)

**Out of scope**:
- Threshold-number tuning (`EDGE_UPGRADE_MIN`) beyond making units explicit
- NHL ML calibration/modeling changes
- FIRST_PERIOD pricing policy redesign
- Non-wave1 markets

**Acceptance**:
- A single canonical edge field and unit definition is documented in code and used as source of truth for gate flip logic.
- Gate input no longer depends on mixed legacy fields without explicit precedence.
- Edge availability and unit metadata are explicit (no silent coercion, no implicit unit inference).
- Decision events/persistence expose enough information to audit edge contract compliance per flip decision.
- Existing wave1 tests pass with additional coverage for canonical edge precedence and unit handling.

**Owner agent**: unassigned
**Time window**: TBD
**Coordination flag**: needs-sync
**Depends on**: WI-0535

**Tests to run**:
- `npm --prefix apps/worker test -- src/utils/__tests__/decision-publisher.v2.test.js`
- `npm --prefix apps/worker test -- src/jobs/__tests__/run_nba_model.test.js`
- `npm --prefix apps/worker test -- src/jobs/__tests__/run_nhl_model.test.js`

**Manual validation**:
- Run one NBA and one NHL model cycle and verify published decision events show canonical edge field + units for both kept and blocked decisions.
- Inspect a held decision and confirm gate reasoning references canonical edge semantics (not legacy fallback values).

**Guard for WI closeout**:
- Classify this as `ambiguity` + `design_risk`: fix contract clarity first, do not tune business thresholds in this WI.
- If DB schema is touched, add `needs-sync` coordination notes before implementation and keep migration scope minimal.

CLAIM: unassigned


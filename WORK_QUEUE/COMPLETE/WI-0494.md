# WI-0494: Soccer Model Phase 4 — Calibration Loop Wiring (CLV + Projection Win-Rate)

**ID**: WI-0494
**Goal**: Complete soccer calibration-loop wiring so Tier 1 odds-backed cards flow to CLV telemetry and Tier 2 projection cards are settled into projection win-rate tracking with rollback-safe controls.

**Depends on**:

- `WORK_QUEUE/WI-0492.md`
- `WORK_QUEUE/WI-0493.md`

**Scope**:

- `apps/worker/src/jobs/run_soccer_model.js`
- `apps/worker/src/jobs/settle_soccer_clv.js`
- `apps/worker/src/jobs/settle_soccer_projections.js` (new)
- `packages/data/src/db.js`
- `apps/worker/src/schedulers/main.js`
- `apps/worker/package.json`
- `apps/worker/src/jobs/__tests__/run_soccer_model.test.js`
- `apps/worker/src/jobs/__tests__/settle_soccer_clv.test.js` (new)
- `apps/worker/src/jobs/__tests__/settle_soccer_projections.test.js` (new)
- `docs/DATA_PIPELINE_TROUBLESHOOTING.md`
- `docs/API_BASELINES.md`
- `WORK_QUEUE/WI-0494.md`

**Out of scope**:

- New web pages/endpoints for analytics visualization
- Schema redesign of existing ledgers
- Non-soccer telemetry behavior changes
- Any NFL work

**Acceptance**:

1. Tier 1 soccer odds-backed cards write CLV rows only when `decision_basis='ODDS_BACKED'` and never for projection-only cards.
2. Tier 2 soccer projection cards write projection performance entries and settle to win/loss outcomes via scheduled job.
3. Settlement jobs are idempotent and safe to re-run (no duplicate settlement mutation on second pass).
4. Runbook and baseline docs include soccer-specific verification SQL for ledger inclusion/exclusion and rollback checks.

**Owner agent**: unassigned
**Time window**: TBD -> TBD
**Coordination flag**: solo

**Tests to run**:

- `npm --prefix apps/worker test -- src/jobs/__tests__/run_soccer_model.test.js`
- `npm --prefix apps/worker test -- src/jobs/__tests__/settle_pending_cards.market-contract.test.js`
- `npm --prefix apps/worker run test:pipeline:settlement`
- `ENABLE_CLV_LEDGER=true ENABLE_PROJECTION_PERF_LEDGER=true npm --prefix apps/worker run job:run-soccer-model:test`

**Manual validation**:

- Run one controlled soccer cycle with both ledgers enabled and verify CLV contains only odds-backed rows.
- Verify projection-only soccer cards settle into `projection_perf_ledger` with measurable win-rate fields.
- Re-run settlement jobs and confirm row counts/settled markers do not change unexpectedly.

**Guard for WI closeout**:

- Before marking complete: attach SQL evidence proving CLV exclusion of projection-only rows and projection-ledger inclusion of projection-only rows.
- Confirm single-writer DB contract is preserved (no web-side writes).

CLAIM: unassigned

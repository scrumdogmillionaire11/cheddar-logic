# WI-0948: Odds Freshness Anti-Stale Scheduling Hardening

**ID**: WI-0948
**Goal**: Reduce near-tip stale odds incidents by adding deterministic pre-tip odds refresh coverage and backstop behavior aligned with watchdog freshness expectations.

**Scope**:

- `apps/worker/src/schedulers/main.js`
- `apps/worker/src/schedulers/windows.js`
- `apps/worker/src/jobs/check_pipeline_health.js`
- `apps/worker/src/jobs/__tests__/check_pipeline_health.test.js`
- `apps/worker/src/__tests__/scheduler-windows.test.js`
- `WORK_QUEUE/WI-0948.md`

**Out of scope**:

- New external odds providers or provider credential changes
- DB schema migrations
- Non-odds pipeline checks (cards freshness, settlement backlog, etc.)
- UI/Web alert rendering changes

**Acceptance**:

- Scheduler guarantees at least one deterministic extra odds pull path for near-term games beyond baseline hourly cadence (via time-aware pull and/or fixed-time backstop logic), without duplicate-key spam.
- `odds / freshness` watchdog logic and reason text remain consistent with configured slot behavior and no longer produce false-negative stale outcomes during intended pull windows.
- When odds are genuinely stale, health check still fails and preserves actionable diagnostics (stale count and duplicate suppression detail).
- Legacy scheduling branches and stale-threshold assumptions are either removed or explicitly documented with compatibility rationale in code comments where removal is unsafe.
- Automated tests cover scheduler timing windows and odds freshness classification paths for: fresh, stale-near-term, stale-outside-alert-window, and deduped duplicate game rows.

**Owner agent**: github-copilot
**Time window**: 2026-04-15T00:00:00Z -> TBD
**Coordination flag**: solo

**Tests to run**:

- `npm --prefix apps/worker run test -- --runInBand src/jobs/__tests__/check_pipeline_health.test.js`
- `npm --prefix apps/worker run test -- --runInBand src/__tests__/scheduler-windows.test.js`
- `npm --prefix apps/worker run job:check-pipeline-health`

**Manual validation**:

- Run scheduler in a controlled window with upcoming games in T-2h and confirm at least one extra odds refresh trigger executes before tip window closes.
- Re-run pipeline health after refresh and confirm `odds / freshness` transitions from failed/warning to ok when snapshots are updated.
- Validate a known genuinely stale fixture still reports failed status (no masking of true stale conditions).
- Verify any retained legacy scheduling behavior is intentional and logged/documented for later retirement.

**Guard for WI closeout**:

- Before marking complete: include before/after evidence of odds snapshot recency for at least one previously stale matchup.
- Before marking complete: list each touched legacy path and whether it was removed, retained, or deferred with reason.
- Before marking complete: confirm test commands run from repo root and pass.

CLAIM: github-copilot 2026-04-16T00:57:36Z

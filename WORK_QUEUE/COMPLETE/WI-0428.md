# WI-0428: Do not skip fallback settlement when ESPN scoreboards are empty

**ID**: WI-0428
**Goal**: Ensure settlement still runs secondary sources (Odds API/SportsRef) when ESPN returns no scoreboard data for a sport.

**Scope**:

- `WORK_QUEUE/WI-0428.md`
- `WORK_QUEUE/COMPLETE/WI-0428.md`
- `apps/worker/src/jobs/settle_game_results.js`

**Out of scope**:

- New matching heuristics for ambiguous games
- Web `/results` presentation changes
- Scheduler cadence or retry policy changes

**Acceptance**:

- Settlement does not short-circuit sport processing when all ESPN scoreboard fetches are empty.
- Odds API fallback attempt counters increment for affected games even when ESPN is unavailable.
- Job logs explicitly indicate fallback-only mode for that sport.

**Owner agent**: codex
**Time window**: 2026-03-13T12:15:00Z -> TBD
**Coordination flag**: solo

**Tests to run**:

- `node --check apps/worker/src/jobs/settle_game_results.js`
- `npm --prefix apps/worker run test -- src/jobs/__tests__/settle_game_results.matching.test.js`
- `/bin/zsh -lc 'set -a; source .env; set +a; SETTLEMENT_INCLUDE_DISPLAYED_PENDING_IGNORE_CUTOFF=true npm --prefix apps/worker run job:settle-games'`

**Manual validation**:

- Confirm log line `continuing with fallback sources` appears when ESPN has no data for a sport.
- Confirm Odds API fallback summary shows non-zero attempts in that scenario.

CLAIM: codex 2026-03-13T12:15:00Z

## Execution notes

- Replaced ESPN-empty short-circuit (`continue`) with fallback-only mode so per-game fallback logic still executes.
- Added explicit fallback-only marker to per-sport summary log line.

## Validation evidence

- `node --check apps/worker/src/jobs/settle_game_results.js` (pass)
- `npm --prefix apps/worker run test -- src/jobs/__tests__/settle_game_results.matching.test.js` (pass)
- `/bin/zsh -lc 'set -a; source .env; set +a; SETTLEMENT_INCLUDE_DISPLAYED_PENDING_IGNORE_CUTOFF=true npm --prefix apps/worker run job:settle-games'` (pass)
  - Observed for each sport when ESPN unavailable: `continuing with fallback sources`
  - Observed summary: `Odds API fallback summary — attempts=11, matches=0, misses=11`

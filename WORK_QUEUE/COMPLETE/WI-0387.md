# WI-0387: Cross-Sport Cards Flow Counters + NHL ML Call Surface

**ID**: WI-0387
**Goal**: Restore expected playable-card visibility by adding NHL moneyline call cards, enabling projection-inclusive NHL totals filtering, and instrumenting `/api/games` stage counters across active sports.

**Scope**:

- `WORK_QUEUE/WI-0387.md`
- `web/src/app/api/games/route.ts`
- `web/src/lib/game-card/filters.ts`
- `web/src/lib/game-card/presets.ts`
- `web/src/lib/game-card/transform.ts`
- `apps/worker/src/jobs/run_nhl_model.js`
- `apps/worker/src/jobs/__tests__/run_nhl_model.market-calls.test.js`
- `apps/worker/src/models/__tests__/cross-market.test.js`
- `web/src/__tests__/api-games-repair-budget.test.js`
- `web/src/__tests__/filters-nhl-totals-preset.test.js`

**Out of scope**:

- DB migrations and schema changes
- Soccer/MLB/NFL behavior changes
- Historical payload backfill
- `/api/cards` and `/api/cards/[gameId]` behavior changes

**Acceptance**:

- `/api/games` includes non-prod sport/market stage counters for `base_games`, `card_rows`, `parsed_rows`, `wave1_skipped_no_d2`, `plays_emitted`, `games_with_plays`.
- `/api/games` applies configurable horizon filtering (`API_GAMES_HORIZON_HOURS`, default `36`) without breaking existing response contract.
- `/api/games` enforces explicit sport/card-type kind contract for active sports (NBA/NHL/NCAAM) with diagnostics for missing expected playable market families.
- NHL worker market-call generation emits `nhl-moneyline-call` cards when ML decision status is `FIRE`/`WATCH` with available price.
- `nhl-moneyline-call` payloads are canonical (`kind=PLAY`, `market_type=MONEYLINE`, selection side/team, price, pricing_trace, market_context) and pass through existing decision publisher flow (`decision_v2` attached).
- `NHL Totals Only` preset includes PASS projection rows and excludes PASS noise without total projection data via explicit filter flag.
- Added regression tests cover NHL ML market-call payload contract, NHL totals preset projection behavior, and `/api/games` diagnostics/source contract updates.

**Owner agent**: github-copilot (GPT-5.3-Codex)
**Status**: CLOSED (2026-03-11)
**Time window**: 2026-03-10T23:15:00Z -> 2026-03-11T13:20:00Z
**Coordination flag**: needs-sync

**Tests to run**:

- `node web/src/__tests__/filters-nhl-totals-preset.test.js`
- `node web/src/__tests__/api-games-repair-budget.test.js`
- `npm --prefix web run test:games-filter`
- `npm --prefix apps/worker test -- --runTestsByPath src/jobs/__tests__/run_nhl_model.market-calls.test.js src/models/__tests__/cross-market.test.js`

**Manual validation**:

- Start web locally and confirm `NHL Totals Only` shows PASS rows when total projection exists.
- Confirm NHL ML cards appear as playable rows in `/api/games` output when ML decisions qualify.
- Confirm non-prod `/api/games` response `meta` includes stage counters and playable market-family diagnostics.

## Execution evidence

- `node src/__tests__/filters-nhl-totals-preset.test.js` (from `web/`) → passed
- `node src/__tests__/api-games-repair-budget.test.js` (from `web/`) → passed
- `npm --prefix web run test:games-filter` → passed (18/18)
- `npm --prefix apps/worker test -- --runTestsByPath src/jobs/__tests__/run_nhl_model.market-calls.test.js src/models/__tests__/cross-market.test.js` → passed (10/10)

## Coordination note

- WI-0387 is complete; proceed to later UI/card endpoint work items only after this closed baseline.

CLAIM: github-copilot 2026-03-10T23:15:00Z

# WI-0430: Audit and safe cleanup tooling for display-log backfill artifacts

**ID**: WI-0430
**Goal**: Audit existing `card_display_log` rows for likely backfill artifacts and provide safe SQL cleanup scripts for dev first, then prod.

**Scope**:

- `WORK_QUEUE/WI-0430.md`
- `WORK_QUEUE/COMPLETE/WI-0430.md`
- `scripts/sql/audit_card_display_log_backfill.sql`
- `scripts/sql/cleanup_card_display_log_backfill_dev.sql`
- `scripts/sql/cleanup_card_display_log_backfill_prod.sql`

**Out of scope**:

- Settlement logic changes
- API query changes for `/api/results`
- Automated deletion in production without manual review

**Acceptance**:

- Read-only audit script reports suspected backfill artifacts with tiered confidence.
- Dev cleanup script deletes only high-confidence settled candidates and snapshots deleted rows first.
- Prod cleanup script runs in preview mode (rollback default) and requires explicit commit change.

**Owner agent**: codex
**Time window**: 2026-03-13T12:30:00Z -> TBD
**Coordination flag**: solo

**Tests to run**:

- `sqlite3 "$CHEDDAR_DB_PATH" < scripts/sql/audit_card_display_log_backfill.sql`
- `sqlite3 /tmp/cheddar-display-cleanup-test.db < scripts/sql/cleanup_card_display_log_backfill_dev.sql`
- `sqlite3 /tmp/cheddar-display-cleanup-test.db < scripts/sql/cleanup_card_display_log_backfill_prod.sql`

**Manual validation**:

- Confirm audit shows candidate tiers and sample rows.
- Confirm dev cleanup creates backup table and reports deleted count.
- Confirm prod cleanup ends with rollback by default.

CLAIM: codex 2026-03-13T12:30:00Z

## Execution notes

- Added conservative anomaly heuristics for identifying likely backfill-derived display-log rows:
  - missing payload/card_result links,
  - run/game id mismatches between display log and payload,
  - displayed timestamp materially before payload creation,
  - bookmaker drop signal (medium confidence only).
- Constrained cleanup candidates to `high` suspicion + `card_result_status='settled'` only.
- Created a dedicated backup table in cleanup scripts before deleting rows.

## Audit result (dev DB)

- DB path: `/Users/ajcolubiale/projects/cheddar-logic/packages/data/cheddar.db`
- `card_display_log` rows: `27`
- Settled rows (eligible to render on `/results`): `11`
- High suspicion: `0`
- Medium suspicion: `0`
- By sport:
  - NHL: 18 rows (11 settled), 0 suspicious
  - NCAAM: 5 rows (0 settled), 0 suspicious
  - NBA: 4 rows (0 settled), 0 suspicious

## Validation evidence

- `sqlite3 "$CHEDDAR_DB_PATH" < scripts/sql/audit_card_display_log_backfill.sql` (pass)
- `cp "$CHEDDAR_DB_PATH" /tmp/cheddar-display-cleanup-test.db && sqlite3 /tmp/cheddar-display-cleanup-test.db < scripts/sql/cleanup_card_display_log_backfill_dev.sql` (pass; `deleted_rows=0`)
- `cp "$CHEDDAR_DB_PATH" /tmp/cheddar-display-cleanup-test.db && sqlite3 /tmp/cheddar-display-cleanup-test.db < scripts/sql/cleanup_card_display_log_backfill_prod.sql` (pass; preview rollback)

# WI-0394: Settlement Completeness Hardening (Card-Results Scope)

**ID**: WI-0394
**Goal**: Improve settlement completeness so runs do not silently skip settleable work, while preserving strict grading contracts and 3-hour game-settlement buffer.

**Scope**:

- `WORK_QUEUE/WI-0394.md`
- `apps/worker/src/jobs/settle_game_results.js`
- `apps/worker/src/jobs/settle_pending_cards.js`
- `apps/worker/src/schedulers/main.js`
- `env.example`
- `apps/worker/src/__tests__/settle-coverage.test.js`

**Out of scope**:

- Broad historical resettlement or DB backfills outside nightly flow
- Changes to `/api/results` contract shape
- Non-worker DB write paths
- Sport-specific settlement logic beyond existing match safety rules

**Acceptance**:

- Game settlement candidate selection is scoped to pending `card_results` (not blocked by missing `card_display_log`).
- Card settlement remains strict by default, but nightly scheduler path can run payload→display backfill before grading.
- Hourly scheduler path keeps strict display-log mode (no auto-backfill).
- Coverage diagnostics expose blocked reasons (at minimum: pending-with-final-but-not-displayed).
- Existing 3-hour minimum (`SETTLEMENT_MIN_HOURS_AFTER_START`) remains unchanged.

**Owner agent**: github-copilot (GPT-5.3-Codex)
**Time window**: 2026-03-11T00:00:00Z -> TBD
**Coordination flag**: needs-sync

**Tests to run**:

- `npm --prefix apps/worker test -- --runTestsByPath src/__tests__/settle-coverage.test.js`
- `set -a; source .env; set +a; npm --prefix apps/worker run job:settle-games`
- `set -a; source .env; set +a; npm --prefix apps/worker run job:settle-cards`

**Manual validation**:

- Verify `job:settle-games` can find pending-game candidates even when display-log count is low/zero.
- Verify hourly path stays strict (no backfill) and nightly path enables backfill.
- Verify card-settlement diagnostics show pending rows blocked by missing display-log entries.

CLAIM: github-copilot 2026-03-11T00:00:00Z

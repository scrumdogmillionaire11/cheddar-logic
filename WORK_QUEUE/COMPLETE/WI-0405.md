# WI-0405: Retire Legacy NCAAM FT Card Alias

**ID**: WI-0405
**Goal**: Remove legacy `ncaam-ft-spread` compatibility paths after active payload history no longer depends on them, leaving `ncaam-ft-trend` as the only supported FT card type.

**Scope**:

- `WORK_QUEUE/WI-0405.md`
- `web/src/components/cards-page-client.tsx`
- `web/src/lib/game-card/transform.ts`
- `web/src/__tests__/cards-ft-advantage-source.test.js`
- Any narrowly-scoped FT alias references discovered during implementation in existing web-only FT rendering paths

**Out of scope**:

- Worker-side FT trigger logic changes
- NCAAM FT rule threshold changes
- DB migrations or payload backfills
- Broad refactors unrelated to FT alias removal

**Acceptance**:

- No runtime dependency remains on `ncaam-ft-spread` in scoped files.
- `ncaam-ft-trend` remains the only supported FT card type in current rendering code.
- Regression coverage proves FT cards still render with correct advantage text after alias removal.
- If historical payload support is still required, this WI documents the blocker and closes with explicit rescope instead of partial cleanup.

**Owner agent**: unassigned
**Time window**: TBD -> TBD
**Coordination flag**: none

**Tests to run**:

- `node web/src/__tests__/cards-ft-advantage-source.test.js`

**Manual validation**:

- Load cards page with current NCAAM FT-trend cards and confirm FT callouts still render correctly.
- Verify no user-facing regression from alias removal.

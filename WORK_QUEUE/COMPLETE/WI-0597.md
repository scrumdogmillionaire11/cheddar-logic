# WI-0597: Pitcher Ks odds pull and dual-mode runtime wiring

**ID**: WI-0597
**Goal**: Add pitcher strikeout prop odds ingestion and wire market mode control so the market supports both `ODDS_BACKED` and `PROJECTION_ONLY` execution.

**Scope**:

- `apps/worker/src/jobs/pull_mlb_pitcher_strikeout_props.js`
- `apps/worker/src/jobs/__tests__/pull_mlb_pitcher_strikeout_props.test.js`
- `apps/worker/src/schedulers/main.js`
- `apps/worker/package.json`
- `env.example`
- `packages/odds/src/config.js`

**Out of scope**:

- Core pitcher K scoring formula changes
- Card contract/validator changes
- Web transform/filter changes

**Acceptance**:

- A dedicated pull job ingests MLB pitcher strikeout prop lines into the existing prop storage path with clear source and freshness metadata.
- New mode flag `PITCHER_KS_MODEL_MODE=ODDS_BACKED|PROJECTION_ONLY` is honored by scheduler and manual runs.
- Projection-only mode works with no fetched prop lines; odds-backed mode requires usable lines and emits line-basis diagnostics when absent.
- Scheduler integration does not alter existing non-pitcher MLB job cadence.

**Owner agent**: unassigned
**Time window**: 2026-03-26T00:00:00Z -> TBD
**Coordination flag**: needs-sync

**Tests to run**:

- `npm --prefix apps/worker run test -- src/jobs/__tests__/pull_mlb_pitcher_strikeout_props.test.js`
- `npm --prefix apps/worker run test -- src/__tests__/scheduler-windows.test.js`

**Manual validation**:

- Run `set -a; source .env; set +a; npm --prefix apps/worker run job:pull-mlb-pitcher-strikeout-props` and confirm fresh lines exist for scheduled games.
- Run MLB model once in each mode and confirm expected mode-specific behavior.

**Guard for WI closeout**:

- Before marking complete: confirm every test command in `Tests to run` is runnable from repo root.
- Before marking complete: verify scheduler overlap/idempotency behavior is unchanged for existing jobs.

CLAIM: claude | STATUS: COMPLETE | 2026-03-27

**Evidence:**

- `pull_mlb_pitcher_strikeout_props.js` fetches `pitcher_strikeouts` from The Odds API per-event endpoint, resolves canonical `game_id` via two-step exact+prefix strategy, normalizes American/decimal prices, and upserts into `player_prop_lines`.
- Dual-mode guard: `MLB_PITCHER_K_PROP_EVENTS_ENABLED=true` + `PITCHER_KS_MODEL_MODE=ODDS_BACKED` required to run pull; `PROJECTION_ONLY` skips pull cleanly with explicit log.
- `main.js` imports and wires `pullMlbPitcherStrikeoutProps` via `queueMlbPitcherKPropIngestBeforeModel` gated by `ENABLE_MLB_PITCHER_K_PROP_PULL`; no cadence change to existing non-pitcher MLB jobs.
- `package.json` has `job:pull-mlb-pitcher-strikeout-props` script; `env.example` documents `MLB_PITCHER_K_PROP_EVENTS_ENABLED` and `PITCHER_KS_MODEL_MODE`.
- 14 pull-job tests pass; 8 scheduler-windows tests pass; no regressions.

# WI-0884: POTD multi-day sanity tracking — potd_daily_stats table and diagnostic script

**ID**: WI-0884
**Goal**: Persist a lightweight stats row for every POTD engine run (fired or not) so the system can self-report fire rate, average edge, and average stake — making miscalibrated thresholds visible before they cost money.

**Scope**:
- `packages/data/db/migrations/*.sql` — one new migration creating `potd_daily_stats` table
- `apps/worker/src/jobs/potd/run_potd_engine.js`
- `apps/worker/src/jobs/potd/__tests__/run-potd-engine.test.js`
- `apps/worker/src/scripts/potd-sanity-check.js` (new file)
- `apps/worker/package.json` — add `"stats:potd"` script entry

**Out of scope**:
- Web API or UI surface for sanity stats — this is a worker-side diagnostic only
- Settlement outcomes in stats (those live in `potd_plays.result` already)
- Retroactive backfill of historical runs before this migration

**Acceptance**:
1. New migration creates `potd_daily_stats` table with columns: `play_date TEXT PRIMARY KEY`, `potd_fired INTEGER NOT NULL` (0 or 1), `candidate_count INTEGER NOT NULL`, `viable_count INTEGER NOT NULL`, `top_edge_pct REAL`, `top_score REAL`, `selected_edge_pct REAL` (null when no play), `selected_score REAL` (null when no play), `stake_pct_of_bankroll REAL` (null when no play), `created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP`
2. `runPotdEngine` writes a `potd_daily_stats` row on every code path that returns a result for the day — **except** the `alreadyPublished` early-exit (which is a re-run guard, not a new run; writing stats there would double-count the day). All other paths (`noPlay: true` for any reason, and `success` with a published play) write a stats row.
3. `candidate_count` and `viable_count` require extending `gatherBestCandidate`'s return value:
   - Add `viableCount` to the return by computing it inline before delegating to `selectBestPlayFn`:
     ```js
     const viableCandidates = scoredCandidates.filter(c =>
       isFiniteNumber(c.edgePct) && c.edgePct > POTD_MIN_EDGE &&
       isFiniteNumber(c.totalScore) && c.totalScore >= confidenceThreshold('HIGH')
     );
     return {
       bestCandidate: selectBestPlayFn(scoredCandidates, { minConfidence: 'HIGH', minEdgePct: POTD_MIN_EDGE }),
       fetchErrors,
       activeSports: sports,
       candidatesCount: scoredCandidates.length,
       viableCount: viableCandidates.length,
     };
     ```
   - `candidatesCount` maps to `candidate_count`; `viableCount` maps to `viable_count`.
   - `gatherBestCandidate` is not exported; this is internal to `run_potd_engine.js` only.
4. New file `apps/worker/src/scripts/potd-sanity-check.js` queries last 30 days from `potd_daily_stats` and prints a summary table: columns `play_date | fired | viable | top_edge | stake_pct`; final row shows `fire_rate`, `avg_edge_when_fired`, `avg_stake_when_fired`; exits 0
5. `npm --prefix apps/worker run stats:potd` added to `apps/worker/package.json` and runs `potd-sanity-check.js`
6. Test: engine run with a valid candidate writes a stats row with `potd_fired = 1` and non-null `selected_edge_pct`
7. Test: engine run with no viable candidates writes a stats row with `potd_fired = 0` and null `selected_edge_pct`
8. Test: second run on same `play_date` upserts (row count stays 1, values updated)
9. All pre-existing `run-potd-engine` tests pass

**Owner agent**: unassigned
**Time window**: 2026-04-11 -> TBD
**Coordination flag**: needs-sync (adds migration; **WI-0883 also adds a migration — whichever of WI-0883/WI-0884 merges second must increment to the next available number. As of 2026-04-11 the last migration is 073; first merger takes 074, second takes 075. Confirm the free number immediately before writing the migration file.**)

**Depends on**: WI-0882 merged ✅ (PR #238). `gatherBestCandidate` extension in Acceptance #3 above is the only remaining prerequisite for `candidate_count`/`viable_count` — no additional external dependency.

**Tests to run**:
- `npm --prefix apps/worker test -- --testPathPattern="run-potd-engine"`
- `npm --prefix apps/worker run stats:potd` (smoke test — must exit 0 against a test or empty DB)

**Manual validation**:
- After a real or fixture POTD run: `SELECT * FROM potd_daily_stats ORDER BY play_date DESC LIMIT 5` and confirm columns populated correctly
- Run `npm --prefix apps/worker run stats:potd` and confirm table output includes `fire_rate` summary row

**Guard for WI closeout**:
- Confirm migration file exists and `potd_daily_stats` table has all 10 columns
- Confirm `stats:potd` script present in `apps/worker/package.json` and is runnable from repo root
- Confirm engine writes stats row on play-fired path and all noPlay paths; **skips** stats on the `alreadyPublished` early-exit path
- Confirm `gatherBestCandidate` returns `candidatesCount` and `viableCount`; both values are non-null in the stats row even when no play fires

CLAIM: unassigned

# WI-0800: Wire pull_schedule_nba and pull_schedule_nhl into the automated scheduler

**ID**: WI-0800
**Goal**: Register `pull_schedule_nba.js` and `pull_schedule_nhl.js` in `schedulers/main.js` at the `04:00 ET` fixed window so game schedule data refreshes automatically on restart/reboot without manual intervention.

**Scope**:

- `apps/worker/src/schedulers/main.js` (import and register schedule jobs at 04:00 ET)
- `apps/worker/src/jobs/pull_schedule_nba.js` (verify export interface)
- `apps/worker/src/jobs/pull_schedule_nhl.js` (verify export interface)

**Out of scope**:

- `pull_espn_games_direct.js` (separate ingestion path — do not remove)
- MLB schedule (handled by ESPN direct + statsapi)
- NFL/NCAAM schedule jobs

**Acceptance**:

- `pull_schedule_nhl` and `pull_schedule_nba` are imported and called in `computeDueJobs()` at the `04:00 ET` fixed window, gated by `ENABLE_NHL_MODEL` and `ENABLE_NBA_MODEL` respectively
- Each job uses a deterministic daily key (e.g., `pull_schedule_nhl|${nowEt.toISODate()}`) so reruns are idempotent
- `scheduler-windows.test.js` covers that these jobs appear in due-jobs output at 04:00 ET

**Owner agent**: unassigned
**Time window**: TBD
**Coordination flag**: solo

**Tests to run**:

- `npm --prefix apps/worker run test -- --testPathPattern scheduler-windows`

**Manual validation**:

- Set clock to 04:05 ET in test; confirm both schedule pull jobs appear in computed due-jobs list
- Run `DRY_RUN=true node apps/worker/src/schedulers/main.js` at 04:05 ET on Pi and confirm schedule pull job keys appear in logs

**Guard for WI closeout**:

- Both imports present in `main.js`
- Scheduler test confirms jobs fire at 04:00 ET window
- Idempotency key format documented in job key audit

CLAIM: unassigned

# WI-0422: Canonical True Play Selection Across Logging, Games API, Results, and UI

**ID**: WI-0422
**Goal**: Enforce one canonical true play per `game_id + run_id` using PLAY/LEAN-only eligibility and align data logging, API exposure, results dedupe, and UI consumption to that single selector.

**Scope**:
- `WORK_QUEUE/COMPLETE/WI-0422.md`
- `packages/data/src/db.js`
- `packages/data/__tests__/card-display-log-play-selection.test.js`
- `packages/data/__tests__/card-payload-sport.test.js`
- `apps/worker/src/jobs/settle_pending_cards.js`
- `web/src/app/api/games/route.ts`
- `web/src/app/api/results/route.ts`
- `web/src/lib/game-card/transform.ts`
- `web/src/components/cards-page-client.tsx`
- `web/src/__tests__/api-games-repair-budget.test.js`
- `web/src/__tests__/integration/games-pipeline-contract.test.ts`

**Out of scope**:
- Model inference behavior and candidate generation in worker model modules
- Schema migrations and lockfile/tooling changes
- UI redesign beyond canonical play-source alignment

**Acceptance**:
- Display-log enrollment uses only actionable `official_status IN (PLAY, LEAN)` and valid locked market payloads.
- Exactly one logged selection exists per `run_id + game_id`, chosen by deterministic canonical ranking.
- Backfill path applies identical eligibility + ranking + one-per-game-run constraints.
- `/api/games` includes `true_play` per game while preserving `plays` for evidence context.
- `/api/results?dedupe=1` dedupes per `game_id` via canonical true-play lineage.
- Cards UI prefers canonical `true_play` and does not drift market intent via fallback when true play exists.

**Owner agent**: codex
**Time window**: 2026-03-13T00:00:00Z -> TBD
**Coordination flag**: solo

**Tests to run**:
- `npm --prefix packages/data run test -- card-display-log-play-selection.test.js card-payload-sport.test.js`
- `npm --prefix web run test:api:games:repair-budget`
- `npm --prefix web run test:integration:games-pipeline-contract`
- `npm --prefix web run test:card-decision`

**Manual validation**:
- Verify `/api/games` response includes `true_play` and retains `plays`.
- Verify game with mixed market candidates exposes only one canonical true play.
- Verify results dedupe picks the same game-level true play lineage.

CLAIM: codex 2026-03-13T02:00:00Z

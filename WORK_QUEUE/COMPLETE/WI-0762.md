# WI-0762: Build `pull_public_splits.js` job to populate Action Network public betting columns in `odds_snapshots`

| Field              | Value |
|--------------------|-------|
| **ID**             | WI-0762 |
| **Goal**           | Build a `pull_public_splits.js` pull job that fetches public betting percentages from the Action Network API and writes into the 8 stub columns added by migration 055, then wire the splits into the NHL/NBA model consensus signal |
| **Priority**       | High (schema stub with permanently-NULL columns affects model signal quality) |
| **Owner agent**    | Unassigned |
| **Time window**    | TBD |
| **Coordination flag** | `needs-sync` — touches scheduler registration in `main.js` and model signal wiring |

---

## Problem

Migration `055_add_public_splits_to_odds_snapshots.sql` added 8 columns to `odds_snapshots`:

```
public_bets_pct_home, public_bets_pct_away
public_handle_pct_home, public_handle_pct_away
public_tickets_pct_home, public_tickets_pct_away
splits_source, splits_captured_at
```

**No pull job has ever written to any of these columns.** Zero references exist in `apps/`, `packages/data/src/`, or `web/src/`.

**Impact:**
- All 8 columns are permanently NULL
- The intended consensus signal (public vs sharp divergence) is completely absent from the NHL and NBA models
- Migration 055 has been shipping in schema for unknown number of releases with no data

---

## Scope (revised 2026-04-04 — VSIN/DK implementation)

Source selection: **VSIN** (`data.vsin.com`) using the pre-existing `packages/adapters/src/vsin.js` HTML scraper (confirmed live capture 2026-04-03, DK + CIRCA sources). Action Network adapter (`pull_public_splits.js`) remains active writing `public_*` columns in parallel.

| File | Change |
|------|--------|
| `packages/data/db/migrations/058_add_vsin_splits_to_odds_snapshots.sql` | New migration — 7 `dk_*` columns + `vsin_captured_at` on `odds_snapshots` |
| `packages/data/src/db/odds.js` | Add `updateOddsSnapshotVsinSplits({ gameId, vsinData })` — UPDATE dk_* columns |
| `packages/data/src/db/index.js` | Export `updateOddsSnapshotVsinSplits` |
| `apps/worker/src/jobs/pull_vsin_splits.js` | New job — fetch VSIN DK page, parse HTML, match to game IDs, write `dk_*` columns; all 4 sports (MLB/NBA/NHL/NFL); soft-fail per-sport |
| `apps/worker/src/schedulers/main.js` | Import `runPullVsinSplits`; add `keyVsinSplits`; register section 2.75 (hourly, 09:00–23:00 ET) immediately after the AN splits block |
| `apps/worker/src/jobs/run_nhl_model.js` | Add `splits_divergence` field (PUBLIC_HEAVY_HOME / PUBLIC_HEAVY_AWAY / BALANCED / null) to all 3 payloadData blocks (TOTAL, SPREAD, ML) — reads `public_bets_pct_home/away` from oddsSnapshot |
| `apps/worker/src/jobs/run_nba_model.js` | Same as NHL — `splits_divergence` added to TOTAL and SPREAD payloadData blocks |
| `packages/adapters/src/vsin.js` | Pre-existing — HTML scraper confirmed 2026-04-03; no changes required |

## Out of Scope

- Action Network adapter changes (`packages/adapters/src/action-network.js`) — out of scope; AN job continues unchanged
- Circa sharp splits (`source='CIRCA'`) — data layer add-on; follow-on WI after DK validated
- Using splits_divergence as a hard gate/blocker — first pass is informational signal in payloadData only
- Dropping stub columns if integration proves infeasible — separate schema cleanup WI

---

## Acceptance

- After `pull_public_splits` runs, `SELECT public_bets_pct_home FROM odds_snapshots WHERE public_bets_pct_home IS NOT NULL LIMIT 1` returns a row
- NHL and NBA model runs no longer treat the divergence signal as absent
- `splits_source` = `'action_network'` on all written rows
- Existing NHL/NBA model tests still pass

---

## Tests to run

- `node apps/worker/src/jobs/pull_public_splits.js` (smoke test — no uncaught errors)
- `npm --prefix packages/adapters test` (adapter unit tests)
- `CHEDDAR_DB_ALLOW_MULTI_PROCESS=true node apps/worker/src/jobs/run_nhl_model.js` (smoke — no regressions)

## Manual validation

- Run pull job, then query `odds_snapshots` to confirm non-NULL splits columns on at least one row
- Run NHL model, confirm divergence signal appears in card payload `pricing_context` when splits data is present

**Guard for WI closeout**: grep confirms `public_bets_pct_home` is referenced in at least one model file.

CLAIM: unassigned

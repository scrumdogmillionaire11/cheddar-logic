# WI-0892: Unify Projection-Only Visibility Contract Across /api/cards and /api/games

**ID**: WI-0892
**Goal**: Remove endpoint contract drift so projection-only and synthetic fallback rows are treated consistently between /api/cards, /api/cards/[gameId], and /api/games.

**Depends on**: WI-0890

**Scope**:
- web/src/app/api/cards/route.ts
- web/src/app/api/cards/[gameId]/route.ts
- web/src/lib/games/route-handler.ts
- web/src/__tests__/api-games-prop-decision-contract.test.js
- web/src/__tests__/cards-1p-projection-source.test.js

**Out of scope**:
- Model runner decision generation
- New filter controls in UI
- Any changes to non-card endpoints

**Acceptance**:
- One shared rule-set determines projection-only inclusion/exclusion for all three routes.
- Pass-through exceptions are explicit, minimal, and tested.
- Contract tests assert behavior (response-level) for representative payload combinations.
- Route comments/documentation accurately describe final behavior.

**Owner agent**: github-copilot
**Time window**: 2026-04-11 -> 2026-04-12
**Coordination flag**: needs-sync

**Requirements coverage map**:
- R1 (shared rule-set across /api/cards, /api/cards/[gameId], /api/games) -> Task 1, Task 2
- R2 (explicit minimal pass-through exceptions) -> Task 1, Task 2
- R3 (response-level contract tests for representative payloads) -> Task 3
- R4 (route comments/docs match final behavior) -> Task 2

**Execution tasks**:

### Task 1: Define and centralize projection visibility contract
**Files**:
- web/src/lib/games/route-handler.ts

**Action**:
- Add or consolidate one exported visibility decision helper in route-handler.ts that determines whether a projection-only or synthetic fallback row is included, excluded, or passed through by explicit exception.
- Ensure the helper returns a deterministic decision shape that can be consumed identically by /api/cards, /api/cards/[gameId], and /api/games.
- Keep exception categories narrow and explicitly named to avoid implicit pass-through behavior.

**Verify**:
- node web/src/__tests__/api-games-prop-decision-contract.test.js

**Done**:
- A single exported helper is the source of truth for projection-only visibility decisions.
- Exception reasons are explicit in helper output or call-site metadata.

### Task 2: Wire all three routes to the shared helper and align route comments
**Files**:
- web/src/app/api/cards/route.ts
- web/src/app/api/cards/[gameId]/route.ts
- web/src/lib/games/route-handler.ts

**Action**:
- Replace route-local projection inclusion/exclusion logic with calls into the shared helper from Task 1.
- Ensure both cards endpoints and games route-handler code path apply identical decision inputs and decision handling.
- Update route-level comments to describe final contract behavior and explicit exception handling.

**Verify**:
- node web/src/__tests__/api-games-prop-decision-contract.test.js
- node web/src/__tests__/cards-1p-projection-source.test.js

**Done**:
- Route-local logic drift is removed.
- All route paths consume the same decision helper and produce consistent projection-only behavior for equivalent payloads.
- Comments accurately describe behavior.

### Task 3: Expand/adjust response-level contract coverage
**Files**:
- web/src/__tests__/api-games-prop-decision-contract.test.js
- web/src/__tests__/cards-1p-projection-source.test.js

**Action**:
- Add representative payload cases that cover: standard projection-only exclusion, explicit pass-through exception, and synthetic fallback classification.
- Assert behavior at response level (what clients receive), not only internal helper return values.
- Keep fixtures minimal but representative across cards and games payload shapes.

**Verify**:
- node web/src/__tests__/api-games-prop-decision-contract.test.js
- node web/src/__tests__/cards-1p-projection-source.test.js
- npm --prefix web run build

**Done**:
- Both contract tests fail if shared rule behavior drifts across endpoints.
- Build succeeds with updated tests and route wiring.

**Key links (critical wiring)**:
- web/src/lib/games/route-handler.ts (shared visibility helper) -> web/src/app/api/cards/route.ts via direct helper call in card row filtering/annotation path.
- web/src/lib/games/route-handler.ts (shared visibility helper) -> web/src/app/api/cards/[gameId]/route.ts via direct helper call in per-game row filtering/annotation path.
- web/src/lib/games/route-handler.ts (shared visibility helper) -> /api/games assembly path via existing route-handler aggregation flow.

**Tests to run**:
- node web/src/__tests__/api-games-prop-decision-contract.test.js
- node web/src/__tests__/cards-1p-projection-source.test.js
- npm --prefix web run build

**Manual validation**:
- Query /api/cards and /api/games for the same game id and confirm projection-only rows are either consistently excluded or consistently marked by agreed exceptions.
- Query /api/cards/[gameId] for the same game id and confirm behavior matches /api/cards and /api/games.

CLAIM: github-copilot 2026-04-12T19:48:40Z

# WI-0892: Unify Projection-Only Visibility Contract Across /api/cards and /api/games

**ID**: WI-0892
**Goal**: Remove endpoint contract drift so projection-only and synthetic fallback rows are treated consistently between /api/cards, /api/cards/[gameId], and /api/games.

**Depends on**: WI-0890

**Scope**:
- web/src/app/api/cards/route.ts
- web/src/app/api/cards/[gameId]/route.ts
- web/src/lib/games/route-handler.ts
- web/src/__tests__/api-games-prop-decision-contract.test.js
- web/src/__tests__/cards-1p-projection-source.test.js

**Out of scope**:
- Model runner decision generation
- New filter controls in UI
- Any changes to non-card endpoints

**Acceptance**:
- One shared rule-set determines projection-only inclusion/exclusion for all three routes.
- Pass-through exceptions are explicit, minimal, and tested.
- Contract tests assert behavior (response-level) for representative payload combinations.
- Route comments/documentation accurately describe final behavior.

**Owner agent**: unassigned
**Time window**: 2026-04-11 -> TBD
**Coordination flag**: needs-sync

**Tests to run**:
- node web/src/__tests__/api-games-prop-decision-contract.test.js
- node web/src/__tests__/cards-1p-projection-source.test.js
- npm --prefix web run build

**Manual validation**:
- Query /api/cards and /api/games for the same game id and confirm projection-only rows are either consistently excluded or consistently marked by agreed exceptions.

CLAIM: unassigned
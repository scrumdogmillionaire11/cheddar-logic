# WI-0512: FPL Sage recommendation hard filter for blank GW + DGW prioritization

**ID**: WI-0512
**Goal**: Ensure FPL Sage never recommends players with a blank next gameweek and prioritizes top playable DGW candidates across player recommendation surfaces.

**Scope**:

- `WORK_QUEUE/WI-0512.md`
- `cheddar-fpl-sage/src/cheddar_fpl_sage/analysis/decision_framework/transfer_advisor.py`
- `cheddar-fpl-sage/src/cheddar_fpl_sage/analysis/decision_framework/captain_selector.py`
- `cheddar-fpl-sage/tests/test_fixture_horizon_planner.py`
- `cheddar-fpl-sage/tests/test_strategy_mode_recovery.py`
- `web/src/lib/fpl-api.ts`
- `web/src/lib/api-security/security-headers.ts`
- `web/next.config.ts`

**Out of scope**:

- Fixture ingestion/collector changes
- UI rendering changes
- Non-FPL systems in `apps/` or `packages/`

**Acceptance**:

- Transfer recommendation paths do not emit transfer-in candidates that blank in immediate next GW.
- Captain/vice recommendation path does not pick players blanking in immediate next GW.
- DGW candidates in the immediate next GW are prioritized over comparable single-fixture candidates via bounded scoring bonus.
- Existing strategy/risk-mode behavior remains intact except for the intended blank exclusion and DGW preference.
- Tests cover blank exclusion and DGW prioritization regressions.

**Owner agent**: GitHub Copilot
**Time window**: 2026-03-19T00:00:00Z -> TBD
**Coordination flag**: solo

**Tests to run**:

- `pytest cheddar-fpl-sage/tests/test_fixture_horizon_planner.py -k "exclude_blank_next_gameweek or weighted_score_prefers_near_dgw_over_near_blank"`
- `pytest cheddar-fpl-sage/tests/test_strategy_mode_recovery.py -k "captain"`

**Manual validation**:

- Run a local FPL Sage analysis and confirm transfer/captain outputs do not include blank-next-GW players.
- Confirm DGW players are preferred when projections are otherwise close and legal.

**Guard for WI closeout**:

- Keep diffs limited to the scoped files above.

CLAIM: GitHub-Copilot 2026-03-19T00:00:00Z

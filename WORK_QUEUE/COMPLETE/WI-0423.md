# WI-0423: Auto-close non-actionable final pending card results

**ID**: WI-0423
**Goal**: Prevent finished games from leaving non-actionable card_results stuck in pending by auto-closing final PASS/EVIDENCE rows during settlement.

**Scope**:

- `WORK_QUEUE/WI-0423.md`
- `WORK_QUEUE/COMPLETE/WI-0423.md`
- `apps/worker/src/jobs/settle_pending_cards.js`
- `apps/worker/src/__tests__/settle-coverage.test.js`

**Out of scope**:

- Changes to canonical grading rules for actionable locked markets
- Changes to game result ingestion/matching
- UI/API contract changes

**Acceptance**:

- Settlement detects final-game pending rows that are non-actionable (`kind != PLAY` and/or PASS status) and auto-closes them as `status='error', result='void'` with explicit metadata reason.
- Actionable settlement path remains unchanged for valid displayed locked markets.
- Settlement logs include count of non-actionable final rows auto-closed.
- Coverage/telemetry in job response includes auto-closed non-actionable count.
- Tests cover at least one final PASS row auto-closing path.

**Owner agent**: codex
**Time window**: 2026-03-13T00:00:00Z -> TBD
**Coordination flag**: solo

**Tests to run**:

- `node --check apps/worker/src/jobs/settle_pending_cards.js`
- `npm --prefix apps/worker run test -- src/__tests__/settle-coverage.test.js`
- `npm --prefix apps/worker run job:settle-cards`

**Manual validation**:

- Run settlement against dev DB and confirm final non-actionable rows no longer remain pending.
- Confirm actionable rows still follow existing display-log and market-contract settlement rules.

CLAIM: codex 2026-03-13T00:00:00Z

## Execution notes

- Persisted explicit metadata reason for non-actionable auto-close rows in `card_results.metadata.settlement_error`:
  - `code` (`NON_ACTIONABLE_FINAL_PASS` / `NON_ACTIONABLE_FINAL_KIND`)
  - `message`
  - `at`
  - `classification` (`NON_ACTIONABLE_AUTO_CLOSE`)
  - `details`
- Kept settlement telemetry/logging behavior:
  - `autoClosedNonActionable` count in Step 1 log
  - `coverage.nonActionableAutoClosedFinal` and reason counts in job response
- Expanded test coverage assertion for PASS auto-close to verify metadata reason fields are written.

## Validation evidence

- `node --check apps/worker/src/jobs/settle_pending_cards.js` (pass)
- `npm --prefix apps/worker run test -- src/__tests__/settle-coverage.test.js` (pass)
- `npm --prefix apps/worker run job:settle-cards` (pass; no eligible final pending rows in current dev DB snapshot)

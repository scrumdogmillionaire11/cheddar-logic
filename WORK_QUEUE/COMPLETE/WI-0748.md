# WI-0748: Remove projection-only payloads from betting card surfaces

**ID**: WI-0748
**Goal**: Ensure projection-only payloads never reach `/cards`, `/api/cards`, `/api/cards/[gameId]`, or `/api/games` betting surfaces, and remove the stale `/cards` projections tab entrypoint.

**Scope**:
- `WORK_QUEUE/WI-0748.md`
- `web/src/app/api/cards/route.ts`
- `web/src/app/api/cards/[gameId]/route.ts`
- `web/src/lib/games/route-handler.ts`
- `web/src/lib/game-card/transform/index.ts`
- `web/src/components/prop-game-card.tsx`
- `web/src/components/cards/CardsModeTabs.tsx`
- `web/src/components/cards/CardsPageContext.tsx`
- `web/src/__tests__/cards-projection-exclusion.test.js`
- `web/src/__tests__/api-cards-lifecycle-regression.test.js`
- `web/src/__tests__/game-card-transform-market-contract.test.js`
- `web/src/__tests__/ui-cards-smoke.test.js`
- `web/package.json`

**Out of scope**:
- `apps/worker/**`
- `packages/data/**`
- `/results` UI/API changes already owned by WI-0746
- Any DB migration or physical `card_payloads.card_mode` column

**Acceptance**:
- `/api/cards` and `/api/cards/[gameId]` do not return payloads tagged as `PROJECTION_ONLY` via `basis`, `execution_status`, `play.execution_status`, `prop_display_state`, or projection-floor/synthetic line-source fields.
- `/api/games` omits projection-only card payloads from `games[].plays`, so `/cards` game-line and player-props betting views never render research-only cards.
- `/cards` no longer renders the `Game Props` tab, and direct `?mode=projections` requests fall back to game mode.
- Existing dedicated betting card flows remain intact for `ODDS_BACKED` payloads.

**Owner agent**: codex
**Time window**: 2026-04-03T13:58:47Z -> TBD
**Coordination flag**: needs-sync

**Tests to run**:
- `npm --prefix web run test:ui:cards`
- `npm --prefix web run test:cards-sport-filter`
- `npm --prefix web run test:cards-lifecycle-regression`
- `npm --prefix web run test:transform:market`
- `npm --prefix web run test:cards:projection-exclusion`
- `npm --prefix web run lint -- src/app/api/cards/route.ts src/app/api/cards/[gameId]/route.ts src/lib/games/route-handler.ts src/lib/game-card/transform/index.ts src/components/cards/CardsModeTabs.tsx src/components/cards/CardsPageContext.tsx src/__tests__/cards-projection-exclusion.test.js src/__tests__/game-card-transform-market-contract.test.js src/__tests__/ui-cards-smoke.test.js`

**Manual validation**:
- Confirm `/cards` only shows ODDS_BACKED betting cards in Game Lines and Player Props.
- Confirm `?mode=projections` lands back in Game Lines and no Game Props tab is shown.
- Confirm only scoped cards files changed for this work item.

**Guard for WI closeout**:
- Before marking complete: confirm every test command in `Tests to run` exists in `web/package.json` or is a direct `node` command runnable from repo root.

CLAIM: codex 2026-04-03T13:58:47Z

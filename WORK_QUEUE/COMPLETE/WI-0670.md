# WI-0670: Update analyze API cache tests for strict cached payload contract

**ID**: WI-0670
**Goal**: Align analyze API tests with the current cached-result contract so contract-valid cached payloads return `200`, while stale cached payloads continue to force fresh analysis.

**Background**:
After fixing `WI-0669`, the full `cheddar-fpl-sage` suite exposed a stale test in `tests/test_analyze_api.py::test_post_analyze_can_return_cached_result`. The route now correctly rejects legacy cached payloads that do not satisfy `_cached_result_meets_fpl_contract`, but the test still injects `{"team_name": "Cached Team"}` and expects `200`.

**Scope**:
- `cheddar-fpl-sage/tests/test_analyze_api.py`

**Out of scope**:
- Changes to `backend/routers/analyze.py`
- Changes to cache-service behavior
- Any non-analyze API tests

**Acceptance**:
- `test_post_analyze_can_return_cached_result` passes with a contract-valid cached payload
- Test coverage preserves the distinction between contract-valid cached payloads and stale cached payloads
- `cd cheddar-fpl-sage && python -m pytest tests/test_analyze_api.py -v` exits `0`

**Owner agent**: codex
**Time window**: 2026-03-29T00:00:00-04:00 -> TBD
**Coordination flag**: solo
**LOE**: XS

**Tests to run**:
- `cd cheddar-fpl-sage && python -m pytest tests/test_analyze_api.py -v`
- `cd cheddar-fpl-sage && python -m pytest tests/ -v -x`

**Manual validation**:
- Confirm the cached-result test now returns `200` only when the injected payload satisfies the FPL contract guard
- Confirm stale cache payload coverage still exists elsewhere and expects fresh-analysis behavior

**Guard for WI closeout**:
- Before marking complete: confirm both pytest commands above are runnable from repo root and that only the scoped test file changed.

CLAIM: codex 2026-03-29T00:00:00-04:00

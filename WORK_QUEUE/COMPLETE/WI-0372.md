# WI-0372: Decision Publisher Legacy Compatibility Cleanup

**ID**: WI-0372
**Goal**: Remove worker-side legacy decision compatibility paths that remain redundant after `pipeline_state` adoption, but only where downstream consumers are proven canonical-safe.
**Status**: COMPLETE

**Scope**:

- `WORK_QUEUE/WI-0372.md`
- `apps/worker/src/utils/decision-publisher.js`
- `web/src/lib/game-card/decision.js`
- `web/src/lib/game-card/decision.ts`
- `web/src/lib/game-card/transform.ts`
- related tests covering play/action/classification display behavior

**Out of scope**:

- Any verdict threshold or `decision_v2` logic changes
- Web/API behavior changes unrelated to legacy decision compatibility
- Schema migrations

**Acceptance**:

- Redundant worker compatibility fields are removed only where downstream web consumers no longer require them.
- Display/action parity remains unchanged for canonical wave-1 cards.
- Regression tests cover canonical decision rendering without fallback to legacy fields.
- No breaking change is introduced for non-wave-1 or out-of-scope payloads.

**Owner agent**: GitHub Copilot
**Time window**: 2026-03-11T00:00:00Z -> TBD
**Coordination flag**: needs-sync

CLAIM: GitHub Copilot 2026-03-11T00:00:00Z

**Tests to run**:

- `npm --prefix apps/worker test -- src/utils/__tests__/decision-publisher.v2.test.js`
- `npm --prefix web run test:card-decision`
- `npm --prefix web run test:transform:market`

**Manual validation**:

- Verify wave-1 cards still render the same action/classification in the UI after any compatibility removal.
- Verify non-wave-1 cards still fall back safely where canonical decision fields are absent.

---

## Completion notes (2026-03-11)

- Removed redundant wave-1 worker backfill of legacy probability aliases (`model_prob`, `p_fair`, `p_implied`) in `apps/worker/src/utils/decision-publisher.js`.
- Preserved compatibility outputs for display/action (`action`, `classification`, `status`) to avoid user-facing regressions.
- Updated worker regression coverage to assert canonical `decision_v2` remains authoritative while legacy prob aliases are no longer rewritten.
- Aligned JS helper precedence in `web/src/lib/game-card/decision.js` with TS helper order (`action` -> `classification` -> legacy `status`) to avoid drift.
- Added web decision tests for canonical precedence over conflicting legacy fields.

## Investigation log (follow-up candidates)

- **Invalid WI test command discovered**: `npm --prefix web test -- --runInBand` is not executable because `web/package.json` has no `test` script.
  - Suggested follow-up: standardize WI test templates to use existing `web` scripts or add a canonical aggregate `test` script.

- **Cross-language helper drift risk**: the repo keeps parallel decision logic in `web/src/lib/game-card/decision.ts` and `web/src/lib/game-card/decision.js`.
  - Suggested follow-up: define single-source strategy (shared module or generated parity checks) to prevent future precedence divergence.

- **Legacy `status` dependency still widespread (out of scope for this WI)**: multiple web paths still branch on `play.status` directly.
  - Suggested follow-up: phase plan to migrate remaining call sites to canonical action/classification resolvers and then retire legacy-only status branching.

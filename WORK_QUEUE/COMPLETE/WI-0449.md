# WI-0449: SettleCards Run-Log Failure Remediation

**ID**: WI-0449
**Goal**: Fix settlement pipeline issues observed in the latest run log, specifically `Statement run error: undefined` during non-actionable auto-close and contradictory close/fail counters.

**Scope**:

- `WORK_QUEUE/WI-0449.md`
- `apps/worker/src/jobs/settle_cards.js`
- `apps/worker/src/jobs/__tests__/settle_cards*.test.js` (existing or new focused regression tests)
- `packages/data/src/**` (only query helpers directly used by settle-cards auto-close path)
- `docs/ops-runbook.md` (only settlement troubleshooting section, if required)

**Out of scope**:

- Model inference/math changes (NBA/NHL/NCAAM/Soccer)
- UI/dashboard changes
- New migrations unless strictly required to resolve settlement write-path failure
- Broad queue reprioritization/documentation tasks unrelated to settlement run-log defects

**Acceptance**:

- Non-actionable auto-close path no longer emits `Statement run error: undefined` for eligible rows.
- Settlement logging distinguishes true auto-close success from failures with accurate counters (no contradiction like “auto-closed N” and “failed N” for same rows).
- On write failure, logs include actionable diagnostics (operation name + key identifiers + concrete DB error message).
- Regression tests cover:
  - non-actionable final rows are auto-closed successfully,
  - failed DB writes are counted/logged correctly,
  - coverage summary counters align with row-level outcomes.
- Re-run of settle-cards shows stable summary accounting (`pending`, `autoClosedNonActionable`, `failedAutoClose`, `settled`) with internally consistent totals.

**Owner agent**: unassigned
**Time window**: 2026-03-14T00:00:00Z -> TBD
**Coordination flag**: needs-sync

**Tests to run**:

- `npm --prefix apps/worker test -- src/jobs/__tests__/settle_cards*.test.js`
- `npm --prefix apps/worker run job:settle-cards`
- `npm --prefix apps/worker run job:settle-cards -- --dry-run` (if supported)

**Manual validation**:

- Confirm previously failing non-actionable card IDs from the run log no longer produce `Statement run error: undefined`.
- Confirm settlement summary no longer reports contradictory success/failure counts for same bucket.
- Confirm post-run coverage counters move as expected and reconcile with sampled `card_results` rows.

**Guard for WI closeout**:

- Before marking complete: include before/after settle-cards summaries in WI notes.
- Before marking complete: include at least 5 sampled row IDs demonstrating correct non-actionable close behavior.

CLAIM: unassigned TBD

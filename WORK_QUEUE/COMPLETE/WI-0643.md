# WI-0643: Clear current web lint backlog

**ID**: WI-0643
**Goal**: Resolve the current `npm run lint` warnings and error in the scoped web UI/transform/game helper files without changing behavior outside those surfaces.

**Scope**:
- `WORK_QUEUE/WI-0643.md`
- `web/src/components/cards/CardsPageContext.tsx`
- `web/src/components/cards/GameCardItem.tsx`
- `web/src/components/prop-game-card.tsx`
- `web/src/lib/game-card/transform/index.ts`
- `web/src/lib/games/market-inference.ts`
- `web/src/lib/games/route-handler.ts`

**Out of scope**:
- New product behavior beyond what is required to satisfy lint
- Files not listed in Scope
- Repo/tooling config changes

**Acceptance**:
- `npm run lint` passes from repo root.
- No changed file falls outside this WI scope.
- Driver-toggle persistence on cards still works after the React hook cleanup.

**Owner agent**: Codex
**Time window**: 2026-03-29T01:58:11Z -> TBD
**Coordination flag**: solo

**Tests to run**:
- `npm --prefix web run test:ui:cards`
- `npm --prefix web run test:transform:market`
- `npm --prefix web run test:api:games:market`
- `npm run lint`

**Manual validation**:
- Open a card locally and verify the "show all drivers" toggle still persists per card refresh/session.

**Guard for WI closeout**:
- Before marking complete: confirm every test command in `Tests to run` is runnable from repo root (i.e., exists in `web/package.json` scripts or is a valid command).

CLAIM: Codex 2026-03-29T01:58:11Z

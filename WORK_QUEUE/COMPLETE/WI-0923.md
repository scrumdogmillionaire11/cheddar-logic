# WI-0923: Retire Orphaned MLB Prop Candidate/Budget Modules

**ID**: WI-0923

**Goal**: Remove disconnected legacy prop-engine modules under worker `src/props` that are no longer wired into runtime jobs.

**Depends on**: WI-0922

**Scope**:

- `apps/worker/src/props/prop_budget_controller.js`
- `apps/worker/src/props/mlb_pitcher_k_candidate_engine.js`

**Out of scope**:

- Replacing this functionality with a new props orchestration system
- Changes to `run_mlb_model.js` model-scoring logic
- Web card rendering/filters

**Acceptance**:

- `src/props` legacy modules are removed or archived with explicit non-runtime status.
- No runtime source import remains for retired modules.
- MLB/NHL/NBA model runner tests still pass, demonstrating no hidden dependency on retired code.
- Repo search confirms no active references to retired module paths outside historical docs.

**Owner agent**: copilot

**Time window**: 2026-04-13T12:25:52Z -> TBD

**Coordination flag**: solo

**Tests to run**:

- `npm --prefix apps/worker run test -- --testPathPattern="run_mlb_model|run_nhl_model|run_nba_model" --no-coverage`
- `rg -n "src/props/|prop_budget_controller|mlb_pitcher_k_candidate_engine" apps/worker/src --glob '*.js'`

**Manual validation**:

- Run `rg --files apps/worker/src/props` and confirm legacy files are gone or explicitly archived-only.
- Spot-check runner startup (`job:run-mlb-model`) to confirm no module resolution errors.

CLAIM: copilot 2026-04-13T12:25:52Z

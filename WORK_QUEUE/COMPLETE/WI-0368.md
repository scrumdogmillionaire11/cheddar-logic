# WI-0368: Frontend Play Settlement Coverage Parity

**ID**: WI-0368
**Goal**: Ensure every play logged/displayed on frontend (`/cards`, `/api/games`) is eligible for settlement and appears in results, with reconciliation tooling to verify parity.

**Scope**:
- `WORK_QUEUE/WI-0368.md`
- `WORK_QUEUE/README.md`
- `WORK_QUEUE/WI-0369.md` (new)
- `WORK_QUEUE/COMPLETE/WI-0368.md` (archive at completion)
- `apps/worker/src/jobs/settle_pending_cards.js`
- `apps/worker/src/jobs/settle_game_results.js`
- `packages/data/src/db.js`
- `packages/data/__tests__/card-display-log-play-selection.test.js` (new)
- `web/src/app/api/results/route.ts`
- `web/src/__tests__/integration/settlement-parity.test.ts` (new)
- `apps/worker/src/__tests__/settle-coverage.test.js` (new)
- `docs/ops-runbook.md` (settlement verification SQL)

**Out of scope**:
- Props settlement (future WI)
- Historical backfill or resettlement
- Schema migrations (ledger table already exists from WI-0306)
- Frontend UI changes

**Acceptance**:
- Every play shown on frontend has a `card_display_log` entry and corresponding `card_results` row.
- Settlement runs against `card_display_log` join (already implemented in WI-0306) produce 100% coverage for final games.
- Reconciliation query shows zero orphans: `SELECT COUNT(*) FROM card_display_log cdl LEFT JOIN card_results cr ON cdl.pick_id = cr.card_id WHERE cr.id IS NULL AND EXISTS (SELECT 1 FROM game_results gr WHERE gr.game_id = cdl.game_id AND gr.status = 'final')` returns 0.
- `/api/results` returns exactly the plays that were displayed (no phantom results, no missing results).
- Documentation includes SQL verification commands for ops runbook.

**Owner agent**: Codex
**Time window**: 2026-03-10T02:14:49Z -> TBD
**Coordination flag**: needs-sync

**Tests to run**:
- `node --experimental-strip-types web/src/__tests__/integration/settlement-parity.test.ts`
- `npm --prefix apps/worker test -- __tests__/settle-coverage.test.js`
- `npm --prefix packages/data test -- card-display-log-play-selection.test.js`
- `CARDS_API_BASE_URL=http://127.0.0.1:3000 node web/src/__tests__/ui-results-smoke.test.js`

**Manual validation**:
- Run all three model jobs (NBA, NHL, NCAAM) for one day.
- Query logged plays: `SELECT COUNT(*) FROM card_display_log WHERE displayed_at >= datetime('now', '-2 hours');`
- Query pending settlement: `SELECT COUNT(*) FROM card_results WHERE status = 'pending' AND card_id IN (SELECT pick_id FROM card_display_log);`
- Run settlement jobs: `npm --prefix apps/worker run job:settle-games && npm --prefix apps/worker run job:settle-cards`
- Query settled: `SELECT COUNT(*) FROM card_results WHERE status = 'settled' AND card_id IN (SELECT pick_id FROM card_display_log WHERE displayed_at >= datetime('now', '-2 hours'));`
- Verify counts match: logged = pending + settled.
- Check results API: `curl http://localhost:3000/api/results` and confirm all settled plays appear.
- Run orphan check: `SELECT cdl.pick_id, cdl.game_id, cdl.sport FROM card_display_log cdl LEFT JOIN card_results cr ON cdl.pick_id = cr.card_id WHERE cr.id IS NULL AND EXISTS (SELECT 1 FROM game_results gr WHERE gr.game_id = cdl.game_id AND gr.status = 'final');` should return empty.

CLAIM: Codex 2026-03-10T02:14:49Z

---

## Background

Builds on WI-0306 (display ledger implementation) and WI-0324 (settlement race mitigation). Current settlement jobs use `card_display_log` join, but coverage and parity have not been systematically verified. This WI adds:
1. Test coverage for display → settlement → results parity
2. Reconciliation SQL for ops validation
3. Documentation of expected counts and known gaps

## Dependencies

- Requires WI-0306 complete (display ledger exists)
- Requires WI-0324 complete (settlement jobs atomic)
- Related to WI-0345 (decision_v2 pass-through reduces web-side inference)

## Implementation Plan

1. **Test Infrastructure** (worker + web)
   - Create `apps/worker/src/__tests__/settle-coverage.test.js` to verify every display_log entry with final game_result gets settled
   - Create `web/src/__tests__/integration/settlement-parity.test.ts` to verify results API matches display log
   
2. **Settlement Coverage Audit**
   - Add logging in `settle_pending_cards.js` to report: total pending, eligible (in display_log), settled, skipped
   - Add diagnostic query helper: `getSettlementCoverageDiagnostics(db, sport?, dateRange?)`
   
3. **Results API Parity Check**
   - Verify `/api/results` query already filters by display_log join (from WI-0306)
   - Add response header or metadata field: `X-Settlement-Coverage: {settled}/{displayed}`
   
4. **Ops Runbook Documentation**
   - Add "Settlement Verification" section to `docs/ops-runbook.md`
   - Include all SQL queries from manual validation section
   - Document expected count relationships and troubleshooting steps

## Success Metrics

- Zero orphaned settlements (plays in card_results but not in display_log): always 0
- Zero missing settlements (plays in display_log with final game_results but not settled): always 0 after settlement job runs
- Results API count matches display_log count for same time window
- All tests pass on CI

## Execution Status (2026-03-10)

- Implemented:
  - Worker coverage diagnostics + logging in settlement jobs
  - `/api/results` settlement coverage metadata + `X-Settlement-Coverage` header
  - New worker + web parity tests
  - Ops runbook settlement verification SQL
- Automated checks passing:
  - `node --experimental-strip-types web/src/__tests__/integration/settlement-parity.test.ts`
  - `npm --prefix apps/worker test -- __tests__/settle-coverage.test.js`
  - `CARDS_API_BASE_URL=http://127.0.0.1:3000 node web/src/__tests__/ui-results-smoke.test.js`
- Resolved during execution:
  - Implemented worker-owned display-log producer in `insertCardPayload()` for `kind=PLAY` + `official_status IN (PLAY, LEAN)`.
  - Added best-candidate dedupe for same run/game/market/selection:
    - spread: better line (more favorable points), then better odds
    - total-over: lower line, then better odds
    - total-under: higher line, then better odds
  - Added settlement-time backfill to capture existing PLAY/LEAN payloads into `card_display_log` before grading.
- Live validation evidence:
  - Before fix:
    - `/api/games?limit=200` => `games: 54`, `plays: 130`
    - `SELECT COUNT(*) FROM card_display_log;` => `0`
    - `/api/results` => empty
  - After fix + `npm --prefix apps/worker run job:settle-cards`:
    - `SELECT COUNT(*) FROM card_display_log;` => `24`
    - `SELECT status, COUNT(*) FROM card_results GROUP BY status;` => `pending: 1842`, `settled: 11`
    - final displayed orphan check => `0`
    - `/api/results?limit=50` => `settledCards: 11`, `wins: 6`, `losses: 5`, `ledger: 11`
    - `X-Settlement-Coverage` and parity meta report `11/11` final displayed settled.

**Status**: COMPLETE 2026-03-10T02:43:20Z

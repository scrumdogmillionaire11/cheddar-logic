# WI-0980: Add config drift detection and startup config contract

**ID**: WI-0980
**Goal**: Detect and block production deploys when critical runtime configuration differs from expected release contract.

**Depends on**: WI-0975

**Scope**:
- `.github/workflows/deploy-production.yml`
- `scripts/discover-prod-setup.sh`
- `scripts/deployment-verify.js`
- `docs/runbooks/config-drift-detection.md`

**Out of scope**:
- Secret rotation policy overhaul
- Non-production config standardization

**Acceptance**:
- Deployment verification includes required configuration presence and checksum/contract checks.
- Deployment fails fast when required config is missing or drifted.
- Runbook documents required vars and drift triage steps.

**Owner agent**: unassigned
**Time window**: 2026-04-16 -> TBD
**Coordination flag**: needs-sync

**Tests to run**:
- `npm --prefix web run lint`
- `npm --prefix web run test:api:games:repair-budget`

**Manual validation**:
- Temporarily unset a required config in a safe environment and confirm verification fails.
- Restore config and confirm deploy verification succeeds.

CLAIM: unassigned
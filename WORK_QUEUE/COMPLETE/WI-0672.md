# WI-0672: Align wildcard chip-engine tests with current minimum thresholds

**ID**: WI-0672
**Goal**: Update wildcard chip-engine tests so their “below minimum” inputs reflect the current evaluator constants and reason-code expectations.

**Background**:
After fixing `WI-0671`, the full `cheddar-fpl-sage` suite exposed `tests/test_chip_engine_wildcard_free_hit.py::TestWildcardPass::test_below_min_ev_delta_pass`. The evaluator’s `_WC_MIN_EV_DELTA` is now `0.5`, but the test still assumes a `2.0` minimum and uses `team_ev_delta=1.0`, which no longer exercises the minimum-threshold branch.

**Scope**:
- `cheddar-fpl-sage/tests/test_chip_engine_wildcard_free_hit.py`

**Out of scope**:
- Changes to wildcard evaluator constants or logic
- Any free-hit scoring changes
- Non-chip-engine tests

**Acceptance**:
- Wildcard “below minimum” tests use inputs that actually hit the minimum-threshold branches
- `cd cheddar-fpl-sage && python -m pytest tests/test_chip_engine_wildcard_free_hit.py -v` exits `0`
- Full suite progresses past the stale wildcard-threshold failure

**Owner agent**: codex
**Time window**: 2026-03-29T00:00:00-04:00 -> TBD
**Coordination flag**: solo
**LOE**: XS

**Tests to run**:
- `cd cheddar-fpl-sage && python -m pytest tests/test_chip_engine_wildcard_free_hit.py -v`
- `cd cheddar-fpl-sage && python -m pytest tests/ -v -x`

**Manual validation**:
- Confirm the “below min EV delta” and “below min fixture score” cases now map to the intended reason codes
- Confirm no evaluator code changed as part of this WI

**Guard for WI closeout**:
- Before marking complete: confirm both pytest commands above are runnable from repo root and that only the scoped test file changed.

CLAIM: codex 2026-03-29T00:00:00-04:00

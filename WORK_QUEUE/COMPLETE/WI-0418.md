# WI-0418: Fix Active Games Lifecycle Regression

**ID**: WI-0418
**Goal**: Restore accurate active game/card visibility by hardening terminal status exclusions and preventing odds ingestion from overwriting authoritative game lifecycle status.

**Scope**:

- `WORK_QUEUE/WI-0418.md`
- `web/src/app/api/games/route.ts`
- `web/src/app/api/cards/route.ts`
- `web/src/app/api/cards/[gameId]/route.ts`
- `web/src/__tests__/api-games-filter.test.js`
- `web/src/__tests__/api-cards-lifecycle-regression.test.js`
- `apps/worker/src/jobs/pull_odds_hourly.js`
- `apps/worker/src/jobs/refresh_stale_odds.js`

**Out of scope**:

- Scheduler ownership/cron architecture changes
- Cards UI toggle behavior changes
- Settlement algorithm changes

**Acceptance**:

- Active lifecycle excludes terminal status variants consistently across `/api/games`, `/api/cards`, and `/api/cards/[gameId]`.
- Odds jobs no longer force existing game rows back to `scheduled` when provider status is absent/ambiguous.
- Regression tests cover additional terminal status variants and pass.

**Owner agent**: GitHub Copilot
**Time window**: 2026-03-12T00:00:00Z -> 2026-03-12T23:59:59Z
**Coordination flag**: needs-sync

**Tests to run**:

- `node web/src/__tests__/api-games-filter.test.js`
- `node web/src/__tests__/api-cards-lifecycle-regression.test.js`

**Manual validation**:

- Verify `GET /api/games?lifecycle=active` and `GET /api/cards?lifecycle=active` return matching active game IDs for in-progress games.
- Confirm terminal-status games (`final`, `complete`, `completed`, `ft`, `closed`, `cancelled`) are excluded in active mode.

CLAIM: GitHub Copilot 2026-03-12T00:00:00Z

**Status**: COMPLETE 2026-03-12T23:59:59Z

**Validation evidence**:

- `cd web && node src/__tests__/api-games-filter.test.js && node src/__tests__/api-cards-lifecycle-regression.test.js` (pass)

# WI-0468: NHL player-shots dedupe + consistency/matchup decisioning

**ID**: WI-0468
**Goal**: Improve NHL player-shots card quality by deduplicating per-player cards within a run and deriving `PLAY/NO_PLAY` plus confidence from player consistency and opponent matchup profile.

**Scope**:
- `WORK_QUEUE/WI-0468.md`
- `apps/worker/src/jobs/run_nhl_player_shots_model.js`
- `apps/worker/src/jobs/__tests__/run_nhl_player_shots_model.test.js`

**Out of scope**:
- UI preset behavior changes
- Odds line ingestion behavior changes
- Schema/API contract migrations

**Acceptance**:
- `run_nhl_player_shots_model` deduplicates cards by canonical game + player + period within a run.
- `payloadData.play.action/classification/status` are explicitly set from consistency + matchup-driven logic.
- Confidence is computed from consistency + matchup signal (not hard-coded).
- Existing datetime window fix remains covered and tests pass.

**Owner agent**: codex
**Time window**: 2026-03-16T00:00:00Z -> TBD
**Coordination flag**: solo

**Tests to run**:
- `npm --prefix apps/worker test -- src/jobs/__tests__/run_nhl_player_shots_model.test.js`

**Manual validation**:
- Run `npm --prefix apps/worker run job:run-nhl-player-shots-model` and confirm:
  - no duplicate player rows for same game/period in a run,
  - cards include explicit `play.action/classification/status`,
  - confidence varies with consistency/matchup signal.

CLAIM: codex 2026-03-16T00:00:00Z

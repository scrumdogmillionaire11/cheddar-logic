# WI-0365: BMAD Risk-First Queue Prioritization + Queue Hygiene

**ID**: WI-0365
**Goal**: Execute BMAD-style risk-first prioritization for remaining work and enforce queue hygiene so completed items are excluded from active execution order.

**Scope**:

- `WORK_QUEUE/WI-0365.md`
- `WORK_QUEUE/COMPLETE/WI-0365.md`
- `WORK_QUEUE/WI-0350.md`
- `WORK_QUEUE/WI-0363.md`
- `WORK_QUEUE/COMPLETE/WI-0363.md`

**Out of scope**:

- Any runtime code changes in `apps/**`, `packages/**`, `web/**`, or `cheddar-fpl-sage/**`
- New ADRs or architecture changes
- Editing acceptance criteria for active non-complete WIs

**Acceptance**:

- Remaining queue is explicitly prioritized using lens `Unblock + Prod Risk` with `Ranked Queue + Rationale`.
- `WI-0350` and `WI-0363` are treated as complete and excluded from remaining active ordering.
- Sequence constraints are validated:
  - Worker/data serialization: `0351 -> 0361 -> 0362 -> 0349`
  - Web serialization: `0345 -> 0364`
  - Isolated lanes parallel-eligible: `0354`, `0359`
- Queue hygiene flags are documented (owner/claim/template issues) so execution can proceed safely.

**Owner agent**: Codex
**Time window**: 2026-03-09T23:27:52Z -> TBD
**Coordination flag**: solo

**Tests to run**:

- `rg -n "^\\*\\*Status\\*\\*: COMPLETE" WORK_QUEUE/WI-*.md`
- `rg -n "^# WI-0(351|361|362|349|345|364|354|359)" WORK_QUEUE/WI-*.md`
- `test -f WORK_QUEUE/COMPLETE/WI-0363.md && echo "WI-0363 archived"`
- `test ! -f WORK_QUEUE/WI-0350.md && echo "WI-0350 removed from active queue"`

**Manual validation**:

- Confirm active queue order for remaining items is:
  1. `WI-0351`
  2. `WI-0361`
  3. `WI-0362`
  4. `WI-0349`
  5. `WI-0345`
  6. `WI-0364`
  7. `WI-0354`
  8. `WI-0359`
- Confirm no adjacent `execute-now` items in this order share the same scoped files.

CLAIM: Codex 2026-03-09T23:27:52Z

## BMAD Master Prompt Used

`Prioritize remaining WORK_QUEUE/WI-*.md items using Unblock + Prod Risk. Exclude WIs marked Status: COMPLETE. Respect AGENTS non-negotiables and serialize overlapping scopes by file. Output a strict numbered ranking with one-line rationale per WI.`

## Ranked Queue + Rationale (Final)

1. **WI-0351** - Foundational projection-completeness gating and cache path touches the most worker/data overlap points and directly controls production safety.
2. **WI-0361** - Restores ESPN-metrics coverage after strict source enforcement so valid games are not unnecessarily blocked.
3. **WI-0362** - Adds structured diagnostics that make residual null-data failures actionable once baseline gating/fallback behavior is stable.
4. **WI-0349** - Establishes canonical per-game `pipeline_state` contract after upstream reason signals/checkpoints are stabilized.
5. **WI-0345** - Applies web hard-cut to `decision_v2` to align `/api/games` with the worker-side canonical contract.
6. **WI-0364** - Finalizes Main View filter/projection display behavior after `/api/games` route hard-cut work settles.
7. **WI-0354** - Important but isolated FPL strategy-kernel work with lower cross-system unblock pressure than core worker/web contract stabilization.
8. **WI-0359** - Governance pointer cleanup with low runtime risk and no immediate production-blocking impact.

## Queue Hygiene Flags

- `WI-0349`: missing explicit `Owner agent` value and missing `CLAIM` line.
- `WI-0359`: non-template structure (missing required field formatting from `WI-TEMPLATE.md`).
- `WI-0350`: duplicate existed in active + complete; active copy removed in this WI.

## Validation Evidence

- Active queue now excludes complete statuses:
  - `rg -n "^\*\*Status\*\*: COMPLETE" WORK_QUEUE/WI-*.md` -> no matches
- Completed items handled:
  - `WI-0350` removed from active queue (duplicate cleanup)
  - `WI-0363` archived to `WORK_QUEUE/COMPLETE/WI-0363.md`
- Required remaining WIs present in active queue:
  - `WI-0351, WI-0361, WI-0362, WI-0349, WI-0345, WI-0364, WI-0354, WI-0359`
- Parallel-safe execute-now set validated with no shared scope files:
  - `WI-0351, WI-0345, WI-0354, WI-0359`

**Status**: COMPLETE 2026-03-09T23:27:52Z

# WI-0312: End-to-End Card Payload Contract Regression Suite

**ID**: WI-0312
**Goal**: Add cross-layer regression tests that lock the canonical pipeline from generation -> validation -> API -> UI decision.

**Scope**:

- `apps/worker/src/__tests__/integration/pipeline-card-payload.test.js` (new)
- `apps/worker/src/__tests__/fixtures/**` (only new/needed fixtures)
- `web/src/__tests__/integration/games-pipeline-contract.test.ts` (new or existing integration target)
- `packages/data/src/validators/card-payload.js` (test-facing contract assertions only if required)
- `packages/data/src/market-contract.js` (test-facing contract assertions only if required)

**Out of scope**:

- Functional feature additions
- Broad refactors outside test harness/fixtures

**Acceptance**:

- Golden-path test for playable WATCH total with low coverage but valid edge
- PASS and unrepairable legacy cases remain blocked as intended
- Contract validation failures are explicit (not silently repaired into ambiguous states)
- At least one fixture each for NHL, NBA, NCAAM payload paths
- Tests fail on contract drift and legacy fallback drift

**Owner agent**: unassigned
**Time window**: 2026-03-07T14:00:00Z -> 2026-03-08T02:00:00Z
**Coordination flag**: needs-sync

**Tests to run**:

- `npm --prefix apps/worker test -- __tests__/integration/pipeline-card-payload.test.js`
- `npm --prefix web test -- __tests__/integration/games-pipeline-contract.test.ts`

**Manual validation**:

- Run model job + /api/games check on one NHL and one NBA game and compare with expected contract snapshots

CLAIM: unassigned 2026-03-05T00:00:00Z

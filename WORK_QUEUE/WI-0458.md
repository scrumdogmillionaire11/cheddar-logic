# WI-0458: Security audit and hardening — auth walls, admin route gates, and surface review

**ID**: WI-0458
**Goal**: Audit all API routes for security vulnerabilities and fix confirmed gaps — primarily the three disabled auth walls and fragile `NODE_ENV` admin gates.

**Scope**:
- `web/src/app/api/cards/route.ts`
- `web/src/app/api/cards/[gameId]/route.ts`
- `web/src/app/api/games/route.ts`
- `web/src/app/api/admin/audit/route.ts`
- `web/src/app/api/admin/odds-ingest/route.ts`
- `web/src/app/api/results/route.ts`
- `web/src/app/api/team-metrics/route.ts`
- `web/src/app/api/props/shots/route.ts`
- `web/src/lib/api-security/` (all files)
- `web/src/middleware.ts`

**Out of scope**:
- FPL Sage backend (separate service)
- Worker/scheduler processes (not HTTP-facing)
- Database-level permissions

**Acceptance**:
- `AUTH DISABLED` comment blocks are removed; `requireEntitlementForRequest` is restored on `/api/cards`, `/api/cards/[gameId]`, `/api/games`
- Admin routes (`/api/admin/audit`, `/api/admin/odds-ingest`) use an explicit `ADMIN_SECRET` header check (or equivalent) instead of relying solely on `NODE_ENV !== 'development'`
- A written audit report (`docs/security-audit-2026-03.md`) documents every API route, its auth posture, rate-limit status, and any residual findings
- All existing test suites pass after changes: `npm --prefix web run test:auth`, `npm --prefix web run lint`

**Owner agent**: unassigned
**Time window**: 2026-03-15 -> TBD
**Coordination flag**: solo

**Tests to run**:
- `npm --prefix web run test:auth`
- `npm --prefix web run lint`

**Manual validation**:
- `curl http://localhost:3000/api/cards` without a token returns 401 (not 200)
- `curl http://localhost:3000/api/games` without a token returns 401 (not 200)
- `curl http://localhost:3000/api/admin/audit` in production (NODE_ENV=production, no secret header) returns 403

**Guard for WI closeout**:
- Before marking complete: confirm every test command in `Tests to run` is runnable from repo root.
- Verify the audit report file exists at `docs/security-audit-2026-03.md`.

CLAIM: completed by Claude agent — 2026-03-15 (quick task 43)

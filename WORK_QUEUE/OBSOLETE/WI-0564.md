# WI-0564: Soccer game settlement — ingest final scores and grade ML/total/spread cards

**ID**: WI-0564
**Goal**: Soccer cards (moneyline, game total, asian handicap) currently generate but never settle. Implement the full settlement pipeline: final score ingest → market bucket routing → grading → card result write.

**Scope**:
- `apps/worker/src/jobs/settle_game_results.js` — add soccer to `ESPN_SPORT_MAP` and persist final scores in `game_results.metadata`
- `apps/worker/src/jobs/settle_pending_cards.js` — add soccer to `resolveSettlementMarketBucket()` and grading paths for `soccer_ml`, `soccer_game_total`, `asian_handicap_home`, `asian_handicap_away`, `soccer_double_chance`
- `apps/worker/src/__tests__/settle-coverage.test.js` — add soccer assertions
- New test: `apps/worker/src/__tests__/settlement-soccer-integration.test.js`

**Out of scope**:
- Soccer player props settlement (covered by WI-0566)
- Soccer xG model changes
- Draw leg of soccer double-chance (can void/defer if draw result not yet modeled)

**Background**:
`run_soccer_model.js` generates cards for MLS, EPL, UCL, and other leagues. The settlement job (`settle_pending_cards.js`) has no soccer path in `resolveSettlementMarketBucket()` (lines 925–948) — it returns `null` for any SOCCER sport, so cards stay PENDING indefinitely. `settle_game_results.js` only maps NHL/NBA/NCAAM to ESPN endpoints (lines 49–53); no soccer feed is wired.

Soccer market types in use: `soccer_ml` (moneyline), `soccer_game_total` (totals), `asian_handicap_home` / `asian_handicap_away` (spread-equivalent), `soccer_double_chance`.

**Acceptance**:
- Soccer game final scores (home/away goals) are persisted in `game_results.metadata` after games complete.
- `settle_pending_cards.js` routes soccer cards to correct market buckets and grades them WIN/LOSS/PUSH.
- Asian handicap and double-chance markets are graded correctly or voided with a deterministic failure code if draw handling is deferred.
- Cards that were previously stuck PENDING for completed soccer games settle correctly (backfill path or one-time re-run).
- Integration test covers at least: soccer ML home win, away win, total over, total under, asian handicap cover, push scenario.

**Owner agent**: unassigned
**Time window**: 2026-03-22T00:00:00Z -> TBD
**Coordination flag**: solo

**Tests to run**:
- `npm --prefix apps/worker run test -- --testPathPattern settlement-soccer`
- `npm --prefix apps/worker run test -- --testPathPattern settle-coverage`

**Manual validation**:
- Identify 3 completed soccer games in DB; confirm their cards settle after running the settlement job.
- Confirm no NBA/NHL cards are affected by the soccer routing changes.

**Guard for WI closeout**:
- Before marking complete: confirm every test command in `Tests to run` is runnable from repo root.
- All soccer card results must write a non-null `result` (WIN/LOSS/PUSH/VOID) — no PENDING rows for games with final scores.

CLAIM: unassigned

# WI-0401: Investigate NCAAM FT-Trend Market-Type Drift (Spread -> Moneyline)

**ID**: WI-0401
**Goal**: Investigate why `ncaam-ft-trend` plays that are intended to be spread-only are surfacing in the board as moneyline plays, using the Louisville @ Miami case as the primary repro.

**Scope**:
- `WORK_QUEUE/WI-0401.md`
- `apps/worker/src/jobs/run_ncaam_model.js`
- `apps/worker/src/models/index.js`
- `packages/data/src/validators/card-payload.js`
- `web/src/app/api/games/route.ts`
- `web/src/lib/game-card/transform.ts`
- `web/src/components/cards-page-client.tsx`
- `apps/worker/src/models/__tests__/ncaam-drivers.test.js`
- `web/src/__tests__/cards-ft-advantage-source.test.js`

**Out of scope**:
- Changing FT trigger criteria (`total < 160`, FT% threshold split)
- Broad decision-tier/status redesign already tracked by `WI-0396`
- Non-NCAAM market-type rendering issues
- Settlement/history cleanup for previously emitted cards

**Acceptance**:
- Root cause is identified for why an FT-trend card can render or publish as `MONEYLINE`/`ML` instead of `SPREAD`.
- Reproduction is documented against the Louisville Cardinals @ Miami Hurricanes example from Mar 11/12 board state.
- Ownership of the drift is isolated to one layer (worker publish, payload validation/storage, API transform, or UI rendering) with a concrete fix path.
- Regression coverage is defined or added so `ncaam-ft-trend` cards preserve spread market identity end-to-end.
- Follow-up implementation decision is recorded: either fix within this WI scope or log a narrower successor WI if the repair is larger than the investigation.

**Owner agent**: unassigned
**Time window**: 2026-03-11T23:59:00Z -> TBD
**Coordination flag**: needs-sync

**Tests to run**:
- `npm --prefix apps/worker test -- --runTestsByPath src/models/__tests__/ncaam-drivers.test.js`
- `node web/src/__tests__/cards-ft-advantage-source.test.js`
- `curl -s http://localhost:3000/api/games | jq '.data[] | select(.homeTeam=="MIAMI HURRICANES" and .awayTeam=="LOUISVILLE CARDINALS") | .plays'`

**Manual validation**:
- Reproduce the board state where the FT Trend card for Louisville @ Miami shows `Moneyline` / `ML` instead of a spread context.
- Compare persisted payload market fields vs `/api/games` transformed output vs cards UI rendering for the same play.
- Confirm whether the issue is only label/presentation drift or a true market-contract mutation.

**Related work items**:
- Depends on context from: `WI-0390`
- Adjacent to: `WI-0396`

---

## Reported Symptom

Board snapshot reported on 2026-03-11:
- Game: Louisville Cardinals @ Miami Hurricanes
- Sport: NCAAM
- Preset/context: FT Trend play surfaced in main board
- Observed output: `Moneyline` section with `ML` recommendation (`MIAMI HURRICANES ML +118`)
- Expected behavior: FT-trend driver should present as a `SPREAD` play, not a moneyline play

# WI-0438: Soccer draw_odds Ingestion/Normalization (Zero Token Delta)

**ID**: WI-0438
**Goal**: Ingest and expose soccer `draw_odds` from existing soccer `h2h` payloads with zero monthly The Odds API token impact.

**Depends on**: `WORK_QUEUE/WI-0437.md`

**Scope**:
- `WORK_QUEUE/WI-0438.md`
- `packages/odds/src/index.js`
- `packages/odds/src/config.js`
- `apps/worker/src/jobs/run_soccer_model.js`
- `apps/worker/src/jobs/pull_odds_hourly.js`
- `apps/worker/src/jobs/__tests__/run_soccer_model.test.js`
- `apps/worker/src/jobs/__tests__/soccer-odds-quota-guard.test.js`

**Out of scope**:
- Adding new soccer markets beyond existing `h2h`
- Adding new soccer leagues/endpoints
- Scheduler cadence changes
- New public API routes
- DB schema or migration changes

**Acceptance**:
- Soccer `draw_odds` is captured from existing `h2h` bookmaker outcomes and preserved through normalization.
- `soccer-model-output` payload sets `odds_context.draw_odds` to a numeric value when available, otherwise `null`.
- Existing moneyline recommendation behavior remains unchanged (`HOME`/`AWAY` only).
- Soccer market config remains `markets: ['h2h']` and `tokensPerFetch` remains `1`.
- Monthly quota proof is documented with zero WI delta:
  - baseline tokens/fetch across active sports = `9`
  - hourly baseline = `9 * 21 * 30 = 5,670` tokens/month
  - WI-0438 incremental delta = `0` tokens/month

**Owner agent**: unassigned
**Time window**: 2026-03-13T00:00:00Z -> TBD
**Coordination flag**: needs-sync

**Tests to run**:
- `npm --prefix apps/worker test -- src/jobs/__tests__/run_soccer_model.test.js`
- `npm --prefix apps/worker test -- src/jobs/__tests__/soccer-odds-quota-guard.test.js`

**Manual validation**:
- Pull fresh soccer odds and verify `draw_odds` is populated when provider includes a draw line.
- Verify soccer cards continue to render via `/api/cards?sport=SOCCER` and `/api/games?sport=SOCCER`.
- Run `python3 scripts/analyze_odds_api_quota.py` before/after and confirm WI-0438 adds `0` monthly tokens.

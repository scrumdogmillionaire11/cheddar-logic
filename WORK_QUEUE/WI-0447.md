# WI-0447: Shield Core Cards Surfaces From Props Run-State Leakage

**ID**: WI-0447
**Goal**: Prevent projection-only/player-props run-state rows from affecting core `/cards` and `/games` game-lines surfaces so actionable cards cannot be blanked by non-canonical runs.

**Scope**:
- `WORK_QUEUE/WI-0447.md`
- `web/src/app/api/cards/route.ts`
- `web/src/app/api/cards/[gameId]/route.ts`
- `web/src/app/api/games/route.ts`
- `web/src/__tests__/api-cards-lifecycle-regression.test.js`
- `web/src/components/cards-page-client.tsx`

**Out of scope**:
- NHL props model math or scheduler cadence
- New UI player-props tab behavior
- DB migrations
- Worker job output schema changes

**Acceptance**:
- Core API read surfaces (`/api/cards`, `/api/cards/[gameId]`, `/api/games`) only consider canonical run-state sports for default active-run filtering.
- Non-canonical run-state rows (e.g. `nhl_props`) do not displace or contaminate core game-lines payloads.
- Existing lifecycle parity behavior remains unchanged.
- Regression test coverage asserts non-canonical run-state rows are ignored by core cards run-id selection.
- `/cards` lifecycle restore defaults to `pregame` unless URL explicitly requests `?lifecycle=active`, preventing stale session lifecycle from blanking cards.

**Owner agent**: codex
**Time window**: 2026-03-14T01:15:00Z -> TBD
**Coordination flag**: needs-sync

**Tests to run**:
- `node web/src/__tests__/api-cards-lifecycle-regression.test.js`
- `npm --prefix web run test:ui:cards`

**Manual validation**:
- Query `/api/games` with mixed run_state rows and confirm returned plays come from canonical core runs.
- Confirm `/cards` game filters are not blanked by projection-only props rows.

CLAIM: codex 2026-03-14T01:15:00Z

# WI-0506: NHL results segmentation on /results (game vs 1P vs player shots props)

**ID**: WI-0506
**Goal**: Add segmented settlement display on the existing `/results` surface (not a new route) for NHL game sides/totals, NHL 1P totals, and NHL player shots props.

**Depends on**:

- `WORK_QUEUE/WI-0502.md`

**Scope**:

- `web/src/app/api/results/route.ts`
- `web/src/app/results/page.tsx`
- `web/src/__tests__/api-results-flags.test.js`
- `web/src/__tests__/integration/games-pipeline-contract.test.ts`
- `web/src/__tests__/integration/settlement-parity.test.ts`
- `web/src/__tests__/ui-results-smoke.test.js`
- `docs/DATA_PIPELINE_TROUBLESHOOTING.md` (if result segmentation notes are required)
- `WORK_QUEUE/WI-0506.md`

**Out of scope**:

- New pages or routes for results
- Broad UI redesign
- Expansion beyond NHL player shots props

**Acceptance**:

1. `/api/results` returns segment metadata sufficient to separate NHL game totals/sides, NHL 1P totals, and NHL player shots props.
2. `/results` renders segmented sections on the same page with no new route.
3. Filters/presentation preserve existing game-pick behavior while adding player-prop segment visibility.
4. Settlement coverage metadata/header parity remains intact.
5. Tests are updated to cover segmentation contract and page rendering.

**Owner agent**: github-copilot
**Time window**: 2026-03-19T11:23:03Z -> TBD
**Coordination flag**: needs-sync

**Scope notes**:

- Shared docs coordination approved for `docs/DATA_PIPELINE_TROUBLESHOOTING.md` alongside WI-0486. Changes in this WI are limited to `/results` segmentation interpretation and must not alter Phase-4 soak cadence content.

**Tests to run**:

- `npm --prefix web test -- src/__tests__/api-results-flags.test.js`
- `npm --prefix web test -- src/__tests__/integration/games-pipeline-contract.test.ts`
- `npm --prefix web test -- src/__tests__/integration/settlement-parity.test.ts`
- `npm --prefix web test -- src/__tests__/ui-results-smoke.test.js`
- `npx --prefix web tsc --noEmit -p web/tsconfig.json`

**Manual validation**:

- Open `/results` and visually verify segmented sections for NHL game totals/sides, NHL 1P totals, and NHL player shots props.
- Validate at least one sample row appears in each segment when test data includes all three market families.
- Confirm existing game-only filters still behave as before and do not regress.

**Guard for WI closeout**:

- Before marking complete: attach API payload evidence showing segment metadata for all three NHL market families.
- Attach UI evidence (screenshots or equivalent) confirming same-page segmentation and no route changes.

CLAIM: github-copilot 2026-03-19T11:23:03Z

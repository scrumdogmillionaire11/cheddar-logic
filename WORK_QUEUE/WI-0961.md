# WI-0961: Restore NHL decision consistency fields before audit snapshotting

**ID**: WI-0961
**Goal**: Eliminate NHL invariant warnings for missing `consistency.pace_tier` and `consistency.event_env` by ensuring the publish/audit pipeline snapshots the finalized decision envelope, not a partially populated payload.

**Depends on**: WI-0907 (Phase 3 checkpoint: recovery-path-classification.md committed)

**Coordination**: WI-0907 owns recovery-path classification. WI-0961 must define which paths should trigger the consistency invariant check. MUST NOT execute both phase modifications to run_nhl_model.js in parallel. WI-0961 executor must rebase on WI-0907 output or escalate merge conflicts immediately.

**Scope**:
- `WORK_QUEUE/WI-0961.md`
- `apps/worker/src/jobs/run_nhl_model.js` — consistency field attachment order only (lines 1459-1530, not recovery paths)
- `apps/worker/src/utils/decision-publisher.js` — verify `ensureDecisionConsistencyEnvelope()` flow (read-only analysis first)
- `apps/worker/src/audit/audit_invariants.js` — snapshot invariant conditions (read-only analysis first)
- `apps/worker/src/jobs/__tests__/run_nhl_model.market-calls.test.js` — add consistency field population test
- `apps/worker/src/utils/__tests__/decision-publisher.v2.test.js` — verify existing tests still pass

**Out of scope** (owned by WI-0907):
- Recovery path classification updates (Phase 1-2)
- Execution-gate logic changes
- Decision-v2 watchdog modifications
- New audit channels or alert transport

**Acceptance**:
- **Phase 1**: Diagnostic document `.planning/debug/WI-0961-diagnosis.md` proves which code path(s) emit the warning. Must include: (a) reproducer seed, (b) call stack, (c) payload state at warning, (d) Wave1 eligibility result.
- **Phase 2**: Consistency fields are populated **before** `buildNhlModelSnapshot()` for publish-ready paths (post-WI-0907 Phase 3). Non-publish-ready paths skip warning or use fallback.
- **Phase 3**: Invariant timing guarantee documented + defensive check added if needed.
- **Phase 4**: Tests prove consistency envelope is derived before snapshot reads for all tested paths.
- **Phase 5**: Manual run shows no warnings for standard publish-ready totals.

**Execution plan**:

## Phase 1: Diagnose (UNBLOCKED—start immediately)

1. Find and reproduce: `npm --prefix apps/worker run job:run-nhl-model 2>&1 | grep "snapshot fields missing"`
2. Trace code path: determine which `buildNhlModelSnapshot()` call fires the warning
3. Check payload state: Is `payload.consistency.pace_tier` null? Does `isWave1EligiblePayload()` pass?
4. Deliverable: `.planning/debug/WI-0961-diagnosis.md` with reproducer, call stack, and hypothesis

## Phase 2: Verify flow (COORDINATED—wait for WI-0907 Phase 3)

1. Read `recovery-path-classification.md` to identify "publish-ready" definition
2. Verify `ensureDecisionConsistencyEnvelope()` runs before snapshot for publish-ready paths
3. Gate invariant check: skip for non-publish-ready paths or use fallback
4. Deliverable: Updated `buildNhlModelSnapshot()` with conditional logic

## Phase 3: Wire timing guarantee (INDEPENDENT)

1. Verify call order: `publishDecisionForCard()` → `attachNhlSnapshotAuditFields()`
2. Document guarantee in code comments
3. Add defensive check: call `ensureDecisionConsistencyEnvelope()` again if timing uncertain
4. Deliverable: Updated run_nhl_model.js with comments + optional defensive code

## Phase 4: Add tests (TDD)

1. Test consistency fields populated for standard path
2. Test snapshot invariant silent for publish-ready paths (no warning)
3. Test invariant respects recovery classification (skip for degraded/hidden)
4. Deliverable: All tests passing, no regressions

## Phase 5: Manual validation (FINAL)

1. Fresh run: `npm --prefix apps/worker run job:pull-odds && npm --prefix apps/worker run job:run-nhl-model 2>&1 | tee /tmp/nhl.log && grep "snapshot fields" /tmp/nhl.log`
2. Verify: No warnings for standard publish-ready totals
3. Test regressions: run decision-publisher + market-call + execution-gate test suites
4. Deliverable: Test results + manual log

**Owner agent**: unassigned
**Time window**: 2026-04-16 -> TBD (Phase 1 now; Phase 2+ blocked by WI-0907 Phase 3)
**Coordination flag**: needs-sync

**Merge order**: Rebase on WI-0907 Phase 3 commit before final PR. If conflict in run_nhl_model.js: WI-0907 owner wins.

CLAIM: unassigned

# WI-0961: Restore NHL decision consistency fields before audit snapshotting

**ID**: WI-0961
**Goal**: Eliminate NHL invariant warnings for missing `consistency.pace_tier` and `consistency.event_env` by ensuring the publish/audit pipeline snapshots the finalized decision envelope, not a partially populated payload.

**Depends on**: WI-0907

**Scope**:
- `WORK_QUEUE/WI-0961.md`
- `apps/worker/src/jobs/run_nhl_model.js`
- `apps/worker/src/utils/decision-publisher.js`
- `apps/worker/src/audit/audit_invariants.js`
- `apps/worker/src/jobs/__tests__/run_nhl_model.market-calls.test.js`
- `apps/worker/src/utils/__tests__/decision-publisher.v2.test.js`

**Out of scope**:
- NHL projection formula changes
- Sigma calibration changes
- New audit channels or alert transport

**Acceptance**:
- NHL runs no longer emit `snapshot fields missing ... consistency.pace_tier, consistency.event_env` for publish-ready totals cards produced by the standard path.
- If a card truly lacks required consistency context, the missing state is surfaced from one canonical place with deterministic fallback values or a tested blocking reason, not an incidental ordering bug.
- Decision-publisher tests prove the consistency envelope is derived before downstream audit/snapshot consumers read it.
- NHL market-call regression tests cover a real publish path, not just isolated helper behavior.

**Execution plan (deferred):**
- Phase 1: Reproduce invariant warnings and trace where consistency fields are first required vs first populated.
- Phase 2: Ensure decision consistency envelope is finalized before audit snapshot reads in NHL publish path.
- Phase 3: Centralize fallback source for `consistency.pace_tier` and `consistency.event_env` to avoid ordering race semantics.
- Phase 4: Add targeted tests in decision-publisher + NHL market-call pipeline for publish-ready totals cards.
- Phase 5: Execute pipeline test and manual NHL run to confirm warning removal with no behavior regressions.

**Owner agent**: unassigned
**Time window**: 2026-04-15 -> TBD
**Coordination flag**: needs-sync

**Tests to run**:
- `npm --prefix apps/worker run test -- src/jobs/__tests__/run_nhl_model.market-calls.test.js --runInBand`
- `npm --prefix apps/worker run test -- src/utils/__tests__/decision-publisher.v2.test.js --runInBand`
- `npm --prefix apps/worker run test:pipeline:nhl`

**Manual validation**:
- Run `npm --prefix apps/worker run job:run-nhl-model` after a fresh odds pull and confirm the prior invariant warnings for missing consistency fields are absent from standard totals-card output.

CLAIM: unassigned

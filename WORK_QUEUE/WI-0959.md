# WI-0959: Complete after-odds settlement chain

**ID**: WI-0959
**Goal**: Make the post-odds settlement sweep run the full guarded chain (`settle_game_results` -> `settle_projections` -> `settle_pending_cards`) so after-odds card settlement does not self-skip.

**Depends on**: WI-0783

**Scope**:

- `WORK_QUEUE/WI-0959.md`
- `apps/worker/src/jobs/pull_odds_hourly.js`
- `apps/worker/src/jobs/settle_projections.js`
- `apps/worker/src/jobs/__tests__/pull_odds_hourly.test.js`
- `apps/worker/src/jobs/__tests__/settle_projections.test.js`
- `apps/worker/src/jobs/__tests__/settle_pending_cards.ordering-guard.test.js`
- `apps/worker/src/__tests__/job-key-audit.test.js`

**Out of scope**:

- Settlement grading logic changes
- ESPN/Odds API matching heuristics
- POTD selection logic

**Acceptance**:

- The after-odds sweep triggered from `pull_odds_hourly.js` executes `settle_projections` before `settle_pending_cards` using aligned job-key suffixes.
- A successful after-odds run no longer logs `SKIP: settle_projections not yet SUCCESS for this window` for the immediately triggered card-settlement step.
- If game-result settlement is skipped or fails, projection/card settlement remains correctly guarded rather than running out of order.
- Regression tests cover hourly/nightly key derivation and the post-odds chain path.
- Worker audit tests accept the canonical after-odds key suffixes used by the new chain.

**Execution plan (deferred):**

- Phase 1: Reproduce current behavior in logs and capture baseline evidence for guard/order behavior in `pull_odds_hourly` plus settlement jobs.
- Phase 2: Implement deterministic job-key alignment and ordering guarantees between `settle_projections` and `settle_pending_cards`.
- Phase 3: Add or adjust unit tests for hourly and nightly suffix derivation plus guarded-chain execution.
- Phase 4: Run listed tests and record before/after log snippets in WI notes.
- Phase 5: Manual validation run in dev with settlement enabled; confirm no immediate guard-skip after odds pull.

**Owner agent**: github-copilot
**Time window**: 2026-04-15T00:00:00Z -> 2026-04-15T23:59:59Z
**Coordination flag**: solo

**Tests to run**:

- `npm --prefix apps/worker run test -- src/jobs/__tests__/pull_odds_hourly.test.js --runInBand`
- `npm --prefix apps/worker run test -- src/jobs/__tests__/settle_projections.test.js --runInBand`
- `npm --prefix apps/worker run test -- src/jobs/__tests__/settle_pending_cards.ordering-guard.test.js --runInBand`
- `npm --prefix apps/worker run test -- src/__tests__/job-key-audit.test.js --runInBand`

**Manual validation**:

- Run `npm --prefix apps/worker run job:pull-odds` in a dev environment with settlement enabled and confirm the after-odds sweep records a projections success before card settlement, with no immediate guard-skip for cards.

CLAIM: github-copilot 2026-04-15T00:00:00Z

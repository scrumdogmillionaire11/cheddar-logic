# WI-0873: Full-game moneyline model + market selector upgrade

**ID**: WI-0873
**Goal**: Add `projectFullGameML()` that derives full-game win probability from `projectFullGameTotal()` run differential, wire it into `computeMLBDriverCards()`, and upgrade `selectMlbGameMarket()` to choose the highest-edge market across F5 total, full-game total, and full-game ML.

**Scope**:
- `apps/worker/src/models/mlb-model.js`
- `apps/worker/src/jobs/run_mlb_model.js`
- `apps/worker/src/models/__tests__/mlb-model.test.js`

**Out of scope**:
- POTD integration (WI-0874)
- New DB tables or schema changes
- NHL/NBA models

**Depends on**: WI-0872 (needs `projectFullGameTotal()`)

**Acceptance**:
1. `projectFullGameML(homePitcher, awayPitcher, mlHome, mlAway, context = {})` added to `mlb-model.js`; returns `{ side, edge, projected_win_prob_home, tie_probability, confidence, used_full_model_path }`
2. Logistic coefficient for full-game is `0.55` (vs F5's `0.8`) reflecting more regression over 9 innings
3. Devig: raw implied probs normalized via two-sided normalization before computing edge
4. `computeMLBDriverCards()` produces `full_game_ml` card when `mlb.h2h_home` and `mlb.h2h_away` are both non-null
5. `selectMlbGameMarket()` evaluates all available markets (f5_total, full_game_total, full_game_ml) and returns the one with highest `Math.abs(edge) * confidence`; `chosen_market` and `why_this_market` updated accordingly
6. `run_mlb_model.js.resolveMlbFullGameTotalContext()` extended to also read h2h moneyline prices (`mlb.h2h_home`, `mlb.h2h_away`, `oddsSnapshot.moneyline_home`, `oddsSnapshot.moneyline_away`)
7. All existing MLB model tests pass; ≥ 3 new tests for full-game ML

**Owner agent**: unassigned
**Time window**: TBD
**Coordination flag**: solo

**Tests to run**:
- `npm --prefix apps/worker test -- --testPathPattern=mlb-model`
- `npm --prefix apps/worker test -- --testPathPattern=run_mlb_model`

**Manual validation**:
- With a snapshot containing `h2h_home: -130, h2h_away: +110`, `computeMLBDriverCards` should include a `full_game_ml` card; `selectMlbGameMarket` should return the highest-edge market

**Guard for WI closeout**:
- Before marking complete: both test patterns pass

CLAIM: unassigned

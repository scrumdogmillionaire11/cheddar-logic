# WI-0930: Reactivate MLB game lines (full-game total + moneyline)

**ID**: WI-0930
**Goal**: Reactivate MLB full-game total and full-game moneyline as odds-backed game-line lanes by restoring featured MLB odds pulls and removing the default projection-only assumption from the MLB scheduler, worker, and health/docs surfaces.

**Scope**:
- `WORK_QUEUE/WI-0930.md`
- `docs/decisions/ADR-0013-reactivate-mlb-game-lines.md`
- `packages/odds/src/config.js`
- `packages/odds/src/__tests__/config.season.test.js`
- `apps/worker/src/schedulers/mlb.js`
- `apps/worker/src/jobs/run_mlb_model.js`
- `apps/worker/src/jobs/check_pipeline_health.js`
- `apps/worker/src/jobs/__tests__/run_mlb_model.test.js`
- `apps/worker/src/__tests__/check-pipeline-health.mlb.test.js`
- `apps/worker/src/__tests__/scheduler-main-calibration.test.js`
- `web/src/lib/game-card/transform/index.ts`
- `web/src/__tests__/cards-projection-exclusion.test.js`
- `web/src/__tests__/game-card-transform-market-contract.test.js`
- `web/src/__tests__/game-card-transform-mlb-game-lines.test.js`
- `docs/MARKET_REGISTRY.md`
- `docs/ops-runbook.md`

**Out of scope**:
- MLB spread markets
- MLB F5 runtime behavior or settlement logic
- MLB pitcher-K runtime behavior or line sourcing
- `/results` UI/API changes beyond existing MLB full-game support
- Any changes outside the files listed in Scope

**Acceptance**:
- `SPORTS_CONFIG.MLB` is active by default during season with featured markets `['h2h', 'totals']` and token cost `2`.
- MLB scheduler defaults to odds-backed operation when `ENABLE_WITHOUT_ODDS_MODE` is false and only queues ESPN-direct seeding for true without-odds mode.
- `run_mlb_model` no longer force-switches into projection-only mode from inactive MLB odds config.
- Health checks treat MLB as an active-odds sport by default, expect full-game totals in MLB market-availability checks, and do not rely on ESPN-direct freshness as the normal MLB path.
- Game-mode transforms no longer surface stale MLB projection-only fallback rows as broken full-game cards, and legitimate MLB no-edge PASS rows do not get mislabeled as incomplete-data failures.
- Docs explicitly state that MLB full-game total and MLB full-game moneyline are active odds-backed lanes, while MLB F5 and MLB pitcher-K remain projection-only.
- Tests cover the new active-default configuration and preserve the projection-only exception behavior for F5 and pitcher-K.

**Owner agent**: gsd-executor
**Time window**: 2026-04-13T13:54:10Z -> TBD
**Coordination flag**: solo

**Tests to run**:
- `npm --prefix packages/odds run test -- --runInBand src/__tests__/config.season.test.js`
- `npm --prefix apps/worker run test -- --runInBand src/jobs/__tests__/run_mlb_model.test.js`
- `npm --prefix apps/worker run test -- --runInBand src/__tests__/check-pipeline-health.mlb.test.js`
- `npm --prefix apps/worker run test -- --runInBand src/__tests__/scheduler-main-calibration.test.js`
- `cd web && node --import tsx/esm src/__tests__/cards-projection-exclusion.test.js`
- `cd web && node --import tsx/esm src/__tests__/game-card-transform-market-contract.test.js`
- `cd web && node --import tsx/esm src/__tests__/game-card-transform-mlb-game-lines.test.js`

**Manual validation**:
- Run MLB odds pull plus `job:run-mlb-model` with live odds enabled and confirm `mlb-full-game` / `mlb-full-game-ml` can emit when thresholds are met.
- Confirm MLB F5 and MLB pitcher-K remain projection-only after the reactivation.

CLAIM: gsd-executor 2026-04-13T13:54:10Z

# WI-0618: Delete orphaned archive directories

**ID**: WI-0618
**Goal**: Remove `cheddar-fpl-sage/archive/` and `cheddar-fpl-sage/.planning/legacy/` from the repo. Content is preserved in git history.

**Background**:
Archive and legacy planning directories are committed to the main branch. These inflate git checkout size and confuse contributors about what is canonical. No production code references them.

**Scope**:
- Delete `cheddar-fpl-sage/archive/` (entire directory)
- Delete `cheddar-fpl-sage/.planning/legacy/` (entire directory)
- `.gitignore` — add `archive/` and `.planning/legacy/` under `cheddar-fpl-sage/` if not already covered

**Out of scope**:
- Any changes to active code in `cheddar-fpl-sage/`

**Acceptance**:
- Neither directory exists in the working tree after the commit.
- `git status` is clean.

**Owner agent**: unassigned
**Time window**: TBD
**Coordination flag**: solo

CLAIM: unassigned

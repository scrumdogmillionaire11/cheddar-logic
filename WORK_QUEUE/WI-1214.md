# WI-1214: Wire F5 Gray-Zone-Aware Proxy Settlement Writes

**ID**: WI-1214

**Goal**:
Wire gray-zone-aware proxy settlement into runtime writes so official and track-only outcomes are persisted with explicit reasoned semantics.

**Scope**:

- `apps/worker/src/jobs/settle_projections.js`
- `packages/data/src/db/projection-accuracy.js`
- `apps/worker/src/jobs/__tests__/settle_projections.test.js`
- `packages/data/__tests__/projection-accuracy-engine.test.js`

**Out of scope**:

- New public API fields in web route responses
- Dashboard/UI visualization changes
- Re-bucketing thresholds (contract is defined in WI-1213)

**Acceptance**:

- Settlement writes include explicit grading mode (`OFFICIAL` vs `TRACK_ONLY`) for MLB F5 proxy rows.
- Gray-zone rows are persisted for analytics/calibration but excluded from official side W/L aggregation inputs.
- Clear-zone rows continue to write official proxy settlement outcomes against `U3.5`/`O4.5`.
- Missing/invalid projection inputs fail closed with explicit reason codes instead of inferred side outcomes.
- Existing non-F5 proxy settlement behavior remains unchanged.

**Priority**: High

**Owner agent**: unassigned
**Time window**: 2026-04-29T00:00:00Z -> 2026-05-03T23:59:59Z
**Coordination flag**: needs-sync
**Depends on**: WI-1213

**Tests to run**:

- `npm --prefix apps/worker test -- src/jobs/__tests__/settle_projections.test.js --runInBand`
- `npm --prefix apps/worker test -- src/jobs/__tests__/projection_accuracy_health.test.js --runInBand`
- `npm --prefix packages/data test -- projection-accuracy-engine.test.js --runInBand`

**Manual validation**:

- Execute settlement for sample MLB F5 rows spanning clear and gray buckets.
- Confirm persisted rows show `TRACK_ONLY` in gray zone and `OFFICIAL` in clear zone.
- Confirm settlement job completion remains non-fatal if proxy insert path fails (existing guard behavior retained).

**Guard for WI closeout**:

- No path converts gray-zone rows into official W/L.
- Insert payload contains explicit bucket + grading-mode metadata.
- No unrelated file diffs outside scope.

CLAIM: unassigned 2026-04-29T00:00:00Z

---
phase: WI-1169
plan: "01"
type: rollout
wave: 2
depends_on:
  - WI-1168
files_modified:
  - web/src/app/api/cards/route.ts
  - web/src/lib/cards/query.ts
  - web/src/lib/cards/payload-classifier.ts
  - web/src/__tests__/cards-projection-exclusion.test.js
  - web/src/__tests__/api-cards-lifecycle-regression.test.js
  - web/src/__tests__/api-cards-sport-filter.test.js
  - WORK_QUEUE/WI-1169.md
autonomous: true
requirements:
  - WI-1169-CARDS-01
  - WI-1169-CARDS-02
  - WI-1169-CARDS-03
  - WI-1169-CARDS-04
must_haves:
  truths:
    - Simplified cards gatekeeping is controlled by one explicit runtime flag with a deterministic default and no ambiguous fallback behavior.
    - Shadow comparison emits legacy versus simplified counts grouped by card_type and drop_reason so rollout safety can be validated before cutover.
    - API consumers see stable response contract while rollout is staged and can be reverted by flag toggle without redeploy.
    - Legacy gate path is removed after validation, or temporarily retained with explicit rationale and removal trigger.
  artifacts:
    - path: web/src/app/api/cards/route.ts
      provides: Route-level flag switch, shadow comparison emission, and staged cutover control
      min_lines: 30
    - path: web/src/lib/cards/query.ts
      provides: Query behavior for simplified gate path and legacy-path parity comparison inputs
      min_lines: 30
    - path: web/src/lib/cards/payload-classifier.ts
      provides: Card-type and drop-reason classification used by shadow comparison reports
      min_lines: 20
    - path: web/src/__tests__/api-cards-lifecycle-regression.test.js
      provides: Regression coverage for staged rollout behavior and parity-safe cutover
      min_lines: 20
    - path: web/src/__tests__/cards-projection-exclusion.test.js
      provides: Projection exclusion behavior under legacy and simplified gate modes
      min_lines: 20
    - path: web/src/__tests__/api-cards-sport-filter.test.js
      provides: Sport-filter stability checks across gate modes
      min_lines: 20
  key_links:
    - from: web/src/app/api/cards/route.ts
      to: web/src/lib/cards/query.ts
      via: Route query construction routes requests through selected gate mode and runs optional shadow compare path
    - from: web/src/app/api/cards/route.ts
      to: web/src/lib/cards/payload-classifier.ts
      via: Route emits structured legacy versus simplified drop-reason telemetry using classifier outputs
    - from: web/src/lib/cards/query.ts
      to: web/src/__tests__/api-cards-lifecycle-regression.test.js
      via: Regression assertions validate parity-safe staged cutover and revert behavior
    - from: web/src/lib/cards/payload-classifier.ts
      to: web/src/__tests__/cards-projection-exclusion.test.js
      via: Projection exclusion tests assert expected drop-reason grouping in shadow telemetry
    - from: web/src/app/api/cards/route.ts
      to: web/src/__tests__/api-cards-sport-filter.test.js
      via: Sport-lane contract remains stable when toggling legacy and simplified gate paths
---

# WI-1169: Cards Gate Simplification Rollout, Flagged Cutover, And Legacy Path Removal

**ID**: WI-1169
**Goal**: Roll out simplified cards gatekeeping behind a feature flag, compare old vs new behavior, and retire superseded legacy pathing after validation.

**Requirements**: [WI-1169-CARDS-01, WI-1169-CARDS-02, WI-1169-CARDS-03, WI-1169-CARDS-04]

**Roadmap traceability**:

- Extends WI-1168 run-scope and settled-scope hardening with a rollout-focused gate simplification pass.
- Dependency gate: implementation tasks in this item begin only after WI-1168 acceptance and test baseline is confirmed.

**Scope**:

- `web/src/app/api/cards/route.ts`
- `web/src/lib/cards/query.ts`
- `web/src/lib/cards/payload-classifier.ts`
- `web/src/__tests__/cards-projection-exclusion.test.js`
- `web/src/__tests__/api-cards-lifecycle-regression.test.js`
- `web/src/__tests__/api-cards-sport-filter.test.js`
- `WORK_QUEUE/WI-1169.md`

**Out of scope**:

- Product redesign of cards UI
- Worker model threshold tuning
- Non-cards API endpoint behavior changes

**Acceptance**:

- [WI-1169-CARDS-01] Simplified gate path is controllable by explicit runtime flag with deterministic default.
- [WI-1169-CARDS-02] Shadow comparison telemetry reports old vs new counts by card_type and drop reason.
- [WI-1169-CARDS-03] Legacy gate branch is removed (or explicitly retained with temporary rationale and removal condition).
- [WI-1169-CARDS-04] Rollout can be executed in stages with zero consumer contract breaks and flag-based rollback.

**Owner agent**: github-copilot
**Time window**: 2026-04-24T00:00:00Z -> TBD
**Coordination flag**: needs-sync
**Depends on**: WI-1168

## Execution tasks

<tasks>

<task type="auto">
  <name>Task 0: Enforce WI-1168 prerequisite baseline before rollout work</name>
  <requirements>WI-1169-CARDS-01, WI-1169-CARDS-04</requirements>
  <files>WORK_QUEUE/WI-1169.md</files>
  <action>
    Treat WI-1168 as a hard prerequisite.

    Validate WI-1168 acceptance behavior remains intact so WI-1169 changes are purely rollout and gate simplification work.

    Record this dependency gate in this work item to keep needs-sync sequencing deterministic.
  </action>
  <verify>
    <automated>npm --prefix web run test:cards:projection-exclusion && npm --prefix web run test:cards-lifecycle-regression && npm --prefix web run test:cards-sport-filter && npm --prefix web run test:api:nhl-contract</automated>
  </verify>
  <done>
    WI-1168 behavior is verified as baseline and WI-1169 tasks proceed only after dependency checks pass.
  </done>
</task>

<task type="auto">
  <name>Task 1: Add deterministic simplified-gate runtime flag and staged mode selection</name>
  <requirements>WI-1169-CARDS-01, WI-1169-CARDS-04</requirements>
  <files>web/src/app/api/cards/route.ts, web/src/lib/cards/query.ts, web/src/lib/cards/payload-classifier.ts</files>
  <action>
    Implement a single explicit runtime flag for simplified gatekeeping with deterministic default behavior.

    Ensure route and query path selection cleanly supports staged rollout modes without changing API response contract.

    Keep sport-lane compatibility behavior stable while selecting legacy or simplified path.
  </action>
  <verify>
    <automated>npm --prefix web run test:cards-sport-filter && npm --prefix web run test:api:nhl-contract</automated>
  </verify>
  <done>
    Flag behavior is deterministic, route-query wiring supports staged mode selection, and consumer contract remains unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement shadow comparison telemetry for legacy versus simplified outcomes</name>
  <requirements>WI-1169-CARDS-02, WI-1169-CARDS-04</requirements>
  <files>web/src/app/api/cards/route.ts, web/src/lib/cards/query.ts, web/src/lib/cards/payload-classifier.ts, web/src/__tests__/api-cards-lifecycle-regression.test.js, web/src/__tests__/cards-projection-exclusion.test.js</files>
  <action>
    Add shadow-compare execution path that computes legacy and simplified gate outcomes in parallel when enabled.

    Emit structured telemetry for legacy_count versus simplified_count grouped by card_type and drop_reason.

    Extend regression tests to assert telemetry schema and parity expectations so rollout decisions are evidence-backed.
  </action>
  <verify>
    <automated>npm --prefix web run test:cards:projection-exclusion && npm --prefix web run test:cards-lifecycle-regression</automated>
  </verify>
  <done>
    Shadow comparison telemetry includes count deltas by card_type and drop_reason and tests prove the telemetry contract.
  </done>
</task>

<task type="auto">
  <name>Task 3: Retire legacy gate branch after validation and preserve rollback safety</name>
  <requirements>WI-1169-CARDS-03, WI-1169-CARDS-04</requirements>
  <files>web/src/app/api/cards/route.ts, web/src/lib/cards/query.ts, web/src/lib/cards/payload-classifier.ts, web/src/__tests__/api-cards-lifecycle-regression.test.js, web/src/__tests__/api-cards-sport-filter.test.js, WORK_QUEUE/WI-1169.md</files>
  <action>
    Remove superseded legacy gate branch once telemetry validation confirms cutover safety.

    If temporary retention is required, document rationale and explicit removal trigger in this work item.

    Validate rollback by confirming gate mode can still be switched by runtime flag without redeploy.
  </action>
  <verify>
    <automated>npm --prefix web run test:cards:projection-exclusion && npm --prefix web run test:cards-lifecycle-regression && npm --prefix web run test:cards-sport-filter && npm --prefix web run test:api:nhl-contract</automated>
  </verify>
  <done>
    Legacy gate path is removed or explicitly time-boxed with rationale, and staged rollback remains operational via flag toggle.
  </done>
</task>

</tasks>

**Tests to run**:

- `npm --prefix web run test:cards:projection-exclusion`
- `npm --prefix web run test:cards-lifecycle-regression`
- `npm --prefix web run test:cards-sport-filter`
- `npm --prefix web run test:api:nhl-contract`

**Manual validation**:

- Run with legacy and simplified flags toggled and compare result counts and card types for `/api/cards?sport=nhl`.
- Run with shadow comparison enabled and verify telemetry includes legacy_count and simplified_count grouped by card_type and drop_reason.
- Confirm staged rollout can be reverted by flag without redeploying code.


CLAIM: github-copilot 2026-04-24T00:00:00Z

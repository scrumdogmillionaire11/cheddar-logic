# WI-1139: Add Frozen-Domain Fail-Closed Runtime Guards (NFL/FPL)

**ID**: WI-1139
**Goal**: Prevent frozen or archived domains from being reactivated by env drift by enforcing explicit fail-closed guards in scheduler and job entrypoints.

**Scope**:

- apps/worker/src/schedulers/main.js
- apps/worker/src/schedulers/nfl.js
- apps/worker/src/schedulers/fpl.js
- apps/worker/src/jobs/run_nfl_model.js
- apps/worker/src/jobs/run_fpl_model.js

**Out of scope**:

- Rebuilding NFL/FPL models
- Changes to active MLB/NHL/NBA scheduling policies
- Product/UX changes in web routes

**Acceptance**:

- Frozen domains are blocked at scheduler enqueue stage with explicit reason logs.
- Frozen domains are blocked at job entrypoint stage as a second line of defense.
- Guard behavior is deterministic and testable for both enabled and disabled env combinations.
- Existing active domain schedulers remain unchanged in behavior.

**Owner agent**: unassigned
**Time window**: 2026-04-22T11:21:22Z -> TBD
**Coordination flag**: solo
**Depends on**: none

**Tests to run**:

- node apps/worker/src/\_\_tests\_\_/scheduler-windows.test.js
- npm --prefix apps/worker run test -- src/schedulers/\_\_tests\_\_/player-props.test.js --runInBand
- rg -n "frozen|disabled|fail-closed|ENABLE_NFL|ENABLE_FPL" apps/worker/src/schedulers/main.js apps/worker/src/schedulers/nfl.js apps/worker/src/schedulers/fpl.js apps/worker/src/jobs/run_nfl_model.js apps/worker/src/jobs/run_fpl_model.js

**Manual validation**:

- Run worker with NFL/FPL disabled and verify no enqueue occurs for those jobs in scheduler logs.
- Toggle an override env for local verification and confirm explicit guard behavior (allow or deny) matches contract.

**Guard for WI closeout**:

- Before marking complete: confirm every test command in Tests to run is runnable from repo root.
- Add or update focused scheduler/job tests if guard logic changes branch behavior.

CLAIM: unassigned 2026-04-22T11:21:22Z

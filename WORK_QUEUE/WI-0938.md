# WI-0938: Market Board Program Umbrella (The Wedge + The Board)

**ID**: WI-0938
**Goal**: Track the product split into two sibling surfaces: The Wedge (picks) and The Board (market intelligence) under one authoritative work item.

**Depends on**: none

**Scope**:

- `.planning/phases/market-board/market-board-01-PLAN.md`
- `.planning/phases/market-board/market-board-02-PLAN.md`
- `web/src/app/wedge/**`
- `web/src/app/board/**`
- `web/src/app/cards/page.tsx`
- `web/src/app/page.tsx`
- `web/src/__tests__/ui-potd-smoke.test.js`
- `web/src/components/cards/**`
- `web/src/components/play-of-the-day-client.tsx`
- `web/src/components/global-stale-asset-guard.tsx`
- `web/src/app/api/board/**`
- `web/src/lib/api-security/validation.ts`

**Out of scope**:

- Changing model formulas, thresholds, or bet selection policy
- Renaming existing `/api/cards` contract
- Worker-side odds ingestion architecture changes
- Bankroll policy updates

**Canonical References**:

- Value Proposition & Requirements: `docs/THE_BOARD_VALUE_ONE_PAGER.md`
- Edge Reasoning Data Schema: `WORK_QUEUE/THE_BOARD_EDGE_SCHEMA_SPEC.md`

**Acceptance**:

- WI-0938-01 (PLAN) exists and describes architecture, feature flag strategy, and phased delivery
- WI-0938-02 (SKELETON) is complete: `/board` route live (behind feature flag), empty tabs rendered
- WI-0938-03 (OPPORTUNITIES TAB) is complete: card component with edge reasoning data
- WI-0938-04 (BLOCKED EDGES TAB) is complete: blocked edge component with unlock conditions
- WI-0938-05 (EDGE TYPE TRACKER) is complete: pattern performance aggregation
- WI-0938-06 (EDGE REASONING LAYER) is complete: enricher function + backend integration
- WI-0938-07 (INTEGRATION + POLISH) is complete: Discord formatter, feature flag removal, production ready

**Owner agent**: codex
**Time window**: 2026-04-14 -> TBD
**Coordination flag**: needs-sync

**Tests to run**:

- `npm --prefix web run build`
- `node web/src/__tests__/ui-potd-smoke.test.js`

**Manual validation**:

- Confirm both plan files exist and describe distinct surfaces:
  - The Wedge (picks route and naming)
  - The Board (market-state intelligence route and tabs)

**Notes**:

- Market Pulse is the starting reference surface for The Board rebuild and should inform Board information architecture.
- Board market-state modules must display current deterministic scan output, not stale or previously favorable odds retained from older snapshots.
- Any Market Pulse-derived board API or UI should prefer the latest snapshot per game and avoid caching that can outlive active price movement.
- Treat Market Pulse as reusable board intelligence infrastructure, not as a separate one-off prototype to be duplicated.

CLAIM: codex 2026-05-05T00:00:00-04:00

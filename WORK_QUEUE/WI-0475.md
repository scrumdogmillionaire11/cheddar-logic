# WI-0475: Inefficient Model Replacement Policy + Ops Runbook

**ID**: WI-0475
**Goal**: Define and operationalize objective replacement/demotion criteria for inefficient models using projection win-rate and CLV telemetry, with a safe rollback/kill-switch runbook.

**Depends on**:

- `WORK_QUEUE/WI-0472.md`
- `WORK_QUEUE/WI-0474.md`

**Scope**:

- `WORK_QUEUE/WI-0475.md`
- `docs/DATA_PIPELINE_TROUBLESHOOTING.md`
- `docs/ARCHITECTURE_SEPARATION.md`
- `docs/API_BASELINES.md`
- `README.md`

**Out of scope**:

- Building a new model architecture from scratch
- Replacing web card UI components
- Bulk historical backfill migration
- Global refactors outside documentation and runbook criteria

**Acceptance**:

1. Runbook defines model inefficiency triggers (minimum sample size, rolling win-rate floor, CLV degradation threshold, and action matrix).
2. Runbook maps each trigger to allowed actions (`demote thresholds`, `disable market via flag`, `rollback`), including owner and verification steps.
3. Documentation includes phased enablement sequence and production-safe rollback commands.
4. Team can execute the runbook end-to-end without code changes.

**Owner agent**: unassigned
**Time window**: 2026-03-16T00:00:00Z -> TBD
**Coordination flag**: solo

**Tests to run**:

- `npm --prefix web run test:api:games:market`
- `npm --prefix web run test:card-decision`
- `npm --prefix apps/worker run scheduler`

**Manual validation**:

- Dry-run the runbook in staging: enable one phase flag, verify telemetry query, then rollback.
- Confirm documented thresholds and SQL checks match live schema names in `projection_perf_ledger` and `clv_ledger`.

**Guard for WI closeout**:

- Before marking complete: confirm every test command in `Tests to run` is runnable from repo root.
- Include copy-paste-ready operational commands for enable, verify, and rollback.

CLAIM: unassigned

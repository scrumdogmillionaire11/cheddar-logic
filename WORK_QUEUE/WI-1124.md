# WI-1124: Fail-Closed Auth Policy For Protected Surfaces

**ID**: WI-1124
**Goal**: Enforce fail-closed authentication and entitlement checks for protected web pages and API routes.

**Scope**:
- web/src/lib/games/route-handler.ts
- web/src/app/api/cards/route.ts
- web/src/app/api/cards/[gameId]/route.ts
- web/src/app/wedge/page.tsx
- web/src/app/fpl/page.tsx
- web/src/lib/api-security/config.ts
- web/src/__tests__/api-auth-walls-default-deny.test.js
- web/src/__tests__/page-auth-wall-contract.test.js
- web/src/__tests__/api-auth-walls-default-deny.test.js
- web/src/__tests__/page-auth-wall-contract.test.js

**Out of scope**:
- Admin endpoint policy redesign
- Payment and subscription product logic changes

**Acceptance**:
- Protected surfaces deny access by default when auth wall config is missing or false.
- Any intentionally public route/page requires explicit allow-public flag and is documented in code.
- Existing card and games route contracts continue to pass non-auth regression checks.

**Owner agent**: unassigned
**Time window**: 2026-04-21T00:00:00Z -> TBD
**Coordination flag**: needs-sync
**Depends on**: none (Wave 1 root)

**Tests to run**:
- node web/src/__tests__/api-auth-walls-default-deny.test.js
- node web/src/__tests__/page-auth-wall-contract.test.js
- npm --prefix web run test:api:cards:security

**Manual validation**:
- Verify unauthenticated requests to protected routes receive 401 or 403 when auth walls are enabled by policy.
- Verify wedge and fpl page behavior matches intended public or protected posture explicitly.

**Guard for WI closeout**:
- Before marking complete: confirm every test command in Tests to run is runnable from repo root (i.e., exists in web/package.json scripts or is a valid node path/to/test.js command).
- If a script is added to web/package.json as part of this WI, add it to this WI's Tests section with the full npm --prefix web run <script> form.

CLAIM: unassigned 2026-04-21T00:00:00Z

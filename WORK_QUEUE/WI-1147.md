# WI-1147: Results Nav Shell — Add /results/projections Route And Tab Navigation

**ID**: WI-1147
**Goal**: Introduce the structural split between betting results and projections by adding a `/results/projections` route skeleton and a shared tab nav, with no behavior change to the existing `/results` page.

**Scope**:
- web/src/app/results/layout.tsx
- web/src/app/results/projections/page.tsx (new)
- web/src/app/results/projections/layout.tsx (new, metadata only)
- web/src/components/results/ResultsNav.tsx (new)

**Out of scope**:
- Moving any data-fetching or sections out of the existing results page
- Changing the `/api/results` response shape
- Any new filters or controls

**Acceptance**:
- Navigating to `/results` renders the existing page unchanged, plus a tab nav with "Betting" and "Projections" links.
- Navigating to `/results/projections` renders the tab nav plus a placeholder body (e.g. "Projections coming soon").
- The active tab is visually indicated based on the current route (`usePathname`).
- `ResultsNav` is a client component; layout remains a server component.
- No existing tests are broken.

**Owner agent**: unassigned
**Time window**: TBD
**Coordination flag**: solo
**Depends on**: none

**Tests to run**:
- npm --prefix web run test:ui:results
- npm --prefix web run lint
- npm --prefix web run type-check

**Manual validation**:
- Visit `/results` — existing content renders, "Betting" tab is active.
- Visit `/results/projections` — tab nav renders, "Projections" tab is active, placeholder body shows.
- Tab links navigate between pages without full reload (Next.js client nav).

**Guard for WI closeout**:
- Before marking complete: confirm every test command in Tests to run is runnable from repo root.
- Confirm no visual regression on existing `/results` page content.

# WI-0308: Deterministic Filters, Dynamic Presets, and Debug Payloads

## Superseded Notice

- Status: Superseded
- Superseded by: WI-0311, WI-0312
- Date: 2026-03-05
- Reason: moved to canonical pipeline + legacy cleanup + regression test track

---

**ID**: WI-0308

**Goal**: Make quick filters stable and debuggable; add goalie/recent-game info; support dynamic sport presets from snapshot.

**Scope**:

- `web/src/lib/game-card/filters.ts` (stabilize sorting: FIRE first, then WATCH, then time)
- `web/src/lib/game-card/presets.ts` (generate sport presets from snapshot instead of hard-coded list)
- `apps/worker/src/models/welcome-home-v2.js` (inject recent_games array into payload)
- `apps/worker/src/models/index.js` (NHL: expose goalie fields in play payload)
- `web/src/app/api/games/route.ts` (flatten goalie, recent_games into play JSON)
- `packages/data/db/migrations/020_add_pass_reason_categories.sql` (comment-only seed for pass reasons if table needed; can defer)

**Out of scope**:

- Totals missing-reason deep audit (separate P2 pipeline audit WI)
- Pass-reason drill-down UI (just expose in API, no cascading dropdown)
- Multi-model disagreement metrics (separate WI)
- Historical pass-reason conversion (start fresh)

**Acceptance**:

- FIRE plays sort first, WATCH second, others third; secondary sort by game start time (deterministic).
- "Next 4 hours" filter: config is ET (America/New_York); tests use frozen Date mock.
- Sport preset buttons only appear if snapshot has >= 1 card for that sport.
- NHL plays include `goalie_home_name`, `goalie_home_status` (CONFIRMED|EXPECTED|UNKNOWN), and same for away.
- Welcome Home card includes `recent_games`: array of last 3 games with opponent, home/away, date, travel_note.
- Pass reasons categorized: `PASS_NO_VALUE`, `PASS_INSUFFICIENT_DATA`, `PASS_MODEL_NOT_RUN`, `PASS_OTHER`.

**Owner agent**: unassigned

**Time window**: 2026-03-07T11:00:00Z - 2026-03-08T10:00:00Z

**Coordination flag**: none (UI only; API additions non-breaking)

**Tests to run**:

- `npm --prefix web run test -- lib/game-card/filters.test.ts`
- `npm --prefix web run test -- lib/game-card/presets.test.ts`
- New test: `npm --prefix apps/worker run test -- models/welcome-home-v2.test.js --testNamePattern="recent_games"` (see WI-0308-TEST-SPIKE.md)
- Manual: render preset buttons, count visible sports, verify all are in snapshot with >= 1 card.
- Manual: set system time to 2026-02-15 14:00 ET, filter "Next 4 hours", verify games 14:00–18:00 ET only.

**Manual validation**:

- Sort order correct: FIRE plays render above WATCH; WATCH above PASS.
- NHL game sample from `/api/games`: goalie fields present and not null/empty.
- Welcome Home card: recent_games array has 3 entries, each with opponent name and date.
- Preset buttons: only sports with cards in current snapshot show buttons; "NHL Totals Only" hides if NHL has 0 cards.

---

## Implementation Checklist

### Phase 1: Filter Sorting Stability

- [ ] Update `web/src/lib/game-card/filters.ts`
  - [ ] In `getPlayDisplayAction()` or `sortCards()`, implement:
    - [ ] Sort by status: FIRE, WATCH, PASS, others (order maintained)
    - [ ] Secondary sort by `card.startTime` (game start time, soonest first)
    - [ ] Use stable sort: `Array.sort((a, b) => {...})`
  - [ ] Test stable sort with mock data: 3 FIRE, 2 WATCH, 1 PASS (verify order never changes)

### Phase 2: Dynamic Sport Presets

- [ ] Update `web/src/lib/game-card/presets.ts`
  - [ ] Modify `getPresetsForMode(mode)`: instead of hard-coded array, generate from snapshot
  - [ ] Logic: if `games.length > 0`, compute `availableSports = new Set(games.map(g => g.sport))`
  - [ ] Filter GAME_PRESETS by sport availability: only show preset if sport in availableSports
  - [ ] Example: "NHL Totals Only" only appears if snapshot has >= 1 NHL card
  - [ ] Test: with snapshot of only NBA cards, verify NHL/NCAAM presets hidden

### Phase 3: Timezone-Aware Time Window

- [ ] Update time-window filter in `filters.ts`
  - [ ] "Next 4 hours" config: hardcode timezone to ET (America/New_York)
  - [ ] Compute `now = new Date()` in ET timezone (use Intl.DateTimeFormat or date-fns)
  - [ ] Filter games where `startTime >= now` and `startTime <= now + 4h` (in ET)
  - [ ] Add comment: `// All times in America/New_York timezone`
  - [ ] Test with frozen date: `jest.useFakeTimers(); jest.setSystemTime(new Date('2026-02-15T14:00:00-05:00'));`

### Phase 4: Welcome Home Recent Games

- [ ] Update `apps/worker/src/models/welcome-home-v2.js`
  - [ ] After computing road-trip logic, fetch last 3 games for team:
    - [ ] SQL: `SELECT g.* FROM games g WHERE (g.home_team = ? OR g.away_team = ?) AND g.game_time_utc < ? ORDER BY g.game_time_utc DESC LIMIT 3`
  - [ ] For each, build: `{opponent, home_away (HOME if home_team, AWAY if away_team), date, travel_note}`
  - [ ] Add to play payload: `recent_games: [{opponent, home_away, date, travel_note}, ...]`
  - [ ] Test: mock game history, verify array has 3 entries

### Phase 5: NHL Goalie Fields

- [ ] Update `apps/worker/src/models/index.js` (or specific NHL model file)
  - [ ] Extract goalie data from raw_data (already fetched by MoneyPuck enrichment)
  - [ ] Path: `raw_data.goalie.home.name`, `raw_data.goalie.home.status` (if available)
  - [ ] For each play, add: `goalie_home_name, goalie_home_status, goalie_away_name, goalie_away_status`
  - [ ] Status values: CONFIRMED, EXPECTED, UNKNOWN (based on data availability)
  - [ ] Test: verify goalie fields in sample NHL card payload

### Phase 6: API Flattening

- [ ] Update `web/src/app/api/games/route.ts`
  - [ ] In play transformation, flatten goalie fields and recent_games into top-level play object
  - [ ] Ensure fields survive JSON serialization
  - [ ] Test: sample NHL play, verify goalie fields present

### Phase 7: Pass Reason Categories

- [ ] Update `web/src/app/api/games/route.ts`
  - [ ] Add logic to categorize pass reasons:
    - [ ] If `edge < threshold`: PASS_INSUFFICIENT_EDGE → NO_VALUE_* (edge checker handles)
    - [ ] If missing drivers/data: PASS_INSUFFICIENT_DATA
    - [ ] If model not run yet: PASS_MODEL_NOT_RUN (check run_id/status)
    - [ ] Default: PASS_OTHER
  - [ ] Add to play: `pass_reason_category: 'PASS_INSUFFICIENT_DATA'` (if action='PASS')

### Phase 8: Testing

- [ ] Write filter tests (see WI-0308-TEST-SPIKE.md)
- [ ] Test dynamic presets: with snapshot of only NBA, verify presets hidden for NHL
- [ ] Test time-window: freeze date to 2026-02-15 14:00 ET, filter "next 4h", verify games 14:00–18:00
- [ ] Test goalie fields: sample NHL game, verify goalie names present
- [ ] Test recent games: sample Welcome Home card, verify recent_games array with 3 entries

### Phase 9: Validation

- [ ] Sort order: FIRE plays render above WATCH in UI
- [ ] Sport presets: only visible sports shown as buttons
- [ ] Time window: "next 4h" filter in UI shows only games in next 4 hours ET
- [ ] NHL: goalie names visible in card UI
- [ ] Welcome Home: recent games expandable/visible in card

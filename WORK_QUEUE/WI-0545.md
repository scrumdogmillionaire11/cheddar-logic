# WI-0545: Side-Flip Historical Backtest + Profile Decision

**ID**: WI-0545
**Goal**: Complete `WI-0539` by running historical flip backtests (baseline/moderate/aggressive), selecting one profile, and codifying the decision.

**Scope**:

- `WORK_QUEUE/WI-0545.md`
- `packages/models/src/decision-gate.js` (only if selected profile requires change)
- `apps/worker/src/utils/decision-publisher.js` (only if instrumentation needed)
- `apps/worker/src/jobs/report_flip_threshold_backtest.js` (new)
- `apps/worker/src/utils/__tests__/decision-gate.flip-threshold.test.js`
- `docs/API_BASELINES.md`

**Out of scope**:

- NHL ML calibration
- DB schema migration unless strictly required

**Acceptance**:

- Backtest command produces flip_count/blocked_count and downstream quality proxy for three profiles.
- One profile is selected and documented with measured tradeoff.
- Decision-gate reason fields remain unit-explicit (`decimal_fraction` semantics).
- Tests cover chosen profile behavior + invariant guards.

**Owner agent**: unassigned
**Time window**: 2026-03-21T18:00:00Z -> TBD
**Coordination flag**: needs-sync

**Tests to run**:

- `npm --prefix apps/worker test -- src/utils/__tests__/decision-gate.flip-threshold.test.js --no-coverage`
- `npm --prefix apps/worker test -- src/utils/__tests__/decision-publisher.v2.test.js --no-coverage`
- `node apps/worker/src/jobs/report_flip_threshold_backtest.js --days 120 --json`

**Manual validation**:

- Confirm selected profile is reflected in docs and, if changed, in live gate constants with matching units.

CLAIM: unassigned

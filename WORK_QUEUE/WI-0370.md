# WI-0370: NHL Settlement Enrollment + Sport Casing Fix

**ID**: WI-0370
**Goal**: Ensure NHL PLAY/LEAN cards consistently enroll into settlement by fixing `card_results` sport casing on insert and adding guardrail tests for settlement eligibility.

**Scope**:

- `WORK_QUEUE/WI-0370.md`
- `WORK_QUEUE/WI-0371.md`
- `WORK_QUEUE/README.md`
- `packages/data/src/db.js`
- `packages/data/__tests__/card-payload-sport.test.js`
- `apps/worker/src/jobs/settle_pending_cards.js`
- `apps/worker/src/__tests__/settle-coverage.test.js`
- `apps/worker/src/jobs/__tests__/run_nhl_model.test.js`

**Out of scope**:

- Reclassifying historical `official_status=PASS` cards as settlement-eligible plays
- Adding edge/profit-loss analytics beyond win/loss tracking
- Frontend presentation changes outside settlement parity validation

**Acceptance**:

- New NHL PLAY/LEAN payloads write `card_results` rows reliably with valid lowercase `sport` values.
- Settlement enrollment remains restricted to PLAY/LEAN market-contract rows and includes eligible NHL rows when games finalize.
- Automated coverage exists for (a) sport casing persistence and (b) NHL settlement eligibility path.

**Owner agent**: GitHub Copilot
**Time window**: 2026-03-10T00:00:00Z -> TBD
**Coordination flag**: needs-sync

**Tests to run**:

- `npm --prefix packages/data test -- __tests__/card-payload-sport.test.js`
- `npm --prefix apps/worker test -- __tests__/run_nhl_model.test.js`
- `npm --prefix apps/worker test -- __tests__/settle-coverage.test.js`

**Manual validation**:

- Run NHL model once and verify new PLAY/LEAN cards have corresponding pending `card_results` rows.
- Run settlement after NHL final scores exist and verify eligible NHL cards transition from `pending` to `settled`.

**Implementation evidence (2026-03-10)**:

- `npm --prefix packages/data test -- __tests__/card-payload-sport.test.js` ✅ pass (1/1)
- `npm --prefix apps/worker test -- __tests__/run_nhl_model.test.js` ✅ pass (6/6)
- `npm --prefix apps/worker test -- __tests__/settle-coverage.test.js` ✅ pass (2/2)
- Verified persistence path in `packages/data/src/db.js`: `insertCardResult` now normalizes `sport` to lowercase before insert.
- Verified regression coverage:
  - `packages/data/__tests__/card-payload-sport.test.js` asserts lowercase `sport` in both `card_payloads` and auto-enrolled `card_results`.
  - `apps/worker/src/jobs/__tests__/run_nhl_model.test.js` asserts enrolled `card_results.sport` rows are lowercase `nhl`.
  - `apps/worker/src/__tests__/settle-coverage.test.js` includes NHL displayed+final eligible row and confirms settlement path.

CLAIM: GitHub Copilot 2026-03-10T00:00:00Z

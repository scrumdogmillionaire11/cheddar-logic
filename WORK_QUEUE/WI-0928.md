# WI-0928: Remove DATABASE_PATH and RECORD_DATABASE_PATH fallbacks from db-path.js

**ID**: WI-0928
**Goal**: Remove the `DATABASE_PATH` and `RECORD_DATABASE_PATH` env-var fallback branches from `packages/data/src/db-path.js` — `CHEDDAR_DB_PATH` is the canonical var and neither legacy var is set in any deployment.

**Scope**:

- `packages/data/src/db-path.js`
- `apps/worker/src/__tests__/db-path-integration.test.js` — remove test cases that assert `DATABASE_PATH` fallback behavior

**Out of scope**:

- `.env` files — `DATABASE_PATH` is already absent in production; no env changes needed
- `CHEDDAR_DATA_DIR` handling — leave untouched
- Any other resolution logic in `db-path.js` beyond the two legacy branches

**Acceptance**:

- Lines referencing `env.DATABASE_PATH` and `env.RECORD_DATABASE_PATH` removed from `db-path.js`
- `grep -n "DATABASE_PATH\|RECORD_DATABASE_PATH" packages/data/src/db-path.js` returns 0 matches
- `npm --prefix packages/data run test -- --no-coverage` passes
- `npm --prefix apps/worker run test -- --no-coverage` passes (db-path integration tests updated)

**Owner agent**: unassigned
**Time window**: TBD
**Coordination flag**: needs-sync

**Tests to run**:

- `npm --prefix packages/data run test -- --no-coverage`
- `npm --prefix apps/worker run test -- --no-coverage`

**Manual validation**:

- Start worker locally with only `CHEDDAR_DB_PATH` set; confirm DB path resolves correctly

CLAIM: unassigned

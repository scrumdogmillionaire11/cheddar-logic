# WI-0882: Fix getActivePotdSports to respect active:false in SPORTS_CONFIG

**ID**: WI-0882
**Goal**: getActivePotdSports() in run_potd_engine.js does not check
ODDS_SPORTS_CONFIG[sport].active -- it only checks the object is truthy and the
env var is not "false". Sports with active:false sneak into the POTD pipeline.
MLB and NFL already have active:false in packages/odds/src/config.js (lines 65, 86),
so this bug is live — not merely latent. Any env without an explicit ENABLE_MLB_MODEL=false
or ENABLE_NFL_MODEL=false is currently routing those sports into the POTD pipeline.

**Scope**:
- apps/worker/src/jobs/potd/run_potd_engine.js
- apps/worker/src/jobs/potd/__tests__/run-potd-engine.test.js

**Out of scope**:
- Changing SPORTS_CONFIG values for any sport
- Any other logic in run_potd_engine.js

**Change**:
In getActivePotdSports(), change the filter from:
  .filter(([sport, envKey]) => ODDS_SPORTS_CONFIG[sport] && process.env[envKey] !== "false")
to:
  .filter(([sport, envKey]) => ODDS_SPORTS_CONFIG[sport] && ODDS_SPORTS_CONFIG[sport].active !== false && process.env[envKey] !== "false")

**Acceptance**:
1. getActivePotdSports() excludes sports with ODDS_SPORTS_CONFIG[sport].active === false
2. Sports with active:true (or no active field) continue to be included
3. Env var "false" override continues to work independently of the active flag
4. All existing tests pass
5. New unit tests cover all three behavioral cases (see Tests to run)

**Owner agent**: unassigned
**Time window**: 2026-04-11 -> TBD
**Coordination flag**: None

**Tests to run**:
- npm --prefix apps/worker test -- --testPathPattern="run-potd-engine"
- New test cases required in run-potd-engine.test.js (not optional):
  1. Sport with active:false is excluded from getActivePotdSports() result
  2. Sport with active:true is included in getActivePotdSports() result
  3. Sport with active:true but env var set to "false" is still excluded

**Manual validation**:
- Confirm MLB and NFL are absent from getActivePotdSports() output without needing
  ENABLE_MLB_MODEL=false / ENABLE_NFL_MODEL=false in the environment. Add a
  temporary debug log (or run via node REPL) to call getActivePotdSports() and
  verify only NHL and NBA are returned.

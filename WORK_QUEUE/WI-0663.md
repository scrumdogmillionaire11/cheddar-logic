# WI-0663: MLB pitcher-K strong under monitoring

**ID**: WI-0663
**Goal**: Extend the MLB pitcher-K odds-backed pipeline to rank and emit strong `UNDER` watch/play cards using existing repo data only.

**Scope**:
- `apps/worker/src/models/mlb-model.js`
- `apps/worker/src/jobs/run_mlb_model.js`
- `apps/worker/src/jobs/pull_mlb_pitcher_stats.js`
- `apps/worker/src/jobs/__tests__/run_mlb_model.test.js`
- `web/src/lib/games/route-handler.ts`
- `web/src/lib/game-card/transform/index.ts`
- `web/src/__tests__/api-games-market-smoke.test.js`
- `web/src/__tests__/api-games-prop-decision-contract.test.js`
- `web/src/__tests__/game-card-transform-market-contract.test.js`
- `web/src/__tests__/prop-game-card-contract.test.js`
- `packages/data/src/validators/card-payload.js`
- `packages/data/src/__tests__/validators/card-payload.mlb-pitcher-k.test.js`

**Out of scope**:
- New external lineup, umpire, or park-factor feeds
- Settlement or ROI reporting
- New Player Props UI surfaces or card components beyond existing pitcher-K rendering in the Player Props tab

**Acceptance**:
- Odds-backed MLB pitcher-K flow selects an under-friendly market by highest line, then best `under_price`, then bookmaker priority
- MLB pitcher-K under scoring uses existing pitcher stats, game logs, prop lines, and weather to produce `NO_PLAY` / `WATCH` / `PLAY`
- Odds-backed MLB runs emit `mlb-pitcher-k` cards for `WATCH` and `PLAY` under calls with populated `prop_decision`
- Projection-only MLB pitcher-K rows persist with canonical prop metadata so the Player Props tab can render them as non-actionable `PROJECTION` rows
- `/api/games` preserves `mlb-pitcher-k` rows as `market_type='PROP'` with normalized `prop_decision` and MLB-safe dedupe
- Targeted MLB runner tests cover market selection, lookback behavior, scoring thresholds, and payload shape

**Owner agent**: codex
**Time window**: 2026-03-29T16:15:00-04:00 -> TBD
**Coordination flag**: solo

**Tests to run**:
- `npm --prefix apps/worker test -- --runInBand --testPathPattern=run_mlb_model.test.js`
- `node web/src/__tests__/api-games-market-smoke.test.js`
- `node web/src/__tests__/api-games-prop-decision-contract.test.js`
- `node web/src/__tests__/game-card-transform-market-contract.test.js`

**Manual validation**:
- Run the MLB model in `PITCHER_KS_MODEL_MODE=ODDS_BACKED` with seeded pitcher-K lines and confirm only `UNDER` `WATCH`/`PLAY` cards are emitted for this feature path
- Confirm `mlb-pitcher-k` payloads include `prop_decision.lean_side='UNDER'` and expanded `pitcher_k_result` under metrics
- Run the cards page in Player Props mode and confirm MLB pitcher-K projection rows render as `Strikeouts` alongside NHL shots/blocks without surfacing as actionable plays

**Guard for WI closeout**:
- Before marking complete: confirm every test command in `Tests to run` is runnable from repo root.

CLAIM: codex 2026-03-29T16:15:00-04:00

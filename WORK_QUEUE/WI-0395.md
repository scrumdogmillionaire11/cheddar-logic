# WI-0395: Cards Edge Copy Simplification + Branded Verdict Language

**ID**: WI-0395
**Goal**: Simplify edge explanation on cards, hide extra edge math behind a collapsible section, and replace jargon-heavy verdict copy with branded human-readable language.

**Depends on**: WI-0377 closed (to avoid overlapping ownership on `web/src/components/cards-page-client.tsx`)

**Scope**:
- `WORK_QUEUE/WI-0395.md`
- `web/src/components/cards-page-client.tsx`
- `web/src/__tests__/ui-evidence-no-play-contract.test.js`
- `web/src/__tests__/integration/games-pipeline-contract.test.ts`

**Out of scope**:
- Changes to worker/API canonical status contract (`PLAY/LEAN/PASS`)
- Decision thresholds, scoring, or edge math formulas
- Filter panel/preset labeling changes outside cards page
- Settlement, results API, or DB changes

**Acceptance**:
- Cards show a simplified edge line by default in plain language:
  - `Projected: <value> (<pct>% beyond line)` when data exists.
- All additional edge math detail is hidden by default in a collapsible container.
- User-facing `LEAN` label is removed from cards and replaced with `SLIGHT EDGE`.
- Branded verdict language is applied on cards:
  - `🔥 PLAY` + `Fresh Cheddar`
  - `📊 SLIGHT EDGE` + `Mild Cheddar`
  - `PASS` + `Cottage Cheese`
- PASS diagnostics remove `Model Lean` wording and use plain-language `Model Direction`.
- Existing source-contract tests are updated for new copy anchors and pass.

**Owner agent**: unassigned
**Time window**: after WI-0377 close -> TBD
**Coordination flag**: solo

**Tests to run**:
- `npm --prefix web run test:ui:evidence`
- `node web/src/__tests__/integration/games-pipeline-contract.test.ts`
- `npm --prefix web run test:ui:cards`
- `npm --prefix web run test:transform:truth-price`

**Manual validation**:
- Open `/cards` and confirm only one simple projected sentence is visible by default.
- Expand collapsible context and confirm full edge math appears there.
- Verify no visible `Lean` wording remains in cards verdict/diagnostic copy.
- Verify branded verdict labels render correctly for PLAY / SLIGHT EDGE / PASS.

CLAIM: <agent> <ISO8601>

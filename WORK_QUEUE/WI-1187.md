# WI-1187: BLK Freshness Contract Split, Health Watchdog, and Backstop Hardening

**ID**: WI-1187
**Goal**: Prevent BLK freshness degradation from surfacing as generic projection-input failure by splitting missing-input namespaces, adding explicit BLK freshness watchdog checks, and adding a non-weekly BLK pull backstop with end-to-end regression coverage.

## Scope

- `apps/worker/src/jobs/run_nhl_player_shots_model.js`
- `apps/worker/src/jobs/check_pipeline_health.js`
- `apps/worker/src/jobs/__tests__/check_pipeline_health.test.js`
- `apps/worker/src/schedulers/player-props.js`
- `apps/worker/src/schedulers/__tests__/player-props.test.js`
- `web/src/lib/game-card/transform/index.ts`
- `web/src/lib/game-card/reason-labels.ts`
- `web/src/__tests__/game-card-pass-surface-contract.test.js`

## Out of scope

- Any threshold retuning for existing model confidence or edge policy
- Broader PASS taxonomy redesign beyond BLK freshness namespace split
- New DB tables or schema migrations

## Acceptance

- `block_rates_stale` no longer renders as generic `MISSING_DATA_PROJECTION_INPUTS` in pass-surface output
- Web pass reason rendering distinguishes feature freshness degradation from core projection-input incompleteness
- Pipeline health emits explicit BLK freshness status for NST and MoneyPuck BLK rates sources
- Player-props scheduler provides an in-week BLK rates pull backstop beyond Monday 09:00 ET
- Regression tests cover BLK stale reason classification and watchdog behavior

## Owner agent

github-copilot

## Time window

1 session

## Coordination flag

solo

## Depends on

none

## Tests to run

- `npm --prefix apps/worker run test -- src/jobs/__tests__/check_pipeline_health.test.js`
- `npm --prefix apps/worker run test -- src/schedulers/__tests__/player-props.test.js`
- `cd web && node --import tsx/esm src/__tests__/game-card-pass-surface-contract.test.js`

## Manual validation

- Run worker health check and confirm BLK freshness rows are written to `pipeline_health`
- Confirm PASS cards with BLK stale inputs surface feature-freshness wording (not projection-input failure)

CLAIM: github-copilot 2026-04-26T00:00:00Z

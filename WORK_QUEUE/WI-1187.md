# WI-1187: Canonicalize NHL Totals Status Reason Codes

**ID**: WI-1187
**Goal**: Add the active NHL totals status reason codes to the canonical reason-code registry and guard them with a registry test so the worker and web surfaces stop drifting.

**Scope**:

- `WORK_QUEUE/WI-1187.md`
- `packages/data/src/reason-codes.js`
- `packages/data/src/__tests__/reason-codes.test.js`
- `web/src/lib/game-card/reason-labels.ts`

**Out of scope**:

- Reworking NHL totals status policy thresholds or downgrade behavior
- Registry alignment for unrelated MLB or execution-gate internal tokens
- Web UI copy changes beyond labels generated from the canonical registry

**Implementation notes**:

- `apps/worker/src/models/nhl-totals-status.js` emits inline reason codes that are consumed by worker tests and decision publishing, but several are not registered in `packages/data/src/reason-codes.js`.
- This WI only canonicalizes the NHL totals status literals that currently surface from that classifier.

**Acceptance**:

- Active `nhl-totals-status.js` reason codes are present in `ALL_REASON_CODES` with labels in `REASON_CODE_LABELS`.
- A canonical registry test fails if those NHL totals reason codes drift out of the shared registry again.
- Existing canonical reason-code tests continue to pass.

**Owner agent**: github-copilot
**Time window**: 2026-04-27T00:00:00Z -> TBD
**Coordination flag**: solo
**Depends on**: none

**Tests to run**:

- `npm --prefix packages/data test -- src/__tests__/reason-codes.test.js`

**Manual validation**:

- Confirm `apps/worker/src/models/nhl-totals-status.js` no longer contains surfaced reason codes missing from the canonical registry.

**Guard for WI closeout**:

- Before marking complete: confirm all test commands run from repo root.
- Keep the slice limited to canonical registry alignment for NHL totals reason codes.

CLAIM: github-copilot 2026-04-27T00:00:00Z

# WI-0938-07: THE BOARD - Integration + Production Ready (Final Phase)

**ID**: WI-0938-07  
**Parent**: WI-0938  
**Goal**: Wire all components together, update Discord formatter, remove feature flag, and deploy THE BOARD to production.

**Depends on**: WI-0938-02 (skeleton), WI-0938-03 (Opportunities), WI-0938-04 (Blocked Edges), WI-0938-05 (Edge Type Tracker), WI-0938-06 (enricher)

**Scope**:

- Discord formatter updates: include market error + action state in messages
- Feature flag removal: set FEATURE_BOARD_ENABLED=true permanently in production
- Documentation: onboarding guide for users, runbook for operators
- Performance testing: ensure THE BOARD endpoints don't degrade web API performance
- Smoke testing: end-to-end tests on staging before production deploy
- Monitoring: set up alerts for THE BOARD endpoints (error rates, latency)
- Community messaging: Discord/email announcement about THE BOARD launch
- Documentation updates: update site copy, help docs to reference THE BOARD

**Out of scope**:

- New feature development (all features must be complete in prior WIs)
- Database optimization (out of scope unless performance testing reveals issues)
- Advanced analytics (tracking/dashboards are post-launch)

**Acceptance**:

- Discord formatter updated to show action state (FIRE/WAIT/PASS) and market error in bot messages
- FEATURE_BOARD_ENABLED environment variable is set to true in production
- Onboarding guide exists: `docs/THE_BOARD_ONBOARDING.md`
- Operator runbook exists: `docs/THE_BOARD_RUNBOOK.md` (troubleshooting, monitoring)
- Production checklist completed:
  - All unit tests passing
  - All integration tests passing
  - Smoke tests passing on staging
  - Monitoring alerts configured
  - Rollback plan documented
  - Zero regressions in existing card/wedge functionality
- Performance benchmarks: `/api/board/*` endpoints respond in <500ms
- User-facing documentation updated (site help, onboarding flow)
- Community announcement prepared (Discord message, email)
- Observability: logs for enricher function, error tracking enabled

**Owner agent**: unassigned  
**Time window**: 2-3 days (mostly coordination + testing)  
**Coordination flag**: needs-sync (final gate before production)

**Tests to run**:

- `npm --prefix web run test` (all tests)
- `npm --prefix web run build` (production build)
- Smoke tests: hit `/board`, all tabs, check no 500 errors
- Performance: load test `/api/board/*` endpoints (target: <500ms p99)
- Manual: Verify Discord messages include action state
- Manual: Verify `/board` visible in production nav
- Manual: Verify Blocked Edges trust-building narrative works
- Manual: Verify Edge Type Tracker patterns are visible

**Manual validation**:

- `/board` is accessible and feature flag is on
- All three tabs load and display data correctly
- Discord bot messages show market error + action state
- No regressions in `/wedge` or existing card pages
- Monitoring/alerts are firing correctly
- Performance is acceptable
- Community messaging is clear and accurate

**Notes**:

- This is the final gate: everything must work flawlessly before deploying
- Rollback plan: set FEATURE_BOARD_ENABLED=false if critical issues arise
- Discord formatter change: test with real bot before deploying
- Community messaging should emphasize:
  - THE BOARD is a market reasoning engine, not a picks feed
  - Blocked Edges show disciplined model restraint
  - Edge Type Tracker enables pattern learning
- Post-launch monitoring: track THE BOARD page view counts, tab engagement, community sentiment

**Post-Launch**:

- Monitor THE BOARD engagement metrics
- Gather user feedback via Discord/community
- Plan for post-launch improvements (advanced features, analytics, etc.)
- Consider retroactive implementation: backfill historical edge reasoning data for pattern tracking

CLAIM: unassigned

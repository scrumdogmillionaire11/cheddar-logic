# WI-1193: Persist Pipeline Degraded State in Health Watchdog Rows

**ID**: WI-1193
**Goal**: Persist an overall degraded/healthy pipeline state row each watchdog run so degraded periods remain visible across restarts.

**Scope**:

- `WORK_QUEUE/WI-1193.md`
- `apps/worker/src/jobs/check_pipeline_health.js`
- `apps/worker/src/jobs/__tests__/check_pipeline_health.watchdog.test.js`

**Out of scope**:

- Scheduler/env changes for `ENABLE_PIPELINE_HEALTH_WATCHDOG`
- Job-level `pipeline_health` hooks in model/settlement jobs
- NFL parity or decision pipeline changes

**Acceptance**:

- `check_pipeline_health` writes an overall watchdog state row every run (`phase='watchdog'`, `check_name='degraded_state'`).
- Overall row status is `failed` when one or more checks fail, and `ok` when all checks pass.
- Reason text includes failing check identifiers for operator visibility.
- Scoped watchdog tests pass.

**Owner agent**: github-copilot
**Time window**: 2026-04-27T00:00:00Z -> TBD
**Coordination flag**: solo
**Depends on**: none

**Tests to run**:

- `npm --prefix apps/worker test -- src/jobs/__tests__/check_pipeline_health.watchdog.test.js`

**Manual validation**:

- Run `npm --prefix apps/worker run job:check-pipeline-health` and verify latest `pipeline_health` rows include `watchdog / degraded_state` with expected status.

**Guard for WI closeout**:

- Keep diff scoped to listed files only.

CLAIM: github-copilot 2026-04-27T00:00:00Z

**STATUS**: COMPLETE — 2026-04-27T00:00:00Z

**Validation evidence**:

- `npm --prefix apps/worker test -- src/jobs/__tests__/check_pipeline_health.watchdog.test.js`

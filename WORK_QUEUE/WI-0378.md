# WI-0378: NHL Totals + Goalie Confirmation Semantics Audit (Scoping)

**ID**: WI-0378
**Goal**: Scope and document gaps in NHL totals modeling related to goalie inputs and starter confirmation semantics, then define implementation-ready follow-up work.

**Scope**:
- `WORK_QUEUE/WI-0378.md`
- scoping evidence and notes captured in this WI only (no production code edits)

**Out of scope**:
- Any changes to model math, decision thresholds, or gating behavior
- Any changes to ingestion/enrichment providers
- UI/API payload contract changes
- DB schema or migration changes

**Acceptance**:
- Current-state map is documented for NHL totals inputs (where goalie data enters, where it affects projection/confidence).
- Starter confirmation semantics are documented explicitly (true starter confirmation vs proxy signals like missing GSaX).
- Gap list is captured with severity, user impact, and data-contract implications.
- Recommended next-step WIs are enumerated with proposed scope boundaries and test plan.

**Owner agent**: unassigned
**Time window**: TBD -> TBD
**Coordination flag**: solo

**Tests to run**:
- None (scoping-only WI)

**Manual validation**:
- Review and approve scope findings with stakeholders before opening implementation WIs.



## Summary

Build WI-0378 as a **fault-aware audit**, not a polite documentation exercise. The real job is to expose where the NHL totals stack can lie to itself about goalie certainty, apply model math on shaky starter assumptions, and still accidentally surface a clean-looking FIRE/HOLD/PASS output. The files already show that goalie status drives parsing, model adjustment, wrapper gating, consistency checks, and watchdog vetoes, but the semantics are split across tags, user inputs, and inferred status instead of one canonical contract.   

## Why This Is Needed

The current NHL totals system has the exact kind of architecture that breeds fake confidence.

The parser emits goalie tags and warnings, the workflow expects user-supplied goalie certainty, the pace model applies goalie-tier adjustments including an `Unknown` fallback, consistency checks use goalie tiers to shape volatility and bias, and the watchdog can force totals to PASS if the goalie is unconfirmed. That means one bad or ambiguous goalie state can contaminate the pipeline in multiple ways while still looking structurally valid.    

The biggest faulty-model behaviors to design around are these:

1. **Semantic collapse:** “Confirmed,” “Expected,” and “Unknown” can behave too similarly downstream, even though the wrapper explicitly treats them differently. 
2. **Math on bad identity:** the pace model still applies a goalie adjustment when the goalie is unknown, which is useful as a fallback but dangerous if downstream logic forgets that the number is low-trust. 
3. **Split-brain inputs:** parser-derived goalie tags and user-supplied goalie status can disagree, with no clearly documented arbitration rule.  
4. **Contradiction masking:** consistency checks can label a game with a clean event direction while uncertainty in goalie identity should really cap it harder.  
5. **Late wrapper heroics:** the wrapper can look disciplined with HOLD/FIRE labels while the underlying model total was produced from bad assumptions. That’s lipstick on a pig, buddy. The pig still bites. 

## Design

### Files affected

Scoping-only output:

* `WORK_QUEUE/WI-0378.md`

Files to audit as evidence:

* `parsers__multisport_daily.md`
* `workflows__nhl_card_builder.md`
* `core__watchdog_fail_safes.md`
* `core__consistency_checks.md`
* `core__slate_context.md`
* `core__orchestration.md`
* `models__fade_public.md`
  These files collectively show parser semantics, wrapper gating, watchdog vetoes, environment reconciliation, and orchestration input contracts.       

### New interfaces

Scoping doc should define, even if not yet implemented, these target interfaces:

```ts
type GoalieStarterStatus =
  | "CONFIRMED"
  | "EXPECTED"
  | "PROJECTED"
  | "UNKNOWN"
  | "CONFLICTING";

type GoalieInputQuality =
  | "TRUSTED_OFFICIAL"
  | "TRUSTED_REPORTER"
  | "MODEL_INFERRED"
  | "MISSING"
  | "CONTRADICTORY";

type CanonicalGoalieState = {
  game_id: string;
  team: string;
  goalie_name: string | null;
  starter_status: GoalieStarterStatus;
  input_quality: GoalieInputQuality;
  source: "PARSER" | "USER_INPUT" | "MERGED";
  tier: "ELITE" | "STRONG" | "AVERAGE" | "WEAK" | "UNKNOWN";
  tier_confidence: "HIGH" | "MEDIUM" | "LOW";
  flags: string[];
};
```

### Invariants

The audit should lock these invariants:

* A totals projection must never be treated as high-confidence if goalie identity is not canonicalized first.
* A wrapper FIRE decision must not outrun goalie confidence.
* Parser tags are not enough; downstream layers need a structured goalie state.
* “Unknown goalie” is not the same thing as “bad goalie.”
* Contradictory goalie inputs must degrade trust harder than missing inputs.
* Watchdog and wrapper must consume the same goalie semantics, not parallel homebrew interpretations.
  These invariants are consistent with the slate-context rule that missing or suspect data must be flagged and workflows must downgrade confidence rather than invent certainty. 

## Implementation Plan

### Step 1 — Rewrite the WI as a fault-audit, not a plain scope memo

In `WORK_QUEUE/WI-0378.md`, add a section called:

**“Faulty Model Behavior Inventory”**

This section should force the agent to trace not just where goalie data exists, but where the system can produce the wrong output while appearing valid.

Required subsections:

* semantic faults
* data-contract faults
* model-math faults
* gating faults
* watchdog faults
* reconciliation faults

### Step 2 — Document the current-state goalie path end to end

The doc should include a pipeline map like this:

```text
raw goalie note / user status
  -> parser tags + warnings
  -> player/game objects
  -> merged game goalie state (or lack of one)
  -> pace model goalie adjustment
  -> consistency check vol_env / total_bias
  -> wrapper HOLD/FIRE/PASS gate
  -> watchdog veto/downgrade
```

For each hop, require these fields:

* input
* transform
* assumptions
* output
* failure if wrong
* whether the failure is silent or explicit

That matters because the watchdog is designed to kill silent success, and this WI should use that philosophy as the audit lens. 

### Step 3 — Explicitly enumerate faulty model behaviors

This is the part you asked for. The agent should include at least these named behaviors.

**FM-01: Expected-as-Confirmed drift**
The parser emits `NHL_GOALIE_EXPECTED`, but downstream logic may still let the pace model use a specific goalie tier while the wrapper only partially downgrades. Result: model total looks sharper than it is.  

**FM-02: Unknown-is-bad-goalie contamination**
The pace model applies an `Unknown: +0.10` adjustment. That is a modeling convenience, not evidence the goalie environment is over-friendly. If analysts or downstream code read it as “weak goalie environment,” overs get fake juice. 

**FM-03: Parser/User split-brain**
The orchestrator and workflow require user-supplied goalie status, while the parser can also infer goalie context from text. No canonical merge contract is shown. Result: one layer says Confirmed, another says Expected, and the system has no single truth source.  

**FM-04: Name-resolution tier mismatch**
Parser confidence rules allow missing goalie metrics or mismatches without hard failure. That means a goalie can be identified as starter but fail metric linkage, causing tier degradation or fallback behavior that is easy to miss. 

**FM-05: Environment over-certainty under missing goalie trust**
Consistency checks derive `vol_env` partly from goalie tier combinations and output `OVER/UNDER/NEUTRAL` directionality. If goalie tier itself is uncertain, the derived environment can look cleaner than the evidence supports. 

**FM-06: Under bias from public heuristics**
The fade-public model explicitly warns against “everyone likes the over, so auto-under” logic in NHL totals. The WI should treat any under recommendation that leans on public fade without goalie confirmation as a fault pattern. 

**FM-07: Wrapper rescue illusion**
The wrapper can correctly output HOLD because a goalie is not confirmed, but the model total beneath it may already be biased by a wrong starter assumption. HOLD helps operationally, but it does not fix the corrupted number. 

**FM-08: Watchdog too late**
The watchdog can force totals to PASS if goalie/player is unconfirmed, but by then upstream classifications and notes may already have been generated around compromised model math. The WI should call this a “late veto architecture” issue. 

**FM-09: Contradictory signals not escalated enough**
Slate context and consistency rules say contradictory or missing data should cap confidence. The WI should verify whether goalie contradictions specifically become strong flags, not just generic warnings. 

### Step 4 — Add a fault matrix

The doc should require a matrix like this:

| Fault ID | Trigger                                  |    Where introduced | Silent/Explicit         | Can affect model total? | Can affect FIRE/HOLD/PASS? | Can watchdog catch it? |
| -------- | ---------------------------------------- | ------------------: | ----------------------- | ----------------------- | -------------------------- | ---------------------- |
| FM-01    | Expected starter treated as stable       | parser/model bridge | Silent                  | Yes                     | Yes                        | Sometimes              |
| FM-02    | Unknown goalie gets numeric uplift       |          pace model | Silent                  | Yes                     | Indirectly                 | Only later             |
| FM-03    | user input conflicts with parser         |         merge layer | Silent unless flagged   | Yes                     | Yes                        | Maybe                  |
| FM-04    | starter name fails metrics join          |      parser/metrics | Mostly silent           | Yes                     | Yes                        | Weakly                 |
| FM-05    | derived env overstates certainty         |         consistency | Silent                  | N/A                     | Yes                        | Weakly                 |
| FM-06    | public under bias                        | fade-public overlay | Explicit if coded right | No                      | Yes                        | Sometimes              |
| FM-07    | wrapper masks bad math                   |             wrapper | Silent                  | Already happened        | Yes                        | No                     |
| FM-08    | veto occurs after projection             |     watchdog timing | Explicit late           | No                      | Yes                        | Yes                    |
| FM-09    | contradiction only warned, not escalated |         flags layer | Silent-ish              | Yes                     | Yes                        | Weakly                 |

### Step 5 — Define implementation-ready follow-up WIs

The scoping doc should propose these, with clear boundaries.

**WI-0379 — Canonical goalie state object**
Introduce one merged `CanonicalGoalieState` per team/game before pace-model execution.

**WI-0380 — Goalie source arbitration layer**
Resolve parser tags, manual status, and inferred metrics into one authoritative state with precedence rules.

**WI-0381 — Pace model trust gating**
Allow goalie tier math only when starter identity confidence clears a threshold; otherwise use neutral or uncertainty-banded output.

**WI-0382 — Consistency check goalie uncertainty escalation**
Require `INSUFFICIENT_DATA` or a dedicated uncertainty flag whenever goalie confidence is below threshold.

**WI-0383 — Watchdog/wrapper semantic alignment**
Make wrapper and watchdog consume the exact same starter-status enum.

**WI-0384 — NHL totals fault harness**
Build deterministic scenarios proving the system downgrades correctly when goalie data is missing, conflicting, late, or mismatched.

### Step 6 — Put severity on each fault

Recommended severity calls:

* FM-01 Expected-as-Confirmed drift → **High**
* FM-02 Unknown-is-bad-goalie contamination → **High**
* FM-03 Parser/User split-brain → **Critical**
* FM-04 Name-resolution tier mismatch → **Medium**
* FM-05 Environment over-certainty → **High**
* FM-06 Public under bias → **Medium**
* FM-07 Wrapper rescue illusion → **Critical**
* FM-08 Watchdog too late → **High**
* FM-09 Contradictions not escalated → **High**

## Tests Required

Scoping-only does not mean “no tests.” It means no production code tests. The WI still needs deterministic validation examples.

### Deterministic scenario set

**Case A — Clean confirmed starters**

* Both teams have confirmed goalie names
* metrics join succeeds
* pace model uses real tiers
* wrapper may FIRE depending on edge
* watchdog does not veto

**Case B — Confirmed vs Expected**

* one official starter, one expected
* pace model may use identity but must mark reduced trust
* wrapper defaults HOLD unless edge/time window justifies more
* watchdog behavior must be explicitly defined

**Case C — Unknown vs Unknown**

* no starter names
* pace model cannot pretend precision
* consistency should lean `INSUFFICIENT_DATA` or equivalent conservative result
* wrapper PASS/HOLD
* watchdog totals PASS

**Case D — Parser says Expected, manual input says Confirmed**

* verify precedence rule
* verify contradiction flag or merge rule
* verify no silent overwrite

**Case E — Starter name resolved, metrics missing**

* goalie identity known but tier confidence low
* pace model must not act like this is a clean elite/weak read

**Case F — Conflicting goalie entries for same team**

* parser sees two starters
* merged state becomes `CONFLICTING`
* totals must not surface as official

**Case G — Over 6.5 with backup-goalie rumor only**

* confirm accelerant logic does not treat rumor like confirmed volatility
* consistency and wrapper must both stay conservative
  That matters because the consistency spec already requires extra justification for over 6.5 and fragility handling around key total numbers. 

### Smoke test outputs for the doc

Every scenario should record:

* canonical starter status
* goalie input quality
* whether model total is “trusted,” “degraded,” or “untrusted”
* wrapper status
* watchdog status
* final market eligibility

## Failure Modes & Guards

### Failure mode: one goalie state, five interpretations

The current architecture lets parser tags, workflow inputs, model adjustments, consistency checks, and watchdog rules each interpret goalie certainty a little differently. That is how bugs get dressed up as process.

**Guard:** require the WI to recommend a single pre-model canonical goalie object.

### Failure mode: uncertainty gets numericized too early

The pace model’s `Unknown +0.10` fallback is useful, but it can become a fake signal if not labeled as uncertainty-driven. 

**Guard:** split “projection adjustment” from “confidence in adjustment.”

### Failure mode: wrapper looks disciplined while number is rotten

A HOLD label can hide bad underlying assumptions.

**Guard:** add a model-trust field that wrapper must surface in notes or use in gating.

### Failure mode: watchdog catches things only after ranking

If the watchdog is the first strong gate, your model and wrapper already spent effort ranking junk. 

**Guard:** move goalie trust checks earlier in the eventual implementation WIs.

### Failure mode: contradictions become soft warnings

Parser warnings are useful, but warnings alone do not stop picks. 

**Guard:** define contradiction-specific fatal or near-fatal flags for NHL totals.

## Open Questions

The only real open questions the WI should leave for stakeholder approval are:

1. Should `EXPECTED` starters be allowed to influence goalie tier math, or only wrapper logic?
2. When parser-derived and user-supplied goalie states disagree, which source wins by default?
3. Should “unknown goalie” produce a neutral goalie adjustment, or a conservative uncertainty band rather than a directional over nudge?
4. Should totals be hard-blocked on `CONFLICTING` goalie state at model time, not just watchdog time?

## Faulty Model Behavior Inventory

> This audit treats faulty model behavior as a first-class deliverable. The objective is not only to document where goalie inputs exist, but to identify where the NHL totals system can generate a plausible yet incorrect recommendation due to ambiguous starter confirmation semantics, delayed uncertainty handling, conflicting input sources, or confidence inflation caused by fallback goalie assumptions.

### Current-State Goalie Pipeline Map

Each hop documents: Input → Transform → Assumptions → Output → Failure if wrong → Silent or explicit.

```
raw goalie note / user status
  -> parser tags + warnings (moneypuck.js)
  -> player/game objects (oddsSnapshot.raw_data goalie fields)
  -> merged game goalie state (NO canonical merge — goalie_home_gsax wins by default)
  -> pace model goalie adjustment (nhl-pace-model.js goalieAdjFactor)
  -> cross-market goalie quality driver (cross-market.js goalieSignal)
  -> wrapper HOLD/FIRE/PASS gate (decision-publisher.js deriveAction via tier)
  -> watchdog veto/downgrade (decision-pipeline-v2.js buildDecisionV2)
```

#### Hop 1: MoneyPuck scrape → goalie_home_gsax / goalie_away_gsax

| Field | Detail |
|---|---|
| Input | MoneyPuck season GSaX table (moneypuck.com/goalies.htm) |
| Transform | parseGoalies() — finds goalie row by team, extracts gsax value |
| Assumptions | GSaX present in season table implies goalie is the likely starter. No game-day confirmation field. |
| Output | `raw_data.goalie_home_gsax`, `raw_data.goalie_away_gsax` (number or null) |
| Failure if wrong | Wrong goalie's GSaX used for projection. Tier adjustment applied to incorrect starter. |
| Silent or explicit | Silent — no flag emitted when GSaX owner identity differs from game-day starter. |

#### Hop 2: goalie_home_gsax → homeGoalieConfirmed boolean (index.js)

| Field | Detail |
|---|---|
| Input | `raw_data.goalie_home_gsax` |
| Transform | `homeGoalieConfirmed = goalieHomeGsax !== null` |
| Assumptions | GSaX present = starter confirmed. This is a PROXY, not an authoritative signal. |
| Output | boolean `homeGoalieConfirmed` passed to projectNHL() and predictNHLGame() |
| Failure if wrong | Expected starter treated as confirmed; pace model adjusts at full trust. |
| Silent or explicit | Silent — proxy origin is not labeled in output. |

#### Hop 3: homeGoalieConfirmed → pace model goalie adjustment (nhl-pace-model.js)

| Field | Detail |
|---|---|
| Input | `homeGoalieSavePct`, `homeGoalieConfirmed` boolean |
| Transform | `goalieAdjFactor(savePct)` applied to opponent goals regardless of confirmation boolean |
| Assumptions | If savePct is present, adjustment runs. The `confirmed` boolean only affects confidence cap — not the adjustment math. |
| Output | `awayGoals *= factor` (adjustment range: 0.75–1.25 clamped) |
| Failure if wrong | Wrong goalie's save-pct inflates or deflates projected total. |
| Silent or explicit | Silent — adjustment applied even when `homeGoalieConfirmed = false`. Confidence capped but math not neutralized. |

#### Hop 4: goalieSum → cross-market goalie_quality signal (cross-market.js)

| Field | Detail |
|---|---|
| Input | `goalie_home_gsax + goalie_away_gsax` sum |
| Transform | `goalieSignal = clamp(-goalieSum / 6, -1, 1)` — eligible only if BOTH present |
| Assumptions | Both GSaX values present = reliable signal. No partial-trust path. |
| Output | Driver signal contributing to TOTAL decision net score |
| Failure if wrong | If one team's GSaX is wrong identity, combined signal is directionally biased. |
| Silent or explicit | Silent — ineligible when either is null, but no flag when both present but one is wrong identity. |

#### Hop 5: driver scores → FIRE/HOLD/PASS (decision-publisher.js)

| Field | Detail |
|---|---|
| Input | `confidence` + `tier` from model output |
| Transform | `tier SUPER → FIRE, BEST/WATCH → HOLD, else PASS` |
| Assumptions | Confidence already encodes goalie uncertainty via cap. But confidence cap only fires when BOTH goalies are unconfirmed — not when ONE is proxy-confirmed. |
| Output | `action` field on card payload |
| Failure if wrong | FIRE card emitted while underlying total was computed from wrong-goalie math. |
| Silent or explicit | Silent — no goalie trust field surfaced in card output. |

#### Hop 6: pipeline state / watchdog (decision-pipeline-v2.js)

| Field | Detail |
|---|---|
| Input | card payload, WATCHDOG_REASONS enum |
| Transform | buildDecisionV2 checks CONSISTENCY_MISSING, MARKET_UNAVAILABLE, etc. |
| Assumptions | No GOALIE_UNCONFIRMED reason code exists. Goalie certainty is not a first-class watchdog dimension. |
| Output | `watchdog_reason_codes` on decision_v2 |
| Failure if wrong | Goalie-confidence issues are not escalated to a veto. Watchdog passes the card. |
| Silent or explicit | Silent — no dedicated goalie watchdog path. |

---

### Semantic Faults

**FM-01: Expected-as-Confirmed drift**

`homeGoalieConfirmed` is derived from `goalieHomeGsax !== null`. GSaX presence in MoneyPuck's season table is not game-day starter confirmation. When a goalie is "Expected" (rumor, not official), their season GSaX may still be present, causing the model to treat them as confirmed. The pace-model adjustment runs at full trust. The confidence cap (`goalieConfidenceCapped`) only triggers when BOTH goalies are null — a single partially-trusted goalie keeps the cap from firing.

Severity: **High**

**FM-02: Unknown-is-bad-goalie contamination**

The pace model applies `goalieAdjFactor(savePct)` when savePct is present. There is no numeric fallback for "Unknown goalie" in `nhl-pace-model.js` — when savePct is null, no adjustment fires at all (lines 252-261). However, when a backup goalie's save-pct IS present in the data but their starter status is actually unknown or rumored, the model applies a legitimate-looking weak-goalie adjustment (+uplift on opponent goals). This can produce a nudge toward OVER without any flag that the goalie identity is unverified. Downstream analysis may interpret the over-nudge as a weak-goalie read rather than an uncertainty artifact.

Severity: **High**

**FM-03: Parser/User split-brain**

`moneypuck.js` enriches `raw_data` with GSaX from a season table. There is no user-supplied starter confirmation field in the current pipeline. The `homeGoalieConfirmed` boolean is derived purely from the scrape result — there is no merge contract between a user-supplied goalie status and the scraper output. If a user or upstream system annotates a game with "Expected" or "CONFLICTING" starter intent, that signal has no path into the model. The scrape wins by default.

Severity: **Critical**

**FM-04: Name-resolution tier mismatch**

MoneyPuck's `parseGoalies()` matches by team name (canonical). If the team's primary goalie is out and the backup plays, the backup's GSaX is not necessarily the top row for that team — but the current scraper takes `goaliesByTeam[canonical]` without filtering for starter. The goalie with the best recent game may not be the actual game-day starter. No metric join validation is performed.

Severity: **Medium**

**FM-05: Environment over-certainty under missing goalie trust**

`cross-market.js` uses `goalieSignal` as a driver in the TOTAL net score. The signal is `eligible: goalieSum !== null`. When both GSaX values are present (even if one is wrong-identity), the driver fires at full weight (effective weight contribution to net score). The consistency envelope derived in `decision-publisher.js` uses `vol_env` from conflict score — not from goalie certainty. A game with shaky goalie identity can still receive a clean `STABLE` vol_env and a directional `FAVOR_OVER` or `FAVOR_UNDER` event_direction_tag.

Severity: **High**

**FM-06: Under bias from public heuristics**

The cross-market goalie_quality signal `goalieSignal = clamp(-goalieSum / 6, -1, 1)` maps high combined GSaX to an UNDER signal. If public consensus is "strong goalies tonight → UNDER" and the model's goalie data reinforces this, a fade-public logic overlay could be double-counting: the model already baked in the goalie signal, and then the fade-public heuristic adds further under pressure based on public sentiment that was itself driven by the goalie narrative. This is most dangerous when goalie identity is only partially confirmed.

Severity: **Medium**

---

### Data-Contract Faults

**FM-07: Wrapper rescue illusion**

`decision-publisher.js` maps `tier → action` deterministically. A HOLD output correctly signals caution, but the underlying `expectedTotal` and `expected1pTotal` in the pace model were computed before any goalie uncertainty check. The card payload has no field that surfaces "this total was computed from low-confidence goalie input." A user sees HOLD and interprets it as "sharp but cautious" — not as "model math may be corrupted." HOLD does not fix the number.

Severity: **Critical**

**FM-08: Watchdog too late**

`WATCHDOG_REASONS` in `decision-pipeline-v2.js` does not include a `GOALIE_UNCONFIRMED` code. The only goalie-adjacent protection is the confidence cap in `nhl-pace-model.js` (`confidence = Math.min(confidence, 0.35)` when `goalieConfidenceCapped = true`). But `goalieConfidenceCapped` fires only when BOTH goalies are unconfirmed (null GSaX). The watchdog is downstream of projection, driver computation, card assembly, and validation. By the time the watchdog could theoretically veto on goalie grounds, the model has already run, the card has been assembled, and the notes have been generated with goalie-adjusted numbers baked in.

Severity: **High**

---

### Model-Math Faults

The model applies goalie save-pct adjustment at Hop 3 regardless of the `homeGoalieConfirmed` boolean (see Pipeline Hop 3 above). This means `goalieConfidenceCapped = true` reduces confidence to ≤0.35, but `awayGoals` has already been multiplied by the goalie adjustment factor. The cap is applied after the projection number is baked. The projection itself is not neutralized.

---

### Gating Faults

The `assessProjectionInputs` function in `projections.js` checks for `avgGoalsFor` and `avgGoalsAgainst` — it does not gate on goalie confirmation state. A game can pass the projection gate with full goalie math applied, even when both goalies are unconfirmed.

---

### Reconciliation Faults

No merge contract exists between scraper-derived goalie fields and any authoritative starter source. The `raw_data` goalie fields are write-once from the MoneyPuck enrichment pass. If the game-day starter differs from the season-table leader for that team, there is no reconciliation step and no contradiction flag.

---

### Fault Matrix

| Fault ID | Trigger | Where introduced | Silent/Explicit | Can affect model total? | Can affect FIRE/HOLD/PASS? | Can watchdog catch it? | Severity |
|---|---|---|---|---|---|---|---|
| FM-01 | GSaX present = proxy for "confirmed" | index.js Hop 2 | Silent | Yes | Yes | No | High |
| FM-02 | Backup gsax present but starter unknown | nhl-pace-model.js Hop 3 | Silent | Yes | Indirectly | No | High |
| FM-03 | No user-input merge contract | moneypuck.js / no merge layer | Silent | Yes | Yes | No | Critical |
| FM-04 | Team-level gsax, not starter-specific | moneypuck.js parseGoalies | Mostly silent | Yes | Yes | Weakly | Medium |
| FM-05 | Wrong-identity gsax used in driver signal | cross-market.js Hop 4 | Silent | N/A (driver signal) | Yes | Weakly | High |
| FM-06 | Public fade + model both from goalie narrative | cross-market.js + overlay | Explicit if coded right | No | Yes | Sometimes | Medium |
| FM-07 | Wrapper masks bad math with HOLD | decision-publisher.js Hop 5 | Silent | Already happened | Yes | No | Critical |
| FM-08 | No GOALIE_UNCONFIRMED watchdog code | decision-pipeline-v2.js | Explicit late (confidence cap only) | No | Yes | Partial | High |
| FM-09 | Scraper wins; no contradiction detection | moneypuck.js / no merge | Silent | Yes | Yes | No | High |

---

### Deterministic Scenario Outputs

**Case A — Clean confirmed starters (both teams have known GSaX, season-table match expected)**

| Field | Value |
|---|---|
| canonical starter status | CONFIRMED (both) |
| goalie input quality | TRUSTED_REPORTER (via MoneyPuck; season table) |
| model total trust | TRUSTED |
| wrapper status | FIRE if edge clears, HOLD if edge marginal |
| watchdog status | passes (no goalie veto code) |
| final market eligibility | eligible |

**Case B — Confirmed vs Expected (one official, one expected)**

| Field | Value |
|---|---|
| canonical starter status | CONFIRMED (home), EXPECTED (away) — but current system cannot distinguish; both treated as present-or-absent |
| goalie input quality | TRUSTED_REPORTER (home), MODEL_INFERRED or MISSING (away) |
| model total trust | DEGRADED — away GSaX may be wrong identity |
| wrapper status | Should default HOLD; currently may FIRE if edge is present and confidence cap does not trigger |
| watchdog status | passes (no GOALIE_UNCONFIRMED reason code) |
| final market eligibility | should be restricted; currently unrestricted |

**Case C — Unknown vs Unknown (no starter names, no GSaX)**

| Field | Value |
|---|---|
| canonical starter status | UNKNOWN (both) |
| goalie input quality | MISSING (both) |
| model total trust | UNTRUSTED — confidence capped at 0.35 via goalieConfidenceCapped |
| wrapper status | PASS (confidence too low for tier) |
| watchdog status | passes (confidence cap fires, but no explicit watchdog veto) |
| final market eligibility | blocked by confidence cap — does not surface |

**Case D — Parser says Expected, manual input says Confirmed**

| Field | Value |
|---|---|
| canonical starter status | CONFLICTING — but no merge contract exists to detect this |
| goalie input quality | CONTRADICTORY (target state); currently MISSING (no merge layer) |
| model total trust | UNTRUSTED — but system currently trusts whichever gsax is present |
| wrapper status | should be PASS; currently depends on GSaX presence |
| watchdog status | passes (no contradiction detection) |
| final market eligibility | should be hard-blocked; currently not blocked |

**Case E — Starter name resolved, metrics missing (GSaX absent)**

| Field | Value |
|---|---|
| canonical starter status | EXPECTED or PROJECTED |
| goalie input quality | MODEL_INFERRED (name known, tier unknown) |
| model total trust | DEGRADED — no adjustment runs, but also no neutral band applied |
| wrapper status | confidence cap fires (both gsax null → goalieConfidenceCapped = true → confidence ≤ 0.35) → PASS |
| watchdog status | passes (no specific veto) |
| final market eligibility | blocked by confidence — does not surface |

**Case F — Conflicting goalie entries for same team**

| Field | Value |
|---|---|
| canonical starter status | CONFLICTING (target); currently: whichever gsax is non-null wins silently |
| goalie input quality | CONTRADICTORY (target); currently: TRUSTED_REPORTER (no conflict detection) |
| model total trust | UNTRUSTED — but system is unaware of contradiction |
| wrapper status | may FIRE or HOLD if gsax values are present |
| watchdog status | passes |
| final market eligibility | should be hard-blocked; currently eligible |

**Case G — Over 6.5 with backup-goalie rumor only**

| Field | Value |
|---|---|
| canonical starter status | PROJECTED (backup rumor, not confirmed) |
| goalie input quality | MODEL_INFERRED (rumor-based) |
| model total trust | DEGRADED — backup gsax may drive weak-goalie adjustment |
| wrapper status | may FIRE (backup gsax = high goalie adjustment factor = over nudge) — this is the danger case |
| watchdog status | passes |
| final market eligibility | should be restricted; currently eligible. Consistency fragility check (totalFragilityScore at 6.5) provides weak guard only. |

---

### Implementation-Ready Follow-Up WIs

**WI-0379 — Canonical goalie state object**
Introduce one merged `CanonicalGoalieState` per team/game (see Design section for type definition). Must be computed before pace-model execution. Replaces the current `homeGoalieConfirmed` boolean proxy.

**WI-0380 — Goalie source arbitration layer**
Resolve MoneyPuck scraper output, user-supplied goalie status, and inferred metrics into one authoritative state per team per game. Define explicit precedence rules (user-supplied > scraper name-match > season-table inference). Emit `CONFLICTING` when sources disagree.

**WI-0381 — Pace model trust gating**
Allow goalie save-pct adjustment only when `CanonicalGoalieState.tier_confidence` is HIGH or MEDIUM. When LOW or UNKNOWN, apply neutral fallback (no directional adjustment) with explicit label `adjustment_trust: LOW` in output. The confidence cap remains, but the math itself must also be gated.

**WI-0382 — Consistency check goalie uncertainty escalation**
When `CanonicalGoalieState.starter_status` is UNKNOWN or CONFLICTING, set `vol_env = VOLATILE` and emit `event_direction_tag = INSUFFICIENT_DATA` for the totals direction. This must happen before the driver signal is eligible.

**WI-0383 — Watchdog/wrapper semantic alignment**
Add `GOALIE_UNCONFIRMED` and `GOALIE_CONFLICTING` to `WATCHDOG_REASONS`. Make both wrapper and watchdog consume the `CanonicalGoalieState.starter_status` enum rather than the boolean proxy.

**WI-0384 — NHL totals fault harness**
Build deterministic test scenarios (Cases A-G from this WI) as repeatable test fixtures. Each scenario must verify: canonical starter status output, model total trust label, wrapper action, watchdog reason codes, and market eligibility flag.

---

### Audit Sign-Off Criteria

This audit is complete when:

- [ ] All 9 fault IDs have been reviewed by the implementer
- [ ] Open Questions 1-4 are answered before WI-0381 begins (EXPECTED star treatment, precedence rule, unknown-goalie adjustment direction, CONFLICTING hard-block timing)
- [ ] WI-0379 through WI-0383 are opened with the scope boundaries above
- [ ] WI-0384 test harness is opened and assigned before any implementation WI is marked complete

---

## Recommended text you can hand to the agent

Use this exact framing inside WI-0378:

> This audit must treat faulty model behavior as a first-class deliverable. The objective is not only to document where goalie inputs exist, but to identify where the NHL totals system can generate a plausible yet incorrect recommendation due to ambiguous starter confirmation semantics, delayed uncertainty handling, conflicting input sources, or confidence inflation caused by fallback goalie assumptions.

That’s the right lens. Otherwise the agent will write a cute systems map and completely miss the murder weapon.

The files backing this are your parser goalie tags and warnings, NHL wrapper certainty gates, watchdog unconfirmed-goalie veto, consistency-driven totals classification, and slate-context downgrade rules.     

If you want, next I’ll turn this into a **drop-in `WORK_QUEUE/WI-0378.md` draft** you can paste straight into the project.


Yes — if you give me the **actual model output** I can pinpoint where the inflation is coming from.

For NHL totals (especially **1P totals**) there are a handful of **very specific model failure patterns** that cause numbers to run hot. If your **1P projections and full totals both look high**, the issue is almost always structural rather than a single coefficient.

Send me a sample like this for **5–10 games** and I’ll break it down precisely:

```
Game
Market Line: Full Total / 1P Total

Model Projection:
Full Total:
1P Total:

Inputs:
Pace Projection
Shot Rate Projection
Expected Goals
Goalie Tier
Goalie Status (confirmed/expected)
Special Teams Impact
Recent Form Adjustment
Back-to-back / fatigue
Empty Net Adjustment
```

Even better if you include **driver components** like:

```
PACE_ENVIRONMENT
ACCELERANT_SCORE
SUPPRESSOR_SCORE
MARKET_EDGE
```

(which your output template references). 

---

# The 7 Most Common Reasons NHL Totals Run Too High

Here’s what I will be looking for immediately when you send the outputs.

## 1️⃣ Goalie Adjustment Overweight

A classic model mistake.

Example:

```
Elite goalie   -0.35
Backup goalie  +0.30
Poor goalie    +0.45
```

Those look harmless but if:

* both teams project **+0.25 offense**
* both goalies are downgraded

You get:

```
+0.5 offensive environment
+0.6 goalie effect
---------------------
+1.1 goals inflation
```

That’s enormous.

If your **1P totals are also high**, this is often the cause.

---

## 2️⃣ Pace Model Double Counting

Very common when models use both:

```
shot rate
expected goals
pace
```

These are **not independent variables**.

If pace already drives shot rate, and shot rate drives xG, you are counting the same signal **twice or three times**.

Symptoms:

* Full totals ~0.4–0.8 goals high
* 1P totals especially inflated

---

## 3️⃣ 1P Scaling Incorrect

The typical mistake:

```
1P = Full Total × 0.33
```

But NHL scoring **is not evenly distributed**.

Typical league averages:

```
1P ≈ 2.05 goals
Full ≈ 6.1 goals
Ratio ≈ 0.335
```

But models often forget:

* empty net goals inflate full totals
* penalties late in game inflate 3P

A better ratio is usually:

```
1P ≈ 0.31–0.32 × full total
```

If you’re using **0.33–0.34**, your 1P numbers will run hot.

---

## 4️⃣ Special Teams Overweight

Models often add:

```
PP efficiency
PK weakness
penalty rate
```

But again:

```
penalties → already in xG
```

Double counting again.

Result:

```
special teams inflation +0.2–0.4 goals
```

---

## 5️⃣ Empty Net Adjustment Applied Too Early

If your model does something like:

```
+0.25 empty net expectation
```

and applies it **before deriving 1P totals**, the 1P projection becomes inflated.

Empty net goals belong **only in full game totals**.

---

## 6️⃣ League Mean Drift

Another subtle issue.

If your baseline is:

```
league mean = 6.2
```

but the market baseline is:

```
6.0
```

your entire system runs hot.

Small drift compounds across games.

---

## 7️⃣ Market Anchor Missing

Your model may be **pure projection**, but NHL totals models **almost always need a market anchor**.

Example workflow:

```
Market Total = 6
Model Projection = 6.4

Final Projection = 6.2
```

Without this, models drift upward over time.

---

# Quick Diagnostic Trick

If you send the output I’ll run a simple test.

I check:

```
Model Total - Market Total
```

Across games.

If the average is:

```
+0.3 to +0.6
```

Your model is structurally inflated.

---

# What I Suspect Right Now

Based on your description:

> 1P totals seem high
> totals in general seem high

The **most likely culprit** is:

### Pace + xG double counting

or

### Goalie adjustments too large

or

### 1P scaling too aggressive

Those three together can inflate totals by **0.6–1.0 goals**, which is huge.

---

# What To Send Me

Post **one card output** like this:

```
Game: TOR @ TBL
Market Total: 6.5
Market 1P: 1.5

Model Full: 7.1
Model 1P: 2.4

Pace:
Shot Rate:
xG:
Goalie Tier:
Goalie Status:
Accelerant:
Suppressor:
```

I’ll tell you:

* **exactly which component is wrong**
* **how much it inflates the model**
* **what the correct weighting should be**

and if necessary I’ll give you a **fix that will stabilize the model immediately**.

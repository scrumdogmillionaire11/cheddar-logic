# WI-0515: Deterministic Bench Boost + Triple Captain evaluators

**ID**: WI-0515
**Goal**: Implement deterministic `Bench Boost` and `Triple Captain` evaluators that follow the March 2026 chip spec for scoring, hard vetoes, DGW preference, horizon suppression, and late-season force logic.

**Depends on**:

- WI-0513

**Scope**:

- `WORK_QUEUE/WI-0515.md`
- `cheddar-fpl-sage/src/cheddar_fpl_sage/analysis/decision_framework/chip_engine/bench_boost_triple_captain.py`
- `cheddar-fpl-sage/tests/test_chip_engine_bench_boost_triple_captain.py`

**Out of scope**:

- `Wildcard` / `Free Hit` evaluator work
- `ChipAnalyzer` adapter wiring
- Backend/frontend API contract changes

**Acceptance**:

- `evaluate_bench_boost()` uses bench projected total, DGW bench bonus, and blank-starter penalty, with a hard veto on any blank starter.
- `evaluate_triple_captain()` uses captain projection, DGW multiplier, ownership effect, and fixture quality, with a hard veto on blank captains and better-window-ahead hold logic.
- Tests cover FIRE, WATCH, PASS, soft escalation, hard escalation, and emergency conditions for both chips.
- Tests explicitly prove force logic cannot bypass `Bench Boost` blank-starter vetoes or `Triple Captain` blank-captain vetoes.

**Owner agent**: unassigned
**Time window**: 2026-03-19T22:40:00Z -> TBD
**Coordination flag**: solo

**Tests to run**:

- `pytest cheddar-fpl-sage/tests/test_chip_engine_bench_boost_triple_captain.py`

**Manual validation**:

- Review one `WATCH` result and confirm `watchUntil` is populated.
- Review one forced FIRE result and confirm `forcedBy` is present and readable.

**Guard for WI closeout**:

- Keep all scoring and veto logic in the new evaluator module; do not split formulas across frontend or legacy selector files.
- Keep all changes inside the scoped files.

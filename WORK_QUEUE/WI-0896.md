# WI-0896: Correct model_outputs Ownership Contract and Source Metadata

**ID**: WI-0896
**Goal**: Fix ownership drift by aligning model_outputs API contract, comments, and source metadata with actual writers (including FPL).

**Depends on**: none

**Scope**:
- web/src/app/api/model-outputs/route.ts
- apps/worker/src/jobs/run_fpl_model.js
- docs/

**Out of scope**:
- Changing model_outputs schema
- Changing NHL/NBA write strategy
- Replacing model_outputs endpoint behavior

**Acceptance**:
- /api/model-outputs docs/comments list real writer set.
- FPL-origin rows are distinguishable via explicit source/model metadata conventions.
- Any dashboards/readers depending on old assumptions are updated.
- Contract test or assertion added to prevent ownership drift regression.

**Owner agent**: unassigned
**Time window**: 2026-04-11 -> TBD
**Coordination flag**: needs-sync

**Tests to run**:
- npm --prefix web run build

**Manual validation**:
- Query /api/model-outputs and verify returned rows include FPL-origin metadata when present.
- Confirm route description matches current runtime behavior.

CLAIM: unassigned
# WI-0896: Correct model_outputs Ownership Contract and Source Metadata

**ID**: WI-0896
**Goal**: Fix the JSDoc comment in `web/src/app/api/model-outputs/route.ts` which omits `run_fpl_model.js` from the writer list, and add a contract test asserting FPL-origin rows are distinguishable by sport/model_name.

**Context**: `run_fpl_model.js` already writes `sport: 'FPL'` and `modelName: 'fpl-model-v1'` — no change to the writer is needed. `packages/data/src/db/models.js` already lists all three writers correctly. The only drift is a stale comment in the route file.

**Depends on**: none

**Scope**:
- web/src/app/api/model-outputs/route.ts
- web/src/__tests__/model-outputs-writers.contract.test.js
- web/package.json

**Out of scope**:
- Changing model_outputs schema
- Changing NHL/NBA write strategy
- Replacing model_outputs endpoint behavior
- Any changes to apps/worker/src/jobs/run_fpl_model.js (already writes correct metadata)
- Any changes to packages/data/src/db/models.js (already lists all writers correctly)
- Exporting a KNOWN_MODEL_OUTPUT_WRITERS symbol from any module (keep it test-local)

**Acceptance**:
- JSDoc comment in `web/src/app/api/model-outputs/route.ts` lists all three writers: `run_mlb_model.js`, `run_nfl_model.js`, `run_fpl_model.js`.
- `web/src/__tests__/model-outputs-writers.contract.test.js` exists and passes. The test defines `KNOWN_MODEL_OUTPUT_WRITERS` as a local `const` array (not imported from any module), then asserts: (1) the array contains exactly `['run_mlb_model.js', 'run_nfl_model.js', 'run_fpl_model.js']`, and (2) a synthetic FPL row object with `sport: 'FPL'` and `model_name: 'fpl-model-v1'` passes the distinguishability checks.
- `web/package.json` has a `test:model-outputs-writers` script: `node --import tsx/esm src/__tests__/model-outputs-writers.contract.test.js` (run from `web/` as cwd, matching the convention used by all other test scripts in that file).

**Owner agent**: unassigned
**Time window**: 2026-04-11 -> TBD
**Coordination flag**: needs-sync — `web/src/app/api/**` is a serialized shared touchpoint per AGENTS.md. No concurrent WI may claim `web/src/app/api/model-outputs/route.ts` in the same time window. Joint review required before staging.

**Tests to run**:
- npm --prefix web run test:model-outputs-writers
- npm --prefix web run build

**Manual validation**:
- Confirm route JSDoc header lists run_mlb_model.js, run_nfl_model.js, and run_fpl_model.js.

CLAIM: unassigned

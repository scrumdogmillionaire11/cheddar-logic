# WI-1131: Standardize Auth Secret Hardening Across Shared Modules

**ID**: WI-1131 - DEFERRED WITH OTHER AUTH WORK
**Goal**: Remove permissive auth-secret fallback behavior from shared runtime helpers and align with production hard-fail policy.

**Scope**:
- packages/data/src/auth.js
- web/src/lib/api-security/jwt.ts
- packages/data/src/db/auth-store.js
- web/src/__tests__/auth-secret-policy.test.js

**Out of scope**:
- Session model redesign
- Auth product feature changes

**Acceptance**:
- Shared auth helper no longer defaults silently to insecure placeholder in production paths.
- Auth secret handling behavior is consistent between web and shared data modules.
- Regression tests cover valid secret, missing secret, and insecure-placeholder scenarios.

**Owner agent**: unassigned
**Time window**: 2026-04-21T00:00:00Z -> TBD
**Coordination flag**: needs-sync
**Depends on**: WI-1124

**Tests to run**:
- node web/src/__tests__/auth-secret-policy.test.js
- npm --prefix apps/worker run test -- src/jobs/__tests__/execution-gate.test.js --runInBand

**Manual validation**:
- Verify production-mode startup behavior fails clearly when secret policy is violated.

**Guard for WI closeout**:
- Before marking complete: confirm every test command in Tests to run is runnable from repo root (i.e., exists in web/package.json scripts or is a valid node path/to/test.js command).
- If a script is added to web/package.json as part of this WI, add it to this WI's Tests section with the full npm --prefix web run script form.

CLAIM: unassigned 2026-04-21T00:00:00Z

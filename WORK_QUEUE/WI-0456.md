# WI-0456: Fix Web Lint Regressions Blocking CI

**ID**: WI-0456
**Goal**: Resolve current `web` lint failures by removing the unused symbol warning and restoring React hook ordering correctness.

**Scope**:
- `WORK_QUEUE/WI-0456.md`
- `web/src/components/cards-page-client.tsx`
- `web/src/components/fpl-dashboard.tsx`

**Out of scope**:
- Feature behavior changes beyond lint compliance
- Any worker/data-layer updates
- Repo-wide lint or formatting changes

**Acceptance**:
- `npm --prefix web run lint` exits with code 0
- `react-hooks/rules-of-hooks` violations are removed from `fpl-dashboard.tsx`
- `@typescript-eslint/no-unused-vars` warning is removed from `cards-page-client.tsx`

**Owner agent**: codex
**Time window**: 2026-03-15T00:06:33Z -> TBD
**Coordination flag**: solo

**Tests to run**:
- `npm --prefix web run lint`

**Manual validation**:
- Confirm FPL dashboard still renders without runtime hook errors in local dev flow.

**Guard for WI closeout**:
- Before marking complete: confirm every test command in `Tests to run` is runnable from repo root (i.e., exists in web/package.json scripts or is a valid `node path/to/test.js` command).
- If a script is added to web/package.json as part of this WI, add it to this WI's Tests section with the full `npm --prefix web run <script>` form.

CLAIM: codex 2026-03-15T00:06:33Z

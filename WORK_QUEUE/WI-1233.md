# WI-1233 Execution Prompt — Worker Lock Incident RCA + Safe Hardening

You are assigned WI-1233.

Primary rule: do not write hardening code until RCA evidence proves the failure mode.

Read:
- WORK_QUEUE/WI-1233.md
- apps/worker/src/schedulers/main.js
- apps/worker/src/jobs/check_pipeline_health.js
- packages/data/src/db/connection.js
- packages/data/src/job-runtime.js
- current settlement runtime jobs:
  - settle_pending_cards.js
  - settle_game_results.js

Do not assume legacy settle_cards.js is involved unless runtime evidence proves it.

## Phase 0 — Evidence Pack First

Collect and attach:

1. Worker logs for 2026-05-03 14:30–14:40 ET
2. DB job_run rows for 2026-05-03T18:30:00Z–18:45:00Z
3. Settlement job rows for:
   - settle_pending_cards
   - settle_game_results
4. Scheduler heartbeat / tick state around the incident
5. Lock-owner evidence:
   - lockfile contents if available
   - PID liveness behavior
   - acquireDbFileLock refusal/reclaim logs

Classify the incident as exactly one primary failure mode:

- UNHANDLED_EXCEPTION
- LIVE_LOCK_OWNER_CONTENTION
- LONG_RUNNING_JOB_OVER_DEADLINE
- PROCESS_CRASH_OR_RESTART_LOOP
- INSUFFICIENT_EVIDENCE

If INSUFFICIENT_EVIDENCE, stop and document what evidence is missing. Do not invent root cause.

## Phase 1 — Hardening Constraints

Allowed:
- Better structured logs around DB lock refusal, owner PID, owner metadata, and reclaim reason
- Scheduler health visibility for long-running ticks and missing completion markers
- Cooperative cancellation only where jobs explicitly support abort
- Fail-fast at scheduler/process boundary if safe
- Runbook updates

Forbidden:
- No age-only lock deletion
- No `CHEDDAR_DB_ALLOW_MULTI_PROCESS=true` production path
- No second DB writer
- No wrapper-only `Promise.race` that leaves background DB work running
- No production kill tests
- No betting/model threshold changes

## Required Implementation Targets

Only after RCA gate passes, update the minimum necessary files from:

- packages/data/src/db/connection.js
- packages/data/src/job-runtime.js
- apps/worker/src/schedulers/main.js
- apps/worker/src/jobs/check_pipeline_health.js
- docs/runbooks/worker-lock-recovery.md
- related tests

## Required Tests

Run or add coverage for:

```bash
npm --prefix apps/worker run test:scheduler:windows -- --runInBand
npm --prefix apps/worker run test:pipeline:settlement -- --runInBand
npm --prefix apps/worker run test -- src/schedulers/__tests__/scheduler-job-failure-degraded-state.test.js --runInBand
npm --prefix apps/worker run job:check-pipeline-health
````

Add regression tests proving:

1. Live PID lock is never reclaimed by age alone
2. Dead PID lock can be reclaimed
3. Scheduler emits degraded/alertable state for prolonged running job
4. Job runtime cleanup still closes DB in failure paths
5. Settlement runtime names use current jobs, not legacy assumptions

## Closeout Requirements

Return:

1. Incident timeline with exact log/DB evidence
2. Primary failure mode
3. Code changes made
4. Tests added/updated
5. Commands run + results
6. Runbook update summary
7. Explicit proof that no forbidden behavior was introduced

Do not close WI-1233 unless the RCA is evidence-backed and lock safety remains single-writer.

```

The biggest fix: **make “INSUFFICIENT_EVIDENCE → stop” explicit.** Otherwise an agent will cosplay Sherlock Holmes, pick a convenient villain, and patch the lock layer like a raccoon with sudo. WI already says evidence-first and bans unsafe age-based lock deletion / multi-writer bypasses; this version makes those gates executable. :contentReference[oaicite:0]{index=0}
```

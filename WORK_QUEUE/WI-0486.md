# WI-0486: Phase 4 soak-window runbook + weekly go/no-go cadence

**ID**: WI-0486
**Goal**: Formalize the 14-30 day telemetry soak process with weekly go/no-go checkpoints and explicit rollback actions tied to threshold breaches.

**Depends on**:

- `WORK_QUEUE/WI-0485.md`

**Scope**:

- `docs/DATA_PIPELINE_TROUBLESHOOTING.md`
- `docs/QUICKSTART.md`
- `docs/API_BASELINES.md`
- `WORK_QUEUE/WI-0486.md`

**Out of scope**:

- New worker jobs or schema changes
- New feature flags
- Any updates to `web/src/app/api/**`

**Acceptance**:

1. Docs include a day-based soak checklist (Day 0, Day 7, Day 14, Day 30) with concrete pass/fail criteria.
2. Docs define exact weekly operator commands and evidence capture format (command output + SQL parity checks).
3. Docs map each breach type to a single rollback action owner (Model Ops vs Settlement Ops vs Incident Commander).

**Owner agent**: unassigned
**Time window**: TBD -> TBD
**Coordination flag**: solo

**Tests to run**:

- `npm --prefix apps/worker run job:report-telemetry-calibration`
- `npm --prefix web run test:api:games:market`
- `npm --prefix web run test:decision:canonical`

**Manual validation**:

- Perform one dry-run of the weekly checklist from repo root and verify each command is copy/paste runnable.
- Confirm docs call out sample-size gates before any threshold is interpreted as breach.
- Validate rollback steps are aligned with existing flag defaults in `env.example`.

**Guard for WI closeout**:

- Before marking complete: confirm no contradictory threshold numbers across the three docs in scope.
- Confirm every command in the soak checklist includes full repo-root form.

CLAIM: github-copilot 2026-03-18T00:00:00Z

# WI-1121: Results Calibration UI and API Mapping

**ID**: WI-1121
**Goal**: Update `/results` so F5 ML rows expose real prediction signal, calibration fields, and official-pick versus calibration-only status.

**Scope**:
- Covered by active umbrella execution scope in `WORK_QUEUE/WI-1117.md`

**Out of scope**:
- Non-results page UI redesign

**Acceptance**:
- `/api/results/projection-accuracy` defaults to 30 days and accepts `MLB_F5_ML`.
- `/api/results/projection-settled` exposes F5 ML win probability, edge pp, confidence score/band, tracking role, calibration bucket, Brier score, and expected outcome label.
- F5 ML UI displays `TEAM (57.8%)`, edge as percent points, confidence as `HIGH/MED/LOW (score)`, and actual as `WIN/LOSS/PUSH`.
- Legacy rows without prediction signal do not display fake `+0.50` edge or `UNKNOWN` confidence.

**Owner agent**: codex
**Time window**: 2026-04-21 -> TBD
**Coordination flag**: needs-sync
**CLAIM**: codex 2026-04-21T19:44:19Z

**Tests to run**:
- `npm --prefix web run test:api:results:projection-split`
- `npm --prefix web run test:ui:results`

**Manual validation**:
- Open `/results` and inspect F5 ML rows for probability, edge, confidence, and expected-vs-actual labels.

**Guard for WI closeout**:
- Confirm changed files match `WI-1117` umbrella scope.

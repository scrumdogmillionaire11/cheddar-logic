# WI-0385: NHL Totals Canonical Wiring + Inflation Calibration

**ID**: WI-0385
**Goal**: Make NHL totals projection display canonical-first and calibrate NHL totals/1P inflation by regressing/capping pace output, tightening goalie impact, capping modifier stacking, and adding probability sanity rails.

**Scope**:
- `web/src/components/cards-page-client.tsx`
- `web/src/lib/game-card/transform.ts`
- `web/src/app/api/games/route.ts`
- `web/src/__tests__/cards-1p-projection-source.test.js`
- `web/src/__tests__/cards-total-projection-source.test.js`
- `apps/worker/src/models/nhl-pace-model.js`
- `apps/worker/src/models/index.js`
- `packages/models/src/edge-calculator.js`
- `apps/worker/src/models/__tests__/nhl-1p-totals.test.js`
- `apps/worker/src/models/__tests__/nhl-pace-calibration.test.js`
- `WORK_QUEUE/WI-0385.md`

**Out of scope**:
- Odds ingestion/provider changes
- DB schema/migration changes
- Non-NHL card rendering behavior changes
- Historical backfill/rewrite of existing card payload rows

**Acceptance**:
- NHL total display path uses `nhl-totals-call` as primary source and `nhl-pace-totals` as fallback.
- `/api/games` normalization includes `driverInputs.expected_total` in projected-total recovery chain.
- `/api/games` run filtering includes per-game fallback retrieval so partial active runs do not degrade unrelated games.
- Transform tie-break logic explicitly prefers `nhl-totals-call` over `nhl-pace-totals` when market/side compete.
- Existing NHL 1P display behavior remains unchanged (`nhl-pace-1p` source).
- NHL pace total path stores and uses `raw_total_model` and regressed/clamped total values.
- NHL pace total pre-market output is clamped to `[5.0, 7.6]` with clamp flags when triggered.
- Goalie directional effect is tightened, certainty-scaled, and non-directional for unknown certainty.
- Modifier stack is capped (absolute max 0.70 goals from non-base modifiers) with breakdown telemetry.
- Total probability conversion includes NHL sanity rails and edge clamp flags.
- NHL total and NHL 1P projection cards are emitted whenever projections are available, independent of final PLAY/PASS call.
- New and updated tests pass for source-priority wiring and NHL calibration behavior.

**Owner agent**: codex
**Time window**: 2026-03-10T21:05:45Z -> TBD
**Coordination flag**: solo

**Tests to run**:
- `node web/src/__tests__/cards-1p-projection-source.test.js`
- `node web/src/__tests__/cards-total-projection-source.test.js`
- `set -a; source .env; set +a; npm --prefix apps/worker run job:run-nhl-model`
- `npm --prefix apps/worker test -- --runTestsByPath src/models/__tests__/nhl-1p-totals.test.js src/models/__tests__/nhl-pace-calibration.test.js src/models/__tests__/cross-market.test.js`

**Manual validation**:
- Open `/cards` and confirm NHL "Model" total reflects `nhl-totals-call` projection when present.
- Confirm NHL 1P row still uses `nhl-pace-1p` projection.
- Confirm NHL total and NHL 1P projection rows render even when the card action is PASS.
- Confirm latest NHL pace cards show reduced projection inflation and no unrealistic probability spikes without clamp flags.

CLAIM: codex 2026-03-10T21:05:45Z

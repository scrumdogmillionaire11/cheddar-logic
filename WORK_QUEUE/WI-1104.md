# WI-1104: Web Games Read Path Debt — Isolate Legacy Repair Adapter

**ID**: WI-1104
**Goal**: Reduce fragility in the web games read path by isolating legacy repair into a versioned adapter boundary while preserving current API behavior.

**Scope**:
- `web/src/lib/games/route-handler.ts`
- `web/src/lib/game-card/transform/index.ts`
- `web/src/lib/game-card/transform/legacy-repair.ts`
- `web/src/lib/game-card/transform/adapters/v1-legacy-repair.ts` (new)
- `web/src/__tests__/api-games-repair-budget.test.js`
- `web/src/__tests__/integration/games-pipeline-contract.test.ts`
- `web/src/__tests__/game-card-transform-hardening.test.js`

**Out of scope**:
- UI layout/styling changes
- API route path changes
- Removal of legacy fallback behavior in this tranche
- Any worker-side payload writer changes

**Acceptance**:
- Route-handler and transform entrypoints call a single adapter module for legacy repair behavior.
- Existing games API and transform contract tests pass without baseline fixture updates.
- New adapter has explicit interface docs in-code for accepted payload versions.
- No net increase in route-handler branching complexity.

**Owner agent**: unassigned
**Time window**: TBD
**Coordination flag**: needs-sync on `web/src/app/api/**` contract-adjacent surface

**Tests to run**:
- `node web/src/__tests__/api-games-repair-budget.test.js`
- `node web/src/__tests__/game-card-transform-hardening.test.js`
- `npm --prefix web run test -- src/__tests__/integration/games-pipeline-contract.test.ts --runInBand`

**Manual validation**:
- Compare `/api/games` response samples before/after on the same fixture DB and confirm parity for repaired legacy cards.
- Verify diagnostics fields remain present for malformed-card fallback paths.

**Guard for WI closeout**:
- Before marking complete: confirm every test command in `Tests to run` is runnable from repo root.
- If a script is added to web/package.json as part of this WI, add it to this WI's Tests section with the full `npm --prefix web run <script>` form.

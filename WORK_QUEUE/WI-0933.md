# WI-0933: NHL wrapper hardening for stale snapshots + mixed-book price integrity

**ID**: WI-0933
**Goal**: Prevent false confidence from stale snapshots and mixed-book pricing by enforcing freshness-first handling and source-integrity gating before executable recommendations.

**Depends on**: WI-0932

**Scope**:

- `apps/worker/src/jobs/execution-gate.js`
- `apps/worker/src/jobs/run_nhl_model.js`
- `apps/worker/src/jobs/check_pipeline_health.js`
- `apps/worker/src/jobs/refresh_stale_odds.js`
- `apps/worker/src/jobs/__tests__/execution-gate.test.js`
- `apps/worker/src/jobs/__tests__/check_pipeline_health.test.js`
- `apps/worker/src/jobs/__tests__/negative-path-gates.test.js`

**Out of scope**:

- NHL projection model adjustments
- Discord copy/style adjustments outside reason-backed stale/integrity wording
- MLB/NBA market-specific execution policy changes

**Acceptance**:

- Wrapper marks stale-input paths with explicit hold/block reason codes and does not surface them as normal slight-edge opportunities.
- Execution gate enforces mixed-book source integrity guard for line/price pairing before executable status.
- Health and refresh jobs expose stale-snapshot diagnostics sufficient to distinguish: detected, refreshed, and blocked.
- Regression tests cover stale snapshot block behavior and mixed-book integrity block behavior.

**Owner agent**: pax-executor
**Time window**: 2026-04-13T00:00:00Z -> TBD
**Coordination flag**: solo

**Tests to run**:

- `npm --prefix apps/worker run test -- --runInBand src/jobs/__tests__/execution-gate.test.js`
- `npm --prefix apps/worker run test -- --runInBand src/jobs/__tests__/negative-path-gates.test.js`
- `npm --prefix apps/worker run test -- --runInBand src/jobs/__tests__/check_pipeline_health.test.js`

**Manual validation**:

- Simulate stale NHL odds and a mixed-book line/price mismatch, then run `npm --prefix apps/worker run job:run-nhl-model`; verify no executable FIRE output is emitted for those rows.

CLAIM: pax-executor 2026-04-13T00:00:00Z

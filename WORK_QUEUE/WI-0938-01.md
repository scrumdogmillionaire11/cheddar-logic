# WI-0938-01: THE BOARD Architecture + Feature Flag Strategy (PLAN)

**ID**: WI-0938-01  
**Parent**: WI-0938  
**Goal**: Define THE BOARD architecture, feature flag strategy, phased delivery roadmap, and integration points without breaking existing systems.

**Depends on**: none

**Scope**:

- `.planning/phases/market-board/WI-0938-01-PLAN.md` (create new)
- Feature flag configuration framework design (FEATURE_BOARD_ENABLED or similar)
- Architecture decision: where enricher function lives in pipeline
- Integration points: decision-publisher, web API layer, Discord formatter
- Phased delivery roadmap (skeleton → components → enrichment → production)

**Out of scope**:

- Implementation of any components (that's WI-0938-02 onward)
- Worker-side changes (edge reasoning lives in web layer)
- Database schema changes

**Acceptance**:

- `.planning/phases/market-board/WI-0938-01-PLAN.md` exists with:
  - Architecture diagram (text or ASCII) showing where enricher sits in pipeline
  - Feature flag strategy documented (how to enable/disable `/board` route)
  - Phased delivery roadmap (skeleton → 3 tabs → enricher → integration → production)
  - Integration points explicitly called out (decision-publisher, web endpoints, Discord)
  - Backward compatibility strategy (all edge_reasoning fields optional)
  - Risk assessment (what breaks if enricher fails, rollback plan)
  - Timeline estimate for each phase
  - Testing strategy (unit tests, integration tests, manual validation)
  
- Feature flag skeleton code exists (e.g., environment variable documentation, configuration examples)
- Decision: enricher runs where (answer: web API layer post-watchdog, or decision-publisher in worker? decide here)
- No implementation code written yet

**Owner agent**: unassigned  
**Time window**: 1-2 days of planning work  
**Coordination flag**: none  

**Tests to run**:

- N/A (planning phase only)

**Manual validation**:

- Review `.planning/phases/market-board/WI-0938-01-PLAN.md` with team
- Confirm feature flag strategy understood
- Confirm all integration points identified
- Confirm no blockers for WI-0938-02

**Notes**:

- Reference `docs/THE_BOARD_VALUE_ONE_PAGER.md` for product requirements
- Reference `WORK_QUEUE/THE_BOARD_EDGE_SCHEMA_SPEC.md` for data schema
- Key decision: when/where does edge_reasoning get attached to card payload?
  - Option A: Web API layer (cleanest separation, no worker changes)
  - Option B: Decision publisher in worker (but breaks single-writer contract if web enriches)
  - Recommend Option A

CLAIM: unassigned

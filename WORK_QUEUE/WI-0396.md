# WI-0396: Cards Lifecycle Refetch Race Fix

**ID**: WI-0396
**Goal**: Ensure `/cards` performs the correct active/pregame fetch when lifecycle mode changes during initial mount so live games render immediately.

**Scope**:
- `WORK_QUEUE/WI-0396.md`
- `web/src/components/cards-page-client.tsx`
- `web/src/__tests__/cards-lifecycle-fetch-race.test.js`

**Out of scope**:
- `/api/games` SQL or lifecycle semantics
- Card transform/filter scoring logic
- Visual copy or badge styling changes unrelated to lifecycle fetch correctness

**Acceptance**:
- Loading `/cards?lifecycle=active` does not let an in-flight pregame fetch suppress the active fetch.
- The cards page source contains an explicit retry path when a lifecycle change collides with an in-flight request.
- A targeted regression test covers the lifecycle fetch race and passes.

**Owner agent**: GitHub Copilot
**Time window**: 2026-03-11T00:00:00Z -> TBD
**Coordination flag**: solo

**Tests to run**:
- `node web/src/__tests__/cards-lifecycle-fetch-race.test.js`

**Manual validation**:
- Open `/cards?lifecycle=active` on a cold load and confirm the active game card appears without waiting for the next poll interval.

CLAIM: GitHub Copilot 2026-03-11T00:00:00Z

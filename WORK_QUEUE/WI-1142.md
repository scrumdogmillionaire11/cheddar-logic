---
phase: WI-1142
plan: 01
type: execute
wave: 1
depends_on: [WI-1128, WI-1140]
files_modified:
  - web/src/__tests__/ui-results-smoke.test.js
  - web/src/__tests__/api-results-flags.test.js
  - web/src/__tests__/integration/settlement-parity.test.ts
  - web/src/__tests__/integration/results-behavioral-contract.test.ts
autonomous: true
requirements: [WI-1142]
must_haves:
  truths:
    - "Critical /api/results and /api/results/projection-settled invariants are verified by behavioral tests using seeded fixtures."
    - "No source.includes-style assertions remain for migrated invariants."
    - "Behavioral tests cover dedupe, payload-missing visibility, and settlement coverage."
    - "Tests are deterministic and runnable from repo root."
  artifacts:
    - path: "web/src/__tests__/ui-results-smoke.test.js"
      provides: "UI results smoke behavioral contract tests"
    - path: "web/src/__tests__/api-results-flags.test.js"
      provides: "API results flags behavioral contract tests"
    - path: "web/src/__tests__/integration/settlement-parity.test.ts"
      provides: "Settlement parity behavioral contract tests"
    - path: "web/src/__tests__/integration/results-behavioral-contract.test.ts"
      provides: "Results endpoint behavioral contract tests"
  key_links:
    - from: "behavioral contract tests"
      to: "/api/results"
      via: "supertest or fetch"
      pattern: "request.*api/results"
    - from: "behavioral contract tests"
      to: "seeded fixtures"
      via: "fixture loader"
      pattern: "loadFixture|seed"
---

# WI-1142: Replace Results Source-String Assertions With Behavioral Contract Tests

**ID**: WI-1142
**Goal**: Improve confidence in results reliability by replacing source-text assertions with seeded behavioral endpoint tests.

**Scope**:

- web/src/__tests__/ui-results-smoke.test.js
- web/src/__tests__/api-results-flags.test.js
- web/src/__tests__/integration/settlement-parity.test.ts
- web/src/__tests__/integration/results-behavioral-contract.test.ts

**Out of scope**:

- Refactoring `/api/games` tests
- UI visual snapshot framework adoption
- Non-results API test rewrites

**Acceptance**:

- Critical `/api/results` and `/api/results/projection-settled` invariants are verified via request/response behavior using seeded fixtures.
- Existing source.includes-style assertions for migrated invariants are removed.
- New behavioral tests cover dedupe, payload-missing visibility contract, and settlement coverage metadata.
- Tests remain deterministic and runnable from repo root.

**Owner agent**: unassigned
**Time window**: 2026-04-22T11:21:22Z -> TBD
**Coordination flag**: needs-sync
**Depends on**: WI-1128, WI-1140

**Tests to run**:

- npm --prefix web run test:ui:results
- npm --prefix web run test:api:results:flags
- npm --prefix web run test:api:validation-registry
- node web/src/__tests__/integration/settlement-parity.test.ts

**Manual validation**:

- Run `/results` locally and verify Betting Ledger and Projection Settlement both populate with expected rows using seeded fixture state.
- Confirm failing fixtures report clear invariant names (dedupe, coverage, visibility).

**Guard for WI closeout**:

- Before marking complete: confirm every test command in Tests to run is runnable from repo root.
- If a dedicated results behavioral test script is added, include it in this section with full command.

CLAIM: unassigned 2026-04-22T11:21:22Z

<tasks>
<task type="auto">
  <name>Task 1: Replace source-string assertions with behavioral contract tests</name>
  <files>web/src/__tests__/ui-results-smoke.test.js, web/src/__tests__/api-results-flags.test.js, web/src/__tests__/integration/settlement-parity.test.ts, web/src/__tests__/integration/results-behavioral-contract.test.ts</files>
  <action>
    - Identify all source.includes or similar string assertions for /api/results and /api/results/projection-settled invariants in the listed test files.
    - Replace each with a behavioral contract test using seeded fixtures, verifying dedupe, payload-missing visibility, and settlement coverage.
    - Remove the old string-based assertions for migrated invariants.
    - Ensure new tests are deterministic and runnable from the repo root.
  </action>
  <verify>
    <automated>
      npm --prefix web run test:ui:results
      npm --prefix web run test:api:results:flags
      npm --prefix web run test:api:validation-registry
      node web/src/__tests__/integration/settlement-parity.test.ts
    </automated>
  </verify>
  <done>
    - All migrated invariants are covered by behavioral contract tests.
    - No source.includes-style assertions remain for those invariants.
    - All listed test commands pass from the repo root.
  </done>
</task>
</tasks>

<verification>
- All behavioral contract tests pass and cover dedupe, payload-missing visibility, and settlement coverage.
- No source.includes-style assertions remain for migrated invariants.
- All test commands in "Tests to run" section are runnable from repo root and pass.
</verification>

<success_criteria>
- All migrated invariants are verified by behavioral contract tests.
- No string-based assertions remain for those invariants.
- All tests pass and are deterministic.
</success_criteria>

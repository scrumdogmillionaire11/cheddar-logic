# WI-0472: Phase 1 Low-Volatility Integration (NHL Shots + Soccer Projection Basis)

**ID**: WI-0472
**Goal**: Implement Phase 1 low-volatility market integration by attaching decision basis metadata and projection-performance telemetry for NHL shots and soccer projection-only cards.

**Depends on**:

- `WORK_QUEUE/WI-0471.md`

**Scope**:

- `WORK_QUEUE/WI-0472.md`
- `apps/worker/src/jobs/run_nhl_player_shots_model.js`
- `apps/worker/src/jobs/run_soccer_model.js`
- `apps/worker/src/utils/nhl-shots-patch.js`
- `apps/worker/src/utils/soccer-patch.js`
- `apps/worker/src/jobs/__tests__/run_nhl_player_shots_model.test.js`
- `apps/worker/src/jobs/__tests__/run_soccer_model.test.js`
- `apps/worker/src/__tests__/scheduler-soccer-t45.test.js`

**Out of scope**:

- NBA/NCAAM/NFL/MLB decision pipeline threshold tuning
- Web quick-filter UX changes
- CLV ledger writes for odds-backed cards
- Settlement/backfill migration work

**Acceptance**:

1. NHL shots and soccer jobs add `decision_basis_meta` only when `ENABLE_DECISION_BASIS_TAGS=true`.
2. Projection-only cards are tracked via `recordProjectionEntry` only when `ENABLE_PROJECTION_PERF_LEDGER=true`.
3. Flag-off behavior is unchanged for payload contract and decisioning.
4. Soccer Track 2 cards remain execution-ineligible while still surfacing projection context.

**Owner agent**: unassigned
**Time window**: 2026-03-16T00:00:00Z -> TBD
**Coordination flag**: solo

**Tests to run**:

- `npm --prefix apps/worker test -- src/jobs/__tests__/run_nhl_player_shots_model.test.js`
- `npm --prefix apps/worker test -- src/jobs/__tests__/run_soccer_model.test.js`
- `npm --prefix apps/worker test -- src/__tests__/scheduler-soccer-t45.test.js`
- `npm --prefix apps/worker run job:run-soccer-model:test`
- `npm --prefix apps/worker run job:run-nhl-player-shots-model`

**Manual validation**:

- With flags on, verify generated NHL/soccer payloads include `decision_basis_meta` and expected decision basis values.
- Query `projection_perf_ledger` to confirm rows are added for projection-only cards and excluded for odds-backed cards.

**Guard for WI closeout**:

- Before marking complete: confirm every test command in `Tests to run` is runnable from repo root.
- Include one payload sample per sport (NHL + soccer) showing flag-on additive fields.

CLAIM: unassigned

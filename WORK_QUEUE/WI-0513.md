# WI-0513: FPL chip engine foundation contracts + invariant harness

**ID**: WI-0513
**Goal**: Create the deterministic FPL chip-engine foundation so all later chip evaluators share one stateless contract, one horizon suppressor, and one force-escalation rule set.

**Depends on**:

- None

**Scope**:

- `WORK_QUEUE/WI-0513.md`
- `cheddar-fpl-sage/src/cheddar_fpl_sage/analysis/decision_framework/chip_engine/__init__.py`
- `cheddar-fpl-sage/src/cheddar_fpl_sage/analysis/decision_framework/chip_engine/types.py`
- `cheddar-fpl-sage/src/cheddar_fpl_sage/analysis/decision_framework/chip_engine/shared.py`
- `cheddar-fpl-sage/tests/test_chip_engine_invariants.py`

**Out of scope**:

- Per-chip scoring formulas for Wildcard / Free Hit / Bench Boost / Triple Captain
- Wiring `ChipAnalyzer` to the new engine
- Backend/frontend contract updates

**Acceptance**:

- A stateless `GameweekState` / `ChipDecision` contract exists under `chip_engine/` and is reusable by all chip evaluators.
- Shared helpers implement score clamping, season-horizon scoring, better-window suppression, and force-escalation normalization without mutating external state.
- Invariant tests cover: one chip per GW, `WATCH` requires `watchUntil`, forced decisions require `forcedBy`, reason codes are always populated, and force logic never bypasses hard vetoes.
- Test coverage includes at least one horizon-suppression case where a would-be `FIRE` is capped to `WATCH` because a materially better window is imminent.

**Owner agent**: copilot
**Time window**: 2026-03-19T22:30:00Z -> 2026-03-19T23:30:00Z
**Coordination flag**: solo
**CLAIM**: copilot 2026-03-19T22:30:00Z
**Status**: COMPLETE
**Completed**: 2026-03-19
**Commits**: 4365414, 7163467, 0c3ea94, b1268a9
**Evidence**: pytest tests/test_chip_engine_invariants.py → 41 passed in 0.03s

**Tests to run**:

- `pytest cheddar-fpl-sage/tests/test_chip_engine_invariants.py`

**Manual validation**:

- Inspect the new engine contract and confirm `ChipDecision` can represent `FIRE`, `WATCH`, and `PASS` without relying on frontend-only fields.
- Confirm invariant tests explicitly assert `watchUntil`, `forcedBy`, and reason-code behavior.

**Guard for WI closeout**:

- Keep all deterministic shared logic in `chip_engine/shared.py` rather than scattering it into `chip_analyzer.py` or frontend files.
- Do not add per-chip evaluator files in this WI beyond the shared foundation.

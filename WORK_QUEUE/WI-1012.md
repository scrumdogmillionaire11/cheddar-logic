# WI-1012: Fix duplicate `ApiPlay` interface in legacy-repair.ts

**ID**: WI-1012
**Goal**: Eliminate the manually-synced copy of the `ApiPlay` interface in `legacy-repair.ts` by exporting the canonical definition from `transform/index.ts` and importing it in `legacy-repair.ts`. This closes the structural drift risk where a new required field on the canonical type would compile silently but behave incorrectly in the legacy path.

**Depends on**: none

**Scope**:
- `WORK_QUEUE/WI-1012.md`
- `.planning/STATE.md`
- `web/src/lib/game-card/transform/index.ts`
- `web/src/lib/game-card/transform/legacy-repair.ts`

**Out of scope**:
- Changes to any fields or logic in `ApiPlay` beyond making the type exportable
- Changes to any other interface or type in `transform/index.ts`
- `legacy-repair.test.ts` (only update if it references the local `ApiPlay` directly)

**Acceptance**:
- `ApiPlay` interface is exported from `transform/index.ts`
- Local `ApiPlay` declaration in `legacy-repair.ts` is deleted; the file imports `ApiPlay` from `./index` instead
- Comment "must stay structurally compatible with the ApiPlay interface in transform/index.ts" is removed (no longer needed)
- `npm run build` in `web/` passes with no new TypeScript errors
- All existing web tests pass

**Owner agent**: unassigned
**Time window**: unscheduled
**Coordination flag**: solo

**Tests to run**:
- `npm --prefix web run build`
- `npm --prefix web test -- legacy-repair --runInBand`

**Manual validation**:
- Confirm `grep "interface ApiPlay" web/src/lib/game-card/transform/legacy-repair.ts` returns nothing
- Confirm `grep "export.*ApiPlay\|export interface ApiPlay" web/src/lib/game-card/transform/index.ts` returns a match

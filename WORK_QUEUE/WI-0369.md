# WI-0369: Settlement Telemetry Cleanup (W/L-First)

**ID**: WI-0369
**Goal**: Clean up post-WI-0368 settlement telemetry so reporting is W/L-first, with consistent settled counters and no misleading skip logs.

**Scope**:
- `WORK_QUEUE/WI-0369.md`
- `apps/worker/src/jobs/settle_pending_cards.js`
- `web/src/app/api/results/route.ts`
- `web/src/app/results/page.tsx`
- `web/src/__tests__/ui-results-smoke.test.js`
- `docs/ops-runbook.md`

**Out of scope**:
- Reworking settlement eligibility/ledger producer logic from WI-0368
- DB schema migrations
- Historical resettlement imports

**Acceptance**:
- `settle_pending_cards` reports settled/skipped counters that match DB post-state (no false “already settled” skips).
- `/results` can run in W/L-only mode (wins/losses/pushes primary; PnL treated optional/non-blocking).
- Runbook documents W/L-first operational checks and how to interpret optional PnL fields.

**Owner agent**: unassigned
**Time window**: TBD -> TBD
**Coordination flag**: needs-sync

**Tests to run**:
- `npm --prefix apps/worker test -- __tests__/settle-coverage.test.js`
- `node --experimental-strip-types web/src/__tests__/integration/settlement-parity.test.ts`
- `CARDS_API_BASE_URL=http://127.0.0.1:3000 node web/src/__tests__/ui-results-smoke.test.js`

**Manual validation**:
- Verify `/results` headline metrics are interpretable from W/L counts alone.
- Verify settlement logs do not report skipped rows that were actually settled in that run.

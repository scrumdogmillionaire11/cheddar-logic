# WI-1208: Kill Legacy Decision Interpreters

**ID**: WI-1208
**Goal**: Remove all remaining duplicate decision mappers, fallback logic, and stale adapters now that web, Discord, and POTD all read from canonical `DecisionOutcome`. Archive dead code paths and finalize Decision Truth Layer.

**Scope**:
- `web/src/lib/game-card/decision.ts` (remove old decision_v2 mappers; keep DecisionOutcome only)
- `web/src/lib/game-card/transform/index.ts` (remove stale fallback status logic)
- `apps/worker/src/utils/discord-formatter.js` (remove remaining decision_v2 adapter paths)
- `apps/worker/src/jobs/publish_discord_decisions.js` (remove remaining old decision logic, keep DecisionOutcome path only)
- `apps/worker/src/utils/potd-selector.js` (confirm easy hanging dead code removed in WI-1202; remove any remaining adapter paths)
- `apps/worker/src/jobs/publish_potd_selection.js` (confirm easy hanging dead code removed in WI-1202; remove any remaining fallback/consensus logic)
- Archive or delete:
  - `apps/worker/src/utils/decision-v2-to-discord.js` (if exists; replaced by discord-formatter + canonical builder)
  - `apps/worker/src/utils/potd-legacy-adapter.js` (if exists; replaced by potd-selector + canonical builder)
  - Any decision_v2 mapper utilities that are now unused
- Update any remaining decision_v2 imports: confirm they are internal-only (not public API)

**Out of scope**:
- Changing decision_v2 production itself (worker still produces decision_v2; it's now input to DecisionOutcome mapper only)
- API contract changes
- Performance optimization
- Easy hanging dead code removal (completed in WI-1201 and WI-1202)

**Acceptance**:
- All mappers removed; codebase reads DecisionOutcome exclusively on surfaces
- No duplicate status/classification logic in web, Discord, or POTD code
- All existing tests pass (they already consume DecisionOutcome from WI-1200/1/2)
- No TypeScript errors; all imports resolve
- Git log shows clear removal commits (one per adapter/utility)
- Decision_v2 is now internal only (not imported by web/Discord/POTD)
- Code is 10-20% smaller in surface modules (dead code gone)
- No gaps from WI-1201/1202 easy dead code removal (those should have deleted immediately-unused functions)

**Owner agent**: unassigned
**Time window**: 2026-05-02T00:00:00Z -> 2026-05-03T23:59:59Z
**Coordination flag**: solo
**Depends on**: WI-1202

**Tests to run**:
- `npm --prefix web test -- src/` (all web tests, confirm no regression)
- `npm --prefix apps/worker test -- src/jobs/publish_discord_decisions.test.js`
- `npm --prefix apps/worker test -- src/jobs/publish_potd_selection.test.js`
- `npm --prefix apps/worker test -- src/utils/discord-formatter.test.js`
- `npm --prefix apps/worker test -- src/utils/potd-selector.test.js`

**Manual validation**:
- Grep for stale imports: `rg "decision-v2-to-discord|potd-legacy-adapter|old.*decision.*mapper" --type ts --type js` (should find 0)
- Confirm web, Discord, POTD flows still work on staging
- Spot-check: PLAY/SLIGHT_EDGE/PASS counts match across all surfaces

**Guard for WI closeout**:
- All tests pass
- No dead code remaining in surface modules
- No circular imports
- **Critical grep gate must pass** (required before merge):

```bash
rg "status.*PLAY|native.*status|decision_v2.*status(?!.*canonical|builder|DecisionOutcome)|classification|final_play_state|fallbackToLegacy" web/src apps/worker/src --type ts --type js
```

Any remaining hit must be either (1) internal production of `decision_v2`, (2) explicit builder import, or (3) justified in commit message. Zero drift hits allowed.
- **Verification:** Confirm WI-1201 and WI-1202 are merged and present in `WORK_QUEUE/COMPLETE/` before execution; final comprehensive cleanup is owned by `WORK_QUEUE/WI-1203.md`
- Ready for production deployment of Decision Truth Layer

CLAIM: unassigned 2026-05-02T00:00:00Z

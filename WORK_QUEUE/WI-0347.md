# WI-0347: NHL 1P Totals Fixed Reference (`1.5`) Projection-Only Contract

**ID**: WI-0347
**Goal**: Implement NHL 1P totals as projection-only context cards against a fixed `1.5` reference line, without entering wave-1 playable `decision_v2` flow.

**Scope**:
- `WORK_QUEUE/WI-0347.md`
- `apps/worker/src/models/index.js`
- `apps/worker/src/models/__tests__/nhl-1p-totals.test.js`

**Out of scope**:
- `web/src/**`
- `apps/worker/src/utils/decision-publisher.js`
- `packages/models/src/decision-pipeline-v2.js`
- API schema changes

**Acceptance**:
- NHL 1P descriptor uses fixed `1.5` reference line (not `raw.total_1p` / `raw.first_period_total`).
- `nhl-pace-1p` can emit when full-game `oddsSnapshot.total` is missing.
- 1P descriptor remains projection-only (no `market_type`/`selection`/`line`/`price`).
- Unit tests cover fixed-line edge, decoupling from full-game total, raw 1P field ignore behavior, and projection-only descriptor contract.

**Owner agent**: Codex
**Time window**: 2026-03-09T01:28:13Z -> TBD
**Coordination flag**: solo

**Tests to run**:
- `npm --prefix apps/worker test -- src/models/__tests__/nhl-1p-totals.test.js`

**Manual validation**:
- Run NHL model on sample data and verify `nhl-pace-1p` reasoning references projection vs `1.5`.
- Confirm emitted `nhl-pace-1p` payload remains evidence context and does not include playable market fields.

CLAIM: Codex 2026-03-09T01:28:13Z

# WI-1258: Clean Up Unused Variables and Dependencies

**ID**: WI-1258

**Goal**: Eliminate 9 eslint warnings across the web codebase related to unused variables and incorrect React Hook dependency arrays.

**Context**:

Eslint is detecting unused variables and problematic dependency arrays that clutter the build output and create code smell. These should be removed or corrected:

- 6 unused variable warnings (statusFromAction, classificationFromAction, context, FamilyOption, hasActionableProjectionCall, SUPPORTED_CARD_FAMILY_SQL, createProjectionFilterCard)
- 1 missing dependency warning (uiState.viewMode in useEffect)
- 1 unnecessary dependency warning (effectiveFilters in useMemo)

**Scope**:

- `web/src/lib/runtime-decision-authority.ts`
- `web/src/lib/results/transform-layer.ts`
- `web/src/components/results/ProjectionAccuracyClient.tsx`
- `web/src/components/projection-card.tsx`
- `web/src/components/cards/CardsPageContext.tsx`
- `web/src/app/api/results/projection-settled/route.ts`

**Out of scope**:

- Behavioral changes to the application
- Refactoring beyond what's needed to fix linting warnings
- Changes to non-web packages

**Acceptance**:

- All 9 eslint warnings are resolved
- `npm run lint --workspace web` produces no warnings for the targeted files
- Tests pass: `npm --prefix web run test`
- No functional behavior changes introduced
- Changes are minimal and focused on the specific warnings

**Owner agent**: unassigned

**Time window**: 30 minutes

**Coordination flag**: none

**Depends on**: none

**Tests to run**:

```bash
npm run lint --workspace web
npm --prefix web run test
```

**Manual validation**:

- Run eslint output before and after to confirm 9 warnings are eliminated
- Verify no new warnings introduced

CLAIM: unassigned

# WI-1011: Dead code removal sweep — deprecated exports and leftover shims

**ID**: WI-1011
**Goal**: Delete confirmed dead code identified in the 2026-04-18 tech debt audit: the `projectNBA` deprecated function, the `hasFreshOddsForModels` dead import, the `_archive/run_nfl_model.js` stale reference copy, and the `transform.ts` re-export shim whose only remaining consumer can point directly to the source.

**Depends on**: none

**Scope**:
- `WORK_QUEUE/WI-1011.md`
- `.planning/STATE.md`
- `apps/worker/src/models/projections.js`
- `apps/worker/src/models/__tests__/nba-projection-parity.test.js`
- `apps/worker/src/schedulers/quota.js`
- `apps/worker/src/schedulers/main.js`
- `apps/worker/src/jobs/_archive/run_nfl_model.js`
- `web/src/lib/game-card/transform.ts`
- `web/src/components/cards/CardsPageContext.tsx`

**Out of scope**:
- Any changes to `projectNBACanonical` or `analyzePaceSynergy`
- Changes to the active `run_nfl_model.js` in `apps/worker/src/jobs/`
- Any other transform or game-card files beyond the shim and its one consumer
- The `hasFreshInputsForModels` function itself

**Acceptance**:
- `projectNBA` function body and its entry in the `projections.js` export object are deleted
- The test in `nba-projection-parity.test.js` that asserts `projectNBA` is still present is removed or updated to assert it is gone
- `hasFreshOddsForModels` is removed from `quota.js` exports; its import is removed from `main.js`
- `apps/worker/src/jobs/_archive/run_nfl_model.js` is deleted
- `web/src/lib/game-card/transform.ts` shim is deleted
- `CardsPageContext.tsx` import updated from `@/lib/game-card/transform` to `@/lib/game-card/transform/index`
- All existing tests pass with no new failures

**Owner agent**: unassigned
**Time window**: unscheduled
**Coordination flag**: solo

**Tests to run**:
- `npm --prefix apps/worker test -- projections-gate.test.js --runInBand`
- `npm --prefix apps/worker test -- nba-projection-parity.test.js --runInBand`
- `npm --prefix web run build`

**Manual validation**:
- Confirm `grep -r "projectNBA\b" apps/worker/src --include="*.js"` returns only `projectNBACanonical` matches
- Confirm `grep "hasFreshOddsForModels" apps/worker/src/schedulers/main.js` returns nothing
- Confirm `_archive/run_nfl_model.js` is gone from the filesystem
- Confirm `web/src/lib/game-card/transform.ts` is gone and web build succeeds

# WI-0953: Odds Snapshot Timestamp Provenance and Integrity Enforcement

**ID**: WI-0953
**Goal**: Guarantee captured/fetched timestamp provenance is consistent from ingest to runner payload so stale decisions are based on trustworthy snapshot age.

**Depends on**: WI-0950

**Scope**:

- `packages/data/src/db/odds.js`
- `apps/worker/src/jobs/pull_odds_hourly.js`
- `apps/worker/src/jobs/run_mlb_model.js`
- `apps/worker/src/jobs/run_nhl_model.js`
- `apps/worker/src/jobs/__tests__/pull_odds_hourly.test.js`
- `apps/worker/src/jobs/__tests__/run_mlb_model.test.js`
- `apps/worker/src/jobs/__tests__/run_nhl_model.test.js`
- `WORK_QUEUE/WI-0953.md`

**Out of scope**:

- Schema migration adding new columns
- Historical backfill of old snapshots
- Scheduler changes
- UI surface changes

**Acceptance**:

- Ingest path consistently writes `captured_at` and runner age resolver behavior is explicit and deterministic when falling back to secondary timestamp fields.
- Runner emits structured diagnostics for missing, malformed, or non-monotonic snapshot timestamps.
- Execution envelope includes clear timestamp provenance metadata used for stale decisions.
- Tests cover valid timestamps, missing `captured_at` fallback, malformed timestamps, and null-age behavior.

**Tech debt and legacy expectation**:

- Consolidate legacy timestamp fallback parsing in touched runners to a single helper-style behavior.
- Remove stale/duplicated timestamp normalization snippets in-scope.
- No silent legacy fallback paths remain without explicit diagnostic emission.

**Owner agent**: unassigned
**Time window**: 2026-04-15T00:00:00Z -> TBD
**Coordination flag**: solo

**Tests to run**:

- `node --check packages/data/src/db/odds.js`
- `node --check apps/worker/src/jobs/pull_odds_hourly.js`
- `npm --prefix apps/worker run test -- --runInBand src/jobs/__tests__/pull_odds_hourly.test.js`
- `npm --prefix apps/worker run test -- --runInBand src/jobs/__tests__/run_mlb_model.test.js`
- `npm --prefix apps/worker run test -- --runInBand src/jobs/__tests__/run_nhl_model.test.js`

**Manual validation**:

- Inspect recent payloads and confirm timestamp provenance fields are present and coherent for moneyline cards.
- Validate malformed timestamp injection triggers diagnostics without process crash.

**Guard for WI closeout**:

- Before marking complete: provide sample payload snippets showing provenance fields for MLB + NHL.
- Before marking complete: provide one sample diagnostic event for malformed/missing timestamp handling.

CLAIM: unassigned

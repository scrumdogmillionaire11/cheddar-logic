# WI-0481: Phase 3 Preflight Eligibility + SQL Verification (CLV)

**ID**: WI-0481
**Goal**: Define CLV preflight eligibility and SQL verification criteria before enabling `ENABLE_CLV_LEDGER`.

**Depends on**:

- `WORK_QUEUE/COMPLETE/WI-0474.md`

**Scope**:

- `WORK_QUEUE/WI-0481.md`
- `docs/ARCHITECTURE_SEPARATION.md`

**Out of scope**:

- Enabling `ENABLE_CLV_LEDGER=true`
- Editing env flags
- Settlement flow code changes

**Acceptance**:

1. Architecture doc explicitly defines CLV inclusion criteria (odds-backed only) and exclusion criteria (projection-only/synthetic).
2. Preflight SQL checks are documented to verify no unintended CLV capture paths.
3. Go/no-go criteria for Phase 3 activation are documented.

**Owner agent**: unassigned
**Time window**: TBD -> TBD
**Coordination flag**: solo

**Tests to run**:

- `npm --prefix apps/worker test -- src/jobs/__tests__/settle_pending_cards.market-contract.test.js`
- `npm --prefix apps/worker test -- src/jobs/__tests__/settle_game_results.matching.test.js`

**Manual validation**:

- Confirm preflight SQL checks can distinguish odds-backed vs projection-only card populations.
- Confirm activation WI (WI-0482) has all required preconditions satisfied.

**Guard for WI closeout**:

- Before marking complete: confirm every test command in `Tests to run` is runnable from repo root.
- Attach exact SQL snippets to be used during WI-0482 execution.

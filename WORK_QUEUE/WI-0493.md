# WI-0493: Soccer Model Phase 3 — League Differentiation + Away-Win Guardrails

**ID**: WI-0493
**Goal**: Apply league-specific thresholds and away-win guardrails for soccer Tier 1 markets so EPL/MLS/UCL recommendations reflect different market efficiency profiles.

**Depends on**:

- `WORK_QUEUE/WI-0492.md`

**Scope**:

- `apps/worker/src/jobs/run_soccer_model.js`
- `packages/models/src/decision-pipeline-v2.patch.js`
- `apps/worker/src/jobs/__tests__/run_soccer_model.test.js`
- `packages/models/src/__tests__/decision-pipeline-v2.patch.test.js` (new or expanded)
- `docs/SOCCER_MODEL_SPECIFICATION.md`
- `WORK_QUEUE/WI-0493.md`

**Out of scope**:

- New external data providers
- CLV settlement job architecture redesign
- Any NFL/MLB model logic
- Web UI feature additions

**Acceptance**:

1. Soccer cards include and use `league_tag` (`EPL`, `MLS`, `UCL`) for threshold routing in Track 1 odds-backed markets.
2. League-specific threshold map is applied when `ENABLE_MARKET_THRESHOLDS_V2=true` and preserves baseline behavior when false.
3. Away-win recommendations are downgraded unless league-specific hard-cap threshold is met, with explicit reason code `AWAY_WIN_CAPPED`.
4. Existing payload shape remains backward-compatible except additive metadata/reason fields.

**Owner agent**: unassigned
**Time window**: TBD -> TBD
**Coordination flag**: solo

**Tests to run**:

- `npm --prefix apps/worker test -- src/jobs/__tests__/run_soccer_model.test.js`
- `npm --prefix apps/worker test -- src/jobs/__tests__/run_nhl_model.market-calls.test.js`
- `npm --prefix web run test:decision:canonical`
- `npm --prefix web run test:api:games:market`

**Manual validation**:

- Run soccer model with `ENABLE_MARKET_THRESHOLDS_V2=true` and verify league-specific status changes for equivalent edge bands.
- Validate at least one away-win candidate is downgraded with `AWAY_WIN_CAPPED` reason when under cap.
- Re-run with `ENABLE_MARKET_THRESHOLDS_V2=false` and confirm baseline-equivalent status behavior.

**Guard for WI closeout**:

- Before marking complete: capture one representative payload snippet per league (`EPL`, `MLS`, `UCL`) showing threshold routing evidence.
- Confirm no edits outside scope and no NFL/MLB logic changes.

CLAIM: unassigned

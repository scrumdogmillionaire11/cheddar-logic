# WI-0789: Fix nba-availability-gate and nhl-goalie-state (no Jest blocks)

| Field | Value |
|---|---|
| **ID** | WI-0789 |
| **Goal** | Convert nba-availability-gate.test.js and nhl-goalie-state.test.js from custom console.log assertion scripts into proper Jest test()/it() blocks. Jest reports Your-test-suite-must-contain-at-least-one-test on both suites every CI run. |
| **Priority** | High |
| **Depends on** | None |
| **Owner agent** | Unassigned |
| **Time window** | TBD |
| **Coordination flag** | None |

---

## Context

Both files use a custom self-contained assertion loop with zero Jest test()/it() blocks.
Internal logic passes (nba-availability-gate: 6 passed, 0 failed) but Jest marks the suite failed.

---

## Scope

| File | Change |
|---|---|
| apps/worker/src/__tests__/nba-availability-gate.test.js | Wrap assertions in describe()/test() blocks |
| apps/worker/src/__tests__/nhl-goalie-state.test.js | Same |

## Out of Scope

- Changing assertion logic or coverage (purely structural)

---

## Acceptance

- npm --prefix apps/worker test -- --testPathPattern="nba-availability-gate|nhl-goalie-state" exits 0
- Both suites report named passing tests in Jest output (not just suite pass)
- No "Your test suite must contain at least one test" error

## Tests to run

- npm --prefix apps/worker test -- --testPathPattern="nba-availability-gate|nhl-goalie-state"

## Manual validation

- N/A (structural migration only)

---

CLAIM: unassigned

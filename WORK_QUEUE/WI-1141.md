# WI-1141: Enforce Public Package Boundaries Across Worker And Scripts

**ID**: WI-1141
**Goal**: Eliminate cross-package internal `src` imports and enforce package entrypoint contracts to reduce coupling and refactor risk.

**Scope**:

- apps/worker/src/**/*.js
- scripts/**/*.js
- packages/data/index.js
- packages/models/index.js
- packages/odds/index.js
- eslint.config.mjs

**Out of scope**:

- Functional changes to model logic
- Monorepo tooling migration
- Repo-wide code formatting

**Acceptance**:

- Targeted worker/scripts imports use package public entrypoints instead of internal `src` paths.
- Package index exports include any required stable symbols previously sourced through internals.
- Lint or static guard prevents newly introduced cross-package internal imports.
- Existing runtime behavior is unchanged after import-path cleanup.

**Owner agent**: unassigned
**Time window**: 2026-04-22T11:21:22Z -> TBD
**Coordination flag**: needs-sync
**Depends on**: WI-1138

**Tests to run**:

- rg -n "@cheddar-logic/.*/src/" apps/worker/src scripts
- npm --prefix web run test:api:validation-registry
- npm --prefix apps/worker run test -- src/jobs/\_\_tests\_\_/run_nhl_model.test.js --runInBand

**Manual validation**:

- Start worker and run one representative job path to ensure imports resolve without internal `src` references.
- Confirm lint/static guard catches a synthetic bad import in local check.

**Guard for WI closeout**:

- Before marking complete: confirm every test command in Tests to run is runnable from repo root.
- If lint configuration changes, document the exact rule name enforcing package boundaries.

CLAIM: unassigned 2026-04-22T11:21:22Z

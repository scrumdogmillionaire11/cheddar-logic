# WI-0936: NHL player shots legacy-decision payload dedup

**ID**: WI-0936
**Goal**: Reduce duplicated legacy decision payload assembly in the NHL player shots model job by centralizing shared builders, with no behavior change.

**Scope**:

- `WORK_QUEUE/WI-0936.md`
- `apps/worker/src/jobs/run_nhl_player_shots_model.js`
- `apps/worker/src/jobs/__tests__/run_nhl_player_shots_model.test.js`

**Out of scope**:

- Any projection math or model parameter changes
- Schema changes or payload contract removals
- Web route normalization refactors

**Acceptance**:

- Add shared helper(s) for repeated legacy payload assembly (`action/status/classification` and `decision_v2`) used by NHL SOG/BLK card paths.
- Preserve existing payload contract and values for full-game, 1P, BLK, and BLK extra-line cards.
- `npm --prefix apps/worker run test -- --runInBand src/jobs/__tests__/run_nhl_player_shots_model.test.js` passes.

**Owner agent**: copilot
**Time window**: 2026-04-13 -> TBD
**Coordination flag**: solo

**Tests to run**:

- `npm --prefix apps/worker run test -- --runInBand src/jobs/__tests__/run_nhl_player_shots_model.test.js`

**Manual validation**:

- Confirm representative generated payload paths still include `decision_v2.official_status`, `play.decision_v2.official_status`, and unchanged `action/status/classification` mappings.

CLAIM: copilot 2026-04-13T23:59:00Z

# WI-0668: Stabilize result_transformer reload isolation in FPL API contract tests

**ID**: WI-0668
**Goal**: Fix the pre-existing FPL Sage test isolation issue so `backend.services.result_transformer` can be reloaded reliably during contract tests and the full `cheddar-fpl-sage` pytest suite can return green.

**Background**:
`WI-0649` closed its scoped work, but full-suite verification still stops on `cheddar-fpl-sage/tests/api_contract_suite.py::test_transformer_backfills_manager_state_with_rank_aware_strategy` with `ImportError: module backend.services.result_transformer not in sys.modules`. This is a leftover suite-health issue, not an FDR implementation gap.

**Scope**:
- `cheddar-fpl-sage/backend/services/result_transformer.py`
- `cheddar-fpl-sage/tests/api_contract_suite.py`

**Out of scope**:
- Fixture difficulty / FDR logic from `WI-0649`
- Any unrelated failing tests outside the `result_transformer` reload/isolation path
- Broad refactors of backend service loading beyond what is required to stabilize this test path

**Acceptance**:
- `cheddar-fpl-sage/tests/api_contract_suite.py::test_transformer_backfills_manager_state_with_rank_aware_strategy` passes consistently
- `backend.services.result_transformer` reload/import behavior is stable under pytest test isolation
- `cd cheddar-fpl-sage && python -m pytest tests/ -v` exits `0`

**Owner agent**: codex
**Time window**: 2026-03-29T00:00:00-04:00 -> TBD
**Coordination flag**: solo
**LOE**: S

**Tests to run**:
- `cd cheddar-fpl-sage && python -m pytest tests/api_contract_suite.py -v -k result_transformer`
- `cd cheddar-fpl-sage && python -m pytest tests/ -v`

**Manual validation**:
- Confirm the failing contract test no longer raises `ImportError` when reloading `backend.services.result_transformer`
- Confirm the full `cheddar-fpl-sage` suite completes without stopping on the previous reload/isolation failure

**Guard for WI closeout**:
- Before marking complete: confirm both pytest commands above are runnable from repo root and that no files outside the declared scope were changed.

CLAIM: codex 2026-03-29T00:00:00-04:00

# WI-0668: Natural Language → Intent Translation Layer

**ID**: WI-0668
**Goal**: Add a server-side translation layer that maps free-text user input to typed intents, so the Draft Lab accepts natural language without relaxing the internal contract model. Input resolves to a named intent or is rejected with a structured error — no ambiguous passthrough.

**Depends on**:
- WI-0660 (Draft Lab typed intent UI must exist)
- WI-0652 (ProductStore and intent contracts)

**Scope**:
- `WORK_QUEUE/WI-0668.md`
- `cheddar-fpl-sage/backend/services/intent_translator.py`
- `cheddar-fpl-sage/backend/models/intent_models.py` (extend with `UnresolvedIntent`, `IntentTranslationResult`)
- `cheddar-fpl-sage/backend/routers/draft_lab.py` (wire translate endpoint)
- `cheddar-fpl-sage/tests/test_intent_translator.py`

**Out of scope**:
- Changes to any existing typed intent execution path
- Frontend chat/NL input UI (that is a separate WI gated on this one)
- Fine-tuning or embedding models — rule-based keyword/phrase mapping only for v1
- Player name resolution — only constraint-class phrases (safer, bench, ban, lock, risky)

**Intent mapping coverage (v1 minimum)**:

| Phrase class | Resolved intent |
|---|---|
| "make this safer", "too risky", "lower risk" | `risk_down` |
| "go more aggressive", "need upside", "attack mode" | `risk_up` |
| "I don't trust X", "drop X", "ban X" | `ban_player` |
| "keep X", "lock X in", "must start X" | `lock_player` |
| "stronger bench", "bench coverage", "more bench depth" | `bench_priority_up` |
| "lighter bench", "sacrifice bench" | `bench_priority_down` |
| "double Arsenal", "too many X", "exposure concerns" | `ban_team_combo` |
| no recognizable phrase | `unresolved` → structured error with suggestions |

**Acceptance**:
- `POST /api/v1/draft-lab/translate` accepts `{"input": "<free text>"}` and returns `{"resolved_intent": "<name>", "confidence": <0-1>, "params": {...}}` or `{"resolved_intent": "unresolved", "suggestions": [...]}`.
- Resolved intents are structurally identical to directly-submitted typed intents — no special-casing downstream.
- Unrecognized input always returns a structured error with suggestions; never a silent no-op.
- No LLM calls, no external API dependencies — pure deterministic phrase matching in v1.
- `pytest tests/test_intent_translator.py` passes covering: all 7 phrase classes, fuzzy variants, unresolved rejection, case insensitivity.

**Owner agent**: unassigned
**Time window**: TBD
**Coordination flag**: solo
**LOE**: M

**Tests to run**:
- `cd cheddar-fpl-sage && python -m pytest tests/test_intent_translator.py -v`

**Manual validation**:
- POST `{"input": "this feels too risky"}` → response contains `risk_down`.
- POST `{"input": "asdfjkl"}` → response contains `unresolved` and non-empty `suggestions`.
- Existing draft lab typed intent tests remain green after router wiring.

**Guard for WI closeout**:
- Translation layer must be server-side only. Frontend sends raw text; backend resolves and executes. No intent resolution in the browser.
- Do not change any existing typed intent execution contracts.

CLAIM: unassigned

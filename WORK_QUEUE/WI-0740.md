# WI-0740: Enable NHL Player Blocks Card Surfacing

**ID**: WI-0740
**Goal**: Surface NHL player blocked-shots cards. The full BLK pipeline (sync, pull, NST ingest, model math, card builder) is already implemented and the 3 ingest jobs are already scheduled in `player-props.js` at the 09:00 ET window. The only blocker is `NHL_BLK_CARDS_ENABLED` not being documented or enabled. Document both BLK flags in `env.example` and enable `NHL_BLK_CARDS_ENABLED` in the deployment environment so the worker emits `nhl-player-blocks` cards using projection-only synthetic lines (consistent with NO_ODDS repo policy).

**Scope**:
- `env.example`
- `WORK_QUEUE/WI-0740.md`

**Out of scope**:
- Any changes to the model, card builder, DB layer, or scheduler
- Odds API integration (intentionally excluded — projection-only is repo policy for all player props)
- Test coverage for player-prop card paths (zero existing coverage; tracked separately)

**Acceptance**:
- `env.example` documents `ENABLE_NHL_BLK_INGEST=true` and `NHL_BLK_CARDS_ENABLED=true` with comments explaining their role
- `NHL_BLK_CARDS_ENABLED=true` is set in Railway (or deployment `.env`)
- After next worker run logs show `nhl-player-blocks` card emissions for players with sufficient BLK lookback data

**Owner agent**: copilot
**Time window**: 2026-04-02
**Coordination flag**: none
**Tests to run**: none — `env.example` is the only changed file; no automated tests cover player-prop card output
**Manual validation**: After deploying env var, confirm `nhl-player-blocks` cards appear in `card_payloads` for the next model run. Verify `player_blk_logs` and `player_blk_rates` have rows (they should — ingest has been running).

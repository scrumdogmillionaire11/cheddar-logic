# WI-1266: Add Canonical ID De-Dupe Coverage Regression

**ID**: WI-1266
**Goal**: Add end-to-end watchdog coverage proving scheduled game coverage de-duplicates canonical and mapped external game IDs.

**Scope**:
- `apps/worker/src/jobs/check_pipeline_health.js`
- `apps/worker/src/jobs/__tests__/check_pipeline_health.test.js`
- `apps/worker/src/jobs/__tests__/check_pipeline_health.watchdog.test.js`

**Out of scope**:
- New reconciliation job
- Web display changes
- Schema changes

**Acceptance**:
- Test creates same matchup under canonical and external game IDs.
- `game_id_map` links external ID to canonical ID.
- Coverage check counts the matchup once.
- Missing-card diagnostics do not fire for the external row when canonical row has valid coverage.
- Regression fails if duplicate rows produce contradictory lifecycle outcomes.

**Owner agent**: unassigned
**Time window**: 2-3 hours
**Coordination flag**: none
**Depends on**: WI-1264

**Tests to run**:
- `npm --prefix apps/worker test -- check_pipeline_health`

**Manual validation**:
- Execute watchdog tests and inspect diagnostic payloads for single-matchup counting behavior.

**Guard for WI closeout**:
- Keep coverage thresholds unchanged; this WI is regression safety only.

CLAIM: unassigned

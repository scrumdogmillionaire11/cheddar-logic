# WI-1216: Fix Pending Settlement Selector Regression

**ID**: WI-1216

**Goal**:
Restore `settlePendingCards()` success for displayed-final eligible rows by removing the invalid SQL helper call introduced into the pending selector path.

**Scope**:

- `apps/worker/src/jobs/settle_pending_cards.js`
- `apps/worker/src/__tests__/settle-coverage.test.js`
- `apps/worker/src/__tests__/settlement-pipeline-integration.test.js`
- `apps/worker/src/jobs/__tests__/settle_mlb_f5.test.js`

**Out of scope**:

- MLB F5 proxy grading policy changes
- Projection accuracy reporting changes
- Web/API `/results` performance work

**Acceptance**:

- `settlePendingCards()` no longer emits `no such function: isDisplayExemptSettlementCardType` from SQLite.
- Pending settlement continues to require a surfaced `card_display_log` row under strict mode.
- The three failing regression suites pass without relaxing settlement authority rules.
- No unrelated file diffs outside scope.

**Priority**: High

**Owner agent**: codex
**Time window**: 2026-04-29
**Coordination flag**: solo
**Depends on**: WI-1211

**Tests to run**:

- `npm --prefix apps/worker test -- src/__tests__/settle-coverage.test.js --runInBand`
- `npm --prefix apps/worker test -- src/__tests__/settlement-pipeline-integration.test.js --runInBand`
- `npm --prefix apps/worker test -- src/jobs/__tests__/settle_mlb_f5.test.js --runInBand`

**Manual validation**:

- Run `settlePendingCards()` against a fixture with displayed and non-displayed pending rows and confirm only displayed/final-eligible rows are selected.

**Guard for WI closeout**:

- No SQL string in the pending selector references a JavaScript-only helper function.
- Strict settlement authority behavior remains intact for non-displayed rows.

CLAIM: codex 2026-04-29T18:20:00Z

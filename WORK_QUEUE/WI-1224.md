# WI-1224: Persist Explicit MLB F5 Settlement Call in Card Payload Contract

**ID**: WI-1224
**Goal**: Stop inferring MLB F5 proxy settlement side from historical projection values by persisting an explicit settlement call and grading mode on each card payload at creation time, then using that persisted contract everywhere downstream.

**Context**:

The current MLB F5 proxy-results flow still depends on reconstructing intent from `proj_value` after the fact. That is fragile for three reasons:

- The producer knows the intended call when the card is created, but that intent is not persisted in a canonical machine-readable field.
- Settlement/reporting code is forced to reverse-engineer side intent from numeric ranges (`< 3.5`, `> 4.5`, gray zone), which makes historical cleanup and replay awkward.
- Old gray-zone rows can remain visible in `projection_proxy_evals` because the UI reads materialized rows directly; restarting services does not rewrite that historical interpretation.

For MLB F5 projection-only cards, the source of truth should live on the card payload itself. The producer should persist whether the card is:

- an official `UNDER_3_5` call
- an official `OVER_4_5` call
- a gray-zone `TRACK_ONLY` / `NO_CALL`

Downstream settlement, backfill, and results APIs should consume that explicit contract instead of re-deriving the call from projection math.

**Proposed payload contract**:

Add an explicit object on MLB F5 card payloads, for example:

```json
{
  "projection_settlement_policy": {
    "market_family": "MLB_F5_TOTAL",
    "grading_mode": "OFFICIAL",
    "official_call": "UNDER_3_5",
    "reason_code": "CLEAR_UNDER"
  }
}
```

Gray-zone example:

```json
{
  "projection_settlement_policy": {
    "market_family": "MLB_F5_TOTAL",
    "grading_mode": "TRACK_ONLY",
    "official_call": null,
    "reason_code": "GRAY_ZONE_NO_CALL"
  }
}
```

**Scope**:

- `packages/data/src/validators/card-payload.js` — add/validate explicit MLB F5 settlement-policy contract
- `apps/worker/src/jobs/run_mlb_model.js` — write the explicit policy fields when `mlb-f5` cards are produced
- `apps/worker/src/jobs/settle_projections.js` — consume persisted policy instead of inferring side from `projected_total`
- `web/src/app/api/results/projection-settled/route.ts` — use persisted grading-mode metadata (directly or via joined payload) so gray-zone cards never surface as official settled F5 rows
- `apps/worker/src/jobs/__tests__/settle_projections.test.js`
- `apps/worker/src/__tests__/run-mlb-model.dual-run.test.js`
- `web/src/app/api/results/__tests__/projection-settled.test.ts` or equivalent route coverage
- `WORK_QUEUE/WI-1224.md`
- `.planning/STATE.md`

**Out of scope**:

- Reworking the underlying MLB F5 model thresholds themselves
- Retrofitting every historical card payload in one migration
- UI redesign of the projection breakdown table
- Non-MLB-F5 markets

**Acceptance**:

- Every newly created `mlb-f5` card payload contains a validated `projection_settlement_policy` object
- Producer writes one of exactly three explicit states for MLB F5 cards:
  - `OFFICIAL` + `UNDER_3_5`
  - `OFFICIAL` + `OVER_4_5`
  - `TRACK_ONLY` + `official_call=null`
- `settle_projections.js` no longer determines MLB F5 official side by comparing `projected_total` to thresholds; it reads the persisted contract from the card payload
- Results API no longer surfaces gray-zone MLB F5 rows as official settled projection outcomes, even if stale historical proxy rows exist
- Backfill/replay path for missing proxy rows uses the persisted contract, not fresh inference
- Missing/invalid MLB F5 settlement-policy payloads fail closed with explicit logging and do not materialize proxy W/L rows

**Owner agent**: unassigned
**Time window**: 2026-04-30T00:00:00Z -> TBD
**Coordination flag**: needs-sync
**Depends on**: none

**Tests to run**:

- `npm --prefix apps/worker test -- src/jobs/__tests__/settle_projections.test.js --runInBand`
- `npm --prefix apps/worker test -- src/__tests__/run-mlb-model.dual-run.test.js --runInBand`
- `npm --prefix web run test:api:results:projection-settled-contract`

**Manual validation**:

- Create or inspect a fresh `mlb-f5` card payload and confirm `projection_settlement_policy` is present with the expected explicit call/grading mode
- Settle a fresh clear-under card and confirm exactly one official `U3.5` proxy row is written
- Settle a fresh gray-zone card and confirm `actual_result` is written but no official MLB F5 proxy result appears in the projection-settled API response
- Verify a historical gray-zone row no longer surfaces as official settled output once the route honors persisted grading mode

**Guard for WI closeout**:

- No MLB F5 official-settlement path depends on `proj_value < 3.5` / `> 4.5` inference outside the producer contract-generation function
- Validator rejects malformed `projection_settlement_policy` payloads for `mlb-f5`
- Gray-zone semantics are represented explicitly in payload data, not implied by absence

**Residual risks (cannot be fully mitigated in this WI)**:

- Historical rows already materialized in `projection_proxy_evals` may still need one-time cleanup if they predate the new payload contract
- Route-level suppression of stale legacy rows may require a temporary compatibility clause until enough fresh cards are produced under the new contract

CLAIM: unassigned

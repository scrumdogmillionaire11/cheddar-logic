# WI-1195: Lock telemetry calibration against UNKNOWN decision basis leakage

**ID**: WI-1195
**Goal**: Add explicit regression coverage proving telemetry calibration CLV metrics ignore `clv_ledger` rows where `decision_basis='UNKNOWN'`.

**Scope**:

- `WORK_QUEUE/WI-1195.md`
- `apps/worker/src/jobs/__tests__/report_telemetry_calibration.test.js`

**Out of scope**:

- Runtime behavior changes outside telemetry calibration reporting
- Web/admin UI changes
- Schema or migration updates

**Acceptance**:

- Telemetry calibration test suite includes a case that seeds mixed `ODDS_BACKED` + `UNKNOWN` CLV rows and verifies CLV ledger sample/count metrics are based on `ODDS_BACKED` only.
- The regression case asserts both sample-size behavior and threshold math behavior so UNKNOWN rows cannot silently leak back into telemetry CLV metrics.

**Owner agent**: codex
**Time window**: 2026-04-27T00:00:00Z -> TBD
**Coordination flag**: needs-sync
**Depends on**: WI-1194

**Tests to run**:

- `npm --prefix apps/worker run test -- src/jobs/__tests__/report_telemetry_calibration.test.js --runInBand` — Verifies telemetry calibration excludes non-`ODDS_BACKED` rows and retains the explicit UNKNOWN-basis regression lock

**Manual validation**:

- Confirm the test name clearly states `UNKNOWN` basis exclusion behavior.
- Confirm the seeded fixture mixes both `ODDS_BACKED` and `UNKNOWN` `clv_ledger` rows in the same test.
- Confirm the assertions prove UNKNOWN rows do not affect reported CLV sample size, mean, or threshold status.

**Blockers and warnings**:

- **Blocker**: Do not execute WI-1195 before WI-1194 is merged or otherwise present in the working tree, because this test-only lock depends on the runtime filter already existing in `report_telemetry_calibration.js`.
- **Blocker**: Do not close WI-1195 unless the regression test fails when the `decision_basis = 'ODDS_BACKED'` filter is removed or bypassed; this item must prove fail-closed behavior, not restate it.
- **Warning**: This WI shares `apps/worker/src/jobs/__tests__/report_telemetry_calibration.test.js` with WI-1194 and is not parallel-safe with any other item touching that file.
- **Warning**: Keep this WI test-only; any runtime behavior change belongs back in WI-1194 or a new follow-up WI.

**Guard for WI closeout**:

- Before marking complete: confirm the scoped test command runs from repo root and passes.
- Confirm changed files match Scope only.
- Confirm the regression test still names `UNKNOWN` exclusion explicitly and is not folded into a vague generic CLV test.
- Confirm the test covers mixed-basis input in one run rather than seeding only UNKNOWN rows.
- Confirm no runtime files changed while completing this WI.

CLAIM: codex 2026-05-01T00:00:00Z

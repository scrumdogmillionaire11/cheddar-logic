# WI-0739: Remove Legacy MLB Pitcher-K Odds-Backed Helper Code

**ID**: WI-0739
**Goal**: Delete stale MLB pitcher-K odds-backed helper functions and tests that are no longer on the active runtime path after WI-0733 hardens `PROJECTION_ONLY` as the only supported mode.

**Scope**:
- `WORK_QUEUE/WI-0739.md`
- `apps/worker/src/jobs/run_mlb_model.js`
- `apps/worker/src/jobs/__tests__/run_mlb_model.test.js`
- `.planning/todos/pending/2026-04-02-restore-configurable-mlb-pitcher-k-rollout-mode.md`
- `.planning/STATE.md`

**Out of scope**:
- Reintroducing ODDS_BACKED pitcher-K execution
- Changing the WI-0733 projection formula, Poisson ladder, or PASS-only payload contract
- Historical validator compatibility for old `basis='ODDS_BACKED'` rows

**Acceptance**:
- Remove dead helper functions/exports that only serve the old pitcher-K line lookup path (`selectBestPitcherUnderMarket`, `loadPitcherStrikeoutMarkets`, `getPitcherStrikeoutLineForDriver`, and any publish-gate helpers that remain unused).
- Tests no longer maintain dead odds-backed pitcher-K helper behavior.
- `.planning/todos/pending/2026-04-02-restore-configurable-mlb-pitcher-k-rollout-mode.md` is either moved/archived or rewritten to point to WI-0739 if any cleanup remains after implementation.
- `.planning/STATE.md` pending todo count/list is updated accordingly.

**Owner agent**: unassigned
**Time window**: after WI-0733 merges -> TBD
**Coordination flag**: needs-sync

**Tests to run**:
- `npm --prefix apps/worker test -- --runInBand --testPathPattern=run_mlb_model.test.js`

**Manual validation**:
- `rg -n "selectBestPitcherUnderMarket|loadPitcherStrikeoutMarkets|getPitcherStrikeoutLineForDriver" apps/worker/src/jobs/run_mlb_model.js` returns no active function definitions/exports after cleanup.

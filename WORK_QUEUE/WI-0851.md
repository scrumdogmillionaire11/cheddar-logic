# WI-0851: POTD Scheduler Wiring — Dynamic 12–4 PM Window + Mirror Registration

| Field | Value |
|---|---|
| **ID** | WI-0851 |
| **Goal** | Register the POTD publish job and settlement mirror in the worker scheduler using the dynamic 12–4 PM ET posting window and existing job-key idempotency patterns. |
| **Scope** | `apps/worker/src/schedulers/main.js`, `apps/worker/.env.example`, `apps/worker/src/__tests__/scheduler-windows.test.js` |
| **Out of scope** | POTD scoring logic, POTD DB schema, web/API implementation |
| **Acceptance** | (1) `ENABLE_POTD=true` gates all POTD scheduling. (2) The scheduler computes today's earliest eligible game from already-loaded games, derives `target_post_time_et = clamp(earliest_game_et - 90m, 12:00, 16:00)`, and enqueues the publish job only once the current tick reaches that target. (3) `potd|YYYY-MM-DD` prevents duplicate same-day publishes. (4) The settlement mirror runs after canonical settlement jobs, not before them. (5) `.env.example` documents all POTD env vars. (6) Scheduler tests cover before-target, at-target, after-success, and mirror-enqueue behavior. |
| **Owner agent** | codex |
| **Time window** | 2026-04-09T23:50:00Z - until completion |
| **Coordination flag** | none |
| **Tests to run** | `npm --prefix apps/worker run test:scheduler:windows` |
| **Manual validation** | Simulate scheduler ticks across 11:59, target time, and later same-day ticks using a test DB. Confirm POTD enqueues only when expected and never duplicates after success. |

CLAIM: codex 2026-04-09T23:50:00Z

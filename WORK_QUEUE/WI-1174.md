# WI-1174: Audit Legacy Web Read-Path Repair Branches Against Live Payload Reality

**ID**: WI-1174
**Goal**: Determine which legacy game-card repair branches are still required by historical payloads and remove or quarantine only the branches that no longer protect any real rows.

**Scope**:

- `web/src/lib/game-card/transform/legacy-repair.ts`
- `web/src/lib/game-card/transform/adapters/v1-legacy-repair.ts`
- `web/src/lib/game-card/transform/index.ts`
- `web/src/lib/game-card/transform/market-normalize.ts`
- `web/src/lib/games/route-handler.ts`
- `web/src/__tests__/game-card-transform-adapters.test.js`
- `docs/codebase-techdebt.md`

**Out of scope**:

- Broad refactors of the entire game-card transform stack
- Removing welcome-home compatibility paths without evidence they are obsolete

**Implementation notes**:

- The legacy repair surface is still imported by active web read paths, so this is not dead-on-arrival code. The work here is to verify necessity, not assume removability.
- Inventory the exact payload-shape repairs still exercised by historical or fixture-backed rows, distinguish “historical compatibility still needed” from “never observed anymore,” and only delete branches in the second category.
- If evidence is inconclusive, convert this WI into an audit outcome: tighten docs/tests around what must stay and spin follow-on removal WIs for any proven-dead branches.
- Update `docs/codebase-techdebt.md` so the debt register reflects actual remaining compatibility burden rather than stale April assumptions.

**Acceptance**:

- The repo has an explicit, evidence-backed classification of legacy repair branches: still required, safe to remove now, or requires follow-on migration evidence.
- Any branches removed under this WI have regression coverage proving active payload transforms still succeed.
- `docs/codebase-techdebt.md` reflects the post-audit classification instead of treating the whole legacy-repair layer as uniformly dead.

**Owner agent**: github-copilot
**Time window**: 2026-04-24T00:00:00Z -> TBD
**Coordination flag**: solo
**Depends on**: none

**Tests to run**:

- `node web/src/__tests__/game-card-transform-adapters.test.js`
- `npm --prefix web run build`

**Manual validation**:

- Verify representative historical card payloads still render through the web read path after any branch removal.

**Guard for WI closeout**:

- Before marking complete: confirm every test command in `Tests to run` is runnable from repo root.
- Do not delete compatibility branches solely on grep age; removal must be backed by payload evidence or fixture coverage.

CLAIM: github-copilot 2026-04-24T00:00:00Z

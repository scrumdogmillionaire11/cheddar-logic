# WI-0847: POTD Planning Pack — Execution Plans + Work Queue Setup

| Field | Value |
|---|---|
| **ID** | WI-0847 |
| **Goal** | Create the executable planning artifacts for `potd-01-play-of-the-day`: a planning umbrella WI, five implementation WIs, and a corrected PAX-ready `PLAN.md` set that matches the actual repo contracts for settlement, scheduler timing, NHL totals support, and web read-only ownership. |
| **Scope** | `WORK_QUEUE/WI-0847.md`, `WORK_QUEUE/WI-0848.md`, `WORK_QUEUE/WI-0849.md`, `WORK_QUEUE/WI-0850.md`, `WORK_QUEUE/WI-0851.md`, `WORK_QUEUE/WI-0852.md`, `.planning/phases/potd-01-play-of-the-day/potd-01-01-PLAN.md`, `.planning/phases/potd-01-play-of-the-day/potd-01-02-PLAN.md`, `.planning/phases/potd-01-play-of-the-day/potd-01-03-PLAN.md`, `.planning/phases/potd-01-play-of-the-day/potd-01-04-PLAN.md`, `.planning/phases/potd-01-play-of-the-day/potd-01-05-PLAN.md` |
| **Out of scope** | Implementing POTD runtime code, changing worker/web/data source files outside the planning artifacts above, roadmap/state updates unrelated to the POTD plan pack |
| **Acceptance** | (1) Five execution WIs exist for POTD covering DB contract, signal engine, publish + settlement mirror, scheduler, and API/UI. (2) Each WI has required fields, runnable test commands, and a scope that maps cleanly to one implementation slice. (3) The `potd-01` phase has five `PLAN.md` files with valid frontmatter, concrete tasks, verification criteria, and dependencies/waves consistent with the agreed POTD design. (4) The plan pack explicitly uses `insertCardPayload` for settlement compatibility, supports NHL totals, preserves dynamic 12–4 PM timing, and keeps the web layer read-only. |
| **Owner agent** | codex |
| **Time window** | 2026-04-08T16:57:14Z -> TBD |
| **Coordination flag** | none |
| **Tests to run** | Planning artifact only — no runtime tests. Validate by reading each new WI and PLAN file for required fields, scopes, and command shapes. |
| **Manual validation** | Confirm the five WIs can be claimed independently without scope overlap. Confirm the five PLAN files can be executed in wave order without any unresolved architectural decisions. |

## Context

The repo already contains partial POTD planning drafts under `.planning/phases/potd-01-play-of-the-day/`, but they are not execution-safe:

- They assume side-only bets while NHL is totals-only in the active odds config.
- They assume a `card_payloads` insert alone is enough for settlement, but the actual contract requires `insertCardPayload` / locked market handling.
- They simplified away the original dynamic publish timing that the user explicitly wants.

This WI exists to replace those drafts with a correct planning pack before any implementation claim begins.

CLAIM: codex 2026-04-08T16:57:14Z

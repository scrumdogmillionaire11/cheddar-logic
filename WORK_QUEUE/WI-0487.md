# WI-0487: Cross-sport expansion tranche A (MLB/NFL odds-backed markets)

**ID**: WI-0487
**Goal**: Extend decision-basis and CLV-safe telemetry handling to MLB and NFL odds-backed card paths so tranche-A expansion follows the same contracts as NHL/NCAAM/Soccer.

**Depends on**:

- `WORK_QUEUE/WI-0485.md`

**Scope**:

- `apps/worker/src/jobs/run_mlb_model.js`
- `apps/worker/src/jobs/run_nfl_model.js`
- `apps/worker/src/jobs/__tests__/run_mlb_model.test.js` (new)
- `apps/worker/src/jobs/__tests__/run_nfl_model.test.js` (new)
- `docs/ARCHITECTURE_SEPARATION.md`
- `WORK_QUEUE/WI-0487.md`

**Out of scope**:

- Projection-only prop expansion for MLB/NFL
- Any front-end rendering changes
- Lockfile or CI workflow updates

**Acceptance**:

1. MLB and NFL odds-backed cards include `decision_basis_meta` with `decision_basis='ODDS_BACKED'` and `execution_eligible=true` when real market lines are present.
2. CLV telemetry entry conditions remain odds-backed only; no projection-only cards are written to `clv_ledger`.
3. Architecture doc includes updated sport/market matrix for tranche-A enablement and rollback ownership.

**Owner agent**: unassigned
**Time window**: TBD -> TBD
**Coordination flag**: needs-sync

**Tests to run**:

- `npm --prefix apps/worker test -- src/jobs/__tests__/run_mlb_model.test.js`
- `npm --prefix apps/worker test -- src/jobs/__tests__/run_nfl_model.test.js`
- `ENABLE_CLV_LEDGER=true npm --prefix apps/worker run job:settle-cards`
- `npm --prefix apps/worker test -- src/jobs/__tests__/settle_pending_cards.market-contract.test.js`

**Manual validation**:

- Run MLB and NFL model test commands with `ENABLE_DECISION_BASIS_TAGS=true` and inspect recent cards for `decision_basis_meta` presence.
- Validate one settlement cycle with CLV enabled and confirm no `decision_basis='PROJECTION_ONLY'` rows in `clv_ledger`.
- Confirm rollback to `ENABLE_CLV_LEDGER=false` preserves normal settlement writes.

**Guard for WI closeout**:

- Before marking complete: capture one SQL snippet per sport proving odds-backed-only CLV writes.
- Confirm no out-of-scope edits under `web/`.

CLAIM: agent-name YYYY-MM-DDTHH:MM:SSZ

# WI-0488: Cross-sport expansion tranche B (projection props + rollup separation audit)

**ID**: WI-0488
**Goal**: Implement tranche-B projection telemetry coverage for selected MLB/NFL props and verify profitability/reporting surfaces preserve strict separation between odds-backed and projection-only outcomes.

**Depends on**:

- `WORK_QUEUE/WI-0485.md`
- `WORK_QUEUE/WI-0487.md`

**Scope**:

- `apps/worker/src/jobs/run_mlb_model.js`
- `apps/worker/src/jobs/run_nfl_model.js`
- `packages/data/src/db-telemetry.js`
- `apps/worker/src/jobs/report_settlement_health.js`
- `apps/worker/src/jobs/__tests__/run_mlb_model.test.js`
- `apps/worker/src/jobs/__tests__/run_nfl_model.test.js`
- `apps/worker/src/__tests__/settlement-pipeline-integration.test.js`
- `docs/API_BASELINES.md`
- `docs/DATA_PIPELINE_TROUBLESHOOTING.md`
- `WORK_QUEUE/WI-0488.md`

**Out of scope**:

- New web endpoints or schema redesign
- Expanding into additional sports beyond MLB/NFL
- Market-threshold tuning changes

**Acceptance**:

1. Selected projection-only MLB/NFL prop outputs write to `projection_perf_ledger` with `decision_basis='PROJECTION_ONLY'` and do not write to `clv_ledger`.
2. Settlement health reporting keeps profitability separated by decision basis (no mixed aggregates that combine projection-only and odds-backed into one KPI).
3. Troubleshooting and baseline docs include exact parity SQL for separation audits.

**Owner agent**: unassigned
**Time window**: TBD -> TBD
**Coordination flag**: needs-sync

**Tests to run**:

- `npm --prefix apps/worker test -- src/jobs/__tests__/run_mlb_model.test.js`
- `npm --prefix apps/worker test -- src/jobs/__tests__/run_nfl_model.test.js`
- `npm --prefix apps/worker run test:pipeline:settlement`
- `ENABLE_PROJECTION_PERF_LEDGER=true npm --prefix apps/worker run job:settle-cards`
- `npm --prefix apps/worker run job:settlement-report`

**Manual validation**:

- Query both ledgers after one controlled run and confirm projection-only rows are present only in `projection_perf_ledger`.
- Validate settlement report output has explicit segmented metrics (`ODDS_BACKED` vs `PROJECTION_ONLY`).
- Run rollback check with `ENABLE_PROJECTION_PERF_LEDGER=false` and confirm no new projection ledger rows are inserted.

**Guard for WI closeout**:

- Before marking complete: record SQL evidence for inclusion/exclusion and attach to WI notes.
- Confirm updates preserve single-writer DB contract and do not introduce web-side writes.

CLAIM: agent-name YYYY-MM-DDTHH:MM:SSZ

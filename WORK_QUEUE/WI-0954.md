# WI-0954: Moneyline Visibility Contract Across Web Transform and Filters

**ID**: WI-0954
**Goal**: Ensure actionable MLB/NHL moneyline cards that pass worker execution are not silently hidden or reclassified by web transform/filter defaults.

**Depends on**: WI-0952

**Scope**:

- `web/src/lib/game-card/transform/index.ts`
- `web/src/lib/game-card/filters.ts`
- `web/src/components/cards/shared.ts`
- `web/src/__tests__/game-card-decision-authority.test.ts`
- `WORK_QUEUE/WI-0954.md`

**Out of scope**:

- Worker-side model/gate logic changes
- New card component designs
- Changes to non-moneyline market rendering semantics
- API contract versioning

**Acceptance**:

- Transform preserves authoritative worker execution outcomes for moneyline cards unless explicit, test-covered override conditions apply.
- Default active filters do not hide executable moneyline cards due to legacy PASS/projection-only remapping artifacts.
- Tests cover MLB + NHL moneyline rows for EXECUTABLE, BLOCKED, and PROJECTION_ONLY states.
- UI state remains backward compatible for non-moneyline cards.

**Tech debt and legacy expectation**:

- Remove or isolate legacy remap branches in transform/filter code that can override authoritative execution status for moneyline.
- Consolidate duplicate status normalization paths in touched files.
- Any retained legacy compatibility shim must have explicit expiration/follow-up WI note.

**Owner agent**: GitHub Copilot
**Time window**: 2026-04-15T00:00:00Z -> TBD
**Coordination flag**: solo

**Tests to run**:

- `npm --prefix web run test -- --runInBand src/__tests__/game-card-decision-authority.test.ts`
- `npm --prefix web run test`

**Manual validation**:

- Run web app and verify known executable moneyline cards appear in active views by default.
- Confirm blocked/projection-only moneyline cards still display expected status and reason labels.

**Guard for WI closeout**:

- Before marking complete: include before/after UI evidence for at least one MLB and one NHL moneyline card.
- Before marking complete: confirm non-moneyline regression checks pass.

CLAIM: GitHub Copilot 2026-04-15T00:00:00Z

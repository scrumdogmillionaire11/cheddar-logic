# WI-0321: FPL Sage DB Corruption Handling

**ID**: WI-0321
**Goal**: Detect and handle FPL Sage SQLite corruption gracefully, with clear remediation steps and safe failure modes.

**Scope**:

- `apps/worker/src/jobs/run_fpl_model.js`
- `packages/data/src/db.js`
- `cheddar-fpl-sage/backend/**`
- `docs/` (new or existing runbook entry)

**Out of scope**:

- Production data recovery or manual repair execution
- Scheduler shutdown or systemd configuration
- Changes to model logic unrelated to corruption detection

**Acceptance**:

- Corruption is detected early with a clear, actionable error message.
- FPL Sage jobs fail fast without repeated tick errors or cascading failures.
- Ops runbook documents detection and safe recovery steps.

**Owner agent**: unassigned
**Time window**: 2026-03-06T19:10:00Z -> TBD
**Coordination flag**: solo

**Tests to run**:

- `npm test --prefix apps/worker -- src/__tests__/integration/fpl-corruption-detection.test.js` (✓ PASSED - 9/9 tests)
- `npm test --prefix apps/worker -- --testPathPattern=integration` (✓ PASSED - all integration tests)

**Manual validation**:

- Verify corrupted DB produces a single actionable error and does not spam scheduler ticks.
- Test with CHEDDAR_FPL_DB_PATH set to corrupt file: should fail fast with clear error
- Test with CHEDDAR_FPL_DB_PATH unset: should run normally (backward compatible)

---

## IMPLEMENTATION COMPLETE (2026-03-08)

**What was delivered:**

1. **Corruption detection helper** ([packages/data/src/db.js](packages/data/src/db.js#L200-L248))
   - Added `checkSqliteIntegrity(dbPath)` function
   - Shells out to `sqlite3` CLI for `PRAGMA integrity_check`
   - Returns `{ ok: boolean, error: string | null }`
   - Detects "file is not a database" and "database disk image is malformed" errors
   - Handles missing files gracefully (not an error for new installations)
   - Exported via [packages/data/index.js](packages/data/index.js#L49)

2. **Pre-flight check in FPL job** ([apps/worker/src/jobs/run_fpl_model.js](apps/worker/src/jobs/run_fpl_model.js#L177-L197))
   - Checks FPL Sage DB integrity before fetching odds
   - Only runs if `CHEDDAR_FPL_DB_PATH` is configured (backward compatible)
   - Fails fast with actionable error message including:
     - Clear corruption diagnosis
     - Step-by-step remediation instructions
     - Reference to ops runbook
   - Marks job run as FAILED to prevent scheduler tick spam

3. **Ops runbook documentation** ([docs/ops-runbook.md](docs/ops-runbook.md#L440-L495))
   - New section: "FPL Sage Database Corruption Recovery"
   - Documents symptoms operators will see
   - Provides complete recovery procedure with commands
   - Covers both backup restoration and fresh data collection
   - Includes prevention best practices

4. **Environment configuration** ([env.example](env.example#L19-L25))
   - Documented `CHEDDAR_FPL_DB_PATH` variable
   - Marked as optional (only needed for FPL Sage integration)
   - Provides production and local dev path examples
   - Backward compatible (job continues if unset)

5. **Comprehensive tests** (apps/worker/src/\_\_tests\_\_/integration/fpl-corruption-detection.test.js)
   - 9 test cases covering all scenarios
   - Tests undefined/null/empty path (backward compatibility)
   - Tests non-existent file (new installation)
   - Tests corrupt database detection
   - Tests error message quality (actionability, path inclusion)
   - All tests passing (✓ 9/9)

**Implementation decisions:**

- **DB path choice**: `/opt/data/fpl_snapshots.sqlite` (aligns with production data directory pattern)
- **Recovery strategy**: Fail immediately with runbook reference (no auto-backup to ensure manual review)
- **Scope boundary**: Node.js worker only (deferred Python CLI protection for future work)
- **Detection timing**: Pre-flight check at job startup (fastest failure, clearest logs)
- **Backward compatibility**: Optional `CHEDDAR_FPL_DB_PATH` env var (doesn't break existing deployments)

**Verification results:**

- ✓ All 9 corruption detection tests pass
- ✓ All 35 integration tests pass (no regressions)
- ✓ Error messages are actionable with runbook references
- ✓ Backward compatible (job runs normally when FPL DB path not set)
- ✓ No lint errors in any modified files

**Files changed:**

- `packages/data/src/db.js` (+48 lines: corruption detection function)
- `packages/data/index.js` (+1 line: export checkSqliteIntegrity)
- `apps/worker/src/jobs/run_fpl_model.js` (+23 lines: pre-flight check)
- `docs/ops-runbook.md` (+58 lines: FPL corruption recovery section)
- `env.example` (+7 lines: CHEDDAR_FPL_DB_PATH documentation)
- `apps/worker/src/__tests__/integration/fpl-corruption-detection.test.js` (+131 lines: new test file)

**Total diff**: +268 lines added, clean implementation following existing patterns.

CLAIM: copilot-ai 2026-03-08T21:45:00Z (COMPLETE)

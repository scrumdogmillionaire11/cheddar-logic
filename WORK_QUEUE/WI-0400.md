# WI-0400: Unify NBA Total Projection Display + Drift Guardrails

**ID**: WI-0400
**Goal**: Eliminate conflicting NBA total projection numbers by making totals-call projection canonical in UI, aligning worker projection math, and adding soft mismatch telemetry.

**Scope**:
- `WORK_QUEUE/WI-0400.md`
- `web/src/components/cards-page-client.tsx`
- `web/src/app/api/games/route.ts`
- `web/src/__tests__/cards-total-projection-source.test.js`
- `apps/worker/src/models/index.js`
- `apps/worker/src/models/__tests__/nba-total-projection-alignment.test.js`

**Out of scope**:
- Changes to non-total market decision behavior
- DB schema/migrations
- API response shape changes
- Blocking/guardrail behavior that mutates decision outputs

**Acceptance**:
- NBA total projection display prefers `nba-totals-call` and only falls back to `nba-total-projection` when needed.
- Redundant `Model:` line is suppressed when it duplicates the canonical total projection source.
- `nba-total-projection` emitted projection value is aligned to cross-market NBA total projection path.
- Soft warning telemetry is emitted when canonical/fallback total projections differ by more than `0.5` points.
- Regression tests cover source priority, model-line suppression contract, and worker projection parity.

**Owner agent**: codex
**Time window**: 2026-03-12T00:36:09Z -> TBD
**Coordination flag**: solo

**Tests to run**:
- `node web/src/__tests__/cards-total-projection-source.test.js`
- `npm --prefix web run test:ui:cards`
- `npm --prefix apps/worker test -- src/models/__tests__/nba-total-projection-alignment.test.js`
- `npm --prefix apps/worker test -- src/models/__tests__/cross-market.test.js`

**Manual validation**:
- Open cards UI for an NBA totals game and verify only one canonical full-game projection number appears in decision summary context.
- Confirm app logs a warning (non-blocking) when canonical and fallback total projections diverge by more than 0.5 points.

CLAIM: codex 2026-03-12T00:36:09Z

# WI-1162: Canonical Discord Publish Status Contract Across Lines And Props

**ID**: WI-1162
**Goal**: Implement a single cross-market publish-status field in publisher output so Discord eligibility semantics are unified across game lines, game props, and player props.

**Requirements**: [DISCORD-HOOK-01, DISCORD-HOOK-02]

**Roadmap traceability**:
- Executes phase plan: `.planning/phases/discord-hook-eligibility/discord-hook-eligibility-01-PLAN.md`
- This WI introduces the canonical publisher contract used by downstream Discord formatting and posting behavior.

**Scope**:

- `apps/worker/src/utils/decision-publisher.js`
- `apps/worker/src/utils/__tests__/decision-publisher.v2.test.js`
- `WORK_QUEUE/WI-1162.md`

**Out of scope**:

- Modifying Discord message rendering layout
- Removing all legacy fallback logic from Discord formatter
- Backfilling historical payload rows in storage

**Acceptance**:

- [DISCORD-HOOK-01] Publisher stamps `webhook_publish_status` on all card payloads with canonical domain: `PLAY`, `SLIGHT_EDGE`, `PASS_BLOCKED`.
- [DISCORD-HOOK-02] Alias/status normalization for lines/props is centralized in publisher logic; Discord formatter does not gain new inference branches.
- Existing compatibility fields (`webhook_bucket`, `webhook_eligible`, `webhook_lean_eligible`, `webhook_display_side`) remain populated in a backward-compatible manner.
- Tests include representative fixtures for game lines, game props, and player props proving canonical-domain-only output.

**Owner agent**: unassigned
**Time window**: 2026-04-24T00:00:00Z -> TBD
**Coordination flag**: solo
**Depends on**: none

## Execution tasks

<tasks>

<task type="auto">
  <name>Task 1: Add canonical webhook publish status in publisher</name>
  <requirements>DISCORD-HOOK-01</requirements>
  <files>apps/worker/src/utils/decision-publisher.js</files>
  <action>
    Add canonical `webhook_publish_status` stamping to publisher output for every card kind.
    Constrain values to `PLAY`, `SLIGHT_EDGE`, `PASS_BLOCKED` and keep compatibility fields derived consistently.
  </action>
  <verify>
    <automated>npm --prefix apps/worker run test -- --runInBand src/utils/__tests__/decision-publisher.v2.test.js</automated>
  </verify>
  <done>
    Publisher emits canonical status for all tested payload families and no non-canonical values leak.
  </done>
</task>

<task type="auto">
  <name>Task 2: Lock cross-market contract with regression tests</name>
  <requirements>DISCORD-HOOK-02</requirements>
  <files>apps/worker/src/utils/__tests__/decision-publisher.v2.test.js</files>
  <action>
    Add/expand tests covering game lines, game props, and player props to assert canonical status mapping and compatibility field consistency.
  </action>
  <verify>
    <automated>npm --prefix apps/worker run test -- --runInBand src/utils/__tests__/decision-publisher.v2.test.js</automated>
  </verify>
  <done>
    Regression tests prove a single canonical publish-status contract across all in-scope market families.
  </done>
</task>

</tasks>

**Tests to run**:

- `npm --prefix apps/worker run test -- --runInBand src/utils/__tests__/decision-publisher.v2.test.js`
- `rg -n "webhook_publish_status|webhook_bucket|webhook_eligible|webhook_lean_eligible" apps/worker/src/utils/decision-publisher.js`

**Manual validation**:

- Inspect a representative emitted payload for each market family and confirm canonical status is present.
- Confirm compatibility fields still exist so downstream consumers remain stable.

**Guard for WI closeout**:

- Before marking complete: confirm every test command in `Tests to run` is runnable from repo root.
- Confirm canonical domain is exactly `PLAY`, `SLIGHT_EDGE`, `PASS_BLOCKED`.
- Confirm each requirement ID has implementation evidence in code and tests.

CLAIM: unassigned 2026-04-24T00:00:00Z

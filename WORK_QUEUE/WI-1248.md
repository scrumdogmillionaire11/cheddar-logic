# WI-1248: Freeze `/fpl` Public Surface Follow-On

**ID**: WI-1248
**Goal**: Finish removing `/fpl` from the active public-surface story by gating or disabling it with an explicit inactive response and aligned documentation.

**Context**:

The FPL domain is frozen, but the public web surface can still imply an active product. `WI-1223` established the core gating lane. This follow-on closes the public-surface refinement so `/fpl` is not treated as an active operational system.

**Scope**:
- `web/src/lib/api-security/config.ts`
- `web/src/app/fpl/**`
- `web/src/__tests__/fpl-public-surface-freeze.test.js` (new)
- `docs/QUICKSTART.md`
- `WORK_QUEUE/WI-1248.md`

**Out of scope**:
- Rebuilding FPL functionality
- Any change to the external `cheddar-fpl-sage` runtime
- Deleting dormant FPL code paths outright

**Acceptance**:
- `/fpl` is gated or returns an explicit inactive/frozen response rather than behaving like an active public surface.
- Public docs and route behavior are aligned on the inactive state.
- Regression coverage proves public access behavior matches the frozen-domain contract.
- No accidental reactivation path remains implied by public config defaults.

**Owner agent**: unassigned
**Time window**: TBD
**Coordination flag**: needs-sync
**Depends on**: WI-1247, WI-1223

**Tests to run**:
- `node web/src/__tests__/fpl-public-surface-freeze.test.js`

**Manual validation**:
- Access `/fpl` with the frozen/default configuration and confirm the response is explicitly inactive.
- Review public docs and confirm they no longer imply FPL is an active supported surface.

**Guard for WI closeout**:
- Do not reactivate or re-scope frozen FPL feature work in this WI.
- Keep the behavior consistent with the route-gating contract established in `WI-1223`.

CLAIM: unassigned

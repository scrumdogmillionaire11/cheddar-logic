# WI-0669: Fix low-chance availability concern guardrail in transfer advisor

**ID**: WI-0669
**Goal**: Correct transfer advisor availability guardrails so candidates with low `chance_of_playing_next_round` are flagged consistently and the exposed full-suite failure is resolved.

**Background**:
After fixing `WI-0668`, the full `cheddar-fpl-sage` suite advanced to a new failure in `tests/fpl_transfer_guardrails_suite.py::test_candidate_availability_concern_for_low_chance_even_without_doubt_flag`. The current behavior returns `False` for a player with `chance_of_playing_next_round=75`, which contradicts the guardrail contract.

**Scope**:
- `cheddar-fpl-sage/src/cheddar_fpl_sage/analysis/decision_framework/transfer_advisor.py`
- `cheddar-fpl-sage/tests/fpl_transfer_guardrails_suite.py`

**Out of scope**:
- `backend.services.result_transformer` reload/isolation work from `WI-0668`
- Any other suite failures unrelated to availability concern logic
- Broad transfer scoring changes beyond the availability concern predicate

**Acceptance**:
- `tests/fpl_transfer_guardrails_suite.py::test_candidate_availability_concern_for_low_chance_even_without_doubt_flag` passes
- Availability concern logic is explicit and deterministic for `chance_of_playing_next_round`
- `cd cheddar-fpl-sage && python -m pytest tests/fpl_transfer_guardrails_suite.py -v` exits `0`

**Owner agent**: unassigned
**Time window**: 2026-03-29T00:00:00-04:00 -> TBD
**Coordination flag**: solo
**LOE**: S

**Tests to run**:
- `cd cheddar-fpl-sage && python -m pytest tests/fpl_transfer_guardrails_suite.py -v`
- `cd cheddar-fpl-sage && python -m pytest tests/ -v -x`

**Manual validation**:
- Confirm a candidate with `chance_of_playing_next_round=75` is treated as an availability concern even if `status_flag` is not a doubt code
- Confirm no regressions in nearby transfer guardrail cases inside the focused suite

**Guard for WI closeout**:
- Before marking complete: confirm both pytest commands above are runnable from repo root and that no files outside the declared scope were changed.

CLAIM: unassigned

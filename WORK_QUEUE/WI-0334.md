# WI-0334: Harden Static Chunk Availability in Production

**ID**: WI-0334
**Goal**: Prevent and accelerate recovery for production `/_next/static/chunks/*.js` failures that break styles and hydration.

**Scope**:

- `.github/workflows/deploy-production.yml`
- `docs/ops-runbook.md`

**Out of scope**:

- Nginx host config files under `/etc/nginx/**`
- Runtime service/unit ownership changes
- App feature/UI behavior changes

**Acceptance**:

- Deploy workflow verifies Next.js chunk availability after build/restart and fails on missing/unservable chunks.
- Runbook includes a dedicated `/_next/static` 502 troubleshooting path with exact diagnostics and recovery commands.
- No changes outside listed scope files.

**Owner agent**: GitHub Copilot
**Time window**: 2026-03-07T00:00:00Z -> TBD
**Coordination flag**: needs-sync

**Tests to run**:

- `npm --prefix web run build`
- YAML sanity check for `.github/workflows/deploy-production.yml`

**Manual validation**:

- On Pi: verify `curl -I http://127.0.0.1:3000/_next/static/chunks/<sample>.js` returns `200`.
- Public URL for a sampled chunk returns non-502 after deploy.

CLAIM: GitHub Copilot 2026-03-07T00:00:00Z

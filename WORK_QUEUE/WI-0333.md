# WI-0333: Restore plays and stabilize deploys

**ID**: WI-0333
**Goal**: Restore non-degraded plays in dev and production while hardening deployment checks.

**Scope**:

- `web/.env.local`
- `apps/worker/.env.local`
- `env.example`
- `web/src/app/api/games/route.ts`
- `docs/DEPLOY_GITHUB.md`

**Out of scope**:

- `.github/workflows/**`
- `packages/data/db/migrations/**`
- `packages/data/cheddar.db`

**Acceptance**:

- Web and worker resolve the same `CHEDDAR_DB_PATH` with legacy DB vars removed.
- `/api/games` returns plays even if `run_state` is missing or stale (no PASS-only regression).
- Deployment doc includes DB path + run_state checks to prevent no-play releases.

**Owner agent**: GitHub Copilot
**Time window**: 2026-03-07T00:00:00Z -> TBD
**Coordination flag**: solo

**Tests to run**:

- `npm --prefix web run lint` (if available)

**Manual validation**:

- Load `/cards` and confirm FIRE/WATCH plays render in dev.
- Verify `resolveDatabasePath()` matches across web + worker.

CLAIM: GitHub Copilot 2026-03-07T00:00:00Z

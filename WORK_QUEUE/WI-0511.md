# WI-0511: Replace `fetch_failure:unclassified_no_play_pipeline` with root-cause diagnostics

**ID**: WI-0511
**Goal**: Remove the unclassified no-play fallback for NHL game cards by emitting deterministic root-cause diagnostics (or explicit no-edge) from the play pipeline.

**Scope**:

- `WORK_QUEUE/WI-0511.md`
- `web/src/lib/game-card/transform.ts`
- `web/src/app/api/cards/route.ts`
- `web/src/app/api/games/route.ts`
- `web/src/__tests__/cards-1p-projection-source.test.js`

**Out of scope**:

- Team-name alias expansion in `packages/data/src/normalize.js`
- Worker model math/threshold tuning
- DB schema migrations

**Acceptance**:

- No NHL card emitted through `/api/cards` returns `transform_meta.missing_inputs` containing `fetch_failure:unclassified_no_play_pipeline`.
- `PASS_NO_ACTIONABLE_PLAY` is classified as either explicit no-edge (when model no-edge signals are present) or concrete fetch/data-path failure diagnostics.
- If no play-producer signals exist, diagnostics use a specific pipeline token (not unclassified) and include enough context to identify the failing stage.
- Regression coverage exists for both: (a) explicit no-edge path and (b) missing-producer/fetch-failure path.

**Owner agent**: github-copilot
**Time window**: 2026-03-19T21:20:00Z -> TBD
**Coordination flag**: solo

**Tests to run**:

- `node web/src/__tests__/cards-1p-projection-source.test.js`
- `npm --prefix web run test:card-decision`

**Manual validation**:

- Query `http://localhost:3000/api/cards?sport=NHL&limit=400` and confirm no card uses `fetch_failure:unclassified_no_play_pipeline`.
- Spot-check known no-play NHL matchups and verify `whyText` aligns with either explicit no-edge or concrete fetch/data-path reason.

**Guard for WI closeout**:

- Before marking complete: confirm every test command in `Tests to run` is runnable from repo root (exists in `web/package.json` scripts or is a valid direct `node` test command).

CLAIM: github-copilot 2026-03-19T21:20:00Z

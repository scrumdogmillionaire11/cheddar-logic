# WI-0503: NHL cross-market orchestration dual-run (market-stratified, no-leakage)

**ID**: WI-0503
**Goal**: Implement TOTAL/SPREAD/ML independent engines with signed driver signals and conflict scoring, then dual-run orchestration output into logs without serving it as production pick output yet.

**Depends on**:

- `WORK_QUEUE/WI-0502.md`

**Scope**:

- `apps/worker/src/models/cross-market.js`
- `apps/worker/src/jobs/run_nhl_model.js`
- `apps/worker/src/models/index.js`
- `apps/worker/src/models/__tests__/cross-market.test.js`
- `apps/worker/src/jobs/__tests__/run_nhl_model.market-calls.test.js`
- `packages/data/db/migrations/**` (if `expression_choice_log` table is added)
- `packages/data/src/db.js`
- `docs/CROSS_MARKET_ORCHESTRATION.md`
- `WORK_QUEUE/WI-0503.md`

**Out of scope**:

- Production cutover of chosen-market output
- Removing legacy global blend path
- 1P fair-probability activation

**Acceptance**:

1. Driver scoping contract is enforced: totals-only directional drivers do not contribute directionally to SPREAD/ML (`eligible=false` risk-only behavior).
2. Conflict is computed as `min(support, oppose)` with threshold semantics from target spec.
3. Selector applies deterministic rules (status > score gap > preference tie-break > ML-vs-spread value realism).
4. Dual-run mode records `expression_choice` artifacts for comparison (table or run log) while current production output remains unchanged.
5. Unit tests explicitly validate no leakage (`pace`/`pdoRegression` directional effect is zero for SPREAD/ML).

**Owner agent**: unassigned
**Time window**: TBD -> TBD
**Coordination flag**: needs-sync

**Tests to run**:

- `npm --prefix apps/worker test -- src/models/__tests__/cross-market.test.js`
- `npm --prefix apps/worker test -- src/jobs/__tests__/run_nhl_model.market-calls.test.js`
- `npm --prefix apps/worker test -- src/jobs/__tests__/run_nhl_model.test.js`

**Manual validation**:

- Run NHL model in dual-run mode and inspect one game record where TOTAL/SPREAD/ML differ, confirming selector reasoning matches rule order.
- Confirm SPREAD/ML outputs are unchanged in served card payloads during dual-run.

**Guard for WI closeout**:

- Before marking complete: attach at least 10 sampled dual-run records proving deterministic selector behavior.
- If migration added, include rollback and lock-safe migration execution notes.

CLAIM: unassigned

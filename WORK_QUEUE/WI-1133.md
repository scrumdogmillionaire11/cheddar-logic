---
phase: WI-1133
plan: 01
type: execute
wave: 3
depends_on:
  - WI-1124
  - WI-1127
files_modified:
  - web/src/lib/cards/query.ts
  - web/src/lib/cards/payload-classifier.ts
  - web/src/app/api/cards/route.ts
  - web/src/app/api/cards/[gameId]/route.ts
autonomous: true
requirements:
  - WI-1133-CQ-01
  - WI-1133-CQ-02
  - WI-1133-CQ-03
must_haves:
  truths:
    - "Duplicate helpers (resolveLifecycleMode, buildBettingSurfacePayloadPredicate, clampNumber, safeJsonParse, normalizePayloadMeta, getPayloadString, isBettingSurfacePayload, getActiveRunIds, getRunStatus, CORE_RUN_STATE_SPORT_SQL) exist in exactly one place."
    - "Both /api/cards and /api/cards/[gameId] import shared helpers from web/src/lib/cards/."
    - "All existing cards regression test suites continue to pass without modification."
  artifacts:
    - path: "web/src/lib/cards/query.ts"
      provides: "Shared SQL query helpers: getActiveRunIds, getRunStatus, CORE_RUN_STATE_SPORT_SQL, buildBettingSurfacePayloadPredicate, resolveLifecycleMode, clampNumber"
    - path: "web/src/lib/cards/payload-classifier.ts"
      provides: "Shared payload helpers: safeJsonParse, normalizePayloadMeta, getPayloadString, isBettingSurfacePayload"
    - path: "web/src/app/api/cards/route.ts"
      provides: "Route consuming shared helpers (no local copies of centralized functions)"
    - path: "web/src/app/api/cards/[gameId]/route.ts"
      provides: "Route consuming shared helpers (no local copies of centralized functions)"
  key_links:
    - from: "web/src/app/api/cards/route.ts"
      to: "web/src/lib/cards/query.ts"
      via: "import of shared query helpers"
      pattern: "from.*lib/cards/query"
    - from: "web/src/app/api/cards/route.ts"
      to: "web/src/lib/cards/payload-classifier.ts"
      via: "import of shared payload helpers"
      pattern: "from.*lib/cards/payload-classifier"
    - from: "web/src/app/api/cards/[gameId]/route.ts"
      to: "web/src/lib/cards/query.ts"
      via: "import of shared query helpers"
      pattern: "from.*lib/cards/query"
    - from: "web/src/app/api/cards/[gameId]/route.ts"
      to: "web/src/lib/cards/payload-classifier.ts"
      via: "import of shared payload helpers"
      pattern: "from.*lib/cards/payload-classifier"
---

# WI-1133: Extract Shared Cards Query And Payload Classification Module

**ID**: WI-1133

**Goal**: Remove duplicated cards endpoint logic by introducing shared query and payload classification helpers used by both cards routes.

**Requirements**:
- WI-1133-CQ-01: Shared query helpers (SQL predicates, active-run resolution, lifecycle mode, clamp/parse utils) must live in a single module consumed by both routes.
- WI-1133-CQ-02: Shared payload classifiers (safeJsonParse, normalizePayloadMeta, isBettingSurfacePayload, getPayloadString) must live in a single module consumed by both routes.
- WI-1133-CQ-03: Both routes must have zero local copies of any function moved to the shared module.

**Scope**:
- web/src/lib/cards/query.ts (new)
- web/src/lib/cards/payload-classifier.ts (new)
- web/src/app/api/cards/route.ts
- web/src/app/api/cards/[gameId]/route.ts

**Out of scope**:
- Cards response schema changes
- Games route handler refactor

**Acceptance**:
- Duplicate SQL predicate and payload-classification logic are centralized in shared module(s).
- Both cards routes consume the shared module without behavioral regressions.
- Existing cards regression suites pass.

**Owner agent**: Codex
**Time window**: 2026-04-21T00:00:00Z -> TBD
**Coordination flag**: needs-sync
**Depends on**: WI-1124, WI-1127

<tasks>

<task type="auto">
  <name>Task 1: Create shared query helpers module</name>
  <files>web/src/lib/cards/query.ts</files>
  <action>
    Create web/src/lib/cards/query.ts by extracting identical functions from both route files. The following are duplicated verbatim between web/src/app/api/cards/route.ts and web/src/app/api/cards/[gameId]/route.ts:
    - CORE_RUN_STATE_SPORT_SQL (const, lines ~93/86)
    - resolveLifecycleMode() (function, lines ~97/90)
    - buildBettingSurfacePayloadPredicate() (function, lines ~121/114)
    - clampNumber() (function, lines ~159/152)
    - getActiveRunIds() (function, lines ~241/234)
    - getRunStatus() (function, lines ~294/287)
    Export all of the above. Do not yet modify the route files.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project web/tsconfig.json 2>&1 | grep "lib/cards/query" || echo "type-check clean"</automated>
  </verify>
  <done>web/src/lib/cards/query.ts exists and exports all six helpers with no type errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create shared payload classifier module</name>
  <files>web/src/lib/cards/payload-classifier.ts</files>
  <action>
    Create web/src/lib/cards/payload-classifier.ts by extracting the following identical functions from both route files:
    - safeJsonParse() (lines ~170/163)
    - normalizePayloadMeta() (lines ~179/172)
    - getPayloadString() (lines ~184/177)
    - isBettingSurfacePayload() (lines ~200/193)
    Export all four. Do not yet modify the route files.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project web/tsconfig.json 2>&1 | grep "lib/cards/payload-classifier" || echo "type-check clean"</automated>
  </verify>
  <done>web/src/lib/cards/payload-classifier.ts exists and exports all four helpers with no type errors.</done>
</task>

<task type="auto">
  <name>Task 3: Wire both routes to shared modules and remove local copies</name>
  <files>web/src/app/api/cards/route.ts, web/src/app/api/cards/[gameId]/route.ts</files>
  <action>
    In both route files:
    1. Add imports from web/src/lib/cards/query and web/src/lib/cards/payload-classifier.
    2. Delete the local definitions of all ten extracted functions/constants.
    3. Verify no remaining local copies by grepping for the function names.
    Behavioral change: none — pure extraction, identical logic.
  </action>
  <verify>
    <automated>rg -n "function resolveLifecycleMode|function buildBettingSurfacePayloadPredicate|function clampNumber|function safeJsonParse|function normalizePayloadMeta|function getPayloadString|function isBettingSurfacePayload|function getActiveRunIds|function getRunStatus|^const CORE_RUN_STATE_SPORT_SQL" web/src/app/api/cards/route.ts "web/src/app/api/cards/[gameId]/route.ts" && echo "FAIL: local copies remain" || echo "PASS: no local copies"</automated>
    <automated>rg -c "from.*lib/cards/query|from.*lib/cards/payload-classifier" web/src/app/api/cards/route.ts "web/src/app/api/cards/[gameId]/route.ts"</automated>
    <automated>npm --prefix web run test:api:cards:security</automated>
    <automated>npm --prefix web run test:cards-sport-filter</automated>
    <automated>npm --prefix web run test:cards-lifecycle-regression</automated>
    <automated>npm --prefix web run lint</automated>
  </verify>
  <done>Both routes import from shared modules, zero local copies of extracted functions remain, and all three regression suites pass.</done>
</task>

</tasks>

**Tests to run**:
- npm --prefix web run test:api:cards:security
- npm --prefix web run test:cards-sport-filter
- npm --prefix web run test:cards-lifecycle-regression
- npm --prefix web run lint

**Manual validation**:
- Verify cards and cards-by-game endpoints return consistent filtered results for equivalent filters.

**Guard for WI closeout**:
- Before marking complete: confirm every test command in Tests to run is runnable from repo root (i.e., exists in web/package.json scripts or is a valid node path/to/test.js command).
- If a script is added to web/package.json as part of this WI, add it to this WI's Tests section with the full npm --prefix web run script form.

CLAIM: unassigned 2026-04-21T00:00:00Z
CLAIM: Codex 2026-04-22T22:57:16Z

# WI-0743: Exclude NCAAM from Results by default

**ID**: WI-0743
**Goal**: Hide NCAAM from default `/results` and `/api/results` output while preserving explicit `sport=NCAAM` archival access.

**Scope**:
- `web/src/app/api/results/route.ts`
- `web/src/app/results/page.tsx`
- `web/src/__tests__/ui-results-smoke.test.js`
- `web/src/__tests__/api-results-flags.test.js`
- `web/src/__tests__/api-results-decision-segmentation.test.js`
- `web/src/__tests__/api-results-sport-filter.test.js`
- `web/package.json`
- `WORK_QUEUE/WI-0743.md`

**Out of scope**:
- `apps/worker/**`
- `packages/data/**`
- Historical NCAAM row deletion or settlement rewrites

**Acceptance**:
- Default `/api/results` responses exclude `sport: "NCAAM"` rows from summary, segments, ledger, and result-set counts.
- `/api/results?sport=NCAAM` still returns NCAAM rows when they exist.
- `/results` no longer offers NCAAM in sport filters.

**Owner agent**: codex
**Time window**: 2026-04-03T12:12:24Z -> TBD
**Coordination flag**: needs-sync

**Tests to run**:
- `npm --prefix web run test:ui:results`
- `npm --prefix web run test:api:results:flags`
- `npm --prefix web run test:api:results:decision-segmentation`
- `npm --prefix web run test:api:results:sport-filter`
- `npm --prefix web run lint -- src/app/api/results/route.ts src/app/results/page.tsx src/__tests__/ui-results-smoke.test.js src/__tests__/api-results-flags.test.js src/__tests__/api-results-decision-segmentation.test.js src/__tests__/api-results-sport-filter.test.js`

**Manual validation**:
- Confirm only scoped results UI/API/test files changed and no historical NCAAM data deletion logic was introduced.

**Guard for WI closeout**:
- Before marking complete: confirm every test command in `Tests to run` is runnable from repo root (i.e., exists in web/package.json scripts or is a valid `node path/to/test.js` command).
- If a script is added to web/package.json as part of this WI, add it to this WI's Tests section with the full `npm --prefix web run <script>` form.

CLAIM: codex 2026-04-03T12:12:24Z

# WI-0765: Schedule `pull_nhl_player_blk.js` and replace manual `ingest_nst_blk_rates.js` with automated pull

| Field              | Value |
|--------------------|-------|
| **ID**             | WI-0765 |
| **Goal**           | Register `pull_nhl_player_blk` in the nightly NHL scheduler window, and either build an automated `pull_nst_blk_rates.js` job or publish a formal runbook documenting the required manual cadence for `ingest_nst_blk_rates.js` |
| **Priority**       | High (shots model silently goes stale when manual ingest falls behind) |
| **Owner agent**    | Unassigned |
| **Time window**    | TBD |
| **Coordination flag** | `needs-sync` — modifies `apps/worker/src/schedulers/main.js` |

---

## Problem

Two NHL block data jobs exist but are not registered in the scheduler:

**`pull_nhl_player_blk.js`**
- Fetches per-game block data from the NHL API
- Writes to `player_blk_logs`
- Zero references in `apps/worker/src/schedulers/main.js`
- Must be run manually; data goes stale between manual runs

**`ingest_nst_blk_rates.js`**
- Reads a CSV from Natural Stat Trick and populates `player_blk_rates`
- Zero references in scheduler
- Requires downloading a CSV manually and pointing the script at it
- No documented cadence or runbook exists

**Downstream consumer:** `run_nhl_player_shots_model.js` reads `player_blk_rates`.

**Impact:**
- Block-shot inputs silently go stale without manual intervention
- There is no alerting or watchdog when block rates are > N days old
- Model continues to run with stale block rates, producing degraded shot projections with no warning

---

## Scope

| File | Change |
|------|--------|
| `apps/worker/src/schedulers/main.js` | Register `pull_nhl_player_blk` in the nightly NHL window (after `pull_nhl_player_shots`, before model run); add `maxAgeHours` guard if pattern exists |
| `apps/worker/src/jobs/pull_nst_blk_rates.js` | **New job** — automated HTTP fetch from Natural Stat Trick export URL (or documented API equivalent); parse CSV in-process; upsert into `player_blk_rates` |
| `apps/worker/src/schedulers/main.js` | Register `pull_nst_blk_rates` on weekly cadence (NST data updates weekly) |
| `apps/worker/src/jobs/run_nhl_player_shots_model.js` | Add staleness check: if `MAX(updated_at) FROM player_blk_rates` is > 8 days old, emit `missing_inputs: ['block_rates_stale']` warning on affected cards |
| `docs/runbooks/nst-blk-rates-manual-ingest.md` | If automated fetch is not feasible (NST blocks scraping): formal runbook documenting exact steps, required frequency, and how to trigger ingest manually |

## Out of Scope

- Refactoring `ingest_nst_blk_rates.js` internals — only adding automation wrapper or runbook
- Changes to `player_blk_logs` schema
- NHL shot model projection logic changes

---

## Decision point

If NST does not expose a stable export URL, the automated path is blocked. In that case:
1. Create the runbook (docs/runbooks path above)
2. Add the staleness watchdog to the shots model
3. Create a follow-on WI for automated NST data sourcing

---

## Acceptance

- `pull_nhl_player_blk` appears in `main.js` scheduler registration
- `player_blk_logs` is populated automatically after nightly run without manual intervention
- Either: `pull_nst_blk_rates` runs on schedule and writes non-null `player_blk_rates` rows
- Or: runbook exists at `docs/runbooks/nst-blk-rates-manual-ingest.md` with explicit steps and required cadence
- Shots model emits `missing_inputs: ['block_rates_stale']` when `player_blk_rates` is > 8 days old

---

## Tests to run

- `node apps/worker/src/jobs/pull_nhl_player_blk.js` (smoke — no uncaught errors)
- `node apps/worker/src/jobs/pull_nst_blk_rates.js` (smoke — if automated path taken)
- `CHEDDAR_DB_ALLOW_MULTI_PROCESS=true node apps/worker/src/jobs/run_nhl_player_shots_model.js` (smoke)

## Manual validation

- After running `pull_nhl_player_blk`, query `player_blk_logs` and confirm new rows with `updated_at` matching today
- Force `player_blk_rates` rows to have `updated_at` = 10 days ago; run shots model; confirm stale warning appears in card payload

**Guard for WI closeout**: `grep "pullNhlPlayerBlk\|pull_nhl_player_blk" apps/worker/src/schedulers/main.js` returns at least one hit.

CLAIM: unassigned

# WI-0589: Confidence tier correction layer for PLAY vs LEAN

**ID**: WI-0589
**Goal**: Redefine `PLAY` vs `LEAN` so `PLAY` is rarer, cleaner, and measurably stronger than `LEAN`, with explicit cleanliness/contradiction gates and a recent-sample regrade report.

**Scope**:
- `packages/models/src/decision-pipeline-v2.js`
- `packages/models/src/decision-pipeline-v2.patch.js`
- `apps/worker/src/jobs/report_telemetry_calibration.js`
- `apps/worker/src/jobs/__tests__/report_telemetry_calibration.test.js`
- `docs/runbooks/play-vs-lean-tier-audit-2026-03.md`

**Out of scope**:
- Sport-specific model rewrites
- Permanent NBA totals tuning beyond the temporary quarantine
- UI badge copy changes — the front-end label **"Slight Edge"** (which maps to the `LEAN` tier) must not change; this work touches internal tier logic only, not display strings

**Background**:
Recent production performance shows `PLAY` underperforming `SLIGHT EDGE`, which means the system is mis-ranking confidence. The correction needed here is structural: `PLAY` should require both stronger edge and cleaner conditions than `LEAN`. Missing inputs, stale market data, proxy dependence, or contradictory drivers should block `PLAY` even if raw edge clears a threshold.

**Terminology note**: The internal tier names `PLAY` and `LEAN` map to front-end labels **"Play"** and **"Slight Edge"** respectively. This work changes internal tier assignment logic only — the display labels and their mapping to internal names are frozen and must not be touched.

**Acceptance**:
- `PLAY` requirements are stricter than `LEAN` on both edge and support/cleanliness criteria.
- A row with missing inputs, stale market data, proxy usage, or meaningful driver contradiction cannot resolve to `PLAY`.
- `report_telemetry_calibration` (or equivalent report output in scope) includes a decision-tier section summarizing recent settled performance for `PLAY` vs `LEAN`.
- The regraded recent sample shows fewer `PLAY` decisions than the current ruleset.
- The regraded recent sample satisfies the operational target: `PLAY` win rate > `LEAN` win rate and `PLAY` ROI > `LEAN` ROI, or the WI remains open until the rule set is tightened enough to achieve that.

**Owner agent**: unassigned
**Time window**: 2026-03-23T00:00:00Z -> TBD
**Coordination flag**: solo
**Depends on**: WI-0587, WI-0588

**Tests to run**:
- `npm --prefix apps/worker run job:report-telemetry-calibration`
- `npm --prefix web run test:card-decision`
- `npm --prefix web run test:decision:canonical`

**Manual validation**:
- Run the telemetry report before and after the new tier rules and compare `PLAY`/`LEAN` counts, win rate, and ROI.
- Inspect sample payloads downgraded from `PLAY` to `LEAN` and confirm the downgrade is explained by cleanliness or contradiction rules rather than arbitrary tier collapse.

**Guard for WI closeout**:
- Before marking complete: confirm every test command in `Tests to run` is runnable from repo root.
- Before marking complete: attach the recent-sample regrade report in `docs/runbooks/` and record the exact sample window used.

CLAIM: unassigned

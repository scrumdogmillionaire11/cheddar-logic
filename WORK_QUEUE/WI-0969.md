# WI-0969: Freeze NHL 1P baseline and bucket scorecard for offseason rebuild

**ID**: WI-0969
**Goal**: Create a locked baseline snapshot for NHL 1P model performance, including side-specific records across fixed projection buckets.

**Depends on**: WI-0967

**Scope**:
- `apps/worker/src/jobs/report_telemetry_calibration.js`
- `apps/worker/src/jobs/__tests__/report_telemetry_calibration.test.js`
- `web/src/app/api/results/projection-metrics.ts`

**Out of scope**:
- Changes to live NHL model decision thresholds
- Any staking or bankroll policy changes
- Scheduler cadence changes

**Acceptance**:
- Baseline report includes NHL 1P metrics for exactly these buckets: `1.0-1.4`, `1.5-1.9`, `2.0-2.19`, `2.20+`.
- Report includes side-specific directional record per bucket (`OVER W-L`, `UNDER W-L`) and overall directional accuracy.
- Baseline output can be regenerated deterministically from current settled data.
- Unit tests cover bucket assignment and side-specific tally integrity.

**Owner agent**: unassigned
**Time window**: 2026-04-16 -> 2026-04-22
**Coordination flag**: solo

**Tests to run**:
- `npm --prefix apps/worker run test -- src/jobs/__tests__/report_telemetry_calibration.test.js --runInBand`
- `npm --prefix web run build`

**Manual validation**:
- Run calibration telemetry and confirm NHL 1P output includes all four buckets and numeric counts, even when a bucket has zero rows.
- Compare one bucket's W-L values against `/results` segment table for consistency.

CLAIM: unassigned
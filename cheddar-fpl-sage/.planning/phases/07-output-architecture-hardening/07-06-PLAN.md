---
phase: 07-output-architecture-hardening
plan: 06
type: execute
wave: 4
depends_on:
  - "07-03"
  - "07-04"
requirements:
  - OA-04
files_modified:
  - cheddar-fpl-sage/frontend/src/components/WeeklyReview.tsx
  - cheddar-fpl-sage/frontend/src/pages/Results.tsx
  - cheddar-fpl-sage/frontend/src/lib/decisionViewModel.ts
  - cheddar-fpl-sage/frontend/src/components/__tests__/DecisionBrief.test.tsx
autonomous: true
must_haves:
  truths:
    - "WeeklyReviewCard is rendered as a distinct retrospective section"
    - "Results section ordering keeps retrospective separate from current and horizon surfaces"
    - "No UI errors on GW1 or first-run null weekly_review"
  artifacts:
    - path: "cheddar-fpl-sage/frontend/src/components/WeeklyReview.tsx"
      provides: "Retrospective review component"
      min_lines: 80
    - path: "cheddar-fpl-sage/frontend/src/pages/Results.tsx"
      provides: "Ordered weekly flow rendering"
      contains: "WeeklyReview"
---

<objective>
Integrate WeeklyReviewCard into the frontend as a dedicated retrospective section while preserving current-week recommendation clarity.

Purpose: Complete retrospective/current/horizon separation in user-facing weekly flow.
Output: WeeklyReview component and ordered results page rendering.
</objective>

<execution_context>
@cheddar-fpl-sage/.planning/workflows/workflows__fpl_transfer_advisor.md
</execution_context>

<context>
@cheddar-fpl-sage/.planning/phases/07-output-architecture-hardening/07-03-PLAN.md
@cheddar-fpl-sage/.planning/phases/07-output-architecture-hardening/07-04-PLAN.md
@cheddar-fpl-sage/frontend/src/pages/Results.tsx
@cheddar-fpl-sage/frontend/src/lib/decisionViewModel.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add WeeklyReview component and map weekly_review payload</name>
  <files>
    cheddar-fpl-sage/frontend/src/components/WeeklyReview.tsx
    cheddar-fpl-sage/frontend/src/lib/decisionViewModel.ts
  </files>
  <action>
Implement WeeklyReview component props from canonical `weekly_review` card fields and expose mapper output without introducing any fallback business inference.
  </action>
  <verify>
`cd cheddar-fpl-sage/frontend && npm run build`
  </verify>
  <done>
WeeklyReview component renders canonical retrospective fields and mapper remains presentation-only.
  </done>
</task>

<task type="auto">
  <name>Task 2: Enforce weekly section ordering and null-safe behavior</name>
  <files>
    cheddar-fpl-sage/frontend/src/pages/Results.tsx
    cheddar-fpl-sage/frontend/src/components/__tests__/DecisionBrief.test.tsx
  </files>
  <action>
Render sections in this order: WeeklyReview, Current Squad State, Gameweek Plan, Transfer Recommendation, Captaincy, Chip Strategy, Horizon Watch. Hide WeeklyReview cleanly when `weekly_review` is null.
  </action>
  <verify>
`cd cheddar-fpl-sage/frontend && npm run build && npm test -- --run DecisionBrief.test.tsx`
  </verify>
  <done>
Results order is deterministic and retrospective section is safely absent for null cases.
  </done>
</task>

</tasks>

<verification>
Build frontend and validate a fixture with and without `weekly_review` payload.
</verification>

<success_criteria>
- WeeklyReview renders as a distinct retrospective section
- Section ordering enforces retrospective/current/horizon separation
- Null weekly_review paths are handled without UI errors
</success_criteria>

<output>
After completion, create `cheddar-fpl-sage/.planning/phases/07-output-architecture-hardening/07-06-SUMMARY.md`
</output>

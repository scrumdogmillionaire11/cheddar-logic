---
phase: 07-output-architecture-hardening
plan: 05
type: execute
wave: 5
depends_on:
  - "07-02"
  - "07-04"
  - "07-06"
requirements:
  - OA-05
files_modified:
  - cheddar-fpl-sage/backend/services/contract_transformer.py
  - cheddar-fpl-sage/backend/routers/dashboard.py
  - cheddar-fpl-sage/tests/test_analyze_api.py
  - cheddar-fpl-sage/tests/tests_new/test_api_endpoints.py
autonomous: true
must_haves:
  truths:
    - "Downstream contract and dashboard paths consume canonical cards instead of re-deriving"
    - "Parallel business-logic transformation paths are retired"
    - "Exported payloads remain behavior-compatible for clients"
  artifacts:
    - path: "cheddar-fpl-sage/backend/services/contract_transformer.py"
      provides: "Canonical-card consumer transformer"
      contains: "transform_analysis_contract"
    - path: "cheddar-fpl-sage/backend/routers/dashboard.py"
      provides: "Dashboard response sourced from canonical cards"
      contains: "get_dashboard_data"
---

<objective>
Remove remaining parallel transformation ownership by converting dashboard and integration contract paths to canonical-card consumers.

Purpose: Prevent reintroduction of duplicate business derivation after canonical payload rollout.
Output: Contract and dashboard services aligned to canonical output cards with regression coverage.
</objective>

<execution_context>
@cheddar-fpl-sage/.planning/workflows/workflows__fpl_transfer_advisor.md
</execution_context>

<context>
@cheddar-fpl-sage/.planning/phases/07-output-architecture-hardening/07-02-PLAN.md
@cheddar-fpl-sage/backend/services/contract_transformer.py
@cheddar-fpl-sage/backend/routers/dashboard.py
@cheddar-fpl-sage/backend/services/result_transformer.py
@cheddar-fpl-sage/tests/test_analyze_api.py
@cheddar-fpl-sage/tests/tests_new/test_api_endpoints.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert contract_transformer to consume canonical cards</name>
  <files>
    cheddar-fpl-sage/backend/services/contract_transformer.py
    cheddar-fpl-sage/tests/test_analyze_api.py
  </files>
  <action>
Refactor contract transformer to map from canonical cards (`transfer_recommendation`, `chip_strategy`, `captaincy`, `squad_state`) instead of reconstructing business logic from mixed legacy keys. Preserve external payload compatibility.
  </action>
  <verify>
`cd cheddar-fpl-sage && pytest tests/test_analyze_api.py -q`
  </verify>
  <done>
Contract transformer has no independent chip/transfer/captain decision derivation and passes API regression tests.
  </done>
</task>

<task type="auto">
  <name>Task 2: Convert dashboard router to canonical-card sourcing</name>
  <files>
    cheddar-fpl-sage/backend/routers/dashboard.py
    cheddar-fpl-sage/tests/tests_new/test_api_endpoints.py
  </files>
  <action>
Update dashboard assembly to consume canonical cards directly. Mark or remove legacy extractor helpers that re-derive values from raw job blobs. Add regression tests for behavior parity.
  </action>
  <verify>
`cd cheddar-fpl-sage && pytest tests/tests_new/test_api_endpoints.py -q`
  </verify>
  <done>
Dashboard endpoint is behavior-compatible and no longer maintains a parallel transformation logic path.
  </done>
</task>

</tasks>

<verification>
Run both API test files and inspect one dashboard payload to confirm values are sourced from canonical cards.
</verification>

<success_criteria>
- Contract transformer consumes canonical cards only
- Dashboard router consumes canonical cards only
- API endpoint tests pass with no regression in externally visible behavior
</success_criteria>

<output>
After completion, create `cheddar-fpl-sage/.planning/phases/07-output-architecture-hardening/07-05-SUMMARY.md`
</output>

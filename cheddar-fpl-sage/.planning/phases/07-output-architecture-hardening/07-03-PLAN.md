---
phase: 07-output-architecture-hardening
plan: 03
type: execute
wave: 3
depends_on:
  - "07-02"
requirements:
  - OA-03
files_modified:
  - cheddar-fpl-sage/frontend/src/lib/api.ts
  - cheddar-fpl-sage/frontend/src/lib/decisionViewModel.ts
  - cheddar-fpl-sage/frontend/src/pages/Results.tsx
  - cheddar-fpl-sage/frontend/src/components/__tests__/DecisionBrief.test.tsx
  - cheddar-fpl-sage/frontend/src/components/__tests__/TransferSection.test.tsx
  - cheddar-fpl-sage/frontend/src/components/__tests__/CaptaincySection.test.tsx
autonomous: true
must_haves:
  truths:
    - "Frontend maps canonical cards and does not re-derive business decisions"
    - "Results page does not perform XI/bench field-cascade resolution"
    - "Open-ended payload typing is removed from frontend API contract"
  artifacts:
    - path: "cheddar-fpl-sage/frontend/src/lib/decisionViewModel.ts"
      provides: "Thin card-to-props mapper"
      contains: "buildDecisionViewModel"
    - path: "cheddar-fpl-sage/frontend/src/lib/api.ts"
      provides: "Strict WeeklyAnalysisPayload-aligned frontend types"
      contains: "interface AnalysisResults"
---

<objective>
Make frontend a rendering layer by removing fallback inference and consuming canonical cards directly.

Purpose: Prevent duplicated business logic and stop frontend/backend decision drift.
Output: Strict frontend typing and a mapper-only decision view model.
</objective>

<execution_context>
@cheddar-fpl-sage/.planning/workflows/workflows__fpl_transfer_advisor.md
</execution_context>

<context>
@cheddar-fpl-sage/.planning/phases/07-output-architecture-hardening/07-02-PLAN.md
@cheddar-fpl-sage/frontend/src/lib/api.ts
@cheddar-fpl-sage/frontend/src/lib/decisionViewModel.ts
@cheddar-fpl-sage/frontend/src/pages/Results.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace open-ended frontend payload type with strict canonical fields</name>
  <files>
    cheddar-fpl-sage/frontend/src/lib/api.ts
  </files>
  <action>
Update `AnalysisResults` to strictly model canonical cards and remove `[key: string]: unknown`. Keep migration aliases explicitly typed as deprecated optionals only if required for transition.
  </action>
  <verify>
`cd cheddar-fpl-sage/frontend && npm run build`
  </verify>
  <done>
Frontend payload contract is closed and aligned to canonical backend output objects.
  </done>
</task>

<task type="auto">
  <name>Task 2: Thin decision view model and remove frontend re-derivation</name>
  <files>
    cheddar-fpl-sage/frontend/src/lib/decisionViewModel.ts
    cheddar-fpl-sage/frontend/src/pages/Results.tsx
  </files>
  <action>
Remove local business-rule helpers (`toPrimaryAction`, `toChipVerdict`, `toChipStatus`, transfer dedup inference, XI/bench fallback cascades). Ensure mapper-only behavior and canonical card consumption.
  </action>
  <verify>
`cd cheddar-fpl-sage/frontend && npm run build && npm test -- --run DecisionBrief.test.tsx TransferSection.test.tsx CaptaincySection.test.tsx`
  </verify>
  <done>
View model is mapper-only, page-level fallback logic is eliminated, and component tests pass with canonical card inputs.
  </done>
</task>

</tasks>

<verification>
Build frontend and run targeted component tests to confirm rendering uses canonical cards without fallback inference.
</verification>

<success_criteria>
- Frontend payload typing is strict and canonical
- Decision view model contains no business re-derivation
- Results page renders from canonical card objects only
</success_criteria>

<output>
After completion, create `cheddar-fpl-sage/.planning/phases/07-output-architecture-hardening/07-03-SUMMARY.md`
</output>

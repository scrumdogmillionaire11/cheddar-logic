---
phase: 07-output-architecture-hardening
plan: 01
type: execute
wave: 1
depends_on: []
requirements:
  - OA-01
files_modified:
  - cheddar-fpl-sage/backend/models/api_models.py
  - cheddar-fpl-sage/tests/test_weekly_analysis_payload_contract.py
autonomous: true
must_haves:
  truths:
    - "A single typed weekly payload contract exists and is the official response target"
    - "Analysis status payload is no longer an untyped Dict[str, Any]"
    - "Canonical card objects are explicit and versionable"
  artifacts:
    - path: "cheddar-fpl-sage/backend/models/api_models.py"
      provides: "WeeklyAnalysisPayload and nested card schemas"
      contains: "class WeeklyAnalysisPayload"
    - path: "cheddar-fpl-sage/tests/test_weekly_analysis_payload_contract.py"
      provides: "Contract validation and unknown-key guardrails"
      min_lines: 80
  key_links:
    - from: "cheddar-fpl-sage/backend/models/api_models.py"
      to: "cheddar-fpl-sage/backend/services/result_transformer.py"
      via: "transform_analysis_results output typing"
      pattern: "WeeklyAnalysisPayload"
---

<objective>
Define and lock the canonical weekly output contract as the single source of truth for API and UI surfaces.

Purpose: All downstream cleanup depends on one stable typed boundary.
Output: A typed `WeeklyAnalysisPayload` model with explicit card objects and strict contract tests.
</objective>

<execution_context>
@cheddar-fpl-sage/.planning/workflows/workflows__fpl_transfer_advisor.md
</execution_context>

<context>
@cheddar-fpl-sage/.planning/PROJECT.md
@cheddar-fpl-sage/.planning/ROADMAP.md
@cheddar-fpl-sage/.planning/STATE.md
@cheddar-fpl-sage/.planning/codebase/FPL_SAGE_OUTPUT_ARCHITECTURE_AUDIT.md
@cheddar-fpl-sage/backend/models/api_models.py
@cheddar-fpl-sage/backend/services/result_transformer.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add canonical card models and top-level WeeklyAnalysisPayload</name>
  <files>
    cheddar-fpl-sage/backend/models/api_models.py
  </files>
  <action>
Add Pydantic models for weekly cards and a `WeeklyAnalysisPayload` containing exactly: `weekly_review`, `squad_state`, `gameweek_plan`, `transfer_recommendation`, `captaincy`, `chip_strategy`, `horizon_watch`, `decision_confidence`.

Update `AnalysisStatus.results` to `Optional[WeeklyAnalysisPayload]`.
  </action>
  <verify>
`cd cheddar-fpl-sage && pytest tests/test_weekly_analysis_payload_contract.py -q`
  </verify>
  <done>
Canonical payload class exists, card objects are typed, and API status model references canonical payload.
  </done>
</task>

<task type="auto">
  <name>Task 2: Enforce strict schema behavior in tests</name>
  <files>
    cheddar-fpl-sage/tests/test_weekly_analysis_payload_contract.py
  </files>
  <action>
Create tests that assert required top-level fields, card-level required fields, and strict unknown-key rejection behavior. Include migration fixture coverage for additive aliases.
  </action>
  <verify>
`cd cheddar-fpl-sage && pytest tests/test_weekly_analysis_payload_contract.py -q`
  </verify>
  <done>
Contract tests fail for missing or unknown keys and pass for canonical payload fixtures.
  </done>
</task>

</tasks>

<verification>
Run the contract test file and confirm all payload assertions are strict.
</verification>

<success_criteria>
- WeeklyAnalysisPayload exists and is referenced by API status model
- Canonical card schemas are defined and validated
- Contract tests enforce strict payload structure
</success_criteria>

<output>
After completion, create `cheddar-fpl-sage/.planning/phases/07-output-architecture-hardening/07-01-SUMMARY.md`
</output>

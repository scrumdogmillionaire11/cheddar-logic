---
phase: 07-output-architecture-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - "07-01"
requirements:
  - OA-02
  - OA-06
files_modified:
  - cheddar-fpl-sage/backend/services/result_transformer.py
  - cheddar-fpl-sage/tests/test_transformer.py
  - cheddar-fpl-sage/tests/test_result_transformer_strategy_contract.py
autonomous: true
must_haves:
  truths:
    - "Result transformer emits one canonical card per concept"
    - "Chip, transfer, and squad surfaces are no longer exploded into parallel top-level shapes"
    - "Legacy key aliases are additive and explicitly temporary with a removal gate"
  artifacts:
    - path: "cheddar-fpl-sage/backend/services/result_transformer.py"
      provides: "Canonical WeeklyAnalysisPayload producer"
      contains: "def transform_analysis_results"
    - path: "cheddar-fpl-sage/tests/test_result_transformer_strategy_contract.py"
      provides: "Canonical output shape tests"
      min_lines: 120
  key_links:
    - from: "cheddar-fpl-sage/backend/services/result_transformer.py"
      to: "cheddar-fpl-sage/backend/models/api_models.py"
      via: "Runtime WeeklyAnalysisPayload validation"
      pattern: "WeeklyAnalysisPayload"
---

<objective>
Make `result_transformer.py` the canonical producer and collapse duplicate chip/transfer/XI representations.

Purpose: This is the ownership boundary fix that stops frontend guessing and contract drift.
Output: Canonical card output plus migration-safe aliases and explicit alias-removal gate.
</objective>

<execution_context>
@cheddar-fpl-sage/.planning/workflows/workflows__fpl_transfer_advisor.md
</execution_context>

<context>
@cheddar-fpl-sage/.planning/codebase/FPL_SAGE_OUTPUT_ARCHITECTURE_AUDIT.md
@cheddar-fpl-sage/.planning/phases/07-output-architecture-hardening/07-01-PLAN.md
@cheddar-fpl-sage/backend/services/result_transformer.py
@cheddar-fpl-sage/tests/test_transformer.py
@cheddar-fpl-sage/tests/test_result_transformer_strategy_contract.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collapse chip and transfer duplicates into canonical cards</name>
  <files>
    cheddar-fpl-sage/backend/services/result_transformer.py
    cheddar-fpl-sage/tests/test_result_transformer_strategy_contract.py
  </files>
  <action>
Refactor transformer output so canonical fields are primary:
- `chip_strategy` absorbs legacy chip fields
- `transfer_recommendation` absorbs legacy transfer fields
- transfer alternatives are embedded under transfer items

Retain legacy aliases temporarily but derive them from canonical cards only.
  </action>
  <verify>
`cd cheddar-fpl-sage && pytest tests/test_result_transformer_strategy_contract.py -q`
  </verify>
  <done>
Canonical transfer/chip cards are primary; legacy keys are compatibility aliases only.
  </done>
</task>

<task type="auto">
  <name>Task 2: Collapse XI and projected variants into squad_state</name>
  <files>
    cheddar-fpl-sage/backend/services/result_transformer.py
    cheddar-fpl-sage/tests/test_transformer.py
  </files>
  <action>
Create canonical `squad_state` containing current lineup, bench, projected lineup, projected bench, and lineup metadata. Keep old lineup keys as temporary alias projections only.
  </action>
  <verify>
`cd cheddar-fpl-sage && pytest tests/test_transformer.py -q`
  </verify>
  <done>
`squad_state` is canonical and alias parity is verified.
  </done>
</task>

<task type="auto">
  <name>Task 3: Enforce runtime payload validation and alias-removal gate</name>
  <files>
    cheddar-fpl-sage/backend/services/result_transformer.py
    cheddar-fpl-sage/tests/test_result_transformer_strategy_contract.py
  </files>
  <action>
At the transformer boundary, instantiate/validate output through `WeeklyAnalysisPayload` before returning response payloads. Add strict-mode test path that fails if legacy aliases are emitted when strict canonical mode is enabled. Add explicit deprecation horizon comment for alias removal.
  </action>
  <verify>
`cd cheddar-fpl-sage && pytest tests/test_result_transformer_strategy_contract.py -q`
  </verify>
  <done>
Runtime payload validation is active and strict-mode alias removal behavior is test-covered.
  </done>
</task>

</tasks>

<verification>
Run transformer contract tests and inspect one response fixture to confirm canonical cards exist and alias emission is controlled by migration mode.
</verification>

<success_criteria>
- Canonical cards are emitted by transformer and validated at runtime
- Parallel chip/transfer/squad primary forms are removed
- Alias migration has explicit strict-mode and removal gate
</success_criteria>

<output>
After completion, create `cheddar-fpl-sage/.planning/phases/07-output-architecture-hardening/07-02-SUMMARY.md`
</output>

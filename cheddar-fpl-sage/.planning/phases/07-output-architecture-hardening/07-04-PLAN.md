---
phase: 07-output-architecture-hardening
plan: 04
type: execute
wave: 3
depends_on:
  - "07-01"
  - "07-02"
requirements:
  - OA-04
files_modified:
  - cheddar-fpl-sage/backend/services/weekly_review_service.py
  - cheddar-fpl-sage/backend/services/engine_service.py
  - cheddar-fpl-sage/backend/services/result_transformer.py
  - cheddar-fpl-sage/backend/models/product_models.py
  - cheddar-fpl-sage/tests/test_decision_receipts_api.py
autonomous: true
must_haves:
  truths:
    - "DecisionReceipt outcomes are populated with previous-GW evaluation data"
    - "Weekly review card payload is produced in backend and null-safe for first-run paths"
  artifacts:
    - path: "cheddar-fpl-sage/backend/services/weekly_review_service.py"
      provides: "Retrospective review computation and payload builder"
      contains: "def build_review"
    - path: "cheddar-fpl-sage/tests/test_decision_receipts_api.py"
      provides: "Read-after-write receipt outcome verification"
      contains: "process_verdict"
  key_links:
    - from: "cheddar-fpl-sage/backend/services/weekly_review_service.py"
      to: "cheddar-fpl-sage/backend/models/product_models.py"
      via: "DecisionReceipt outcome and verdict persistence"
      pattern: "outcome|process_verdict|drift_flags"
---

<objective>
Implement backend retrospective pipeline for weekly review and persist outcome quality into DecisionReceipt.

Purpose: Retrospective truth must exist before UI rendering can be trusted.
Output: Weekly review service and persisted receipt outcome fields with read-after-write verification.
</objective>

<execution_context>
@cheddar-fpl-sage/.planning/workflows/workflows__fpl_transfer_advisor.md
</execution_context>

<context>
@cheddar-fpl-sage/.planning/codebase/FPL_SAGE_OUTPUT_ARCHITECTURE_AUDIT.md
@cheddar-fpl-sage/backend/models/product_models.py
@cheddar-fpl-sage/backend/services/engine_service.py
@cheddar-fpl-sage/backend/services/result_transformer.py
@cheddar-fpl-sage/backend/services/product_store.py
@cheddar-fpl-sage/tests/test_decision_receipts_api.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add weekly review service and persist receipt outcomes</name>
  <files>
    cheddar-fpl-sage/backend/services/weekly_review_service.py
    cheddar-fpl-sage/backend/services/engine_service.py
    cheddar-fpl-sage/backend/models/product_models.py
    cheddar-fpl-sage/tests/test_decision_receipts_api.py
  </files>
  <action>
Implement retrospective review builder that reads previous-GW FPL history and receipt records, computes points/rank delta/captain-followed/recommendation-followed/process_verdict/drift_flags, and persists `DecisionReceipt.outcome` + `process_verdict`. Ensure graceful fallback for missing history.
  </action>
  <verify>
`cd cheddar-fpl-sage && pytest tests/test_decision_receipts_api.py -q`
  </verify>
  <done>
Receipt outcomes are populated for eligible runs and tests cover populated + null-safe paths.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add explicit read-after-write verification for receipt persistence</name>
  <files>
    cheddar-fpl-sage/tests/test_decision_receipts_api.py
  </files>
  <action>
Add test that writes retrospective outcome to a receipt via service path, then reads the stored record and asserts persisted `outcome`, `process_verdict`, and `drift_flags` are present and unchanged. Include negative test for unavailable history where fields remain null-safe without crashes.
  </action>
  <verify>
`cd cheddar-fpl-sage && pytest tests/test_decision_receipts_api.py -q -k "outcome or verdict or drift"`
  </verify>
  <done>
Persistence path is verified by read-after-write tests; retrospective data cannot silently drop.
  </done>
</task>

</tasks>

<verification>
Run receipt tests and inspect one backend fixture to confirm weekly review payload and persisted outcome fields align.
</verification>

<success_criteria>
- Receipt outcome/process_verdict are persisted and read-after-write verified
- Weekly review backend payload exists and is null-safe for first-run/no-history
</success_criteria>

<output>
After completion, create `cheddar-fpl-sage/.planning/phases/07-output-architecture-hardening/07-04-SUMMARY.md`
</output>

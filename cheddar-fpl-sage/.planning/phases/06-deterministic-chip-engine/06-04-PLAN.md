---
phase: 06-deterministic-chip-engine
plan: 04
type: execute
wave: 3
depends_on:
  - "06-01"
  - "06-02"
  - "06-03"
files_modified:
  - cheddar-fpl-sage/src/cheddar_fpl_sage/analysis/decision_framework/chip_analyzer.py
  - cheddar-fpl-sage/backend/services/result_transformer.py
  - cheddar-fpl-sage/frontend/src/lib/api.ts
  - cheddar-fpl-sage/frontend/src/lib/decisionViewModel.ts
  - cheddar-fpl-sage/frontend/src/lib/decisionViewModel.test.ts
  - cheddar-fpl-sage/frontend/src/components/ChipDecision.tsx
  - cheddar-fpl-sage/tests/test_chip_engine_adapter_integration.py
autonomous: true

must_haves:
  truths:
    - "The backend analysis flow evaluates chips through the new deterministic engine instead of legacy window-only heuristics"
    - "Backend responses expose chip status, score, reason codes, forcedBy, and watchUntil in a frontend-safe contract"
    - "The frontend renders FIRE, WATCH, and PASS chip states distinctly without collapsing everything into use-now vs none"
  artifacts:
    - path: "cheddar-fpl-sage/src/cheddar_fpl_sage/analysis/decision_framework/chip_analyzer.py"
      provides: "Adapter from repo data sources into GameweekState and engine outputs"
      contains: "GameweekState"
    - path: "cheddar-fpl-sage/backend/services/result_transformer.py"
      provides: "Backend chip response transformer with deterministic fields"
      contains: "chip_recommendation"
    - path: "cheddar-fpl-sage/frontend/src/components/ChipDecision.tsx"
      provides: "UI that distinguishes FIRE, WATCH, and PASS"
      min_lines: 80
    - path: "cheddar-fpl-sage/tests/test_chip_engine_adapter_integration.py"
      provides: "Integration coverage from adapter to transformed decision"
      min_lines: 80
  key_links:
    - from: "cheddar-fpl-sage/src/cheddar_fpl_sage/analysis/decision_framework/chip_analyzer.py"
      to: "cheddar-fpl-sage/backend/services/result_transformer.py"
      via: "ChipDecision to API payload mapping"
      pattern: "reasonCodes|forcedBy|watchUntil|score"
    - from: "cheddar-fpl-sage/frontend/src/lib/api.ts"
      to: "cheddar-fpl-sage/frontend/src/lib/decisionViewModel.ts"
      via: "Typed analysis result contract"
      pattern: "chip_recommendation|watchUntil|forcedBy"
    - from: "cheddar-fpl-sage/frontend/src/lib/decisionViewModel.ts"
      to: "cheddar-fpl-sage/frontend/src/components/ChipDecision.tsx"
      via: "View-model state mapping for FIRE/WATCH/PASS"
      pattern: "chipVerdict|chipExplanation|chipStatus"
---

<objective>
Wire the deterministic chip engine into the existing analysis flow, backend transformer, and frontend chip UI so the richer decision contract is visible end-to-end.

Purpose: Per-chip evaluators are only valuable if the live product uses them and surfaces their decision evidence clearly.
Output: Adapter integration in Python plus backend/frontend contract updates that expose the new deterministic chip decision fields.
</objective>

<execution_context>
@.claude/process-acceleration-executors/workflows/execute-plan.md
@.claude/process-acceleration-executors/templates/summary.md
</execution_context>

<context>
@cheddar-fpl-sage/.planning/PROJECT.md
@cheddar-fpl-sage/.planning/STATE.md
@cheddar-fpl-sage/.planning/phases/06-deterministic-chip-engine/06-CONTEXT.md
@cheddar-fpl-sage/src/cheddar_fpl_sage/analysis/decision_framework/chip_analyzer.py
@cheddar-fpl-sage/backend/services/result_transformer.py
@cheddar-fpl-sage/frontend/src/lib/api.ts
@cheddar-fpl-sage/frontend/src/lib/decisionViewModel.ts
@cheddar-fpl-sage/frontend/src/components/ChipDecision.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace legacy chip-analysis wiring with a GameweekState adapter</name>
  <files>
    cheddar-fpl-sage/src/cheddar_fpl_sage/analysis/decision_framework/chip_analyzer.py
    cheddar-fpl-sage/tests/test_chip_engine_adapter_integration.py
  </files>
  <action>Refactor `ChipAnalyzer` into an adapter that builds `GameweekState` from existing squad data, captain candidates, chip availability, fixture-horizon context, rank context, and team-EV signals. Route chip evaluation through the new deterministic engine modules instead of legacy `chip_windows` ranking. Preserve safe fallback behavior when required inputs are missing, but keep formulas centralized in the new engine. Add integration tests that verify the adapter returns deterministic chip output and still honors one-chip-per-GW priority order.</action>
  <verify>`cd cheddar-fpl-sage && pytest tests/test_chip_engine_adapter_integration.py`</verify>
  <done>`ChipAnalyzer` is no longer just a window scorer; it builds a valid `GameweekState`, calls the deterministic engine, and integration tests prove the adapter path works.</done>
</task>

<task type="auto">
  <name>Task 2: Expose the richer chip contract through the backend transformer</name>
  <files>
    cheddar-fpl-sage/backend/services/result_transformer.py
  </files>
  <action>Update the result transformer so `chip_recommendation` preserves the deterministic engine fields: `status`, `score`, `reasonCode`, `reasonCodes`, `forcedBy`, `watchUntil`, `narrative`, and future-window/opportunity-cost metadata when available. Keep backward-compatible fallbacks for older consumers, but make the new fields the canonical contract. Do not force the frontend to reconstruct chip reasoning from prose alone.</action>
  <verify>`cd cheddar-fpl-sage && pytest tests/test_chip_engine_adapter_integration.py`</verify>
  <done>The backend response contains structured chip evidence, not just a binary recommendation string, and the adapter integration tests still pass.</done>
</task>

<task type="auto">
  <name>Task 3: Update frontend types and UI for FIRE/WATCH/PASS chip states</name>
  <files>
    cheddar-fpl-sage/frontend/src/lib/api.ts
    cheddar-fpl-sage/frontend/src/lib/decisionViewModel.ts
    cheddar-fpl-sage/frontend/src/lib/decisionViewModel.test.ts
    cheddar-fpl-sage/frontend/src/components/ChipDecision.tsx
  </files>
  <action>Extend the frontend analysis types to accept the richer chip contract. Update the decision view-model so it preserves chip `status`, maps `FIRE/WATCH/PASS` distinctly, and keeps `watchUntil`, `forcedBy`, and `reasonCodes` available to the UI. Update `ChipDecision.tsx` so WATCH windows are rendered as hold/monitor states with the next review GW, forced decisions are visibly labeled, and PASS/no-chip is no longer conflated with WATCH. Add or update frontend tests for the new mapping behavior.</action>
  <verify>`cd cheddar-fpl-sage/frontend && npm test -- --run decisionViewModel.test.ts && npm run build`</verify>
  <done>The frontend compiles, the chip section displays deterministic engine states clearly, and tests confirm the view-model no longer collapses WATCH into NONE.</done>
</task>

</tasks>

<verification>
Run the Python adapter integration test, then run the targeted frontend test and production build. Inspect one WATCH case and one forced FIRE case in the transformed payload/UI mapping to confirm `watchUntil` and `forcedBy` are preserved end-to-end.
</verification>

<success_criteria>
- Live analysis uses the deterministic engine through `ChipAnalyzer`
- Backend payload exposes structured chip evidence fields
- Frontend renders FIRE, WATCH, and PASS distinctly and builds successfully
</success_criteria>

<output>
After completion, create `cheddar-fpl-sage/.planning/phases/06-deterministic-chip-engine/06-04-SUMMARY.md`
</output>

---
phase: 06-deterministic-chip-engine
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - cheddar-fpl-sage/src/cheddar_fpl_sage/analysis/decision_framework/chip_engine/wildcard_free_hit.py
  - cheddar-fpl-sage/tests/test_chip_engine_wildcard_free_hit.py
autonomous: true

must_haves:
  truths:
    - "Wildcard only fires when squad-improvement conditions are meaningful and hard vetoes are clear"
    - "Free Hit chooses between BGW defense and DGW attack using the stronger use case"
    - "Late-season escalation can force Wildcard or Free Hit only when their chip-specific minimum safety conditions are met"
  artifacts:
    - path: "cheddar-fpl-sage/src/cheddar_fpl_sage/analysis/decision_framework/chip_engine/wildcard_free_hit.py"
      provides: "Deterministic Wildcard and Free Hit evaluators"
      contains: "def evaluate_wildcard"
    - path: "cheddar-fpl-sage/tests/test_chip_engine_wildcard_free_hit.py"
      provides: "Scenario coverage for WC/FH formulas, vetoes, and force logic"
      min_lines: 120
  key_links:
    - from: "cheddar-fpl-sage/tests/test_chip_engine_wildcard_free_hit.py"
      to: "cheddar-fpl-sage/src/cheddar_fpl_sage/analysis/decision_framework/chip_engine/wildcard_free_hit.py"
      via: "Spec scenario matrix"
      pattern: "WC_|FH_"
    - from: "cheddar-fpl-sage/src/cheddar_fpl_sage/analysis/decision_framework/chip_engine/wildcard_free_hit.py"
      to: "cheddar-fpl-sage/src/cheddar_fpl_sage/analysis/decision_framework/chip_engine/shared.py"
      via: "Shared horizon and escalation helpers"
      pattern: "compute_horizon_score|apply_force_escalation"
---

<objective>
Implement the deterministic Wildcard and Free Hit evaluators, including scoring formulas, hard vetoes, reason codes, horizon suppression, and late-season force escalation.

Purpose: These two chips depend most heavily on season shape, squad delta, and BGW/DGW window quality. They need to be locked before adapter wiring happens.
Output: A dedicated evaluator module for Wildcard and Free Hit with spec-derived test coverage.
</objective>

<execution_context>
@.claude/process-acceleration-executors/workflows/execute-plan.md
@.claude/process-acceleration-executors/templates/summary.md
</execution_context>

<context>
@cheddar-fpl-sage/.planning/PROJECT.md
@cheddar-fpl-sage/.planning/TODOS.md
@cheddar-fpl-sage/.planning/phases/06-deterministic-chip-engine/06-CONTEXT.md
@cheddar-fpl-sage/.planning/todos/pending/new-features.md
@cheddar-fpl-sage/src/cheddar_fpl_sage/analysis/decision_framework/chip_analyzer.py
@cheddar-fpl-sage/src/cheddar_fpl_sage/analysis/decision_framework/fixture_horizon.py
@cheddar-fpl-sage/.planning/phases/06-deterministic-chip-engine/06-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Turn the Wildcard and Free Hit spec into scenario tests</name>
  <files>
    cheddar-fpl-sage/tests/test_chip_engine_wildcard_free_hit.py
  </files>
  <action>Write focused tests from the provided scenario table and veto matrix before implementing the evaluators. Cover Wildcard: early-GW veto, low-delta veto, DGW-imminent veto, normal FIRE, soft escalation, and GW37 emergency. Cover Free Hit: heavy BGW defense, insufficient-gain PASS, DGW attack FIRE, Wildcard-preferred veto, and force priority behavior. Assert `status`, `score`, `reasonCode`, `reasonCodes`, `forcedBy`, and `watchUntil` where relevant.</action>
  <verify>`cd cheddar-fpl-sage && pytest tests/test_chip_engine_wildcard_free_hit.py`</verify>
  <done>The WC/FH behavior is locked in tests and directly matches the March 2026 spec tables, including force-escalation documentation fields.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Wildcard and Free Hit evaluators</name>
  <files>
    cheddar-fpl-sage/src/cheddar_fpl_sage/analysis/decision_framework/chip_engine/wildcard_free_hit.py
  </files>
  <action>Create pure evaluators for `evaluate_wildcard()` and `evaluate_free_hit()` using the exact formula structure from the spec. Wildcard must use `teamEvDelta`, `fixtureRunScore`, and `hitCostAvoided`, plus half-season minimums and DGW-imminent vetoes. Free Hit must evaluate BGW defense and DGW attack separately, take the higher score, and hard-veto when Wildcard is still available with meaningful squad EV delta. Use shared helpers for clamping, horizon suppression, and soft/hard/GW37 escalation. Do not wire these evaluators into `ChipAnalyzer` yet; this plan only delivers tested, standalone deterministic evaluators.</action>
  <verify>`cd cheddar-fpl-sage && pytest tests/test_chip_engine_wildcard_free_hit.py`</verify>
  <done>`wildcard_free_hit.py` returns stable `ChipDecision` objects for both chips, and all scenario tests pass without relying on legacy `chip_windows` config.</done>
</task>

</tasks>

<verification>
Run the WC/FH test file and inspect at least one forced decision to confirm `forcedBy` identifies the escalation path instead of silently changing the status.
</verification>

<success_criteria>
- Wildcard evaluator honors half boundaries, delta thresholds, and DGW timing vetoes
- Free Hit evaluator distinguishes BGW defense from DGW attack and chooses the stronger case
- Soft/hard/GW37 force behavior is covered and cannot bypass hard vetoes
</success_criteria>

<output>
After completion, create `cheddar-fpl-sage/.planning/phases/06-deterministic-chip-engine/06-02-SUMMARY.md`
</output>

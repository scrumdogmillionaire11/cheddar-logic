---
phase: 06-deterministic-chip-engine
plan: 03
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - cheddar-fpl-sage/src/cheddar_fpl_sage/analysis/decision_framework/chip_engine/bench_boost_triple_captain.py
  - cheddar-fpl-sage/tests/test_chip_engine_bench_boost_triple_captain.py
autonomous: true

must_haves:
  truths:
    - "Bench Boost only fires when the bench has meaningful projected value and blank starters do not invalidate the week"
    - "Triple Captain only fires when the best captain has the projection, fixture quality, and timing to justify the chip"
    - "Healthy no-edge windows can become WATCH, while late-season force logic still documents itself via forcedBy"
  artifacts:
    - path: "cheddar-fpl-sage/src/cheddar_fpl_sage/analysis/decision_framework/chip_engine/bench_boost_triple_captain.py"
      provides: "Deterministic Bench Boost and Triple Captain evaluators"
      contains: "def evaluate_bench_boost"
    - path: "cheddar-fpl-sage/tests/test_chip_engine_bench_boost_triple_captain.py"
      provides: "Scenario coverage for BB/TC formulas, vetoes, horizon caps, and force logic"
      min_lines: 120
  key_links:
    - from: "cheddar-fpl-sage/tests/test_chip_engine_bench_boost_triple_captain.py"
      to: "cheddar-fpl-sage/src/cheddar_fpl_sage/analysis/decision_framework/chip_engine/bench_boost_triple_captain.py"
      via: "Bench and captain scenario matrix"
      pattern: "BB_|TC_"
    - from: "cheddar-fpl-sage/src/cheddar_fpl_sage/analysis/decision_framework/chip_engine/bench_boost_triple_captain.py"
      to: "cheddar-fpl-sage/src/cheddar_fpl_sage/analysis/decision_framework/chip_engine/shared.py"
      via: "Shared suppression and escalation helpers"
      pattern: "compute_horizon_score|apply_force_escalation"
---

<objective>
Implement the deterministic Bench Boost and Triple Captain evaluators, including their chip-specific formulas, hard veto rules, DGW sensitivity, and late-season force escalation.

Purpose: These chips depend on tightly testable input/output rules and should be completed in isolation before integration wiring starts.
Output: A dedicated evaluator module for Bench Boost and Triple Captain with spec-driven tests.
</objective>

<execution_context>
@.claude/process-acceleration-executors/workflows/execute-plan.md
@.claude/process-acceleration-executors/templates/summary.md
</execution_context>

<context>
@cheddar-fpl-sage/.planning/PROJECT.md
@cheddar-fpl-sage/.planning/TODOS.md
@cheddar-fpl-sage/.planning/phases/06-deterministic-chip-engine/06-CONTEXT.md
@cheddar-fpl-sage/src/cheddar_fpl_sage/analysis/decision_framework/captain_selector.py
@cheddar-fpl-sage/src/cheddar_fpl_sage/analysis/decision_framework/models.py
@cheddar-fpl-sage/tests/fpl_captain_selector_suite.py
@cheddar-fpl-sage/.planning/phases/06-deterministic-chip-engine/06-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Encode Bench Boost and Triple Captain scenarios as tests</name>
  <files>
    cheddar-fpl-sage/tests/test_chip_engine_bench_boost_triple_captain.py
  </files>
  <action>Write tests directly from the spec scenarios and invariants before implementing the evaluators. For Bench Boost, cover DGW-premium bench FIRE, blank-starter veto, weak-bench veto, WATCH when a better DGW is shortly ahead, soft escalation, and emergency minimums. For Triple Captain, cover DGW-premium FIRE, single-GW WATCH when a better DGW is imminent, blank-captain veto, low-projection veto, differential ownership effects, soft escalation, and the rule that force logic still cannot bypass a blank captain hard veto.</action>
  <verify>`cd cheddar-fpl-sage && pytest tests/test_chip_engine_bench_boost_triple_captain.py`</verify>
  <done>The BB/TC spec is executable as tests and proves the engine distinguishes normal timing, WATCH windows, hard vetoes, and force-triggered activation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Bench Boost and Triple Captain evaluators</name>
  <files>
    cheddar-fpl-sage/src/cheddar_fpl_sage/analysis/decision_framework/chip_engine/bench_boost_triple_captain.py
  </files>
  <action>Implement `evaluate_bench_boost()` and `evaluate_triple_captain()` as pure functions returning `ChipDecision`. Bench Boost must use bench projected total, DGW bench count bonus, and blank-starter penalty, with a hard block on any blank starter. Triple Captain must use captain projected points, DGW multiplier, ownership penalty, and FDR bonus, with the DGW priority matrix and better-window veto. Reuse the shared helpers from Plan 06-01 for horizon suppression and force escalation; do not mirror this logic in `CaptainSelector` or frontend code.</action>
  <verify>`cd cheddar-fpl-sage && pytest tests/test_chip_engine_bench_boost_triple_captain.py`</verify>
  <done>`bench_boost_triple_captain.py` produces deterministic BB/TC decisions with full reason-code coverage, and all spec scenarios pass.</done>
</task>

</tasks>

<verification>
Run the BB/TC test file and inspect at least one WATCH result plus one forced FIRE result to confirm `watchUntil` and `forcedBy` are both populated correctly.
</verification>

<success_criteria>
- Bench Boost logic blocks blank starters and values DGW bench strength correctly
- Triple Captain logic respects captain blanks, projection floors, DGW priority, and ownership effects
- Both chips participate in horizon suppression and late-season escalation without violating hard vetoes
</success_criteria>

<output>
After completion, create `cheddar-fpl-sage/.planning/phases/06-deterministic-chip-engine/06-03-SUMMARY.md`
</output>

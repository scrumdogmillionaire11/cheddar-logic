---
phase: 06-deterministic-chip-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cheddar-fpl-sage/src/cheddar_fpl_sage/analysis/decision_framework/chip_engine/__init__.py
  - cheddar-fpl-sage/src/cheddar_fpl_sage/analysis/decision_framework/chip_engine/types.py
  - cheddar-fpl-sage/src/cheddar_fpl_sage/analysis/decision_framework/chip_engine/shared.py
  - cheddar-fpl-sage/tests/test_chip_engine_invariants.py
autonomous: true

must_haves:
  truths:
    - "A stateless chip engine contract exists and can evaluate one gameweek without external mutation"
    - "WATCH decisions always include watchUntil and FIRE decisions record forcedBy when escalation is used"
    - "Season-horizon suppression can cap a qualifying decision to WATCH without bypassing hard vetoes"
  artifacts:
    - path: "cheddar-fpl-sage/src/cheddar_fpl_sage/analysis/decision_framework/chip_engine/types.py"
      provides: "Typed engine input/output contracts"
      contains: "class GameweekState"
    - path: "cheddar-fpl-sage/src/cheddar_fpl_sage/analysis/decision_framework/chip_engine/shared.py"
      provides: "Shared horizon and force-escalation helpers"
      contains: "def compute_horizon_score"
    - path: "cheddar-fpl-sage/tests/test_chip_engine_invariants.py"
      provides: "Deterministic invariant coverage"
      min_lines: 80
  key_links:
    - from: "cheddar-fpl-sage/src/cheddar_fpl_sage/analysis/decision_framework/chip_engine/types.py"
      to: "cheddar-fpl-sage/src/cheddar_fpl_sage/analysis/decision_framework/chip_engine/shared.py"
      via: "ChipDecision normalization and status finalization"
      pattern: "watchUntil|forcedBy|reasonCodes"
    - from: "cheddar-fpl-sage/tests/test_chip_engine_invariants.py"
      to: "cheddar-fpl-sage/src/cheddar_fpl_sage/analysis/decision_framework/chip_engine/shared.py"
      via: "force and horizon invariant tests"
      pattern: "compute_horizon_score|apply_force_escalation|finalize"
---

<objective>
Establish the deterministic chip engine foundation: typed contracts, shared season-horizon logic, force-escalation helpers, and invariant tests that lock the spec before per-chip formulas are implemented.

Purpose: All later chip work depends on one stable stateless contract. Without this foundation, Wildcard/Free Hit/Bench Boost/Triple Captain logic will drift or duplicate rules.
Output: A new `chip_engine/` package scaffold plus tests that codify the engine invariants from the March 2026 spec.
</objective>

<execution_context>
@.claude/process-acceleration-executors/workflows/execute-plan.md
@.claude/process-acceleration-executors/templates/summary.md
</execution_context>

<context>
@cheddar-fpl-sage/.planning/PROJECT.md
@cheddar-fpl-sage/.planning/ROADMAP.md
@cheddar-fpl-sage/.planning/STATE.md
@cheddar-fpl-sage/.planning/TODOS.md
@cheddar-fpl-sage/.planning/phases/06-deterministic-chip-engine/06-CONTEXT.md
@cheddar-fpl-sage/src/cheddar_fpl_sage/analysis/decision_framework/chip_analyzer.py
@cheddar-fpl-sage/src/cheddar_fpl_sage/analysis/decision_framework/models.py
@cheddar-fpl-sage/src/cheddar_fpl_sage/analysis/decision_framework/fixture_horizon.py
@cheddar-fpl-sage/tests/tests_new/test_chip_window_edge_cases.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock the engine contract and invariants in tests</name>
  <files>
    cheddar-fpl-sage/tests/test_chip_engine_invariants.py
  </files>
  <action>Write executable tests first for the spec-level rules that must never regress. Cover: stateless evaluation, one-chip-per-GW priority ordering, `WATCH` requiring `watchUntil`, populated `reasonCode` + `reasonCodes`, `forcedBy` on forced decisions, and the rule that force escalation cannot bypass hard vetoes. Include at least one horizon-suppression case where a would-be `FIRE` becomes `WATCH` because a better window is visible in the next two GWs while `gwsRemaining > 6`.</action>
  <verify>`cd cheddar-fpl-sage && pytest tests/test_chip_engine_invariants.py`</verify>
  <done>The test file fails before implementation and then passes once the shared engine contract exists. Every invariant listed in the spec has direct test coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Implement shared chip-engine contracts and helpers</name>
  <files>
    cheddar-fpl-sage/src/cheddar_fpl_sage/analysis/decision_framework/chip_engine/__init__.py
    cheddar-fpl-sage/src/cheddar_fpl_sage/analysis/decision_framework/chip_engine/types.py
    cheddar-fpl-sage/src/cheddar_fpl_sage/analysis/decision_framework/chip_engine/shared.py
  </files>
  <action>Create the new `chip_engine/` package. In `types.py`, define the stateless engine contracts from the spec: `GameweekState`, `GwProfile`, `PlayerProjection`, `CaptainScore`, and `ChipDecision`. In `shared.py`, add reusable helpers for clamping scores, computing season-horizon metadata, applying the better-window suppression cap, normalizing status transitions, and applying soft/hard/GW37 force escalation. Keep these helpers pure and side-effect free; do not wire them to `ChipAnalyzer` yet, and do not reintroduce the old window-based config logic inside the new engine.</action>
  <verify>`cd cheddar-fpl-sage && pytest tests/test_chip_engine_invariants.py`</verify>
  <done>A reusable engine foundation exists under `chip_engine/`, all invariant tests pass, and the shared helpers are ready for per-chip evaluators in later plans.</done>
</task>

</tasks>

<verification>
Run the invariant test file and inspect the new contracts to confirm the engine can represent `FIRE`, `WATCH`, and `PASS` without relying on mutable global state or frontend-only fields.
</verification>

<success_criteria>
- Deterministic chip-engine types exist under `chip_engine/`
- Shared helpers encode horizon suppression and force escalation
- Invariant tests pass and explicitly prove `watchUntil` / `forcedBy` behavior
</success_criteria>

<output>
After completion, create `cheddar-fpl-sage/.planning/phases/06-deterministic-chip-engine/06-01-SUMMARY.md`
</output>

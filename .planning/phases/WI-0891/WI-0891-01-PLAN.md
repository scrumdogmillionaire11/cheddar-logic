---
phase: WI-0891-remove-live-split-brain
plan: 01
type: execute
wave: 1
depends_on:
  - WI-0890
files_modified:
  - apps/worker/src/jobs/settle_pending_cards.js
  - web/src/lib/games/route-handler.ts
  - apps/worker/src/jobs/__tests__/settle_pending_cards.phase2.test.js
  - web/src/__tests__/api-games-missing-data-contract.test.js
autonomous: true
requirements:
  - WI-0891

must_haves:
  truths:
    - "Settlement replay does not change live true_play authority for active game responses"
    - "The /api/games selection path is deterministic and uses one authority chain whether display log rows exist or not"
    - "Historical backfill behavior remains available without mutating live-authority fields"
    - "Regression tests cover active-run and no-active-run authority paths"
  artifacts:
    - path: "apps/worker/src/jobs/settle_pending_cards.js"
      provides: "guardrails preventing backfill writes to live-authority fields"
      contains: "card_display_log"
    - path: "web/src/lib/games/route-handler.ts"
      provides: "single true_play authority selector"
      contains: "true_play"
    - path: "apps/worker/src/jobs/__tests__/settle_pending_cards.phase2.test.js"
      provides: "worker regression proving settlement replay is non-authoritative for live selection"
    - path: "web/src/__tests__/api-games-missing-data-contract.test.js"
      provides: "API regression for active-run and no-active-run selection consistency"
  key_links:
    - from: "apps/worker/src/jobs/settle_pending_cards.js"
      to: "web/src/lib/games/route-handler.ts"
      via: "fields written by settlement are excluded from live authority read path"
      pattern: "true_play|card_display_log|active_run"
    - from: "web/src/lib/games/route-handler.ts"
      to: "web/src/__tests__/api-games-missing-data-contract.test.js"
      via: "contract assertions on stable source selection"
      pattern: "selected source|true_play"
    - from: "apps/worker/src/jobs/__tests__/settle_pending_cards.phase2.test.js"
      to: "apps/worker/src/jobs/settle_pending_cards.js"
      via: "replay test proves no live-authority mutation"
      pattern: "phase2"
---

<objective>
Remove split-brain behavior between settlement-time backfill and live /api/games true_play selection.

Purpose: live card surfacing must have one runtime authority chain. Settlement replay can keep historical support, but it cannot mutate fields that change current true_play ranking for active cards.

Output: worker backfill guards, deterministic /api/games selection path, and regression tests for active-run and no-active-run paths.
</objective>

<execution_context>
@./.claude/process-acceleration-executors/workflows/execute-plan.md
@./.claude/process-acceleration-executors/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@WORK_QUEUE/WI-0890.md
@WORK_QUEUE/WI-0891.md
@apps/worker/src/jobs/settle_pending_cards.js
@web/src/lib/games/route-handler.ts
@apps/worker/src/jobs/__tests__/settle_pending_cards.phase2.test.js
@web/src/__tests__/api-games-missing-data-contract.test.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock settlement backfill away from live-authority fields</name>
  <files>apps/worker/src/jobs/settle_pending_cards.js
apps/worker/src/jobs/__tests__/settle_pending_cards.phase2.test.js</files>
  <behavior>
    - Test 1: Running settlement twice on the same fixture does not alter fields used by /api/games live true_play ranking.
    - Test 2: Historical backfill fields still update when allowed.
  </behavior>
  <action>
In `settle_pending_cards.js`, add explicit write guards so settlement backfill never mutates fields that participate in live /api/games true_play authority for active cards. Keep historical-only updates intact.

In `settle_pending_cards.phase2.test.js`, add regression coverage that executes replay and asserts: (a) live-authority fields are unchanged; (b) permitted historical fields still backfill.
  </action>
  <verify>
    <automated>npm --prefix apps/worker test -- --testPathPattern="settle_pending_cards.phase2"</automated>
  </verify>
  <done>
Settlement replay is non-authoritative for live selection inputs, and worker phase2 tests prove replay stability.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce a single true_play authority selector in /api/games</name>
  <files>web/src/lib/games/route-handler.ts
web/src/__tests__/api-games-missing-data-contract.test.js</files>
  <behavior>
    - Test 1: Active-run path selects true_play from the same authority chain whether display-log rows exist or not.
    - Test 2: No-active-run path uses deterministic fallback from that same chain.
    - Test 3: Settlement replay data does not outrank live authority for active cards.
  </behavior>
  <action>
In `route-handler.ts`, replace any split-path true_play selection logic with one deterministic contract-ordered selector for both active-run and no-active-run conditions.

Selection must not branch to settlement-mutated display-log precedence for live ranking. Keep behavior response-level compatible, but make source ordering explicit and single-path.

In `api-games-missing-data-contract.test.js`, add/adjust assertions for active-run and no-active-run contract behavior, including display-log-present and display-log-absent fixtures.
  </action>
  <verify>
    <automated>node web/src/__tests__/api-games-missing-data-contract.test.js</automated>
  </verify>
  <done>
/api/games true_play selection uses one consistent path, and contract tests prove consistency across data-presence variants.
  </done>
</task>

<task type="auto">
  <name>Task 3: End-to-end regression proving replay cannot change live surfaced pick</name>
  <files>apps/worker/src/jobs/__tests__/settle_pending_cards.phase2.test.js
web/src/__tests__/api-games-missing-data-contract.test.js</files>
  <action>
Add cross-fixture assertions that model this sequence: compute baseline active response -> run settlement backfill/replay -> re-read response. Assert that surfaced true_play identity and ordering for active cards remain unchanged.

Include explicit no-active-run scenario to ensure deterministic fallback is unchanged after replay.
  </action>
  <verify>
    <automated>npm --prefix apps/worker test -- --testPathPattern="settle_pending_cards.phase2" && node web/src/__tests__/api-games-missing-data-contract.test.js</automated>
  </verify>
  <done>
Both active-run and no-active-run paths remain stable after settlement replay, closing the split-brain regression vector.
  </done>
</task>

</tasks>

<verification>
Run:
- npm --prefix apps/worker test -- --testPathPattern="settle_pending_cards.phase2"
- node web/src/__tests__/api-games-missing-data-contract.test.js

Pass condition: both suites pass with explicit assertions that settlement replay does not alter live true_play selection authority.
</verification>

<success_criteria>
- Settlement backfill no longer writes fields that can change live true_play ranking for active cards.
- /api/games true_play selection uses one deterministic authority chain with and without display-log rows.
- Historical backfill remains available where needed.
- Tests cover active-run and no-active-run behavior and prevent split-brain regression.
</success_criteria>

<output>
After completion, create `.planning/phases/WI-0891/WI-0891-SUMMARY.md`
</output>

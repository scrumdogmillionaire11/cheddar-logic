---
phase: WI-1134
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/lib/games/route-handler.ts
  - web/src/lib/games/query-layer.ts
  - web/src/lib/games/service-layer.ts
  - web/src/lib/games/transform-layer.ts
  - web/src/lib/games/perf-metrics.ts
autonomous: true
requirements:
  - WI-1134-API-01
  - WI-1134-PERF-01
  - WI-1134-REG-01
must_haves:
  truths:
    - "GET /api/games still returns the existing contract shape after refactor"
    - "Query, service, and transform stages each emit timing metrics per request"
    - "Forced timeout/fallback paths continue to produce a valid response mode"
  artifacts:
    - path: "web/src/lib/games/query-layer.ts"
      provides: "Query-stage extraction for /api/games data access"
    - path: "web/src/lib/games/service-layer.ts"
      provides: "Service-stage orchestration and business-safe composition"
    - path: "web/src/lib/games/transform-layer.ts"
      provides: "Transform-stage response shaping"
    - path: "web/src/lib/games/perf-metrics.ts"
      provides: "Stage timing helpers and budget checks"
    - path: "web/src/lib/games/route-handler.ts"
      provides: "Thin orchestration entrypoint wiring query/service/transform"
  key_links:
    - from: "web/src/lib/games/route-handler.ts"
      to: "web/src/lib/games/query-layer.ts"
      via: "query-stage call"
      pattern: "from './query-layer'|from \"./query-layer\""
    - from: "web/src/lib/games/route-handler.ts"
      to: "web/src/lib/games/service-layer.ts"
      via: "service-stage call"
      pattern: "from './service-layer'|from \"./service-layer\""
    - from: "web/src/lib/games/route-handler.ts"
      to: "web/src/lib/games/transform-layer.ts"
      via: "transform-stage call"
      pattern: "from './transform-layer'|from \"./transform-layer\""
    - from: "web/src/lib/games/route-handler.ts"
      to: "web/src/lib/games/perf-metrics.ts"
      via: "stage timing instrumentation"
      pattern: "from './perf-metrics'|from \"./perf-metrics\""
---

<objective>
Decompose the `/api/games` route handler into explicit query, service, and transform layers with stage-level timing instrumentation and deterministic fallback behavior.

Purpose: Reduce fragility and improve observability without changing business policy.
Output: Layered modules + route-handler wiring + passing regression tests.
</objective>

<execution_context>
@/Users/ajcolubiale/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ajcolubiale/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@WORK_QUEUE/WI-1134.md
@WORK_QUEUE/COMPLETE/WI-1124.md
@WORK_QUEUE/COMPLETE/WI-1125.md
@WORK_QUEUE/COMPLETE/WI-1126.md
@WORK_QUEUE/COMPLETE/WI-1127.md
@web/src/lib/games/route-handler.ts
@web/src/__tests__/api-games-market-smoke.test.js
@web/src/__tests__/api-games-repair-budget.test.js
@web/src/__tests__/api-games-missing-data-contract.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract query/service/transform contracts and modules</name>
  <files>web/src/lib/games/query-layer.ts, web/src/lib/games/service-layer.ts, web/src/lib/games/transform-layer.ts</files>
  <action>Create explicit exported functions for query, service, and transform stages used by `/api/games`. Preserve behavior and route output contract; do not introduce decision policy changes. Keep function signatures narrow and typed so the handler can orchestrate with minimal inline logic.</action>
  <verify>
    <automated>npm --prefix web run test:api:games:market</automated>
  </verify>
  <done>New modules exist with exported stage functions and tests still pass for market smoke behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Wire thin route-handler orchestration + stage timing instrumentation</name>
  <files>web/src/lib/games/route-handler.ts, web/src/lib/games/perf-metrics.ts</files>
  <action>Refactor `route-handler.ts` to orchestrate query -> service -> transform stages through the extracted modules. Add stage-level timing instrumentation helpers in `perf-metrics.ts` and ensure each stage emits deterministic timing keys. Keep timeout/fallback response mode behavior equivalent to pre-refactor semantics.</action>
  <verify>
    <automated>npm --prefix web run test:api:games:repair-budget</automated>
  </verify>
  <done>Handler imports and invokes all extracted layers, stage metrics are emitted for query/service/transform, and repair-budget regression passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Lock contract and fallback behavior with regression checks</name>
  <files>web/src/__tests__/api-games-missing-data-contract.test.js</files>
  <behavior>
    - Test 1: Missing-data responses remain contract-compatible after decomposition.
    - Test 2: Forced timeout/fallback path still returns valid response mode output.
  </behavior>
  <action>Update or extend missing-data contract coverage only as needed to assert the decomposed flow preserves public behavior. Do not weaken assertions; strengthen explicit fallback guarantees where ambiguity existed.</action>
  <verify>
    <automated>node web/src/__tests__/api-games-missing-data-contract.test.js</automated>
  </verify>
  <done>Regression tests prove output/fallback parity for missing-data and timeout scenarios.</done>
</task>

</tasks>

<verification>
Run all WI-required checks from repo root:
- `npm --prefix web run test:api:games:market`
- `npm --prefix web run test:api:games:repair-budget`
- `node web/src/__tests__/api-games-missing-data-contract.test.js`
</verification>

<success_criteria>
- `/api/games` handler delegates to query/service/transform modules with reduced inline orchestration complexity.
- Query, service, and transform stage timing metrics are emitted in one deterministic instrumentation path.
- Existing smoke, repair-budget, and missing-data contract tests all pass without behavior regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/WI-1134/WI-1134-01-SUMMARY.md`
</output>

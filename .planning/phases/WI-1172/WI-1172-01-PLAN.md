---
phase: WI-1172
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/worker/src/jobs/run_mlb_model.js
  - apps/worker/src/jobs/__tests__/run_mlb_model.test.js
  - docs/decisions/ADR-0007-mlb-f5-full-model-projection-contract.md
autonomous: true
requirements:
  - WI-1172-FALLBACK-01
  - WI-1172-ADR-01
  - WI-1172-REG-01
must_haves:
  truths:
    - "MLB starter-skill fallback does not include dead siera/x_era null-weight blend branches unless active populated signals are proven in-repo"
    - "When x_fip exists, fallback projection uses the active runtime contract and produces deterministic values"
    - "When starter-skill inputs are missing, fallback path degrades safely without formula drift or crashes"
    - "ADR-0007 fallback contract text matches the implemented fallback behavior exactly"
  artifacts:
    - path: "apps/worker/src/jobs/run_mlb_model.js"
      provides: "Starter-skill fallback contract simplified to active runtime signals"
    - path: "apps/worker/src/jobs/__tests__/run_mlb_model.test.js"
      provides: "Regression coverage for populated x_fip, missing starter-skill inputs, and raw-vs-DB fallback parity"
    - path: "docs/decisions/ADR-0007-mlb-f5-full-model-projection-contract.md"
      provides: "Documented fallback contract aligned to runtime behavior"
  key_links:
    - from: "apps/worker/src/jobs/run_mlb_model.js"
      to: "apps/worker/src/jobs/__tests__/run_mlb_model.test.js"
      via: "Regression tests assert active-signal fallback behavior and missing-data degradation"
      pattern: "getPitcherEraFromDb|computeProjectionFloorF5|x_fip|siera|x_era"
    - from: "apps/worker/src/jobs/run_mlb_model.js"
      to: "docs/decisions/ADR-0007-mlb-f5-full-model-projection-contract.md"
      via: "ADR contract wording mirrors implementation"
      pattern: "fallback|starter-skill|x_fip|siera|x_era"
---

<objective>
Remove dead SIERA/xERA weighting branches from MLB starter-skill fallback logic and lock runtime + ADR behavior to the active signal contract that exists today.

Purpose: Prevent contract drift where docs and code imply a three-signal blend that is not materially available in runtime data.
Output: Simplified fallback implementation, targeted regressions, and ADR language parity.
</objective>

<execution_context>
@/Users/ajcolubiale/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ajcolubiale/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@WORK_QUEUE/WI-1172.md
@apps/worker/src/jobs/run_mlb_model.js
@apps/worker/src/jobs/__tests__/run_mlb_model.test.js
@docs/decisions/ADR-0007-mlb-f5-full-model-projection-contract.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing regressions that encode the active fallback contract</name>
  <files>apps/worker/src/jobs/__tests__/run_mlb_model.test.js</files>
  <behavior>
    - Test 1: Populated `x_fip` starter-skill input drives fallback output without requiring `siera` or `x_era`.
    - Test 2: Missing starter-skill input degrades to existing safe fallback behavior with deterministic output (no crash).
    - Test 3: Raw-data fallback path and DB fallback path produce parity under equivalent `x_fip` availability.
  </behavior>
  <action>Add or update targeted tests around `getPitcherEraFromDb()` and `computeProjectionFloorF5()` fallback semantics so that any reintroduction of dead null-weight SIERA/xERA blend logic fails. Keep tests narrow to this WI scope and avoid broad formula redesign assertions.</action>
  <verify>
    <automated>npm --prefix apps/worker run test -- src/jobs/__tests__/run_mlb_model.test.js --runInBand</automated>
  </verify>
  <done>Regression tests explicitly lock populated-`x_fip`, missing-skill fallback, and raw-vs-DB parity expectations before runtime edits.</done>
</task>

<task type="auto">
  <name>Task 2: Simplify starter-skill fallback implementation to active runtime signals</name>
  <files>apps/worker/src/jobs/run_mlb_model.js, apps/worker/src/jobs/__tests__/run_mlb_model.test.js</files>
  <action>In `run_mlb_model.js`, remove dead fallback weighting branches that treat `siera`/`x_era` as active contributors unless this task first proves in-repo they are materially populated by the active ingest path. Implement the smallest honest contract for current runtime data (direct `x_fip` usage or equivalent active-signal-only branch), keep existing guardrails for missing data, and update tests from Task 1 to green without broad model redesign.</action>
  <verify>
    <automated>npm --prefix apps/worker run test -- src/jobs/__tests__/run_mlb_model.test.js --runInBand</automated>
  </verify>
  <done>Fallback projection path no longer carries dead `siera`/`x_era` weighting logic and all WI-1172 regressions pass.</done>
</task>

<task type="auto">
  <name>Task 3: Align ADR-0007 fallback contract text with runtime behavior</name>
  <files>docs/decisions/ADR-0007-mlb-f5-full-model-projection-contract.md</files>
  <action>Update ADR-0007 sections describing starter-skill fallback to match the implemented active-signal contract exactly. Remove or clearly mark dormant future-state language so the document does not present inactive SIERA/xERA weighting as current behavior.</action>
  <verify>
    <automated>rg -n "siera|x_era|xERA|starter-skill|fallback" docs/decisions/ADR-0007-mlb-f5-full-model-projection-contract.md</automated>
  </verify>
  <done>ADR-0007 and runtime fallback behavior are contract-consistent for active data availability.</done>
</task>

</tasks>

<verification>
Run required test command from repo root:
- `npm --prefix apps/worker run test -- src/jobs/__tests__/run_mlb_model.test.js --runInBand`

Supplementary checks:
- Confirm no dead fallback weighting branch survives in runtime path:
  - `rg -n "siera|x_era|xERA|getPitcherEraFromDb|computeProjectionFloorF5" apps/worker/src/jobs/run_mlb_model.js`
- Confirm ADR contract language mirrors active runtime behavior:
  - `rg -n "fallback|starter-skill|x_fip|siera|x_era" docs/decisions/ADR-0007-mlb-f5-full-model-projection-contract.md`
</verification>

<success_criteria>
- Runtime fallback path is simplified to active signals and no dead SIERA/xERA weighting remains unless proven active in-repo.
- Regression tests cover populated `x_fip`, missing starter-skill fallback, and raw-vs-DB parity.
- ADR-0007 and runtime implementation describe the same active fallback contract.
</success_criteria>

<output>
After completion, create `.planning/phases/WI-1172/WI-1172-01-SUMMARY.md`
</output>

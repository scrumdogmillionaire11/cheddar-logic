---
status: testing
phase: WI-1220-1224
source: WORK_QUEUE/WI-1220.md, WORK_QUEUE/WI-1224.md
started: 2026-04-30T00:00:00Z
updated: 2026-04-30T00:00:00Z
---

## Current Test

number: 1
name: WI-1224 — Fresh mlb-f5 card payload contains projection_settlement_policy
expected: |
  Create or inspect a fresh mlb-f5 card payload in the DB (or from a recent model run).
  The payload JSON should contain a `projection_settlement_policy` object with:
  - market_family: "MLB_F5_TOTAL"
  - grading_mode: "OFFICIAL" or "TRACK_ONLY"
  - official_call: "UNDER_3_5", "OVER_4_5", or null
  - reason_code: "CLEAR_UNDER", "CLEAR_OVER", or "GRAY_ZONE_NO_CALL"
awaiting: user response

## Tests

### AUTO-1. WI-1220 — probe-espn-series-status.js exists and is runnable
expected: File exists at apps/worker/src/scripts/probe-espn-series-status.js and can be executed with node
result: pass

### AUTO-2. WI-1220 — TODO(WI-0970) removed from nhl-pace-model.js
expected: grep "TODO(WI-0970)" returns no matches in nhl-pace-model.js
result: pass

### AUTO-3. WI-1220 — seriesStatus path documented as PROVISIONAL
expected: nhl-pace-model.js line 937 contains a PROVISIONAL comment and uses raw_data.seriesStatus
result: pass

### AUTO-4. WI-1220 — Warning emitted when isPlayoff=true and seriesStatus=null
expected: Unit test confirms console.warn('[NHL-playoff] seriesStatus missing...') fires when playoff game lacks seriesStatus
result: pass

### AUTO-5. WI-1220 — Three unit fixtures for extractPlayoffRegimeFeatures
expected: (a) seriesStatus present → playoff_series_game_num positive int + boolean pressure; (b) absent → null without throw; (c) missing emits warn
result: pass

### AUTO-6. WI-1220 — seriesStatus ingestion into raw_data confirmed
expected: pull_espn_games_direct.js writes competition.seriesStatus to raw_data.seriesStatus; 3 ingestion tests pass
result: pass

### AUTO-7. WI-1224 — projection_settlement_policy validator schema exists
expected: packages/data/src/validators/card-payload.js contains projectionSettlementPolicySchema with correct enum values
result: pass

### AUTO-8. WI-1224 — Producer emits all three policy states
expected: run-mlb-model.dual-run.test.js tests OFFICIAL/UNDER, OFFICIAL/OVER, TRACK_ONLY policy objects; all 22 tests pass
result: pass

### AUTO-9. WI-1224 — Settlement uses persisted policy not proj_value inference
expected: settle_projections.js reads policy from payload.projection_settlement_policy; no 3.5/4.5 threshold check for official call; 50 settle tests pass
result: pass

### AUTO-10. WI-1224 — Results API filters TRACK_ONLY rows
expected: projection-settled/route.ts query includes AND (ppe.card_family != 'MLB_F5_TOTAL' OR ppe.grading_mode = 'OFFICIAL')
result: pass

### AUTO-11. WI-1224 — Legacy payload handling logs LEGACY_PAYLOAD_NO_POLICY
expected: settle_projections logs LEGACY_PAYLOAD_NO_POLICY and returns 0 when projection_settlement_policy is absent; tested
result: pass

### 1. WI-1224 — Fresh mlb-f5 card payload contains projection_settlement_policy
expected: Create or inspect a fresh mlb-f5 card payload in the DB (or from a recent model run). The payload JSON should contain a `projection_settlement_policy` object with market_family, grading_mode, official_call, and reason_code fields.
result: [pending]

### 2. WI-1224 — TRACK_ONLY card produces no official proxy row in results API
expected: Settle a TRACK_ONLY (gray-zone) mlb-f5 card. Confirm the card's result does NOT appear in the projection-settled API response as an official MLB F5 outcome.
result: [pending]

### 3. WI-1224 — Legacy card skips settlement with LEGACY_PAYLOAD_NO_POLICY log
expected: Trigger settlement on a pre-WI-1224 mlb-f5 card (one without projection_settlement_policy). Confirm the log shows LEGACY_PAYLOAD_NO_POLICY, no proxy row is written, and no crash occurs.
result: [pending]

### 4. WI-1220 — NHL model run with playoff game emits playoff regime fields
expected: During a playoff NHL model run (or with a copied playoff snapshot in dry-run), confirm playoff_series_game_num is non-null and playoff_elimination_pressure is a boolean in the card payload output.
result: [pending]

## Summary

total: 15
passed: 11
issues: 0
pending: 4
skipped: 0

## Gaps

[none yet]

---
phase: WI-0914-playoff-overlay-layer
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/worker/src/utils/playoff-overlay.js
  - apps/worker/src/utils/playoff-detection.js
  - apps/worker/src/schedulers/main.js
  - apps/worker/src/jobs/check_pipeline_health.js
  - apps/worker/src/jobs/execution-gate.js
  - packages/data/src/validators/card-payload.js
  - apps/worker/src/__tests__/playoff-overlay.test.js
  - apps/worker/src/jobs/__tests__/check_pipeline_health.watchdog.test.js
  - apps/worker/src/__tests__/scheduler-main-calibration.test.js
autonomous: true
requirements:
  - PO-OVERLAY-01
  - PO-OVERLAY-02
  - PO-OVERLAY-03
must_haves:
  truths:
    - "Playoff-game cards include a normalized playoff overlay block with phase, leverage, and volatility context"
    - "When playoff-critical context is unresolved or stale, affected outputs resolve to HOLD/PASS/BLOCKED rather than EXECUTABLE"
    - "Cards with malformed playoff overlay metadata are prevented from actionable publication"
  artifacts:
    - path: "apps/worker/src/utils/playoff-overlay.js"
      provides: "shared overlay resolver and per-sport bounded adjustment contract"
      contains: "resolvePlayoffOverlay"
    - path: "apps/worker/src/jobs/check_pipeline_health.js"
      provides: "health checks that account for playoff-context staleness and stricter gating"
      contains: "playoff"
    - path: "packages/data/src/validators/card-payload.js"
      provides: "payload contract validation for playoff overlay metadata"
      contains: "playoff_overlay"
  key_links:
    - from: "apps/worker/src/utils/playoff-overlay.js"
      to: "apps/worker/src/jobs/execution-gate.js"
      via: "overlay strictness and volatility multipliers applied before actionable decision"
      pattern: "playoff|overlay|volatility"
    - from: "apps/worker/src/schedulers/main.js"
      to: "apps/worker/src/jobs/check_pipeline_health.js"
      via: "playoff-sensitive cadence and stale-context checks"
      pattern: "pipeline health|playoff"
---

<objective>
Create a shared playoff overlay foundation and wire it into scheduling, gating, and payload contracts.

Purpose: make postseason behavior deterministic and repeatable across all sport runners, instead of ad hoc per-runner tweaks.

Output: a central overlay resolver, playoff-aware watchdog/scheduler behavior, and schema-level validation for overlay metadata.
</objective>

<execution_context>
@./.claude/process-acceleration-executors/workflows/execute-plan.md
@./.claude/process-acceleration-executors/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@WORK_QUEUE/WI-0913.md
@WORK_QUEUE/WI-0914.md
@apps/worker/src/utils/playoff-detection.js
@apps/worker/src/schedulers/main.js
@apps/worker/src/jobs/check_pipeline_health.js
@apps/worker/src/jobs/execution-gate.js
@packages/data/src/validators/card-payload.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define shared playoff overlay contract and bounded resolver</name>
  <files>apps/worker/src/utils/playoff-overlay.js
apps/worker/src/utils/playoff-detection.js
apps/worker/src/__tests__/playoff-overlay.test.js</files>
  <behavior>
    - Test 1: Resolver emits deterministic phase and leverage context (REGULAR/FINAL_WEEKS/PLAYOFFS) from snapshot metadata.
    - Test 2: Resolver returns bounded multipliers for volatility, edge thresholds, and eligibility strictness per sport.
    - Test 3: Unknown/missing inputs fail closed to conservative defaults (higher pass tendency, no aggressive boosts).
  </behavior>
  <action>
Create `playoff-overlay.js` with `resolvePlayoffOverlay({ sport, snapshot, marketType, nowUtc })` returning a normalized object:
- `season_phase`, `leverage_context`, `volatility_band`, `edge_threshold_delta`, `eligibility_strictness`, `reason_codes`
- sport-specific extension fields: NHL goalie/PP/TOI strictness, NBA rotation/blowout adjustments, NFL weather/conservatism multipliers, MLB bullpen/F5-vs-FG weights

Use `playoff-detection.js` as the phase detector source of truth and extend it only where needed to support deterministic phase resolution.

Add a unit suite in `playoff-overlay.test.js` covering all four sports and hard bounds so overlays cannot exceed configured ranges.
  </action>
  <verify>
    <automated>npm --prefix apps/worker run test -- --testPathPattern="playoff-overlay"</automated>
  </verify>
  <done>
Shared overlay resolver exists, is bounded, and has deterministic cross-sport test coverage.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire playoff overlay into scheduler and watchdog conservative behavior</name>
  <files>apps/worker/src/schedulers/main.js
apps/worker/src/jobs/check_pipeline_health.js
apps/worker/src/jobs/__tests__/check_pipeline_health.watchdog.test.js
apps/worker/src/__tests__/scheduler-main-calibration.test.js</files>
  <behavior>
    - Test 1: Health checks flag unresolved playoff-critical context as warning/failure and surface reason codes.
    - Test 2: Scheduler cadence respects playoff-sensitive refresh needs without forcing non-playoff games into the same strict path.
    - Test 3: Overlay-driven stale context causes conservative status preference.
  </behavior>
  <action>
Integrate overlay metadata reads in scheduler/watchdog paths so playoff games can use stricter freshness/gating checks. Keep logic bounded and additive: no existing regular-season behavior regressions.

In health checks, add playoff-context guardrails (for example unresolved confirmation-critical inputs or stale leverage context) that classify to warning/failed with explicit reason text.

Update scheduler calibration tests and watchdog tests to verify playoff-aware behavior and backward-compatible non-playoff behavior.
  </action>
  <verify>
    <automated>npm --prefix apps/worker run test -- --testPathPattern="check_pipeline_health.watchdog|scheduler-main-calibration"</automated>
  </verify>
  <done>
Scheduler and watchdog honor playoff-aware conservative rules with passing regression coverage.
  </done>
</task>

<task type="auto">
  <name>Task 3: Extend card payload validation for playoff overlay metadata</name>
  <files>packages/data/src/validators/card-payload.js
apps/worker/src/jobs/execution-gate.js</files>
  <action>
Extend payload validators to accept a strict `playoff_overlay` object and reject malformed shape/types. Wire execution gate to read validated overlay fields when deciding actionable status under high-variance playoff conditions.

Ensure this remains backward compatible for legacy payloads by treating missing overlay as regular-season defaults.
  </action>
  <verify>
    <automated>npm --prefix apps/worker run test -- --testPathPattern="check_pipeline_health.watchdog|run_nhl_model.playoff-sigma|run_nba_model.test"</automated>
  </verify>
  <done>
Payload contract enforces overlay schema and execution gate can consume it without breaking existing runners.
  </done>
</task>

</tasks>

<verification>
Run:
- npm --prefix apps/worker run test -- --testPathPattern="playoff-overlay"
- npm --prefix apps/worker run test -- --testPathPattern="check_pipeline_health.watchdog"
- npm --prefix apps/worker run test -- --testPathPattern="scheduler-main-calibration"

Pass condition: shared contract tests and scheduler/watchdog regression tests pass with playoff-aware checks enabled.
</verification>

<success_criteria>
- Shared playoff overlay object exists and is bounded per sport.
- Watchdog and scheduler use playoff context to prefer conservative outcomes when uncertainty is unresolved.
- Payload validation contract supports overlay metadata safely.
</success_criteria>

<output>
After completion, create `.planning/phases/WI-0914/WI-0914-SUMMARY.md`
</output>

---
phase: WI-0914-playoff-overlay-layer
plan: "03"
type: execute
wave: 2
depends_on:
  - WI-0914-01
files_modified:
  - apps/worker/src/jobs/run_nfl_model.js
  - apps/worker/src/jobs/run_mlb_model.js
  - apps/worker/src/models/mlb-model.js
  - packages/models/src/decision-pipeline-v2-edge-config.js
  - apps/worker/src/jobs/__tests__/run_nfl_model.test.js
  - apps/worker/src/jobs/__tests__/run_mlb_model.test.js
autonomous: true
requirements:
  - PO-NFL-01
  - PO-MLB-01
  - PO-THRESH-01
must_haves:
  truths:
    - "NFL playoff overlays increase weather/coaching conservatism impact and require stronger edges"
    - "MLB playoff overlays account for shorter starter leash, bullpen hierarchy, and F5-vs-FG separation"
    - "Playoff market sharpness is reflected by stricter threshold requirements and more PASS outcomes"
  artifacts:
    - path: "apps/worker/src/jobs/run_nfl_model.js"
      provides: "NFL playoff overlay integration in model-to-card path"
      contains: "playoff"
    - path: "apps/worker/src/jobs/run_mlb_model.js"
      provides: "MLB playoff overlay integration with bullpen/F5 context"
      contains: "playoff"
    - path: "packages/models/src/decision-pipeline-v2-edge-config.js"
      provides: "threshold profile adjustments for playoff strictness"
      contains: "resolveThresholdProfile"
  key_links:
    - from: "apps/worker/src/jobs/run_nfl_model.js"
      to: "packages/models/src/decision-pipeline-v2-edge-config.js"
      via: "playoff edge threshold requirement"
      pattern: "threshold|playoff"
    - from: "apps/worker/src/jobs/run_mlb_model.js"
      to: "apps/worker/src/models/mlb-model.js"
      via: "F5-vs-FG and bullpen-aware overlay modifiers"
      pattern: "bullpen|f5|playoff"
---

<objective>
Apply postseason overlays to NFL and MLB model runners and harden threshold behavior for sharper playoff markets.

Purpose: encode playoff-specific risk dynamics (weather/leverage for NFL, bullpen/short leash for MLB) while enforcing stricter execution thresholds.

Output: NFL+MLB overlay wiring and threshold hardening with regression tests.
</objective>

<execution_context>
@./.claude/process-acceleration-executors/workflows/execute-plan.md
@./.claude/process-acceleration-executors/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@WORK_QUEUE/WI-0914.md
@apps/worker/src/jobs/run_nfl_model.js
@apps/worker/src/jobs/run_mlb_model.js
@apps/worker/src/models/mlb-model.js
@packages/models/src/decision-pipeline-v2-edge-config.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NFL playoff overlay integration for weather, conservatism, and star dependency</name>
  <files>apps/worker/src/jobs/run_nfl_model.js
apps/worker/src/jobs/__tests__/run_nfl_model.test.js</files>
  <behavior>
    - Test 1: Wind/rain playoff contexts downgrade marginal pass-heavy overs and passing ladders.
    - Test 2: Conservative script context reduces aggressive edge confidence.
    - Test 3: Primary usage dependency gets stronger confidence weighting while secondary pieces are dampened.
  </behavior>
  <action>
Wire shared playoff overlay into NFL runner before final decision/card construction.

Apply bounded modifiers for weather and coaching conservatism in playoff context and emit reason codes in payload metadata. Preserve non-playoff defaults.

Add NFL runner regression tests for playoff weather and conservatism overlays.
  </action>
  <verify>
    <automated>npm --prefix apps/worker run test -- --testPathPattern="run_nfl_model.test"</automated>
  </verify>
  <done>
NFL playoff risk overlays are applied deterministically and covered by tests.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MLB playoff overlay integration for bullpen hierarchy and F5/FG separation</name>
  <files>apps/worker/src/jobs/run_mlb_model.js
apps/worker/src/models/mlb-model.js
apps/worker/src/jobs/__tests__/run_mlb_model.test.js</files>
  <behavior>
    - Test 1: Playoff context shifts confidence weighting toward bullpen quality for full-game paths.
    - Test 2: F5 markets retain stronger starter-context signal than full-game paths.
    - Test 3: Cold-weather suppression and lineup concentration apply bounded run-environment adjustments.
  </behavior>
  <action>
Integrate playoff overlay in MLB runner/model with explicit distinctions:
- shorter starter leash awareness
- bullpen hierarchy effects on full-game volatility
- F5 preference logic where overlay indicates higher bullpen uncertainty

Keep deterministic shape and annotate payload metadata with applied overlays.
  </action>
  <verify>
    <automated>npm --prefix apps/worker run test -- --testPathPattern="run_mlb_model.test"</automated>
  </verify>
  <done>
MLB playoff overlays are present, bounded, and validated by regression tests.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Raise playoff edge thresholds in shared decision profile</name>
  <files>packages/models/src/decision-pipeline-v2-edge-config.js
apps/worker/src/jobs/__tests__/run_nfl_model.test.js
apps/worker/src/jobs/__tests__/run_mlb_model.test.js</files>
  <behavior>
    - Test 1: Playoff profile requires stronger edge than regular season for the same market.
    - Test 2: Marginal edges that pass in regular season can downgrade to PASS/HOLD in playoff context.
  </behavior>
  <action>
Extend `resolveThresholdProfile` contract to support playoff strictness adjustment while preserving existing call signatures.

Use bounded increments and avoid hardcoding sport-specific magic numbers in runner code when profile lookup can own them.

Add/adjust tests to confirm stricter playoff thresholds are observable in NFL/MLB decision outcomes.
  </action>
  <verify>
    <automated>npm --prefix apps/worker run test -- --testPathPattern="run_nfl_model.test|run_mlb_model.test|decision-pipeline-v2"</automated>
  </verify>
  <done>
Playoff threshold strictness is centralized in shared decision profiles and validated by tests.
  </done>
</task>

</tasks>

<verification>
Run:
- npm --prefix apps/worker run test -- --testPathPattern="run_nfl_model.test"
- npm --prefix apps/worker run test -- --testPathPattern="run_mlb_model.test"
- npm --prefix apps/worker run test -- --testPathPattern="decision-pipeline-v2"

Pass condition: NFL/MLB playoff overlay tests and threshold profile tests pass with stricter playoff gating behavior.
</verification>

<success_criteria>
- NFL and MLB runners consume overlay context and apply bounded playoff adjustments.
- Sharper playoff-market threshold strictness is centralized and testable.
- More marginal playoff spots resolve to PASS/HOLD versus regular-season profile.
</success_criteria>

<output>
After completion, append outcomes to `.planning/phases/WI-0914/WI-0914-SUMMARY.md`
</output>

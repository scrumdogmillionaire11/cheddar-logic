---
phase: WI-0780-scheduler-decompose
plan: 02
type: execute
wave: 2
depends_on:
  - WI-0780-01
files_modified:
  - apps/worker/src/schedulers/nhl.js
  - apps/worker/src/schedulers/nba.js
  - apps/worker/src/schedulers/main.js
autonomous: true

must_haves:
  truths:
    - "nhl.js exports computeNhlDueJobs; emits availability sync, SOG sync, team stats, goalie starters, and NHL model jobs"
    - "nba.js exports computeNbaDueJobs; emits availability sync and NBA model jobs"
    - "main.js imports both; existing scheduler tests still pass; DRY_RUN output unchanged"
  artifacts:
    - path: apps/worker/src/schedulers/nhl.js
      provides: "NHL job registration"
      exports: [computeNhlDueJobs]
    - path: apps/worker/src/schedulers/nba.js
      provides: "NBA job registration"
      exports: [computeNbaDueJobs]
  key_links:
    - from: apps/worker/src/schedulers/nhl.js
      to: apps/worker/src/schedulers/windows.js
      via: "require('./windows')"
      pattern: "require.*windows"
    - from: apps/worker/src/schedulers/nba.js
      to: apps/worker/src/schedulers/windows.js
      via: "require('./windows')"
      pattern: "require.*windows"
    - from: apps/worker/src/schedulers/main.js
      to: apps/worker/src/schedulers/nhl.js
      via: "computeNhlDueJobs"
      pattern: "computeNhlDueJobs"
    - from: apps/worker/src/schedulers/main.js
      to: apps/worker/src/schedulers/nba.js
      via: "computeNbaDueJobs"
      pattern: "computeNbaDueJobs"
---

<objective>
Create the two largest per-sport sub-schedulers: schedulers/nhl.js and schedulers/nba.js.

NHL bundles 5 concerns: player availability sync (2.8), SOG player-ID sync (2.55),
team stats pull (2.6), goalie starters pre-fetch (2.9), and NHL model at fixed + T-minus
windows. NBA bundles 2: player availability sync (2.8b) and NBA model.

Both import from windows.js (Plan 01). Main.js gets the require() and commented-out
delegate calls — old sections remain active until Plan 03 deletes them.

Purpose: After this plan the two largest extraction targets are file-complete. Plan 03
only needs to handle MLB + settlement + the final slimming pass.

Output: nhl.js, nba.js — each a self-contained module; main.js updated with imports.
</objective>

<execution_context>
@./.claude/process-acceleration-executors/workflows/execute-plan.md
@./.claude/process-acceleration-executors/templates/summary.md
</execution_context>

<context>
@.planning/phases/WI-0780/WI-0780-01-SUMMARY.md
@apps/worker/src/schedulers/main.js
@apps/worker/src/schedulers/windows.js
@apps/worker/src/schedulers/player-props.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create schedulers/nhl.js</name>
  <files>
    apps/worker/src/schedulers/nhl.js
    apps/worker/src/schedulers/main.js
  </files>
  <action>
--- CREATE apps/worker/src/schedulers/nhl.js ---

Extracts from main.js sections: NHL SOG PLAYER ID SYNC (2.55), NHL TEAM STATS (2.6),
NHL PLAYER AVAILABILITY SYNC (2.8), NHL GOALIE STARTERS PRE-FETCH (2.9), and the
NHL-specific job registrations from MODELS (3).

Interface:
  computeNhlDueJobs(nowEt, {
    nowUtc, games, dryRun, quotaTier,
    maybeQueueTeamMetricsRefresh,
    claimTminusPullSlot,
    pullOddsHourly,
    ENABLE_WITHOUT_ODDS_MODE,
  })

Imports:
  const { isFixedDue, keyFixed, keyTminus, dueTminusMinutes,
          keyNhlPlayerAvailabilitySync, keyNhlGoalieStarters,
          keyNhlSogPlayerSync, keyNhlTeamStats } = require('./windows');
  const { runNHLModel } = require('../jobs/run_nhl_model');
  const { syncNhlPlayerAvailability } = require('../jobs/sync_nhl_player_availability');
  const { pullNhlGoalieStarters } = require('../jobs/pull_nhl_goalie_starters');
  const { syncNhlSogPlayerIds } = require('../jobs/sync_nhl_sog_player_ids');
  const { pullNhlTeamStats } = require('../jobs/pull_nhl_team_stats');

Body (copy sections verbatim from main.js, replacing local function refs with imports):

  1. Guard: if (process.env.ENABLE_NHL_MODEL === 'false' &&
                process.env.ENABLE_NHL_PLAYER_AVAILABILITY_SYNC === 'false' &&
                process.env.ENABLE_NHL_GOALIE_STARTERS === 'false' &&
                !process.env.ENABLE_NHL_SOG_PLAYER_SYNC) return [];

  2. Section 2.55 — NHL SOG PLAYER ID SYNC:
     Copy the block from main.js section "NHL SOG PLAYER ID SYNC (2.55)" verbatim.
     Gate: process.env.ENABLE_NHL_SOG_PLAYER_SYNC === 'true'

  3. Section 2.6 — NHL TEAM STATS:
     Copy the block from main.js section "NHL TEAM STATS (2.6)" verbatim.
     Gate: existing env check in the block.

  4. Section 2.8 — NHL PLAYER AVAILABILITY SYNC:
     Copy the block from main.js section "NHL PLAYER AVAILABILITY SYNC (2.8)" verbatim.
     Gate: ENABLE_NHL_PLAYER_AVAILABILITY_SYNC !== 'false'

  5. Section 2.9 — NHL GOALIE STARTERS PRE-FETCH:
     Copy the block from main.js section "NHL GOALIE STARTERS PRE-FETCH (2.9)" verbatim.
     Gate: ENABLE_NHL_GOALIE_STARTERS !== 'false'

  6. NHL model — fixed-time (09:00, 12:00 ET):
     Copy only the NHL-specific path from the SPORT_JOBS loop in section 3.
     Inline: sport = 'nhl', jobName = 'run_nhl_model', execute = runNHLModel

  7. NHL model — T-minus per game:
     Filter games to sport === 'nhl'. Copy the T-minus block from section 3
     (including pre-model odds pull with claimTminusPullSlot dedup guard).

  return jobs;
  module.exports = { computeNhlDueJobs };

--- UPDATE main.js ---
Add at the require block bottom:
  const { computeNhlDueJobs } = require('./nhl');

After the PLAYER PROPS SCHEDULER (8) call (near the end of computeDueJobs),
add a commented migration note:
  // WI-0780: computeNhlDueJobs wired here in Plan 03 after old sections removed
  // const nhlJobs = computeNhlDueJobs(nowEt, { nowUtc, games, dryRun, quotaTier,
  //   maybeQueueTeamMetricsRefresh, claimTminusPullSlot, pullOddsHourly, ENABLE_WITHOUT_ODDS_MODE });
  // jobs.push(...nhlJobs);
  </action>
  <verify>
node --check apps/worker/src/schedulers/nhl.js && echo "nhl OK"
node --check apps/worker/src/schedulers/main.js && echo "main OK"
npm --prefix apps/worker run test -- --testPathPattern scheduler 2>&1 | tail -10
  </verify>
  <done>nhl.js exports computeNhlDueJobs; syntax clean; scheduler tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Create schedulers/nba.js</name>
  <files>
    apps/worker/src/schedulers/nba.js
    apps/worker/src/schedulers/main.js
  </files>
  <action>
--- CREATE apps/worker/src/schedulers/nba.js ---

Extracts from main.js: NBA PLAYER AVAILABILITY SYNC (2.8b) and NBA model registrations
from MODELS (3).

Interface:
  computeNbaDueJobs(nowEt, {
    nowUtc, games, dryRun, quotaTier,
    maybeQueueTeamMetricsRefresh,
    claimTminusPullSlot,
    pullOddsHourly,
    ENABLE_WITHOUT_ODDS_MODE,
  })

Imports:
  const { isFixedDue, keyFixed, keyTminus, dueTminusMinutes,
          keyNbaPlayerAvailabilitySync } = require('./windows');
  const { runNBAModel } = require('../jobs/run_nba_model');
  const { syncNbaPlayerAvailability } = require('../jobs/sync_nba_player_availability');

Body:
  1. Guard: if (process.env.ENABLE_NBA_MODEL === 'false' &&
                process.env.ENABLE_NBA_PLAYER_AVAILABILITY_SYNC === 'false') return [];

  2. Section 2.8b — NBA PLAYER AVAILABILITY SYNC:
     Copy the block from main.js section "NBA PLAYER AVAILABILITY SYNC (2.8b)" verbatim.
     Gate: ENABLE_NBA_PLAYER_AVAILABILITY_SYNC !== 'false'

  3. NBA model — fixed-time (09:00, 12:00 ET):
     Copy only NBA-specific path from the SPORT_JOBS loop in section 3.
     Inline: sport = 'nba', jobName = 'run_nba_model', execute = runNBAModel

  4. NBA model — T-minus per game:
     Filter games to sport === 'nba'. Copy the T-minus block from section 3
     including the pre-model odds pull with claimTminusPullSlot guard.

  return jobs;
  module.exports = { computeNbaDueJobs };

--- UPDATE main.js ---
Add at the require block bottom:
  const { computeNbaDueJobs } = require('./nba');

Add commented migration note in computeDueJobs (same position as nhl comment):
  // WI-0780: computeNbaDueJobs wired here in Plan 03 after old sections removed
  // const nbaJobs = computeNbaDueJobs(nowEt, { ... });
  // jobs.push(...nbaJobs);
  </action>
  <verify>
node --check apps/worker/src/schedulers/nba.js && echo "nba OK"
node --check apps/worker/src/schedulers/main.js && echo "main OK"
npm --prefix apps/worker run test -- --testPathPattern scheduler 2>&1 | tail -10
  </verify>
  <done>nba.js exports computeNbaDueJobs; syntax clean; scheduler tests pass</done>
</task>

</tasks>

<verification>
node --check apps/worker/src/schedulers/nhl.js
node --check apps/worker/src/schedulers/nba.js
node --check apps/worker/src/schedulers/main.js
npm --prefix apps/worker run test:scheduler:windows 2>&1 | tail -5
</verification>

<success_criteria>
- nhl.js and nba.js exist, each exporting their compute*DueJobs function
- main.js imports both (with migration calls still commented)
- All scheduler tests pass
- DRY_RUN output from main.js unchanged (old sections still active)
</success_criteria>

<output>
After completion, create `.planning/phases/WI-0780/WI-0780-02-SUMMARY.md`
</output>

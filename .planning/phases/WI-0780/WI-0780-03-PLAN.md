---
phase: WI-0780-scheduler-decompose
plan: 03
type: execute
wave: 3
depends_on:
  - WI-0780-02
files_modified:
  - apps/worker/src/schedulers/mlb.js
  - apps/worker/src/schedulers/settlement.js
  - apps/worker/src/schedulers/main.js
autonomous: true

must_haves:
  truths:
    - "mlb.js exports computeMlbDueJobs; emits MLB ESPN-direct seeding and MLB model jobs"
    - "settlement.js exports computeSettlementDueJobs; emits settlement health report, public/vsin splits, hourly/nightly settlement chain, and MLB F5 settlement"
    - "main.js wires all 5 sub-schedulers via uncommented calls; old duplicated sections removed"
    - "wc -l apps/worker/src/schedulers/main.js is under 300"
    - "npm run test:scheduler:windows passes; DRY_RUN job list matches pre-refactor baseline"
  artifacts:
    - path: apps/worker/src/schedulers/mlb.js
      provides: "MLB job registration"
      exports: [computeMlbDueJobs]
    - path: apps/worker/src/schedulers/settlement.js
      provides: "Settlement and reporting job registration"
      exports: [computeSettlementDueJobs]
  key_links:
    - from: apps/worker/src/schedulers/mlb.js
      to: apps/worker/src/schedulers/windows.js
      via: "require('./windows')"
      pattern: "require.*windows"
    - from: apps/worker/src/schedulers/settlement.js
      to: apps/worker/src/schedulers/windows.js
      via: "require('./windows')"
      pattern: "require.*windows"
    - from: apps/worker/src/schedulers/main.js
      to: apps/worker/src/schedulers/settlement.js
      via: "computeSettlementDueJobs"
      pattern: "computeSettlementDueJobs"
---

<objective>
Complete the decomposition: create mlb.js and settlement.js, then perform the final
slimming pass on main.js — removing all the now-extracted sections, uncommenting the
delegate calls to all 5 sub-schedulers, and verifying the 300-line acceptance criterion.

MLB owns: ESPN-direct seeding (section 2B) and MLB model at fixed + T-minus windows.
Settlement is the largest module: settlement health report (2.65), public splits (2.7),
VSin splits (2.75), hourly/nightly settlement chain (4), and MLB F5 settlement (4C).
Health watchdogs 5 and 6, FPL (7), and player-props (8) stay in main.js (they are
already sub-schedulers or trivial call sites).

The slimming pass also deletes the now-redundant helper functions from main.js that were
copied to windows.js in Plan 01 and never removed: all key* functions and window
predicates. getPipelineHealthJobs and getOddsHealthJobs (sections 5/6) stay in main.js
since they are wired directly into computeDueJobs.

Purpose: Achieves the WI acceptance criterion — main.js under 300 lines, all sub-scheduler
modules wired, DRY_RUN output identical to pre-refactor.

Output: mlb.js, settlement.js, slimmed main.js. Full test suite green.
</objective>

<execution_context>
@./.claude/process-acceleration-executors/workflows/execute-plan.md
@./.claude/process-acceleration-executors/templates/summary.md
</execution_context>

<context>
@.planning/phases/WI-0780/WI-0780-02-SUMMARY.md
@apps/worker/src/schedulers/main.js
@apps/worker/src/schedulers/windows.js
@apps/worker/src/schedulers/player-props.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create schedulers/mlb.js and schedulers/settlement.js</name>
  <files>
    apps/worker/src/schedulers/mlb.js
    apps/worker/src/schedulers/settlement.js
    apps/worker/src/schedulers/main.js
  </files>
  <action>
--- CREATE apps/worker/src/schedulers/mlb.js ---

Extracts: MLB ESPN-DIRECT SEEDING (2B) and MLB model registrations from MODELS (3).

Interface:
  computeMlbDueJobs(nowEt, {
    nowUtc, games, dryRun, quotaTier,
    maybeQueueTeamMetricsRefresh,
    claimTminusPullSlot,
    pullOddsHourly,
    ENABLE_WITHOUT_ODDS_MODE,
  })

Imports:
  const { isFixedDue, keyFixed, keyTminus, dueTminusMinutes,
          keyEspnGamesDirect } = require('./windows');
  const { runMLBModel } = require('../jobs/run_mlb_model');
  const { pullEspnGamesDirect } = require('../jobs/pull_espn_games_direct');

Body:
  1. Guard: if (process.env.ENABLE_MLB_MODEL === 'false' &&
                process.env.ENABLE_ESPN_DIRECT !== 'true') return [];
     (Check the actual env-var name used in main.js for ESPN-direct gate)

  2. Section 2B — MLB ESPN-DIRECT SEEDING:
     Copy the block from main.js section "MLB ESPN-DIRECT SEEDING (2B)" verbatim.
     Preserve all existing env and timing gates.

  3. MLB model — fixed-time (09:00, 12:00 ET):
     Inline: sport = 'mlb', jobName = 'run_mlb_model', execute = runMLBModel
     Include maybeQueueTeamMetricsRefresh call.

  4. MLB model — T-minus per game:
     Filter games to sport === 'mlb'. Copy the T-minus block from section 3
     including the pre-model odds pull with claimTminusPullSlot guard.

  return jobs;
  module.exports = { computeMlbDueJobs };

--- CREATE apps/worker/src/schedulers/settlement.js ---

Extracts: SETTLEMENT HEALTH REPORT (2.65), PUBLIC SPLITS (2.7), VSIN/DK SPLITS (2.75),
SETTLEMENT (4), MLB F5 SETTLEMENT (4C).

Interface:
  computeSettlementDueJobs(nowEt, {
    nowUtc, games, dryRun,
    ENABLE_WITHOUT_ODDS_MODE,
    hasRunningJobName,
    isNightlySettlementOwningHourlyWindow,   // already imported by caller OR re-import from windows
  })

Imports:
  const { isFixedDue, keyFixed,
          keySettlementHealthReport, keyHourlySettlementSweep,
          keyHourlySettlementJob, keyNightlySettlementJob, keyNightlySweep,
          keyPublicSplits, keyVsinSplits,
          isHourlySettlementDue, isNightlySettlementOwningHourlyWindow } = require('./windows');
  const { hasRunningJobName } = require('@cheddar-logic/data');
  const { settleGameResults } = require('../jobs/settle_game_results');
  const { settleProjections } = require('../jobs/settle_projections');
  const { settlePendingCards } = require('../jobs/settle_pending_cards');
  const { syncGameStatuses } = require('../jobs/sync_game_statuses');
  const { backfillCardResults } = require('../jobs/backfill_card_results');
  const { settleMlbF5 } = require('../jobs/settle_mlb_f5');
  const { run: generateSettlementHealthReport } = require('../jobs/report_settlement_health');
  const { runPullPublicSplits } = require('../jobs/pull_public_splits');
  const { runPullVsinSplits } = require('../jobs/pull_vsin_splits');

Body: Copy sections 2.65, 2.7, 2.75, 4, and 4C verbatim from main.js.
  Early return if ENABLE_WITHOUT_ODDS_MODE is true (same gate already in section 4).
  The ENABLE_WITHOUT_ODDS_MODE check for settlement health report (2.65) and splits
  (2.7, 2.75) — check main.js for whether these sections also have that gate; if not,
  still include them in the return when ENABLE_WITHOUT_ODDS_MODE is true.

  module.exports = { computeSettlementDueJobs };

--- UPDATE main.js ---
Add at the require block bottom:
  const { computeMlbDueJobs } = require('./mlb');
  const { computeSettlementDueJobs } = require('./settlement');

Add commented migration notes (same pattern as nhl/nba in Plan 02), to be uncommented
in Task 2 of this plan.
  </action>
  <verify>
node --check apps/worker/src/schedulers/mlb.js && echo "mlb OK"
node --check apps/worker/src/schedulers/settlement.js && echo "settlement OK"
node --check apps/worker/src/schedulers/main.js && echo "main OK"
npm --prefix apps/worker run test -- --testPathPattern scheduler 2>&1 | tail -10
  </verify>
  <done>mlb.js and settlement.js exist and syntax-check clean; main.js imports both; scheduler tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Slim main.js — remove extracted sections, uncomment delegate calls, verify &lt;300 lines</name>
  <files>apps/worker/src/schedulers/main.js</files>
  <action>
This is the final slimming pass. Perform in this order to avoid reference errors:

STEP 1 — Uncomment all 5 sub-scheduler delegate calls:
  Uncomment the calls added (commented) in Plans 01, 02, and this plan's Task 1:
    computeNflDueJobs, computeNhlDueJobs, computeNbaDueJobs,
    computeMlbDueJobs, computeSettlementDueJobs
  Place all five calls in computeDueJobs just before the FPL (7) call, in this order:
    const nhlJobs = computeNhlDueJobs(...);  jobs.push(...nhlJobs);
    const nbaJobs = computeNbaDueJobs(...);  jobs.push(...nbaJobs);
    const mlbJobs = computeMlbDueJobs(...);  jobs.push(...mlbJobs);
    const nflJobs = computeNflDueJobs(...);  jobs.push(...nflJobs);
    const settlementJobs = computeSettlementDueJobs(...);  jobs.push(...settlementJobs);

STEP 2 — Delete now-redundant sections from computeDueJobs:
  Remove the following sections (leave section markers as one-line comments if helpful):
    - MLB ESPN-DIRECT SEEDING (2B)
    - NHL SOG PLAYER ID SYNC (2.55)
    - NHL TEAM STATS (2.6)
    - SETTLEMENT HEALTH REPORT (2.65)
    - PUBLIC SPLITS (2.7)
    - VSIN/DK SPLITS (2.75)
    - NHL PLAYER AVAILABILITY SYNC (2.8)
    - NBA PLAYER AVAILABILITY SYNC (2.8b)
    - NHL GOALIE STARTERS PRE-FETCH (2.9)
    - MODELS (3) — both the Discord snapshot block AND the SPORT_JOBS loop and T-minus loop
    - SETTLEMENT (4), MLB F5 SETTLEMENT (4C),
      PROJECTION ACTUAL RESULT INGESTION (4D comment block)
  Keep:
    - SCHEDULES (1)
    - SCHEDULE PULLS (1) — schedule refresh for NBA/NHL
    - INGESTION (2) — syncGameStatuses is cross-sport
    - ODDS (2) — hourly odds pull loop
    - TEAM METRICS CACHE (2.5)
    - HEALTH WATCHDOG (5) — getPipelineHealthJobs call
    - ODDS HEALTH WATCHDOG (6) — checkOddsHealth / getOddsHealthJobs call
    - FPL (7) and PLAYER PROPS (8)
    - The 5 new sub-scheduler delegate calls

STEP 3 — Delete now-exported helper functions from main.js top:
  Delete the function bodies for every function that was copied to windows.js in Plan 01:
    keyEspnGamesDirect, keyOddsHourly, keyFixed, keyDiscordCardsSnapshot, keyTminus,
    keyNightlySweep, keyNhlPlayerAvailabilitySync, keyNbaPlayerAvailabilitySync,
    keyNhlGoalieStarters, keyNhlSogPlayerSync, keyNhlTeamStats, keyPullScheduleNba,
    keyPullScheduleNhl, keySettlementHealthReport, keyHourlySettlementSweep,
    keyPublicSplits, keyVsinSplits, keyHourlySettlementJob, keyNightlySettlementJob,
    isHourlySettlementDue, isNightlySettlementOwningHourlyWindow,
    getOddsIntervalMinutes, getScheduleRefreshDue, shouldRefreshOddsForGame,
    isFixedDue, dueTminusMinutes
  Add a barrel import from windows.js instead:
    const { isFixedDue, keyFixed, keyTminus, keyOddsHourly, keyEspnGamesDirect,
            keyNightlySweep, keyPullScheduleNba, keyPullScheduleNhl,
            getOddsIntervalMinutes, getScheduleRefreshDue, shouldRefreshOddsForGame,
            ...etc } = require('./windows');

STEP 4 — Clean up sport imports that are now owned by sub-schedulers:
  Remove job imports that are no longer referenced in main.js after extraction:
    runNHLModel, runNBAModel, runMLBModel, runNFLModel — now imported only in sub-schedulers
    syncNhlPlayerAvailability, syncNbaPlayerAvailability, pullNhlGoalieStarters,
    syncNhlSogPlayerIds, pullNhlTeamStats, settleGameResults, settleProjections,
    settlePendingCards, syncGameStatuses, backfillCardResults, settleMlbF5,
    generateSettlementHealthReport, runPullPublicSplits, runPullVsinSplits,
    pullEspnGamesDirect (if not referenced in the kept ingestion section)
  IMPORTANT: Only remove imports that are no longer referenced in main.js. Verify with
  grep before deleting. pullOddsHourly MUST stay — it's passed as a context param to sub-schedulers.

STEP 5 — node --check + wc -l guard:
  Run: node --check apps/worker/src/schedulers/main.js && wc -l apps/worker/src/schedulers/main.js
  If wc -l > 300, identify remaining removable noise and continue trimming.
  If something fails: revert only the last deletion and re-check.

STEP 6 — DRY_RUN baseline comparison:
  DRY_RUN=true node apps/worker/src/schedulers/main.js 2>&1 | grep -E "due|Queuing|added" | sort > /tmp/after.txt
  Compare against a pre-refactor baseline captured before this task:
    DRY_RUN=true git stash && node apps/worker/src/schedulers/main.js 2>&1 | grep -E "due|Queuing|added" | sort > /tmp/before.txt && git stash pop
  diff /tmp/before.txt /tmp/after.txt   # should be empty

STEP 7 — Full test suite:
  npm --prefix apps/worker run test 2>&1 | tail -15
  </action>
  <verify>
node --check apps/worker/src/schedulers/main.js
wc -l apps/worker/src/schedulers/main.js   # must be &lt;300
npm --prefix apps/worker run test:scheduler:windows 2>&1 | tail -5
npm --prefix apps/worker run test 2>&1 | tail -15
  </verify>
  <done>
main.js under 300 lines; node --check passes; all tests green; DRY_RUN output
matches pre-refactor baseline (same jobs fire at same windows).
  </done>
</task>

</tasks>

<verification>
node --check apps/worker/src/schedulers/mlb.js
node --check apps/worker/src/schedulers/settlement.js
node --check apps/worker/src/schedulers/main.js
wc -l apps/worker/src/schedulers/main.js     # must be &lt;300
npm --prefix apps/worker run test:scheduler:windows 2>&1 | tail -5
npm --prefix apps/worker run test 2>&1 | tail -10
DRY_RUN=true node apps/worker/src/schedulers/main.js 2>&1 | grep -c "due\|Queuing"  # count non-zero
</verification>

<success_criteria>
- mlb.js and settlement.js each export their compute*DueJobs function
- wc -l main.js is under 300
- npm run test:scheduler:windows: all pass
- npm run test (full suite): no regressions vs pre-refactor baseline
- DRY_RUN output: same jobs at same windows as before
</success_criteria>

<output>
After completion, create `.planning/phases/WI-0780/WI-0780-03-SUMMARY.md`
</output>

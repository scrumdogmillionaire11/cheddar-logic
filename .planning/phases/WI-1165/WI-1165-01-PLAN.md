---
phase: WI-1165
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/worker/src/jobs/potd/run_potd_engine.js
  - apps/worker/src/jobs/potd/__tests__/run-potd-engine.test.js
  - apps/worker/src/jobs/potd/__tests__/settlement-mirror.test.js
autonomous: true
requirements:
  - WI-1165-POOL-01
  - WI-1165-NOM-01
  - WI-1165-REG-01
must_haves:
  truths:
    - "Near-miss shadow writes are sourced from the full eligible post-gate, post-dedupe shadow pool rather than the reduced ranked nominee list"
    - "Official nominee persistence still uses the ranked nominee path and preserves one-per-sport behavior separate from shadow-candidate capture"
    - "FIRED and all supported NO_PICK terminal paths capture near misses from the same shadow-pool contract"
  artifacts:
    - path: "apps/worker/src/jobs/potd/run_potd_engine.js"
      provides: "POTD shadow-pool derivation and terminal-branch write wiring"
    - path: "apps/worker/src/jobs/potd/__tests__/run-potd-engine.test.js"
      provides: "Regression coverage for fired and no-pick near-miss capture semantics"
    - path: "apps/worker/src/jobs/potd/__tests__/settlement-mirror.test.js"
      provides: "Regression guard that nominee persistence remains separate from shadow-candidate capture side effects"
  key_links:
    - from: "apps/worker/src/jobs/potd/run_potd_engine.js"
      to: "apps/worker/src/jobs/potd/__tests__/run-potd-engine.test.js"
      via: "branch-level regression tests for shadowCandidatePool-driven writes"
      pattern: "shadowCandidatePool|candidatePool: shadowCandidatePool|confidence_below_high_gate|zero_wager|stake_below_minimum"
    - from: "apps/worker/src/jobs/potd/run_potd_engine.js"
      to: "apps/worker/src/jobs/potd/__tests__/settlement-mirror.test.js"
      via: "persistence contract checks that official nominees remain independent from shadow-candidate logging"
      pattern: "writeNominees|winnerStatus: 'FIRED'|winnerStatus: 'NO_PICK'"
---

<objective>
Lock WI-1165 to the actual POTD behavior change: near-miss shadow candidates must be captured from the full eligible daily pool after the WI-1153 best-edge-per-market/match dedupe, while official nominee persistence stays on the existing ranked nominee path.

Purpose: Prevent a false fix where call sites pass `shadowCandidatePool` by name but the pool semantics still collapse to nominee-reduced candidates or diverge across FIRED and NO_PICK branches.
Output: Explicit shadow-pool contract in engine flow + branch-level regression coverage proving nominee separation.
</objective>

<execution_context>
@/Users/ajcolubiale/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ajcolubiale/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@WORK_QUEUE/WI-1165.md
@WORK_QUEUE/COMPLETE/WI-1153.md
@apps/worker/src/jobs/potd/run_potd_engine.js
@apps/worker/src/jobs/potd/__tests__/run-potd-engine.test.js
@apps/worker/src/jobs/potd/__tests__/settlement-mirror.test.js

<interfaces>
From apps/worker/src/jobs/potd/run_potd_engine.js:
```javascript
const bestEdgeSelectorPool = selectBestEdgeByShadowGroup(fireableSelectorPool);
const shadowCandidatePool = bestEdgeSelectorPool;
const fireableNominees = selectTopPlaysFn(bestEdgeSelectorPool, {
  minConfidence: POTD_MIN_TOTAL_SCORE,
  minEdgePct: 0,
  maxNominees: POTD_MAX_NOMINEES,
  requirePositiveEdge: true,
});
const rankedNominees = fireableNominees;
```

Relevant write sites to preserve as separate contracts:
```javascript
writeNominees(db, { playDate, capturedAt: nowIso, winnerStatus: 'FIRED', nominees: rankedNominees });
writeShadowCandidates(db, {
  playDate,
  capturedAt: nowIso,
  minEdgePct: POTD_MIN_EDGE,
  candidates: selectNearMissShadowCandidates({
    winnerStatus: 'FIRED',
    candidatePool: shadowCandidatePool,
    winnerCandidate: bestCandidate,
  }),
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the shadow-candidate pool contract to the full eligible post-gate, post-dedupe pool</name>
  <files>apps/worker/src/jobs/potd/run_potd_engine.js, apps/worker/src/jobs/potd/__tests__/run-potd-engine.test.js</files>
  <behavior>
    - Test 1: A fired NBA-only day can persist one official winner and three same-sport near misses drawn from the deduped shadow pool, even when the ranked nominee path is reduced separately.
    - Test 2: NO_PICK terminal branches (`no_viable_candidates`, `confidence_below_high_gate`, `zero_wager`, `stake_below_minimum`) all source near misses from the same shadow pool contract.
  </behavior>
  <action>In `run_potd_engine.js`, keep the WI-1153 prerequisite intact: `shadowCandidatePool` must remain the best-edge-per-market/match deduped pool produced from model-backed, positive-edge, post-noise-floor, post-score-gate candidates. Do not redefine near misses from `rankedNominees`, do not add one-per-sport reduction to shadow capture, and do not change official play selection policy. Add or update focused tests that fail if same-sport eligible rows disappear from shadow capture after nominee reduction.</action>
  <verify>
    <automated>npm --prefix apps/worker run test -- --runInBand src/jobs/potd/__tests__/run-potd-engine.test.js</automated>
  </verify>
  <done>The engine exposes one shadow-pool contract reused across fired and no-pick paths, and regression coverage proves nominee reduction does not collapse the near-miss pool.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve official nominee persistence as a separate one-per-sport contract</name>
  <files>apps/worker/src/jobs/potd/run_potd_engine.js, apps/worker/src/jobs/potd/__tests__/run-potd-engine.test.js, apps/worker/src/jobs/potd/__tests__/settlement-mirror.test.js</files>
  <action>Keep `writeNominees(...)` and Discord-facing nominee usage on `rankedNominees` only, preserving the existing official nominee path and one-per-sport reduction semantics. Add assertions that official nominee persistence does not expand when shadow capture expands, and that fired-day near misses can include same-sport non-winning market/match rows after excluding the winner’s group. Avoid any schema changes, scheduler changes, or policy changes to scoring, thresholds, or stake sizing.</action>
  <verify>
    <automated>npm --prefix apps/worker run test -- --runInBand src/jobs/potd/__tests__/settlement-mirror.test.js</automated>
  </verify>
  <done>Nominee persistence remains independent from shadow-candidate capture, and same-sport near misses are allowed without regressing the official nominee contract.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the exact WI acceptance contract with narrow branch-level regressions</name>
  <files>apps/worker/src/jobs/potd/__tests__/run-potd-engine.test.js</files>
  <action>Add explicit branch-level assertions for the acceptance bullets in `WORK_QUEUE/WI-1165.md`: fired-day same-sport near misses, no-pick branch parity, near-miss cap staying in the requested 3-5 range with default 3, and separation between official nominees and near-miss writes. Use fixtures that would still pass if call sites merely forwarded `shadowCandidatePool` by name but fail unless the pool contents are the full eligible deduped set.</action>
  <verify>
    <automated>npm --prefix apps/worker test -- --runInBand</automated>
  </verify>
  <done>Worker regressions directly prove the intended pool composition and branch parity, not just variable plumbing.</done>
</task>

</tasks>

<verification>
Run all WI-required checks from repo root:
- `npm --prefix apps/worker run test -- --runInBand src/jobs/potd/__tests__/run-potd-engine.test.js`
- `npm --prefix apps/worker run test -- --runInBand src/jobs/potd/__tests__/settlement-mirror.test.js`
- `npm --prefix apps/worker test -- --runInBand`

Manual validation:
- Inspect near-miss writes in `run_potd_engine.js` and confirm all terminal branches call `selectNearMissShadowCandidates` with `candidatePool: shadowCandidatePool`, while nominee persistence continues to call `writeNominees(... rankedNominees)`.
- Run a fixture set where one sport has more than one eligible post-dedupe candidate and confirm the fired result logs the official winner plus three same-sport near misses without expanding the official nominee set.
</verification>

<success_criteria>
- Near-miss shadow writes are derived from the full eligible, post-gate, post-dedupe shadow pool rather than the reduced official nominee list.
- Official POTD selection and nominee persistence remain on the existing ranked nominee path with one-per-sport behavior intact.
- FIRED and all supported NO_PICK terminal paths share the same shadow-pool semantics.
- Regression tests prove pool composition and nominee separation directly.
</success_criteria>

<output>
After completion, create `.planning/phases/WI-1165/WI-1165-01-SUMMARY.md`
</output>

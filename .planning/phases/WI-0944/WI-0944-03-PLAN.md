---
phase: WI-0944
plan: "03"
type: execute
wave: 3
depends_on:
  - WI-0944-01
  - WI-0944-02
files_modified:
  - apps/worker/src/models/mlb-model.js
  - apps/worker/src/models/__tests__/mlb-model.test.js
  - apps/worker/src/jobs/run_mlb_model.js
  - apps/worker/src/jobs/__tests__/run_mlb_model.test.js
autonomous: true
requirements:
  - WI-0944-ML-01
  - WI-0944-GATE-01
must_haves:
  truths:
    - "Full-game MLB moneyline cards can surface as odds-backed official plays when the model edge is at least 0.025 and the capped confidence gate is satisfied"
    - "Full-game moneyline uses a dedicated adjustment layer for bullpen asymmetry, home-inning edge, and late-leverage context rather than relying only on raw projected run differential"
    - "Fresh-priced full-game total and full-game moneyline cards survive the runner path and are inserted with odds-backed execution context"
    - "PASS->PLAY, PASS->PASS, and PLAY unchanged scenarios are covered for the moneyline path and for runner insertion with fresh odds"
  artifacts:
    - path: "apps/worker/src/models/mlb-model.js"
      provides: "Retuned full-game ML adjustment layer and threshold/confidence logic"
      contains: "projectFullGameML"
    - path: "apps/worker/src/jobs/run_mlb_model.js"
      provides: "Runner proof that fresh-priced full-game MLB cards remain executable and persist through write flow"
      contains: "mlb-full-game-ml"
    - path: "apps/worker/src/jobs/__tests__/run_mlb_model.test.js"
      provides: "Runner-level odds-backed insertion coverage for full-game total and full-game moneyline cards"
      min_lines: 12
  key_links:
    - from: "projectFullGameML"
      to: "driver_support"
      via: "bullpen asymmetry, home inning edge, and late leverage adjustments"
      pattern: "bullpen_asymmetry_adj|home_inning_edge_adj|late_leverage_adj"
    - from: "apps/worker/src/jobs/run_mlb_model.js"
      to: "deriveMlbExecutionEnvelope"
      via: "fresh-priced full-game markets stay EXECUTABLE and emit_allowed"
      pattern: "deriveMlbExecutionEnvelope"
---

<objective>
Retune MLB full-game moneyline and prove that odds-backed full-game MLB plays now surface through the runner with fresh pricing.

Purpose: the remaining blocker for surfacing these plays is over-suppression in the full-game ML model and the need to prove end-to-end that fresh-priced full-game candidates are still treated as odds-backed executable cards after the model changes.

Output: an adjusted full-game ML model with WI-0944 thresholds plus runner-level regression coverage showing that full-game totals and moneyline plays reach card insertion with fresh odds.
</objective>

<execution_context>
@.claude/process-acceleration-executors/workflows/execute-plan.md
@.claude/process-acceleration-executors/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/MONEYLINE_AUDIT_FULL_SYSTEM.md
@WORK_QUEUE/WI-0944.md
@apps/worker/src/models/mlb-model.js
@apps/worker/src/models/__tests__/mlb-model.test.js
@apps/worker/src/jobs/run_mlb_model.js
@apps/worker/src/jobs/__tests__/run_mlb_model.test.js
@.planning/phases/ime-01-independent-market-eval/ime-01-03-SUMMARY.md
</context>

<interfaces>
<!-- Current full-game ML controls to retune, not replace wholesale. -->

From apps/worker/src/models/mlb-model.js:
```javascript
const LEAN_EDGE_MIN = 0.025;
const CONFIDENCE_MIN = Math.min(
  6,
  Math.max(5, 5 + Math.round((variance.variance_multiplier - 1) * 2)),
);
const diffMean =
  runDiff +
  homeFieldRuns +
  bullpenAsymmetryAdj +
  homeInningEdgeAdj +
  lateLeverageAdj;
```

From apps/worker/src/jobs/run_mlb_model.js:
```javascript
const oddsBackedQualified = qualified.filter((d) =>
  resolveMlbTrustClass(resolveMlbMarketGroup(d)) === MLB_MARKET_TRUST_CLASS.ODDS_BACKED
);
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Retune full-game moneyline to the WI-0944 adjustment-layer contract</name>
  <files>apps/worker/src/models/mlb-model.js
apps/worker/src/models/__tests__/mlb-model.test.js</files>
  <behavior>
    - Test 1: A moneyline candidate can upgrade from PASS to PLAY when the dedicated adjustment layer lifts a real edge above 0.025 and the confidence cap is satisfied.
    - Test 2: A weak-support moneyline candidate remains PASS with explicit reason codes.
    - Test 3: An already-valid PLAY remains unchanged.
  </behavior>
  <action>
Update `projectFullGameML()` to match the WI-0944 acceptance contract:
- keep the separate adjustment layer for bullpen asymmetry, home-inning edge, and late leverage
- reduce the full-game ML edge threshold to `0.025`
- cap the confidence gate requirement at `6`
- collapse redundant post-projection hard gates so the moneyline path is controlled by a small, explicit set of fail conditions

Keep driver-support and reason-code output readable enough that the funnel from Plan 01 can attribute suppressors correctly.
  </action>
  <verify>
    <automated>npm --prefix apps/worker run test -- --runInBand src/models/__tests__/mlb-model.test.js</automated>
  </verify>
  <done>Full-game ML uses the dedicated adjustment layer, the WI-0944 threshold, and deterministic model regressions cover PASS->PLAY, PASS->PASS, and unchanged PLAY cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove fresh-priced full-game MLB cards surface through the runner as odds-backed writes</name>
  <files>apps/worker/src/jobs/run_mlb_model.js
apps/worker/src/jobs/__tests__/run_mlb_model.test.js</files>
  <behavior>
    - Test 1: Fresh-priced full_game_total and full_game_ml candidates remain EXECUTABLE and are inserted as card payloads.
    - Test 2: Projection-only candidates still do not masquerade as odds-backed cards.
    - Test 3: The runner preserves explicit reason codes for downgraded but surfaced full-game candidates.
  </behavior>
  <action>
Extend the existing MLB runner tests so they exercise the post-model write path with fresh odds-backed full-game candidates.

Assert the end-to-end conditions that matter for surfacing with odds:
- fresh pricing produces odds-backed execution envelopes
- `mlb-full-game` and `mlb-full-game-ml` survive to insertCardPayload
- downgraded cards preserve reason-code visibility instead of being silently converted or skipped

Only touch production runner code if the tests expose a real write-path mismatch; do not refactor unrelated MLB runner areas.
  </action>
  <verify>
    <automated>npm --prefix apps/worker run test -- --runInBand src/jobs/__tests__/run_mlb_model.test.js</automated>
  </verify>
  <done>Runner-level tests prove fresh-priced full-game MLB totals and moneyline cards surface through the odds-backed write path.</done>
</task>

</tasks>

<verification>
Run:
- `node --check apps/worker/src/models/mlb-model.js`
- `node --check apps/worker/src/jobs/run_mlb_model.js`
- `npm --prefix apps/worker run test -- --runInBand src/models/__tests__/mlb-model.test.js`
- `npm --prefix apps/worker run test -- --runInBand src/jobs/__tests__/run_mlb_model.test.js`

Pass condition: the model and runner tests together prove that odds-backed full-game totals and moneyline plays can surface when their edge survives the simplified WI-0944 gate path.
</verification>

<success_criteria>
- Full-game ML is no longer over-suppressed relative to its actual modeled edge.
- Fresh-priced full-game MLB totals and moneyline cards can reach persistence as odds-backed cards.
- End-to-end regression coverage prevents the old silent non-surfacing behavior from returning.
</success_criteria>

<output>
After completion, create `.planning/phases/WI-0944/WI-0944-03-SUMMARY.md`
</output>

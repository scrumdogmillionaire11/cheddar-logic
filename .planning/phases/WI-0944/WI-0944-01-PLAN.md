---
phase: WI-0944
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/worker/src/jobs/run_mlb_model.js
  - apps/worker/src/jobs/__tests__/run_mlb_model.test.js
autonomous: true
requirements:
  - WI-0944-OBS-01
  - WI-0944-OBS-02
must_haves:
  truths:
    - "The worker emits a deterministic MLB full-game suppression funnel for the last 50 full-game candidates, with stage counts and percentages for totals and moneyline candidates"
    - "The funnel identifies the top two suppressors from actual candidate drops and ties each suppressor to an exact condition or reason-code bucket"
    - "No full-game candidate can disappear from the runner without entering the funnel accounting output"
  artifacts:
    - path: "apps/worker/src/jobs/run_mlb_model.js"
      provides: "Deterministic full-game funnel sampler, stage accounting, and suppressor summary logging for MLB full-game markets"
      contains: "buildMlbFullGameSuppressionFunnelReport"
    - path: "apps/worker/src/jobs/__tests__/run_mlb_model.test.js"
      provides: "Runner-level coverage proving deterministic funnel stage counts and suppressor ordering"
      min_lines: 12
  key_links:
    - from: "apps/worker/src/jobs/run_mlb_model.js"
      to: "evaluateMlbFullGameFunnelCandidate"
      via: "per-driver sampling for full_game_total and full_game_ml candidates before write"
      pattern: "evaluateMlbFullGameFunnelCandidate"
    - from: "apps/worker/src/jobs/run_mlb_model.js"
      to: "buildMlbFullGameSuppressionFunnelReport"
      via: "windowed report emitted after candidate evaluation"
      pattern: "buildMlbFullGameSuppressionFunnelReport"
---

<objective>
Install a deterministic observability layer for MLB full-game suppression before retuning thresholds.

Purpose: the current runner already has partial funnel helpers, but WI-0944 needs an operator-usable report that shows exactly where full-game totals and moneyline candidates die. Without that accounting, threshold changes are guesswork and regressions stay silent.

Output: a stable N=50 full-game funnel report in the MLB runner plus tests that lock the report format, stage counts, and suppressor ranking.
</objective>

<execution_context>
@.claude/process-acceleration-executors/workflows/execute-plan.md
@.claude/process-acceleration-executors/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/MONEYLINE_AUDIT_FULL_SYSTEM.md
@WORK_QUEUE/WI-0944.md
@WORK_QUEUE/WI-0943.md
@apps/worker/src/jobs/run_mlb_model.js
@apps/worker/src/jobs/__tests__/run_mlb_model.test.js
@.planning/phases/ime-01-independent-market-eval/ime-01-03-SUMMARY.md
</context>

<interfaces>
<!-- Existing runner hooks relevant to this plan. -->

From apps/worker/src/jobs/run_mlb_model.js:
```javascript
const officialFullGameMarkets = new Set(
  (Array.isArray(gameEval.official_plays) ? gameEval.official_plays : [])
    .map((play) => String(play?.market_type || '').toUpperCase()),
);
for (const driver of gameDriverCards) {
  const marketKey = getMlbFullGameMarketKey(driver);
  if (!marketKey) continue;
  mlbFullGameFunnelSamples.push(
    evaluateMlbFullGameFunnelCandidate(
      driver,
      officialFullGameMarkets.has(marketKey),
    ),
  );
}
```

Current acceptance stages required by WI-0944:
```text
total_candidates_created
passed_projection
passed_edge_threshold
passed_volatility_threshold
passed_driver_support
passed_f5_contradiction
passed_confidence
final_official_plays
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Finish and normalize the MLB full-game funnel contract in the runner</name>
  <files>apps/worker/src/jobs/run_mlb_model.js
apps/worker/src/jobs/__tests__/run_mlb_model.test.js</files>
  <behavior>
    - Test 1: A mixed sample of full_game_total and full_game_ml candidates produces the exact required stage keys and deterministic counts.
    - Test 2: Stage percentages are reproducible from the same sample and ordered consistently.
    - Test 3: The top two suppressors are derived from actual drop buckets, not hardcoded labels.
  </behavior>
  <action>
Audit the existing helper set in `run_mlb_model.js` (`getMlbFullGameMarketKey`, `evaluateMlbFullGameFunnelCandidate`, `buildMlbFullGameSuppressionFunnelReport`) and convert it into the exact WI-0944 funnel contract.

Keep the scope tight to the runner:
- use only `full_game_total` and `full_game_ml` candidates
- emit the required stages in order
- include both raw counts and percentages
- derive suppressor ranking from observed drops over the last N=50 samples
- preserve existing reason-code vocabulary where possible instead of inventing a parallel taxonomy

Do not retune thresholds in this task. The goal is accounting fidelity, not behavior change.
  </action>
  <verify>
    <automated>npm --prefix apps/worker run test -- --runInBand src/jobs/__tests__/run_mlb_model.test.js</automated>
  </verify>
  <done>The runner emits the required full-game funnel report and tests prove its counts, percentages, and suppressor ordering are deterministic.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the funnel output format to the WI-0944 operator contract</name>
  <files>apps/worker/src/jobs/run_mlb_model.js</files>
  <action>
Add a single deterministic log/report shape for the funnel output so execution work in later plans can compare before/after suppression behavior without log parsing ambiguity.

The report must include:
- window size
- stage counts
- stage percentages
- top two suppressors with condition key and impact percentage

Keep the log compact enough for production use. Summarize, do not dump whole candidate payloads.
  </action>
  <verify>
    <automated>node --check apps/worker/src/jobs/run_mlb_model.js</automated>
  </verify>
  <done>Operator-facing funnel output is compact, deterministic, and stable enough to compare across runs.</done>
</task>

</tasks>

<verification>
Run:
- `node --check apps/worker/src/jobs/run_mlb_model.js`
- `npm --prefix apps/worker run test -- --runInBand src/jobs/__tests__/run_mlb_model.test.js`

Pass condition: the runner test suite proves the funnel report stage keys, counts, percentages, and suppressor ranking for a fixed sample.
</verification>

<success_criteria>
- MLB full-game totals and moneyline candidates are fully accounted for in a deterministic funnel report.
- The top suppressors can be named from runner output without reading raw candidate payloads.
- Later plans can use the report to prove that odds-backed full-game plays surface more often for the right reasons.
</success_criteria>

<output>
After completion, create `.planning/phases/WI-0944/WI-0944-01-SUMMARY.md`
</output>

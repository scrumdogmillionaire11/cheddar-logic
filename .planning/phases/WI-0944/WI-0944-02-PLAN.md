---
phase: WI-0944
plan: "02"
type: execute
wave: 2
depends_on:
  - WI-0944-01
files_modified:
  - apps/worker/src/models/mlb-model.js
  - apps/worker/src/models/__tests__/mlb-model.test.js
  - apps/worker/src/models/__tests__/mlb-model-gate.test.js
autonomous: true
requirements:
  - WI-0944-TOTAL-01
  - WI-0944-GATE-01
must_haves:
  truths:
    - "Full-game total cards can promote from PASS to WATCH/FIRE when a real odds-backed full-game edge survives the explicit post-projection gates"
    - "Full-game total volatility threshold is dynamic but capped so the threshold never exceeds 0.65"
    - "F5 contradiction no longer acts as a hard veto for full-game totals; it degrades the outcome via a soft penalty instead"
    - "No more than two independent hard gates remain after projection for MLB full-game totals"
  artifacts:
    - path: "apps/worker/src/models/mlb-model.js"
      provides: "Retuned full-game total gate ladder with capped dynamic threshold and soft-penalty handling"
      contains: "projectFullGameTotalCard"
    - path: "apps/worker/src/models/__tests__/mlb-model.test.js"
      provides: "PASS->PLAY, PASS->PASS, and PLAY unchanged regression coverage for full-game totals"
      min_lines: 15
    - path: "apps/worker/src/models/__tests__/mlb-model-gate.test.js"
      provides: "Gate-focused assertions proving hard-gate collapse and reason-code continuity"
      min_lines: 10
  key_links:
    - from: "projectFullGameTotalCard"
      to: "resolveVarianceEdgeThreshold"
      via: "capped dynamic volatility threshold"
      pattern: "resolveVarianceEdgeThreshold"
    - from: "projectFullGameTotalCard"
      to: "reason_codes"
      via: "soft-penalty downgrade instead of contradiction veto"
      pattern: "SOFT_F5_CONTRADICTION|PASS_"
---

<objective>
Retune MLB full-game total suppression so real odds-backed total edges survive, while preserving deterministic guardrails.

Purpose: the independent-market-eval work made full-game totals eligible for insertion, but the model still suppresses too many candidates through a stacked post-projection gate ladder. This plan reduces redundant hard gating, caps the dynamic threshold, and converts F5 contradiction into a downgrade signal instead of a kill switch.

Output: a smaller and more legible full-game total gate ladder plus deterministic regression tests covering PASS->PLAY, PASS->PASS, and unchanged PLAY cases.
</objective>

<execution_context>
@.claude/process-acceleration-executors/workflows/execute-plan.md
@.claude/process-acceleration-executors/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/MONEYLINE_AUDIT_FULL_SYSTEM.md
@WORK_QUEUE/WI-0944.md
@apps/worker/src/models/mlb-model.js
@apps/worker/src/models/__tests__/mlb-model.test.js
@apps/worker/src/models/__tests__/mlb-model-gate.test.js
@.planning/phases/ime-01-independent-market-eval/ime-01-02-SUMMARY.md
@.planning/phases/ime-01-independent-market-eval/ime-01-03-SUMMARY.md
</context>

<interfaces>
<!-- Current full-game total controls to simplify rather than replace wholesale. -->

From apps/worker/src/models/mlb-model.js:
```javascript
const dynamicThreshold = resolveVarianceEdgeThreshold(
  proj.volatility_bucket,
  proj.variance_multiplier,
);
const preferF5 = Number.isFinite(f5Edge) && Math.abs(f5Edge) >= MLB_F5_EDGE_THRESHOLD && Math.abs(bullpenShift) < 0.12;
const fgContradictsF5 = Number.isFinite(f5Edge) && (Math.sign(edge) !== Math.sign(f5Edge)) && Math.abs(bullpenShift) > 0.14;
const confidenceGate = 6;
```

Current downstream contract:
```javascript
return {
  market: 'full_game_total',
  prediction,
  confidence: proj.confidence / 10,
  ev_threshold_passed: status !== 'PASS',
  status,
  action,
  classification,
  pass_reason_code,
  reason_codes,
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Collapse the full-game total hard-gate ladder into a capped edge-plus-confidence decision path</name>
  <files>apps/worker/src/models/mlb-model.js
apps/worker/src/models/__tests__/mlb-model.test.js
apps/worker/src/models/__tests__/mlb-model-gate.test.js</files>
  <behavior>
    - Test 1: A candidate previously blocked only by excessive volatility threshold can upgrade from PASS to WATCH/FIRE when the capped threshold is satisfied.
    - Test 2: A weak-edge candidate remains PASS with explicit PASS reason codes after the gate collapse.
    - Test 3: An already-valid PLAY remains a PLAY under the new logic.
  </behavior>
  <action>
Refactor `projectFullGameTotalCard()` so the explicit post-projection hard gates are reduced to the minimum necessary set.

Implement the WI-0944 constraints exactly:
- convert the volatility edge threshold to a dynamic threshold with a hard upper cap of `0.65`
- treat F5 contradiction as a soft penalty or downgrade, not a hard veto
- keep explicit PASS reason codes intact when a candidate still fails
- keep the resulting gate tree legible enough that the funnel from Plan 01 can map each hard-stop stage directly

Do not weaken the model into auto-publish behavior. The target is fewer redundant kills, not fewer standards.
  </action>
  <verify>
    <automated>npm --prefix apps/worker run test -- --runInBand src/models/__tests__/mlb-model.test.js</automated>
  </verify>
  <done>Full-game total logic uses a capped dynamic threshold, contradiction is a downgrade path, and deterministic total-market regressions pass.</done>
</task>

<task type="auto">
  <name>Task 2: Encode explicit gate-stage reason-code coverage for full-game totals</name>
  <files>apps/worker/src/models/__tests__/mlb-model-gate.test.js
apps/worker/src/models/mlb-model.js</files>
  <action>
Add focused assertions around the remaining hard gates so the new funnel stages and model outputs stay aligned.

Cover at least:
- edge below threshold
- confidence below threshold
- downgraded contradiction path still emitting a non-PASS outcome when the edge survives
- unchanged PASS reason-code continuity for genuinely weak candidates

Keep this task scoped to model-level gate semantics; runner-level funnel coverage belongs to Plan 01.
  </action>
  <verify>
    <automated>npm --prefix apps/worker run test -- --runInBand src/models/__tests__/mlb-model-gate.test.js</automated>
  </verify>
  <done>Model-level tests explicitly prove the remaining full-game-total hard gates and downgrade paths.</done>
</task>

</tasks>

<verification>
Run:
- `node --check apps/worker/src/models/mlb-model.js`
- `npm --prefix apps/worker run test -- --runInBand src/models/__tests__/mlb-model.test.js`
- `npm --prefix apps/worker run test -- --runInBand src/models/__tests__/mlb-model-gate.test.js`

Pass condition: deterministic model tests prove PASS->PLAY, PASS->PASS, and PLAY unchanged behavior for full-game totals.
</verification>

<success_criteria>
- Full-game total suppression is materially simpler and still explicit.
- The model now allows odds-backed total plays to surface when edge and confidence genuinely survive the capped gate path.
- Contradiction and volatility effects remain visible as reason codes and/or downgrades, not silent kills.
</success_criteria>

<output>
After completion, create `.planning/phases/WI-0944/WI-0944-02-SUMMARY.md`
</output>

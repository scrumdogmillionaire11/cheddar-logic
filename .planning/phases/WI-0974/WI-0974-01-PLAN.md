---
phase: WI-0974
plan: 01
type: execute
---

<objective>
Add a GitHub Actions workflow that builds an immutable tarball artifact per commit and deploys
the worker to the Pi from any branch (not just main).

Purpose: enable testing branch worker code on the Pi without merging to main first.
Output: .github/workflows/deploy-branch.yml
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Create deploy-branch.yml workflow</name>
  <files>
    .github/workflows/deploy-branch.yml
  </files>
  <action>
Create a workflow_dispatch-triggered workflow that:
1. Checks out the specified branch (default: triggering branch)
2. Installs worker + package deps
3. Packs a tarball and uploads it as an immutable artifact named worker-${sha}
4. SSHs to Pi via Tailscale, fetches + resets to that branch, installs deps, restarts cheddar-worker
  </action>
  <done>
Workflow file exists, immutable artifact upload step uses github.sha, SSH step checks out branch not main.
  </done>
</task>

</tasks>

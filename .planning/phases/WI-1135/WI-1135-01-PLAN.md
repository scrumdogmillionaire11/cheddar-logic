---
phase: WI-1135
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/api/results/route.ts
  - web/src/app/api/results/projection-metrics.ts
  - web/src/lib/results/query-layer.ts
  - web/src/lib/results/transform-layer.ts
  - web/src/lib/results/cache.ts
autonomous: true
requirements:
  - WI-1135-API-01
  - WI-1135-PERF-01
  - WI-1135-REG-01
must_haves:
  truths:
    - "GET /api/results default responses preserve summary, segments, projectionSummaries, and ledger contracts"
    - "Expensive diagnostic-only paths remain explicitly gated and do not run on default requests"
    - "Route responsibilities are split into query, transform, and cache stages with thin orchestration"
  artifacts:
    - path: "web/src/lib/results/query-layer.ts"
      provides: "Query-stage extraction for result retrieval and reporting inputs"
    - path: "web/src/lib/results/transform-layer.ts"
      provides: "Transform-stage response shaping for stable `/api/results` output contracts"
    - path: "web/src/lib/results/cache.ts"
      provides: "Cache ownership for report-friendly response fragments and invalidation hooks"
    - path: "web/src/app/api/results/projection-metrics.ts"
      provides: "Projection metrics helpers used by diagnostics/reporting logic"
    - path: "web/src/app/api/results/route.ts"
      provides: "Thin orchestration that wires query, transform, cache, and diagnostics gating"
  key_links:
    - from: "web/src/app/api/results/route.ts"
      to: "web/src/lib/results/query-layer.ts"
      via: "query-stage call"
      pattern: "from '../../lib/results/query-layer'|from \"../../lib/results/query-layer\"|from '@/lib/results/query-layer'|from \"@/lib/results/query-layer\""
    - from: "web/src/app/api/results/route.ts"
      to: "web/src/lib/results/transform-layer.ts"
      via: "transform-stage call"
      pattern: "from '../../lib/results/transform-layer'|from \"../../lib/results/transform-layer\"|from '@/lib/results/transform-layer'|from \"@/lib/results/transform-layer\""
    - from: "web/src/app/api/results/route.ts"
      to: "web/src/lib/results/cache.ts"
      via: "cache read/write orchestration"
      pattern: "from '../../lib/results/cache'|from \"../../lib/results/cache\"|from '@/lib/results/cache'|from \"@/lib/results/cache\""
    - from: "web/src/app/api/results/route.ts"
      to: "web/src/app/api/results/projection-metrics.ts"
      via: "metrics computation gated by diagnostics flags"
      pattern: "from './projection-metrics'|from \"./projection-metrics\""
---

<objective>
Decompose the `/api/results` route into explicit query, transform, and cache modules, with diagnostics/reporting workloads isolated behind intentional gates.

Purpose: Reduce route complexity and improve optimization safety without changing default contracts.
Output: Layered results modules + thin route orchestration + passing API/UI regression checks.
</objective>

<execution_context>
@/Users/ajcolubiale/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ajcolubiale/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@WORK_QUEUE/WI-1135.md
@WORK_QUEUE/COMPLETE/WI-1127.md
@web/src/app/api/results/route.ts
@web/src/app/api/results/projection-metrics.ts
@web/src/lib/results/query-layer.ts
@web/src/lib/results/transform-layer.ts
@web/src/lib/results/cache.ts
@web/src/__tests__/api-results-decision-segmentation.test.js
@web/src/__tests__/api-results-flags.test.js
@web/src/__tests__/ui-results-smoke.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish query/transform/cache boundaries for `/api/results`</name>
  <files>web/src/lib/results/query-layer.ts, web/src/lib/results/transform-layer.ts, web/src/lib/results/cache.ts</files>
  <action>Define explicit exports for query, transform, and cache responsibilities used by `/api/results`. Keep default response behavior contract-compatible and avoid policy changes to segmentation or ledger derivation. Ensure cache helpers remain side-effect scoped and do not implicitly enable diagnostics-only expensive work.</action>
  <verify>
    <automated>npm --prefix web run test:api:results:decision-segmentation</automated>
  </verify>
  <done>Results query/transform/cache modules expose stable orchestration interfaces and decision-segmentation behavior remains unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor route orchestration to consume extracted layers and preserve diagnostics gating</name>
  <files>web/src/app/api/results/route.ts, web/src/app/api/results/projection-metrics.ts</files>
  <action>Refactor `route.ts` into a thin orchestrator that calls query -> transform -> cache modules. Keep diagnostic/reporting-only expensive paths explicit and gated behind existing flags; default requests must not trigger those paths. Keep `projection-metrics.ts` responsibilities focused on metrics computation used by gated diagnostics/reporting flows.</action>
  <verify>
    <automated>npm --prefix web run test:api:results:flags</automated>
  </verify>
  <done>Route handler delegates to extracted layers, diagnostics gates are explicit, and flags behavior stays consistent with pre-refactor semantics.</done>
</task>

<task type="auto">
  <name>Task 3: Lock public contract parity for default requests</name>
  <files>web/src/app/api/results/route.ts, web/src/lib/results/transform-layer.ts</files>
  <action>Finalize default-response contract parity by ensuring summary, segments, projectionSummaries, and ledger payload shapes remain unchanged for non-diagnostic requests. Tighten transform-level guardrails as needed so decomposition does not alter UI-consumed fields.</action>
  <verify>
    <automated>npm --prefix web run test:ui:results</automated>
  </verify>
  <done>UI results smoke tests pass with unchanged default contract behavior after route decomposition.</done>
</task>

</tasks>

<verification>
Run all WI-required checks from repo root:
- `npm --prefix web run test:api:results:decision-segmentation`
- `npm --prefix web run test:api:results:flags`
- `npm --prefix web run test:ui:results`
</verification>

<success_criteria>
- `/api/results` route responsibilities are split into query, transform, and cache modules.
- Expensive diagnostics/reporting code paths are explicit and gated, not default-request behavior.
- Decision segmentation, flags, and results UI smoke checks pass with no contract regression.
</success_criteria>

<output>
After completion, create `.planning/phases/WI-1135/WI-1135-01-SUMMARY.md`
</output>

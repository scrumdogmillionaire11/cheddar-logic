---
phase: WI-0941
plan: "03"
type: execute
wave: 2
depends_on: [WI-0941-01, WI-0941-02]
files_modified:
  - apps/worker/src/jobs/run_nba_model.js
  - apps/worker/src/jobs/__tests__/run_nba_model.test.js
  - docs/audits/nba-blocking-audit.md
autonomous: true
requirements: [WI-0941-TD-05, WI-0941-acceptance]

must_haves:
  truths:
    - "The required WI-0941 suites run green: decision-pipeline-v2-nba-total-quarantine, run_nba_model, and check-pipeline-health.nba"
    - "NBA totals quarantine rows are clearly tagged and separable from non-quarantine blocks in latest DB output"
    - "At least one non-quarantine NBA market path surfaces when eligibility conditions are met (confirmed by test or DB query)"
    - "Stale or misleading comments catalogued in the audit are removed from run_nba_model.js or explicitly marked retained-intentional"
    - "Debt ledger in the audit doc is finalized with all TD items marked removed or retained-intentional"
  artifacts:
    - path: "apps/worker/src/jobs/run_nba_model.js"
      provides: "TD-05 stale comment cleanup; no orphaned misleading NBA market-eval comments"
      contains: "NBA_NO_ODDS_MODE_LEAN"
    - path: "apps/worker/src/jobs/__tests__/run_nba_model.test.js"
      provides: "≥3 new tests: quarantine path, non-quarantine actionable path, execution-gate parity"
      min_lines: 5
    - path: "docs/audits/nba-blocking-audit.md"
      provides: "Finalized debt ledger with all TD proofs and closeout commands"
      contains: "retained-intentional"
  key_links:
    - from: "run_nba_model.test.js quarantine regression test"
      to: "publishDecisionForCard / buildDecisionV2"
      via: "generateNBAMarketCallCards fixture → quarantine on/off path"
      pattern: "NBA_TOTAL_QUARANTINE_DEMOTE"
    - from: "run_nba_model.test.js execution gate parity test"
      to: "applyExecutionGateToNbaCard"
      via: "blocked card fixture proves decision_v2.official_status becomes PASS"
      pattern: "decision_v2"
---

<objective>
TD-05 + acceptance closeout: Remove stale comments and finalize worker-side regression evidence for WI-0941.

Purpose: With TD-01 through TD-04 fixed in Plans 01/02, this plan removes stale or misleading comments from `run_nba_model.js` that were catalogued in the audit (TD-05), then adds the worker-side regression tests that could not live in the models package: quarantine path, non-quarantine path, and execution-gate parity. Final acceptance validation ensures the required test suites and audit doc are complete.

Output: Clean `run_nba_model.js`, worker-side regression tests in `run_nba_model.test.js`, and finalized audit doc.
</objective>

<execution_context>
@.claude/process-acceleration-executors/workflows/execute-plan.md
@.claude/process-acceleration-executors/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@apps/worker/src/jobs/run_nba_model.js
@apps/worker/src/jobs/__tests__/run_nba_model.test.js
@docs/audits/nba-blocking-audit.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TD-05 stale comment cleanup in run_nba_model.js</name>
  <files>apps/worker/src/jobs/run_nba_model.js</files>
  <action>
Read `docs/audits/nba-blocking-audit.md` TD-05 section for the catalogued stale comment list from Plan 02.

For each comment identified as stale or misleading:
- remove it or replace it with an accurate description of current behavior
- add a short `// WI-0941: ...` note only where the new behavior would otherwise be unclear

If no stale comments were catalogued, record that outcome in the audit doc and mark TD-05 `retained-intentional` with rationale that no actionable legacy NBA selector-era comments remain.
  </action>
  <verify>
    <automated>npm --prefix apps/worker run test -- --runInBand src/jobs/__tests__/run_nba_model.test.js</automated>
  </verify>
  <done>TD-05 candidates resolved or explicitly retained-intentional; run_nba_model.test.js still green after comment cleanup</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Worker-side regression tests for quarantine and execution-gate parity</name>
  <files>apps/worker/src/jobs/__tests__/run_nba_model.test.js</files>
  <behavior>
    - Test A (quarantine path): generateNBAMarketCallCards with QUARANTINE_NBA_TOTAL=true produces a totals card that carries NBA_TOTAL_QUARANTINE_DEMOTE after publish
    - Test B (non-quarantine path): generateNBAMarketCallCards with QUARANTINE_NBA_TOTAL=false produces a totals card whose decision_v2.official_status remains PLAY when edge and support pass
    - Test C (execution gate parity): a card blocked by applyExecutionGateToNbaCard has decision_v2.official_status === 'PASS' and primary_reason_code === pass_reason_code
  </behavior>
  <action>
Add a new `describe('WI-0941 quarantine and execution-gate regression')` block in `run_nba_model.test.js`.

Use the existing `generateNBAMarketCallCards` export already imported in the file. Reuse the existing fixture pattern already present in the test file.

For Test A:
- set `process.env.QUARANTINE_NBA_TOTAL = 'true'`
- build a minimal odds snapshot and decision fixture where the totals path would otherwise surface as PLAY
- call `generateNBAMarketCallCards`
- publish/finalize the card using the same helper flow the file already uses for market-call assertions
- assert the totals card includes `NBA_TOTAL_QUARANTINE_DEMOTE`

For Test B:
- set `process.env.QUARANTINE_NBA_TOTAL = 'false'`
- same fixture pattern
- assert `decision_v2.official_status === 'PLAY'`

For Test C:
- use `applyExecutionGateToNbaCard`
- construct a minimal blocked-card fixture with an existing `decision_v2`
- assert `decision_v2.official_status === 'PASS'`
- assert `decision_v2.primary_reason_code === pass_reason_code`

Use `jest.resetModules()` between tests that mutate `process.env.QUARANTINE_NBA_TOTAL`.
  </action>
  <verify>
    <automated>npm --prefix apps/worker run test -- --runInBand src/jobs/__tests__/run_nba_model.test.js</automated>
  </verify>
  <done>≥3 new worker-side tests pass; quarantine and non-quarantine totals paths are covered; execution-gate parity confirms TD-01 fix</done>
</task>

<task type="auto">
  <name>Task 3: Final acceptance run and audit doc finalization</name>
  <files>docs/audits/nba-blocking-audit.md</files>
  <action>
**Step 1 — Run the required WI-0941 test suites** and record results:
```
npm --prefix packages/models run test -- decision-pipeline-v2-nba-total-quarantine.test.js
npm --prefix apps/worker run test -- --runInBand src/jobs/__tests__/run_nba_model.test.js
npm --prefix apps/worker run test -- --runInBand src/__tests__/check-pipeline-health.nba.test.js
```

**Step 2 — Run deterministic closeout commands** and paste outputs into the audit doc:
```
cd /Users/ajcolubiale/projects/cheddar-logic && rg -n "NBA_NO_ODDS_MODE_LEAN" apps/worker/src/jobs/run_nba_model.js
cd /Users/ajcolubiale/projects/cheddar-logic && rg -n "decision_v2\.official_status\s*=" apps/worker/src/jobs/run_nba_model.js
cd /Users/ajcolubiale/projects/cheddar-logic && rg -n "checkNbaMarketCallDiagnostics|summarizeNbaRejectReasonFamilies|nba_market_call_diagnostics" apps/worker/src/jobs/check_pipeline_health.js
cd /Users/ajcolubiale/projects/cheddar-logic && rg -n "NBA_TOTAL_QUARANTINE_DEMOTE" apps/worker/src/jobs/run_nba_model.js packages/models/src/decision-pipeline-v2-edge-config.js packages/models/src/__tests__/decision-pipeline-v2-nba-total-quarantine.test.js
cd /Users/ajcolubiale/projects/cheddar-logic && rg -n "decision_v2\.official_status|play\.action === 'PASS'|play\.classification === 'PASS'" web/src/lib/game-card/filters.ts
```

**Step 3 — Finalize the audit doc.** Update each TD entry with:
- `Proof`: test name or rg command + output
- `Decision`: `removed` or `retained-intentional`
- `Follow-up WI`: if a retained item needs one

**Step 4 — Record policy vs operational block partition** in the audit:
- Policy blocks: `NBA_TOTAL_QUARANTINE_DEMOTE`
- Operational blocks: `PASS_NO_EDGE`, `PASS_EXECUTION_GATE_BLOCKED`, `PROJECTION_ONLY_EXCLUSION`, `NOT_EXECUTABLE_PATH`, etc.
- Per-market produced vs surfaced counts: query `card_payloads` for the last 24h and record counts by `card_type` + `official_status`
  </action>
  <verify>
    <automated>npm --prefix apps/worker run test -- --runInBand src/jobs/__tests__/run_nba_model.test.js src/__tests__/check-pipeline-health.nba.test.js && npm --prefix packages/models run test -- decision-pipeline-v2-nba-total-quarantine.test.js</automated>
  </verify>
  <done>Required WI-0941 test suites pass; audit doc contains final debt ledger with proofs; all TDs are marked removed or retained-intentional; policy vs operational block partition is documented</done>
</task>

</tasks>

<verification>
- `npm --prefix packages/models run test -- decision-pipeline-v2-nba-total-quarantine.test.js` — pass
- `npm --prefix apps/worker run test -- --runInBand src/jobs/__tests__/run_nba_model.test.js` — pass
- `npm --prefix apps/worker run test -- --runInBand src/__tests__/check-pipeline-health.nba.test.js` — pass
- `ls docs/audits/nba-blocking-audit.md` — exists with complete debt ledger
- `rg -n "checkNbaMarketCallDiagnostics" apps/worker/src/jobs/check_pipeline_health.js` — wired
- `rg -n "NBA_NO_ODDS_MODE_LEAN" apps/worker/src/jobs/run_nba_model.js` — present
</verification>

<success_criteria>
- NBA block reasons are partitioned into POLICY_QUARANTINE vs operational reason families
- Per-market produced vs surfaced counts are explainable by explicit reason codes
- No contradictory demotion paths remain for execution-gate-blocked cards
- Quarantine behavior is reversible via QUARANTINE_NBA_TOTAL flag with test coverage
- Audit doc is complete with per-TD proof and policy partition
</success_criteria>

<output>
After completion, create `.planning/phases/WI-0941/WI-0941-03-SUMMARY.md`
</output>

---
phase: WI-0941
plan: "03"
type: execute
wave: 2
depends_on: [WI-0941-01, WI-0941-02]
files_modified:
  - apps/worker/src/jobs/run_nba_model.js
  - apps/worker/src/jobs/__tests__/run_nba_model.test.js
  - apps/worker/src/jobs/__tests__/report_telemetry_calibration.test.js
  - docs/audits/nba-blocking-audit.md
autonomous: true
requirements: [WI-0941-TD-05, WI-0941-acceptance]

must_haves:
  truths:
    - "All three enumerated tests from WI-0941 run green: decision-pipeline-v2-nba-total-quarantine, run_nba_model, report_telemetry_calibration"
    - "NBA totals quarantine rows are clearly tagged and separable from non-quarantine blocks in latest DB output"
    - "At least one non-quarantine NBA market path surfaces when eligibility conditions are met (confirmed by test or DB query)"
    - "Stale/misleading comments catalogued in Plan 02 audit are removed from run_nba_model.js"
    - "Debt ledger in audit doc is finalized with all TD items marked removed or retained-intentional"
  artifacts:
    - path: "apps/worker/src/jobs/run_nba_model.js"
      provides: "TD-05 stale comment cleanup; no orphaned legacy selector-era comments"
      contains: "NBA_NO_ODDS_MODE_LEAN"
    - path: "apps/worker/src/jobs/__tests__/run_nba_model.test.js"
      provides: "≥2 new tests: one quarantine path, one non-quarantine actionable path"
      min_lines: 5
    - path: "docs/audits/nba-blocking-audit.md"
      provides: "Finalized debt ledger with all TD proofs and absence commands"
      contains: "retained-intentional"
  key_links:
    - from: "run_nba_model.test.js quarantine regression test"
      to: "applyExecutionGateToNbaCard / publishDecisionForCard"
      via: "generateNBAMarketCallCards fixture → check reason_codes includes NBA_TOTAL_QUARANTINE_DEMOTE when quarantine=true"
      pattern: "NBA_TOTAL_QUARANTINE_DEMOTE"
    - from: "run_nba_model.test.js non-quarantine test"
      to: "decision_v2.official_status=PLAY or LEAN"
      via: "generateNBAMarketCallCards with QUARANTINE_NBA_TOTAL=false → confirm surfaced"
      pattern: "official_status"
---

<objective>
TD-05 + acceptance closeout: Remove stale comments and finalize test evidence for WI-0941.

Purpose: With TD-01 through TD-04 fixed in Plans 01/02, this plan removes stale or misleading comments from `run_nba_model.js` that were catalogued in the Plan 02 audit (TD-05), then adds regression tests to `run_nba_model.test.js` covering the quarantine and non-quarantine NBA paths. Final acceptance validation: all three WI-0941 required test runs pass and the audit doc is complete.

Output: Clean run_nba_model.js (no orphaned selector-era comments); two new regression tests; finalized audit doc.
</objective>

<execution_context>
@.claude/process-acceleration-executors/workflows/execute-plan.md
@.claude/process-acceleration-executors/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@apps/worker/src/jobs/run_nba_model.js
@apps/worker/src/jobs/__tests__/run_nba_model.test.js
@docs/audits/nba-blocking-audit.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TD-05 stale comment cleanup in run_nba_model.js</name>
  <files>apps/worker/src/jobs/run_nba_model.js</files>
  <action>
Read `docs/audits/nba-blocking-audit.md` TD-05 section for the catalogued stale comment list from Plan 02.

For each comment identified as stale/misleading (NOT `choosePrimaryDisplayMarket`, NOT `buildDualRunRecord`/`[DUAL_RUN]` infra — those are retained-intentional per WI-0940 precedent):
- Remove it or replace with accurate description of current behavior
- Add a one-line `// WI-0941: ...` note if the behavior it describes has changed

Scope: run_nba_model.js ONLY. Do NOT touch decision-publisher.js, check_pipeline_health.js, or any other file in this task.

If no stale comments were catalogued (TD-05 candidates list is empty), note this in the audit doc under TD-05 as "no actionable stale comments found" and mark `retained-intentional` with rationale "no legacy NBA selector-era comments present; NBA market eval was written after the migration".
  </action>
  <verify>
    <automated>npm --prefix apps/worker run test -- --runInBand src/jobs/__tests__/run_nba_model.test.js</automated>
  </verify>
  <done>TD-05 candidates resolved; run_nba_model.test.js still fully green after comment-only changes</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Regression tests for quarantine and non-quarantine NBA paths</name>
  <files>apps/worker/src/jobs/__tests__/run_nba_model.test.js</files>
  <behavior>
    - Test A (quarantine path): generateNBAMarketCallCards with QUARANTINE_NBA_TOTAL=true produces nba-totals-call cards where reason_codes includes 'NBA_TOTAL_QUARANTINE_DEMOTE' when the engine would otherwise produce a PLAY
    - Test B (non-quarantine path): generateNBAMarketCallCards with QUARANTINE_NBA_TOTAL=false produces nba-totals-call cards where decision_v2.official_status is 'PLAY' (not demoted) when edge and support pass
    - Test C (execution gate parity): a card blocked by applyExecutionGateToNbaCard has decision_v2.official_status === 'PASS' (proves TD-01 fix from Plan 01)
  </behavior>
  <action>
Add a new `describe('WI-0941 quarantine and non-quarantine regression')` block in `run_nba_model.test.js`.

Use the existing `generateNBAMarketCallCards` export (already imported in the test file at line 17). The existing test at line 129 shows the fixture pattern — follow it.

For Test A: set `process.env.QUARANTINE_NBA_TOTAL = 'true'` in `beforeEach`, construct a minimal odds snapshot where the total decision would be PLAY, call `generateNBAMarketCallCards`, find the `nba-totals-call` card, assert `reason_codes` includes `'NBA_TOTAL_QUARANTINE_DEMOTE'`.

For Test B: set `process.env.QUARANTINE_NBA_TOTAL = 'false'`, same fixture, assert `decision_v2.official_status === 'PLAY'`.

For Test C: use `applyExecutionGateToNbaCard` export (already exported at line 1983), construct a minimal card with execution gate conditions that trigger a block, call the function, assert `card.payloadData.decision_v2.official_status === 'PASS'`.

Use `jest.resetModules()` between subtests that mutate `process.env.QUARANTINE_NBA_TOTAL`.
  </action>
  <verify>
    <automated>npm --prefix apps/worker run test -- --runInBand src/jobs/__tests__/run_nba_model.test.js</automated>
  </verify>
  <done>≥3 new tests passing; quarantine demoted path produces NBA_TOTAL_QUARANTINE_DEMOTE; non-quarantine path surfaces PLAY without demotion; execution gate parity test confirms TD-01 fix</done>
</task>

<task type="auto">
  <name>Task 3: Final acceptance run and audit doc finalization</name>
  <files>docs/audits/nba-blocking-audit.md</files>
  <action>
**Step 1 — Run all three required WI-0941 test suites** and record results:
```
npm --prefix packages/models run test -- decision-pipeline-v2-nba-total-quarantine.test.js
npm --prefix apps/worker run test -- --runInBand src/jobs/__tests__/report_telemetry_calibration.test.js
npm --prefix apps/worker run test -- --runInBand src/jobs/__tests__/run_nba_model.test.js
```

**Step 2 — Run deterministic closeout commands** and paste outputs into audit doc:
```
cd /Users/ajcolubiale/projects/cheddar-logic && rg -n "NBA_NO_ODDS_MODE_LEAN" apps/worker/src/jobs/run_nba_model.js
cd /Users/ajcolubiale/projects/cheddar-logic && rg -n "decision_v2\.official_status\s*=" apps/worker/src/jobs/run_nba_model.js
cd /Users/ajcolubiale/projects/cheddar-logic && rg -n "checkNbaMarketCallDiagnostics\|summarizeNbaRejectReasonFamilies" apps/worker/src/jobs/check_pipeline_health.js
cd /Users/ajcolubiale/projects/cheddar-logic && rg -n "NBA_TOTAL_QUARANTINE_DEMOTE" apps/worker/src/jobs/run_nba_model.js packages/models/src/decision-pipeline-v2-edge-config.js packages/models/src/__tests__/decision-pipeline-v2-nba-total-quarantine.test.js
```

**Step 3 — Finalize audit doc**. Update each TD entry with:
- `Proof`: test name or rg command + output
- `Decision`: `removed` or `retained-intentional`
- `Follow-up WI`: if any retained item warrants a follow-up

**Step 4 — Record policy vs operational block partition** in audit:
- Policy blocks (fixed, controlled by flag): `NBA_TOTAL_QUARANTINE_DEMOTE` (QUARANTINE_NBA_TOTAL=true)
- Operational blocks (data-driven): `PASS_NO_EDGE`, `PASS_EXECUTION_GATE_BLOCKED`, `PROJECTION_ONLY_EXCLUSION`, `NOT_EXECUTABLE_PATH`, etc.
- Per-market produced vs surfaced counts: query card_payloads for last 24h and record counts by card_type + official_status
  </action>
  <verify>
    <automated>npm --prefix apps/worker run test -- --runInBand src/jobs/__tests__/run_nba_model.test.js src/jobs/__tests__/report_telemetry_calibration.test.js && npm --prefix packages/models run test -- decision-pipeline-v2-nba-total-quarantine.test.js</automated>
  </verify>
  <done>All three required WI-0941 test suites pass; audit doc contains final debt ledger with proofs; all TDs marked removed or retained-intentional; policy vs operational block partition is documented</done>
</task>

</tasks>

<verification>
All WI-0941 acceptance criteria:
- `npm --prefix packages/models run test -- decision-pipeline-v2-nba-total-quarantine.test.js` — pass
- `npm --prefix apps/worker run test -- --runInBand src/jobs/__tests__/report_telemetry_calibration.test.js` — pass
- `npm --prefix apps/worker run test -- --runInBand src/jobs/__tests__/run_nba_model.test.js` — pass
- `npm --prefix apps/worker run test -- --runInBand src/__tests__/check-pipeline-health.nba.test.js` — pass
- `ls docs/audits/nba-blocking-audit.md` — exists with complete debt ledger
- `rg -n "checkNbaMarketCallDiagnostics" apps/worker/src/jobs/check_pipeline_health.js` — wired
- `rg -n "NBA_NO_ODDS_MODE_LEAN" apps/worker/src/jobs/run_nba_model.js` — present
</verification>

<success_criteria>
- NBA block reasons are partitioned into POLICY_QUARANTINE vs operational reason families
- Per-market produced vs surfaced counts are explainable by explicit reason codes
- No contradictory demotion paths remain (decision_v2.official_status === action/classification/status)
- Quarantine behavior is reversible via QUARANTINE_NBA_TOTAL flag with test coverage
- Audit doc complete with per-TD proof and policy partition
</success_criteria>

<output>
After completion, create `.planning/phases/WI-0941/WI-0941-03-SUMMARY.md`
</output>

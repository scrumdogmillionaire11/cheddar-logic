# WI-1210-01 Plan

---
phase: WI-1210
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/api/results/route.ts
  - web/src/lib/results/query-layer.ts
  - web/src/lib/results/transform-layer.ts
  - web/src/__tests__/**
  - apps/worker/src/jobs/settle_pending_cards.js
  - apps/worker/src/jobs/__tests__/**
autonomous: true
requirements:
  - WI-1210-PERF-BUDGET
  - WI-1211-SURFACED-SETTLEMENT-POLICY
  - WI-1212-LEGACY-HELPER-CLEANUP
must_haves:
  truths:
    - "Performance regressions for /api/results fail CI via explicit latency/heap budgets"
    - "Unsurfaced non-exception plays do not settle"
    - "Legacy helper paths in results transform are either removed or directly exercised"
  artifacts:
    - path: "web/src/__tests__/"
      provides: "High-cardinality performance budget harness for /api/results"
    - path: "apps/worker/src/jobs/__tests__/"
      provides: "Surfaced-play settlement policy regression matrix"
    - path: "web/src/lib/results/transform-layer.ts"
      provides: "Dead-path cleanup or explicitly covered runtime helpers"
---

## Objective

Deliver phased hardening for results performance and settlement correctness while minimizing cross-surface regression risk.

Purpose: convert concern statements into enforceable tests and explicit policy contracts.
Output: one execution sequence with clear gates, commands, and closeout criteria.

## Context

- @.planning/codebase/CONCERNS.md
- @WORK_QUEUE/WI-1210.md
- @WORK_QUEUE/WI-1211.md
- @WORK_QUEUE/WI-1212.md
- @web/src/app/api/results/route.ts
- @web/src/lib/results/query-layer.ts
- @web/src/lib/results/transform-layer.ts
- @apps/worker/src/jobs/settle_pending_cards.js
- @apps/worker/src/jobs/__tests__/settle_pending_cards.phase2.test.js

## Phases

### Phase 0 - Baseline Lock (0.5-1 day)

Goals:
- Lock p50/p95 latency budget and heap delta budget for /api/results scenarios.
- Lock surfaced-play exception matrix by market.
- Lock helper disposition strategy (delete-first unless runtime-required).

Done criteria:
- Budget values are documented in test source comments or an adjacent note.
- Exception matrix is explicit enough to map one test per rule.

### Phase 1 - Performance Budget Harness (2-4 days, maps to WI-1210)

Goals:
- Add deterministic high-cardinality fixture path for /api/results tests.
- Add enforceable latency and heap checks for default, limit=200, dedupe=0, and one filtered variant.
- Ensure stable failure messaging with measured vs budget values.

Verification:
- npm --prefix web run test:results:behavioral-contract
- npm --prefix web run test:api:results:decision-segmentation
- npm --prefix web run test:api:results:performance-budget

Done criteria:
- Regressions fail deterministically in CI-like runs.

### Phase 2 - Settlement Policy Regression Matrix (1.5-3 days, maps to WI-1211, parallel-safe with Phase 1)

Goals:
- Assert non-displayed non-exception rows do not settle.
- Assert displayed eligible rows settle.
- Assert approved exception pathways are explicit and non-duplicative.

Verification:
- npm --prefix apps/worker test -- src/jobs/__tests__/settle_pending_cards.phase2.test.js
- npm --prefix apps/worker test -- src/jobs/__tests__/settle_pending_cards.non-actionable.test.js
- npm --prefix apps/worker test -- src/jobs/__tests__/negative-path-settlement-live-truth.test.js

Done criteria:
- Policy violations are caught with readable behavioral assertions.

### Phase 3 - Transform Legacy Helper Cleanup (1-2 days, maps to WI-1212, depends on Phase 1)

Goals:
- Resolve hasDecisionV2, resolveLegacyMlbResultsTier, canUseLegacyMlbResultsAdapter.
- For each helper: remove or wire with direct behavioral coverage.
- Preserve active decision-tier contract behavior.

Verification:
- npm --prefix web run test:api:results:decision-segmentation
- npm --prefix web run test:api:results:no-implicit-actionable-fallback

Done criteria:
- No ambiguous dead paths remain in results transform logic.

### Phase 4 - Closeout (0.5-1 day, depends on Phases 1-3)

Goals:
- Run all phase verification commands.
- Confirm scope hygiene and document any residual risk.
- Update .planning/codebase/CONCERNS.md statuses.

Done criteria:
- Concerns are marked resolved/reduced with explicit evidence and next actions.

## Execution Order

1. Phase 0
2. Phase 1 and Phase 2 in parallel if different owners
3. Phase 3 after Phase 1
4. Phase 4 final closeout

## Success Criteria

- Performance budgets are enforced by deterministic tests.
- Settlement policy boundaries are enforced by behavioral tests.
- Legacy transform helper ambiguity is eliminated.
- Concern status reflects current repository state.

## Output

After execution, create .planning/phases/WI-1210/WI-1210-01-SUMMARY.md.

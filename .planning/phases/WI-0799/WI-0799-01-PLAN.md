---
phase: WI-0799
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/admin/page.tsx
autonomous: false

must_haves:
  truths:
    - Snapshot cards show "failed x N" or "warning x N" streak badge when streak >= 2
    - ok streaks produce no badge (no noise)
    - Cards with created_at age > 35min show opacity-50 and "check dormant" label
    - Stale mlb:f5_market_availability card at 23h looks clearly dormant, not alarming
  artifacts:
    - path: web/src/app/admin/page.tsx
      provides: computeStreak(), isStale(), StreakBadge, STALE_THRESHOLD_MS=35*60*1000
      contains: opacity-50
  key_links:
    - from: snapshot.map render in AdminPage
      to: computeStreak(health, row.phase, row.check_name)
      via: direct call in JSX
      pattern: "computeStreak\\(health"
    - from: snapshot.map render in AdminPage
      to: isStale(row.created_at)
      via: direct call in JSX
      pattern: "isStale\\(row\\.created_at\\)"
    - from: stale === true
      to: opacity-50 className + "check dormant" label
      via: conditional render
      pattern: "check dormant"
---

<objective>
Verify and close out WI-0799 admin dashboard improvements. Both features —
consecutive failure streak badges and stale-card visual muting — are already
implemented in web/src/app/admin/page.tsx. This plan confirms the implementation
matches all acceptance criteria and gates close-out.

Purpose: Close-out gate confirming no rework needed.
Output: Visual verification that streak badges and dormant muting work as specified.
</objective>

<execution_context>
@./.claude/process-acceleration-executors/workflows/execute-plan.md
@./.claude/process-acceleration-executors/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@WORK_QUEUE/WI-0799.md
@web/src/app/admin/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm implementation matches all four acceptance criteria</name>
  <files>web/src/app/admin/page.tsx</files>
  <action>
Static verification — no code changes expected. Run each check and confirm:

1. Streak badge components present:
   grep -n "computeStreak\|StreakBadge" web/src/app/admin/page.tsx
   → Must show computeStreak defined AND called in snapshot.map, StreakBadge defined AND rendered

2. Stale muting present:
   grep -n "check dormant\|opacity-50\|isStale" web/src/app/admin/page.tsx
   → Must show isStale() defined AND called in render, opacity-50 in stale className, "check dormant" text

3. STALE_THRESHOLD_MS = 35 minutes:
   grep -n "STALE_THRESHOLD_MS\|35 \* 60" web/src/app/admin/page.tsx
   → Must return 35 * 60 * 1000

4. ok streak suppression:
   grep -A8 "function StreakBadge" web/src/app/admin/page.tsx | grep -i "ok"
   → Must show .toLowerCase() === 'ok' guard before rendering badge

If any check fails, make the minimal targeted fix in web/src/app/admin/page.tsx to
satisfy it. All four checks already pass as of 2026-04-10 (verified by static scan).
  </action>
  <verify>
grep -c "computeStreak\|isStale\|check dormant\|opacity-50\|StreakBadge\|STALE_THRESHOLD_MS" web/src/app/admin/page.tsx
Must return 6 (each of the 6 terms present at least once).
  </verify>
  <done>All 6 implementation terms confirmed present; all four acceptance criteria satisfied</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Admin /admin page already has:
- Streak badges: "failed x N" / "warning x N" below status badge when streak >= 2
- ok streaks produce no badge (StreakBadge returns null for ok status)
- Stale-card muting: opacity-50 + "check dormant" pill for cards older than 35min
  </what-built>
  <how-to-verify>
1. Open http://localhost:3000/admin (dev server at web/ must be running)
2. Wait for Model Health snapshot to load
3. Streak badge check: find any card with status=failed or warning that has been in
   that state for multiple consecutive checks — it should show "failed x N" or
   "warning x N" badge below the status badge
4. ok streak suppression: find any card with status=ok — it should show NO streak
   badge regardless of how long it has been ok
5. Dormant muting check: find any card whose last check was > 35min ago — it should
   appear at reduced opacity and show "check dormant" instead of an age ("5m ago")
6. Note: mlb:f5_market_availability only fires when MLB games are within T-6h — if
   no MLB games today its card will be stale and should appear dormant
  </how-to-verify>
  <resume-signal>Type "approved" if all visible cards match the spec. Describe any mismatch.</resume-signal>
</task>

</tasks>

<verification>
grep -c "computeStreak\|isStale\|check dormant\|opacity-50\|StreakBadge\|STALE_THRESHOLD_MS" web/src/app/admin/page.tsx
</verification>

<success_criteria>
- grep returns 6 (all implementation terms present)
- Human visual verification passes: streak badges show on failed/warning streaks,
  ok cards show no badge, stale cards show opacity-50 + "check dormant"
- WI-0799 moved to WORK_QUEUE/COMPLETE/
</success_criteria>

<output>
No SUMMARY.md needed — single-plan close-out WI with no downstream plan dependencies.
Commit: verify(WI-0799): confirm streak+stale implementation complete in admin/page.tsx
</output>

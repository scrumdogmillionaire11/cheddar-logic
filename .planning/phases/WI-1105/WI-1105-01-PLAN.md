---
phase: WI-1105
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/data/src/db/projection-accuracy.js
  - packages/data/src/db/cards.js
  - packages/data/db/migrations/20260421_wi_1105_projection_accuracy_materialized.sql
  - web/src/app/api/results/route.ts
  - web/src/__tests__/api-results-route.test.ts
  - packages/data/src/db/__tests__/projection-accuracy.test.js
autonomous: true
requirements:
  - WI-1105-SCHEMA-01
  - WI-1105-GRADE-01
  - WI-1105-CONF-01
  - WI-1105-TRUST-01
  - WI-1105-API-01

must_haves:
  truths:
    - "New projection-accuracy rows read from materialized columns without JSON dependency"
    - "Synthetic line and direction are deterministic and never recomputed after insert"
    - "WEAK direction rows are excluded from directional W/L aggregates but kept in error metrics"
    - "Settlement writes grading outputs once and does not overwrite immutable projection fields"
    - "Results API returns confidence-band performance and market-trust status with fallback tagging"
  artifacts:
    - path: "packages/data/db/migrations/20260421_wi_1105_projection_accuracy_materialized.sql"
      provides: "materialized projection-accuracy columns and indexes"
      contains: "projection_raw"
    - path: "packages/data/src/db/projection-accuracy.js"
      provides: "immutability guards, synthetic line math, confidence/trust computations"
      contains: "Math.round(projection_raw * 2) / 2"
    - path: "packages/data/src/db/cards.js"
      provides: "write-time stamping for immutable projection core fields"
      contains: "synthetic_direction"
    - path: "web/src/app/api/results/route.ts"
      provides: "materialized-first read path with explicit legacy fallback tags"
      contains: "FALLBACK_USED"
    - path: "packages/data/src/db/__tests__/projection-accuracy.test.js"
      provides: "contract tests for synthetic math, immutability, weak-direction filtering, confidence determinism"
    - path: "web/src/__tests__/api-results-route.test.ts"
      provides: "results API regression tests for shape parity and fallback tagging"
  key_links:
    - from: "packages/data/src/db/cards.js"
      to: "packages/data/src/db/projection-accuracy.js"
      via: "new row insert stamps immutable projection core fields once"
      pattern: "projection_raw|synthetic_line|synthetic_direction"
    - from: "packages/data/src/db/projection-accuracy.js"
      to: "web/src/app/api/results/route.ts"
      via: "query consumes materialized grading/confidence/trust columns"
      pattern: "projection_confidence|confidence_band|market_trust_status|FALLBACK_USED"
    - from: "packages/data/src/db/projection-accuracy.js"
      to: "packages/data/src/db/__tests__/projection-accuracy.test.js"
      via: "tests enforce synthetic-line and immutability invariants"
      pattern: "DIRECTION_TOO_WEAK|NO_EDGE|graded_result"
---

<objective>
Materialize projection-accuracy core fields and enforce immutable grading contracts so analytics and results reads do not depend on JSON paths for new rows.

Purpose: remove drift-prone recomputation and make grading, confidence, and market-trust behavior deterministic and testable.

Output: migration, DB write/read contract updates, and regression tests for data and API paths.
</objective>

<execution_context>
@./.claude/process-acceleration-executors/workflows/execute-plan.md
@./.claude/process-acceleration-executors/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@WORK_QUEUE/WI-1105.md
@packages/data/src/db/projection-accuracy.js
@packages/data/src/db/cards.js
@web/src/app/api/results/route.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add schema columns and immutable write-path contracts</name>
  <files>packages/data/db/migrations/20260421_wi_1105_projection_accuracy_materialized.sql
packages/data/src/db/cards.js
packages/data/src/db/projection-accuracy.js
packages/data/src/db/__tests__/projection-accuracy.test.js</files>
  <behavior>
    - Test 1: Synthetic line uses nearest half-point math exactly: Math.round(projection_raw * 2) / 2.
    - Test 2: projection_raw equals synthetic_line yields synthetic_direction NO_EDGE.
    - Test 3: Attempted overwrite of immutable projection fields is rejected or hard-flagged.
  </behavior>
  <action>
Create a migration that adds materialized core columns: projection_raw, synthetic_line, synthetic_rule, synthetic_direction, edge_distance, direction_strength, actual, graded_result, abs_error, signed_error, projection_confidence, confidence_band.

Add indexes for (market_type, status), confidence_band, and direction_strength.

In data write paths, stamp immutable projection fields at initial insert only, and add a guard path that blocks or flags any recomputation mismatch after insert.
  </action>
  <verify>
    <automated>npm --prefix packages/data run test -- --runInBand src/db/__tests__/projection-accuracy.test.js</automated>
  </verify>
  <done>
Materialized schema and immutable write-path contract are in place, and synthetic-line invariants are covered by automated tests.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Materialize grading/confidence/trust and lock weak-direction behavior</name>
  <files>packages/data/src/db/projection-accuracy.js
packages/data/src/db/__tests__/projection-accuracy.test.js</files>
  <behavior>
    - Test 1: edge_distance below 0.15 is WEAK and sets DIRECTION_TOO_WEAK.
    - Test 2: WEAK rows remain in MAE/bias calculations but are excluded from directional W/L aggregates.
    - Test 3: Confidence score is deterministic for identical inputs, including sample-size weighting.
    - Test 4: market_trust_status is NOISE when win_rate(STRONG) <= win_rate(WEAK/WATCH).
  </behavior>
  <action>
Write settlement-time materialization for actual, graded_result, abs_error, and signed_error. Add deterministic confidence derivation with edge distance, historical hit rate, variance penalty, and sample-size weighting, then map to confidence_band.

Implement market trust classification surface with required statuses and hard NOISE gate when STRONG underperforms WEAK/WATCH.

Ensure projection_accuracy_line_evals remains keyed by (card_id, line), written once during grading, and never recomputed.
  </action>
  <verify>
    <automated>npm --prefix packages/data run test -- --runInBand src/db/__tests__/projection-accuracy.test.js</automated>
  </verify>
  <done>
Directional filtering, confidence determinism, trust-gate logic, and line-eval integrity are all test-covered and deterministic.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update /results API to materialized-first reads with legacy fallback tagging</name>
  <files>web/src/app/api/results/route.ts
web/src/__tests__/api-results-route.test.ts</files>
  <behavior>
    - Test 1: API prefers materialized columns and uses JSON path only when column is null.
    - Test 2: Any legacy fallback path sets FALLBACK_USED indicator in response rows.
    - Test 3: Response shape remains backward compatible while adding confidence-band, weak-share, and market-trust metrics.
  </behavior>
  <action>
Refactor results route reads to use materialized projection/grading/confidence columns first, with strict compatibility fallback for legacy rows only.

Expose win-rate excluding WEAK and PUSH rows, MAE, bias, confidence-band performance, weak-direction share, and market_trust_status.

Add regression tests for materialized-first behavior, fallback tagging, and unchanged API envelope shape.
  </action>
  <verify>
    <automated>npm --prefix web run test -- src/__tests__/api-results-route.test.ts --runInBand</automated>
  </verify>
  <done>
/results reads from materialized fields for new rows, legacy JSON fallback is explicit and tagged, and API contract remains stable.
  </done>
</task>

</tasks>

<verification>
Run:
- npm --prefix packages/data run test -- --runInBand src/db/__tests__/projection-accuracy.test.js
- npm --prefix web run test -- src/__tests__/api-results-route.test.ts --runInBand

Pass condition:
- synthetic-line math and immutability invariants pass
- weak-direction and confidence/trust contracts pass
- /results API materialized-first behavior and fallback tagging pass without shape regressions
</verification>

<success_criteria>
- New projection-accuracy writes are JSON-independent for core analytics fields.
- Immutable projection fields are never recomputed post-insert.
- Settlement writes grading outputs once with deterministic confidence/trust surfaces.
- /results exposes required metrics from materialized fields while preserving response compatibility.
- Automated tests lock all listed invariants.
</success_criteria>

<output>
After completion, create `.planning/phases/WI-1105/WI-1105-SUMMARY.md`
</output>

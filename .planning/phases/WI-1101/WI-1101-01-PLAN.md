---
phase: WI-1101
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/components/play-of-the-day-client.tsx
  - web/src/app/play-of-the-day/page.tsx
autonomous: true
requirements:
  - WI-1101-UI-01

must_haves:
  truths:
    - "User can activate the Near-Miss Settled metric as a visible interactive control"
    - "Activating the Settled metric moves the viewport to the POTD play log/history section"
    - "Anchor navigation works on desktop and mobile"
    - "Keyboard users can focus and activate the Settled metric control"
  artifacts:
    - path: "web/src/components/play-of-the-day-client.tsx"
      provides: "Interactive Settled metric affordance and in-page anchor link"
      contains: "Near-Miss Tracking"
    - path: "web/src/app/play-of-the-day/page.tsx"
      provides: "Anchor target wiring only if required by component structure"
      contains: "play-of-the-day"
  key_links:
    - from: "web/src/components/play-of-the-day-client.tsx"
      to: "web/src/components/play-of-the-day-client.tsx"
      via: "Settled metric href points to history-section anchor when target id is defined in component tree"
      pattern: "href=\"#potd-play-log\".*id=\"potd-play-log\"|id=\"potd-play-log\".*href=\"#potd-play-log\""
    - from: "web/src/components/play-of-the-day-client.tsx"
      to: "web/src/app/play-of-the-day/page.tsx"
      via: "Settled metric href remains #potd-play-log while page-level wrapper owns the anchor id"
      pattern: "href=\"#potd-play-log\""
---

<objective>
Add a direct in-page navigation path from Near-Miss Tracking Settled count to the POTD play log/history section.

Purpose: make settled near-miss totals immediately explorable without scrolling or hunting for the history panel.
Output: accessible Settled metric link, stable play-log anchor target, and regression-safe verification.
</objective>

<execution_context>
@./.claude/process-acceleration-executors/workflows/execute-plan.md
@./.claude/process-acceleration-executors/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@WORK_QUEUE/WI-1101.md
@web/src/components/play-of-the-day-client.tsx
@web/src/app/play-of-the-day/page.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire Settled metric to a stable play-log anchor target</name>
  <files>web/src/components/play-of-the-day-client.tsx
web/src/app/play-of-the-day/page.tsx</files>
  <behavior>
    - Test 1: Near-Miss Settled metric renders as a visible interactive link/button-style control.
    - Test 2: Clicking Settled navigates to the play log/history section via hash anchor.
    - Test 3: Tab then Enter on Settled triggers the same navigation.
  </behavior>
  <action>
In play-of-the-day client UI, convert the Near-Miss Settled metric block into an anchor-style control that links to a stable in-page target id (preferred: #potd-play-log).

Ensure the history/log section has that stable id and preserve existing layout/styling with only minimal hover/focus-visible affordances for accessibility.

If the target section is owned in page.tsx, add minimal id wiring there only; do not change settlement logic, APIs, or data contracts.
  </action>
  <verify>
    <automated>node web/src/__tests__/ui-potd-smoke.test.js</automated>
  </verify>
  <done>
Settled metric is clearly interactive and deep-links to the play log/history region with keyboard support and no layout redesign.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run API/UI regression checks and lock acceptance behavior</name>
  <files>web/src/components/play-of-the-day-client.tsx</files>
  <action>
Execute POTD UI and API regression suites listed in the work item, and confirm no regression to existing page behavior or API contracts.

If tests expose pre-existing unrelated failures, document them and keep WI-1101 changes scoped to the anchor-link behavior.
  </action>
  <verify>
    <automated>node web/src/__tests__/ui-potd-smoke.test.js && node web/src/__tests__/api-potd.test.js</automated>
  </verify>
  <done>
Both regression commands pass or any unrelated pre-existing failure is explicitly identified with WI-1101 scope preserved.
  </done>
</task>

</tasks>

<verification>
Run:
- node web/src/__tests__/ui-potd-smoke.test.js
- node web/src/__tests__/api-potd.test.js

Manual check:
- Open /play-of-the-day
- Activate Near-Miss Settled metric with click and keyboard (Tab + Enter)
- Confirm viewport jumps to the play log/history section
</verification>

<success_criteria>
- Near-Miss Settled metric has explicit interactive affordance.
- Settled interaction navigates to play log/history via stable anchor.
- Keyboard navigation parity is preserved.
- Desktop and mobile navigation behavior is correct.
- No API contract or settlement logic changes are introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/WI-1101/WI-1101-SUMMARY.md`
</output>

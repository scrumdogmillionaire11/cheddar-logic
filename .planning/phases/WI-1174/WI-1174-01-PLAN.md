---
phase: WI-1174
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/lib/game-card/transform/legacy-repair.ts
  - web/src/lib/game-card/transform/adapters/v1-legacy-repair.ts
  - web/src/lib/game-card/transform/index.ts
  - web/src/lib/game-card/transform/market-normalize.ts
  - web/src/lib/games/route-handler.ts
  - web/src/__tests__/game-card-transform-adapters.test.js
  - docs/codebase-techdebt.md
autonomous: true
requirements:
  - WI-1174-AUDIT-01
  - WI-1174-LEGACY-01
  - WI-1174-REG-01
  - WI-1174-DEBT-01
must_haves:
  truths:
    - "Every legacy repair branch in scope is explicitly classified as required-now, safe-to-remove-now, or evidence-inconclusive with follow-on required"
    - "Any branch removed from legacy repair paths is backed by fixture or payload evidence and does not regress active web read-path transforms"
    - "Web read-path wiring still routes historical payload rows through required compatibility logic after cleanup"
    - "Tech debt docs describe only the remaining compatibility burden and list next-step removal/migration evidence for inconclusive branches"
  artifacts:
    - path: "web/src/lib/game-card/transform/legacy-repair.ts"
      provides: "Legacy branch classification outcomes applied to canonical repair layer"
    - path: "web/src/lib/game-card/transform/adapters/v1-legacy-repair.ts"
      provides: "V1 adapter parity after branch cleanup"
    - path: "web/src/__tests__/game-card-transform-adapters.test.js"
      provides: "Regression coverage proving active payload transforms still succeed"
    - path: "docs/codebase-techdebt.md"
      provides: "Post-audit dead/legacy debt register with retained vs removable branch evidence"
  key_links:
    - from: "web/src/lib/games/route-handler.ts"
      to: "web/src/lib/game-card/transform/index.ts"
      via: "read-path transform invocation remains stable after cleanup"
      pattern: "transform|normalize|repair"
    - from: "web/src/lib/game-card/transform/index.ts"
      to: "web/src/lib/game-card/transform/legacy-repair.ts"
      via: "legacy repair entrypoint wiring for historical rows"
      pattern: "legacy|repair"
    - from: "web/src/lib/game-card/transform/legacy-repair.ts"
      to: "web/src/__tests__/game-card-transform-adapters.test.js"
      via: "fixtures assert required branches still pass and removed branches stay removed"
      pattern: "legacy|repair|historical"
---

<objective>
Audit and harden legacy web read-path repair code by converting implied assumptions into an evidence-backed branch classification, then remove or quarantine only proven-dead branches.

Purpose: Prevent both false deletions (breaking historical payload rendering) and lingering dead code debt in active transform paths.
Output: Classified repair surface, evidence-backed removals/quarantines, regression coverage, and updated debt ledger.
</objective>

<execution_context>
@/Users/ajcolubiale/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ajcolubiale/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@WORK_QUEUE/WI-1174.md
@web/src/lib/game-card/transform/legacy-repair.ts
@web/src/lib/game-card/transform/adapters/v1-legacy-repair.ts
@web/src/lib/game-card/transform/index.ts
@web/src/lib/game-card/transform/market-normalize.ts
@web/src/lib/games/route-handler.ts
@web/src/__tests__/game-card-transform-adapters.test.js
@docs/codebase-techdebt.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build adversarial evidence matrix for each legacy repair branch and lock it with tests</name>
  <files>web/src/lib/game-card/transform/legacy-repair.ts, web/src/lib/game-card/transform/adapters/v1-legacy-repair.ts, web/src/__tests__/game-card-transform-adapters.test.js</files>
  <behavior>
    - Every scoped legacy branch maps to one of three states: required_now, safe_to_remove_now, or evidence_inconclusive.
    - Tests cover representative historical payload shapes that prove required_now branches still protect real rows.
    - Branches marked safe_to_remove_now have tests that continue to pass without those branches.
  </behavior>
  <action>Enumerate each conditional repair branch in `legacy-repair.ts` and `v1-legacy-repair.ts`, map each to fixture-backed evidence, and add/adjust tests so the classification is executable rather than doc-only. For ambiguous cases, add explicit TODO/marker comments and test notes that force follow-on evidence before deletion.</action>
  <verify>
    <automated>node web/src/__tests__/game-card-transform-adapters.test.js</automated>
  </verify>
  <done>Classification exists for every legacy branch in scope and regression tests fail if required compatibility behavior is removed accidentally.</done>
</task>

<task type="auto">
  <name>Task 2: Remove or quarantine only proven-dead legacy repair paths while preserving read-path wiring</name>
  <files>web/src/lib/game-card/transform/legacy-repair.ts, web/src/lib/game-card/transform/adapters/v1-legacy-repair.ts, web/src/lib/game-card/transform/index.ts, web/src/lib/game-card/transform/market-normalize.ts, web/src/lib/games/route-handler.ts</files>
  <action>Apply branch-level cleanup only for entries classified safe_to_remove_now. If deletion risk remains non-trivial, quarantine behind explicit compatibility guard with a removal condition. Keep transform call-chain contracts intact from route handler through index/normalize/repair modules, and do not broaden scope into unrelated refactors.</action>
  <verify>
    <automated>npm --prefix web run build</automated>
  </verify>
  <done>Dead legacy branches are removed or quarantined with explicit conditions, and web read-path transform compilation and routing integrity remain green.</done>
</task>

<task type="auto">
  <name>Task 3: Update tech-debt register with post-audit status and follow-on evidence gates</name>
  <files>docs/codebase-techdebt.md, web/src/__tests__/game-card-transform-adapters.test.js</files>
  <action>Rewrite the legacy-repair debt section so each branch family has current status (retained-required, removed, or evidence-inconclusive), reason, and next evidence gate. Include references to the regression tests that protect retained behavior and capture follow-on WI hooks for inconclusive paths.</action>
  <verify>
    <automated>node web/src/__tests__/game-card-transform-adapters.test.js && npm --prefix web run build</automated>
  </verify>
  <done>`docs/codebase-techdebt.md` reflects post-audit reality and no longer treats the entire legacy layer as uniformly dead.</done>
</task>

</tasks>

<verification>
Run all WI-required checks from repo root:
- `node web/src/__tests__/game-card-transform-adapters.test.js`
- `npm --prefix web run build`

Manual validation:
- Exercise representative historical payload fixtures through the web read path and confirm transformed cards still render when required-now branches are retained.
- Confirm removed/quarantined branches are listed in debt docs with explicit evidence and follow-on gate.
</verification>

<success_criteria>
- All scoped legacy branches are classified with evidence and no unclassified branch remains.
- Only proven-dead branches are removed/quarantined; active compatibility behavior remains intact.
- Regression tests and web build pass after cleanup.
- Debt register reflects post-audit truth and explicit next-step evidence requirements.
</success_criteria>

<output>
After completion, create `.planning/phases/WI-1174/WI-1174-01-SUMMARY.md`.
</output>

---
phase: 03-nhl-1p-settlement-wl
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/worker/src/jobs/settle_game_results.js
  - apps/worker/src/jobs/settle_pending_cards.js
  - apps/worker/src/jobs/report_settlement_health.js
  - apps/worker/src/__tests__/settlement-pipeline-integration.test.js
  - apps/worker/src/__tests__/settle-coverage.test.js
  - apps/worker/src/__tests__/settlement-health-report.test.js
autonomous: true
must_haves:
  truths:
    - "NHL 1P cards with final+displayed eligibility settle to WON/LOST/PUSH instead of remaining pending/error due to missing period context."
    - "Settlement reporting exposes a dedicated NHL 1P coverage signal that clearly separates blocked rows from successfully settled rows."
    - "When first-period data is unavailable, settlement rows fail with deterministic, queryable reason codes rather than ambiguous failures."
  artifacts:
    - path: "apps/worker/src/jobs/settle_game_results.js"
      provides: "Reliable first-period metadata persistence in game_results metadata"
    - path: "apps/worker/src/jobs/settle_pending_cards.js"
      provides: "Deterministic 1P grading and explicit failure reason coding"
    - path: "apps/worker/src/jobs/report_settlement_health.js"
      provides: "NHL 1P settlement coverage metrics in settlement health output"
    - path: "apps/worker/src/__tests__/settlement-pipeline-integration.test.js"
      provides: "Integration regression for NHL 1P settle success and failure paths"
  key_links:
    - from: "apps/worker/src/jobs/settle_game_results.js"
      to: "apps/worker/src/jobs/settle_pending_cards.js"
      via: "game_results.metadata.firstPeriodScores + firstPeriodVerification"
      pattern: "firstPeriodScores|firstPeriodVerification"
    - from: "apps/worker/src/jobs/settle_pending_cards.js"
      to: "apps/worker/src/jobs/report_settlement_health.js"
      via: "settlement_error.code and market bucket counters"
      pattern: "MISSING_PERIOD_SCORE|NHL_1P_TOTAL"
---

<objective>
Stabilize NHL 1P settlement as a production-safe, measurable path.

Purpose: Convert current “not successfully settling 1P plays” uncertainty into deterministic grading behavior with clear diagnostics.
Output: Updated settlement jobs + tests that prove 1P rows either settle correctly or fail with explicit, actionable reason codes.
</objective>

<execution_context>
@./.claude/process-acceleration-executors/workflows/execute-plan.md
@./.claude/process-acceleration-executors/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@WORK_QUEUE/COMPLETE/WI-0506.md
@WORK_QUEUE/COMPLETE/WI-0509.md
@apps/worker/src/jobs/settle_game_results.js
@apps/worker/src/jobs/settle_pending_cards.js
@apps/worker/src/jobs/report_settlement_health.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden first-period result persistence contract</name>
  <files>apps/worker/src/jobs/settle_game_results.js, apps/worker/src/__tests__/settlement-pipeline-integration.test.js</files>
  <action>Normalize and persist first-period metadata (`firstPeriodScores`, verification completeness, and orientation-safe home/away mapping) in every NHL match branch that can produce final scores. Keep NHL API-first + ESPN fallback behavior; do not change non-NHL settlement behavior. Add/adjust tests to prove first-period metadata survives all matching paths used by settle-cards.</action>
  <verify>npm --prefix apps/worker test -- src/jobs/__tests__/settle_game_results.matching.test.js src/__tests__/settlement-pipeline-integration.test.js</verify>
  <done>All NHL matches used for 1P grading persist consistent first-period metadata, and tests fail if metadata is missing in any supported match path.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce deterministic NHL 1P grading + failure taxonomy</name>
  <files>apps/worker/src/jobs/settle_pending_cards.js, apps/worker/src/__tests__/settle-coverage.test.js, apps/worker/src/__tests__/settlement-pipeline-integration.test.js</files>
  <action>Ensure NHL 1P rows route through period-aware grading with strict preconditions and deterministic outcomes. When grading cannot proceed, emit explicit settlement_error codes (for example: `MISSING_PERIOD_SCORE`, missing line, missing market key) with metadata sufficient for SQL triage. Do not silently skip eligible rows and do not downgrade valid 1P rows into generic market buckets.</action>
  <verify>npm --prefix apps/worker test -- src/__tests__/settle-coverage.test.js src/__tests__/settlement-pipeline-integration.test.js</verify>
  <done>NHL 1P rows either settle to terminal outcomes or produce explicit actionable error codes; no ambiguous 1P failures remain in tests.</done>
</task>

<task type="auto">
  <name>Task 3: Add 1P-specific settlement health coverage reporting</name>
  <files>apps/worker/src/jobs/report_settlement_health.js, apps/worker/src/__tests__/settlement-health-report.test.js</files>
  <action>Add a dedicated NHL 1P coverage block to settlement health report output, including pending eligible, settled, blocked-by-no-final, blocked-by-no-display, and top failure reason codes. Preserve existing report JSON/text format compatibility for consumers by adding fields (not renaming/removing existing ones).</action>
  <verify>npm --prefix apps/worker test -- src/__tests__/settlement-health-report.test.js && npm --prefix apps/worker run job:settlement-report -- --json --days=7 --limit=10</verify>
  <done>Settlement health report exposes NHL 1P coverage and blocker counts needed to decide go/no-go for W/L tracking.</done>
</task>

</tasks>

<verification>
- Run targeted settlement test suite for game-result matching, market-contract grading, and integration.
- Run settlement health report and confirm NHL 1P-specific metrics are present and populated.
</verification>

<success_criteria>
- NHL 1P settlement path is deterministic under test (success + failure branches).
- 1P failures are diagnosable by reason code without log scraping.
- Operators can measure 1P W/L readiness from settlement report output alone.
</success_criteria>

<output>
After completion, create `.planning/phases/03-nhl-1p-settlement-wl/03-01-SUMMARY.md`
</output>

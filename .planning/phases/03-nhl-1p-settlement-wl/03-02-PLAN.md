---
phase: 03-nhl-1p-settlement-wl
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - scripts/settlement-health-check.sh
  - docs/DATA_PIPELINE_TROUBLESHOOTING.md
  - WORK_QUEUE/WI-0510.md
autonomous: false
must_haves:
  truths:
    - "Operators can run a single repeatable check to see NHL 1P pending/settled/error counts and blocker reasons before/after settlement runs."
    - "A documented run sequence exists to safely execute settle-games then settle-cards and verify NHL 1P W/L movement."
    - "Go/no-go criteria for tracking NHL 1P win/loss record are explicit and measurable."
  artifacts:
    - path: "scripts/settlement-health-check.sh"
      provides: "CLI diagnostics including NHL 1P-specific coverage and blocker checks"
    - path: "docs/DATA_PIPELINE_TROUBLESHOOTING.md"
      provides: "Runbook for NHL 1P settlement activation and W/L verification"
    - path: "WORK_QUEUE/WI-0510.md"
      provides: "Execution traceability for this rollout plan"
  key_links:
    - from: "scripts/settlement-health-check.sh"
      to: "apps/worker/src/jobs/report_settlement_health.js"
      via: "aligned coverage metrics and reason-code naming"
      pattern: "NHL_1P_TOTAL|MISSING_PERIOD_SCORE"
    - from: "docs/DATA_PIPELINE_TROUBLESHOOTING.md"
      to: "apps/worker/src/jobs/settle_game_results.js"
      via: "ordered run steps and verification SQL"
      pattern: "job:settle-games|job:settle-cards"
---

<objective>
Operationalize NHL 1P settlement into a repeatable W/L-record workflow.

Purpose: Move from “logic exists” to “runbook-proven and measurable” so 1P outcomes can be trusted for record keeping.
Output: Updated health-check + troubleshooting runbook + an explicit acceptance gate for starting 1P W/L tracking.
</objective>

<execution_context>
@./.claude/process-acceleration-executors/workflows/execute-plan.md
@./.claude/process-acceleration-executors/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/DATA_PIPELINE_TROUBLESHOOTING.md
@scripts/settlement-health-check.sh
@WORK_QUEUE/WI-0510.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add NHL 1P readiness checks to settlement health script</name>
  <files>scripts/settlement-health-check.sh</files>
  <action>Extend the health check script with NHL 1P-specific diagnostics: total 1P payload candidates, pending final+displayed rows, settled rows in lookback, and top 1P settlement_error codes. Keep script read-only and production-safe, with strict-mode failure when actionable 1P blockers exceed threshold.</action>
  <verify>scripts/settlement-health-check.sh --db "${CHEDDAR_DB_PATH:-packages/data/cheddar.db}" --grace-hours 6</verify>
  <done>Health check output includes explicit NHL 1P readiness section and can fail-fast on critical unresolved 1P blockers.</done>
</task>

<task type="auto">
  <name>Task 2: Document run order + go/no-go for NHL 1P W/L tracking</name>
  <files>docs/DATA_PIPELINE_TROUBLESHOOTING.md, WORK_QUEUE/WI-0510.md</files>
  <action>Add a focused runbook section that defines: preflight checks, required job order (`job:settle-games` then `job:settle-cards`), post-run SQL verification, and go/no-go thresholds to start counting NHL 1P W/L. Keep scope strictly operational/documentation; no new product features.</action>
  <verify>grep -n "NHL 1P" docs/DATA_PIPELINE_TROUBLESHOOTING.md && grep -n "W/L" docs/DATA_PIPELINE_TROUBLESHOOTING.md</verify>
  <done>Operators have an unambiguous checklist and thresholds for deciding when NHL 1P W/L tracking can start.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>NHL 1P readiness diagnostics + runbook with go/no-go criteria</what-built>
  <how-to-verify>
    1) Run `npm --prefix apps/worker run job:settle-games` on target environment.
    2) Run `npm --prefix apps/worker run job:settle-cards` immediately after.
    3) Run `scripts/settlement-health-check.sh --db "$CHEDDAR_DB_PATH" --strict`.
    4) Confirm NHL 1P section shows settled movement and no critical blockers above threshold.
    5) Confirm `/api/results?sport=NHL` segment `nhl_first_period_totals` reflects non-zero settled rows in lookback window.
  </how-to-verify>
  <resume-signal>Type "approved" if go/no-go criteria pass, or return blocker metrics for follow-up planning.</resume-signal>
</task>

</tasks>

<verification>
- Health-check script emits NHL 1P-specific diagnostics in both normal and strict mode.
- Troubleshooting docs provide exact commands and acceptance thresholds for W/L rollout.
</verification>

<success_criteria>
- Team can run one deterministic sequence to settle NHL 1P and verify outcomes.
- Go/no-go criteria for NHL 1P W/L tracking are objective and documented.
- Human verification confirms readiness or returns exact blocker counts for next gap-closure cycle.
</success_criteria>

<output>
After completion, create `.planning/phases/03-nhl-1p-settlement-wl/03-02-SUMMARY.md`
</output>

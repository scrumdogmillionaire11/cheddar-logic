---
phase: WI-0911-nhl-player-blocks-settlement-policy
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/worker/src/jobs/settle_projections.js
  - apps/worker/src/jobs/settle_pending_cards.js
  - apps/worker/src/jobs/run_nhl_player_shots_model.js
  - apps/worker/src/jobs/__tests__/settle_projections.test.js
  - apps/worker/src/jobs/__tests__/run_nhl_player_shots_model.test.js
autonomous: true
requirements:
  - BLK-SETTLE-01
  - BLK-SETTLE-02
  - BLK-SETTLE-03
must_haves:
  truths:
    - "A card with market nhl-player-blk never enters grading logic that expects line/selection/value contracts from other player-prop markets"
    - "Unsupported nhl-player-blk settlement rows close deterministically with explicit market-specific reason metadata"
    - "Projection and card settlement tests document and enforce the projection-audit-only policy for nhl-player-blk"
  artifacts:
    - path: "apps/worker/src/jobs/settle_projections.js"
      provides: "market-specific nhl-player-blk policy branch and explicit closeout metadata"
      contains: "nhl-player-blk"
    - path: "apps/worker/src/jobs/settle_pending_cards.js"
      provides: "card_results closeout policy for unsupported nhl-player-blk grading"
      contains: "nhl-player-blk"
    - path: "apps/worker/src/jobs/__tests__/settle_projections.test.js"
      provides: "regression tests proving nhl-player-blk projection-only closeout behavior"
      contains: "nhl-player-blk"
  key_links:
    - from: "apps/worker/src/jobs/run_nhl_player_shots_model.js"
      to: "apps/worker/src/jobs/settle_projections.js"
      via: "market metadata emitted by model output and consumed by settlement"
      pattern: "nhl-player-blk|market"
    - from: "apps/worker/src/jobs/settle_projections.js"
      to: "apps/worker/src/jobs/settle_pending_cards.js"
      via: "consistent unsupported-market closeout semantics across projection and card settlement paths"
      pattern: "projection-only|unsupported|reason"
---

<objective>
Enforce one explicit settlement policy for NHL player blocks: projection-audit-only, no grading.

Purpose: remove ambiguity and prevent unsupported grading behavior from silently drifting across projection and card settlement paths.

Output: deterministic market-specific policy logic in settlement jobs plus regression tests that lock the behavior.
</objective>

<execution_context>
@/Users/ajcolubiale/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ajcolubiale/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@WORK_QUEUE/WI-0911.md
@apps/worker/src/jobs/settle_projections.js
@apps/worker/src/jobs/settle_pending_cards.js
@apps/worker/src/jobs/run_nhl_player_shots_model.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock market policy and metadata contract for nhl-player-blk</name>
  <files>apps/worker/src/jobs/run_nhl_player_shots_model.js
apps/worker/src/jobs/settle_projections.js</files>
  <behavior>
    - Test 1: nhl-player-blk settlement path resolves to projection-only handling and never routes to grading contract checks intended for grading-supported markets.
    - Test 2: closeout metadata includes explicit market-specific reason (not generic unsupported/unknown fallback text).
    - Test 3: existing nhl-player-shots behavior remains unchanged.
  </behavior>
  <action>
Implement a dedicated nhl-player-blk policy branch in settlement flow:
- Treat nhl-player-blk as unsupported for card_results grading in this WI.
- Emit explicit reason metadata for projection-only closeout (for example a reason code/message pair specific to nhl-player-blk).
- Keep policy readable and centralized so future grading support can be introduced via a new WI without changing unrelated markets.

If registry or output metadata in run_nhl_player_shots_model.js currently implies grading eligibility for nhl-player-blk, align it to projection-audit-only semantics.
  </action>
  <verify>
    <automated>npm --prefix apps/worker run test -- --testPathPattern="run_nhl_player_shots_model|settle_projections"</automated>
  </verify>
  <done>
nhl-player-blk policy is explicitly projection-only in settlement logic and produces market-specific closeout metadata.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce policy in card settlement and regression tests</name>
  <files>apps/worker/src/jobs/settle_pending_cards.js
apps/worker/src/jobs/__tests__/settle_projections.test.js
apps/worker/src/jobs/__tests__/run_nhl_player_shots_model.test.js</files>
  <behavior>
    - Test 1: pending card settlement auto-closes nhl-player-blk rows with explicit projection-only reason metadata.
    - Test 2: no silent fallback path attempts to grade nhl-player-blk through non-blocks market logic.
    - Test 3: tests fail if generic unsupported errors replace market-specific reason metadata.
  </behavior>
  <action>
Update settle_pending_cards.js so nhl-player-blk rows follow the same explicit projection-only closeout semantics as settle_projections.js.

Add/adjust tests in settle_projections.test.js and run_nhl_player_shots_model.test.js to verify:
- market-specific reason metadata exists,
- grading path is blocked for nhl-player-blk,
- unrelated market behavior remains intact.

Keep assertions precise on reason values/fields so future regressions are caught immediately.
  </action>
  <verify>
    <automated>npm --prefix apps/worker run test -- --testPathPattern="settle_projections|run_nhl_player_shots_model|settle_pending_cards.phase2"</automated>
  </verify>
  <done>
Projection settlement and card settlement both enforce projection-only nhl-player-blk handling with passing regression tests.
  </done>
</task>

</tasks>

<verification>
- npm --prefix apps/worker run test -- --testPathPattern="settle_projections"
- npm --prefix apps/worker run test -- --testPathPattern="run_nhl_player_shots_model"
- npm --prefix apps/worker run test -- --testPathPattern="settle_pending_cards.phase2"

Pass condition: all targeted tests pass and contain assertions that nhl-player-blk is projection-only with explicit reason metadata.
</verification>

<success_criteria>
- nhl-player-blk is explicitly marked and enforced as projection-audit-only (not grading-eligible).
- settlement rows close with market-specific reason metadata.
- regression tests fail on silent fallback or generic unsupported messaging regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/WI-0911/WI-0911-SUMMARY.md`
</output>

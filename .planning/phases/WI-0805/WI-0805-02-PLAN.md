---
phase: WI-0805-devig-game-markets
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/worker/src/jobs/run_nhl_model.js
  - apps/worker/src/jobs/run_nba_model.js
autonomous: true

must_haves:
  truths:
    - "No NHL game total, spread, or ML card payload contains VIG_REMOVAL_SKIPPED: true"
    - "No NBA game total or spread card payload contains VIG_REMOVAL_SKIPPED: true"
    - "edgeCalculator.computeEdge* calls for totals and spreads pass the opposite-side price"
  artifacts:
    - path: apps/worker/src/jobs/run_nhl_model.js
      provides: devigged edge via priceOpposite in computeEdge calls
      contains: priceOpposite
    - path: apps/worker/src/jobs/run_nba_model.js
      provides: devigged edge via priceOpposite in computeEdge calls
      contains: priceOpposite
  key_links:
    - from: apps/worker/src/jobs/run_nhl_model.js
      to: packages/models/src/edge-calculator.js
      via: edgeCalculator.computeEdge with priceOpposite
      pattern: priceOpposite
    - from: apps/worker/src/jobs/run_nba_model.js
      to: packages/models/src/edge-calculator.js
      via: edgeCalculator.computeEdge with priceOpposite
      pattern: priceOpposite
---

<objective>
Fix NHL game totals / spread / ML and NBA game totals / spreads to pass the
opposite-side price into `edgeCalculator.computeEdge*`, enabling the devig path
that already exists in edge-calculator.js but is never reached.

Purpose: Both models currently call edgeCalculator without `priceOpposite`,
silently triggering `VIG_REMOVAL_SKIPPED: true` on every card. The devig logic
is already there — it just needs to be activated by callers passing both prices.

Output: All NHL and NBA game market edge computations use two-sided devigged
implied probability; `VIG_REMOVAL_SKIPPED` disappears from payloads.
</objective>

<execution_context>
@./.claude/process-acceleration-executors/workflows/execute-plan.md
@./.claude/process-acceleration-executors/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@packages/models/src/edge-calculator.js
@apps/worker/src/jobs/run_nhl_model.js
@apps/worker/src/jobs/run_nba_model.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pass priceOpposite in all edgeCalculator calls in run_nhl_model.js</name>
  <files>apps/worker/src/jobs/run_nhl_model.js</files>
  <action>
`edge-calculator.js` `computeEdge` signature:
```js
computeEdge({ pFair, americanOdds, priceOpposite, ... })
```
When `priceOpposite` is provided the calculator hits the `removeVig` path and
`VIG_REMOVAL_SKIPPED` is NOT set.

Search `run_nhl_model.js` for all `edgeCalculator.computeEdge` (and
`computeTotalEdge`, `computeSpreadEdge`, `computeMoneylineEdge` — whatever
variants are actually called). For each:

1. Identify what the "opposite side price" is at the call site:
   - For TOTAL OVER call: `priceOpposite` = the UNDER price
   - For TOTAL UNDER call: `priceOpposite` = the OVER price
   - For SPREAD HOME call: `priceOpposite` = the AWAY spread price
   - For MONEYLINE HOME call: `priceOpposite` = the AWAY ML price

2. Add `priceOpposite: <opposite_price_variable>` to each call object.
   Only add when the opposite price is a finite number — guard with
   `Number.isFinite(oppositePrice) ? oppositePrice : undefined`.

Do not change any other logic — only add `priceOpposite` to existing call sites.
Do not modify any threshold, confidence, or sigma logic.
  </action>
  <verify>
grep -n "priceOpposite" apps/worker/src/jobs/run_nhl_model.js | wc -l
# Should be > 0 (at least one call site updated)

npm --prefix apps/worker run test -- --testPathPattern run_nhl_model --no-coverage 2>&1 | tail -10
  </verify>
  <done>All edgeCalculator.computeEdge* calls in run_nhl_model.js pass the opposite-side price; tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Pass priceOpposite in all edgeCalculator calls in run_nba_model.js</name>
  <files>apps/worker/src/jobs/run_nba_model.js</files>
  <action>
Identical pattern to Task 1, applied to `run_nba_model.js`.

Search for all `edgeCalculator.computeEdge*` call sites. For each:

1. Identify the opposite-side price in scope at that call site.
   NBA markets only have TOTAL (over/under) and SPREAD (home/away).

2. Add `priceOpposite: Number.isFinite(oppositePrice) ? oppositePrice : undefined`
   to each call object.

Do not change any threshold, spread profile, sigma, or confidence logic.
  </action>
  <verify>
grep -n "priceOpposite" apps/worker/src/jobs/run_nba_model.js | wc -l
# Should be > 0

npm --prefix apps/worker run test -- --testPathPattern run_nba_model --no-coverage 2>&1 | tail -10
  </verify>
  <done>All edgeCalculator.computeEdge* calls in run_nba_model.js pass the opposite-side price; tests pass</done>
</task>

</tasks>

<verification>
npm --prefix apps/worker run test -- --testPathPattern "run_nhl_model|run_nba_model" --no-coverage 2>&1 | tail -10

grep -c "priceOpposite" apps/worker/src/jobs/run_nhl_model.js
grep -c "priceOpposite" apps/worker/src/jobs/run_nba_model.js

Both counts should be >= 1. Tests should all pass.
</verification>

<success_criteria>
- `priceOpposite` appears in run_nhl_model.js and run_nba_model.js edgeCalculator calls
- Full NHL model test suite passes
- Full NBA model test suite passes
- No new VIG_REMOVAL_SKIPPED entries when both prices are available at call sites
</success_criteria>

<output>
After completion, create `.planning/phases/WI-0805/WI-0805-02-SUMMARY.md`
</output>
